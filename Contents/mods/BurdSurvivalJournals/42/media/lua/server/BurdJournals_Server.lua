
require "BurdJournals_Shared"

BurdJournals.debugPrint("[BurdJournals] SERVER MODULE LOADING... (require completed)")

BurdJournals = BurdJournals or {}
BurdJournals.Server = BurdJournals.Server or {}
BurdJournals.Server._entryChunkKeyCache = BurdJournals.Server._entryChunkKeyCache or {}
BurdJournals.Server._recordAllExportBaselineQueue = BurdJournals.Server._recordAllExportBaselineQueue or {}
BurdJournals.Server._cursedInsightPreviewCache = BurdJournals.Server._cursedInsightPreviewCache or {}
BurdJournals.Server._cursedInsightPreviewMisses = BurdJournals.Server._cursedInsightPreviewMisses or {}

local bsjFallbackPrint = print

local function bsjWriteLogLine(msg)
    local line = tostring(msg or "")
    -- Log gating: always emit warnings/errors; gate informational lines behind the
    -- verbose toggle (or MP perf logging) so mod-heavy production servers aren't
    -- spammed with per-claim/per-backup diagnostics.
    local upper = line:upper()
    local important = upper:find("WARN", 1, true) ~= nil
        or upper:find("ERROR", 1, true) ~= nil
        or upper:find("FATAL", 1, true) ~= nil
    if not important then
        local verbose = BurdJournals and BurdJournals.shouldDebugLog and BurdJournals.shouldDebugLog()
        local mpPerf = BurdJournals and BurdJournals.shouldLogMPPerf and BurdJournals.shouldLogMPPerf()
        if not verbose and not mpPerf then
            return
        end
    end
    if BurdJournals and BurdJournals.writeLogLine then
        BurdJournals.writeLogLine(line)
    elseif bsjFallbackPrint then
        bsjFallbackPrint(line)
    end
end

local function bsjFormatEntryStoreCounts(counts)
    if type(counts) ~= "table" then
        return "nil"
    end
    return "skills=" .. tostring(tonumber(counts.skills) or 0)
        .. ",traits=" .. tostring(tonumber(counts.traits) or 0)
        .. ",stats=" .. tostring(tonumber(counts.stats) or 0)
        .. ",recipes=" .. tostring(tonumber(counts.recipes) or 0)
end

BurdJournals.Server._rateLimitCache = {}

local COMMAND_RATE_LIMIT_MS = {
    recordProgress = 25,
    claimSkill = 25,
    claimTrait = 25,
    claimForgetSlot = 25,
    claimRecipe = 25,
    claimStat = 25,
    absorbSkill = 25,
    absorbTrait = 25,
    absorbRecipe = 25,
    requestJournalEntryChunk = 75,
    requestCursedInsightPreview = 250,
    sanitizeJournal = 500,
    requestXpSync = 500,
    saveJournalNotes = 500,
    requestBaseline = 750,
    registerBaseline = 750,
    batchClaimRewards = 1000,
    batchAbsorbRewards = 1000,
}

-- Keep this list aligned with every branch in onClientCommand. Rejecting an
-- invented command before response scopes, compaction, logging, and rate-limit
-- bookkeeping prevents arbitrary client strings from creating server work or
-- growing transient command caches.
BurdJournals.Server.RECOGNIZED_CLIENT_COMMANDS = {
    logSkills = true, learnSkills = true, openCursedJournal = true,
    requestCursedInsightPreview = true, openYuletideJournal = true,
    breakJournalSeal = true, yuletideGiftMaterialized = true,
    absorbSkill = true, absorbTrait = true, absorbRecipe = true,
    claimSkill = true, claimTrait = true, claimForgetSlot = true,
    claimRecipe = true, claimStat = true, eraseJournal = true,
    cleanBloody = true, convertToClean = true, initializeJournal = true,
    recordProgress = true, syncJournalData = true,
    requestJournalEntryChunk = true, eraseEntry = true,
    registerBaseline = true, requestBaseline = true,
    requestRespawnJournalProvision = true, trackVhsSkillXP = true,
    deleteBaseline = true, dissolveJournal = true, sanitizeJournal = true,
    saveJournalNotes = true, clearAllBaselines = true, renameJournal = true,
    removeCraftDuplicates = true, requestXpSync = true,
    batchClaimRewards = true, batchAbsorbRewards = true,
    saveDebugJournalBackup = true, requestDebugJournalBackup = true,
    setVerboseLogging = true,
    debugSetSkill = true, debugSetAllSkills = true, debugAddXP = true,
    debugSetSkillToLevel = true, debugSetSkillXP = true,
    debugAddSkillXP = true, debugAddTrait = true, debugRemoveTrait = true,
    debugAddRecipe = true, debugRemoveRecipe = true, debugBulkRecipes = true,
    debugRemoveAllTraits = true, debugBulkTraits = true,
    debugClearBaseline = true, debugRecalcBaseline = true,
    debugUpdateSkillBaseline = true, debugUpdateTraitBaseline = true,
    debugSaveBaselineDraft = true, debugListBaselineCache = true,
    debugListBaselineSnapshots = true, debugGetBaselineSnapshot = true,
    debugGetTargetBaselinePayload = true, debugRequestCharacterData = true,
    debugSaveBaselineSnapshot = true, debugApplyBaselineSnapshot = true,
    debugDeleteBaselineSnapshot = true, debugRequestAdminPolicy = true,
    debugSetAdminPolicy = true, debugBulkSetAdminPolicy = true,
    debugResetAdminPolicy = true, debugRequestLoreTemplateOptions = true,
    debugSpawnJournal = true, debugDissolveJournal = true,
    debugJournalCreated = true, debugApplyJournalEdits = true,
    debugMigrateOnlineJournals = true, debugLookupJournalByUUID = true,
    debugRepairJournalByUUID = true, debugListJournalUUIDIndex = true,
    debugDeleteJournalByUUID = true, debugExportJournalJSON = true,
    debugImportJournalJSON = true,
}

function BurdJournals.Server.cleanupRateLimitCache()
    local now = getTimestampMs and getTimestampMs() or 0
    local staleThreshold = 60000
    for playerId, timestamp in pairs(BurdJournals.Server._rateLimitCache) do
        if now - timestamp > staleThreshold then
            BurdJournals.Server._rateLimitCache[playerId] = nil
        end
    end
end

function BurdJournals.Server.cleanupTransientCommandCaches(nowMs)
    nowMs = tonumber(nowMs) or ((getTimestampMs and getTimestampMs()) or ((os.time() or 0) * 1000))
    local recordAllTTL = 600000
    for key, pending in pairs(BurdJournals.Server._recordAllExportBaselineQueue) do
        local updatedAt = type(pending) == "table" and tonumber(pending._updatedAt) or 0
        if updatedAt <= 0 or (nowMs - updatedAt) >= recordAllTTL then
            BurdJournals.Server._recordAllExportBaselineQueue[key] = nil
            bsjWriteLogLine("[BurdJournals] WARNING: discarded abandoned record-all baseline staging key=" .. tostring(key))
        end
    end
    local preambleCache = BurdJournals.Server._lastPreambleCompactAt
    if type(preambleCache) == "table" then
        for key, lastAt in pairs(preambleCache) do
            if (nowMs - (tonumber(lastAt) or 0)) >= 600000 then
                preambleCache[key] = nil
            end
        end
    end
end

function BurdJournals.Server.deepCopy(orig, copies)
    copies = copies or {}
    local origType = type(orig)
    local copy

    if origType == 'table' then

        if copies[orig] then
            copy = copies[orig]
        else
            copy = {}
            copies[orig] = copy
            for origKey, origValue in pairs(orig) do

                local keyCopy = BurdJournals.Server.deepCopy(origKey, copies)
                local valueCopy = BurdJournals.Server.deepCopy(origValue, copies)
                copy[keyCopy] = valueCopy
            end

        end
    else

        copy = orig
    end
    return copy
end

-- Safe wrapper for shouldDissolve that re-fetches the journal by ID to avoid zombie object errors
-- This prevents "Object tried to call nil" crashes when the journal becomes invalid during processing
function BurdJournals.Server.safeShouldDissolve(player, journalId)
    if not player or not journalId then return false end

    -- Re-fetch the journal by ID to get a fresh reference
    local freshJournal = BurdJournals.Server.resolvePostActionJournal(player, journalId, nil)
    if not freshJournal then
        -- Journal no longer exists - treat as dissolved
        return false
    end

    -- Validate the item is still valid (not a zombie object) before calling shouldDissolve
    -- isValidItem uses instanceof which doesn't trigger error logging
    if not BurdJournals.isValidItem(freshJournal) then
        BurdJournals.debugPrint("[BurdJournals] safeShouldDissolve: Item is invalid/zombie, skipping dissolution check")
        return false
    end

    -- Call shouldDissolve with the validated reference
    if BurdJournals.shouldDissolve then
        return BurdJournals.shouldDissolve(freshJournal, player)
    end
    return false
end

-- Post-action claim checks are possession-only. Re-resolve from the requester's
-- inventory so a stale cached reference or nearby world item can never be mutated.
function BurdJournals.Server.resolvePostActionJournal(player, journalId, cachedJournal)
    if not player or journalId == nil then return nil end
    if BurdJournals.Server.findJournalInRequestersInventory then
        local journal, inventorySearched = BurdJournals.Server.findJournalInRequestersInventory(player, journalId, nil, nil)
        if inventorySearched then return journal end
    end
    local inventory = player.getInventory and player:getInventory() or nil
    if inventory and BurdJournals.findItemByIdInContainer then
        return BurdJournals.findItemByIdInContainer(inventory, journalId)
    end
    return nil
end

function BurdJournals.Server.copyJournalData(journal)
    if not journal then return nil end
    local modData = journal:getModData()
    if not modData or not modData.BurdJournals then return nil end

    return BurdJournals.Server.deepCopy(modData.BurdJournals)
end

local function isStrictMPServer()
    return BurdJournals.isStrictMPServerContext and BurdJournals.isStrictMPServerContext()
end

local function estimatePayloadBytes(value, seen, depth)
    local valueType = type(value)
    if valueType == "nil" then
        return 4
    elseif valueType == "boolean" then
        return value and 4 or 5
    elseif valueType == "number" then
        return string.len(tostring(value))
    elseif valueType == "string" then
        return string.len(value)
    elseif valueType == "table" then
        seen = seen or {}
        if seen[value] then
            return 0
        end
        seen[value] = true
        depth = (depth or 0) + 1
        if depth > 32 then
            return 0
        end
        local bytes = 2
        for key, entryValue in pairs(value) do
            bytes = bytes + estimatePayloadBytes(key, seen, depth)
            bytes = bytes + estimatePayloadBytes(entryValue, seen, depth)
            bytes = bytes + 2
        end
        return bytes
    end
    return string.len(tostring(value))
end

function BurdJournals.Server.countJournalPayloadEntries(journalData)
    if type(journalData) ~= "table" then
        return 0
    end
    local count = 0
    local buckets = {"skills", "traits", "stats", "recipes"}
    for _, bucketName in ipairs(buckets) do
        local bucket = journalData[bucketName]
        if type(bucket) == "table" then
            for _ in pairs(bucket) do
                count = count + 1
            end
        end
    end
    local storedCount = 0
    if type(journalData.entryStoreEntryCounts) == "table" then
        for _, bucketName in ipairs(buckets) do
            storedCount = storedCount + math.max(0, tonumber(journalData.entryStoreEntryCounts[bucketName]) or 0)
        end
    end
    if storedCount > 0 then
        if count <= 0 then
            return storedCount
        end
        if journalData.entryStoreEnabled == true and count < storedCount then
            return storedCount + count
        end
    end
    return count
end

function BurdJournals.Server.countResidentJournalPayloadEntries(journalData)
    if type(journalData) ~= "table" then return 0 end
    local count = 0
    for _, bucketName in ipairs(BurdJournals.ENTRY_STORE_BUCKETS or {"skills", "traits", "stats", "recipes"}) do
        local bucket = journalData[bucketName]
        if type(bucket) == "table" then for _ in pairs(bucket) do count = count + 1 end end
    end
    return count
end

function BurdJournals.Server.getJournalEntryFileBackupName(uuid)
    local s = tostring(uuid or "")
    if s == "" then
        return nil
    end
    local cleaned = string.gsub(s, "[^%w_%-]", "_")
    if string.len(cleaned) > 64 then
        cleaned = string.sub(cleaned, 1, 64)
    end
    return "BurdJournals_JournalEntryBackup_" .. cleaned .. ".json"
end

function BurdJournals.Server.buildJournalDataFromEntryStoreEntry(uuid, entry)
    if type(entry) ~= "table" then
        return nil
    end
    local journalData = {
        uuid = uuid,
        entryStoreEnabled = true,
        entryStoreUUID = uuid,
        entryStoreEntryCounts = {},
    }
    for _, bucketName in ipairs(BurdJournals.ENTRY_STORE_BUCKETS or {"skills", "traits", "stats", "recipes"}) do
        local bucket = type(entry[bucketName]) == "table" and entry[bucketName] or {}
        journalData[bucketName] = BurdJournals.Server.deepCopy and BurdJournals.Server.deepCopy(bucket) or bucket
        journalData.entryStoreEntryCounts[bucketName] = BurdJournals.Server.countJournalPayloadEntries({[bucketName] = bucket})
    end
    journalData.entryStoreUpdatedAt = entry.updatedAt
    return journalData
end

function BurdJournals.Server.restoreJournalEntryStoreFromPayload(uuid, journalData, sourceTag)
    if type(uuid) ~= "string" or uuid == "" or type(journalData) ~= "table"
        or BurdJournals.Server.countResidentJournalPayloadEntries(journalData) <= 0
        or not BurdJournals.getOrCreateJournalEntryStoreEntryForData then return false end
    local identity = {uuid = uuid, entryStoreUUID = uuid, entryStoreEnabled = true}
    local entry, shardKey = BurdJournals.getOrCreateJournalEntryStoreEntryForData(identity, true, nil,
        sourceTag or "entryStoreRecovery")
    if type(entry) ~= "table" or type(shardKey) ~= "string" or shardKey == "" then return false end
    for _, bucketName in ipairs(BurdJournals.ENTRY_STORE_BUCKETS or {"skills", "traits", "stats", "recipes"}) do
        local sourceBucket = journalData[bucketName]
        local targetBucket = type(entry[bucketName]) == "table" and entry[bucketName] or {}
        if type(sourceBucket) == "table" then
            for key, value in pairs(sourceBucket) do
                targetBucket[key] = BurdJournals.normalizeEntryBucketValue
                    and BurdJournals.normalizeEntryBucketValue(bucketName, value)
                    or BurdJournals.Server.deepCopy(value)
            end
        end
        entry[bucketName] = targetBucket
    end
    entry.updatedAt = math.max(tonumber(entry.updatedAt) or 0, tonumber(journalData.entryStoreUpdatedAt) or 0,
        getTimestampMs and getTimestampMs() or 0)
    local shard = BurdJournals._getJournalEntryStoreShard and BurdJournals._getJournalEntryStoreShard(shardKey, false) or nil
    if type(shard) ~= "table" or not BurdJournals.persistJournalEntryStoreShard
        or BurdJournals.persistJournalEntryStoreShard(shardKey, shard) ~= true then return false end
    bsjWriteLogLine("[BurdJournals][EntryStoreFileBackup] restored authoritative entry store"
        .. " uuid=" .. tostring(uuid) .. " source=" .. tostring(sourceTag or "entryStoreRecovery")
        .. " entries=" .. tostring(BurdJournals.Server.countResidentJournalPayloadEntries(journalData)))
    return true
end

function BurdJournals.Server.writeJournalEntryFileBackup(uuid, entry, sourceTag)
    if not (getFileWriter and BurdJournals.encodeJournalExportJSON) then
        return false
    end
    local filename = BurdJournals.Server.getJournalEntryFileBackupName(uuid)
    if not filename then
        return false
    end
    local journalData = BurdJournals.Server.buildJournalDataFromEntryStoreEntry(uuid, entry)
    if type(journalData) ~= "table" or BurdJournals.Server.countJournalPayloadEntries(journalData) <= 0 then
        return false
    end
    local payload = {
        schema = "BurdJournals.EntryStoreFileBackup",
        version = 1,
        uuid = uuid,
        source = tostring(sourceTag or "entryStore"),
        updatedAt = getTimestampMs and getTimestampMs() or os.time(),
        journalData = journalData,
    }
    local jsonText = BurdJournals.encodeJournalExportJSON(payload)
    if type(jsonText) ~= "string" or jsonText == "" then
        return false
    end
    -- File IO can legitimately fail independently of journal state. Keep each
    -- protected operation narrow so a failed write cannot masquerade as a
    -- successful backup and always attempt to release an opened writer.
    local opened, writer = pcall(getFileWriter, filename, true, false)
    if not opened or not writer then
        return false
    end
    local wrotePayload = pcall(writer.write, writer, jsonText)
    local wroteNewline = wrotePayload and pcall(writer.write, writer, "\r\n") or false
    local closed = pcall(writer.close, writer)
    local ok = wrotePayload and wroteNewline and closed
    if ok then
        bsjWriteLogLine("[BurdJournals][EntryStoreFileBackup] wrote uuid="
            .. tostring(uuid)
            .. " source=" .. tostring(sourceTag or "entryStore")
            .. " entries=" .. tostring(BurdJournals.Server.countJournalPayloadEntries(journalData)))
    end
    return ok == true
end

function BurdJournals.Server.invalidateJournalEntryFileBackup(uuid, sourceTag)
    if not (getFileReader and getFileWriter and BurdJournals.encodeJournalExportJSON) then
        return false
    end
    local filename = BurdJournals.Server.getJournalEntryFileBackupName(uuid)
    if not filename then
        return false
    end

    -- PZ exposes overwrite-capable readers/writers here but no stable cross-build
    -- delete API. Only create a tombstone when a backup actually exists.
    local openedReader, reader = pcall(getFileReader, filename, true)
    if not openedReader or not reader then
        return false
    end
    pcall(reader.close, reader)

    local payload = {
        schema = "BurdJournals.EntryStoreFileBackupTombstone",
        version = 1,
        uuid = uuid,
        source = tostring(sourceTag or "purged"),
        deleted = true,
        updatedAt = getTimestampMs and getTimestampMs() or os.time(),
    }
    local jsonText = BurdJournals.encodeJournalExportJSON(payload)
    if type(jsonText) ~= "string" or jsonText == "" then
        return false
    end
    local openedWriter, writer = pcall(getFileWriter, filename, true, false)
    if not openedWriter or not writer then
        return false
    end
    local wrotePayload = pcall(writer.write, writer, jsonText)
    local wroteNewline = wrotePayload and pcall(writer.write, writer, "\r\n") or false
    local closed = pcall(writer.close, writer)
    return wrotePayload and wroteNewline and closed
end

-- File backups are belt-and-suspenders recovery data (the authoritative entries
-- live in global ModData), so they never need to happen on the hot path. Writers
-- enqueue here and the server tick drains one JSON write per tick; the entry is
-- re-resolved at write time so coalesced queue hits always capture fresh data.
BurdJournals.Server._pendingEntryFileBackups = BurdJournals.Server._pendingEntryFileBackups or {}

function BurdJournals.Server.queueJournalEntryFileBackup(uuid, shardKey, sourceTag)
    if type(uuid) ~= "string" or uuid == "" then
        return false
    end
    BurdJournals.Server._pendingEntryFileBackups[uuid] = {
        shardKey = shardKey,
        sourceTag = tostring(sourceTag or "queued"),
    }
    return true
end

function BurdJournals.Server.drainPendingEntryFileBackups(maxWrites)
    local pending = BurdJournals.Server._pendingEntryFileBackups
    if type(pending) ~= "table" then
        return 0
    end
    local budget = math.max(1, tonumber(maxWrites) or 1)
    local written = 0
    local nowMs = (getTimestampMs and getTimestampMs()) or ((os.time() or 0) * 1000)
    for uuid, info in pairs(pending) do
        if budget <= 0 then
            break
        end
        local retryAfterMs = type(info) == "table" and tonumber(info.retryAfterMs) or 0
        if retryAfterMs <= nowMs then
            local entry = nil
            if BurdJournals._getLegacyJournalEntryStoreEntry then
                entry = BurdJournals._getLegacyJournalEntryStoreEntry(uuid, info and info.shardKey)
            end
            if type(entry) ~= "table" then
                -- The authoritative entry no longer exists, so there is no
                -- recovery payload left to write.
                pending[uuid] = nil
            elseif BurdJournals.Server.writeJournalEntryFileBackup(uuid, entry, info and info.sourceTag) then
                pending[uuid] = nil
                written = written + 1
            else
                info = type(info) == "table" and info or {}
                info.attempts = (tonumber(info.attempts) or 0) + 1
                info.retryAfterMs = nowMs + math.min(60000, info.attempts * 5000)
                pending[uuid] = info
            end
            budget = budget - 1
        end
    end
    return written
end

function BurdJournals.Server.readJournalEntryFileBackup(uuid, sourceTag)
    if not (getFileReader and BurdJournals.decodeJournalExportJSON) then
        return nil
    end
    local filename = BurdJournals.Server.getJournalEntryFileBackupName(uuid)
    if not filename then
        return nil
    end
    -- File handles are an optional persistence boundary and may legitimately
    -- be stale. Protect only the individual I/O calls.
    local opened, reader = pcall(getFileReader, filename, true)
    if not opened or not reader then return nil end
    local text, byteCount, readFailed = {}, 0, false
    while #text < 8000 and byteCount <= 1048576 do
        local readOk, line = pcall(reader.readLine, reader)
        if not readOk then readFailed = true break end
        if line == nil then break end
        byteCount = byteCount + #tostring(line)
        text[#text + 1] = line
    end
    local closeOk = pcall(reader.close, reader)
    if readFailed or not closeOk or byteCount > 1048576 or #text >= 8000 or #text <= 0 then
        return nil
    end
    local decoded = BurdJournals.decodeJournalExportJSON(table.concat(text, "\n"))
    local journalData = type(decoded) == "table" and decoded.journalData or nil
    if type(journalData) ~= "table" or BurdJournals.Server.countJournalPayloadEntries(journalData) <= 0 then
        return nil
    end
    journalData.uuid = journalData.uuid or uuid
    journalData.entryStoreUUID = journalData.entryStoreUUID or uuid
    journalData.entryStoreEnabled = true
    bsjWriteLogLine("[BurdJournals][EntryStoreFileBackup] read uuid="
        .. tostring(uuid)
        .. " source=" .. tostring(sourceTag or "nil")
        .. " entries=" .. tostring(BurdJournals.Server.countJournalPayloadEntries(journalData)))
    return journalData
end

function BurdJournals.Server.hasAnyDeltaEntries(delta)
    if type(delta) ~= "table" then
        return false
    end
    return (BurdJournals.hasAnyEntries and (
            BurdJournals.hasAnyEntries(delta.skills)
            or BurdJournals.hasAnyEntries(delta.traits)
            or BurdJournals.hasAnyEntries(delta.stats)
            or BurdJournals.hasAnyEntries(delta.recipes)
        ))
        or false
end

function BurdJournals.Server.normalizeEntrySyncBucket(bucketName)
    local wanted = tostring(bucketName or "")
    for _, candidate in ipairs(BurdJournals.ENTRY_STORE_BUCKETS or {"skills", "traits", "stats", "recipes"}) do
        if wanted == candidate then
            return candidate
        end
    end
    return "skills"
end

function BurdJournals.Server.collectSortedEntryKeys(bucket)
    local keys = {}
    if type(bucket) ~= "table" then
        return keys
    end
    for key, value in pairs(bucket) do
        if key ~= nil and value ~= nil then
            keys[#keys + 1] = tostring(key)
        end
    end
    table.sort(keys)
    return keys
end

BurdJournals.Server.ENTRY_CHUNK_KEY_CACHE_TTL_MS = 30000
BurdJournals.Server.ENTRY_CHUNK_KEY_CACHE_MAX = 64

function BurdJournals.Server.pruneEntryChunkKeyCache(now)
    now = tonumber(now) or 0
    local cache = BurdJournals.Server._entryChunkKeyCache
    local retained = {}
    for key, cached in pairs(cache) do
        local timestamp = tonumber(type(cached) == "table" and cached.timestamp or 0) or 0
        if type(cached) ~= "table" or (now > 0 and timestamp > 0 and (now - timestamp) > BurdJournals.Server.ENTRY_CHUNK_KEY_CACHE_TTL_MS) then
            cache[key] = nil
        else
            retained[#retained + 1] = { key = key, timestamp = timestamp }
        end
    end
    table.sort(retained, function(a, b) return a.timestamp < b.timestamp end)
    for index = 1, math.max(0, #retained - BurdJournals.Server.ENTRY_CHUNK_KEY_CACHE_MAX) do
        cache[retained[index].key] = nil
    end
end

function BurdJournals.Server.collectCachedEntryKeys(journalData, bucketName, bucket)
    local now = getTimestampMs and getTimestampMs() or 0
    BurdJournals.Server.pruneEntryChunkKeyCache(now)
    local journalUUID = BurdJournals.getJournalIdentityUUID and BurdJournals.getJournalIdentityUUID(journalData) or nil
    local cacheKey = tostring(journalUUID or "unknown") .. ":" .. tostring(bucketName or "skills")
    local bucketToken = tostring(bucket)
    local count = 0
    if type(bucket) == "table" then
        for key, value in pairs(bucket) do
            if key ~= nil and value ~= nil then
                count = count + 1
            end
        end
    end
    local cached = BurdJournals.Server._entryChunkKeyCache[cacheKey]
    if cached
        and cached.bucketToken == bucketToken
        and cached.count == count
        and now - (cached.timestamp or 0) <= 10000
        and type(cached.keys) == "table"
    then
        cached.timestamp = now
        return cached.keys
    end
    local keys = BurdJournals.Server.collectSortedEntryKeys(bucket)
    BurdJournals.Server._entryChunkKeyCache[cacheKey] = {
        bucketToken = bucketToken,
        count = count,
        timestamp = now,
        keys = keys,
    }
    BurdJournals.Server.pruneEntryChunkKeyCache(now)
    return keys
end

function BurdJournals.Server.getJournalEntrySyncBucket(journalData, bucketName)
    if type(journalData) ~= "table" then
        return nil
    end
    local entry = BurdJournals.getOrCreateJournalEntryStoreEntryForData
        and BurdJournals.getOrCreateJournalEntryStoreEntryForData(journalData, false)
        or nil
    if type(entry) == "table" and type(entry[bucketName]) == "table" then
        return entry[bucketName]
    end
    return journalData[bucketName]
end

function BurdJournals.Server.getJournalEntrySyncBucketFromRegistry(journalData, bucketName, sourceTag)
    if type(journalData) ~= "table" or type(bucketName) ~= "string" then
        return nil
    end
    local journalUUID = BurdJournals.getJournalIdentityUUID and BurdJournals.getJournalIdentityUUID(journalData) or nil
    if not journalUUID then
        return nil
    end
    local registryEntry = BurdJournals.Server.getPlayerJournalRegistryEntryByUUID
        and BurdJournals.Server.getPlayerJournalRegistryEntryByUUID(journalUUID)
        or nil
    local registryData = type(registryEntry) == "table" and registryEntry.journalData or nil
    -- The registry deliberately stores an entry-store manifest in strict MP.
    -- Materialize it only for this actual server recovery/chunk request.
    if type(registryData) == "table" and registryData.entryStoreEnabled == true
        and BurdJournals.hydrateJournalDataFromEntryStore then
        registryData = BurdJournals.Server.deepCopy(registryData)
        BurdJournals.hydrateJournalDataFromEntryStore(registryData, "playerRegistryEntryRecovery")
    end
    local registryBucket = type(registryData) == "table" and registryData[bucketName] or nil
    if type(registryBucket) ~= "table" or not (BurdJournals.hasAnyEntries and BurdJournals.hasAnyEntries(registryBucket)) then
        return nil
    end
    bsjWriteLogLine("[BurdJournals][EntryStoreFallback] serving chunk bucket from player journal registry"
        .. " uuid=" .. tostring(journalUUID)
        .. " bucket=" .. tostring(bucketName)
        .. " source=" .. tostring(sourceTag or "nil")
        .. " entries=" .. tostring(BurdJournals.Server.countJournalPayloadEntries(registryData)))
    return registryBucket, registryData
end

function BurdJournals.Server.getJournalEntrySyncBucketFromFileBackup(journalData, bucketName, sourceTag)
    if type(journalData) ~= "table" or type(bucketName) ~= "string" then
        return nil
    end
    local journalUUID = BurdJournals.getJournalIdentityUUID and BurdJournals.getJournalIdentityUUID(journalData) or nil
    if not journalUUID then
        return nil
    end
    local backupData = BurdJournals.Server.readJournalEntryFileBackup
        and BurdJournals.Server.readJournalEntryFileBackup(journalUUID, sourceTag or "entryChunk")
        or nil
    local backupBucket = type(backupData) == "table" and backupData[bucketName] or nil
    if type(backupBucket) ~= "table" or not (BurdJournals.hasAnyEntries and BurdJournals.hasAnyEntries(backupBucket)) then
        return nil
    end
    bsjWriteLogLine("[BurdJournals][EntryStoreFileBackup] serving chunk bucket"
        .. " uuid=" .. tostring(journalUUID)
        .. " bucket=" .. tostring(bucketName)
        .. " source=" .. tostring(sourceTag or "nil")
        .. " entries=" .. tostring(BurdJournals.Server.countJournalPayloadEntries(backupData)))
    return backupBucket, backupData
end

function BurdJournals.Server.repairMissingEntryStoreFromInlineData(journalData, sourceTag)
    if type(journalData) ~= "table" or journalData.entryStoreEnabled ~= true then
        return false
    end
    local hasInlineEntries = false
    for _, bucketName in ipairs(BurdJournals.ENTRY_STORE_BUCKETS or {"skills", "traits", "stats", "recipes"}) do
        if BurdJournals.hasAnyEntries and BurdJournals.hasAnyEntries(journalData[bucketName]) then
            hasInlineEntries = true
            break
        end
    end
    if not hasInlineEntries then
        return false
    end

    local entry, shardKey = BurdJournals.getOrCreateJournalEntryStoreEntryForData
        and BurdJournals.getOrCreateJournalEntryStoreEntryForData(journalData, true, nil, sourceTag or "repairMissingEntryStore")
        or nil
    if type(entry) ~= "table" then
        return false
    end

    journalData.entryStoreEntryCounts = type(journalData.entryStoreEntryCounts) == "table" and journalData.entryStoreEntryCounts or {}
    for _, bucketName in ipairs(BurdJournals.ENTRY_STORE_BUCKETS or {"skills", "traits", "stats", "recipes"}) do
        local bucket = journalData[bucketName]
        if type(bucket) == "table" then
            entry[bucketName] = entry[bucketName] or {}
            local count = 0
            for key, value in pairs(bucket) do
                entry[bucketName][key] = BurdJournals.Server.deepCopy and BurdJournals.Server.deepCopy(value) or value
                count = count + 1
            end
            if count > 0 then
                journalData.entryStoreEntryCounts[bucketName] = math.max(count, tonumber(journalData.entryStoreEntryCounts[bucketName]) or 0)
            end
        end
    end
    if shardKey and BurdJournals.markEntryStoreShardDirty then
        BurdJournals.markEntryStoreShardDirty(shardKey, true)
    end
    bsjWriteLogLine("[BurdJournals][EntryStoreRepair] repaired missing shard from inline data uuid="
        .. tostring(journalData.uuid or journalData.entryStoreUUID)
        .. " source=" .. tostring(sourceTag or "nil")
        .. " counts=" .. bsjFormatEntryStoreCounts(journalData.entryStoreEntryCounts))
    return true
end

function BurdJournals.Server.buildJournalEntryChunkPayload(journalId, journalData, args)
    local canonicalUUID = BurdJournals.getJournalIdentityUUID and BurdJournals.getJournalIdentityUUID(journalData) or nil
    local bucketName = BurdJournals.Server.normalizeEntrySyncBucket(args and args.bucket)
    local offset = math.max(0, tonumber(args and args.offset) or 0)
    local chunkSize = math.max(1, math.min(128, tonumber(args and args.chunkSize) or tonumber(BurdJournals.ENTRY_SYNC_CHUNK_SIZE) or 64))
    local sourceBucket = BurdJournals.Server.getJournalEntrySyncBucket(journalData, bucketName)
    local expectedCount = type(journalData.entryStoreEntryCounts) == "table"
        and math.max(0, tonumber(journalData.entryStoreEntryCounts[bucketName]) or 0)
        or 0
    if journalData.entryStoreEnabled == true and expectedCount > 0 and type(sourceBucket) ~= "table" then
        if BurdJournals.Server.repairMissingEntryStoreFromInlineData(journalData, "buildJournalEntryChunkPayload") then
            sourceBucket = BurdJournals.Server.getJournalEntrySyncBucket(journalData, bucketName)
        end
    end
    if journalData.entryStoreEnabled == true and expectedCount > 0 and type(sourceBucket) ~= "table" then
        local registryBucket, registryData = BurdJournals.Server.getJournalEntrySyncBucketFromRegistry(journalData, bucketName, "missingSourceBucket")
        if type(registryBucket) == "table" then
            sourceBucket = registryBucket
            if type(registryData) == "table" and type(registryData.entryStoreEntryCounts) == "table" then
                journalData.entryStoreEntryCounts = registryData.entryStoreEntryCounts
                expectedCount = math.max(0, tonumber(journalData.entryStoreEntryCounts[bucketName]) or 0)
            end
        end
    end
    if journalData.entryStoreEnabled == true and expectedCount > 0 and type(sourceBucket) ~= "table" then
        local fileBucket, fileData = BurdJournals.Server.getJournalEntrySyncBucketFromFileBackup(journalData, bucketName, "missingSourceBucket")
        if type(fileBucket) == "table" then
            sourceBucket = fileBucket
            if type(fileData) == "table" and type(fileData.entryStoreEntryCounts) == "table" then
                journalData.entryStoreEntryCounts = fileData.entryStoreEntryCounts
                expectedCount = math.max(0, tonumber(journalData.entryStoreEntryCounts[bucketName]) or 0)
            end
        end
    end
    if journalData.entryStoreEnabled == true and expectedCount > 0 and type(sourceBucket) ~= "table" then
        local journalUUID = canonicalUUID
        local derivedShardKey = BurdJournals.getJournalEntryStoreShardKeyForUUID and BurdJournals.getJournalEntryStoreShardKeyForUUID(journalUUID) or nil
        local legacyShardKey = BurdJournals._getJournalEntryStoreLegacyShardKeyForUUID and BurdJournals._getJournalEntryStoreLegacyShardKeyForUUID(journalUUID) or nil
        bsjWriteLogLine("[BurdJournals][EntryStoreUnavailable] missing source bucket"
            .. " journalId=" .. tostring(journalId)
            .. " uuid=" .. tostring(journalUUID)
            .. " bucket=" .. tostring(bucketName)
            .. " expected=" .. tostring(expectedCount)
            .. " stampedShard=" .. tostring(journalData.entryStoreShardKey)
            .. " derivedShard=" .. tostring(derivedShardKey)
            .. " legacyShard=" .. tostring(legacyShardKey)
            .. " counts=" .. bsjFormatEntryStoreCounts(journalData.entryStoreEntryCounts))
        return {
            requestId = args and args.requestId or nil,
            serverRuntimeMarker = BurdJournals.RUNTIME_LOAD_MARKER,
            journalId = journalId,
            journalUUID = journalUUID,
            bucket = bucketName,
            offset = offset,
            nextOffset = offset,
            total = expectedCount,
            done = false,
            chunkSize = chunkSize,
            entryStoreEnabled = true,
            entryStoreEntryCounts = journalData.entryStoreEntryCounts,
            error = "Journal entry store unavailable.",
            terminal = true,
            entryStoreUnavailable = true,
        }
    end
    local keys = BurdJournals.Server.collectCachedEntryKeys(journalData, bucketName, sourceBucket)
    local total = #keys
    if journalData.entryStoreEnabled == true and expectedCount > 0 and total <= 0 then
        local registryBucket, registryData = BurdJournals.Server.getJournalEntrySyncBucketFromRegistry(journalData, bucketName, "emptySourceBucket")
        if type(registryBucket) == "table" then
            sourceBucket = registryBucket
            if type(registryData) == "table" and type(registryData.entryStoreEntryCounts) == "table" then
                journalData.entryStoreEntryCounts = registryData.entryStoreEntryCounts
                expectedCount = math.max(0, tonumber(journalData.entryStoreEntryCounts[bucketName]) or 0)
            end
            keys = BurdJournals.Server.collectCachedEntryKeys(journalData, bucketName, sourceBucket)
            total = #keys
        end
    end
    if journalData.entryStoreEnabled == true and expectedCount > 0 and total <= 0 then
        local fileBucket, fileData = BurdJournals.Server.getJournalEntrySyncBucketFromFileBackup(journalData, bucketName, "emptySourceBucket")
        if type(fileBucket) == "table" then
            sourceBucket = fileBucket
            if type(fileData) == "table" and type(fileData.entryStoreEntryCounts) == "table" then
                journalData.entryStoreEntryCounts = fileData.entryStoreEntryCounts
                expectedCount = math.max(0, tonumber(journalData.entryStoreEntryCounts[bucketName]) or 0)
            end
            keys = BurdJournals.Server.collectCachedEntryKeys(journalData, bucketName, sourceBucket)
            total = #keys
        end
    end
    if journalData.entryStoreEnabled == true and expectedCount > 0 and total <= 0 then
        local journalUUID = canonicalUUID
        local derivedShardKey = BurdJournals.getJournalEntryStoreShardKeyForUUID and BurdJournals.getJournalEntryStoreShardKeyForUUID(journalUUID) or nil
        local legacyShardKey = BurdJournals._getJournalEntryStoreLegacyShardKeyForUUID and BurdJournals._getJournalEntryStoreLegacyShardKeyForUUID(journalUUID) or nil
        bsjWriteLogLine("[BurdJournals][EntryStoreUnavailable] empty source bucket"
            .. " journalId=" .. tostring(journalId)
            .. " uuid=" .. tostring(journalUUID)
            .. " bucket=" .. tostring(bucketName)
            .. " expected=" .. tostring(expectedCount)
            .. " stampedShard=" .. tostring(journalData.entryStoreShardKey)
            .. " derivedShard=" .. tostring(derivedShardKey)
            .. " legacyShard=" .. tostring(legacyShardKey)
            .. " counts=" .. bsjFormatEntryStoreCounts(journalData.entryStoreEntryCounts))
        return {
            requestId = args and args.requestId or nil,
            serverRuntimeMarker = BurdJournals.RUNTIME_LOAD_MARKER,
            journalId = journalId,
            journalUUID = journalUUID,
            bucket = bucketName,
            offset = offset,
            nextOffset = offset,
            total = expectedCount,
            done = false,
            chunkSize = chunkSize,
            entryStoreEnabled = true,
            entryStoreEntryCounts = journalData.entryStoreEntryCounts,
            error = "Journal entry store unavailable.",
            terminal = true,
            entryStoreUnavailable = true,
        }
    end
    if offset > total then
        offset = total
    end

    local entries = {}
    local lastIndex = math.min(total, offset + chunkSize)
    for i = offset + 1, lastIndex do
        local key = keys[i]
        entries[key] = BurdJournals.Server.deepCopy and BurdJournals.Server.deepCopy(sourceBucket[key]) or sourceBucket[key]
    end

    return {
        requestId = args and args.requestId or nil,
        serverRuntimeMarker = BurdJournals.RUNTIME_LOAD_MARKER,
        journalId = journalId,
        journalUUID = canonicalUUID,
        bucket = bucketName,
        entries = entries,
        offset = offset,
        nextOffset = lastIndex,
        total = total,
        done = lastIndex >= total,
        chunkSize = chunkSize,
        buckets = BurdJournals.ENTRY_STORE_BUCKETS or {"skills", "traits", "stats", "recipes"},
        entryStoreEnabled = journalData.entryStoreEnabled == true,
        entryStoreEntryCounts = type(journalData.entryStoreEntryCounts) == "table" and journalData.entryStoreEntryCounts or nil,
        entryStoreUpdatedAt = journalData.entryStoreUpdatedAt,
    }
end

local function isEntryStoreFallbackPlayerJournalIdentitySource(source)
    if type(source) ~= "table" then
        return false
    end
    if source.isYuletideJournal == true
        and source.yuletideState == BurdJournals.YULETIDE_STATE_UNWRAPPED
    then
        return false
    end
    if source.isPlayerCreated == true then
        return true
    end
    if source.isPlayerCreated == nil
        and (source.ownerUsername or source.ownerSteamId or source.ownerCharacterName)
        and source.isWorn ~= true
        and source.isBloody ~= true
    then
        return true
    end
    local sourceType = string.lower(tostring(source.sourceType or ""))
    local originMode = string.lower(tostring(source.originMode or ""))
    return sourceType == "personal" or sourceType == "player" or originMode == "personal" or originMode == "player"
end

local function copyEntryStoreFallbackIdentityFields(target, source)
    if type(target) ~= "table" or type(source) ~= "table" then
        return
    end
    local keys = {
        "ownerUsername",
        "ownerSteamId",
        "ownerCharacterName",
        "author",
        "profession",
        "professionName",
        "recordedWithBaseline",
        "baselineId",
        "baselineCharacterId",
        "sourceType",
        "originMode",
        "respawnGeneration",
        "lastPlayerJournalUseTs",
    }
    for _, key in ipairs(keys) do
        if target[key] == nil and source[key] ~= nil then
            target[key] = source[key]
        end
    end
    if target.skillBaseline == nil and type(source.skillBaseline) == "table" then
        target.skillBaseline = BurdJournals.Server.deepCopy and BurdJournals.Server.deepCopy(source.skillBaseline) or source.skillBaseline
    end
end

local function preserveEntryStoreFallbackPlayerJournalIdentity(fallbackData, sourceData, journalUUID, sourceTag)
    if type(fallbackData) ~= "table" then
        return fallbackData
    end

    local registryEntry = nil
    if type(journalUUID) == "string" and journalUUID ~= "" and BurdJournals.Server.getPlayerJournalRegistryEntryByUUID then
        registryEntry = BurdJournals.Server.getPlayerJournalRegistryEntryByUUID(journalUUID)
    end
    local registryData = type(registryEntry) == "table" and registryEntry.journalData or nil

    local hasPlayerJournalProof = isEntryStoreFallbackPlayerJournalIdentitySource(sourceData)
        or isEntryStoreFallbackPlayerJournalIdentitySource(registryData)
        or isEntryStoreFallbackPlayerJournalIdentitySource(registryEntry)
    if not hasPlayerJournalProof then
        return fallbackData
    end

    copyEntryStoreFallbackIdentityFields(fallbackData, registryData)
    copyEntryStoreFallbackIdentityFields(fallbackData, registryEntry)
    copyEntryStoreFallbackIdentityFields(fallbackData, sourceData)
    fallbackData.isPlayerCreated = true
    if fallbackData.sourceType == nil or tostring(fallbackData.sourceType) == "" then
        fallbackData.sourceType = "personal"
    end
    if fallbackData.originMode == nil or tostring(fallbackData.originMode) == "" then
        fallbackData.originMode = "personal"
    end
    fallbackData.isWorn = false
    fallbackData.isBloody = false

    bsjWriteLogLine("[BurdJournals][EntryStoreFallback] preserved player journal identity"
        .. " uuid=" .. tostring(journalUUID or fallbackData.uuid or fallbackData.entryStoreUUID)
        .. " source=" .. tostring(sourceTag or "nil")
        .. " owner=" .. tostring(fallbackData.ownerUsername or fallbackData.ownerSteamId or fallbackData.ownerCharacterName or "nil"))

    return fallbackData
end

function BurdJournals.Server.buildEntryStoreOnlyJournalData(journalUUID, sourceTag)
    if type(journalUUID) ~= "string" or journalUUID == "" then
        return nil
    end
    local journalData = {
        uuid = journalUUID,
        entryStoreEnabled = true,
        entryStoreUUID = journalUUID,
        entryStoreEntryCounts = {},
    }
    local entry = BurdJournals.getOrCreateJournalEntryStoreEntryForData
        and BurdJournals.getOrCreateJournalEntryStoreEntryForData(journalData, false, nil, sourceTag or "entryStoreOnly")
        or nil
    local liveEntryCount = type(entry) == "table"
        and BurdJournals.Server.countResidentJournalPayloadEntries(entry) or 0
    if liveEntryCount > 0 and BurdJournals.Server.readJournalEntryFileBackup
        and BurdJournals.Server.restoreJournalEntryStoreFromPayload
    then
        local fileData = BurdJournals.Server.readJournalEntryFileBackup(
            journalUUID, (sourceTag or "entryStoreOnly") .. "CompareLive")
        local fileEntryCount = type(fileData) == "table"
            and BurdJournals.Server.countResidentJournalPayloadEntries(fileData) or 0
        if fileEntryCount > liveEntryCount
            and BurdJournals.Server.restoreJournalEntryStoreFromPayload(
                journalUUID, fileData, (sourceTag or "entryStoreOnly") .. "RepairLive")
        then
            entry = BurdJournals.getOrCreateJournalEntryStoreEntryForData(
                journalData, false, nil, (sourceTag or "entryStoreOnly") .. "RepairLiveVerify")
        end
    end
    if type(entry) ~= "table"
        or BurdJournals.Server.countResidentJournalPayloadEntries(entry) <= 0
    then
        local registryEntry = BurdJournals.Server.getPlayerJournalRegistryEntryByUUID
            and BurdJournals.Server.getPlayerJournalRegistryEntryByUUID(journalUUID)
            or nil
        local registryData = type(registryEntry) == "table" and registryEntry.journalData or nil
        if type(registryData) == "table" and BurdJournals.Server.countResidentJournalPayloadEntries(registryData) > 0 then
            local fallback = BurdJournals.Server.deepCopy(registryData)
            fallback.uuid = fallback.uuid or journalUUID
            fallback.entryStoreUUID = fallback.entryStoreUUID or journalUUID
            local registryStoreRestored = fallback.entryStoreEnabled ~= true
                or BurdJournals.Server.restoreJournalEntryStoreFromPayload(
                    journalUUID, fallback, sourceTag or "entryStoreOnlyRegistry")
            if registryStoreRestored then
                fallback._entryStoreFallbackSource = tostring(sourceTag or "entryStoreOnly")
                preserveEntryStoreFallbackPlayerJournalIdentity(fallback, registryEntry, journalUUID, sourceTag or "entryStoreOnly")
                bsjWriteLogLine("[BurdJournals][EntryStoreFallback] restored journalData from registry backup"
                    .. " uuid=" .. tostring(journalUUID) .. " source=" .. tostring(sourceTag or "entryStoreOnly")
                    .. " entries=" .. tostring(BurdJournals.Server.countJournalPayloadEntries(fallback)))
                return fallback
            end
        end
        local fileData = BurdJournals.Server.readJournalEntryFileBackup
            and BurdJournals.Server.readJournalEntryFileBackup(journalUUID, sourceTag or "entryStoreOnly")
            or nil
        if type(fileData) == "table" and BurdJournals.Server.countResidentJournalPayloadEntries(fileData) > 0 then
            fileData.uuid = fileData.uuid or journalUUID
            fileData.entryStoreUUID = fileData.entryStoreUUID or journalUUID
            if not BurdJournals.Server.restoreJournalEntryStoreFromPayload(
                journalUUID, fileData, sourceTag or "entryStoreOnlyFile") then
                bsjWriteLogLine("[BurdJournals][EntryStoreFileBackup] restore failed uuid=" .. tostring(journalUUID)
                    .. " source=" .. tostring(sourceTag or "entryStoreOnlyFile"))
                return nil
            end
            fileData._entryStoreFallbackSource = tostring(sourceTag or "entryStoreOnlyFile")
            preserveEntryStoreFallbackPlayerJournalIdentity(fileData, registryEntry, journalUUID, sourceTag or "entryStoreOnlyFile")
            return fileData
        end
        return nil
    end
    journalData.entryStoreUpdatedAt = tonumber(entry.updatedAt) or journalData.entryStoreUpdatedAt
    for _, bucketName in ipairs(BurdJournals.ENTRY_STORE_BUCKETS or {"skills", "traits", "stats", "recipes"}) do
        local bucket = entry[bucketName]
        local count = 0
        if type(bucket) == "table" then
            for key, value in pairs(bucket) do
                if key ~= nil and value ~= nil then
                    count = count + 1
                end
            end
        end
        journalData.entryStoreEntryCounts[bucketName] = count
    end
    return journalData
end

function BurdJournals.Server.preferEntryStoreDataForEmptyShell(journalData, args, sourceTag)
    if type(journalData) ~= "table" then
        return journalData
    end
    if BurdJournals.Server.countJournalPayloadEntries(journalData) > 0 then
        return journalData
    end
    local fallbackUUID = type(journalData.entryStoreUUID) == "string" and journalData.entryStoreUUID ~= "" and journalData.entryStoreUUID
        or (type(journalData.uuid) == "string" and journalData.uuid ~= "" and journalData.uuid)
        or (type(journalData.replacedByJournalUUID) == "string" and journalData.replacedByJournalUUID ~= "" and journalData.replacedByJournalUUID)
        or (type(args) == "table" and type(args.journalUUID) == "string" and args.journalUUID ~= "" and args.journalUUID)
        or (type(args) == "table" and type(args.entryStoreUUID) == "string" and args.entryStoreUUID ~= "" and args.entryStoreUUID)
    local fallbackJournalData = fallbackUUID
        and BurdJournals.Server.buildEntryStoreOnlyJournalData
        and BurdJournals.Server.buildEntryStoreOnlyJournalData(fallbackUUID, sourceTag or "emptyShellEntryStoreFallback")
        or nil
    local fallbackStoreCount = 0
    if type(fallbackJournalData) == "table" and type(fallbackJournalData.entryStoreEntryCounts) == "table" then
        for _, bucketName in ipairs(BurdJournals.ENTRY_STORE_BUCKETS or {"skills", "traits", "stats", "recipes"}) do
            fallbackStoreCount = fallbackStoreCount + math.max(0, tonumber(fallbackJournalData.entryStoreEntryCounts[bucketName]) or 0)
        end
    end
    if fallbackStoreCount > 0 then
        preserveEntryStoreFallbackPlayerJournalIdentity(fallbackJournalData, journalData, fallbackUUID, sourceTag or "emptyShellEntryStoreFallback")
        return fallbackJournalData
    end
    return journalData
end

function BurdJournals.Server.countEntryStoreManifestEntries(journalData)
    local counts = type(journalData) == "table" and journalData.entryStoreEntryCounts or nil
    if type(counts) ~= "table" then return 0 end
    local total = 0
    for _, bucketName in ipairs(BurdJournals.ENTRY_STORE_BUCKETS or {"skills", "traits", "stats", "recipes"}) do
        total = total + math.max(0, tonumber(counts[bucketName]) or 0)
    end
    return total
end

-- Inventory replication can briefly restore an older inline ModData shape after
-- a large journal has been moved into the server entry store.  A non-empty
-- partial shape is still stale: for the same UUID, the richer server-owned store
-- must win just as an empty shell would.  Preserve item metadata while returning
-- a compact manifest so callers never rebroadcast the large entry buckets.
function BurdJournals.Server.preferRicherEntryStoreData(journalData, args, sourceTag)
    if type(journalData) ~= "table" then return journalData, false end
    local journalUUID = type(journalData.entryStoreUUID) == "string" and journalData.entryStoreUUID ~= "" and journalData.entryStoreUUID
        or (type(journalData.uuid) == "string" and journalData.uuid ~= "" and journalData.uuid)
        or (type(journalData.replacedByJournalUUID) == "string" and journalData.replacedByJournalUUID ~= "" and journalData.replacedByJournalUUID)
        or (type(args) == "table" and type(args.journalUUID) == "string" and args.journalUUID ~= "" and args.journalUUID)
        or nil
    if not journalUUID then return journalData, false end

    -- A manifest proves what should exist, not that the backing ModData entry
    -- still exists. Validate it before bypassing registry/file recovery.
    if journalData.entryStoreEnabled == true
        and BurdJournals.Server.countEntryStoreManifestEntries(journalData) > 0
    then
        local expectedStoreCount = BurdJournals.Server.countEntryStoreManifestEntries(journalData)
        local liveEntry = BurdJournals.getOrCreateJournalEntryStoreEntryForData
            and BurdJournals.getOrCreateJournalEntryStoreEntryForData(journalData, false, nil,
                sourceTag or "preferRicherEntryStoreData") or nil
        local liveStoreCount = type(liveEntry) == "table"
            and BurdJournals.Server.countResidentJournalPayloadEntries(liveEntry) or 0
        if liveStoreCount >= expectedStoreCount then
            return journalData, false
        end
        if liveStoreCount > 0 and BurdJournals.Server.readJournalEntryFileBackup
            and BurdJournals.Server.restoreJournalEntryStoreFromPayload
        then
            local recoveryTag = (sourceTag or "preferRicherEntryStoreData") .. "PartialStore"
            local fileData = BurdJournals.Server.readJournalEntryFileBackup(journalUUID, recoveryTag)
            local fileStoreCount = type(fileData) == "table"
                and BurdJournals.Server.countResidentJournalPayloadEntries(fileData) or 0
            if fileStoreCount > liveStoreCount
                and BurdJournals.Server.restoreJournalEntryStoreFromPayload(journalUUID, fileData, recoveryTag)
            then
                local restoredEntry = BurdJournals.getOrCreateJournalEntryStoreEntryForData(
                    journalData, false, nil, recoveryTag .. "Verify")
                local restoredCount = type(restoredEntry) == "table"
                    and BurdJournals.Server.countResidentJournalPayloadEntries(restoredEntry) or 0
                if restoredCount >= expectedStoreCount then
                    bsjWriteLogLine("[BurdJournals][EntryStoreFallback] repaired partial live entry store"
                        .. " uuid=" .. tostring(journalUUID)
                        .. " liveEntries=" .. tostring(liveStoreCount)
                        .. " restoredEntries=" .. tostring(restoredCount)
                        .. " expectedEntries=" .. tostring(expectedStoreCount))
                    return journalData, false
                end
            end
        end
    end

    local storeData = BurdJournals.Server.buildEntryStoreOnlyJournalData
        and BurdJournals.Server.buildEntryStoreOnlyJournalData(journalUUID, sourceTag or "preferRicherEntryStoreData")
        or nil
    if type(storeData) ~= "table" then return journalData, false end

    local residentCount = BurdJournals.Server.countJournalPayloadEntries(journalData)
    local residentAuthoritativeCount = residentCount
    if journalData.entryStoreEnabled == true then
        residentAuthoritativeCount = math.max(
            residentAuthoritativeCount,
            BurdJournals.Server.countEntryStoreManifestEntries(journalData)
        )
    end
    local storeCount = BurdJournals.Server.countEntryStoreManifestEntries(storeData)
    if storeCount <= 0 then
        storeCount = BurdJournals.Server.countJournalPayloadEntries(storeData)
    end
    local residentRevision = tonumber(journalData.entryStoreUpdatedAt) or 0
    local storeRevision = tonumber(storeData.entryStoreUpdatedAt) or 0
    if residentRevision > 0 and storeRevision > 0 and storeRevision < residentRevision then
        return journalData, false
    end
    if storeCount <= residentAuthoritativeCount then return journalData, false end

    local preferred = BurdJournals.Server.deepCopy(journalData)
    preferred.uuid = journalUUID
    preferred.entryStoreEnabled = true
    preferred.entryStoreUUID = journalUUID
    preferred.entryStoreEntryCounts = BurdJournals.Server.deepCopy(storeData.entryStoreEntryCounts or {})
    preferred.entryStoreUpdatedAt = math.max(residentRevision, storeRevision)
    for _, bucketName in ipairs(BurdJournals.ENTRY_STORE_BUCKETS or {"skills", "traits", "stats", "recipes"}) do
        preferred[bucketName] = {}
    end
    preserveEntryStoreFallbackPlayerJournalIdentity(preferred, journalData, journalUUID, sourceTag or "preferRicherEntryStoreData")
    bsjWriteLogLine("[BurdJournals][EntryStoreFallback] preferred richer server entry store"
        .. " uuid=" .. tostring(journalUUID)
        .. " source=" .. tostring(sourceTag or "preferRicherEntryStoreData")
        .. " residentEntries=" .. tostring(residentCount)
        .. " storeEntries=" .. tostring(storeCount))
    return preferred, true
end

function BurdJournals.Server.preferEntryStoreDataForMissingEntry(journalData, args, bucketName, entryKey, sourceTag)
    if type(journalData) ~= "table" or type(bucketName) ~= "string" or type(entryKey) ~= "string" or entryKey == "" then
        return journalData
    end
    local bucket = journalData[bucketName]
    local resolvedKey = entryKey
    if bucketName == "skills" and type(bucket) == "table" and BurdJournals.resolveSkillKey then
        resolvedKey = BurdJournals.resolveSkillKey(bucket, entryKey) or entryKey
    end
    if type(bucket) == "table" and bucket[resolvedKey] ~= nil then
        return journalData
    end

    local fallbackUUID = type(journalData.entryStoreUUID) == "string" and journalData.entryStoreUUID ~= "" and journalData.entryStoreUUID
        or (type(journalData.uuid) == "string" and journalData.uuid ~= "" and journalData.uuid)
        or (type(journalData.replacedByJournalUUID) == "string" and journalData.replacedByJournalUUID ~= "" and journalData.replacedByJournalUUID)
        or (type(args) == "table" and type(args.journalUUID) == "string" and args.journalUUID ~= "" and args.journalUUID)
        or (type(args) == "table" and type(args.entryStoreUUID) == "string" and args.entryStoreUUID ~= "" and args.entryStoreUUID)
    local function fallbackHasEntry(candidateData)
        local candidateBucket = type(candidateData) == "table" and candidateData[bucketName] or nil
        local candidateKey = entryKey
        if bucketName == "skills" and type(candidateBucket) == "table" and BurdJournals.resolveSkillKey then
            candidateKey = BurdJournals.resolveSkillKey(candidateBucket, entryKey) or entryKey
        end
        return type(candidateBucket) == "table" and candidateBucket[candidateKey] ~= nil
    end

    local fallbackJournalData = fallbackUUID
        and BurdJournals.Server.buildEntryStoreOnlyJournalData
        and BurdJournals.Server.buildEntryStoreOnlyJournalData(fallbackUUID, sourceTag or "missingEntryStoreFallback")
        or nil
    if not fallbackHasEntry(fallbackJournalData)
        and fallbackUUID
        and BurdJournals.Server.readJournalEntryFileBackup
    then
        local fileData = BurdJournals.Server.readJournalEntryFileBackup(fallbackUUID, sourceTag or "missingEntryStoreFallbackFile")
        if fallbackHasEntry(fileData) then
            fallbackJournalData = fileData
        end
    end
    if fallbackHasEntry(fallbackJournalData) then
        fallbackJournalData._entryStoreFallbackSource = tostring(sourceTag or "missingEntryStoreFallback")
        preserveEntryStoreFallbackPlayerJournalIdentity(fallbackJournalData, journalData, fallbackUUID, sourceTag or "missingEntryStoreFallback")
        bsjWriteLogLine("[BurdJournals][EntryStoreFallback] restored missing claim entry"
            .. " uuid=" .. tostring(fallbackUUID)
            .. " bucket=" .. tostring(bucketName)
            .. " key=" .. tostring(entryKey)
            .. " source=" .. tostring(sourceTag or "missingEntryStoreFallback")
            .. " counts=" .. tostring(BurdJournals.Server.countJournalPayloadEntries(fallbackJournalData)))
        return fallbackJournalData
    end
    return journalData
end

local function shouldSkipPlayerJournalRegistryTouchOnInitialServerTouch(sourceTag)
    local tag = tostring(sourceTag or "")
    return tag == "recordProgress"
        or tag == "requestJournalEntryChunk"
        or tag == "sanitizeJournal"
        or tag == "syncJournalData"
        or tag == "eraseEntry"
end

local function shouldSkipEntryStoreHydrationOnServerTouch(sourceTag)
    local tag = tostring(sourceTag or "")
    return tag == "recordProgress"
        or tag == "requestJournalEntryChunk"
        or tag == "sanitizeJournal"
        or tag == "syncJournalData"
end

local function isStrictMPEmptyJournalShellData(journalData)
    return isStrictMPServer()
        and type(journalData) == "table"
        and journalData.entryStoreEnabled ~= true
        and not (journalData.isPlayerCreated == true and journalData.isWritten == false)
        and BurdJournals.Server.countJournalPayloadEntries(journalData) <= 0
end

local function bsjHasAnyTableEntry(tbl)
    if type(tbl) ~= "table" then
        return false
    end
    for _ in pairs(tbl) do
        return true
    end
    return false
end

function BurdJournals.Server.narrowRuntimeDeltaForClaimPayload(runtimeDelta, payload, sourceTag)
    if type(runtimeDelta) ~= "table" or type(payload) ~= "table" then
        return runtimeDelta
    end
    local tag = tostring(sourceTag or "")
    local isClaimResponse = string.find(tag, "claim", 1, true) ~= nil
        or string.find(tag, "Claim", 1, true) ~= nil
        or string.find(tag, "absorb", 1, true) ~= nil
        or string.find(tag, "Absorb", 1, true) ~= nil
        or string.find(tag, "AlreadyKnown", 1, true) ~= nil
        or string.find(tag, "Maxed", 1, true) ~= nil
    if not isClaimResponse then
        return runtimeDelta
    end

    local characterId = runtimeDelta.characterId
    local sourceClaims = nil
    if type(runtimeDelta.claims) == "table" then
        if characterId and type(runtimeDelta.claims[characterId]) == "table" then
            sourceClaims = runtimeDelta.claims[characterId]
        else
            for claimCharacterId, claimData in pairs(runtimeDelta.claims) do
                if type(claimData) == "table" then
                    characterId = claimCharacterId
                    sourceClaims = claimData
                    break
                end
            end
        end
    end

    local narrowedClaim = {}
    local skillName = type(payload.skillName) == "string" and payload.skillName or nil
    local traitId = type(payload.traitId) == "string" and payload.traitId or nil
    local recipeName = type(payload.recipeName) == "string" and payload.recipeName or nil
    local statId = type(payload.statId) == "string" and payload.statId or nil

    if type(sourceClaims) == "table" then
        if skillName and type(sourceClaims.skills) == "table" and sourceClaims.skills[skillName] ~= nil then
            narrowedClaim.skills = {[skillName] = sourceClaims.skills[skillName]}
        end
        if traitId and type(sourceClaims.traits) == "table" and sourceClaims.traits[traitId] ~= nil then
            narrowedClaim.traits = {[traitId] = sourceClaims.traits[traitId]}
        end
        if recipeName and type(sourceClaims.recipes) == "table" and sourceClaims.recipes[recipeName] ~= nil then
            narrowedClaim.recipes = {[recipeName] = sourceClaims.recipes[recipeName]}
        end
        if statId and type(sourceClaims.stats) == "table" and sourceClaims.stats[statId] ~= nil then
            narrowedClaim.stats = {[statId] = sourceClaims.stats[statId]}
        end
        if traitId and type(sourceClaims.forgetSlots) == "table" and sourceClaims.forgetSlots[traitId] ~= nil then
            narrowedClaim.forgetSlots = {[traitId] = sourceClaims.forgetSlots[traitId]}
        end
        if skillName and type(sourceClaims.drSkillReadCounts) == "table" and sourceClaims.drSkillReadCounts[skillName] ~= nil then
            narrowedClaim.drSkillReadCounts = {[skillName] = sourceClaims.drSkillReadCounts[skillName]}
        end
    end

    local narrowedSkillReads = nil
    if skillName and type(runtimeDelta.skillReadCounts) == "table" and runtimeDelta.skillReadCounts[skillName] ~= nil then
        narrowedSkillReads = {[skillName] = runtimeDelta.skillReadCounts[skillName]}
    end

    if not bsjHasAnyTableEntry(narrowedClaim) and not narrowedSkillReads then
        return runtimeDelta
    end

    local narrowed = {
        version = runtimeDelta.version,
        characterId = characterId or runtimeDelta.characterId,
        readCount = runtimeDelta.readCount,
        readSessionCount = runtimeDelta.readSessionCount,
        currentSessionId = runtimeDelta.currentSessionId,
        currentSessionReadCount = runtimeDelta.currentSessionReadCount,
        skillReadCounts = narrowedSkillReads or {},
        drLegacyMode3Migrated = runtimeDelta.drLegacyMode3Migrated == true,
    }
    if characterId and bsjHasAnyTableEntry(narrowedClaim) then
        narrowed.claims = {
            [characterId] = narrowedClaim
        }
    else
        narrowed.claims = {}
    end
    return narrowed
end

local function safeTransmitServerJournalModData(journal, sourceTag)
    if not (journal and journal.transmitModData) then
        return false
    end
    if isStrictMPServer()
        and journal.getModData
        and BurdJournals.offloadJournalEntriesToStoreIfNeeded
    then
        local modData = journal:getModData()
        local journalData = modData and modData.BurdJournals or nil
        local residentEntries = type(journalData) == "table"
            and BurdJournals.countJournalEntryStoreEntries
            and BurdJournals.countJournalEntryStoreEntries(journalData) or 0
        if type(journalData) == "table" and journalData.entryStoreEnabled == true and residentEntries > 0 then
            BurdJournals.offloadJournalEntriesToStoreIfNeeded(
                journal,
                "preTransmit:" .. tostring(sourceTag or "server"),
                true,
                true
            )
        end
    end
    if BurdJournals.shouldTransmitJournalItemModData
        and not BurdJournals.shouldTransmitJournalItemModData(journal, sourceTag or "server")
    then
        return false
    end
    journal:transmitModData()
    return true
end

local function maybeMigrateRuntimeOnTouch(journal, player, sourceTag)
    -- Hover/preview/open requests must observe the identity chosen at spawn;
    -- they must never turn an explicitly sealed journal into a hidden one.
    local presentationOnlyCursedTouch = sourceTag == "requestCursedInsightPreview"
        or sourceTag == "openCursedJournal"
        or sourceTag == "openCursedJournalConfirm"
    if not presentationOnlyCursedTouch
        and BurdJournals.Server
        and BurdJournals.Server.ensureSandboxHiddenCursedBootstrap
    then
        BurdJournals.Server.ensureSandboxHiddenCursedBootstrap(journal, sourceTag)
    end
    if journal and journal.getModData
        and BurdJournals.hydrateJournalDataFromEntryStore
        and not shouldSkipEntryStoreHydrationOnServerTouch(sourceTag)
    then
        local modData = journal:getModData()
        if modData and type(modData.BurdJournals) == "table" then
            BurdJournals.hydrateJournalDataFromEntryStore(modData.BurdJournals, sourceTag or "serverTouch")
        end
    end
    if BurdJournals.migrateJournalRuntimeToGlobalIfNeeded then
        BurdJournals.migrateJournalRuntimeToGlobalIfNeeded(journal, player, sourceTag)
    end
    if BurdJournals.Server
        and BurdJournals.Server.rejectStaleRespawnJournalInstance
        and BurdJournals.Server.rejectStaleRespawnJournalInstance(player, journal, sourceTag)
    then
        return false
    end
    if BurdJournals.Server
        and BurdJournals.Server.touchPlayerJournalRegistry
        and not shouldSkipPlayerJournalRegistryTouchOnInitialServerTouch(sourceTag)
    then
        BurdJournals.Server.touchPlayerJournalRegistry(journal, player, sourceTag)
    end
    return true
end

local function getJournalLightRequirementMessage()
    local tooDarkText = (getText and getText("ContextMenu_TooDark")) or "Too dark to read."
    if tooDarkText == "ContextMenu_TooDark" then
        tooDarkText = "Too dark to read."
    end
    return tooDarkText
end

local function enforceJournalLightRequirement(player, sourceTag)
    if not (BurdJournals.requiresLightForJournalUse and BurdJournals.requiresLightForJournalUse()) then
        return true
    end

    local canUse = true
    local reason = nil
    if BurdJournals.canUseJournalInCurrentLight then
        canUse, reason = BurdJournals.canUseJournalInCurrentLight(player)
    elseif not player or (player.tooDarkToRead and player:tooDarkToRead()) then
        canUse = false
        reason = getJournalLightRequirementMessage()
    end

    if canUse ~= false then
        return true
    end

    BurdJournals.debugPrint("[BurdJournals] " .. tostring(sourceTag or "journalLightRequirement")
        .. ": blocked by RequireLightForJournalUse")
    BurdJournals.Server.sendToClient(player, "error", {
        message = reason or getJournalLightRequirementMessage()
    })
    return false
end

function BurdJournals.Server.validatePlayerJournalMutation(player, journal, options)
    options = type(options) == "table" and options or {}
    if not player or not journal or not journal.getModData then
        return false, "invalidJournal", "Invalid journal."
    end

    local modData = journal:getModData()
    local journalData = modData and modData.BurdJournals or nil
    local isBlank = BurdJournals.isBlankJournal and BurdJournals.isBlankJournal(journal) == true
    local isPlayerJournal = type(journalData) == "table"
        and ((BurdJournals.isEffectivelyPlayerJournal and BurdJournals.isEffectivelyPlayerJournal(journalData) == true)
            or journalData.isPlayerCreated == true)
    local invalidState = (BurdJournals.isClean and BurdJournals.isClean(journal) ~= true)
        or (BurdJournals.isCursedJournalItem and BurdJournals.isCursedJournalItem(journal) == true)
        or (BurdJournals.isHiddenCursedJournal and BurdJournals.isHiddenCursedJournal(journal) == true)
        or (type(journalData) == "table" and (journalData.isCursedJournal == true or journalData.isCursedReward == true))
        or (BurdJournals.isYuletideJournalItem and BurdJournals.isYuletideJournalItem(journal) == true)
    if invalidState then
        return false, "journalNotWritable", "Only clean personal journals can be modified."
    end
    if isBlank then
        if options.allowBlank ~= true then
            return false, "journalNotWritable", "This operation requires a written personal journal."
        end
    elseif not isPlayerJournal then
        return false, "journalNotWritable", "Only personal journals can be modified."
    end

    local hasOwnership = type(journalData) == "table"
        and (journalData.ownerSteamId or journalData.ownerUsername or journalData.ownerCharacterName or journalData.author)
    if (isPlayerJournal or hasOwnership)
        and BurdJournals.isJournalOwner
        and BurdJournals.isJournalOwner(player, journal) ~= true
    then
        return false, "notJournalOwner", "Only the journal owner can modify it."
    end
    return true, nil, nil
end

local function addJournalItemToContainerCompat(container, itemType)
    if not container or type(itemType) ~= "string" or itemType == "" then
        return nil
    end

    -- Create once, then insert once. Retrying a stateful AddItem call after a
    -- protected-call failure can duplicate an item when a hook throws after
    -- the container was already mutated.
    if InventoryItemFactory and InventoryItemFactory.CreateItem and container.AddItem then
        local item = InventoryItemFactory.CreateItem(itemType)
        if item then
            return container:AddItem(item) or item
        end
    end

    if container.AddItem then
        return container:AddItem(itemType)
    end

    return nil
end

local function hiddenCursedBootstrapHasMeaningfulData(journalData)
    if type(journalData) ~= "table" then
        return false
    end

    local meaningfulKeys = {
        "uuid",
        "author",
        "profession",
        "professionName",
        "flavorKey",
        "flavorText",
        "loreNoteText",
        "timestamp",
        "cursedEffectType",
    }
    for _, key in ipairs(meaningfulKeys) do
        local value = journalData[key]
        if value ~= nil and value ~= false and tostring(value) ~= "" then
            return true
        end
    end

    if BurdJournals.hasAnyEntries then
        if BurdJournals.hasAnyEntries(journalData.skills)
            or BurdJournals.hasAnyEntries(journalData.traits)
            or BurdJournals.hasAnyEntries(journalData.recipes)
            or BurdJournals.hasAnyEntries(journalData.stats)
            or BurdJournals.hasAnyEntries(journalData.cursedPendingRewards)
        then
            return true
        end
    end

    return journalData.forgetSlot == true
end

local function isRawCursedShellDataForHiddenBootstrap(journalData)
    if type(journalData) ~= "table" then
        return true
    end
    if journalData.isCursedReward == true or journalData.isHiddenCursedJournal == true then
        return false
    end
    if (journalData.isDebugSpawned == true or journalData.debugBackupEnabled == true)
        and (journalData.isCursedJournal == true or journalData.cursedState == "dormant")
    then
        return false
    end
    if journalData.cursedEffectType ~= nil or journalData.cursedForcedEffectType ~= nil then
        return false
    end
    if journalData.author ~= nil
        or journalData.profession ~= nil
        or journalData.professionName ~= nil
        or journalData.flavorKey ~= nil
        or journalData.flavorText ~= nil
        or journalData.loreNoteText ~= nil
    then
        return false
    end
    if BurdJournals.hasAnyEntries then
        if BurdJournals.hasAnyEntries(journalData.skills)
            or BurdJournals.hasAnyEntries(journalData.traits)
            or BurdJournals.hasAnyEntries(journalData.recipes)
            or BurdJournals.hasAnyEntries(journalData.stats)
            or BurdJournals.hasAnyEntries(journalData.cursedPendingRewards)
        then
            return false
        end
    end

    return journalData.forgetSlot ~= true
end

function BurdJournals.Server.isPlaceholderHiddenCursedAuthor(author)
    local normalizedAuthor = tostring(author or "")
    local unknownAuthorText = getText and getText("UI_BurdJournals_UnknownSurvivor") or "UI_BurdJournals_UnknownSurvivor"
    return normalizedAuthor == ""
        or normalizedAuthor == unknownAuthorText
        or normalizedAuthor == "Unknown Survivor"
end

function BurdJournals.Server.isPlaceholderHiddenCursedProfession(professionId, professionName)
    local normalizedProfession = tostring(professionId or "")
    local normalizedProfessionName = tostring(professionName or "")
    local unknownProfessionText = getText and getText("UI_BurdJournals_UnknownProfession") or "UI_BurdJournals_UnknownProfession"
    return normalizedProfessionName == ""
        or normalizedProfessionName == unknownProfessionText
        or normalizedProfessionName == "Unknown Profession"
        or (string.lower(normalizedProfession) == "survivor" and normalizedProfessionName == "Survivor")
end

function BurdJournals.Server.syncHiddenCursedPendingLoreIdentity(data)
    if type(data) ~= "table" or type(data.cursedPendingRewards) ~= "table" then
        return false
    end

    local pending = data.cursedPendingRewards
    local changed = false
    local rootAuthor = tostring(data.author or "")
    local rootProfession = tostring(data.profession or "")
    local rootProfessionName = tostring(data.professionName or "")

    if rootAuthor ~= ""
        and not BurdJournals.Server.isPlaceholderHiddenCursedAuthor(rootAuthor)
        and pending.author ~= rootAuthor
    then
        pending.author = rootAuthor
        changed = true
    end

    if not BurdJournals.Server.isPlaceholderHiddenCursedProfession(rootProfession, rootProfessionName) then
        if rootProfession ~= "" and pending.profession ~= rootProfession then
            pending.profession = rootProfession
            changed = true
        end
        if rootProfessionName ~= "" and pending.professionName ~= rootProfessionName then
            pending.professionName = rootProfessionName
            changed = true
        end
    end

    for _, key in ipairs({
        "flavorKey",
        "loreNoteText",
        "loreNoteTemplateVersion",
        "loreNoteTemplateFamily",
    }) do
        if data[key] ~= nil and pending[key] ~= data[key] then
            pending[key] = data[key]
            changed = true
        end
    end

    return changed
end

function BurdJournals.Server.ensureSandboxHiddenCursedBootstrap(journal, sourceTag, forceRawDisguise)
    if not (journal and journal.getModData and journal.getFullType) then
        return false
    end
    if not (BurdJournals.isDisguiseCursedJournalsAsBloodyEnabled and BurdJournals.isDisguiseCursedJournalsAsBloodyEnabled()) then
        return false
    end

    local cursedType = BurdJournals.CURSED_ITEM_TYPE or "BurdJournals.CursedJournal"
    local fullType = tostring(journal:getFullType() or "")
    if fullType ~= cursedType then
        return false
    end

    local modData = journal:getModData()
    modData.BurdJournals = modData.BurdJournals or {}
    local data = modData.BurdJournals

    local forceShellDisguise = forceRawDisguise == true and isRawCursedShellDataForHiddenBootstrap(data)

    if data.isCursedReward == true then
        return false
    end
    if (data.isCursedJournal == true or data.isHiddenCursedJournal == true) and not forceShellDisguise then
        return false
    end
    if hiddenCursedBootstrapHasMeaningfulData(data) and not forceShellDisguise then
        return false
    end

    local professionId, professionName, flavorKey
    if BurdJournals.getRandomProfession then
        professionId, professionName, flavorKey = BurdJournals.getRandomProfession()
    else
        professionId, professionName, flavorKey = "survivor", "Survivor", "UI_BurdJournals_BloodyFlavor"
    end
    local disguisedSeed = {
        uuid = data.uuid
            or ((BurdJournals.resolveJournalUUIDForRuntime and BurdJournals.resolveJournalUUIDForRuntime(data, journal, true))
                or (BurdJournals.generateUUID and BurdJournals.generateUUID())
                or ("cursed-" .. tostring(ZombRand(999999999)))),
        timestamp = tonumber(data.timestamp)
            or (getGameTime and getGameTime() and getGameTime():getWorldAgeHours())
            or 0,
        author = data.author
            or ((BurdJournals.generateRandomSurvivorName and BurdJournals.generateRandomSurvivorName()) or "Unknown Survivor"),
        profession = data.profession or professionId,
        professionName = data.professionName or professionName,
        flavorKey = data.flavorKey or flavorKey or "UI_BurdJournals_BloodyFlavor",
        lootNotesEligible = data.lootNotesEligible == true,
    }
    data.cursedPendingRewards = data.cursedPendingRewards or {
        uuid = disguisedSeed.uuid,
        timestamp = disguisedSeed.timestamp,
        author = disguisedSeed.author,
        profession = disguisedSeed.profession,
        professionName = disguisedSeed.professionName,
        flavorKey = disguisedSeed.flavorKey,
        lootNotesEligible = data.lootNotesEligible == true,
        cursedDeferredRewards = true,
    }

    data.uuid = data.uuid or disguisedSeed.uuid
    data.timestamp = tonumber(data.timestamp) or disguisedSeed.timestamp
    data.author = data.author or disguisedSeed.author
    data.profession = data.profession or disguisedSeed.profession
    data.professionName = data.professionName or disguisedSeed.professionName
    data.flavorKey = data.flavorKey or disguisedSeed.flavorKey
    local insightEffectType = type(data.cursedInsightEffectType) == "string" and data.cursedInsightEffectType ~= "" and data.cursedInsightEffectType
        or (BurdJournals.Server.rollCursedInsightEffectType and BurdJournals.Server.rollCursedInsightEffectType(nil, nil))
        or nil
    if insightEffectType then
        data.cursedInsightEffectType = insightEffectType
        if BurdJournals.Server.getCurseEffectOmenCategory then
            data.cursedOmenCategory = BurdJournals.Server.getCurseEffectOmenCategory(insightEffectType)
        end
    end
    data.isHiddenCursedJournal = true
    data.isCursedJournal = false
    data.cursedState = "hidden"
    -- Expired curses: record spawn time so old curses can go dormant.
    data.cursedSpawnedAtHours = data.cursedSpawnedAtHours
        or (BurdJournals.Server.getWorldAgeHoursSafe and BurdJournals.Server.getWorldAgeHoursSafe())
        or nil
    data.isCursedReward = false
    data.cursedEffectType = nil
    data.cursedUnleashedByCharacterId = nil
    data.cursedUnleashedByUsername = nil
    data.cursedUnleashedAtHours = nil
    data.cursedSealSoundEvent = nil
    data.cursedForcedEffectType = nil
    data.cursedForcedTraitId = nil
    data.cursedForcedSkillName = nil
    data.isBloody = true
    data.isWorn = false
    data.wasFromBloody = true
    data.hasBloodyOrigin = true
    data.isPlayerCreated = false
    data.isZombieJournal = true
    data.loreNoteTemplateVersion = tonumber(BurdJournals.Server and BurdJournals.Server.LORE_NOTE_TEMPLATE_VERSION) or 1
    data.loreNoteTemplateFamily = "bloody"
    data.loreNoteText = nil
    data.claims = data.claims or {}
    data.claimedSkills = data.claimedSkills or {}
    data.claimedTraits = data.claimedTraits or {}
    data.claimedRecipes = data.claimedRecipes or {}
    data.claimedForgetSlot = data.claimedForgetSlot or {}

    if BurdJournals.updateJournalName then
        BurdJournals.updateJournalName(journal, true)
    end
    if BurdJournals.updateJournalIcon then
        BurdJournals.updateJournalIcon(journal)
    end
    if journal.transmitModData and not BurdJournals.Server.shouldDeferBatchTransmit() then
        safeTransmitServerJournalModData(journal, "hiddenCursedBootstrap")
    end
    BurdJournals.debugPrint("[BurdJournals] " .. tostring(sourceTag or "hiddenCursedBootstrap")
        .. ": bootstrapped raw cursed item into hidden bloody disguise")
    return true
end

function BurdJournals.Server.materializeSandboxHiddenCursedBloodyItemIfNeeded(player, journal, sourceTag)
    if not (journal and journal.getFullType and journal.getModData) then
        return journal, false
    end
    if not (BurdJournals.isDisguiseCursedJournalsAsBloodyEnabled and BurdJournals.isDisguiseCursedJournalsAsBloodyEnabled()) then
        return journal, false
    end

    local cursedType = BurdJournals.CURSED_ITEM_TYPE or "BurdJournals.CursedJournal"
    local fullType = tostring(journal:getFullType() or "")
    if fullType ~= cursedType then
        return journal, false
    end

    if BurdJournals.Server.ensureSandboxHiddenCursedBootstrap then
        BurdJournals.Server.ensureSandboxHiddenCursedBootstrap(journal, sourceTag or "hiddenCursedMaterialize", true)
    end

    local sourceModData = journal:getModData()
    local sourceData = sourceModData and sourceModData.BurdJournals or nil
    if type(sourceData) ~= "table"
        or sourceData.isHiddenCursedJournal ~= true
        or sourceData.isCursedReward == true
    then
        return journal, false
    end

    local sourceContainer = journal.getContainer and journal:getContainer() or nil
    local inventory = player and player.getInventory and player:getInventory() or nil
    -- Defer materialization while the journal sits inside a bag / nested container.
    -- The remove+add item swap below, run against a nested ItemContainer, corrupts
    -- the client container UI (the inventory renders as an empty black box and the
    -- bag desyncs). The hidden state stays valid and claims remain blocked, so this
    -- simply retries on the next server touch once the item is moved or opened.
    if sourceContainer and sourceContainer ~= inventory and sourceContainer.getContainingItem then
        if sourceContainer:getContainingItem() ~= nil then
            BurdJournals.debugPrint("[BurdJournals] Deferring hidden cursed materialization: journal is in a nested container ("
                .. tostring(sourceTag) .. ")")
            return journal, false
        end
    end
    local targetContainer = sourceContainer or inventory
    if not (targetContainer and targetContainer.AddItem) then
        return journal, false
    end

    local oldJournalId = journal.getID and journal:getID() or nil
    local oldJournalUUID = sourceData.uuid
    local replacement = addJournalItemToContainerCompat(targetContainer, "BurdJournals.FilledSurvivalJournal_Bloody")
    if not replacement and inventory and inventory ~= targetContainer then
        replacement = addJournalItemToContainerCompat(inventory, "BurdJournals.FilledSurvivalJournal_Bloody")
        if replacement then
            targetContainer = inventory
        end
    end
    if not replacement then
        return journal, false
    end

    local copiedData = BurdJournals.Server.deepCopy and BurdJournals.Server.deepCopy(sourceData) or sourceData
    if BurdJournals.normalizeJournalData then
        copiedData = BurdJournals.normalizeJournalData(copiedData)
    end
    copiedData.isHiddenCursedJournal = true
    copiedData.isCursedJournal = false
    copiedData.isCursedReward = false
    copiedData.cursedState = "hidden"
    -- Expired curses: record spawn time so old curses can go dormant.
    copiedData.cursedSpawnedAtHours = copiedData.cursedSpawnedAtHours
        or (BurdJournals.Server.getWorldAgeHoursSafe and BurdJournals.Server.getWorldAgeHoursSafe())
        or nil
    copiedData.isBloody = true
    copiedData.wasFromBloody = true
    copiedData.hasBloodyOrigin = true
    copiedData.isPlayerCreated = false
    copiedData.isZombieJournal = true

    local replacementModData = replacement:getModData()
    replacementModData.BurdJournals = copiedData

    if player then
        BurdJournals.safePcall(function()
            if player.getPrimaryHandItem and player:getPrimaryHandItem() == journal then
                player:setPrimaryHandItem(nil)
            end
            if player.getSecondaryHandItem and player:getSecondaryHandItem() == journal then
                player:setSecondaryHandItem(nil)
            end
        end)
    end

    if sourceContainer and sourceContainer.Remove then
        sourceContainer:Remove(journal)
        if sourceContainer.setDrawDirty then
            sourceContainer:setDrawDirty(true)
        end
        if sendRemoveItemFromContainer then
            sendRemoveItemFromContainer(sourceContainer, journal)
        end
    elseif inventory and inventory.Remove and inventory.contains and inventory:contains(journal) then
        inventory:Remove(journal)
        if inventory.setDrawDirty then
            inventory:setDrawDirty(true)
        end
        if sendRemoveItemFromContainer then
            sendRemoveItemFromContainer(inventory, journal)
        end
    end

    if BurdJournals.updateJournalName then
        BurdJournals.updateJournalName(replacement, true)
    end
    if BurdJournals.updateJournalIcon then
        BurdJournals.updateJournalIcon(replacement)
    end
    if replacement.transmitModData then
        safeTransmitServerJournalModData(replacement, "hiddenCursedMaterialize")
    end
    if targetContainer and targetContainer.setDrawDirty then
        targetContainer:setDrawDirty(true)
    end
    if sendAddItemToContainer and targetContainer then
        sendAddItemToContainer(targetContainer, replacement)
    end
    if player and player.syncInventory then
        BurdJournals.safePcall(function()
            player:syncInventory()
        end)
    end
    if BurdJournals.Server.updateJournalUUIDIndex then
        BurdJournals.Server.updateJournalUUIDIndex(replacement, player, "hiddenCursedMaterialize")
    end

    BurdJournals.debugPrint("[BurdJournals] " .. tostring(sourceTag or "hiddenCursedMaterialize")
        .. ": materialized raw cursed item " .. tostring(oldJournalId)
        .. " as disguised bloody journal " .. tostring(replacement.getID and replacement:getID() or nil))

    return replacement, true, oldJournalId, oldJournalUUID, copiedData
end

local function normalizeCursedJournalStateForType(journal, sourceTag)
    if not (journal and journal.getModData and journal.getFullType) then
        return false
    end

    local fullType = tostring(journal:getFullType() or "")
    local cursedType = BurdJournals.CURSED_ITEM_TYPE or "BurdJournals.CursedJournal"
    if fullType ~= cursedType then
        return false
    end

    local modData = journal:getModData()
    modData.BurdJournals = modData.BurdJournals or {}
    local journalData = modData.BurdJournals
    local repaired = false

    if BurdJournals.isDisguiseCursedJournalsAsBloodyEnabled
        and BurdJournals.isDisguiseCursedJournalsAsBloodyEnabled()
        and journalData.isHiddenCursedJournal == true
        and journalData.isCursedReward ~= true
    then
        if journalData.isCursedJournal == true then
            journalData.isCursedJournal = false
            repaired = true
        end
        if journalData.cursedState ~= "hidden" then
            journalData.cursedState = "hidden"
            repaired = true
        end
        if journalData.isBloody ~= true then
            journalData.isBloody = true
            repaired = true
        end
        if journalData.wasFromBloody ~= true then
            journalData.wasFromBloody = true
            repaired = true
        end
        if journalData.hasBloodyOrigin ~= true then
            journalData.hasBloodyOrigin = true
            repaired = true
        end

        if repaired then
            if BurdJournals.updateJournalName then
                BurdJournals.updateJournalName(journal, true)
            end
            if BurdJournals.updateJournalIcon then
                BurdJournals.updateJournalIcon(journal)
            end
            if journal.transmitModData and not BurdJournals.Server.shouldDeferBatchTransmit() then
                safeTransmitServerJournalModData(journal, "normalizeCursedJournalStateForType")
            end
        end
        return repaired
    end

    if journalData.isHiddenCursedJournal == true then
        journalData.isHiddenCursedJournal = false
        repaired = true
    end

    if journalData.isCursedReward == true then
        if journalData.isCursedJournal == true then
            journalData.isCursedJournal = false
            repaired = true
        end
        if journalData.cursedState ~= "unleashed" then
            journalData.cursedState = "unleashed"
            repaired = true
        end
    else
        if journalData.isCursedJournal ~= true then
            journalData.isCursedJournal = true
            repaired = true
        end
        if journalData.cursedState ~= "dormant" then
            journalData.cursedState = "dormant"
            repaired = true
        end
        if journalData.isBloody == true then
            journalData.isBloody = false
            repaired = true
        end
        if journalData.wasFromBloody == true then
            journalData.wasFromBloody = false
            repaired = true
        end
        if journalData.hasBloodyOrigin == true then
            journalData.hasBloodyOrigin = false
            repaired = true
        end
    end

    if not repaired then
        return false
    end

    if BurdJournals.updateJournalName then
        BurdJournals.updateJournalName(journal, true)
    end
    if BurdJournals.updateJournalIcon then
        BurdJournals.updateJournalIcon(journal)
    end
    if journal.transmitModData and not BurdJournals.Server.shouldDeferBatchTransmit() then
        safeTransmitServerJournalModData(journal, "normalizeCursedJournalStateForType")
    end
    BurdJournals.debugPrint("[BurdJournals] " .. tostring(sourceTag or "cursedNormalize")
        .. ": normalized cursed journal state for item type")
    return true
end

local function isHiddenCursedJournalState(journal, journalData)
    if not journal or type(journalData) ~= "table" then
        return false
    end
    if journalData.isHiddenCursedJournal ~= true or journalData.isCursedReward == true then
        return false
    end

    local fullType = journal.getFullType and journal:getFullType() or ""
    return fullType == "BurdJournals.FilledSurvivalJournal_Bloody"
        or journalData.isBloody == true
        or journalData.wasFromBloody == true
        or journalData.hasBloodyOrigin == true
end

local function fullTypeHasToken(fullType, token)
    return type(fullType) == "string"
        and type(token) == "string"
        and token ~= ""
        and string.find(fullType, token, 1, true) ~= nil
end

local function isLikelyWornType(fullType)
    return fullTypeHasToken(fullType, "_Worn")
        or fullTypeHasToken(fullType, ".Worn")
        or fullTypeHasToken(fullType, "WornSurvivalJournal")
end

local function isLikelyBloodyType(fullType)
    return fullTypeHasToken(fullType, "_Bloody")
        or fullTypeHasToken(fullType, ".Bloody")
        or fullTypeHasToken(fullType, "BloodySurvivalJournal")
end

local function normalizeFoundJournalClaimFlags(journal, journalData, sourceTag)
    if not journal or type(journalData) ~= "table" then
        return false
    end

    normalizeCursedJournalStateForType(journal, sourceTag)

    local fullType = journal.getFullType and tostring(journal:getFullType() or "") or ""
    local isWornType = (BurdJournals.isWorn and BurdJournals.isWorn(journal)) or isLikelyWornType(fullType)
    local isBloodyType = (BurdJournals.isBloody and BurdJournals.isBloody(journal)) or isLikelyBloodyType(fullType)
    local isYuletideType = fullType == (BurdJournals.YULETIDE_ITEM_TYPE or "BurdJournals.YuletideJournal")
    local repaired = false

    if isWornType and journalData.isWorn ~= true then
        journalData.isWorn = true
        journalData.wasFromWorn = true
        repaired = true
    end
    if isBloodyType and journalData.isBloody ~= true then
        journalData.isBloody = true
        journalData.wasFromBloody = true
        repaired = true
    end
    local explicitPersonalOrigin = tostring(journalData.originMode or journalData.sourceType or "") == "personal"
    if (isWornType or isBloodyType or isYuletideType or journalData.isYuletideJournal == true)
        and ((BurdJournals.isEffectivelyPlayerJournal and BurdJournals.isEffectivelyPlayerJournal(journalData))
            or journalData.isPlayerCreated == true)
        and not explicitPersonalOrigin
    then
        journalData.isPlayerCreated = false
        repaired = true
        BurdJournals.debugPrint("[BurdJournals] " .. tostring(sourceTag or "claimNormalize")
            .. ": repaired misclassified found journal (forced isPlayerCreated=false)")
    end

    if repaired and journal.transmitModData then
        safeTransmitServerJournalModData(journal, "normalizeFoundJournalClaimFlags")
    end
    return repaired
end

local function attachRuntimeDeltaOrLegacyJournalData(payload, journalData, player, sourceTag)
    payload = payload or {}
    payload.serverRuntimeMarker = payload.serverRuntimeMarker or BurdJournals.RUNTIME_LOAD_MARKER
    if type(journalData) ~= "table" then
        payload.needsSync = true
        return payload
    end

    local canonicalUUID = BurdJournals.getJournalIdentityUUID(journalData)
    if canonicalUUID then
        payload.journalUUID = canonicalUUID
    end

    if isStrictMPEmptyJournalShellData(journalData) then
        payload.needsSync = true
        payload.fullJournalDataOmitted = true
        payload.fullJournalDataOmittedReason = "emptyShell"
        payload.entryStoreUnavailable = true
        BurdJournals.debugPrint("[BurdJournals] " .. tostring(sourceTag or "runtimeResponse")
            .. ": refused empty strict-MP journal shell in response payload")
        return payload
    end

    if not isStrictMPServer() then
        payload.journalData = journalData
        return payload
    end

    -- Strict-MP entry-store journals only keep a compact shell on the item.
    -- Every response path using this helper (notably sanitizeJournal and batch
    -- claim/absorb) must advertise that omission so the client hydrates the
    -- authoritative entry store instead of refreshing the UI from that shell.
    if journalData.entryStoreEnabled == true then
        local expectedEntryStoreCount = BurdJournals.Server.countEntryStoreManifestEntries(journalData)
        local actualEntryStoreCount = nil
        if BurdJournals.refreshJournalEntryStoreCounts then
        -- Capture only the first return value. Calling tonumber() with this
        -- multi-return function directly also passes the counts table as the
        -- radix argument under Kahlua, crashing the success-response path.
        local refreshedEntryStoreCount = BurdJournals.refreshJournalEntryStoreCounts(
            journalData, tostring(sourceTag or "runtimeResponse") .. "Counts")
        actualEntryStoreCount = tonumber(refreshedEntryStoreCount) or 0
        end
        payload.entryStoreEnabled = true
        payload.entryStoreEntryCounts = type(journalData.entryStoreEntryCounts) == "table"
            and BurdJournals.Server.deepCopy(journalData.entryStoreEntryCounts) or nil
        payload.entryStoreUpdatedAt = tonumber(journalData.entryStoreUpdatedAt)
        payload.entryStoreActualCount = actualEntryStoreCount
        payload.fullJournalDataOmitted = true
        payload.fullJournalDataOmittedReason = "entryStore"
        payload.entryStoreUnavailable = expectedEntryStoreCount > 0
            and actualEntryStoreCount ~= nil and actualEntryStoreCount <= 0 or false
    end

    local runtimeDelta = BurdJournals.buildRuntimeDeltaForPlayer and BurdJournals.buildRuntimeDeltaForPlayer(journalData, player) or nil
    if type(runtimeDelta) == "table" then
        runtimeDelta = BurdJournals.Server.narrowRuntimeDeltaForClaimPayload
            and BurdJournals.Server.narrowRuntimeDeltaForClaimPayload(runtimeDelta, payload, sourceTag)
            or runtimeDelta
        payload.runtimeDelta = runtimeDelta
    end

    local softLimit = math.max(1024, tonumber(BurdJournals.FULL_SYNC_SOFT_LIMIT_BYTES) or 48000)
    local estimate = estimatePayloadBytes(payload)
    if estimate > softLimit then
        payload.runtimeDelta = nil
        payload.needsSync = true
        BurdJournals.debugPrint("[BurdJournals] " .. tostring(sourceTag or "runtimeResponse")
            .. ": payload soft-limit exceeded (" .. tostring(estimate) .. " > " .. tostring(softLimit) .. "), forcing needsSync")
    end
    return payload
end

local function buildBatchJournalResponse(payload, player, journalId, journalUUID, journalData, sourceTag)
    local response = attachRuntimeDeltaOrLegacyJournalData(payload or {}, journalData, player, sourceTag)
    if journalId ~= nil then
        response.journalId = journalId
    end
    if journalUUID and response.journalUUID == nil then
        response.journalUUID = journalUUID
    end
    return response
end

local function normalizeCommandJournalUUID(args)
    if type(args) ~= "table" then
        return nil
    end

    local uuid = args.journalUUID or args.entryStoreUUID
    if (uuid == nil or uuid == "") and type(args.journalData) == "table" then
        uuid = args.journalData.uuid or args.journalData.entryStoreUUID
    end
    if uuid == nil or uuid == "" then
        uuid = args.journalKey
    end
    if uuid == nil then
        return nil
    end

    uuid = tostring(uuid)
    if string.len(uuid) > 128 or string.find(uuid, "[%z\1-\31\127]") then return nil end
    uuid = uuid:gsub("^%s+", ""):gsub("%s+$", "")
    if uuid == "" or uuid == "nil" or string.len(uuid) > 128 then return nil end
    return uuid
end

local function normalizeCommandJournalFingerprint(args)
    if type(args) ~= "table" then
        return nil
    end

    local fingerprint = args.journalFingerprint
    if fingerprint == nil or fingerprint == "" then
        fingerprint = args.journalLookupFingerprint
    end
    if fingerprint == nil then
        return nil
    end

    fingerprint = tostring(fingerprint)
    if string.len(fingerprint) > 4096 or string.find(fingerprint, "[%z\1-\31\127]") then return nil end
    fingerprint = fingerprint:gsub("^%s+", ""):gsub("%s+$", "")
    if fingerprint == "" or fingerprint == "nil" or string.len(fingerprint) > 4096 then return nil end
    return fingerprint
end

local function hasAnyJournalLookupArgs(args)
    if type(args) ~= "table" then
        return false
    end
    return args.journalId ~= nil
        or normalizeCommandJournalUUID(args) ~= nil
        or normalizeCommandJournalFingerprint(args) ~= nil
end

local function resolveCommandJournalUUIDFromIndex(player, args)
    if type(args) ~= "table" or args.journalId == nil then
        return nil
    end
    if type(ModData) ~= "table" then
        return nil
    end
    if not (BurdJournals.Server and BurdJournals.Server.getJournalUUIDIndex) then
        return nil
    end
    local wantedId = tostring(args.journalId)
    local indexCache = BurdJournals.Server.getJournalUUIDIndex()
    local journals = indexCache and indexCache.journals or nil
    if type(journals) ~= "table" then
        return nil
    end
    local requestedType = type(args.itemFullType) == "string" and args.itemFullType ~= "" and args.itemFullType or nil
    local playerSteamId = player and BurdJournals.getPlayerSteamId
        and BurdJournals.Server.normalizeRespawnIdentityValue(BurdJournals.getPlayerSteamId(player)) or nil
    local ownedMatches, fallbackMatches = {}, {}
    for uuid, entry in pairs(journals) do
        if type(entry) == "table" and entry.itemId ~= nil and tostring(entry.itemId) == wantedId
            and (not requestedType or tostring(entry.itemType or "") == requestedType) then
            local entryOwner = BurdJournals.Server.normalizeRespawnIdentityValue(entry.ownerSteamId)
            local normalizedUUID = trimUUID(uuid)
            if normalizedUUID then
                if playerSteamId and entryOwner == playerSteamId then
                    ownedMatches[#ownedMatches + 1] = normalizedUUID
                elseif not playerSteamId or not entryOwner then
                    fallbackMatches[#fallbackMatches + 1] = normalizedUUID
                end
            end
        end
    end
    local matches = #ownedMatches > 0 and ownedMatches or fallbackMatches
    if #matches == 1 then return matches[1] end
    if #matches > 1 then
        BurdJournals.debugPrint("[BurdJournals] UUID index lookup rejected ambiguous itemId="
            .. tostring(wantedId) .. " matches=" .. tostring(#matches))
    end
    return nil
end

local function getJournalFingerprintToken(fingerprint, token)
    if type(fingerprint) ~= "string" or fingerprint == "" or type(token) ~= "string" or token == "" then
        return nil
    end
    local value = fingerprint:match(token .. "=([^|]+)")
    if value == nil or value == "" then
        return nil
    end
    return value
end

local function getItemFullTypeEarly(item)
    if not (item and item.getFullType) then return nil end
    local fullType = item:getFullType()
    if fullType == nil then return nil end
    return tostring(fullType)
end

local function journalDataNeedsServerBootstrap(journalData)
    if type(journalData) ~= "table" then
        return true
    end
    if not (BurdJournals.getJournalIdentityUUID and BurdJournals.getJournalIdentityUUID(journalData)) then
        return true
    end
    return false
end

local function collectBootstrapJournalCandidatesFromContainer(container, fullType, conditionValue, outCandidates)
    if not container or not container.getItems or type(fullType) ~= "string" or fullType == "" then
        return
    end

    local items = container:getItems()
    if not items then
        return
    end

    for i = 0, items:size() - 1 do
        local item = items:get(i)
        if item then
            if getItemFullTypeEarly(item) == fullType then
                if conditionValue == nil or tonumber(item.getCondition and item:getCondition() or nil) == conditionValue then
                    table.insert(outCandidates, item)
                end
            end
            if item.getInventory then
                local subInventory = item:getInventory()
                if subInventory then
                    collectBootstrapJournalCandidatesFromContainer(subInventory, fullType, conditionValue, outCandidates)
                end
            end
        end
    end
end

local function collectBootstrapJournalCandidatesNearPlayer(player, fullType, conditionValue, outCandidates)
    if not player or type(fullType) ~= "string" or fullType == "" then
        return
    end

    local inventory = player.getInventory and player:getInventory() or nil
    if inventory then
        collectBootstrapJournalCandidatesFromContainer(inventory, fullType, conditionValue, outCandidates)
    end

    local square = player.getCurrentSquare and player:getCurrentSquare() or nil
    if not square then
        return
    end

    for dx = -1, 1 do
        for dy = -1, 1 do
            local nearSquare = getCell():getGridSquare(square:getX() + dx, square:getY() + dy, square:getZ())
            if nearSquare then
                local objects = nearSquare:getObjects()
                if objects then
                    for i = 0, objects:size() - 1 do
                        local obj = objects:get(i)
                        if obj and obj.getItem then
                            local worldItem = obj:getItem()
                            if worldItem and getItemFullTypeEarly(worldItem) == fullType then
                                if conditionValue == nil or tonumber(worldItem.getCondition and worldItem:getCondition() or nil) == conditionValue then
                                    table.insert(outCandidates, worldItem)
                                end
                            end
                        end
                        if obj and obj.getContainer then
                            local container = obj:getContainer()
                            if container then
                                collectBootstrapJournalCandidatesFromContainer(container, fullType, conditionValue, outCandidates)
                            end
                        end
                        if obj and obj.getInventory then
                            local objectInventory = obj:getInventory()
                            if objectInventory then
                                collectBootstrapJournalCandidatesFromContainer(objectInventory, fullType, conditionValue, outCandidates)
                            end
                        end
                    end
                end
            end
        end
    end
end

function BurdJournals.Server.collectBootstrapJournalCandidatesForResolution(player, fullType, conditionValue, outCandidates, includeNearby)
    if includeNearby then
        collectBootstrapJournalCandidatesNearPlayer(player, fullType, conditionValue, outCandidates)
        return
    end
    local inventory = player and player.getInventory and player:getInventory() or nil
    if inventory then
        collectBootstrapJournalCandidatesFromContainer(inventory, fullType, conditionValue, outCandidates)
    end
end

local function findBootstrapJournalCandidate(player, journalFingerprint, includeNearby)
    local fullType = getJournalFingerprintToken(journalFingerprint, "ft")
    if not player or not fullType then
        return nil
    end

    local conditionValue = tonumber(getJournalFingerprintToken(journalFingerprint, "condition"))
    local candidates = {}
    BurdJournals.Server.collectBootstrapJournalCandidatesForResolution(player, fullType, conditionValue, candidates, includeNearby)
    if #candidates == 0 and conditionValue ~= nil then
        BurdJournals.Server.collectBootstrapJournalCandidatesForResolution(player, fullType, nil, candidates, includeNearby)
    end
    if #candidates == 0 then
        return nil
    end

    local bootstrapCandidates = {}
    for _, candidate in ipairs(candidates) do
        local modData = candidate and candidate.getModData and candidate:getModData() or nil
        local journalData = modData and modData.BurdJournals or nil
        if journalDataNeedsServerBootstrap(journalData) then
            table.insert(bootstrapCandidates, candidate)
        end
    end

    if #bootstrapCandidates == 1 then
        return bootstrapCandidates[1]
    end
    if #candidates == 1 then
        return candidates[1]
    end
    return nil
end

local function findBootstrapJournalCandidateFromArgs(player, args, includeNearby)
    local journalFingerprint = normalizeCommandJournalFingerprint(args)
    local candidate = journalFingerprint and findBootstrapJournalCandidate(player, journalFingerprint, includeNearby) or nil
    if candidate then
        return candidate, "bootstrapByFingerprintType"
    end

    local fullType = type(args) == "table" and args.itemFullType or nil
    fullType = type(fullType) == "string" and fullType:gsub("^%s+", ""):gsub("%s+$", "") or nil
    if not fullType or fullType == "" or not string.find(fullType, "^BurdJournals%.") then
        return nil, nil
    end

    local conditionValue = tonumber(journalFingerprint and getJournalFingerprintToken(journalFingerprint, "condition") or nil)
    local candidates = {}
    BurdJournals.Server.collectBootstrapJournalCandidatesForResolution(player, fullType, conditionValue, candidates, includeNearby)
    if #candidates == 0 and conditionValue ~= nil then
        BurdJournals.Server.collectBootstrapJournalCandidatesForResolution(player, fullType, nil, candidates, includeNearby)
    end

    if #candidates == 1 then
        return candidates[1], "bootstrapByItemType"
    end
    return nil, nil
end

-- Recovery data must be derived from a server-owned registry/entry-store record.
-- Command journalData is presentation data from an untrusted client and must never
-- be allowed to create or overwrite reward-bearing state.
function BurdJournals.Server.getServerOwnedJournalRecoveryData(player, journalUUID, sourceTag)
    if not (player and type(journalUUID) == "string" and journalUUID ~= "") then return nil end
    local registryEntry = BurdJournals.Server.getPlayerJournalRegistryEntryByUUID
        and BurdJournals.Server.getPlayerJournalRegistryEntryByUUID(journalUUID) or nil
    if type(registryEntry) ~= "table" then return nil end
    local registryData = type(registryEntry.journalData) == "table" and registryEntry.journalData or registryEntry
    local ownerSteamId = tostring(registryEntry.ownerSteamId or registryData.ownerSteamId or "")
    local ownerUsername = tostring(registryEntry.ownerUsername or registryData.ownerUsername or "")
    local playerSteamId = BurdJournals.getPlayerSteamId and tostring(BurdJournals.getPlayerSteamId(player) or "") or ""
    local playerUsername = player.getUsername and tostring(player:getUsername() or "") or ""
    if (ownerSteamId ~= "" and ownerSteamId ~= playerSteamId)
        or (ownerSteamId == "" and ownerUsername ~= "" and string.lower(ownerUsername) ~= string.lower(playerUsername)) then return nil end
    local recovered = BurdJournals.Server.buildEntryStoreOnlyJournalData
        and BurdJournals.Server.buildEntryStoreOnlyJournalData(journalUUID, sourceTag or "serverOwnedRecovery") or nil
    if type(recovered) ~= "table" then return nil end
    recovered.uuid, recovered.entryStoreUUID, recovered.isPlayerCreated = journalUUID, journalUUID, true
    recovered.sourceType = recovered.sourceType or registryData.sourceType or "personal"
    recovered.originMode = recovered.originMode or registryData.originMode or "personal"
    recovered.ownerSteamId = recovered.ownerSteamId or registryEntry.ownerSteamId or registryData.ownerSteamId
    recovered.ownerUsername = recovered.ownerUsername or registryEntry.ownerUsername or registryData.ownerUsername
    recovered.ownerCharacterName = recovered.ownerCharacterName or registryEntry.ownerCharacterName or registryData.ownerCharacterName
    recovered.author = recovered.author or registryData.author or recovered.ownerCharacterName
    recovered.isWritten = true
    return recovered
end

local function bootstrapResolvedJournalFromCommandData(journal, player, args, sourceTag)
    if not journal or type(args) ~= "table" then
        return journal
    end
    if not BurdJournals.Server.applyNormalizedDebugJournalDataToItem then
        return journal
    end

    local modData = journal.getModData and journal:getModData() or nil
    local existingData = modData and modData.BurdJournals or nil
    local requestedUUID = normalizeCommandJournalUUID(args)
    local needsBootstrap = journalDataNeedsServerBootstrap(existingData)

    if (not needsBootstrap)
        and (not requestedUUID or (type(existingData) == "table"
            and tostring(BurdJournals.getJournalIdentityUUID(existingData) or "") == tostring(requestedUUID))) then
        return journal
    end

    local normalized = BurdJournals.Server.getServerOwnedJournalRecoveryData(player, requestedUUID, "bootstrapResolvedJournal:" .. tostring(sourceTag or "unknown"))
    if type(normalized) ~= "table" then return journal end
    local applied = BurdJournals.Server.applyNormalizedDebugJournalDataToItem(journal, normalized, requestedUUID)
    if type(applied) ~= "table" then
        return journal
    end

    if journal.transmitModData and not BurdJournals.Server.shouldDeferBatchTransmit() then
        safeTransmitServerJournalModData(journal, "bootstrapResolvedJournalFromCommandData")
    end
    if BurdJournals.Server.updateJournalUUIDIndex then
        BurdJournals.Server.updateJournalUUIDIndex(journal, player, tostring(sourceTag or "commandBootstrap"))
    end

    BurdJournals.debugPrint("[BurdJournals] Server: Restored server-owned journal data using client identity hints for source="
        .. tostring(sourceTag or "unknown")
        .. ", journalId=" .. tostring(journal.getID and journal:getID() or args.journalId)
        .. ", journalUUID=" .. tostring(applied.uuid))

    return journal
end

local function materializeServerJournalFromCommandData(player, args, sourceTag)
    if not player or type(args) ~= "table" then
        return nil, nil
    end

    -- Non-player journals spawned client-side (for example via admin item viewers on B41)
    -- may not exist in authoritative inventory state yet. Materializing them into a real
    -- server item should not depend on the debug gate; the payload/fullType checks below
    -- are the actual validation boundary for this bootstrap path.

    local requestedUUID = normalizeCommandJournalUUID(args)
    if not requestedUUID or requestedUUID == "" then
        if requestedUUID and requestedUUID ~= "" then
            bsjWriteLogLine("[BurdJournals] Server: Cannot materialize missing journal; no usable itemFullType"
                .. " source=" .. tostring(sourceTag or "unknown")
                .. " journalUUID=" .. tostring(requestedUUID)
                .. " itemFullType=" .. tostring(args.itemFullType)
                .. " fingerprint=" .. tostring(normalizeCommandJournalFingerprint(args)))
        end
        return nil, nil
    end

    local fullType = "BurdJournals.FilledSurvivalJournal"
    local normalized = BurdJournals.Server.getServerOwnedJournalRecoveryData(player, requestedUUID,
        "materializeServerJournalFromEntryStore:" .. tostring(sourceTag or "unknown"))
    if type(normalized) ~= "table" then
        if requestedUUID and requestedUUID ~= "" then
            bsjWriteLogLine("[BurdJournals] Server: Cannot materialize missing journal; no payload or entry-store backup"
                .. " source=" .. tostring(sourceTag or "unknown")
                .. " journalUUID=" .. tostring(requestedUUID)
                .. " fullType=" .. tostring(fullType))
        end
        return nil, nil
    end

    if args.createIfMissing == false then
        return nil, nil
    end

    local inventory = player.getInventory and player:getInventory() or nil
    if not inventory or not inventory.AddItem then
        return nil, nil
    end

    local journal = inventory:AddItem(fullType)
    if not journal then
        return nil, nil
    end

    local targetCondition = tonumber(normalized.condition)
        or tonumber(getJournalFingerprintToken(normalizeCommandJournalFingerprint(args), "condition"))
    if targetCondition and journal.setCondition then
        BurdJournals.safePcall(function()
            journal:setCondition(math.max(0, math.floor(targetCondition + 0.5)))
        end)
    end

    normalized.fullType = fullType
    if BurdJournals.clearPlayerJournalDebugProvenance then
        BurdJournals.clearPlayerJournalDebugProvenance(normalized, "materializeServerJournalFromCommandData")
    end

    local bj = BurdJournals.Server.applyNormalizedDebugJournalDataToItem
        and BurdJournals.Server.applyNormalizedDebugJournalDataToItem(journal, normalized, requestedUUID)
        or nil
    if type(bj) ~= "table" then
        inventory:Remove(journal)
        return nil, nil
    end

    normalizeFoundJournalClaimFlags(journal, bj, "materializeServerJournalFromCommandData")
    if BurdJournals.updateJournalName then
        BurdJournals.updateJournalName(journal, true)
    end
    if BurdJournals.updateJournalIcon then
        BurdJournals.updateJournalIcon(journal)
    end
    if journal.transmitModData and not BurdJournals.Server.shouldDeferBatchTransmit() then
        safeTransmitServerJournalModData(journal, "materializeServerJournalFromCommandData")
    end
    if inventory.setDrawDirty then
        inventory:setDrawDirty(true)
    end
    if player.syncInventory then
        BurdJournals.safePcall(function()
            player:syncInventory()
        end)
    end
    if sendAddItemToContainer then
        BurdJournals.safePcall(function()
            sendAddItemToContainer(inventory, journal)
        end)
    end
    if BurdJournals.Server.updateJournalUUIDIndex then
        BurdJournals.Server.updateJournalUUIDIndex(journal, player, "materialize:" .. tostring(sourceTag or "unknown"))
    end

    BurdJournals.Server.sendToClient(player, "journalMaterialized", {
        oldJournalId = args.journalId,
        oldJournalUUID = requestedUUID,
        newJournalId = journal.getID and journal:getID() or nil,
        journalUUID = bj.uuid,
        journalData = bj,
        source = sourceTag or "unknown",
    })

    BurdJournals.debugPrint("[BurdJournals] Server: Materialized client journal payload into live item for source="
        .. tostring(sourceTag or "unknown")
        .. ", requestedId=" .. tostring(args.journalId)
        .. ", requestedUUID=" .. tostring(requestedUUID)
        .. ", newJournalId=" .. tostring(journal.getID and journal:getID() or "nil")
        .. ", fullType=" .. tostring(fullType))

    return journal, "materializedFromPayload"
end

-- Repeated timed-action and entry-sync commands normally operate on a journal
-- already carried by the player.  Keep their stale/malformed lookup path out of
-- the recursive 3x3 world/container search used for explicit world interaction.
-- Opening/sealing actions pick the journal up before sending their command, so
-- ordinary command resolution never needs a nearby-world fallback.
BurdJournals.Server.INVENTORY_ONLY_JOURNAL_RESOLUTION_COMMANDS = {
    openCursedJournal = true,
    openCursedJournalConfirm = true,
    openYuletideJournal = true,
    openYuletideJournalConfirm = true,
    breakJournalSeal = true,
    yuletideGiftMaterialized = true,
    initializeJournal = true,
    logSkills = true,
    learnSkills = true,
    recordProgress = true,
    syncJournalData = true,
    requestJournalEntryChunk = true,
    claimSkill = true,
    claimTrait = true,
    claimForgetSlot = true,
    claimRecipe = true,
    claimStat = true,
    absorbSkill = true,
    absorbTrait = true,
    absorbRecipe = true,
    batchClaimRewards = true,
    batchAbsorbRewards = true,
    sanitizeJournal = true,
    saveJournalNotes = true,
    dissolveJournal = true,
    eraseJournal = true,
    convertToClean = true,
    eraseEntry = true,
    renameJournal = true,
    removeCraftDuplicates = true,
}

-- Missing-item recovery is deliberately narrower than ordinary command
-- resolution. These commands may recreate a server-owned registry journal,
-- but they still must not select an unrelated nearby world/container item.
BurdJournals.Server.MISSING_JOURNAL_RECOVERY_COMMANDS = {
    initializeJournal = true,
    syncJournalData = true,
}

function BurdJournals.Server.findJournalInRequestersInventory(player, journalId, journalUUID, journalFingerprint)
    local inventory = player and player.getInventory and player:getInventory() or nil
    if not (inventory and inventory.getItems) then
        return nil, false
    end
    if journalId ~= nil and BurdJournals.findItemByIdInContainer then
        local byId = BurdJournals.findItemByIdInContainer(inventory, journalId)
        if byId then
            return byId, true
        end
    end
    if journalUUID and BurdJournals.findJournalByUUIDInContainer then
        local byUUID = BurdJournals.findJournalByUUIDInContainer(inventory, journalUUID)
        if byUUID then
            return byUUID, true
        end
    end
    if journalFingerprint and BurdJournals.findJournalByLookupFingerprintInContainer then
        return BurdJournals.findJournalByLookupFingerprintInContainer(inventory, journalFingerprint), true
    end
    return nil, true
end

local function resolveServerCommandJournal(player, args, sourceTag)
    if not player or type(args) ~= "table" then
        return nil, nil, nil, "invalidPayload"
    end

    local requestedJournalUUID = normalizeCommandJournalUUID(args) or resolveCommandJournalUUIDFromIndex(player, args)
    local function getCandidateJournalUUID(candidate)
        local modData = candidate and candidate.getModData and candidate:getModData() or nil
        local journalData = modData and modData.BurdJournals or nil
        if type(journalData) ~= "table" then
            return nil
        end
        local uuid = BurdJournals.getJournalIdentityUUID and BurdJournals.getJournalIdentityUUID(journalData)
            or journalData.uuid or journalData.entryStoreUUID
        if type(uuid) == "string" and uuid ~= "" then
            return uuid
        end
        return nil
    end

    local function candidateConflictsWithRequestedUUID(candidate)
        if type(requestedJournalUUID) ~= "string" or requestedJournalUUID == "" then
            return false
        end
        local candidateUUID = getCandidateJournalUUID(candidate)
        return type(candidateUUID) == "string"
            and candidateUUID ~= ""
            and tostring(candidateUUID) ~= tostring(requestedJournalUUID)
    end

    if args._resolvedJournal and BurdJournals.isValidItem and BurdJournals.isValidItem(args._resolvedJournal) then
        local resolvedJournalId = args._resolvedJournal.getID and args._resolvedJournal:getID() or args.journalId
        local resolvedJournalUUID = requestedJournalUUID
        if not resolvedJournalUUID then
            local resolvedModData = args._resolvedJournal.getModData and args._resolvedJournal:getModData() or nil
            local resolvedData = resolvedModData and resolvedModData.BurdJournals or nil
            resolvedJournalUUID = type(resolvedData) == "table" and resolvedData.uuid or nil
        end
        if candidateConflictsWithRequestedUUID(args._resolvedJournal) then
            BurdJournals.debugPrint("[BurdJournals] Server: Ignoring pre-resolved journal for source="
                .. tostring(sourceTag or "unknown")
                .. " because requested UUID " .. tostring(requestedJournalUUID)
                .. " does not match item UUID " .. tostring(getCandidateJournalUUID(args._resolvedJournal)))
        else
            return args._resolvedJournal, resolvedJournalId, resolvedJournalUUID, "batchResolved"
        end
    end

    local journalId = tonumber(args.journalId) or args.journalId
    local journalUUID = requestedJournalUUID
    local journalFingerprint = normalizeCommandJournalFingerprint(args)
    local exactJournalItem = args.exactJournalItem == true and journalId ~= nil
    -- Fail closed for new commands. Nearby-world interaction must use a
    -- dedicated, reachability-validating resolver rather than silently opting
    -- into the recursive mixed-scope helpers.
    local inventoryOnly = true
    local allowMissingRecovery = BurdJournals.Server.MISSING_JOURNAL_RECOVERY_COMMANDS[sourceTag] == true
    local journal = nil
    local conflictingById = nil
    local skipCommandBootstrap = false
    local resolvePath = "unresolved"

    if journalId ~= nil then
        local byId = nil
        if inventoryOnly then
            byId = BurdJournals.findItemByIdInPlayerInventory
                and BurdJournals.findItemByIdInPlayerInventory(player, journalId) or nil
        end
        if byId and exactJournalItem then
            journal = byId
            resolvePath = "requesterByExactId"
            if candidateConflictsWithRequestedUUID(byId) then
                -- Cursed stack activation is bound to the physical item the
                -- player selected. A stale UUID may be reconciled by the reply,
                -- but it must never retarget activation to a sibling in the stack.
                skipCommandBootstrap = true
            end
        elseif byId and candidateConflictsWithRequestedUUID(byId) then
            -- The same physical held item can temporarily have different client
            -- and server UUIDs after upgrading an existing save or initializing a
            -- fresh journal in MP. Keep the authoritative ID match as a fallback,
            -- but first give a genuinely matching held UUID/fingerprint priority.
            conflictingById = byId
            BurdJournals.debugPrint("[BurdJournals] Server: Rejected journalId candidate for source="
                .. tostring(sourceTag or "unknown")
                .. ", requestedId=" .. tostring(journalId)
                .. ", requestedUUID=" .. tostring(journalUUID)
                .. ", itemUUID=" .. tostring(getCandidateJournalUUID(byId)))
        elseif byId then
            journal = byId
            resolvePath = "requesterById"
        end
    end

    if (not journal) and not exactJournalItem and journalUUID and BurdJournals.findJournalByUUID then
        if inventoryOnly then
            journal = BurdJournals.findJournalByUUIDInPlayerInventory
                and BurdJournals.findJournalByUUIDInPlayerInventory(player, journalUUID) or nil
        end
        if journal then
            resolvePath = "requesterByUUID"
        end
    end

    if (not journal) and not exactJournalItem and journalFingerprint and BurdJournals.findJournalByLookupFingerprint then
        local fingerprintJournal = nil
        if inventoryOnly then
            fingerprintJournal = BurdJournals.findJournalByLookupFingerprintInPlayerInventory
                and BurdJournals.findJournalByLookupFingerprintInPlayerInventory(player, journalFingerprint) or nil
        end
        if fingerprintJournal and candidateConflictsWithRequestedUUID(fingerprintJournal) then
            BurdJournals.debugPrint("[BurdJournals] Server: Rejected fingerprint candidate for source="
                .. tostring(sourceTag or "unknown")
                .. ", requestedUUID=" .. tostring(journalUUID)
                .. ", itemUUID=" .. tostring(getCandidateJournalUUID(fingerprintJournal)))
        elseif fingerprintJournal then
            journal = fingerprintJournal
            resolvePath = "requesterByFingerprint"
        end
    end

    -- Missing-journal recovery is inventory-scoped and server-owned. Client
    -- payload data is never used to select a nearby item.
    if not journal and not exactJournalItem and allowMissingRecovery then
        local bootstrapJournal, bootstrapPath = findBootstrapJournalCandidateFromArgs(player, args, false)
        if bootstrapJournal and not candidateConflictsWithRequestedUUID(bootstrapJournal) then
            journal = bootstrapJournal
            resolvePath = bootstrapPath or "bootstrapByCommandType"
        end
    end

    if not journal and conflictingById then
        journal = conflictingById
        resolvePath = "requesterByIdUUIDReconciled"
        -- Never replace the authoritative item's data from a mismatched client
        -- UUID. The normal response payload will hydrate the client with the
        -- server-owned UUID and journal state.
        skipCommandBootstrap = true
        BurdJournals.debugPrint("[BurdJournals] Server: Reconciled stale client UUID using exact held item ID for source="
            .. tostring(sourceTag or "unknown")
            .. ", requestedId=" .. tostring(journalId)
            .. ", requestedUUID=" .. tostring(journalUUID)
            .. ", serverUUID=" .. tostring(getCandidateJournalUUID(journal)))
    end

    if journal then
        journalId = (journal.getID and journal:getID()) or journalId
        if not skipCommandBootstrap then
            journal = bootstrapResolvedJournalFromCommandData(journal, player, args, sourceTag)
        end
        local modData = journal.getModData and journal:getModData() or nil
        local journalData = modData and modData.BurdJournals or nil
        if type(journalData) == "table" and BurdJournals.resolveJournalUUIDForRuntime then
            local repairedUUID = BurdJournals.resolveJournalUUIDForRuntime(journalData, journal, false)
            if repairedUUID then
                journalUUID = repairedUUID
            end
        end
    elseif allowMissingRecovery then
        local materializedJournal, materializePath = materializeServerJournalFromCommandData(player, args, sourceTag)
        if materializedJournal then
            journal = materializedJournal
            resolvePath = materializePath or "materializedFromPayload"
            journalId = journal.getID and journal:getID() or journalId
            local modData = journal.getModData and journal:getModData() or nil
            local journalData = modData and modData.BurdJournals or nil
            if type(journalData) == "table" then
                journalUUID = tostring(journalData.uuid or journalUUID or "")
            end
        end
    end

    if not journal then
        bsjWriteLogLine("[BurdJournals] Server ERROR: Journal not found (source="
            .. tostring(sourceTag or "unknown")
            .. ", journalId=" .. tostring(journalId)
            .. ", journalUUID=" .. tostring(journalUUID)
            .. ", journalFingerprint=" .. tostring(journalFingerprint) .. ")")
    elseif resolvePath ~= "requesterById" then
        BurdJournals.debugPrint("[BurdJournals] Server: Resolved journal via "
            .. tostring(resolvePath)
            .. " for source=" .. tostring(sourceTag or "unknown")
            .. ", requestedId=" .. tostring(journalId)
            .. ", requestedUUID=" .. tostring(journalUUID)
            .. ", requestedFingerprint=" .. tostring(journalFingerprint))
    end

    return journal, journalId, journalUUID, resolvePath
end

local function getResolvedJournalIdentity(journal, resolvedJournalId, resolvedJournalUUID, fallbackJournalId)
    local liveJournalId = resolvedJournalId
    if liveJournalId == nil and journal and journal.getID then
        liveJournalId = journal:getID()
    end
    if liveJournalId == nil then
        liveJournalId = fallbackJournalId
    end

    local liveJournalUUID = resolvedJournalUUID
    if not liveJournalUUID and journal and journal.getModData then
        local modData = journal:getModData()
        local journalData = modData and modData.BurdJournals or nil
        if type(journalData) == "table" then
            liveJournalUUID = (BurdJournals.getJournalIdentityUUID and BurdJournals.getJournalIdentityUUID(journalData))
                or (BurdJournals.resolveJournalUUIDForRuntime and BurdJournals.resolveJournalUUIDForRuntime(journalData, journal, true))
        end
    end

    return liveJournalId, liveJournalUUID
end

local function buildSyncSuccessPayload(journalId, journalData, player)
    local payload = {
        journalId = journalId,
        serverRuntimeMarker = BurdJournals.RUNTIME_LOAD_MARKER
    }
    if type(journalData) ~= "table" then
        payload.needsSync = true
        return payload
    end

    local canonicalUUID = BurdJournals.getJournalIdentityUUID and BurdJournals.getJournalIdentityUUID(journalData) or nil
    local fullSyncEntryLimit = BurdJournals.getJournalFullSyncEntryLimit and BurdJournals.getJournalFullSyncEntryLimit(journalData)
        or math.max(1, tonumber(BurdJournals.FULL_SYNC_ENTRY_LIMIT) or 180)
    local entryCount = BurdJournals.Server.countJournalPayloadEntries(journalData)
    if isStrictMPEmptyJournalShellData(journalData) then
        payload.journalUUID = canonicalUUID
        payload.needsSync = true
        payload.fullJournalDataOmitted = true
        payload.fullJournalDataOmittedReason = "emptyShell"
        payload.entryStoreUnavailable = true
        BurdJournals.debugPrint("[BurdJournals] syncSuccess refused empty strict-MP journal shell for journalId="
            .. tostring(journalId))
        return payload
    end
    -- Entry-store shells are compact in hosted MP too. Treating one as full
    -- journalData works only until the client's hydrated cache is cleared by a
    -- reconnect, after which the journal appears empty despite its durable store.
    if journalData.entryStoreEnabled == true
        or (isStrictMPServer() and entryCount >= fullSyncEntryLimit)
    then
        local expectedEntryStoreCount = BurdJournals.Server.countEntryStoreManifestEntries(journalData)
        local actualEntryStoreCount = nil
        if journalData.entryStoreEnabled == true and BurdJournals.refreshJournalEntryStoreCounts then
            actualEntryStoreCount = BurdJournals.refreshJournalEntryStoreCounts(journalData, "syncSuccessCounts")
            actualEntryStoreCount = tonumber(actualEntryStoreCount) or 0
        end
        payload.journalUUID = canonicalUUID
        payload.entryStoreEnabled = journalData.entryStoreEnabled == true
        payload.entryStoreEntryCounts = type(journalData.entryStoreEntryCounts) == "table" and journalData.entryStoreEntryCounts or nil
        payload.entryStoreUpdatedAt = tonumber(journalData.entryStoreUpdatedAt)
        payload.entryStoreActualCount = actualEntryStoreCount
        payload.entryStoreUnavailable = journalData.entryStoreEnabled == true
            and expectedEntryStoreCount > 0
            and actualEntryStoreCount ~= nil
            and actualEntryStoreCount <= 0
            or false
        local runtimeDelta = BurdJournals.buildRuntimeDeltaForPlayer and BurdJournals.buildRuntimeDeltaForPlayer(journalData, player) or nil
        if type(runtimeDelta) == "table" then
            payload.runtimeDelta = runtimeDelta
        end
        local journalDelta = {}
        local hasJournalDelta = false
        local scalarKeys = {
            "uuid",
            "entryStoreUUID",
            "entryStoreUpdatedAt",
            "isPlayerCreated",
            "isWritten",
            "isWorn",
            "isBloody",
            "wasFromWorn",
            "wasFromBloody",
            "hasBloodyOrigin",
            "isZombieJournal",
            "isCursedJournal",
            "isCursedReward",
            "isYuletideJournal",
            "yuletideState",
            "author",
            "ownerCharacterName",
            "profession",
            "professionName",
            "flavorKey",
            "loreNoteTemplateFamily",
            "lootNotesRollDone",
            "lootNotesRollChance",
            "notes",
        }
        for _, key in ipairs(scalarKeys) do
            if journalData[key] ~= nil then
                journalDelta[key] = BurdJournals.Server.deepCopy and BurdJournals.Server.deepCopy(journalData[key]) or journalData[key]
                hasJournalDelta = true
            end
        end
        if hasJournalDelta then
            payload.journalDelta = journalDelta
        end
        payload.fullJournalDataOmitted = true
        payload.fullJournalDataOmittedReason = journalData.entryStoreEnabled == true and "entryStore" or "largeJournal"
        BurdJournals.debugPrint("[BurdJournals] syncSuccess omitted full journalData before copy for journalId="
            .. tostring(journalId) .. " (entries=" .. tostring(entryCount)
            .. ", storeActual=" .. tostring(actualEntryStoreCount)
            .. ", limit=" .. tostring(fullSyncEntryLimit) .. ")")
        return payload
    end

    local projected = BurdJournals.Server.deepCopy(journalData)
    local runtimeDelta = nil
    if BurdJournals.applyRuntimeProjectionToJournalData then
        runtimeDelta = BurdJournals.applyRuntimeProjectionToJournalData(projected, player)
    end
    payload.journalUUID = BurdJournals.getJournalIdentityUUID and BurdJournals.getJournalIdentityUUID(projected) or nil
    payload.journalData = projected
    if type(runtimeDelta) == "table" then
        payload.runtimeDelta = runtimeDelta
    end

    local softLimit = math.max(1024, tonumber(BurdJournals.FULL_SYNC_SOFT_LIMIT_BYTES) or 48000)
    local estimate = estimatePayloadBytes(payload)
    if estimate > softLimit then
        payload.journalData = nil
        payload.fullJournalDataOmitted = true
        payload.fullJournalDataOmittedReason = "softLimit"
        BurdJournals.debugPrint("[BurdJournals] syncSuccess payload soft-limit exceeded for journalId="
            .. tostring(journalId) .. " (" .. tostring(estimate) .. " > " .. tostring(softLimit)
            .. "), omitting duplicate full journalData")
    end

    return payload
end

local function chooseRandom(list)
    if type(list) ~= "table" or #list == 0 then return nil end
    return list[ZombRand(#list) + 1]
end

local function shuffleArray(list)
    if type(list) ~= "table" then return end
    for i = #list, 2, -1 do
        local j = ZombRand(i) + 1
        list[i], list[j] = list[j], list[i]
    end
end

local function getCursedServerText(key, fallback)
    local text = getText(key)
    if text and text ~= "" and text ~= key then
        return text
    end
    return fallback
end

local function buildCursedRewardSkills(professionId, opts)
    local out = {}
    local allowed = (BurdJournals.getAllowedSkills and BurdJournals.getAllowedSkills()) or {}
    if #allowed == 0 then return out end

    -- opts (optional): forceMaxCount forces the roll to the max skill count;
    -- xpScale multiplies each generated skill's XP (used by severity compensation).
    local forceMaxCount = type(opts) == "table" and opts.forceMaxCount == true
    local xpScale = (type(opts) == "table" and tonumber(opts.xpScale)) or 1

    local minXP = tonumber(BurdJournals.getSandboxOption and BurdJournals.getSandboxOption("CursedJournalMinXP")) or 75
    local maxXP = tonumber(BurdJournals.getSandboxOption and BurdJournals.getSandboxOption("CursedJournalMaxXP")) or 300
    minXP = math.max(1, math.floor(minXP))
    maxXP = math.max(minXP, math.floor(maxXP))

    local minSkills = tonumber(BurdJournals.getSandboxOption and BurdJournals.getSandboxOption("CursedJournalMinSkills")) or 2
    local maxSkills = tonumber(BurdJournals.getSandboxOption and BurdJournals.getSandboxOption("CursedJournalMaxSkills")) or 5
    minSkills = math.max(1, math.floor(minSkills))
    maxSkills = math.max(minSkills, math.floor(maxSkills))
    if forceMaxCount then
        minSkills = maxSkills
    end
    if BurdJournals.rollCoherentSkillsForProfession and professionId then
        local coherentSkills, coreCount, fallbackCount = BurdJournals.rollCoherentSkillsForProfession(
            professionId,
            minSkills,
            maxSkills,
            minXP,
            maxXP
        )
        if coherentSkills and BurdJournals.hasAnyEntries and BurdJournals.hasAnyEntries(coherentSkills) then
            if xpScale > 1 and BurdJournals.Server.scaleCursedRewardSkillXP then
                BurdJournals.Server.scaleCursedRewardSkillXP(coherentSkills, xpScale)
            end
            return coherentSkills, coreCount, fallbackCount
        end
    end

    local numSkills = ZombRand(minSkills, maxSkills + 1)

    local candidates = {}
    for _, skillName in ipairs(allowed) do
        local perk = BurdJournals.getPerkByName and BurdJournals.getPerkByName(skillName)
        if perk then
            candidates[#candidates + 1] = skillName
        end
    end
    shuffleArray(candidates)

    for i = 1, math.min(numSkills, #candidates) do
        local skillName = candidates[i]
        local xp = ZombRand(minXP, maxXP + 1)
        local level = (BurdJournals.getSkillLevelFromXP and BurdJournals.getSkillLevelFromXP(xp, skillName)) or 0
        out[skillName] = {
            xp = xp,
            level = level,
        }
    end
    if xpScale > 1 and BurdJournals.Server.scaleCursedRewardSkillXP then
        BurdJournals.Server.scaleCursedRewardSkillXP(out, xpScale)
    end
    return out, 0, BurdJournals.countTable and BurdJournals.countTable(out) or 0
end

local function buildCursedRewardTraits()
    if BurdJournals.getSandboxOption and BurdJournals.getSandboxOption("EnableCursedJournalTraits") == false then
        return nil
    end

    local traitChance = tonumber(BurdJournals.getSandboxOption and BurdJournals.getSandboxOption("CursedJournalTraitChance")) or 40
    if traitChance <= 0 or ZombRand(100) >= traitChance then
        return nil
    end

    local grantable = (BurdJournals.getGrantableTraitsForJournal
        and BurdJournals.getGrantableTraitsForJournal({ isCursedReward = true, isPlayerCreated = false })) or {}
    if #grantable == 0 then
        return nil
    end

    local pool = {}
    for _, traitId in ipairs(grantable) do
        pool[#pool + 1] = traitId
    end
    shuffleArray(pool)

    local out = {}
    local minTraits = tonumber(BurdJournals.getSandboxOption and BurdJournals.getSandboxOption("CursedJournalMinTraits")) or 1
    local maxTraits = tonumber(BurdJournals.getSandboxOption and BurdJournals.getSandboxOption("CursedJournalMaxTraits")) or 3
    minTraits = math.max(1, math.floor(minTraits))
    maxTraits = math.max(minTraits, math.floor(maxTraits))
    maxTraits = math.min(maxTraits, #pool)
    minTraits = math.min(minTraits, maxTraits)
    local traitCount = ZombRand(minTraits, maxTraits + 1)
    for i = 1, traitCount do
        out[pool[i]] = true
    end
    return out
end

local function buildCursedRewardRecipes()
    if BurdJournals.getSandboxOption and BurdJournals.getSandboxOption("EnableCursedJournalRecipes") == false then
        return nil
    end

    local recipeChance = tonumber(BurdJournals.getSandboxOption and BurdJournals.getSandboxOption("CursedJournalRecipeChance")) or 60
    if recipeChance <= 0 or ZombRand(100) >= recipeChance then
        return nil
    end

    local maxRecipes = tonumber(BurdJournals.getSandboxOption and BurdJournals.getSandboxOption("CursedJournalMaxRecipes")) or 3
    maxRecipes = math.max(1, math.floor(maxRecipes))
    local count = ZombRand(1, maxRecipes + 1)
    if BurdJournals.generateRandomRecipes then
        return BurdJournals.generateRandomRecipes(count)
    end
    return nil
end

-- Scale each skill entry's XP by a multiplier and recompute its level in place.
function BurdJournals.Server.scaleCursedRewardSkillXP(skills, xpScale)
    xpScale = tonumber(xpScale)
    if type(skills) ~= "table" or not xpScale or xpScale <= 1 then
        return skills
    end
    for skillName, entry in pairs(skills) do
        if type(entry) == "table" and tonumber(entry.xp) then
            local scaledXP = math.floor(tonumber(entry.xp) * xpScale)
            entry.xp = scaledXP
            entry.level = (BurdJournals.getSkillLevelFromXP and BurdJournals.getSkillLevelFromXP(scaledXP, skillName)) or entry.level or 0
        end
    end
    return skills
end

-- Resolve severity compensation for a curse category. Returns nil when compensation
-- does not apply (manual rewards, non-severe/persistent curse, or disabled by sandbox).
function BurdJournals.Server.resolveCursedSeverityCompensation(curseCategory, manualRewards)
    if manualRewards == true then
        return nil
    end
    if curseCategory ~= "persistent" and curseCategory ~= "severe" then
        return nil
    end
    if BurdJournals.getSandboxOption and BurdJournals.getSandboxOption("CursedJournalSeverityCompensation") == false then
        return nil
    end

    local comp = { forceForgetSlot = true, forceMaxSkills = false, xpScale = 1 }
    if curseCategory == "severe" then
        comp.forceMaxSkills = true
        local bonusPercent = tonumber(BurdJournals.getSandboxOption and BurdJournals.getSandboxOption("CursedJournalSevereRewardBonusPercent"))
        if bonusPercent == nil then
            bonusPercent = 50
        end
        bonusPercent = math.max(0, bonusPercent)
        comp.xpScale = 1 + (bonusPercent / 100)
    end
    return comp
end

function BurdJournals.Server.generateCursedRewardProfile(sourceData)
    local source = type(sourceData) == "table" and sourceData or {}
    local manualRewards = source.manualRewards == true or source.cursedManualRewards == true
    local severityCompensation = BurdJournals.Server.resolveCursedSeverityCompensation(source.curseCategory, manualRewards)
    local gameTime = type(getGameTime) == "function" and getGameTime() or nil
    local worldAge = gameTime and gameTime:getWorldAgeHours() or 0
    local cursedIdentity = BurdJournals.Server.resolveCursedJournalIdentity and BurdJournals.Server.resolveCursedJournalIdentity(source) or {}
    local professionId = cursedIdentity.profession
    local professionName = cursedIdentity.professionName
    local flavorKey = cursedIdentity.flavorKey

    local sourceSkills = nil
    if manualRewards then
        sourceSkills = type(source.skills) == "table" and source.skills or {}
    else
        sourceSkills = (type(source.skills) == "table" and BurdJournals.hasAnyEntries and BurdJournals.hasAnyEntries(source.skills)) and source.skills or nil
    end
    local skillsCoreCount = tonumber(source.coreSkillCount) or 0
    local skillsFallbackCount = tonumber(source.fallbackSkillCount) or 0
    local generatedSkills = sourceSkills
    if not generatedSkills then
        local skillOpts = nil
        if severityCompensation then
            skillOpts = { forceMaxCount = severityCompensation.forceMaxSkills, xpScale = severityCompensation.xpScale }
        end
        generatedSkills, skillsCoreCount, skillsFallbackCount = buildCursedRewardSkills(professionId, skillOpts)
    elseif severityCompensation and severityCompensation.xpScale and severityCompensation.xpScale > 1 then
        -- Pre-staged (hidden journal) rewards: scale staged skill XP in place so the
        -- revealed set matches what was staged, just severity-compensated.
        BurdJournals.Server.scaleCursedRewardSkillXP(generatedSkills, severityCompensation.xpScale)
    end

    local generatedTraits = nil
    if manualRewards then
        generatedTraits = type(source.traits) == "table" and source.traits or {}
    else
        generatedTraits = (type(source.traits) == "table" and BurdJournals.hasAnyEntries and BurdJournals.hasAnyEntries(source.traits))
            and source.traits or buildCursedRewardTraits()
    end
    local generatedRecipes = nil
    if manualRewards then
        generatedRecipes = type(source.recipes) == "table" and source.recipes or {}
    else
        generatedRecipes = (type(source.recipes) == "table" and BurdJournals.hasAnyEntries and BurdJournals.hasAnyEntries(source.recipes))
            and source.recipes or buildCursedRewardRecipes()
    end

    if BurdJournals.resolveProfessionForGeneratedEntries then
        professionId, professionName, flavorKey = BurdJournals.resolveProfessionForGeneratedEntries(
            professionId,
            professionName,
            flavorKey or "UI_BurdJournals_BloodyFlavor",
            generatedSkills,
            generatedTraits,
            generatedRecipes,
            skillsCoreCount,
            skillsFallbackCount
        )
    end

    local profile = {
        uuid = source.uuid or (BurdJournals.generateUUID and BurdJournals.generateUUID()) or tostring(ZombRand(999999999)),
        author = cursedIdentity.author,
        profession = cursedIdentity.profession or professionId,
        professionName = cursedIdentity.professionName or professionName,
        flavorKey = flavorKey or "UI_BurdJournals_BloodyFlavor",
        flavorText = source.flavorText,
        loreNoteText = source.loreNoteText,
        loreNoteTemplateVersion = tonumber(source.loreNoteTemplateVersion),
        loreNoteTemplateFamily = source.loreNoteTemplateFamily,
        timestamp = tonumber(source.timestamp) or (worldAge - ZombRand(24, 720)),
        skills = generatedSkills or {},
        traits = generatedTraits,
        recipes = generatedRecipes,
        stats = type(source.stats) == "table" and source.stats or {},
        forgetSlot = (severityCompensation and severityCompensation.forceForgetSlot == true and true)
            or (manualRewards
                and (source.forgetSlot == true and true or nil)
                or BurdJournals.rollForgetSlotForType and BurdJournals.rollForgetSlotForType("cursed", source.forgetSlot)
                or (source.forgetSlot == true and true or nil)),
        isWorn = false,
        isBloody = true,
        wasFromBloody = true,
        isPlayerCreated = false,
        isZombieJournal = true,
        condition = tonumber(source.condition) or ZombRand(1, 4),
        claims = type(source.claims) == "table" and source.claims or {},
        claimedSkills = type(source.claimedSkills) == "table" and source.claimedSkills or {},
        claimedTraits = type(source.claimedTraits) == "table" and source.claimedTraits or {},
        claimedRecipes = type(source.claimedRecipes) == "table" and source.claimedRecipes or {},
        claimedStats = type(source.claimedStats) == "table" and source.claimedStats or {},
        claimedForgetSlot = type(source.claimedForgetSlot) == "table" and source.claimedForgetSlot or {},
        isCursedReward = true,
        isCursedJournal = false,
        cursedState = "unleashed",
        cursedSealSoundEvent = source.cursedSealSoundEvent,
        cursedPendingRewards = nil,
        cursedUnleashedByName = source.cursedUnleashedByName,
    }

    if source.lootNotesEligible == true and BurdJournals.Server.tryAttachGeneratedLootNotes then
        BurdJournals.Server.tryAttachGeneratedLootNotes(nil, nil, profile, profile.loreNoteTemplateFamily or "cursed")
    end

    -- Add-on hook: allow companion mods to mutate the profile before it is returned.
    if BurdJournals.runHooks then
        BurdJournals.runHooks("OnCursedRewardProfileGenerated", profile, source)
    end

    return profile
end

YULETIDE_PRACTICAL_GIFT_POOL = {
    { type = "Base.Battery", min = 2, max = 4 },
    { type = "Base.NailsBox", min = 1, max = 2 },
    { type = "Base.ScrewsBox", min = 1, max = 2 },
    { type = "Base.TinnedSoup", min = 1, max = 2 },
    { type = "Base.CannedCorn", min = 1, max = 2 },
    { type = "Base.Torch", min = 1, max = 1 },
    { type = "Base.Hammer", min = 1, max = 1 },
    { type = "Base.Screwdriver", min = 1, max = 1 },
    { type = "Base.Wrench", min = 1, max = 1 },
    { type = "Base.Saw", min = 1, max = 1 },
    { type = "Base.DuctTape", min = 1, max = 1 },
    { type = "Base.FirstAidKit", min = 1, max = 1 },
    { type = "Base.BandageBox", min = 1, max = 1 },
    { type = "Base.Matches", min = 1, max = 1 },
    { type = "Base.Lighter", min = 1, max = 1 },
}

YULETIDE_RARE_GIFT_POOL = {
    { type = "Base.Axe", min = 1, max = 1 },
    { type = "Base.Crowbar", min = 1, max = 1 },
    { type = "Base.WoodAxe", min = 1, max = 1 },
    { type = "Base.HandAxe", min = 1, max = 1 },
    { type = "Base.FishingRod", min = 1, max = 1 },
    { type = "Base.HuntingKnife", min = 1, max = 1 },
    { type = "Base.WeldingMask", min = 1, max = 1 },
    { type = "Base.Spiffo", min = 1, max = 1 },
    { type = "Base.MoneyBundle", min = 1, max = 2 },
    { type = "Base.HottieZ", min = 1, max = 1 },
    { type = "Base.Hat_SantaHat", min = 1, max = 1 },
    { type = "Base.Hat_Army", min = 1, max = 1 },
    { type = "Base.Vest_BulletArmy", min = 1, max = 1 },
    { type = "Base.Wine", min = 1, max = 1 },
    { type = "Base.Pistol", min = 1, max = 1 },
    { type = "Base.Revolver_Short", min = 1, max = 1 },
    { type = "Base.DoubleBarrelShotgun", min = 1, max = 1 },
}

YULETIDE_JACKPOT_GIFT_POOL = {
    { type = "Base.Shotgun", min = 1, max = 1 },
    { type = "Base.Machete", min = 1, max = 1 },
    { type = "Base.Sledgehammer", min = 1, max = 1 },
    { type = "Base.Generator", min = 1, max = 1 },
    { type = "Base.AssaultRifle", min = 1, max = 1 },
    { type = "Base.Katana", min = 1, max = 1 },
    { type = "Base.Revolver_Long", min = 1, max = 1 },
    { type = "Base.GoldBar", min = 1, max = 2 },
    { type = "Base.Bag_ALICEpack_Army", min = 1, max = 1 },
    { type = "Base.Whiskey", min = 1, max = 1 },
    { type = "Base.JerryCan", min = 1, max = 1 },
    { type = "Base.PropaneTank", min = 1, max = 1 },
}

BurdJournals.Server.YULETIDE_GUN_AMMO_BOX_FALLBACKS = BurdJournals.Server.YULETIDE_GUN_AMMO_BOX_FALLBACKS or {
    ["Base.AssaultRifle"] = "Base.556Box",
    ["Base.DoubleBarrelShotgun"] = "Base.ShotgunShellsBox",
    ["Base.Pistol"] = "Base.Bullets9mmBox",
    ["Base.Revolver_Long"] = "Base.Bullets44Box",
    ["Base.Revolver_Short"] = "Base.Bullets38Box",
    ["Base.Shotgun"] = "Base.ShotgunShellsBox",
}

function BurdJournals.Server.resolveYuletideGiftAmmoBoxType(fullType)
    if type(fullType) ~= "string" or fullType == "" then
        return nil
    end

    local scriptManager = getScriptManager and getScriptManager() or ScriptManager and ScriptManager.instance or nil
    if scriptManager and scriptManager.getItem then
        local okItem, scriptItem = pcall(function()
            return scriptManager:getItem(fullType)
        end)
        if okItem and scriptItem and scriptItem.getAmmoBox then
            local okAmmoBox, ammoBoxType = pcall(function()
                return scriptItem:getAmmoBox()
            end)
            if okAmmoBox and type(ammoBoxType) == "string" and ammoBoxType ~= "" then
                return ammoBoxType
            end
        end
    end

    return BurdJournals.Server.YULETIDE_GUN_AMMO_BOX_FALLBACKS[fullType]
end

function BurdJournals.Server.addOrIncrementYuletideGiftEntry(gifts, fullType, count)
    if type(gifts) ~= "table" or type(fullType) ~= "string" or fullType == "" then
        return
    end

    local normalizedCount = math.max(1, math.floor(tonumber(count) or 1))
    for _, gift in ipairs(gifts) do
        if gift.type == fullType then
            gift.count = math.max(1, math.floor(tonumber(gift.count) or 1)) + normalizedCount
            return
        end
    end

    gifts[#gifts + 1] = {
        type = fullType,
        count = normalizedCount,
    }
end

function BurdJournals.Server.appendYuletideGiftEntryWithBundles(gifts, entry)
    if type(entry) ~= "table" or type(entry.type) ~= "string" or entry.type == "" then
        return
    end

    local itemCount = ZombRand(entry.min or 1, (entry.max or entry.min or 1) + 1)
    BurdJournals.Server.addOrIncrementYuletideGiftEntry(gifts, entry.type, itemCount)

    local ammoBoxType = BurdJournals.Server.resolveYuletideGiftAmmoBoxType(entry.type)
    if ammoBoxType then
        BurdJournals.Server.addOrIncrementYuletideGiftEntry(gifts, ammoBoxType, itemCount)
    end
end

local function getYuletideServerText(key, fallback)
    local text = getText(key)
    if text and text ~= "" and text ~= key then
        return text
    end
    return fallback
end

function BurdJournals.Server.normalizeJournalServerText(value)
    if value == nil then
        return nil
    end
    local text = tostring(value)
    text = text:gsub("^%s+", ""):gsub("%s+$", "")
    if text == "" then
        return nil
    end
    return text
end

function BurdJournals.Server.getJournalPlayerDisplayName(player)
    if not player then
        return getYuletideServerText("UI_BurdJournals_UnknownSurvivor", "Unknown Survivor")
    end

    local descriptor = player.getDescriptor and player:getDescriptor() or nil
    if descriptor then
        local forename = BurdJournals.Server.normalizeJournalServerText(descriptor.getForename and descriptor:getForename() or nil)
        local surname = BurdJournals.Server.normalizeJournalServerText(descriptor.getSurname and descriptor:getSurname() or nil)
        if forename and surname then
            return forename .. " " .. surname
        end
        if forename then
            return forename
        end
        if surname then
            return surname
        end
    end

    local displayName = BurdJournals.Server.normalizeJournalServerText(player.getDisplayName and player:getDisplayName() or nil)
    if displayName then
        return displayName
    end

    local username = BurdJournals.Server.normalizeJournalServerText(player.getUsername and player:getUsername() or nil)
    if username then
        return username
    end

    return getYuletideServerText("UI_BurdJournals_UnknownSurvivor", "Unknown Survivor")
end

local function normalizeGeneratedLoreLineBreaks(text)
    if type(text) ~= "string" then
        return text
    end
    return text:gsub("\\r\\n", "\n"):gsub("\\n", "\n")
end

function BurdJournals.Server.getGeneratedLoreText(key, fallback)
    if BurdJournals.Server.getCustomLoreText then
        local customText = BurdJournals.Server.getCustomLoreText(key)
        if customText then
            return normalizeGeneratedLoreLineBreaks(customText)
        end
    end
    local text = getText(key)
    if text and text ~= "" and text ~= key then
        return normalizeGeneratedLoreLineBreaks(text)
    end
    return normalizeGeneratedLoreLineBreaks(fallback)
end

function BurdJournals.Server.appendNormalizedLoreValue(out, seen, value)
    local normalized = BurdJournals.Server.normalizeJournalServerText(value)
    if not normalized then
        return
    end

    local cacheKey = string.lower(normalized)
    if seen[cacheKey] then
        return
    end

    seen[cacheKey] = true
    out[#out + 1] = normalized
end

function BurdJournals.Server.parseLorePoolValues(rawText)
    local out = {}
    local seen = {}
    if type(rawText) ~= "string" then
        return out
    end

    for token in string.gmatch(rawText, "([^|]+)") do
        BurdJournals.Server.appendNormalizedLoreValue(out, seen, token)
    end

    if #out == 0 then
        BurdJournals.Server.appendNormalizedLoreValue(out, seen, rawText)
    end

    return out
end

function BurdJournals.Server.getLorePoolValues(key, fallback)
    local values = BurdJournals.Server.parseLorePoolValues(BurdJournals.Server.getGeneratedLoreText(key, fallback))
    if BurdJournals.Server.applyCustomLorePoolValues then
        return BurdJournals.Server.applyCustomLorePoolValues(key, values)
    end
    return values
end

BurdJournals.CUSTOM_CONTENT_SCHEMA = "BurdJournals.CustomContent"
BurdJournals.CUSTOM_CONTENT_VERSION = 1
BurdJournals.CUSTOM_CONTENT_FILES = {
    "BurdSurvivalJournals/CustomContent.json",
    "BurdSurvivalJournals/CustomLore.json",
    "BurdJournals/CustomContent.json",
    "BurdJournals/CustomLore.json",
    "BurdJournals_CustomContent.json",
    "BurdJournals_CustomLore.json",
}
BurdJournals._customContentLoaded = BurdJournals._customContentLoaded == true
BurdJournals._customContentLoading = false
BurdJournals._customContentSummary = BurdJournals._customContentSummary or {}
BurdJournals._customContentSources = BurdJournals._customContentSources or {}

local function getCustomContentLanguage()
    local core = getCore and getCore() or nil
    local language = nil
    if core then
        if core.getOptionLanguageName then
            language = core:getOptionLanguageName()
        elseif core.getOptionLanguage then
            language = core:getOptionLanguage()
        end
    end
    language = tostring(language or "EN"):upper()
    if language == "" or language == "NULL" then
        language = "EN"
    end
    return language
end

local function getCustomContentCandidateFiles()
    local files = {}
    local seen = {}
    local language = getCustomContentLanguage()
    local function add(path)
        if type(path) ~= "string" or path == "" or seen[path] then
            return
        end
        seen[path] = true
        files[#files + 1] = path
    end
    for _, path in ipairs(BurdJournals.CUSTOM_CONTENT_FILES or {}) do
        add(path)
        local langPath = string.gsub(path, "%.json$", "_" .. language .. ".json")
        add(langPath)
    end
    return files
end

local function customContentFileExists(filename)
    if type(filename) ~= "string" or filename == "" then
        return false
    end
    if not (luajava and luajava.bindClass) then
        return false
    end

    local ok, exists = pcall(function()
        local File = luajava.bindClass("java.io.File")
        local roots = {}
        local function addRoot(root)
            if type(root) == "string" and root ~= "" then
                roots[#roots + 1] = root
            end
        end
        if getMyDocumentFolder then
            local okRoot, root = pcall(getMyDocumentFolder)
            if okRoot then addRoot(root) end
        end
        if getCacheDir then
            local okRoot, root = pcall(getCacheDir)
            if okRoot then addRoot(root) end
        end
        if os and os.getenv then
            local userProfile = os.getenv("USERPROFILE")
            local home = os.getenv("HOME")
            if userProfile then addRoot(userProfile .. "\\Zomboid") end
            if home then addRoot(home .. "\\Zomboid") end
        end

        local direct = File:new(filename)
        if direct and direct:exists() then
            return true
        end
        for _, root in ipairs(roots) do
            local candidate = File:new(root, filename)
            if candidate and candidate:exists() then
                return true
            end
        end
        return false
    end)

    return ok and exists == true
end

local function readCustomContentFile(filename)
    if not (getFileReader and BurdJournals.decodeJournalExportJSON) then
        return nil
    end
    if not customContentFileExists(filename) then
        return nil
    end
    local lines = {}
    local ok = pcall(function()
        local reader = getFileReader(filename, true)
        if not reader then
            return
        end
        local totalBytes = 0
        while true do
            local line = reader:readLine()
            if line == nil then
                break
            end
            totalBytes = totalBytes + string.len(tostring(line))
            if totalBytes > 1024 * 1024 or #lines > 8000 then
                lines = {}
                break
            end
            lines[#lines + 1] = line
        end
        reader:close()
    end)
    if not ok or #lines == 0 then
        return nil
    end
    local decoded, err = BurdJournals.decodeJournalExportJSON(table.concat(lines, "\n"))
    if type(decoded) ~= "table" then
        bsjWriteLogLine("[BurdJournals][CustomContent] ignored " .. tostring(filename) .. ": " .. tostring(err or "invalid JSON"))
        return nil
    end
    return decoded
end

local function customContentArray(raw)
    local values = {}
    local seen = {}
    local function add(value)
        local normalized = BurdJournals.Server.normalizeJournalServerText(value)
        if not normalized then
            return
        end
        local key = string.lower(normalized)
        if seen[key] then
            return
        end
        seen[key] = true
        values[#values + 1] = normalized
    end
    if type(raw) == "string" then
        for token in string.gmatch(raw, "([^|]+)") do
            add(token)
        end
    elseif type(raw) == "table" then
        if #raw > 0 then
            for _, value in ipairs(raw) do
                add(value)
            end
        else
            for key, enabled in pairs(raw) do
                if enabled then
                    add(key)
                end
            end
        end
    end
    return values
end

local function mergeCustomArray(target, raw, replace)
    if type(target) ~= "table" then
        target = {}
    end
    if replace then
        for i = #target, 1, -1 do
            target[i] = nil
        end
    end
    local seen = {}
    for _, value in ipairs(target) do
        local normalized = BurdJournals.Server.normalizeJournalServerText(value)
        if normalized then
            seen[string.lower(normalized)] = true
        end
    end
    for _, value in ipairs(customContentArray(raw)) do
        local key = string.lower(value)
        if not seen[key] then
            seen[key] = true
            target[#target + 1] = value
        end
    end
    return target
end

local function normalizeCustomMode(raw, defaultMode)
    local mode = type(raw) == "table" and raw.mode or nil
    mode = tostring(mode or defaultMode or "append"):lower()
    if mode ~= "replace" then
        mode = "append"
    end
    return mode
end

local function normalizeLorePoolKey(key)
    local raw = tostring(key or "")
    if raw == "" then
        return nil
    end
    if string.sub(raw, 1, 25) == "UI_BurdJournals_LorePool_" then
        return raw
    end
    local lowered = string.lower(raw)
    local map = {
        stashnoun = "UI_BurdJournals_LorePool_StashNoun",
        dangernoun = "UI_BurdJournals_LorePool_DangerNoun",
        supplynoun = "UI_BurdJournals_LorePool_SupplyNoun",
        omennoun = "UI_BurdJournals_LorePool_OmenNoun",
        noun = "UI_BurdJournals_LorePool_Noun",
        nouns = "UI_BurdJournals_LorePool_Noun",
        verb = "UI_BurdJournals_LorePool_Verb",
        verbs = "UI_BurdJournals_LorePool_Verb",
        adjective = "UI_BurdJournals_LorePool_Adjective",
        adjectives = "UI_BurdJournals_LorePool_Adjective",
        adverb = "UI_BurdJournals_LorePool_Adverb",
        adverbs = "UI_BurdJournals_LorePool_Adverb",
        exclamation = "UI_BurdJournals_LorePool_Exclamation",
        exclamations = "UI_BurdJournals_LorePool_Exclamation",
        catchphrase = "UI_BurdJournals_LorePool_Exclamation",
        catchphrases = "UI_BurdJournals_LorePool_Exclamation",
    }
    return map[lowered] or raw
end

local function copyTemplateEntry(raw, fallbackKey)
    if type(raw) == "string" then
        return { text = raw }
    end
    if type(raw) ~= "table" then
        return nil
    end
    local text = raw.text or raw.fallback or raw.template
    local key = raw.key or fallbackKey
    if type(text) ~= "string" and type(key) ~= "string" then
        return nil
    end
    return {
        key = key,
        text = text,
        fallback = text or raw.fallback,
        professionGroups = customContentArray(raw.professionGroups or raw.groups),
        weight = tonumber(raw.weight) or nil,
    }
end

local function applyCustomTextBlock(payload)
    local texts = payload.texts or payload.translations
    if type(texts) ~= "table" then
        return 0
    end
    BurdJournals.Server._customLoreTexts = BurdJournals.Server._customLoreTexts or {}
    local count = 0
    local language = getCustomContentLanguage()
    local function addMap(map)
        if type(map) ~= "table" then
            return
        end
        for key, value in pairs(map) do
            local normalized = nil
            if type(value) ~= "table" then
                normalized = BurdJournals.Server.normalizeJournalServerText(value)
            end
            if type(key) == "string" and normalized then
                BurdJournals.Server._customLoreTexts[key] = normalized
                count = count + 1
            end
        end
    end
    addMap(texts)
    addMap(texts.EN)
    addMap(texts[language])
    return count
end

local function applyCustomNames(payload)
    local names = payload.names or payload.survivorNames
    if type(names) ~= "table" then
        return 0
    end
    local countBefore = #(BurdJournals.RANDOM_FIRST_NAMES or {}) + #(BurdJournals.RANDOM_LAST_NAMES or {})
    BurdJournals.RANDOM_FIRST_NAMES = mergeCustomArray(BurdJournals.RANDOM_FIRST_NAMES, names.first or names.firstNames, names.replaceFirst == true or names.replace == true)
    BurdJournals.RANDOM_LAST_NAMES = mergeCustomArray(BurdJournals.RANDOM_LAST_NAMES, names.last or names.lastNames, names.replaceLast == true or names.replace == true)
    return (#BurdJournals.RANDOM_FIRST_NAMES + #BurdJournals.RANDOM_LAST_NAMES) - countBefore
end

local function applyCustomProfessions(payload)
    local professions = payload.professions
    if type(professions) ~= "table" then
        return 0
    end
    local count = 0
    local profiles = {}
    local byId = {}
    for _, prof in ipairs(BurdJournals.PROFESSIONS or {}) do
        byId[tostring(prof.id)] = prof
    end
    for key, raw in pairs(professions) do
        if type(raw) == "table" then
            local id = raw.id or (type(key) == "string" and key or nil)
            id = BurdJournals.Server.normalizeJournalServerText(id)
            local name = BurdJournals.Server.normalizeJournalServerText(raw.name)
            if id and name then
                local prof = byId[id]
                if not prof then
                    prof = { id = id }
                    BurdJournals.PROFESSIONS[#BurdJournals.PROFESSIONS + 1] = prof
                    byId[id] = prof
                end
                prof.name = name
                prof.nameKey = raw.nameKey
                prof.flavorKey = raw.flavorKey
                profiles[#profiles + 1] = raw
                profiles[#profiles].id = id
                profiles[#profiles].name = name
                count = count + 1
            end
        end
    end
    if #profiles > 0 and BurdJournals.registerProfessionProfiles then
        BurdJournals.registerProfessionProfiles("BSJCustom", profiles)
    end
    return count
end

local function applyCustomLorePools(payload)
    local pools = payload.lorePools or payload.pools or payload.wordPools
    if type(pools) ~= "table" then
        return 0
    end
    BurdJournals.Server._customLorePools = BurdJournals.Server._customLorePools or {}
    local count = 0
    for key, raw in pairs(pools) do
        local normalizedKey = normalizeLorePoolKey(key)
        if normalizedKey then
            local values = type(raw) == "table" and (raw.values or raw.items or raw.words) or raw
            local entry = BurdJournals.Server._customLorePools[normalizedKey] or { values = {}, mode = "append" }
            entry.mode = normalizeCustomMode(raw, entry.mode)
            entry.values = mergeCustomArray(entry.values, values, entry.mode == "replace")
            BurdJournals.Server._customLorePools[normalizedKey] = entry
            count = count + #customContentArray(values)
        end
    end
    return count
end

local function applyCustomLoreTemplates(payload)
    local templates = payload.loreTemplates or payload.templates
    if type(templates) ~= "table" then
        return 0
    end
    BurdJournals.Server._customLoreTemplates = BurdJournals.Server._customLoreTemplates or {}
    local count = 0
    for family, rawList in pairs(templates) do
        local familyId = string.lower(tostring(family or ""))
        if familyId ~= "" then
            local values = type(rawList) == "table" and (rawList.values or rawList.items or rawList.templates) or nil
            if type(values) ~= "table" and type(rawList) == "table" and #rawList > 0 then
                values = rawList
            end
            local entry = BurdJournals.Server._customLoreTemplates[familyId] or { values = {}, mode = "append" }
            entry.mode = normalizeCustomMode(rawList, entry.mode)
            if entry.mode == "replace" then
                entry.values = {}
            end
            if type(values) == "table" then
                for key, raw in pairs(values) do
                    local template = copyTemplateEntry(raw, type(key) == "string" and key or nil)
                    if template then
                        entry.values[#entry.values + 1] = template
                        count = count + 1
                    end
                end
            end
            BurdJournals.Server._customLoreTemplates[familyId] = entry
        end
    end
    return count
end

local function applyCustomProfessionGroups(payload)
    local groups = payload.professionGroups or payload.loreProfessionGroups
    if type(groups) ~= "table" then
        return 0
    end
    BurdJournals.Server.LORE_PROFESSION_GROUPS = BurdJournals.Server.LORE_PROFESSION_GROUPS or {}
    local count = 0
    for groupId, raw in pairs(groups) do
        local normalizedGroup = string.lower(tostring(groupId or ""))
        if normalizedGroup ~= "" then
            local group = BurdJournals.Server.LORE_PROFESSION_GROUPS[normalizedGroup] or {}
            if type(raw) == "table" and raw.mode == "replace" then
                group = {}
            end
            local values = type(raw) == "table" and (raw.values or raw.items or raw.professions) or raw
            for _, professionId in ipairs(customContentArray(values)) do
                group[string.lower(professionId)] = true
                count = count + 1
            end
            BurdJournals.Server.LORE_PROFESSION_GROUPS[normalizedGroup] = group
        end
    end
    return count
end

function BurdJournals.Server.getCustomLoreText(key)
    if BurdJournals.ensureCustomContentLoaded then
        BurdJournals.ensureCustomContentLoaded("getCustomLoreText")
    end
    local text = BurdJournals.Server._customLoreTexts and BurdJournals.Server._customLoreTexts[key] or nil
    return BurdJournals.Server.normalizeJournalServerText(text)
end

function BurdJournals.Server.applyCustomLorePoolValues(key, values)
    if BurdJournals.ensureCustomContentLoaded then
        BurdJournals.ensureCustomContentLoaded("applyCustomLorePoolValues")
    end
    local normalizedKey = normalizeLorePoolKey(key)
    local entry = normalizedKey and BurdJournals.Server._customLorePools and BurdJournals.Server._customLorePools[normalizedKey] or nil
    if type(entry) ~= "table" then
        return values
    end
    local out = {}
    if entry.mode ~= "replace" then
        for _, value in ipairs(values or {}) do
            out[#out + 1] = value
        end
    end
    return mergeCustomArray(out, entry.values, false)
end

function BurdJournals.Server.applyCustomLoreTemplatePool(family, basePool)
    if BurdJournals.ensureCustomContentLoaded then
        BurdJournals.ensureCustomContentLoaded("applyCustomLoreTemplatePool")
    end
    local familyId = string.lower(tostring(family or "worn"))
    local entry = BurdJournals.Server._customLoreTemplates and BurdJournals.Server._customLoreTemplates[familyId] or nil
    if type(entry) ~= "table" then
        return basePool
    end
    local out = {}
    if entry.mode ~= "replace" then
        for _, template in ipairs(basePool or {}) do
            out[#out + 1] = template
        end
    end
    for _, template in ipairs(entry.values or {}) do
        out[#out + 1] = template
    end
    return out
end

function BurdJournals.applyCustomContentPayload(payload, sourceTag)
    if type(payload) ~= "table" then
        return nil
    end
    local summary = {
        source = sourceTag or "unknown",
        texts = applyCustomTextBlock(payload),
        names = applyCustomNames(payload),
        professions = applyCustomProfessions(payload),
        pools = applyCustomLorePools(payload),
        templates = applyCustomLoreTemplates(payload),
        groups = applyCustomProfessionGroups(payload),
    }
    BurdJournals._customContentSummary[#BurdJournals._customContentSummary + 1] = summary
    return summary
end

function BurdJournals.ensureCustomContentLoaded(sourceTag)
    if BurdJournals._customContentLoaded or BurdJournals._customContentLoading then
        return BurdJournals._customContentLoaded
    end
    BurdJournals._customContentLoading = true
    local loaded = 0
    for _, filename in ipairs(getCustomContentCandidateFiles()) do
        local payload = readCustomContentFile(filename)
        if type(payload) == "table" then
            local summary = BurdJournals.applyCustomContentPayload(payload, filename)
            if summary then
                loaded = loaded + 1
                BurdJournals._customContentSources[#BurdJournals._customContentSources + 1] = filename
                bsjWriteLogLine("[BurdJournals][CustomContent] loaded "
                    .. tostring(filename)
                    .. " texts=" .. tostring(summary.texts)
                    .. " names=" .. tostring(summary.names)
                    .. " professions=" .. tostring(summary.professions)
                    .. " pools=" .. tostring(summary.pools)
                    .. " templates=" .. tostring(summary.templates)
                    .. " groups=" .. tostring(summary.groups))
            end
        end
    end
    BurdJournals._customContentLoaded = true
    BurdJournals._customContentLoading = false
    if loaded <= 0 then
        BurdJournals.debugPrint("[BurdJournals] Custom content: no external JSON loaded (" .. tostring(sourceTag or "startup") .. ")")
    end
    return true
end

function BurdJournals.Server.countLoreSentences(text)
    local count = 0
    local source = tostring(text or "")
    for _ in source:gmatch("[%.%!%?]+") do
        count = count + 1
    end
    if count == 0 and BurdJournals.Server.normalizeJournalServerText(source) then
        count = 1
    end
    return count
end

function BurdJournals.Server.trimLoreToMaxSentences(text, maxSentences)
    local normalized = BurdJournals.Server.normalizeJournalServerText(text)
    if not normalized then
        return nil
    end

    local maxCount = math.max(1, tonumber(maxSentences) or 8)
    local pieces = {}
    local sentenceCount = 0
    local lastEnd = 1

    for endIndex in normalized:gmatch("()[%.%!%?]+") do
        local stop = endIndex
        pieces[#pieces + 1] = normalized:sub(lastEnd, stop)
        sentenceCount = sentenceCount + 1
        lastEnd = stop + 1
        if sentenceCount >= maxCount then
            break
        end
    end

    if #pieces == 0 then
        return normalized
    end
    if sentenceCount < maxCount then
        return normalized
    end

    return BurdJournals.Server.normalizeJournalServerText(table.concat(pieces, " "))
end

BurdJournals.Server.LORE_NOTE_TEMPLATE_VERSION = tonumber(BurdJournals.Server and BurdJournals.Server.LORE_NOTE_TEMPLATE_VERSION) or 1
LORE_DYNAMIC_VERSION = tonumber(BurdJournals.Server.LORE_NOTE_TEMPLATE_VERSION) or 1

BurdJournals.Server.LORE_PROFESSION_GROUPS = BurdJournals.Server.LORE_PROFESSION_GROUPS or {
    medical = {
        doctor = true,
        nurse = true,
    },
    outdoors = {
        parkranger = true,
        farmer = true,
        fisherman = true,
        hunter = true,
        lumberjack = true,
        survivalist = true,
    },
    combat = {
        fireofficer = true,
        policeofficer = true,
        securityguard = true,
        veteran = true,
        soldier = true,
        fighter = true,
    },
    stealth = {
        burglar = true,
    },
    trades = {
        carpenter = true,
        constructionworker = true,
        repairman = true,
        electrician = true,
        engineer = true,
        metalworker = true,
        mechanics = true,
        tailor = true,
    },
    food = {
        chef = true,
        burgerflipper = true,
    },
    fitness = {
        fitnessInstructor = true,
        athlete = true,
    },
}

function BurdJournals.Server.getLoreProfessionGroups(professionId)
    local groups = {}
    if type(professionId) ~= "string" or professionId == "" then
        return groups
    end

    local normalized = string.lower(professionId)
    for groupId, entries in pairs(BurdJournals.Server.LORE_PROFESSION_GROUPS) do
        if entries[normalized] then
            groups[groupId] = true
        end
    end

    return groups
end

function BurdJournals.Server.getLoreTokenBase(token)
    if type(token) ~= "string" or token == "" then
        return nil
    end

    local exactTokens = {
        openerName = true,
        authorName = true,
        professionName = true,
        survivorName = true,
        stashNoun = true,
        dangerNoun = true,
        supplyNoun = true,
        omenNoun = true,
        noun = true,
        verb = true,
        adjective = true,
        adverb = true,
        exclamation = true,
    }
    if exactTokens[token] then
        return token
    end
    if string.sub(token, 1, 9) == "entryName" then
        return "entryName"
    end
    if string.sub(token, 1, 11) == "exclamation" then
        return "exclamation"
    end
    if string.sub(token, 1, 9) == "adjective" then
        return "adjective"
    end
    if string.sub(token, 1, 6) == "adverb" then
        return "adverb"
    end
    if string.sub(token, 1, 4) == "noun" then
        return "noun"
    end
    if string.sub(token, 1, 4) == "verb" then
        return "verb"
    end
    if string.sub(token, 1, 9) == "skillName" then
        return "skillName"
    end
    if string.sub(token, 1, 9) == "traitName" then
        return "traitName"
    end
    if string.sub(token, 1, 10) == "recipeName" then
        return "recipeName"
    end
    return nil
end

function BurdJournals.Server.extractLoreTemplateTokens(templateText)
    local tokens = {}
    local seen = {}
    if type(templateText) ~= "string" then
        return tokens
    end

    for token in templateText:gmatch("{{([%w_]+)}}") do
        if not seen[token] then
            seen[token] = true
            tokens[#tokens + 1] = token
        end
    end

    return tokens
end

function BurdJournals.Server.buildLoreDisplayPoolFromTable(input, formatter)
    local out = {}
    local seen = {}
    if type(input) ~= "table" then
        return out
    end

    for key in pairs(input) do
        BurdJournals.Server.appendNormalizedLoreValue(out, seen, formatter and formatter(key) or key)
    end

    return out
end

function BurdJournals.Server.buildLoreContext(player, journalData, family)
    local data = type(journalData) == "table" and journalData or {}
    local resolvedFamily = family
    if not resolvedFamily or resolvedFamily == "" then
        if data.isYuletideJournal == true then
            resolvedFamily = "yuletide"
        elseif data.isCursedReward == true then
            resolvedFamily = (data.profession == "yuletide_krampus" and "krampus") or "cursed"
        elseif data.isBloody == true or data.wasFromBloody == true or data.isZombieJournal == true then
            resolvedFamily = "bloody"
        else
            resolvedFamily = "worn"
        end
    end

    local openerName = BurdJournals.Server.getJournalPlayerDisplayName(player)
    local authorName = BurdJournals.Server.normalizeJournalServerText(data.author)
        or (BurdJournals.generateRandomSurvivorName and BurdJournals.generateRandomSurvivorName())
        or openerName
    local professionName = BurdJournals.Server.normalizeJournalServerText(
        BurdJournals.resolveProfessionName and BurdJournals.resolveProfessionName(data) or data.professionName
    ) or getYuletideServerText("UI_BurdJournals_UnknownProfession", "Unknown Profession")
    local survivorName = BurdJournals.generateRandomSurvivorName and BurdJournals.generateRandomSurvivorName() or authorName

    local skillPool = BurdJournals.Server.buildLoreDisplayPoolFromTable(data.skills, function(skillName)
        return BurdJournals.getPerkDisplayName and BurdJournals.getPerkDisplayName(skillName) or skillName
    end)
    if #skillPool == 0 and BurdJournals.getAllowedSkills then
        local allowedSkills = BurdJournals.getAllowedSkills() or {}
        local seen = {}
        for _, skillName in ipairs(allowedSkills) do
            BurdJournals.Server.appendNormalizedLoreValue(skillPool, seen, BurdJournals.getPerkDisplayName and BurdJournals.getPerkDisplayName(skillName) or skillName)
        end
    end

    local includeNegativeTraits = resolvedFamily == "cursed" or resolvedFamily == "krampus"
    local traitPool = BurdJournals.Server.buildLoreDisplayPoolFromTable(data.traits, function(traitId)
        return BurdJournals.getTraitDisplayName and BurdJournals.getTraitDisplayName(traitId) or traitId
    end)
    if #traitPool == 0 and BurdJournals.getGrantableTraits then
        local discoveredTraits = BurdJournals.getGrantableTraits(includeNegativeTraits) or {}
        local seen = {}
        for _, traitId in ipairs(discoveredTraits) do
            BurdJournals.Server.appendNormalizedLoreValue(traitPool, seen, BurdJournals.getTraitDisplayName and BurdJournals.getTraitDisplayName(traitId) or traitId)
        end
    end

    local recipePool = BurdJournals.Server.buildLoreDisplayPoolFromTable(data.recipes, function(recipeName)
        return BurdJournals.getRecipeDisplayName and BurdJournals.getRecipeDisplayName(recipeName) or recipeName
    end)
    if #recipePool == 0 and BurdJournals.getAllMagazineRecipes then
        local discoveredRecipes = BurdJournals.getAllMagazineRecipes() or {}
        local seen = {}
        for _, recipeName in ipairs(discoveredRecipes) do
            BurdJournals.Server.appendNormalizedLoreValue(recipePool, seen, BurdJournals.getRecipeDisplayName and BurdJournals.getRecipeDisplayName(recipeName) or recipeName)
        end
    end

    local entryPool = {}
    local entrySeen = {}
    for _, pool in ipairs({ skillPool, traitPool, recipePool }) do
        for _, entryName in ipairs(pool or {}) do
            BurdJournals.Server.appendNormalizedLoreValue(entryPool, entrySeen, entryName)
        end
    end

    return {
        family = resolvedFamily,
        journalData = data,
        openerName = openerName,
        authorName = authorName,
        professionName = professionName,
        professionId = data.profession,
        survivorName = BurdJournals.Server.normalizeJournalServerText(survivorName) or authorName,
        professionGroups = BurdJournals.Server.getLoreProfessionGroups(data.profession),
        tokenPools = {
            skillName = skillPool,
            traitName = traitPool,
            recipeName = recipePool,
            entryName = entryPool,
            stashNoun = BurdJournals.Server.getLorePoolValues(
                "UI_BurdJournals_LorePool_StashNoun",
                "the loose floorboard|the false back|the coffee tin|the box spring|the hollow dictionary|the taped vent|the glove compartment|the flour canister|the basement freezer|the church donation box|the tackle box|the rusted locker|the motel Bible|the rain barrel lid|the busted jukebox|the hollowed radio|the pantry wall|the school desk|the ambulance kit|the station wagon"
            ),
            dangerNoun = BurdJournals.Server.getLorePoolValues(
                "UI_BurdJournals_LorePool_DangerNoun",
                "the dark|the dead|the smoke|the noise outside|the fever ward|the fenced road|the coughing crowd|the military line|the red checkpoint|the flooded ditch|the pharmacy riot|the static on the radio|the meat locker|the church basement|the bus depot|the quarantine tents|the screaming stairwell|the empty school|the black rain|the sirens at dawn|the banging in the vents|the wrong kind of silence"
            ),
            supplyNoun = BurdJournals.Server.getLorePoolValues(
                "UI_BurdJournals_LorePool_SupplyNoun",
                "the canned food|the dry socks|the spare batteries|the clean bandages|the bottle of bleach|the last insulin|the map with grease pencil marks|the radio batteries|the cracked compass|the rainwater jars|the rolled tarp|the sewing kit|the unopened coffee|the box of shells|the antibiotics|the hand-crank flashlight|the fuel siphon|the warm gloves|the flathead screwdriver|the ration crackers|the iodine tablets|the extra laces"
            ),
            omenNoun = BurdJournals.Server.getLorePoolValues(
                "UI_BurdJournals_LorePool_OmenNoun",
                "the whispering ink|the seal|the thing in the margin|the debt beneath the page|the black star over Knox|the bell with no church|the mouth in the paper|the deer skull at the roadblock|the red snow|the name under the name|the unpaid winter|the shadow in the broadcast|the tally behind the moon|the courthouse candles|the static choir|the page that sweats|the handprint under the ink|the locked door in the dream|the thing wearing a familiar voice"
            ),
            noun = BurdJournals.Server.getLorePoolValues(
                "UI_BurdJournals_LorePool_Noun",
                "the barricade|the stairwell|the radio|the sink|the mailbox|the pantry|the generator|the church bell|the culvert|the meat counter|the school bus|the payphone|the water tower|the county map|the bridge|the laundromat|the diner sign|the cattle fence|the motel pool|the cornfield|the siren pole|the triage cot|the dog bowl|the patrol car|the freezer aisle|the storm drain|the grave marker|the vending machine|a ham sandwich|the man|the woman|the person|the animal|the dog|the cat|the horse|the cow|the pig|the chicken|the bird|the goat|the sheep|the mule|the rabbit|the raccoon|the possum|the deer|the rat|the mouse|the goose|the duck|the crow|the vulture|the moth|the spider|the fly|the worm|the frog|the turtle|the snake|the fish|the racetrack|the lunchbox|the office chair|the mop bucket|the bus stop|the bowling trophy|the garden hose|the toolbox|the fire axe|the tire iron|the coffee mug|the ashtray|the raincoat|the shopping cart|the cereal box|the birthday candle|the cassette tape|the pocket watch|the loose tooth|the house key|the plastic flamingo|the church pew|the motel key|the taxidermy head|the lawn mower|the broken umbrella|the sock drawer|the folding chair|the soup can|the parking meter|the gas pump|the county clerk|the substitute teacher|the milkman|the undertaker|the preacher|the mail carrier|the night janitor|the crossing guard"
            ),
            verb = BurdJournals.Server.getLorePoolValues(
                "UI_BurdJournals_LorePool_Verb",
                "whispered|rattled|burned|vanished|coughed|waited|scratched|answered|blinked|groaned|leaked|sang|knocked|counted|split|stared|dripped|begged|clicked|shivered|laughed|hummed|breathed|followed|spat|floated|rang|stuttered|ached|remembered|hollowed out|skipped|squatted|thrusted|languished|lamented|jimmied|fiddled|fondled|messed with|tussled up|tossed around|bludgeoned|kicked|skewered|slayed|crouched|babbled|scattered|scrambled"
            ),
            adjective = BurdJournals.Server.getLorePoolValues(
                "UI_BurdJournals_LorePool_Adjective",
                "quiet|wrong|hungry|shaking|bright|hollow|stubborn|merciful|feverish|official|muddy|toothless|boiled|radio-sick|red-lit|half-buried|unmarked|salted|laughing|ash-gray|clean|military|blessed|condemned|frozen|itching|coppery|paper-thin|roadside|borrowed|rotting|useful|unforgiving|sticky|gooey|tactile|unnecessary|altruistic|feeble-minded|obtuse|voyeuristic|weak|promising|harrowing|nightmarish|uplifting|green|happy-go-lucky|scared|tiny|larger-than-life|poopy|gassy|bloated|finnicky|buoyant"
            ),
            adverb = BurdJournals.Server.getLorePoolValues(
                "UI_BurdJournals_LorePool_Adverb",
                "quietly|badly|too late|all night|without mercy|by dawn|under my breath|like a fool|through the static|behind the fence|with both hands|before curfew|after the broadcast|in the rain|under the sirens|for no one|like it knew me|until morning|past the roadblock|with the lights off|between coughs|while counting bullets|as politely as possible|with Christmas music playing|where the map ended|between strokes|while sprinting|while chuckling"
            ),
            exclamation = BurdJournals.Server.getLorePoolValues(
                "UI_BurdJournals_LorePool_Exclamation",
                "God help whoever reads this|Keep your head down|Do not trust the quiet|Merry Christmas, survivor|Run while the road is clear|The phones lied first|Count the living twice|Leave before the birds stop|If it knocks, do not answer|The uniforms knew too much|No cure came down that road|I am sorry about the basement|Keep one bullet for the door|The radio is not your friend|Do not drink from the barrel|Tell Rose I tried|Burn this after reading|The snow has teeth|The page is warm for a reason|Take the back roads|Hope is heavy, pack light|The cake is a lie|I just want to be pure|Oh God oh Jesus|Somebody hold my hand|Shit stinks, bro|Oh the humanity|No excuse on judgement day|What is the charge?|You lose, you get nothing"
            ),
        },
        tokenPoolKeys = {
            stashNoun = "UI_BurdJournals_LorePool_StashNoun",
            dangerNoun = "UI_BurdJournals_LorePool_DangerNoun",
            supplyNoun = "UI_BurdJournals_LorePool_SupplyNoun",
            omenNoun = "UI_BurdJournals_LorePool_OmenNoun",
            noun = "UI_BurdJournals_LorePool_Noun",
            verb = "UI_BurdJournals_LorePool_Verb",
            adjective = "UI_BurdJournals_LorePool_Adjective",
            adverb = "UI_BurdJournals_LorePool_Adverb",
            exclamation = "UI_BurdJournals_LorePool_Exclamation",
        },
    }
end

BurdJournals.Server.LORE_TEMPLATE_SETS = BurdJournals.Server.LORE_TEMPLATE_SETS or {
    worn = {
        {
            key = "UI_BurdJournals_LoreTemplate_WornPages",
            fallback = "If {{openerName}} finds this, {{authorName}} meant the pages to outlast them. I kept {{entryName}} notes behind {{stashNoun}} after {{noun}} {{verb}} {{adverb}}.",
            professionGroups = {"trades", "outdoors", "medical"},
        },
        {
            key = "UI_BurdJournals_LoreTemplate_WornTrade",
            fallback = "I meant to trade {{recipeName}} with {{survivorName}} before the roads closed. If {{openerName}} reads this now, keep moving. {{exclamation}}.",
            professionGroups = {"trades", "food", "stealth"},
        },
        {
            key = "UI_BurdJournals_LoreTemplate_WornProfession",
            fallback = "Being a {{professionName}} taught me {{skillNameA}}, but {{traitName}} kept me breathing when things got {{adjective}}. The rest is in the margins.",
            professionGroups = {"medical", "combat", "fitness"},
        },
        {
            key = "UI_BurdJournals_LoreTemplate_WornWarning",
            fallback = "{{authorName}} wrote this after a run through {{dangerNoun}} for {{recipeName}} went bad. Trust {{skillName}} more than the map, and never follow {{noun}} {{adverb}}.",
        },
        {
            key = "UI_BurdJournals_LoreTemplate_WornQuarantine",
            fallback = "The television kept calling it a disturbance, then an exclusion zone, then nothing at all. {{authorName}} wrote down every broadcast that still came through, especially the ones that mentioned soldiers, roadblocks, and the smell drifting out of Knox County. None of it matched what I saw from the windows.\n\nI was a {{professionName}} before the phones died, so I tried to keep this practical: {{entryNameA}} first, {{entryNameB}} second, and only trust {{supplyNoun}} if you packed it yourself. If {{openerName}} finds these pages, remember that the official voices sounded calm right up until they stopped.",
            professionGroups = {"medical", "combat", "trades"},
        },
        {
            key = "UI_BurdJournals_LoreTemplate_WornRoadblock",
            fallback = "We reached the checkpoint before dawn and the line already stretched past {{noun}}. A man in uniform said the sickness was contained; another had blood on his sleeve and would not look at the families begging to cross. {{survivorName}} said they knew earlier than they admitted, because nobody builds that many barricades overnight.\n\nI do not know if it was the air, the water, or something buried under a lab with no name. I only know {{skillNameA}} kept us alive longer than hope did, and the note on {{recipeName}} belongs to whoever can still use both hands.",
            professionGroups = {"combat", "outdoors", "stealth"},
        },
        {
            key = "UI_BurdJournals_LoreTemplate_WornRadioLog",
            fallback = "{{authorName}} kept a broadcast log until the voices started repeating yesterday's weather. The emergency station told us to shelter, the governor told us to cooperate, and one exhausted reporter told us the roads south were already gone before someone cut her microphone.\n\nI wrote the useful parts in plain language: {{entryNameA}} for the hands, {{entryNameB}} for the head, and {{supplyNoun}} if you can find any. If {{openerName}} hears a calm voice promising evacuation, check whether the voice is breathing.",
            professionGroups = {"trades", "medical", "combat"},
        },
        {
            key = "UI_BurdJournals_LoreTemplate_WornSchoolhouse",
            fallback = "The school became a shelter because it had brick walls, wide doors, and enough chairs to pretend this was temporary. By the third night, {{dangerNoun}} was outside and nobody wanted to say the children were quieter than the adults.\n\nI was only a {{professionName}}, not a leader, but I learned which rooms locked and which windows lied. {{skillName}} mattered more than speeches, {{recipeName}} mattered more than prayer, and {{traitName}} was the sort of thing people noticed after the lights went out.",
            professionGroups = {"medical", "food", "trades"},
        },
        {
            key = "UI_BurdJournals_LoreTemplate_WornBackRoad",
            fallback = "{{survivorName}} said the state roads were traps, so we followed fence lines and mailboxes until the map stopped admitting we existed. Every farmhouse had the same story: one door kicked in, one radio left on, one pot gone cold on the stove.\n\nFor {{openerName}}, the route notes are useless now, but the practice is not. Trust {{skillNameA}}, keep {{entryNameB}} dry, and never assume {{noun}} is empty just because it looks {{adjective}}.",
            professionGroups = {"outdoors", "stealth", "fitness"},
        },
        {
            key = "UI_BurdJournals_LoreTemplate_WornClinic",
            fallback = "The clinic smelled like bleach, sweat, and lies. They told us bites were panic injuries. They told us fever was stress. They told us the coughing people in the back room were being transferred, but the ambulance never moved.\n\n{{authorName}} wrote these notes between shifts because hands need something to do when belief runs out. Use {{skillName}} first, use {{recipeName}} if supplies hold, and do not let {{traitName}} make you generous with masks.",
            professionGroups = {"medical"},
        },
        {
            key = "UI_BurdJournals_LoreTemplate_WornStorefront",
            fallback = "The first riot was not for guns. It was for batteries, formula, and anything with a pull tab. {{authorName}} watched a man trade wedding rings for {{supplyNoun}} while the radio insisted supply lines were stable.\n\nI wrote this after we shuttered the storefront and counted what was left. {{entryNameA}} is what I wish I knew before the shelves emptied. {{entryNameB}} is what I learned after {{noun}} {{verb}} {{adverb}}.",
            professionGroups = {"food", "trades", "stealth"},
        },
        {
            key = "UI_BurdJournals_LoreTemplate_WornStorm",
            fallback = "Rain made everything worse. It hid the footsteps, washed chalk marks off doors, and turned the ditch by {{noun}} into soup thick enough to keep secrets. By morning, the dead were cleaner than the living.\n\nIf {{openerName}} finds the dry pages, start with {{skillName}}. The rest is just weather and stubbornness, except for the note about {{recipeName}}. That one kept {{survivorName}} alive one more day.",
            professionGroups = {"outdoors", "fitness", "trades"},
        },
        {
            key = "UI_BurdJournals_LoreTemplate_WornChurchBasement",
            fallback = "The church basement filled before the sanctuary did. People trusted concrete more than stained glass once {{dangerNoun}} reached the steps, and the preacher traded sermons for inventory lists by candlelight.\n\n{{authorName}} wrote these pages beside {{stashNoun}} while the radio upstairs promised order. Keep {{entryNameA}} close, copy {{entryNameB}} if the page tears, and remember that locked doors make neighbors honest.",
            professionGroups = {"medical", "food", "stealth"},
        },
        {
            key = "UI_BurdJournals_LoreTemplate_WornDepot",
            fallback = "The bus depot became a place for people who almost left. Suitcases stayed packed, tickets stayed in pockets, and every schedule board kept insisting another departure was due. {{survivorName}} laughed at that until {{noun}} {{verb}} by the vending machines.\n\nFor {{openerName}}, the route is gone but the method remains. {{skillName}} gets you through crowds, {{recipeName}} gets you through hunger, and {{traitName}} is useful only if it does not make you loud.",
            professionGroups = {"stealth", "fitness", "food"},
        },
        {
            key = "UI_BurdJournals_LoreTemplate_WornPharmacyLine",
            fallback = "The pharmacy line wrapped around the building before sunrise. People coughed into sleeves, into scarves, into the hands they used to push each other when the shutters did not open. A deputy said medicine was being distributed fairly, then slipped a bag to his cousin.\n\nI wrote this after fairness became a rumor. {{entryNameA}} may help {{openerName}} more than a bottle ever did, and {{entryNameB}} is what I wish I knew before {{supplyNoun}} became currency.",
            professionGroups = {"medical", "stealth"},
        },
        {
            key = "UI_BurdJournals_LoreTemplate_WornRailYard",
            fallback = "The rail yard smelled like diesel, rain, and old promises. Someone said military trains had moved through at night before the broadcasts changed. Someone else said the cars were empty. Nobody wanted to explain the fresh chains.\n\n{{authorName}} kept notes under {{stashNoun}} because paper is lighter than proof. If {{openerName}} can use {{skillName}}, use it quietly. If {{recipeName}} looks foolish now, wait until the pantry is empty.",
            professionGroups = {"trades", "stealth", "outdoors"},
        },
        {
            key = "UI_BurdJournals_LoreTemplate_WornWaterTower",
            fallback = "We climbed the water tower to see where the fires stopped. They did not stop. Smoke marked farms, subdivisions, and one place by the highway where headlights still circled like moths around a bad idea.\n\nThe view made {{authorName}} practical. {{entryNameA}} became a checklist, {{entryNameB}} became a warning, and {{supplyNoun}} became the difference between plans and prayers. If {{openerName}} climbs, count ladders before horizons.",
            professionGroups = {"outdoors", "fitness", "trades"},
        },
        {
            key = "UI_BurdJournals_LoreTemplate_WornDinerShift",
            fallback = "The diner stayed open longer than sense allowed. Coffee made people brave, pancakes made them human, and the bell over the door made everyone reach for something sharp. {{survivorName}} kept saying normal would come back hungry.\n\nIt did not. I left {{skillName}} in the margin beside the old specials and folded {{recipeName}} into the register tape. If {{openerName}} finds the place, do not eat anything from the fridge unless you enjoy regret.",
            professionGroups = {"food", "trades"},
        },
        {
            key = "UI_BurdJournals_LoreTemplate_WornCountyMap",
            fallback = "We pinned the county map to drywall and stabbed it full of guesses. Red for blocked roads, blue for water, black for places nobody came back from. By Friday, black was winning.\n\n{{authorName}} wrote what the map could not. {{entryNameA}} is for moving, {{entryNameB}} is for stopping, and {{traitName}} is for knowing which one you are doing before {{dangerNoun}} decides for you.",
            professionGroups = {"outdoors", "combat", "stealth"},
        },
        {
            key = "UI_BurdJournals_LoreTemplate_WornMotelRoom",
            fallback = "Room 12 had two beds, one working lamp, and a Bible hollowed out for bullets. The guest book said people came from Louisville, Muldraugh, and places they did not want written down after the fence went up.\n\nI was a {{professionName}} before I was a room number. {{skillName}} kept me useful, {{recipeName}} kept me busy, and {{exclamation}} is what I wrote on the mirror before we left.",
            professionGroups = {"combat", "stealth", "trades"},
        },
        {
            key = "UI_BurdJournals_LoreTemplate_WornLibrary",
            fallback = "The library was quiet in a way that felt expensive. People took medical books first, then maps, then anything thick enough to tape over windows. The children hid in the genealogy aisle because family names felt protective.\n\n{{authorName}} copied {{entryNameA}} from memory and tucked {{entryNameB}} between county records. If {{openerName}} finds this, return nothing. Late fees are a luxury.",
            professionGroups = {"medical", "trades", "food"},
        },
        {
            key = "UI_BurdJournals_LoreTemplate_WornTrailerPark",
            fallback = "The trailer park organized faster than city hall. Folks knew who had tools, who had fuel, and who would steal both if left alone. We strung cans on wire and called it infrastructure.\n\n{{survivorName}} taught me that {{skillName}} matters most when nobody is impressed by it. I added {{recipeName}} after {{noun}} {{verb}} near the mailboxes, and I left {{traitName}} circled because pride gets noisy in thin walls.",
            professionGroups = {"trades", "outdoors", "stealth"},
        },
        {
            key = "UI_BurdJournals_LoreTemplate_WornFieldHospital",
            fallback = "They put the field hospital on the football field because the bleachers gave people somewhere to watch hope fail. Triage tags moved faster than stretchers, and every mask looked clean until someone spoke through it.\n\nIf {{openerName}} is reading this, use {{entryNameA}} before kindness. Use {{entryNameB}} before panic. {{authorName}} learned both while {{dangerNoun}} waited outside the fence like a patient with no chart.",
            professionGroups = {"medical", "combat"},
        },
        {
            key = "UI_BurdJournals_LoreTemplate_WornHardwareStore",
            fallback = "The hardware store sold out of locks before it sold out of hammers. That should have told us plenty. People bought plywood like it was medicine and asked for refunds when the dead did not respect carpentry.\n\n{{authorName}} kept notes by the paint mixer. {{skillName}} is useful, {{recipeName}} is better, and {{supplyNoun}} is worth more than cash if {{openerName}} knows where to hide it.",
            professionGroups = {"trades", "combat", "outdoors"},
        },
        {
            key = "UI_BurdJournals_LoreTemplate_WornBirthday",
            fallback = "Somebody had a birthday during the first week and we sang because not singing felt worse. The cake was canned peaches on crackers. The candles were cigarette lighters. The wish was obvious and still did not come true.\n\nI wrote {{entryNameA}} after the song and {{entryNameB}} after the screaming. If {{openerName}} finds this, celebrate small things quickly. They attract attention if they last too long.",
            professionGroups = {"food", "medical", "fitness"},
        },
        {
            key = "UI_BurdJournals_LoreTemplate_WornMadLibsFence",
            fallback = "{{authorName}} watched {{adjective}} {{noun}} {{verb}} {{adverb}} near {{stashNoun}} while {{survivorName}} argued that {{entryNameA}} was more important than {{entryNameB}}.\n\nIf {{openerName}} finds the page, remember this: {{skillName}} gets {{adjective}} when {{dangerNoun}} {{verb}}, {{recipeName}} belongs beside {{supplyNoun}}, and {{exclamation}}.",
            professionGroups = {"outdoors", "stealth", "trades"},
        },
        {
            key = "UI_BurdJournals_LoreTemplate_WornMadLibsCounty",
            fallback = "The county map said nothing about {{omenNoun}}, {{adjective}} {{noun}}, or the way {{supplyNoun}} {{verb}} {{adverb}} whenever the broadcast used the word contained.\n\n{{openerName}} should keep {{entryNameA}} under {{stashNoun}}, trade {{entryNameB}} only for {{supplyNoun}}, and never trust {{traitName}} when the road looks {{adjective}}.",
            professionGroups = {"combat", "outdoors", "medical"},
        },
        {
            key = "UI_BurdJournals_LoreTemplate_WornSkillOnlyNotebook",
            fallback = "{{authorName}} wrote {{skillNameA}} in big block letters because panic makes small writing useless. The second page is {{skillNameB}}, copied twice, once before the sirens and once after.\n\nIf {{openerName}} finds this, practice both before {{dangerNoun}} gets close. A skill remembered late is just regret with better spelling.",
        },
        {
            key = "UI_BurdJournals_LoreTemplate_WornSkillOnlyRoutine",
            fallback = "Routine kept us human longer than optimism did. Every morning: check doors, count water, repeat {{skillNameA}}, then repeat {{skillNameB}} until the hands know it without permission.\n\n{{openerName}} should not wait to feel ready. Read the notes, move {{adverb}}, and let {{adjective}} fear watch from the porch.",
            professionGroups = {"fitness", "outdoors", "trades"},
        },
        {
            key = "UI_BurdJournals_LoreTemplate_WornSkillOnlyPractice",
            fallback = "The first try at {{skillNameA}} was ugly. The second was louder. By the fifth, {{authorName}} understood that competence is mostly embarrassment survived in private.\n\n{{skillNameB}} came later, after {{noun}} {{verb}} near {{stashNoun}}. If {{openerName}} has time to laugh at the mistakes, there is still time to learn from them.",
        },
        {
            key = "UI_BurdJournals_LoreTemplate_WornSkillOnlyFieldNotes",
            fallback = "Field note one: {{skillNameA}} works better before you are tired. Field note two: {{skillNameB}} works better before you are scared. Field note three: you will be both.\n\n{{authorName}} left the rest blank for {{openerName}} to fill with better luck. Keep the page dry and the lesson close.",
            professionGroups = {"outdoors", "medical", "combat"},
        },
        {
            key = "UI_BurdJournals_LoreTemplate_WornSkillOnlyRadioLesson",
            fallback = "A calm voice on the radio said survival depended on cooperation. Nobody explained how to cooperate with a locked pharmacy, a dead battery, and {{dangerNoun}} by the window.\n\nSo {{authorName}} wrote down {{skillNameA}} and {{skillNameB}} instead. They are less polite than speeches, but {{openerName}} may find them more useful.",
            professionGroups = {"trades", "stealth", "medical"},
        },
        {
            key = "UI_BurdJournals_LoreTemplate_WornMadLibsQuarantine",
        },
        {
            key = "UI_BurdJournals_LoreTemplate_WornMadLibsRoadblock",
        },
        {
            key = "UI_BurdJournals_LoreTemplate_WornMadLibsStorm",
        },
        {
            key = "UI_BurdJournals_LoreTemplate_WornSkillOnlyTrade",
        },
        {
            key = "UI_BurdJournals_LoreTemplate_WornGeneral",
            fallback = "The first week taught {{authorName}} to distrust clean answers. The television called Knox County a disturbance, then a quarantine, then said less and less while the fences said everything. People argued over air, water, meat, a lab, a prank, a punishment. None of the theories opened a locked pharmacy.\n\nIf {{openerName}} finds these pages, keep them dry and keep moving. I wrote what I could as a {{professionName}}, but the most useful lesson is simpler than any trade: count your supplies before hope, and trust the quiet only after it has stayed quiet all night.",
        },
    },
    bloody = {
        {
            key = "UI_BurdJournals_LoreTemplate_BloodyPage",
            fallback = "{{authorName}} did not make it out of {{dangerNoun}}. The bite looked {{adjective}} until my hands {{verb}}. If {{openerName}} is reading this, do not waste the warning.",
        },
        {
            key = "UI_BurdJournals_LoreTemplate_BloodyLastWish",
            fallback = "If {{openerName}} found this on me, let {{survivorName}} know {{traitName}} was not enough. Do better with {{entryName}}. {{exclamation}}.",
            professionGroups = {"combat", "outdoors", "medical"},
        },
        {
            key = "UI_BurdJournals_LoreTemplate_BloodySupplies",
            fallback = "I lost too much hauling {{supplyNoun}}, but the note on {{skillName}} might still save someone. I wrote it {{adverb}} while {{noun}} {{verb}} downstairs.",
            professionGroups = {"trades", "food", "fitness"},
        },
        {
            key = "UI_BurdJournals_LoreTemplate_BloodyNearMiss",
            fallback = "There is blood on the page because {{dangerNoun}} got close. Read the part about {{traitName}} and do better before the world gets {{adjective}} again.",
        },
        {
            key = "UI_BurdJournals_LoreTemplate_BloodyBroadcast",
            fallback = "Triple-N said the situation was stabilizing while the sirens ran all night. Then came the cough, the fever, the neighbors hammering boards over doors, and the soldiers firing warning shots at people who only wanted to leave. {{authorName}} kept repeating that the news was behind the truth by a whole day, maybe more.\n\nIf this journal is wet, it is not rain. I bled on the page about {{entryNameA}} after dragging {{supplyNoun}} through {{dangerNoun}}, and I underlined {{entryNameB}} because panic makes hands stupid. Tell {{survivorName}} I was still myself when I wrote this.",
            professionGroups = {"medical", "combat", "fitness"},
        },
        {
            key = "UI_BurdJournals_LoreTemplate_BloodyHazmat",
            fallback = "I saw hazmat suits outside the fence two days before they admitted anything was wrong. They sprayed the road, bagged a dog, and waved rifles at a woman who kept asking why her husband smelled bleach whenever he coughed. That is when {{authorName}} stopped believing in accidents.\n\nThe dead came later. The fear came first. Use the notes on {{recipeName}} and {{skillName}} if your hands are steadier than mine, and do not let {{traitName}} talk you into trusting a clean uniform.",
            professionGroups = {"medical", "trades", "stealth"},
        },
        {
            key = "UI_BurdJournals_LoreTemplate_BloodyFenceLine",
            fallback = "The fence did not fail all at once. It sagged in one place, screamed in another, and then a hundred hands found the weak spots like water finding cracks. {{authorName}} was on the wrong side when {{dangerNoun}} pushed through.\n\nThere is not much clean page left. Read {{entryNameA}} before the stain eats it, and if {{openerName}} still has time, copy {{entryNameB}} somewhere drier than my pocket.",
            professionGroups = {"combat", "fitness", "outdoors"},
        },
        {
            key = "UI_BurdJournals_LoreTemplate_BloodyAmbulance",
            fallback = "The ambulance stalled near {{noun}} with the lights still turning. We thought the siren would bring help. It brought everything else first, and by the time help arrived, help was wearing masks and pointing rifles.\n\nI wrote what I remembered after the bleeding slowed. {{skillName}} for the next person pinned down, {{recipeName}} for the next person hungry enough to risk a store, and {{traitName}} because fear spends itself faster than blood.",
            professionGroups = {"medical", "combat"},
        },
        {
            key = "UI_BurdJournals_LoreTemplate_BloodyKitchen",
            fallback = "The safehouse kitchen became surgery, confession, and court. We boiled water until the windows sweated and pretended the red towels meant we were prepared. {{survivorName}} kept saying the bite was shallow.\n\nIf {{openerName}} finds this, do not believe shallow. Learn {{skillNameA}}, use {{supplyNoun}} sparingly, and burn anything that starts to smell {{adjective}}.",
            professionGroups = {"medical", "food"},
        },
        {
            key = "UI_BurdJournals_LoreTemplate_BloodyGasStation",
            fallback = "Everybody needed fuel, so everybody came armed. The pumps clicked dry before noon, but people kept squeezing the handles like mercy might be hiding in the hose. {{authorName}} got cut when {{noun}} {{verb}} and the crowd decided sound meant supplies.\n\nI left notes on {{entryNameA}} and {{entryNameB}} because those were worth more than gasoline by dusk. If you see lights at a station, circle wide.",
            professionGroups = {"trades", "combat", "stealth"},
        },
        {
            key = "UI_BurdJournals_LoreTemplate_BloodyBasement",
            fallback = "We used the basement because it had no windows. That was the problem and the comfort. When {{dangerNoun}} found the stairs, every sound became bigger in the dark, and every apology arrived too late.\n\n{{authorName}} wrote this by flashlight with one hand wrapped in a shirt. {{skillName}} is the useful part. {{recipeName}} is the hopeful part. The rest is just proof that I tried.",
            professionGroups = {"stealth", "fitness", "trades"},
        },
        {
            key = "UI_BurdJournals_LoreTemplate_BloodyTrainTracks",
            fallback = "We followed the tracks because trains felt honest. Rails go somewhere, even when roads lie. Then we found the cars stopped outside Knox, doors chained from the outside, and {{dangerNoun}} moving inside them like cargo that learned to hate.\n\nI bled on the page about {{entryNameA}}, but the note still reads. Keep {{entryNameB}} in your head, keep {{supplyNoun}} in your bag, and do not sleep near metal that rings at night.",
            professionGroups = {"outdoors", "stealth", "combat"},
        },
        {
            key = "UI_BurdJournals_LoreTemplate_BloodyRoadShoulder",
            fallback = "The shoulder of the road was full of almosts: almost escaped, almost rescued, almost enough fuel. {{authorName}} crawled under {{noun}} after the shooting started and wrote with blood because the pen was gone.\n\n{{openerName}} should read {{entryNameA}} before checking the glove boxes. {{entryNameB}} can wait until the road is quiet, which is to say it cannot wait at all.",
            professionGroups = {"combat", "outdoors", "stealth"},
        },
        {
            key = "UI_BurdJournals_LoreTemplate_BloodyFirehouse",
            fallback = "The firehouse doors were open and the trucks were gone. Someone had painted HELP on the floor in letters big enough for a helicopter that never came. The stain through the P was mine.\n\nI left {{skillName}} because somebody will need hands that know what to do. I left {{recipeName}} because smoke and hunger make cowards of everyone. Tell {{survivorName}} the siren still worked.",
            professionGroups = {"medical", "combat", "trades"},
        },
        {
            key = "UI_BurdJournals_LoreTemplate_BloodyGroceryAisle",
            fallback = "Aisle six was soup, beans, and screaming. The lights flickered just enough to make every person look dead twice. {{supplyNoun}} rolled under the shelves while people fought over things they did not know how to cook.\n\nIf {{openerName}} finds this in the red bag, use {{entryNameA}} first. {{entryNameB}} is written on the receipt because the clean pages stuck together.",
            professionGroups = {"food", "stealth", "fitness"},
        },
        {
            key = "UI_BurdJournals_LoreTemplate_BloodyBridge",
            fallback = "They blocked the bridge with buses and called it containment. Then the buses caught fire, the river took the ash, and people tried swimming with backpacks full of cans. {{authorName}} made it halfway back.\n\nThese pages are lighter than supplies. Keep {{skillName}} in your head, keep {{traitName}} from choosing for you, and do not trust bridges that still have soldiers on only one side.",
            professionGroups = {"combat", "fitness", "outdoors"},
        },
        {
            key = "UI_BurdJournals_LoreTemplate_BloodyLaundry",
            fallback = "The laundromat was a bad place to hide but a good place to bleed. Machines thumped with clothes nobody would claim, and the front window showed {{dangerNoun}} reflected behind us before we heard it.\n\n{{authorName}} used a dryer sheet to blot the page. {{entryNameA}} survived. {{entryNameB}} smells like soap and pennies. If {{openerName}} laughs at that, good. Laughing means you still can.",
            professionGroups = {"stealth", "trades"},
        },
        {
            key = "UI_BurdJournals_LoreTemplate_BloodyBarn",
            fallback = "The barn doors held until the horse panicked. After that, everything was hooves, teeth, and people shouting names into dust. {{survivorName}} kept saying animals know sickness before we do.\n\nI believe it now. Read {{skillName}} if you plan to run fences. Read {{recipeName}} if you plan to stay. Avoid {{noun}} when it looks {{adjective}}, even if it used to love you.",
            professionGroups = {"outdoors", "fitness", "food"},
        },
        {
            key = "UI_BurdJournals_LoreTemplate_BloodyPoliceTape",
            fallback = "Police tape looks official until it is wrapped around a wound. We used it for doors, wrists, and one man who promised he was only scratched. {{authorName}} wrote beside him until his breathing changed.\n\n{{openerName}} gets the lessons without the argument: {{entryNameA}} before pity, {{entryNameB}} before sleep, and {{exclamation}} if anybody says procedure will save us.",
            professionGroups = {"combat", "medical"},
        },
        {
            key = "UI_BurdJournals_LoreTemplate_BloodyWarehouse",
            fallback = "The warehouse had everything except enough doors. Pallets became walls, forklifts became barricades, and inventory sheets became lists of who was missing after each noise. {{noun}} {{verb}} in the loading bay and the whole place answered.\n\nI wrote {{skillName}} while hiding behind dog food. {{recipeName}} is on the back page. If {{openerName}} finds a key ring, do not trust any key that shines.",
            professionGroups = {"trades", "stealth", "combat"},
        },
        {
            key = "UI_BurdJournals_LoreTemplate_BloodyCampground",
            fallback = "The campground looked peaceful until you counted the tents from inside out. Smoke from wet firewood covered the smell for a while, then the rain stopped and honesty came back.\n\n{{authorName}} put {{entryNameA}} in this journal after losing the map. {{entryNameB}} is for anyone who thinks trees make them safe. Trees mostly give things somewhere to stand.",
            professionGroups = {"outdoors", "stealth", "fitness"},
        },
        {
            key = "UI_BurdJournals_LoreTemplate_BloodyApartmentStairs",
            fallback = "Apartment stairs are funnels. We learned that after the third floor started screaming and everyone above us tried to become everyone below us. {{survivorName}} fell near the landing and still told me to write.\n\nSo I wrote {{skillName}} with shaking hands. I wrote {{recipeName}} when the hall went quiet. I wrote {{traitName}} because courage is mostly timing with better manners.",
            professionGroups = {"fitness", "combat", "medical"},
        },
        {
            key = "UI_BurdJournals_LoreTemplate_BloodyDriveIn",
            fallback = "The drive-in screen kept showing a comedy with no sound. People watched from locked cars while the world outside learned to tap on glass. It would have been funny if the blood had not reached the projector booth.\n\n{{openerName}}, use {{entryNameA}} when the lot fills up. Use {{entryNameB}} when the battery dies. Do not sit through the credits.",
            professionGroups = {"stealth", "trades", "combat"},
        },
        {
            key = "UI_BurdJournals_LoreTemplate_BloodyMeatLocker",
            fallback = "The meat locker was cold enough to keep secrets but not cold enough to keep the dead polite. We shut three inside and argued whether they were people until one started knocking in code.\n\n{{authorName}} left {{skillName}} here because practical things do not care what you call the problem. {{recipeName}} is tucked behind the invoice. If {{openerName}} smells thawing meat, leave.",
            professionGroups = {"food", "medical", "combat"},
        },
        {
            key = "UI_BurdJournals_LoreTemplate_BloodyOverpass",
            fallback = "From the overpass, the highway looked like a necklace snapped across the county. Headlights, luggage, smoke, and one school bus rocking long after the children stopped crying.\n\nI bled on {{entryNameA}} but the words held. {{entryNameB}} is cleaner. If {{openerName}} has to cross open road, do it {{adverb}} and never stop for a suitcase unless it calls your name.",
            professionGroups = {"outdoors", "fitness", "stealth"},
        },
        {
            key = "UI_BurdJournals_LoreTemplate_BloodyMadLibsStain",
            fallback = "The blood dried {{adverb}} around a {{adjective}} note about {{entryNameA}}. Beside it, {{noun}} {{verb}} like it understood {{traitName}}, and {{dangerNoun}} answered with {{supplyNoun}} in its teeth.\n\n{{openerName}}, copy {{entryNameB}} before the stain gets {{adjective}}. If {{recipeName}} starts looking back, say {{exclamation}} and run.",
            professionGroups = {"combat", "medical", "stealth"},
        },
        {
            key = "UI_BurdJournals_LoreTemplate_BloodyMadLibsPocket",
            fallback = "{{authorName}} stuffed {{supplyNoun}}, {{stashNoun}}, and one {{adjective}} {{noun}} into a coat pocket before {{dangerNoun}} {{verb}} {{adverb}}.\n\nThe pocket tore. The page stayed. Learn {{skillName}}, respect {{traitName}}, and never let {{recipeName}} distract you from a {{adjective}} sound behind the door.",
            professionGroups = {"outdoors", "food", "fitness"},
        },
        {
            key = "UI_BurdJournals_LoreTemplate_BloodySkillOnlyHands",
            fallback = "{{authorName}} wrote {{skillNameA}} with one clean hand and one hand that would not stop shaking. The page about {{skillNameB}} is worse, but the meaning survived.\n\nIf {{openerName}} reads this, start before the blood dries. Practice is kinder than necessity.",
            professionGroups = {"medical", "combat"},
        },
        {
            key = "UI_BurdJournals_LoreTemplate_BloodySkillOnlyLastLesson",
            fallback = "Last lesson: {{skillNameA}} saves more time than bravery. Next lesson: {{skillNameB}} saves more noise than luck. Final lesson: learn before {{dangerNoun}} teaches you itself.\n\nThe rest of the page is red. {{openerName}} can still use the clean parts.",
        },
        {
            key = "UI_BurdJournals_LoreTemplate_BloodySkillOnlyFloor",
            fallback = "The journal slid under {{noun}} when everything went bad. {{authorName}} reached it after the screaming stopped and circled {{skillNameA}} so hard the paper tore.\n\n{{skillNameB}} is written on the back in smaller letters. It may not look important until {{openerName}} is on the floor too.",
            professionGroups = {"fitness", "stealth", "combat"},
        },
        {
            key = "UI_BurdJournals_LoreTemplate_BloodySkillOnlyBandage",
            fallback = "Bandages run out. Tape runs out. Clean water runs out. What stayed useful was {{skillNameA}}, then {{skillNameB}}, then whatever stubborn part of the body kept obeying.\n\n{{authorName}} left this for {{openerName}} because a learned thing weighs nothing and cannot be stolen from your pack.",
            professionGroups = {"medical", "outdoors"},
        },
        {
            key = "UI_BurdJournals_LoreTemplate_BloodySkillOnlyDoor",
            fallback = "There was a door, then a mistake, then a lesson. {{skillNameA}} was the lesson before the door opened. {{skillNameB}} was the lesson after.\n\nIf {{openerName}} has a door in front of them now, pause. The dead are patient, but they are not clever. That leaves clever to you.",
            professionGroups = {"stealth", "combat", "trades"},
        },
        {
            key = "UI_BurdJournals_LoreTemplate_BloodyMadLibsFence",
        },
        {
            key = "UI_BurdJournals_LoreTemplate_BloodyMadLibsGasStation",
        },
        {
            key = "UI_BurdJournals_LoreTemplate_BloodyMadLibsRoad",
        },
        {
            key = "UI_BurdJournals_LoreTemplate_BloodySkillOnlyAmbulance",
        },
        {
            key = "UI_BurdJournals_LoreTemplate_BloodySkillOnlyBroadcast",
        },
        {
            key = "UI_BurdJournals_LoreTemplate_BloodySkillOnlyFence",
        },
        {
            key = "UI_BurdJournals_LoreTemplate_BloodySkillOnlyKitchen",
        },
        {
            key = "UI_BurdJournals_LoreTemplate_BloodySkillOnlyOverpass",
        },
        {
            key = "UI_BurdJournals_LoreTemplate_BloodyGeneral",
            fallback = "{{authorName}} tried to keep the blood off the useful pages. That was easier before the sirens stopped and every road out of Knox County turned into uniforms, headlights, and people begging to be believed. The official voices kept changing their story, but the fever never changed its work.\n\nIf {{openerName}} is reading this, do not waste time wondering which rumor was true. Pack what you can carry, watch the living as carefully as the dead, and remember that panic makes even familiar streets feel hungry.",
        },
    },
    yuletide = {
        {
            key = "UI_BurdJournals_LoreTemplate_YuletideWarm",
            fallback = "This was wrapped for {{openerName}} with one last bit of {{adjective}} mercy. Keep {{supplyNoun}} close and make it through the week. {{exclamation}}.",
        },
        {
            key = "UI_BurdJournals_LoreTemplate_YuletideMercy",
            fallback = "Santa marked {{survivorName}} down for {{traitName}} and a little mercy. The note on {{recipeName}} should help when {{noun}} {{verb}} {{adverb}}.",
            professionGroups = {"medical", "outdoors", "food"},
        },
        {
            key = "UI_BurdJournals_LoreTemplate_YuletideProfession",
            fallback = "Even a {{professionName}} needs warmth. Keep {{supplyNoun}} close and read the part on {{entryName}} first.",
            professionGroups = {"trades", "medical", "combat"},
        },
        {
            key = "UI_BurdJournals_LoreTemplate_YuletideGift",
            fallback = "I wrapped this so {{openerName}} would have one honest thing left this winter. Use {{recipeName}} before it spoils, and be {{adjective}} about it.",
        },
        {
            key = "UI_BurdJournals_LoreTemplate_YuletideEvacuation",
            fallback = "Ho ho no. The old routes are gone, the radios lie, and every chimney in Knox County sounds like something hungry testing the bricks. I found {{survivorName}}'s list beside {{supplyNoun}} and added what I could before the sleigh bells turned into sirens.\n\nFor {{openerName}}, this is not charity. It is a field manual with ribbon on it. Read {{entryNameA}} before nightfall, keep {{entryNameB}} for when the roads ice over, and leave one light burning only if you can afford visitors.",
            professionGroups = {"outdoors", "food", "trades"},
        },
        {
            key = "UI_BurdJournals_LoreTemplate_YuletideNiceList",
            fallback = "The nice list is mostly ash now, but somebody kept doing the work. I saw barricades where parades used to be, aid stations with no nurses left, and children drawing snowmen on quarantine notices because no one knew what else to give them.\n\nSo I wrapped these pages for {{openerName}}. {{skillName}} is the practical gift, {{recipeName}} is the one you open when the cupboards go quiet, and {{traitName}} is the reminder that surviving the holidays still counts as a miracle.",
            professionGroups = {"medical", "food", "fitness"},
        },
        {
            key = "UI_BurdJournals_LoreTemplate_YuletideRooftop",
            fallback = "The rooftops were safer before the snow started showing footprints that led nowhere. Santa saw {{authorName}} leave {{supplyNoun}} under a plastic tree and call it tradition. That counts.\n\nFor {{openerName}}, these pages are wrapped in the only paper I could find. {{entryNameA}} is for surviving the cold, {{entryNameB}} is for surviving the living, and the ribbon is mostly there to keep the blood off.",
            professionGroups = {"outdoors", "stealth"},
        },
        {
            key = "UI_BurdJournals_LoreTemplate_YuletideMall",
            fallback = "The mall music never stopped. It played over broken glass, evacuation notices, and a food court full of chairs pushed against doors. {{survivorName}} laughed once because the song wished us joy while {{dangerNoun}} waited by the escalator.\n\nI left {{skillName}} and {{recipeName}} in this gift because practical miracles are still miracles. If {{openerName}} finds batteries for the speaker, turn it off.",
            professionGroups = {"food", "trades", "fitness"},
        },
        {
            key = "UI_BurdJournals_LoreTemplate_YuletideWorkshop",
            fallback = "The workshop is not at the North Pole this year. It is any garage with a locked door, any kitchen with a sharp knife, any clinic with one clean bandage left. Good people make gifts out of whatever keeps someone breathing.\n\nSo here: {{entryNameA}}, {{entryNameB}}, and a little note saying {{exclamation}}. {{openerName}} can decide which part is festive.",
            professionGroups = {"trades", "medical", "food"},
        },
        {
            key = "UI_BurdJournals_LoreTemplate_YuletideChimney",
            fallback = "Do not use chimneys anymore. Smoke draws eyes, bells draw teeth, and every rooftop in town sounds like claws if you listen too long. {{authorName}} learned that after {{noun}} {{verb}} {{adverb}}.\n\nThe gift is simple: read {{skillName}}, remember {{traitName}}, and keep {{supplyNoun}} where cold hands can find it. Naughty and nice stopped mattering when the dead learned both lists.",
            professionGroups = {"outdoors", "stealth", "combat"},
        },
        {
            key = "UI_BurdJournals_LoreTemplate_YuletideShelter",
            fallback = "Someone hung stockings in the shelter because children were watching. By morning, the stockings held ration crackers, loose nails, and one note from {{survivorName}} asking Santa to bring the phones back.\n\nI could not manage that. I could manage {{entryNameA}} and {{entryNameB}}. If {{openerName}} finds these pages, give someone else the warm corner when you can afford it.",
            professionGroups = {"medical", "food", "trades"},
        },
        {
            key = "UI_BurdJournals_LoreTemplate_YuletideSnowplow",
            fallback = "The snowplow made it farther than the convoy. It shoved cars, luggage, and bad decisions into the ditch until the fuel died near {{noun}}. After that, winter took the road back one inch at a time.\n\n{{openerName}} gets the useful pieces: {{skillName}} for the body, {{recipeName}} for the pot, and {{traitName}} for whatever stubborn part refuses to lie down.",
            professionGroups = {"fitness", "outdoors", "trades"},
        },
        {
            key = "UI_BurdJournals_LoreTemplate_YuletideCannedHam",
            fallback = "The best gift I saw that year was a canned ham with a bow made from caution tape. {{survivorName}} cried when it opened, not because it was beautiful, but because it was food nobody had to steal.\n\nThis journal is the same kind of gift. {{entryNameA}} fills the hands, {{entryNameB}} fills the head, and {{openerName}} should take both before the holiday spirit starts scratching at the door.",
            professionGroups = {"food", "medical"},
        },
        {
            key = "UI_BurdJournals_LoreTemplate_YuletideRadioCarol",
            fallback = "A carol played over the emergency band for eleven minutes before a man cut in and begged someone to let his family through the fence. Then the song started again from the first verse like nothing had happened.\n\nSanta heard it. So did {{authorName}}. The gift for {{openerName}} is {{skillName}}, {{recipeName}}, and the advice to turn the dial if the music knows your name.",
            professionGroups = {"trades", "medical", "stealth"},
        },
        {
            key = "UI_BurdJournals_LoreTemplate_YuletideToyStore",
            fallback = "The toy store was untouched because nobody wanted to admit children still existed. We took batteries, blankets, and one stuffed rabbit that smelled like smoke. {{noun}} {{verb}} in the back room and every music box started at once.\n\n{{openerName}} can have the notes on {{entryNameA}} and {{entryNameB}}. Leave the toys unless you need something soft enough to remember mercy.",
            professionGroups = {"stealth", "trades", "medical"},
        },
        {
            key = "UI_BurdJournals_LoreTemplate_YuletideNaughtyList",
            fallback = "The naughty list used to be a joke. Then the checkpoints started sorting people into useful, exposed, infected, and gone. Santa's handwriting looked kinder than the army forms, but both lists knew where you lived.\n\nI wrapped {{entryNameA}} because {{openerName}} might deserve a chance. I wrapped {{entryNameB}} because deserving stopped being the measure once the snow turned red.",
            professionGroups = {"combat", "stealth", "fitness"},
        },
        {
            key = "UI_BurdJournals_LoreTemplate_YuletideSoupKitchen",
            fallback = "The soup kitchen kept ladling until the pot was mostly water and hope. People said thank you like manners could keep the fever out. {{authorName}} wrote names on cups so the living could feel counted.\n\nFor {{openerName}}, these pages are hot soup in paper form: {{skillName}} first, {{recipeName}} second, and {{traitName}} if you can carry it without spilling.",
            professionGroups = {"food", "medical"},
        },
        {
            key = "UI_BurdJournals_LoreTemplate_YuletideTreeFarm",
            fallback = "The tree farm smelled so normal it hurt. Pine, mud, gasoline, and somebody's old thermos coffee. We cut branches for camouflage and pretended the wreaths on the office door were not covering bullet holes.\n\n{{survivorName}} left {{supplyNoun}} there for strangers. I left {{entryNameA}} and {{entryNameB}} for the same reason. If {{openerName}} hears bells in the rows, do not follow them.",
            professionGroups = {"outdoors", "stealth", "trades"},
        },
        {
            key = "UI_BurdJournals_LoreTemplate_YuletideHospitalWard",
            fallback = "Some nurse hung paper snowflakes in the fever ward. They shook whenever the coughing started, and for a while that was the only weather anyone saw. {{authorName}} kept working after the exits locked.\n\nThis gift is for whoever still opens doors carefully. {{skillName}} may help {{openerName}} live. {{recipeName}} may help someone else. That is close enough to Christmas.",
            professionGroups = {"medical"},
        },
        {
            key = "UI_BurdJournals_LoreTemplate_YuletideRoofLights",
            fallback = "The rooftop lights came on by timer every night even after the family vanished. They blinked red, green, red, green, inviting every hungry thing on the block to admire the season.\n\n{{authorName}} climbed up to cut the cord and found this journal under the shingles. I added {{entryNameA}} because survival needs maintenance, and {{entryNameB}} because beauty is dangerous when it glows.",
            professionGroups = {"trades", "stealth", "fitness"},
        },
        {
            key = "UI_BurdJournals_LoreTemplate_YuletideFrozenLake",
            fallback = "The lake froze thin and lied about it. People crossed because the road was worse, dragging sleds full of blankets and cans. The ice made little singing sounds under every mistake.\n\n{{openerName}}, keep {{skillName}} in mind before stepping anywhere white. Keep {{recipeName}} for the far shore. If {{noun}} {{verb}} under the ice, do not look down.",
            professionGroups = {"outdoors", "fitness", "food"},
        },
        {
            key = "UI_BurdJournals_LoreTemplate_YuletideGiftExchange",
            fallback = "We held a gift exchange with things nobody owned anymore. A screwdriver, two lighters, clean socks, half a bottle of vitamins. {{survivorName}} gave me this journal and asked me to put something useful in it.\n\nSo I did. {{entryNameA}} for the person who opens it first, {{entryNameB}} for the person who survives long enough to need it, and {{exclamation}} for whoever laughs.",
            professionGroups = {"trades", "medical", "food"},
        },
        {
            key = "UI_BurdJournals_LoreTemplate_YuletidePostOffice",
            fallback = "The post office kept receiving letters after nobody delivered them. Cards to grandparents, bills, one envelope addressed to Santa in a child's red marker. The dead did not care about mail, which made them poor citizens.\n\nI tucked {{skillName}} and {{recipeName}} behind the sorting bins. If {{openerName}} finds the red envelope, leave it closed. Some wishes deserve privacy.",
            professionGroups = {"stealth", "trades"},
        },
        {
            key = "UI_BurdJournals_LoreTemplate_YuletideNativity",
            fallback = "The nativity scene outside city hall was missing a shepherd and had gained three sandbags. Somebody tucked ration wrappers in the manger. Somebody else wrote help us where the star should have been.\n\n{{authorName}} left these notes because faith and preparation are not enemies. {{entryNameA}} is practical. {{entryNameB}} is kind. {{openerName}} should try both before deciding which one failed.",
            professionGroups = {"medical", "food", "combat"},
        },
        {
            key = "UI_BurdJournals_LoreTemplate_YuletideBlackout",
            fallback = "The blackout made the holiday lights more precious and more stupid. Every lit window became a promise, every promise became a target, and every target learned to whisper before it broke.\n\nThis package has no batteries, but it has {{entryNameA}} and {{entryNameB}}. That may be better. If {{openerName}} still wants brightness, make it small enough to hide under a coat.",
            professionGroups = {"stealth", "trades", "outdoors"},
        },
        {
            key = "UI_BurdJournals_LoreTemplate_YuletideMadLibsGift",
            fallback = "Santa wrapped {{adjective}} {{noun}}, {{supplyNoun}}, and a warning that {{omenNoun}} {{verb}} {{adverb}} whenever {{openerName}} looked away.\n\nInside the ribbon: {{entryNameA}} for {{adjective}} mornings, {{entryNameB}} for {{dangerNoun}}, and {{recipeName}} in case {{survivorName}} gets hungry enough to believe in miracles.",
            professionGroups = {"food", "trades", "stealth"},
        },
        {
            key = "UI_BurdJournals_LoreTemplate_YuletideMadLibsCarol",
            fallback = "The carol went {{adverb}} through {{noun}} while {{adjective}} snow covered {{stashNoun}}. Every verse ended with {{exclamation}}, which felt festive until {{dangerNoun}} started singing harmony.\n\n{{openerName}} should read {{skillName}} by candlelight, hide {{traitName}} under the wrapping, and save {{entryName}} for the morning after the bells stop.",
            professionGroups = {"medical", "fitness", "outdoors"},
        },
        {
            key = "UI_BurdJournals_LoreTemplate_YuletideSkillOnlyRibbon",
            fallback = "The ribbon is mostly for morale. The real gift is {{skillNameA}}, written carefully enough for cold fingers, and {{skillNameB}}, written twice because winter makes ink stubborn.\n\n{{openerName}} should open this before the candles burn low. A practical lesson is still a present.",
            professionGroups = {"trades", "medical", "food"},
        },
        {
            key = "UI_BurdJournals_LoreTemplate_YuletideSkillOnlyStocking",
            fallback = "The stocking held no candy, only folded pages. {{skillNameA}} was tucked in the toe. {{skillNameB}} was pinned near the cuff with a bent safety pin.\n\n{{authorName}} called it a poor holiday. I call it honest. If {{openerName}} survives because of it, the gift worked.",
        },
        {
            key = "UI_BurdJournals_LoreTemplate_YuletideSkillOnlySnowLesson",
            fallback = "Snow makes every mistake visible. Footprints, dropped tools, bad routes, panic circles around {{noun}}. {{authorName}} learned {{skillNameA}} by watching what the snow accused them of doing.\n\n{{skillNameB}} came after dark. {{openerName}} should learn both before the next white morning tells on them.",
            professionGroups = {"outdoors", "stealth", "fitness"},
        },
        {
            key = "UI_BurdJournals_LoreTemplate_YuletideSkillOnlyWorkshop",
            fallback = "Workshop rule: if it keeps someone alive, it is a toy for grownups. {{skillNameA}} belongs on the top shelf. {{skillNameB}} belongs near the door. Everything else is decoration.\n\n{{openerName}} can unwrap the lesson now. No batteries required, no assembly except practice.",
            professionGroups = {"trades", "combat"},
        },
        {
            key = "UI_BurdJournals_LoreTemplate_YuletideSkillOnlyNice",
            fallback = "Nice people share food. Useful people share knowledge. {{authorName}} tried to be both and wrote {{skillNameA}} while the house smelled like smoke.\n\nThe page on {{skillNameB}} is the better gift. {{openerName}} should take it without guilt. Surviving can be polite later.",
            professionGroups = {"medical", "food", "fitness"},
        },
        {
            key = "UI_BurdJournals_LoreTemplate_YuletideMadLibsEvacuation",
        },
        {
            key = "UI_BurdJournals_LoreTemplate_YuletideMadLibsGeneral",
        },
        {
            key = "UI_BurdJournals_LoreTemplate_YuletideMadLibsNiceList",
        },
        {
            key = "UI_BurdJournals_LoreTemplate_YuletideGeneral",
            fallback = "This was wrapped because the world kept ending without asking what season it was. Somewhere beyond the fences, someone probably still believed the lights meant safety and the radio meant help. In Knox County, lights draw attention and radios mostly teach you how many ways a calm voice can lie.\n\nFor {{openerName}}, take the gift for what it is: paper, memory, and a little stubborn warmth. Be good if you can. Be alive if you cannot. Either one counts this year.",
        },
    },
    cursed = {
        {
            key = "UI_BurdJournals_LoreTemplate_CursedSeal",
            fallback = "{{openerName}} broke the seal and {{omenNoun}} answered {{adverb}}. {{authorName}} is not the first name it kept.",
        },
        {
            key = "UI_BurdJournals_LoreTemplate_CursedMargin",
            fallback = "{{authorName}} scratched {{recipeName}} in the margin and {{omenNoun}} answered. The debt moved to {{openerName}} when {{noun}} {{verb}}.",
        },
        {
            key = "UI_BurdJournals_LoreTemplate_CursedPrice",
            fallback = "The seal broke because {{traitName}} makes fools brave. Keep the page on {{entryName}} if you want to remember the {{adjective}} price.",
            professionGroups = {"combat", "stealth", "fitness"},
        },
        {
            key = "UI_BurdJournals_LoreTemplate_CursedFollower",
            fallback = "I hid this from {{survivorName}}, but {{omenNoun}} followed anyway. It still wants the note on {{recipeName}}. {{exclamation}}.",
        },
        {
            key = "UI_BurdJournals_LoreTemplate_CursedKnoxPrice",
            fallback = "They called it infection because that word fits in a briefing. {{authorName}} called it judgment after {{omenNoun}} appeared in every mirror and the dead began listening for names. The army drew its line around Knox County, but lines only matter to things that respect maps.\n\nThis page remembers {{openerName}} now. Take {{entryNameA}} if you want the lesson, take {{entryNameB}} if you want the warning, and understand that every cure rumor has a price written somewhere smaller than the print.",
            professionGroups = {"medical", "combat", "stealth"},
        },
        {
            key = "UI_BurdJournals_LoreTemplate_CursedExperiment",
            fallback = "{{survivorName}} swore the smell came from a spill, a weapon, a test, anything human enough to blame. I believed that until the margin wrote back. It knew my profession, knew {{traitName}}, and knew which locked doors opened before July Fourth.\n\nIf {{openerName}} can read this without hearing scratching under the paper, use {{skillName}} and {{recipeName}} quickly. The dead are only the loud part of what escaped.",
            professionGroups = {"medical", "trades", "combat"},
        },
        {
            key = "UI_BurdJournals_LoreTemplate_CursedBroadcast",
            fallback = "The emergency broadcast had a second voice under it. Not an echo. A listener. It repeated {{authorName}}'s name between weather updates and laughed whenever the announcer said contained.\n\n{{openerName}} owns the page now. {{entryNameA}} will help for a while, {{entryNameB}} will help if the road is kind, but {{omenNoun}} already knows which door you prefer.",
            professionGroups = {"stealth", "medical", "combat"},
        },
        {
            key = "UI_BurdJournals_LoreTemplate_CursedWater",
            fallback = "People blamed the water because water goes everywhere and never testifies. {{survivorName}} boiled it, filtered it, prayed over it, and still dreamed of {{omenNoun}} drinking first.\n\nIf {{openerName}} wants a practical answer, take {{skillName}} and {{recipeName}}. If you want the true answer, ask why the page stays dry when everything else in this county sweats.",
            professionGroups = {"medical", "outdoors", "food"},
        },
        {
            key = "UI_BurdJournals_LoreTemplate_CursedCourthouse",
            fallback = "The courthouse burned green for three hours. Nobody mentions that on the radio. They mention disorder, panic, unconfirmed casualties, but not the judge's shadow walking out by itself and pointing toward {{noun}}.\n\nI copied {{entryNameA}} before the ink turned. {{entryNameB}} copied itself. If {{openerName}} sees a signature at the bottom, do not read it aloud.",
            professionGroups = {"combat", "stealth"},
        },
        {
            key = "UI_BurdJournals_LoreTemplate_CursedLabDoor",
            fallback = "There was a door below the clinic that was not on any plan. {{authorName}} found it because {{traitName}} makes people look where they should not. Behind it, {{omenNoun}} breathed like a crowd waiting to be invited.\n\nThe notes on {{skillName}} are clean enough to use. The note on {{recipeName}} has teeth in the margins. Leave that part folded unless hunger wins.",
            professionGroups = {"medical", "trades", "stealth"},
        },
        {
            key = "UI_BurdJournals_LoreTemplate_CursedLedger",
            fallback = "Every name in the ledger had a mark beside it: sick, cleared, missing, useful, owed. {{survivorName}} found {{authorName}} written twice, once in ink and once in something that smelled like pennies.\n\nNow {{openerName}} is in the margin. Use {{entryNameA}} while the page allows it. Hide {{entryNameB}} from mirrors. They read faster than people do.",
            professionGroups = {"combat", "medical", "fitness"},
        },
        {
            key = "UI_BurdJournals_LoreTemplate_CursedStormDrain",
            fallback = "The storm drains carried more than rain after the sirens stopped. Whispers came up through the grates near {{noun}}, offering shortcuts, cures, and old names nobody should have known.\n\n{{authorName}} followed one. I found the journal instead. If {{openerName}} keeps it, learn {{skillName}}, forget the whisper, and never trade {{traitName}} for an answer.",
            professionGroups = {"stealth", "outdoors"},
        },
        {
            key = "UI_BurdJournals_LoreTemplate_CursedMirror",
            fallback = "The mirror in the motel did not reflect the room after midnight. It reflected the checkpoint, the fence, and {{authorName}} standing in a line that never moved. {{omenNoun}} watched from behind every face.\n\n{{openerName}} can use {{entryNameA}} safely if the glass stays dark. {{entryNameB}} should be read with the mirror covered. Reflections are excellent thieves.",
            professionGroups = {"stealth", "medical"},
        },
        {
            key = "UI_BurdJournals_LoreTemplate_CursedSilo",
            fallback = "Something knocked inside the grain silo for three days. Not a person. Not an animal. It knocked in the rhythm of the emergency broadcast and stopped whenever anyone prayed.\n\n{{survivorName}} climbed up with {{supplyNoun}} and came down older. The page on {{skillName}} is theirs. The page on {{recipeName}} is mine. The scratching under both belongs to {{omenNoun}}.",
            professionGroups = {"outdoors", "trades", "food"},
        },
        {
            key = "UI_BurdJournals_LoreTemplate_CursedRadioTower",
            fallback = "The radio tower blinked after the power died. Red light, red light, red light, like a heartbeat nobody authorized. Every time it flashed, {{noun}} {{verb}} somewhere closer.\n\nI wrote {{entryNameA}} while counting the flashes and {{entryNameB}} when the count answered back. If {{openerName}} sees the tower from the road, look at your feet instead. It hates being ignored.",
            professionGroups = {"trades", "stealth", "combat"},
        },
        {
            key = "UI_BurdJournals_LoreTemplate_CursedMeat",
            fallback = "The butcher said the meat came in wrong before people did. Too warm, too sweet, marked with a stamp nobody at the plant recognized. Then customers started asking why it moved when salted.\n\nThe page wants {{openerName}} to blame infection. Do not. Infection does not write recipes in the grease. Take {{skillName}}, take {{recipeName}}, and leave anything that smiles from a hook.",
            professionGroups = {"food", "medical"},
        },
        {
            key = "UI_BurdJournals_LoreTemplate_CursedNursery",
            fallback = "The nursery mobiles turned without wind. Ducks, moons, stars, little plastic sheep, all circling over empty cribs while the county alert called for calm. {{authorName}} found names scratched into the paint under each mattress.\n\nOne name is {{openerName}} now. Use {{entryNameA}} while it still looks useful. Use {{entryNameB}} only if the lullaby starts from a room with no children.",
            professionGroups = {"medical", "stealth"},
        },
        {
            key = "UI_BurdJournals_LoreTemplate_CursedConfession",
            fallback = "The confession booth kept whispering after the priest stopped. It knew thefts, lies, checkpoint bargains, and the names people left behind when the road filled with headlights.\n\n{{survivorName}} confessed to surviving. The booth laughed. I wrote {{skillName}} and {{traitName}} because guilt is heavy, but knowledge is portable. {{openerName}} should not kneel unless invited by the living.",
            professionGroups = {"stealth", "medical", "combat"},
        },
        {
            key = "UI_BurdJournals_LoreTemplate_CursedWell",
            fallback = "The well water was clean until the bucket came up with hair. After that it gave different gifts: teeth, radio parts, a school photograph, one wet page already covered in {{authorName}}'s handwriting.\n\nI added {{entryNameA}} because the page left room. {{entryNameB}} appeared while I slept. If {{openerName}} lowers a bucket, do not ask for water by name.",
            professionGroups = {"outdoors", "medical", "food"},
        },
        {
            key = "UI_BurdJournals_LoreTemplate_CursedVotingMachine",
            fallback = "The voting machine in city hall kept printing results after the election was canceled. Sick won by a landslide. Dead took every precinct. One ballot had {{openerName}} written where the office should be.\n\n{{authorName}} took the tape and hid it behind {{stashNoun}}. These notes on {{skillName}} and {{recipeName}} were printed on the back. Democracy has strange handwriting now.",
            professionGroups = {"trades", "stealth"},
        },
        {
            key = "UI_BurdJournals_LoreTemplate_CursedStaticChoir",
            fallback = "There is a choir under the static. You hear it best when the batteries are dying and the room is too tired to lie. It sings numbers, road names, and sometimes the recipe you needed yesterday.\n\n{{openerName}}, take {{entryNameA}} and {{entryNameB}} before the chorus notices. If it harmonizes with your breathing, turn the radio off with a hammer.",
            professionGroups = {"trades", "medical", "combat"},
        },
        {
            key = "UI_BurdJournals_LoreTemplate_CursedTaxOffice",
            fallback = "The tax office survived the first riots because nobody wanted paperwork at the end of the world. Inside, every filing cabinet had been sorted into sins instead of surnames. {{traitName}} was stamped on more folders than seemed fair.\n\nI found {{authorName}} under penalties due. The notes on {{entryNameA}} were audited. The notes on {{entryNameB}} were forgiven. {{openerName}} should avoid the drawer marked extensions.",
            professionGroups = {"stealth", "trades"},
        },
        {
            key = "UI_BurdJournals_LoreTemplate_CursedPuppetShow",
            fallback = "The children's puppet stage in the library performed at noon without hands. It told the Knox story with felt soldiers, paper masks, and a little red moon that swallowed the set.\n\n{{survivorName}} clapped before we could stop them. That is when the journal opened. {{skillName}} is useful. {{recipeName}} is useful. Applause is not.",
            professionGroups = {"medical", "stealth", "food"},
        },
        {
            key = "UI_BurdJournals_LoreTemplate_CursedBlackRain",
            fallback = "The rain came down black for nine minutes and left handprints on every car roof. Officials called it soot. The handprints called {{authorName}} by a childhood nickname.\n\nI wrote {{entryNameA}} before the drops dried. {{entryNameB}} bled through from the next page. If {{openerName}} smells smoke during clear weather, keep walking until the sky forgets you.",
            professionGroups = {"outdoors", "combat", "medical"},
        },
        {
            key = "UI_BurdJournals_LoreTemplate_CursedElevator",
            fallback = "The elevator in the office building kept opening on floors the building did not have. Basement two smelled like bleach. Basement three smelled like flowers. Basement four had {{omenNoun}} and a clipboard with tomorrow's date.\n\n{{openerName}} should take {{skillName}} from this page and leave the rest in the lobby. If the doors open by themselves, be too busy surviving to step inside.",
            professionGroups = {"trades", "stealth", "combat"},
        },
        {
            key = "UI_BurdJournals_LoreTemplate_CursedMadLibsMargin",
            fallback = "The margin drew {{adjective}} {{noun}} around {{entryNameA}} until {{omenNoun}} {{verb}} {{adverb}}. Then it asked {{openerName}} for {{supplyNoun}}, {{traitName}}, and a signature in the space where {{authorName}} used to be.\n\nDo not sign. Read {{entryNameB}} if you must. If {{recipeName}} changes shape, whisper {{exclamation}} and close the book.",
            professionGroups = {"stealth", "medical", "combat"},
        },
        {
            key = "UI_BurdJournals_LoreTemplate_CursedMadLibsDoor",
            fallback = "Behind {{stashNoun}} was a {{adjective}} door. Behind the door, {{dangerNoun}} {{verb}} {{adverb}} with {{noun}} in one hand and {{supplyNoun}} in the other.\n\n{{survivorName}} opened it for {{entryNameA}}. {{authorName}} closed it with {{entryNameB}}. If {{openerName}} hears knocking, count backwards from {{skillName}} and pretend {{traitName}} is not listening.",
            professionGroups = {"trades", "outdoors", "stealth"},
        },
        {
            key = "UI_BurdJournals_LoreTemplate_CursedSkillOnlyInk",
            fallback = "The ink refused every word except {{skillNameA}}. When {{authorName}} tried to write anything else, the page sweated until {{skillNameB}} appeared underneath.\n\n{{openerName}} should learn them in order. The page likes order. It punishes curiosity.",
        },
        {
            key = "UI_BurdJournals_LoreTemplate_CursedSkillOnlyCounting",
            fallback = "Count the dead. Count the doors. Count the lessons. {{skillNameA}} is one. {{skillNameB}} is two. The third count belongs to {{omenNoun}}, so stop early.\n\n{{authorName}} did not stop early. That is why the page has room for {{openerName}}.",
            professionGroups = {"stealth", "combat"},
        },
        {
            key = "UI_BurdJournals_LoreTemplate_CursedSkillOnlyOffer",
            fallback = "The margin offered power, answers, and the location of every locked pharmacy in Knox County. {{authorName}} chose {{skillNameA}} because it looked least hungry.\n\n{{skillNameB}} chose itself. If {{openerName}} feels the page get warm, close it after the lesson and before the bargain.",
            professionGroups = {"medical", "trades", "stealth"},
        },
        {
            key = "UI_BurdJournals_LoreTemplate_CursedSkillOnlyPractice",
            fallback = "Practice {{skillNameA}} where mirrors cannot watch. Practice {{skillNameB}} where radios cannot listen. If either lesson improves too quickly, thank nobody.\n\n{{authorName}} wrote that warning after {{noun}} {{verb}} {{adverb}}. {{openerName}} should treat it as instruction, not superstition.",
            professionGroups = {"stealth", "fitness", "combat"},
        },
        {
            key = "UI_BurdJournals_LoreTemplate_CursedSkillOnlyName",
            fallback = "{{skillNameA}} was written in {{authorName}}'s hand. {{skillNameB}} was written in mine. I had not opened the journal yet.\n\nIf {{openerName}} sees their own handwriting later, do not correct the spelling. The page enjoys attention.",
        },
        {
            key = "UI_BurdJournals_LoreTemplate_CursedMadLibsStatic",
        },
        {
            key = "UI_BurdJournals_LoreTemplate_CursedMadLibsWater",
        },
        {
            key = "UI_BurdJournals_LoreTemplate_CursedMadLibsWell",
        },
        {
            key = "UI_BurdJournals_LoreTemplate_CursedGeneral",
            fallback = "{{openerName}} broke the seal, and the page remembered. They called it infection because infection fits inside reports, fences, and orders shouted through masks. Curse is an older word. It explains why the dead listen when no one speaks.\n\n{{authorName}} wrote this before the ink began correcting itself. If the margin offers you a bargain, count your fingers, count your memories, and ask why the government needed guns to contain something it insisted was only sickness.",
        },
    },
    krampus = {
        {
            key = "UI_BurdJournals_LoreTemplate_KrampusTallied",
            fallback = "{{openerName}} was marked when the wrapping split. {{omenNoun}} never needed a second invitation, only a {{adjective}} one.",
        },
        {
            key = "UI_BurdJournals_LoreTemplate_KrampusSnow",
            fallback = "Krampus kept the page on {{skillName}} and left the rest to the snow. {{survivorName}} should have listened when {{noun}} {{verb}}.",
        },
        {
            key = "UI_BurdJournals_LoreTemplate_KrampusBundle",
            fallback = "The bundle was meant for {{openerName}}, not as a gift but a warning. {{omenNoun}} never forgets a debt. {{exclamation}}.",
        },
        {
            key = "UI_BurdJournals_LoreTemplate_KrampusBargain",
            fallback = "Even a {{professionName}} cannot bargain with {{omenNoun}}. Take the note on {{entryName}} and run {{adverb}}.",
            professionGroups = {"combat", "outdoors", "trades"},
        },
        {
            key = "UI_BurdJournals_LoreTemplate_KrampusCheckpoints",
            fallback = "Krampus found the checkpoints first. He liked the lists, the stamped passes, the names crossed out by men pretending paperwork could hold back punishment. {{authorName}} saw boot prints in the snow that did not belong to any soldier.\n\nThis bundle is for {{openerName}} because the road still wants payment. Learn {{entryNameA}}, keep {{entryNameB}}, and do not answer if the radio uses your childhood name.",
            professionGroups = {"combat", "stealth", "outdoors"},
        },
        {
            key = "UI_BurdJournals_LoreTemplate_KrampusBroadcast",
            fallback = "The broadcast said stay indoors. The voice behind it said be good. Then the signal bent, {{omenNoun}} laughed through the static, and every bad thing we had done felt tall enough to cast a shadow.\n\n{{survivorName}} left these notes before the wrapping sealed itself again. {{skillName}} may keep you warm, {{recipeName}} may keep you fed, but {{traitName}} will decide whether the winter lets you pass.",
            professionGroups = {"food", "fitness", "medical"},
        },
        {
            key = "UI_BurdJournals_LoreTemplate_KrampusSchoolList",
            fallback = "The school had two lists on the bulletin board: children accounted for, children missing. Krampus added a third in careful red ink. It was only adults, and every name had made a decision at the checkpoint.\n\n{{openerName}} does not need to know which list this journal came from. Learn {{entryNameA}}, carry {{entryNameB}}, and do not laugh if bells ring from an empty classroom.",
            professionGroups = {"medical", "combat", "stealth"},
        },
        {
            key = "UI_BurdJournals_LoreTemplate_KrampusTollBooth",
            fallback = "The toll booth kept taking payment after the road closed. Coins, teeth, wedding rings, confessions. {{authorName}} said the basket was empty every morning and full of frost by noon.\n\nThis gift passed through that booth. {{skillName}} is yours, {{recipeName}} is borrowed, and {{traitName}} may be collected later if {{omenNoun}} decides you underpaid.",
            professionGroups = {"stealth", "outdoors", "combat"},
        },
        {
            key = "UI_BurdJournals_LoreTemplate_KrampusCoal",
            fallback = "Coal would have been kinder. The package left soot on {{survivorName}}'s hands and a smell like old churches after rain. Inside were notes, a ribbon, and a warning that changed words whenever nobody watched.\n\nFor {{openerName}}: {{entryNameA}} first, {{entryNameB}} second, and no bargains after sundown. If {{noun}} {{verb}} {{adverb}}, pretend you heard nothing.",
            professionGroups = {"trades", "fitness", "food"},
        },
        {
            key = "UI_BurdJournals_LoreTemplate_KrampusParade",
            fallback = "The Christmas parade floats were still parked downtown when the quarantine line went up. By the time {{dangerNoun}} reached Main Street, the papier-mache reindeer had real antlers tied to them and nobody admitted doing it.\n\n{{authorName}} found this journal under the sleigh. I added {{skillName}} because survival is a better gift than spectacle, and {{recipeName}} because hunger makes every holiday honest.",
            professionGroups = {"food", "combat", "trades"},
        },
        {
            key = "UI_BurdJournals_LoreTemplate_KrampusBellTower",
            fallback = "The bell tower rang at midnight without a rope. Every dead thing in three blocks turned its head like children hearing dinner. {{survivorName}} cried because the rhythm sounded almost festive.\n\nIf {{openerName}} reads this, keep {{supplyNoun}} wrapped, keep {{traitName}} quiet, and remember {{entryName}} before the next bell. Some invitations are shaped like songs.",
            professionGroups = {"stealth", "outdoors", "medical"},
        },
        {
            key = "UI_BurdJournals_LoreTemplate_KrampusLedger",
            fallback = "Santa checks names. Krampus checks balances. The ledger under {{stashNoun}} had pages for cowardice, cruelty, mercy, theft, and one page labeled survival in handwriting that kept changing.\n\nI do not know where {{openerName}} lands. The notes on {{entryNameA}} and {{entryNameB}} are free enough to use, but if the page asks what you did at the roadblock, lie badly.",
            professionGroups = {"combat", "medical", "stealth"},
        },
        {
            key = "UI_BurdJournals_LoreTemplate_KrampusChimneyAsh",
            fallback = "The chimney ash spelled names after midnight. Not every name belonged to the living, and not every living name stayed in one place. {{authorName}} swept it twice before the broom started bleeding soot.\n\nThe gift for {{openerName}} is {{entryNameA}} and {{entryNameB}}, wrapped before the ash could correct them. If the fireplace coughs, do not answer in your own voice.",
            professionGroups = {"stealth", "trades", "medical"},
        },
        {
            key = "UI_BurdJournals_LoreTemplate_KrampusFrozenCreek",
            fallback = "Krampus crossed the frozen creek without leaving footprints. The convoy did not. We found boots, rifles, candy wrappers, and one package tied with ribbon under {{stashNoun}}.\n\n{{openerName}} can keep {{skillName}} and {{entryNameB}}. The creek keeps the rest. Do not test the ice if it sounds like bells.",
            professionGroups = {"outdoors", "combat", "fitness"},
        },
        {
            key = "UI_BurdJournals_LoreTemplate_KrampusDepartmentStore",
            fallback = "The department store Santa was gone, but the chair kept asking children what they wanted. Adults sat in it too after the food ran out. Nobody liked the answers.\n\n{{survivorName}} found this journal in the gift wrap aisle. I added {{entryNameA}} beside a roll of red paper and {{entryNameB}} beside the tape. If {{openerName}} sees the chair, keep walking.",
            professionGroups = {"food", "stealth", "trades"},
        },
        {
            key = "UI_BurdJournals_LoreTemplate_KrampusSleighTracks",
            fallback = "The tracks behind the school were too narrow for a truck and too deep for a deer. They circled the playground seven times, then stopped at the quarantine notice like something had paused to read.\n\nInside the package were {{entryNameA}}, {{entryNameB}}, and a smell like old snow. {{openerName}} should use the first two and ignore the smell if it starts giving advice.",
            professionGroups = {"outdoors", "medical", "stealth"},
        },
        {
            key = "UI_BurdJournals_LoreTemplate_KrampusCandleFactory",
            fallback = "The candle factory kept working after everyone left. Wax poured, wicks twisted, and every label printed the same warning: good enough is not good. {{authorName}} stole three candles and came back with four shadows.\n\nThis journal was inside the fourth. {{skillName}} is legible. {{recipeName}} smells faintly of smoke. If {{openerName}} lights anything from that place, do it outdoors.",
            professionGroups = {"trades", "stealth", "food"},
        },
        {
            key = "UI_BurdJournals_LoreTemplate_KrampusSnowman",
            fallback = "The snowman outside city hall had coal eyes, twig arms, and teeth that were not coal. Every morning it faced a different house. Every night one more window went dark.\n\n{{authorName}} hid {{entryNameA}} in the scarf and {{entryNameB}} under the hat. {{openerName}} can take the notes, but leave the teeth. Gifts should not bite back.",
            professionGroups = {"stealth", "combat", "medical"},
        },
        {
            key = "UI_BurdJournals_LoreTemplate_KrampusConfessionBox",
            fallback = "Krampus does not need confession, but he enjoys bookkeeping. The box behind {{noun}} was full of apology notes, ration tickets, and a single gift tag with {{openerName}} already written on it.\n\n{{entryNameA}} is what the tag offered. {{entryNameB}} is what {{authorName}} added in defiance. Keep both, but do not apologize to the box.",
            professionGroups = {"medical", "stealth", "combat"},
        },
        {
            key = "UI_BurdJournals_LoreTemplate_KrampusMilkCookies",
            fallback = "Milk spoiled. Cookies molded. The plate stayed full anyway. Something took one bite every night and left a small black hoofprint in the crumbs.\n\n{{survivorName}} said it meant we were chosen. I said chosen for what. The answer was this journal, {{entryNameA}}, {{entryNameB}}, and a ribbon tied too tight to untangle.",
            professionGroups = {"food", "medical"},
        },
        {
            key = "UI_BurdJournals_LoreTemplate_KrampusMadLibsRibbon",
            fallback = "The ribbon went {{adverb}} around {{adjective}} {{noun}} while {{omenNoun}} {{verb}} behind {{stashNoun}}. Krampus wrote {{openerName}} on the tag in {{supplyNoun}} and smiled with somebody else's teeth.\n\nOpen {{entryNameA}} first, {{entryNameB}} second, and say {{exclamation}} only if the bells start counting.",
            professionGroups = {"stealth", "outdoors", "combat"},
        },
        {
            key = "UI_BurdJournals_LoreTemplate_KrampusMadLibsList",
            fallback = "The list said {{adjective}} {{noun}}, {{dangerNoun}}, and {{supplyNoun}} were naughty. It said {{authorName}} was pending. It said {{openerName}} would understand when {{omenNoun}} {{verb}} {{adverb}}.\n\nThe useful parts are {{skillName}}, {{entryNameB}}, and whatever courage survives {{exclamation}}.",
            professionGroups = {"combat", "medical", "fitness"},
        },
        {
            key = "UI_BurdJournals_LoreTemplate_KrampusSkillOnlyBundle",
            fallback = "The bundle had no candy, no coal, and no apology. Only {{skillNameA}} folded around {{skillNameB}}, tied with string that felt warm through gloves.\n\n{{openerName}} should learn both before nightfall. Krampus gives warnings in the shape of gifts because people open gifts faster.",
        },
        {
            key = "UI_BurdJournals_LoreTemplate_KrampusSkillOnlyBell",
            fallback = "Every bell meant practice. One ring for {{skillNameA}}, two rings for {{skillNameB}}, three rings for running before the lesson becomes personal.\n\n{{authorName}} made it to two. {{openerName}} should not wait for three.",
            professionGroups = {"fitness", "stealth", "combat"},
        },
        {
            key = "UI_BurdJournals_LoreTemplate_KrampusSkillOnlyCoalDust",
            fallback = "{{skillNameA}} was written in coal dust across the first page. {{skillNameB}} appeared when the dust was wiped away. The table underneath stayed clean.\n\nThat is not a miracle. It is a warning with tidy hands. {{openerName}} should take the knowledge and leave the dust.",
            professionGroups = {"trades", "medical"},
        },
        {
            key = "UI_BurdJournals_LoreTemplate_KrampusSkillOnlyGoodEnough",
            fallback = "Good children listen. Living adults practice. {{authorName}} wrote {{skillNameA}} because listening stopped being enough, then added {{skillNameB}} after {{dangerNoun}} reached the porch.\n\nIf {{openerName}} wants to stay off the wrong list, usefulness is a better start than innocence.",
            professionGroups = {"outdoors", "combat", "food"},
        },
        {
            key = "UI_BurdJournals_LoreTemplate_KrampusSkillOnlyWrapping",
            fallback = "The wrapping paper showed tiny sleighs circling a county map. Every route ended at the same black square. Inside, {{skillNameA}} and {{skillNameB}} were the only things not crossed out.\n\n{{openerName}} should take that as generosity. Or threat. The difference is mostly timing.",
            professionGroups = {"stealth", "outdoors", "trades"},
        },
        {
            key = "UI_BurdJournals_LoreTemplate_KrampusMadLibsBell",
        },
        {
            key = "UI_BurdJournals_LoreTemplate_KrampusMadLibsBundle",
        },
        {
            key = "UI_BurdJournals_LoreTemplate_KrampusMadLibsCoal",
        },
        {
            key = "UI_BurdJournals_LoreTemplate_KrampusGeneral",
            fallback = "The wrapping was not festive. It was a warning dressed up so desperate hands would open it anyway. Knox County has lists now: infected, missing, cleared, contained, expendable. Krampus keeps older lists, and his are harder to appeal.\n\nIf {{openerName}} hears bells where there should be sirens, move before the snow remembers your footprints. Gifts still exist, but some of them expect payment after dark.",
        },
    },
}

local function getDebugLoreTemplateLabel(templateEntry)
    local key = templateEntry and templateEntry.key or nil
    local label = tostring(key or "")
    label = label:gsub("^UI_BurdJournals_LoreTemplate_", "")
    label = label:gsub("([a-z])([A-Z])", "%1 %2")
    label = label:gsub("_", " ")
    if templateEntry and templateEntry.custom == true then
        label = label .. " (Custom)"
    end
    return label ~= "" and label or tostring(key or "Template")
end

function BurdJournals.Server.getDebugLoreTemplateOptions(family)
    local pool = BurdJournals.Server.getLoreTemplatePool(family)
    local templates = {}
    local seen = {}
    for _, templateEntry in ipairs(pool or {}) do
        local key = type(templateEntry) == "table" and tostring(templateEntry.key or "") or ""
        if key ~= "" and not seen[key] then
            seen[key] = true
            templates[#templates + 1] = {
                key = key,
                label = getDebugLoreTemplateLabel(templateEntry),
            }
        end
    end
    table.sort(templates, function(a, b)
        return string.lower(tostring(a.label or a.key or "")) < string.lower(tostring(b.label or b.key or ""))
    end)
    return {family = family, templates = templates}
end

function BurdJournals.Server.findLoreTemplateByKey(family, templateKey, context)
    local key = BurdJournals.Server.normalizeJournalServerText(templateKey)
    if not key then
        return nil, nil
    end
    for _, templateEntry in ipairs(BurdJournals.Server.getLoreTemplatePool(family) or {}) do
        if type(templateEntry) == "table" and templateEntry.key == key then
            local templateText = BurdJournals.Server.getGeneratedLoreText(templateEntry.key, templateEntry.fallback)
            local tokens = BurdJournals.Server.extractLoreTemplateTokens(templateText)
            local tokenCounts = {}
            for _, token in ipairs(tokens) do
                local base = BurdJournals.Server.getLoreTokenBase(token)
                if not base then
                    return nil, nil
                end
                tokenCounts[base] = (tokenCounts[base] or 0) + 1
            end
            for tokenBase, neededCount in pairs(tokenCounts) do
                if not BurdJournals.Server.isLoreTokenAvailable(context, tokenBase, neededCount) then
                    return nil, nil
                end
            end
            return templateEntry, templateText
        end
    end
    return nil, nil
end

function BurdJournals.Server.handleDebugRequestLoreTemplateOptions(player, args)
    if not BurdJournals.Server.isDebugAllowed(player) then
        BurdJournals.Server.sendToClient(player, "debugError", {message = "Debug commands not allowed"})
        return
    end
    args = type(args) == "table" and args or {}
    BurdJournals.Server.sendToClient(player, "debugLoreTemplateOptions", BurdJournals.Server.getDebugLoreTemplateOptions(args.family or "worn"))
end

function BurdJournals.Server.getLoreTemplatePool(family)
    local resolvedFamily = "worn"
    if type(family) ~= "string" or family == "" then
        resolvedFamily = "worn"
    else
        resolvedFamily = family
    end
    local pool = BurdJournals.Server.LORE_TEMPLATE_SETS[resolvedFamily]
        or BurdJournals.Server.LORE_TEMPLATE_SETS.worn
        or {}
    if BurdJournals.Server.applyCustomLoreTemplatePool then
        return BurdJournals.Server.applyCustomLoreTemplatePool(resolvedFamily, pool)
    end
    return pool
end

function BurdJournals.Server.getLoreTemplateWeight(templateEntry, context)
    local weight = 1
    local groups = templateEntry and templateEntry.professionGroups
    local contextGroups = context and context.professionGroups or {}
    if type(groups) ~= "table" then
        return weight
    end

    for _, groupId in ipairs(groups) do
        if contextGroups[groupId] then
            weight = weight + 2
        end
    end
    if tonumber(templateEntry.weight) then
        weight = math.max(1, weight + tonumber(templateEntry.weight))
    end

    return weight
end

function BurdJournals.Server.isLoreTokenAvailable(context, tokenBase, neededCount)
    local count = math.max(1, tonumber(neededCount) or 1)
    if tokenBase == "openerName" or tokenBase == "authorName" or tokenBase == "professionName" or tokenBase == "survivorName" then
        return BurdJournals.Server.normalizeJournalServerText(context[tokenBase]) ~= nil
    end

    local pool = context.tokenPools and context.tokenPools[tokenBase] or nil
    return type(pool) == "table" and #pool >= count
end

function BurdJournals.Server.chooseLoreTemplate(pool, context)
    if type(pool) ~= "table" or #pool == 0 then
        return nil, nil
    end

    local eligible = {}
    local totalWeight = 0
    for _, templateEntry in ipairs(pool) do
        local templateText = BurdJournals.Server.getGeneratedLoreText(templateEntry.key, templateEntry.fallback)
        local tokenCounts = {}
        local tokens = BurdJournals.Server.extractLoreTemplateTokens(templateText)
        local ok = true
        for _, token in ipairs(tokens) do
            local base = BurdJournals.Server.getLoreTokenBase(token)
            if not base then
                ok = false
                break
            end
            tokenCounts[base] = (tokenCounts[base] or 0) + 1
        end
        if ok then
            for tokenBase, neededCount in pairs(tokenCounts) do
                if not BurdJournals.Server.isLoreTokenAvailable(context, tokenBase, neededCount) then
                    ok = false
                    break
                end
            end
        end
        if ok then
            local weight = BurdJournals.Server.getLoreTemplateWeight(templateEntry, context)
            totalWeight = totalWeight + weight
            eligible[#eligible + 1] = {
                entry = templateEntry,
                templateText = templateText,
                weight = weight,
            }
        end
    end

    if #eligible == 0 then
        return nil, nil
    end

    local pick = ((ZombRand and ZombRand(totalWeight) or 0) + 1)
    for _, eligibleEntry in ipairs(eligible) do
        pick = pick - eligibleEntry.weight
        if pick <= 0 then
            return eligibleEntry.entry, eligibleEntry.templateText
        end
    end

    local final = eligible[#eligible]
    return final.entry, final.templateText
end

function BurdJournals.Server.resolveLoreTokenValue(context, token, tokenCache, usedByBase, tokenMeta, metaKey)
    local shouldCacheToken = token == "openerName"
        or token == "authorName"
        or token == "professionName"
        or token == "survivorName"
        or string.find(token, "[A-Z]$") ~= nil

    if shouldCacheToken and tokenCache[token] ~= nil then
        return tokenCache[token]
    end

    local base = BurdJournals.Server.getLoreTokenBase(token)
    if not base then
        if shouldCacheToken then
            tokenCache[token] = ""
        end
        return ""
    end

    if base == "openerName" or base == "authorName" or base == "professionName" or base == "survivorName" then
        local value = BurdJournals.Server.normalizeJournalServerText(context[base]) or ""
        tokenCache[token] = value
        if type(tokenMeta) == "table" then
            tokenMeta[metaKey or token] = value
        end
        return value
    end

    local pool = context.tokenPools and context.tokenPools[base] or {}
    local used = usedByBase[base] or {}
    local value = nil
    local valueIndex = nil

    local candidates = {}
    for index, candidate in ipairs(pool) do
        local normalizedCandidate = BurdJournals.Server.normalizeJournalServerText(candidate)
        local candidateKey = normalizedCandidate and string.lower(normalizedCandidate) or nil
        if normalizedCandidate and not used[candidateKey] then
            candidates[#candidates + 1] = {
                value = normalizedCandidate,
                key = candidateKey,
                index = index,
            }
        end
    end

    if #candidates > 0 then
        local pickIndex = (ZombRand and ZombRand(#candidates) or 0) + 1
        local picked = candidates[pickIndex]
        value = picked.value
        valueIndex = picked.index
        used[picked.key] = true
    end

    if not value then
        value = BurdJournals.Server.normalizeJournalServerText(pool[1]) or ""
        valueIndex = 1
    end

    usedByBase[base] = used
    if shouldCacheToken then
        tokenCache[token] = value
    end
    if type(tokenMeta) == "table" then
        local poolKey = context.tokenPoolKeys and context.tokenPoolKeys[base] or nil
        if poolKey and valueIndex then
            tokenMeta[metaKey or token] = {
                poolKey = poolKey,
                index = valueIndex,
                value = value,
            }
        else
            tokenMeta[metaKey or token] = value
        end
    end
    return value
end

function BurdJournals.Server.getSparseLoreFallbackTemplate(context)
    local family = context and context.family or "worn"
    if family == "bloody" then
        return {
            fallback = "{{authorName}} left this page in {{stashNoun}} after {{dangerNoun}} got too {{adjective}}. Keep {{supplyNoun}} close, trust {{noun}} once, and remember: {{exclamation}}."
        }, "{{authorName}} left this page in {{stashNoun}} after {{dangerNoun}} got too {{adjective}}. Keep {{supplyNoun}} close, trust {{noun}} once, and remember: {{exclamation}}."
    elseif family == "cursed" or family == "krampus" then
        return {
            fallback = "{{authorName}} wrote where {{omenNoun}} could not {{verb}} the page clean. If {{openerName}} finds it, keep {{supplyNoun}} close and leave {{dangerNoun}} before it gets {{adjective}}."
        }, "{{authorName}} wrote where {{omenNoun}} could not {{verb}} the page clean. If {{openerName}} finds it, keep {{supplyNoun}} close and leave {{dangerNoun}} before it gets {{adjective}}."
    elseif family == "yuletide" then
        return {
            fallback = "{{authorName}} tucked this note near {{stashNoun}} with {{supplyNoun}} and a {{adjective}} hope that {{openerName}} would make it through {{dangerNoun}}. {{exclamation}}."
        }, "{{authorName}} tucked this note near {{stashNoun}} with {{supplyNoun}} and a {{adjective}} hope that {{openerName}} would make it through {{dangerNoun}}. {{exclamation}}."
    end

    return {
        fallback = "{{authorName}} kept these notes near {{stashNoun}} because {{dangerNoun}} made everything {{adjective}}. {{openerName}} should take {{supplyNoun}}, watch {{noun}}, and remember: {{exclamation}}."
    }, "{{authorName}} kept these notes near {{stashNoun}} because {{dangerNoun}} made everything {{adjective}}. {{openerName}} should take {{supplyNoun}}, watch {{noun}}, and remember: {{exclamation}}."
end

function BurdJournals.Server.buildGeneratedLoreNote(player, journalData, family)
    local context = BurdJournals.Server.buildLoreContext(player, journalData, family)
    local customTemplate = BurdJournals.Server.normalizeJournalServerText(journalData and journalData.loreNoteTemplateText or nil)
    local templateEntry, templateText = nil, customTemplate
    if not templateText and journalData and journalData.loreNoteTemplateKey then
        templateEntry, templateText = BurdJournals.Server.findLoreTemplateByKey(context.family, journalData.loreNoteTemplateKey, context)
    end
    if not templateText then
        templateEntry, templateText = BurdJournals.Server.chooseLoreTemplate(BurdJournals.Server.getLoreTemplatePool(context.family), context)
    end
    if not templateText and BurdJournals.Server.getSparseLoreFallbackTemplate then
        templateEntry, templateText = BurdJournals.Server.getSparseLoreFallbackTemplate(context)
    end
    if not templateText then
        return nil, context.openerName, context.family
    end

    local tokenCache = {}
    local tokenMeta = {}
    local usedByBase = {}
    local tokenOccurrences = {}
    local rendered = templateText:gsub("{{([%w_]+)}}", function(token)
        tokenOccurrences[token] = (tonumber(tokenOccurrences[token]) or 0) + 1
        local metaKey = token
        if tokenOccurrences[token] > 1 or not string.find(token, "[A-Z]$") then
            metaKey = token .. "#" .. tostring(tokenOccurrences[token])
        end
        return BurdJournals.Server.resolveLoreTokenValue(context, token, tokenCache, usedByBase, tokenMeta, metaKey)
    end)

    rendered = BurdJournals.Server.trimLoreToMaxSentences(rendered, 8)
    local generatedLore = nil
    if type(templateEntry) == "table" and type(templateEntry.key) == "string" then
        generatedLore = {
            templateKey = templateEntry.key,
            templateFallback = templateEntry.fallback,
            tokens = tokenMeta,
        }
    end
    return rendered, context.openerName, context.family, generatedLore
end

function BurdJournals.Server.getLootJournalNoteChance(family)
    local normalizedFamily = type(family) == "string" and string.lower(family) or "worn"
    local optionName = "WornJournalNoteChance"
    if normalizedFamily == "bloody" then
        optionName = "BloodyJournalNoteChance"
    elseif normalizedFamily == "cursed" or normalizedFamily == "krampus" then
        optionName = "CursedJournalNoteChance"
    elseif normalizedFamily == "yuletide" then
        optionName = "YuletideJournalNoteChance"
    end

    local chance = tonumber(BurdJournals.getSandboxOption and BurdJournals.getSandboxOption(optionName)) or 0
    if chance < 0 then chance = 0 end
    if chance > 100 then chance = 100 end
    return chance
end

function BurdJournals.Server.tryAttachGeneratedLootNotes(player, journal, journalData, family)
    if type(journalData) ~= "table" or journalData.isPlayerCreated == true then
        return false
    end
    if BurdJournals.hasJournalNotes and BurdJournals.hasJournalNotes(journalData) then
        return false
    end

    local legacyLoreNoteText = BurdJournals.Server.normalizeJournalServerText(journalData.loreNoteText)
    if legacyLoreNoteText then
        local resolvedFamily = family or journalData.loreNoteTemplateFamily or "worn"
        journalData.notes = BurdJournals.normalizeJournalNotes and BurdJournals.normalizeJournalNotes({
            pages = { legacyLoreNoteText },
            generated = true,
            source = "loot",
            family = resolvedFamily,
            updatedAt = getTimestampMs and getTimestampMs() or os.time(),
            updatedBy = journalData.loreNoteGeneratedByName,
        }, false) or { pages = { legacyLoreNoteText } }
        journalData.loreNoteText = nil
        journalData.lootNotesRollDone = true
        return true
    end

    if journalData.lootNotesRollDone == true then
        return false
    end

    if BurdJournals.getSandboxOption and BurdJournals.getSandboxOption("EnableLootJournalNotes") == false then
        journalData.lootNotesRollDone = true
        return true
    end

    local resolvedFamily = family or journalData.loreNoteTemplateFamily or "worn"
    local chance = BurdJournals.Server.getLootJournalNoteChance(resolvedFamily)
    journalData.lootNotesRollChance = chance
    if chance <= 0 or (ZombRand and ZombRand(100) or 100) >= chance then
        journalData.lootNotesRollDone = true
        return true
    end

    local generatedText = BurdJournals.Server.normalizeJournalServerText(journalData.loreNoteText)
    local generatedLore = nil
    if not generatedText then
        generatedText, _, _, generatedLore = BurdJournals.Server.buildGeneratedLoreNote(player, journalData, resolvedFamily)
    end
    if not BurdJournals.Server.normalizeJournalServerText(generatedText) then
        journalData.lootNotesRollDone = nil
        return false
    end

    journalData.notes = BurdJournals.normalizeJournalNotes and BurdJournals.normalizeJournalNotes({
        pages = generatedLore and { "" } or { generatedText },
        generated = true,
        source = "loot",
        family = resolvedFamily,
        generatedLore = generatedLore,
        updatedAt = getTimestampMs and getTimestampMs() or os.time(),
        updatedBy = journalData.loreNoteGeneratedByName,
    }, false) or { pages = { generatedText } }
    journalData.lootNotesRollDone = true
    return true
end

function BurdJournals.Server.ensureGeneratedLootIdentityData(journalData)
    if type(journalData) ~= "table" or journalData.isPlayerCreated == true then
        return false
    end

    local normalizeServerText = BurdJournals.Server.normalizeJournalServerText
        or function(value)
            if value == nil then
                return nil
            end
            local text = tostring(value):gsub("^%s+", ""):gsub("%s+$", "")
            return text ~= "" and text or nil
        end

    local changed = false
    local hasExplicitPlayerOwner = normalizeServerText(journalData.ownerUsername)
        or normalizeServerText(journalData.ownerSteamId)

    if not hasExplicitPlayerOwner then
        local ownerAlias = normalizeServerText(journalData.ownerCharacterName)
        if ownerAlias and not normalizeServerText(journalData.author) then
            journalData.author = ownerAlias
            changed = true
        end
    end

    if journalData.isYuletideJournal == true then
        if BurdJournals.Server.ensureYuletideJournalProfileData
            and BurdJournals.Server.ensureYuletideJournalProfileData(journalData) then
            changed = true
        end
        local santaIdentity = BurdJournals.Server.getSantaJournalIdentity and BurdJournals.Server.getSantaJournalIdentity() or {}
        local santaAuthor = normalizeServerText(santaIdentity.author)
        local santaProfession = normalizeServerText(santaIdentity.profession) or "yuletide_santa"
        local santaProfessionName = normalizeServerText(santaIdentity.professionName) or "North Pole Toymaker"
        if santaAuthor and normalizeServerText(journalData.author) ~= santaAuthor then
            journalData.author = santaAuthor
            changed = true
        end
        if normalizeServerText(journalData.profession) ~= santaProfession then
            journalData.profession = santaProfession
            changed = true
        end
        if normalizeServerText(journalData.professionName) ~= santaProfessionName then
            journalData.professionName = santaProfessionName
            changed = true
        end
        if normalizeServerText(journalData.flavorKey) ~= "UI_BurdJournals_YuletideFlavor" then
            journalData.flavorKey = "UI_BurdJournals_YuletideFlavor"
            changed = true
        end
        if normalizeServerText(journalData.loreNoteTemplateFamily) ~= "yuletide" then
            journalData.loreNoteTemplateFamily = "yuletide"
            changed = true
        end
        return changed
    end

    if journalData.isCursedJournal == true or journalData.isCursedReward == true then
        local cursedIdentity = BurdJournals.Server.resolveCursedJournalIdentity
            and BurdJournals.Server.resolveCursedJournalIdentity(journalData)
            or nil
        if type(cursedIdentity) == "table" then
            if not normalizeServerText(journalData.author) and normalizeServerText(cursedIdentity.author) then
                journalData.author = cursedIdentity.author
                changed = true
            end
            if not normalizeServerText(journalData.profession) and normalizeServerText(cursedIdentity.profession) then
                journalData.profession = cursedIdentity.profession
                changed = true
            end
            if not normalizeServerText(journalData.professionName) and normalizeServerText(cursedIdentity.professionName) then
                journalData.professionName = cursedIdentity.professionName
                changed = true
            end
            if not normalizeServerText(journalData.flavorKey) and normalizeServerText(cursedIdentity.flavorKey) then
                journalData.flavorKey = cursedIdentity.flavorKey
                changed = true
            end
        end
        return changed
    end

    local isGeneratedLoot = journalData.isWorn == true
        or journalData.isBloody == true
        or journalData.wasFromWorn == true
        or journalData.wasFromBloody == true
        or journalData.hasBloodyOrigin == true
        or journalData.isZombieJournal == true
    if not isGeneratedLoot then
        return changed
    end

    local fallbackProfession = nil
    local fallbackProfessionName = nil
    local fallbackFlavorKey = nil
    if BurdJournals.getRandomProfession then
        fallbackProfession, fallbackProfessionName, fallbackFlavorKey = BurdJournals.getRandomProfession()
    end
    if not normalizeServerText(journalData.author) then
        journalData.author = (BurdJournals.generateRandomSurvivorName and BurdJournals.generateRandomSurvivorName())
            or (BurdJournals.generateRandomName and BurdJournals.generateRandomName())
            or getYuletideServerText("UI_BurdJournals_UnknownSurvivor", "Unknown Survivor")
        changed = true
    end
    if not normalizeServerText(journalData.profession) and normalizeServerText(fallbackProfession) then
        journalData.profession = fallbackProfession
        changed = true
    end
    if not normalizeServerText(journalData.professionName) and normalizeServerText(fallbackProfessionName) then
        journalData.professionName = fallbackProfessionName
        changed = true
    end
    if not normalizeServerText(journalData.flavorKey) then
        local defaultFlavorKey = (journalData.isBloody == true and "UI_BurdJournals_BloodyFlavor")
            or ((journalData.wasFromBloody == true or journalData.hasBloodyOrigin == true) and "UI_BurdJournals_WornBloodyFlavor")
            or "UI_BurdJournals_WornFlavor"
        journalData.flavorKey = normalizeServerText(fallbackFlavorKey) or defaultFlavorKey
        changed = true
    end

    return changed
end

function BurdJournals.Server.ensureGeneratedLootLoreNote(player, journal, journalData, family)
    if type(journalData) ~= "table" or journalData.isPlayerCreated == true then
        return false
    end
    local identityChanged = BurdJournals.Server.ensureGeneratedLootIdentityData
        and BurdJournals.Server.ensureGeneratedLootIdentityData(journalData)
        or false
    if BurdJournals.Server.tryAttachGeneratedLootNotes then
        local hadNotes = BurdJournals.hasJournalNotes and BurdJournals.hasJournalNotes(journalData) or false
        BurdJournals.Server.tryAttachGeneratedLootNotes(player, journal, journalData, family or journalData.loreNoteTemplateFamily)
        if not hadNotes and BurdJournals.hasJournalNotes and BurdJournals.hasJournalNotes(journalData) then
            identityChanged = true
        end
    end

    if identityChanged and journal and journal.transmitModData then
        safeTransmitServerJournalModData(journal, "ensureGeneratedLootNoteData")
    end
    return identityChanged
end

local function markLootRewardsRevealed(player, journal, journalData)
    local data = type(journalData) == "table" and journalData or nil
    if type(data) ~= "table" then
        return false
    end
    if not (BurdJournals.isLootRewardJournal and BurdJournals.isLootRewardJournal(journal or data)) then
        return false
    end
    if data.lootRewardsRevealed == true then
        return false
    end

    data.lootRewardsRevealed = true
    data.lootRewardsRevealedByName = (BurdJournals.Server.getJournalPlayerDisplayName
        and BurdJournals.Server.getJournalPlayerDisplayName(player))
        or data.lootRewardsRevealedByName
    data.lootRewardsRevealedAtHours = (getGameTime and getGameTime() and getGameTime():getWorldAgeHours())
        or data.lootRewardsRevealedAtHours
    return true
end

function BurdJournals.Server.areFunLootJournalsEnabled()
    return not BurdJournals.getSandboxOption
        or BurdJournals.getSandboxOption("EnableLootJournalsFun") ~= false
end

function BurdJournals.Server.areYuletideJournalSpawnsEnabled()
    return BurdJournals.Server.areFunLootJournalsEnabled()
        and (not BurdJournals.getSandboxOption
            or BurdJournals.getSandboxOption("EnableYuletideJournalSpawns") ~= false)
end

function BurdJournals.Server.getSantaJournalIdentity()
    return {
        author = getYuletideServerText("UI_BurdJournals_YuletideAuthor", "Santa"),
        profession = "yuletide_santa",
        professionName = getYuletideServerText("UI_BurdJournals_YuletideProfession", "North Pole Toymaker"),
    }
end

function BurdJournals.Server.getKrampusJournalIdentity()
    return {
        author = getYuletideServerText("UI_BurdJournals_KrampusAuthor", "Krampus"),
        profession = "yuletide_krampus",
        professionName = getYuletideServerText("UI_BurdJournals_KrampusProfession", "Yule Punisher"),
    }
end

function BurdJournals.Server.shouldUseKrampusCursedIdentity()
    if not BurdJournals.Server.areYuletideJournalSpawnsEnabled() then
        return false
    end

    local mode = BurdJournals.getYuletideKrampusAuthorMode and BurdJournals.getYuletideKrampusAuthorMode() or 1
    if mode == 4 then
        return false
    end

    local seasonContext = BurdJournals.getYuletideSeasonContext and BurdJournals.getYuletideSeasonContext() or nil
    if type(seasonContext) ~= "table" then
        return false
    end
    if mode == 2 then
        return seasonContext.worldDateActive == true
    end
    if mode == 3 then
        return seasonContext.realDateActive == true
    end
    return seasonContext.active == true
end

function BurdJournals.Server.resolveCursedJournalIdentity(sourceData)
    local source = type(sourceData) == "table" and sourceData or {}
    local krampusIdentity = BurdJournals.Server.shouldUseKrampusCursedIdentity and BurdJournals.Server.shouldUseKrampusCursedIdentity()
        and BurdJournals.Server.getKrampusJournalIdentity and BurdJournals.Server.getKrampusJournalIdentity()
        or nil

    local professionId = source.profession
    local professionName = source.professionName
    local flavorKey = source.flavorKey
    if not professionId or not professionName then
        if krampusIdentity then
            professionId = krampusIdentity.profession
            professionName = krampusIdentity.professionName
            flavorKey = flavorKey or "UI_BurdJournals_BloodyFlavor"
        else
            professionId, professionName, flavorKey = BurdJournals.getRandomProfession and BurdJournals.getRandomProfession()
                or {"survivor", "Survivor", "UI_BurdJournals_BloodyFlavor"}
        end
    end

    return {
        author = source.author
            or (krampusIdentity and krampusIdentity.author)
            or ((BurdJournals.generateRandomSurvivorName and BurdJournals.generateRandomSurvivorName()) or "Unknown Survivor"),
        profession = source.profession or (krampusIdentity and krampusIdentity.profession) or professionId,
        professionName = source.professionName or (krampusIdentity and krampusIdentity.professionName) or professionName,
        flavorKey = flavorKey or "UI_BurdJournals_BloodyFlavor",
        isKrampus = krampusIdentity ~= nil,
    }
end

function BurdJournals.Server.buildYuletideLoreNote(player, journalData)
    local loreSeed = BurdJournals.normalizeJournalData and BurdJournals.normalizeJournalData(journalData) or {}
    loreSeed.isYuletideJournal = true
    loreSeed.loreNoteTemplateVersion = tonumber(loreSeed.loreNoteTemplateVersion) or LORE_DYNAMIC_VERSION
    loreSeed.loreNoteTemplateFamily = "yuletide"
    loreSeed.profession = loreSeed.profession or "yuletide_santa"
    loreSeed.professionName = loreSeed.professionName
        or getYuletideServerText("UI_BurdJournals_YuletideProfession", "North Pole Toymaker")
    loreSeed.author = loreSeed.author or getYuletideServerText("UI_BurdJournals_YuletideAuthor", "Santa")
    return BurdJournals.Server.buildGeneratedLoreNote(player, loreSeed, "yuletide")
end

function BurdJournals.Server.buildCursedLoreNote(player, rewardData)
    local identity = BurdJournals.Server.resolveCursedJournalIdentity and BurdJournals.Server.resolveCursedJournalIdentity(rewardData) or {}
    local loreSeed = BurdJournals.normalizeJournalData and BurdJournals.normalizeJournalData(rewardData) or {}
    loreSeed.author = loreSeed.author or identity.author
    loreSeed.profession = loreSeed.profession or identity.profession
    loreSeed.professionName = loreSeed.professionName or identity.professionName
    loreSeed.isCursedReward = true
    loreSeed.loreNoteTemplateVersion = tonumber(loreSeed.loreNoteTemplateVersion) or LORE_DYNAMIC_VERSION
    loreSeed.loreNoteTemplateFamily = identity.isKrampus and "krampus" or "cursed"
    return BurdJournals.Server.buildGeneratedLoreNote(player, loreSeed, loreSeed.loreNoteTemplateFamily)
end

local function buildYuletideRewardSkills(professionId)
    local out = {}
    local allowed = (BurdJournals.getAllowedSkills and BurdJournals.getAllowedSkills()) or {}
    if #allowed == 0 then return out end

    local minXP = tonumber(BurdJournals.getSandboxOption and BurdJournals.getSandboxOption("YuletideJournalMinXP")) or 75
    local maxXP = tonumber(BurdJournals.getSandboxOption and BurdJournals.getSandboxOption("YuletideJournalMaxXP")) or 300
    minXP = math.max(1, math.floor(minXP))
    maxXP = math.max(minXP, math.floor(maxXP))

    local minSkills = tonumber(BurdJournals.getSandboxOption and BurdJournals.getSandboxOption("YuletideJournalMinSkills")) or 2
    local maxSkills = tonumber(BurdJournals.getSandboxOption and BurdJournals.getSandboxOption("YuletideJournalMaxSkills")) or 5
    minSkills = math.max(1, math.floor(minSkills))
    maxSkills = math.max(minSkills, math.floor(maxSkills))
    if BurdJournals.rollCoherentSkillsForProfession and professionId then
        local coherentSkills, coreCount, fallbackCount = BurdJournals.rollCoherentSkillsForProfession(
            professionId,
            minSkills,
            maxSkills,
            minXP,
            maxXP
        )
        if coherentSkills and BurdJournals.hasAnyEntries and BurdJournals.hasAnyEntries(coherentSkills) then
            return coherentSkills, coreCount, fallbackCount
        end
    end

    local numSkills = ZombRand(minSkills, maxSkills + 1)
    local candidates = {}
    for _, skillName in ipairs(allowed) do
        local perk = BurdJournals.getPerkByName and BurdJournals.getPerkByName(skillName)
        if perk then
            candidates[#candidates + 1] = skillName
        end
    end
    shuffleArray(candidates)

    for i = 1, math.min(numSkills, #candidates) do
        local skillName = candidates[i]
        local xp = ZombRand(minXP, maxXP + 1)
        local level = (BurdJournals.getSkillLevelFromXP and BurdJournals.getSkillLevelFromXP(xp, skillName)) or 0
        out[skillName] = {
            xp = xp,
            level = level,
        }
    end
    return out, 0, BurdJournals.countTable and BurdJournals.countTable(out) or 0
end

local function buildYuletideRewardTraits()
    if BurdJournals.getSandboxOption and BurdJournals.getSandboxOption("EnableYuletideJournalTraits") == false then
        return nil
    end

    local traitChance = tonumber(BurdJournals.getSandboxOption and BurdJournals.getSandboxOption("YuletideJournalTraitChance")) or 40
    if traitChance <= 0 or ZombRand(100) >= traitChance then
        return nil
    end
    local grantable = (BurdJournals.getGrantableTraitsForJournal
        and BurdJournals.getGrantableTraitsForJournal({ isYuletideJournal = true, isPlayerCreated = false }))
        or (BurdJournals.getGrantableTraits and BurdJournals.getGrantableTraits())
        or {}
    if #grantable == 0 then
        return nil
    end

    local pool = {}
    for _, traitId in ipairs(grantable) do
        pool[#pool + 1] = traitId
    end
    shuffleArray(pool)

    local out = {}
    local minTraits = tonumber(BurdJournals.getSandboxOption and BurdJournals.getSandboxOption("YuletideJournalMinTraits")) or 1
    local maxTraits = tonumber(BurdJournals.getSandboxOption and BurdJournals.getSandboxOption("YuletideJournalMaxTraits")) or 3
    minTraits = math.max(1, math.floor(minTraits))
    maxTraits = math.max(minTraits, math.floor(maxTraits))
    maxTraits = math.min(maxTraits, #pool)
    minTraits = math.min(minTraits, maxTraits)
    local traitCount = ZombRand(minTraits, maxTraits + 1)
    for i = 1, traitCount do
        out[pool[i]] = true
    end
    return out
end

local function buildYuletideRewardRecipes()
    if BurdJournals.getSandboxOption and BurdJournals.getSandboxOption("EnableYuletideJournalRecipes") == false then
        return nil
    end

    local recipeChance = tonumber(BurdJournals.getSandboxOption and BurdJournals.getSandboxOption("YuletideJournalRecipeChance")) or 60
    if recipeChance <= 0 or ZombRand(100) >= recipeChance then
        return nil
    end

    local maxRecipes = tonumber(BurdJournals.getSandboxOption and BurdJournals.getSandboxOption("YuletideJournalMaxRecipes")) or 3
    maxRecipes = math.max(1, math.floor(maxRecipes))
    local count = ZombRand(1, maxRecipes + 1)
    if BurdJournals.generateRandomRecipes then
        return BurdJournals.generateRandomRecipes(count)
    end
    return nil
end

local function rollYuletideGiftTier()
    local practical = math.max(0, tonumber(BurdJournals.getSandboxOption and BurdJournals.getSandboxOption("YuletideGiftPracticalWeight")) or 70)
    local rare = math.max(0, tonumber(BurdJournals.getSandboxOption and BurdJournals.getSandboxOption("YuletideGiftRareWeight")) or 25)
    local jackpot = math.max(0, tonumber(BurdJournals.getSandboxOption and BurdJournals.getSandboxOption("YuletideGiftJackpotWeight")) or 5)
    local total = practical + rare + jackpot
    if total <= 0 then
        return "practical", 0
    end

    local roll
    if type(ZombRandFloat) == "function" then
        roll = ZombRandFloat(0, total)
    elseif type(ZombRand) == "function" then
        roll = ZombRand(math.max(1, math.floor(total * 1000))) / 1000
    else
        roll = math.random() * total
    end
    if roll < jackpot then
        return "jackpot", roll
    end
    if roll < jackpot + rare then
        return "rare", roll
    end
    return "practical", roll
end

local function normalizeYuletideGiftTier(value)
    local tier = tostring(value or "")
    if tier == "practical" or tier == "rare" or tier == "jackpot" then
        return tier
    end
    return nil
end

local function rollYuletideImmediateGifts(forcedTier)
    local tier, roll = normalizeYuletideGiftTier(forcedTier), nil
    if not tier then
        tier, roll = rollYuletideGiftTier()
    end
    local pool = YULETIDE_PRACTICAL_GIFT_POOL
    if tier == "rare" then
        pool = YULETIDE_RARE_GIFT_POOL
    elseif tier == "jackpot" then
        pool = YULETIDE_JACKPOT_GIFT_POOL
    end

    local minItems = tonumber(BurdJournals.getSandboxOption and BurdJournals.getSandboxOption("YuletideGiftMinItems")) or 1
    local maxItems = tonumber(BurdJournals.getSandboxOption and BurdJournals.getSandboxOption("YuletideGiftMaxItems")) or 2
    minItems = math.max(1, math.floor(minItems))
    maxItems = math.max(minItems, math.floor(maxItems))

    local count = ZombRand(minItems, maxItems + 1)
    local picks = {}
    for _, entry in ipairs(pool) do
        picks[#picks + 1] = entry
    end
    shuffleArray(picks)

    local gifts = {}
    for i = 1, math.min(count, #picks) do
        local entry = picks[i]
        BurdJournals.Server.appendYuletideGiftEntryWithBundles(gifts, entry)
    end
    return gifts, tier, roll
end

function BurdJournals.Server.generateYuletideJournalProfile(sourceData)
    local source = type(sourceData) == "table" and sourceData or {}
    local manualRewards = source.manualRewards == true or source.yuletideManualRewards == true
    local gameTime = type(getGameTime) == "function" and getGameTime() or nil
    local worldAge = gameTime and gameTime:getWorldAgeHours() or 0
    local santaIdentity = BurdJournals.Server.getSantaJournalIdentity and BurdJournals.Server.getSantaJournalIdentity() or {}

    local identityProfessionId = santaIdentity.profession or "yuletide_santa"
    local identityProfessionName = santaIdentity.professionName or "North Pole Toymaker"
    local identityFlavorKey = "UI_BurdJournals_YuletideFlavor"
    local professionId = source.profession or identityProfessionId
    local professionName = source.professionName or identityProfessionName
    local flavorKey = source.flavorKey or identityFlavorKey

    local sourceSkills = nil
    if manualRewards then
        sourceSkills = type(source.skills) == "table" and source.skills or {}
    else
        sourceSkills = (type(source.skills) == "table" and BurdJournals.hasAnyEntries and BurdJournals.hasAnyEntries(source.skills)) and source.skills or nil
    end
    local skillsCoreCount = tonumber(source.coreSkillCount) or 0
    local skillsFallbackCount = tonumber(source.fallbackSkillCount) or 0
    local generatedSkills = sourceSkills
    if not generatedSkills then
        generatedSkills, skillsCoreCount, skillsFallbackCount = buildYuletideRewardSkills(professionId)
    end

    local generatedTraits = nil
    if manualRewards then
        generatedTraits = type(source.traits) == "table" and source.traits or {}
    else
        generatedTraits = (type(source.traits) == "table" and BurdJournals.hasAnyEntries and BurdJournals.hasAnyEntries(source.traits))
            and source.traits or buildYuletideRewardTraits()
    end
    local generatedRecipes = nil
    if manualRewards then
        generatedRecipes = type(source.recipes) == "table" and source.recipes or {}
    else
        generatedRecipes = (type(source.recipes) == "table" and BurdJournals.hasAnyEntries and BurdJournals.hasAnyEntries(source.recipes))
            and source.recipes or buildYuletideRewardRecipes()
    end
    local generatedStats = type(source.stats) == "table" and source.stats or {}
    local resolvedForgetSlot = nil
    if manualRewards then
        resolvedForgetSlot = (source.forgetSlot == true) and true or nil
    else
        resolvedForgetSlot = BurdJournals.rollForgetSlotForType and BurdJournals.rollForgetSlotForType("yuletide", source.forgetSlot)
            or (source.forgetSlot == true and true or nil)
    end

    if BurdJournals.resolveProfessionForGeneratedEntries then
        professionId, professionName, flavorKey = BurdJournals.resolveProfessionForGeneratedEntries(
            professionId,
            professionName,
            flavorKey or "UI_BurdJournals_YuletideFlavor",
            generatedSkills,
            generatedTraits,
            generatedRecipes,
            skillsCoreCount,
            skillsFallbackCount
        )
    end

    local immediateGifts = type(source.yuletideImmediateGifts) == "table" and source.yuletideImmediateGifts or nil
    local giftTier = normalizeYuletideGiftTier(source.yuletideGiftTier)
    local giftRoll = tonumber(source.yuletideGiftRoll)
    local wrappedVariant = nil
    if source.yuletideWrappedVariant ~= nil then
        wrappedVariant = BurdJournals.normalizeYuletideWrappedVariant
            and BurdJournals.normalizeYuletideWrappedVariant(source.yuletideWrappedVariant)
            or tostring(source.yuletideWrappedVariant)
    end
    if not immediateGifts then
        immediateGifts, giftTier, giftRoll = rollYuletideImmediateGifts(giftTier)
    end
    if not wrappedVariant and BurdJournals.chooseRandomYuletideWrappedVariant then
        wrappedVariant = BurdJournals.chooseRandomYuletideWrappedVariant()
    end

    local profile = {
        uuid = source.uuid or (BurdJournals.generateUUID and BurdJournals.generateUUID()) or tostring(ZombRand(999999999)),
        author = source.author or santaIdentity.author,
        profession = identityProfessionId,
        professionName = identityProfessionName,
        flavorKey = identityFlavorKey,
        flavorText = source.flavorText,
        loreNoteText = source.loreNoteText,
        loreNoteTemplateVersion = tonumber(source.loreNoteTemplateVersion),
        loreNoteTemplateFamily = source.loreNoteTemplateFamily or "yuletide",
        timestamp = tonumber(source.timestamp) or worldAge,
        skills = generatedSkills or {},
        traits = generatedTraits,
        recipes = generatedRecipes,
        stats = generatedStats,
        forgetSlot = resolvedForgetSlot,
        isWorn = false,
        isBloody = false,
        wasFromWorn = false,
        wasFromBloody = false,
        isPlayerCreated = false,
        isZombieJournal = source.isZombieJournal == true,
        isWritten = true,
        condition = tonumber(source.condition) or 8,
        claims = type(source.claims) == "table" and source.claims or {},
        claimedSkills = type(source.claimedSkills) == "table" and source.claimedSkills or {},
        claimedTraits = type(source.claimedTraits) == "table" and source.claimedTraits or {},
        claimedRecipes = type(source.claimedRecipes) == "table" and source.claimedRecipes or {},
        claimedStats = type(source.claimedStats) == "table" and source.claimedStats or {},
        claimedForgetSlot = type(source.claimedForgetSlot) == "table" and source.claimedForgetSlot or {},
        isYuletideJournal = true,
        yuletideState = source.yuletideState == BurdJournals.YULETIDE_STATE_UNWRAPPED
            and BurdJournals.YULETIDE_STATE_UNWRAPPED
            or BurdJournals.YULETIDE_STATE_WRAPPED,
        yuletideImmediateGifts = immediateGifts or {},
        yuletideGiftGranted = source.yuletideGiftGranted == true,
        yuletideGiftTier = giftTier or "practical",
        yuletideGiftRoll = giftRoll,
        yuletideManualRewards = manualRewards == true and true or nil,
        yuletideWrappedVariant = wrappedVariant,
        yuletideOpenedByName = source.yuletideOpenedByName,
        yuletideDeliveryToken = source.yuletideDeliveryToken,
        yuletideDeliveredBy = source.yuletideDeliveredBy,
        yuletideDeliveryLabel = source.yuletideDeliveryLabel,
        yuletidePendingDelivery = source.yuletidePendingDelivery == true,
        yuletideBeacon = type(source.yuletideBeacon) == "table" and source.yuletideBeacon or nil,
    }

    if source.lootNotesEligible == true and BurdJournals.Server.tryAttachGeneratedLootNotes then
        BurdJournals.Server.tryAttachGeneratedLootNotes(nil, nil, profile, profile.loreNoteTemplateFamily or "yuletide")
    end

    return profile
end

function BurdJournals.Server.ensureYuletideJournalProfileData(data)
    if type(data) ~= "table" then
        return false
    end

    local normalizeServerText = BurdJournals.Server.normalizeJournalServerText
        or function(value)
            if value == nil then
                return nil
            end
            local text = tostring(value):gsub("^%s+", ""):gsub("%s+$", "")
            return text ~= "" and text or nil
        end

    local manualRewards = data.yuletideManualRewards == true
    local manualRewardsChanged = false
    if manualRewards then
        if type(data.skills) ~= "table" then
            data.skills = {}
            manualRewardsChanged = true
        end
        if type(data.traits) ~= "table" then
            data.traits = {}
            manualRewardsChanged = true
        end
        if type(data.recipes) ~= "table" then
            data.recipes = {}
            manualRewardsChanged = true
        end
        if type(data.stats) ~= "table" then
            data.stats = {}
            manualRewardsChanged = true
        end
        if data.forgetSlot ~= true and data.forgetSlot ~= nil then
            data.forgetSlot = nil
            manualRewardsChanged = true
        end
    end

    local hasEntries = BurdJournals.hasAnyEntries
    local hasRewardEntries = (hasEntries and hasEntries(data.skills))
        or (hasEntries and hasEntries(data.traits))
        or (hasEntries and hasEntries(data.recipes))
        or (hasEntries and hasEntries(data.stats))
        or data.forgetSlot == true

    local sanitizedGifts = {}
    if type(data.yuletideImmediateGifts) == "table" then
        for _, gift in ipairs(data.yuletideImmediateGifts) do
            if type(gift) == "table" and type(gift.type) == "string" and gift.type ~= "" then
                sanitizedGifts[#sanitizedGifts + 1] = {
                    type = gift.type,
                    count = math.max(1, math.floor(tonumber(gift.count) or 1)),
                }
            end
        end
    end

    local needsRewardEntries = (not manualRewards) and (not hasRewardEntries)
    local needsGiftRoll = (#sanitizedGifts == 0) and data.yuletideGiftGranted ~= true
    local normalizedWrappedVariant = BurdJournals.normalizeYuletideWrappedVariant
        and BurdJournals.normalizeYuletideWrappedVariant(data.yuletideWrappedVariant)
        or data.yuletideWrappedVariant
    local needsIdentityData = not normalizeServerText(data.author)
        or not normalizeServerText(data.profession)
        or not normalizeServerText(data.professionName)
        or not normalizeServerText(data.flavorKey)
        or data.yuletideWrappedVariant ~= normalizedWrappedVariant

    if not needsRewardEntries and not needsGiftRoll and not needsIdentityData and not manualRewardsChanged then
        data.yuletideImmediateGifts = sanitizedGifts
        return false
    end

    local regenerated = BurdJournals.Server.generateYuletideJournalProfile({
        uuid = data.uuid,
        author = data.author,
        profession = data.profession,
        professionName = data.professionName,
        flavorKey = data.flavorKey,
        flavorText = data.flavorText,
        loreNoteText = data.loreNoteText,
        loreNoteTemplateVersion = data.loreNoteTemplateVersion,
        loreNoteTemplateFamily = data.loreNoteTemplateFamily,
        timestamp = data.timestamp,
        condition = data.condition,
        skills = needsRewardEntries and nil or data.skills,
        traits = needsRewardEntries and nil or data.traits,
        recipes = needsRewardEntries and nil or data.recipes,
        stats = needsRewardEntries and nil or data.stats,
        forgetSlot = needsRewardEntries and nil or data.forgetSlot,
        claims = data.claims,
        claimedSkills = data.claimedSkills,
        claimedTraits = data.claimedTraits,
        claimedRecipes = data.claimedRecipes,
        claimedStats = data.claimedStats,
        claimedForgetSlot = data.claimedForgetSlot,
        isZombieJournal = data.isZombieJournal == true,
        yuletideState = data.yuletideState,
        yuletideImmediateGifts = needsGiftRoll and nil or sanitizedGifts,
        yuletideGiftGranted = data.yuletideGiftGranted == true,
        yuletideGiftTier = data.yuletideGiftTier,
        yuletideGiftRoll = data.yuletideGiftRoll,
        yuletideManualRewards = manualRewards,
        yuletideWrappedVariant = data.yuletideWrappedVariant,
        yuletideOpenedByName = data.yuletideOpenedByName,
        yuletideDeliveryToken = data.yuletideDeliveryToken,
        yuletideDeliveredBy = data.yuletideDeliveredBy,
        yuletideDeliveryLabel = data.yuletideDeliveryLabel,
        yuletidePendingDelivery = data.yuletidePendingDelivery == true,
        yuletideBeacon = data.yuletideBeacon,
    })

    local metadataChanged = false
    if not normalizeServerText(data.author) and normalizeServerText(regenerated.author) then
        data.author = regenerated.author
        metadataChanged = true
    end
    if not normalizeServerText(data.profession) and normalizeServerText(regenerated.profession) then
        data.profession = regenerated.profession
        metadataChanged = true
    end
    if not normalizeServerText(data.professionName) and normalizeServerText(regenerated.professionName) then
        data.professionName = regenerated.professionName
        metadataChanged = true
    end
    if not normalizeServerText(data.flavorKey) and normalizeServerText(regenerated.flavorKey) then
        data.flavorKey = regenerated.flavorKey
        metadataChanged = true
    end
    if data.yuletideWrappedVariant ~= normalizedWrappedVariant then
        data.yuletideWrappedVariant = normalizedWrappedVariant
        metadataChanged = true
    end
    if needsRewardEntries then
        data.skills = regenerated.skills or {}
        data.traits = regenerated.traits
        data.recipes = regenerated.recipes
        data.stats = regenerated.stats or {}
        data.forgetSlot = regenerated.forgetSlot
    end
    if needsGiftRoll then
        data.yuletideImmediateGifts = regenerated.yuletideImmediateGifts or {}
        data.yuletideGiftTier = regenerated.yuletideGiftTier or data.yuletideGiftTier
        data.yuletideGiftRoll = regenerated.yuletideGiftRoll or data.yuletideGiftRoll
    else
        data.yuletideImmediateGifts = sanitizedGifts
    end
    return needsRewardEntries or needsGiftRoll or metadataChanged or manualRewardsChanged
end

BurdJournals.Server.YULETIDE_DELIVERY_MODDATA_KEY = BurdJournals.Server.YULETIDE_DELIVERY_MODDATA_KEY or "BurdJournals_YuletideDeliveryV1"

YULETIDE_MAILBOX_CONTAINER_TYPES = {
    postbox = true,
    mailbox = true,
}

YULETIDE_FALLBACK_CONTAINER_TYPES = {
    sidetable = true,
    endtable = true,
    nightstand = true,
    desk = true,
    counter = true,
    shelves = true,
    metal_shelves = true,
    crate = true,
    cardboardbox = true,
    smallbox = true,
    dresser = true,
}

YULETIDE_BEACON_SOUND_RADIUS = 12
YULETIDE_BEACON_SOUND_VOLUME = 1
YULETIDE_GIFT_TYPE_ALIASES = {
    ["Base.CannedSoup"] = {"Base.TinnedSoup", "Base.CannedMushroomSoup"},
    ["Base.Flashlight"] = {"Base.Torch"},
    ["Base.BandageBox"] = {"Base.Bandage"},
    ["Base.MoneyBundle"] = {"Base.Money", "Base.Wallet"},
    ["Base.GoldBar"] = {"Base.Necklace_Gold", "Base.Money"},
    ["Base.Whiskey"] = {"Base.Wine", "Base.BeerBottle"},
    ["Base.JerryCan"] = {"Base.PetrolCan", "Base.EmptyPetrolCan"},
}

BurdJournals.Server.YULETIDE_GUARANTEED_GIFT_FALLBACKS = BurdJournals.Server.YULETIDE_GUARANTEED_GIFT_FALLBACKS or {
    { type = "Base.Battery", count = 2 },
    { type = "Base.NailsBox", count = 1 },
    { type = "Base.DuctTape", count = 1 },
    { type = "Base.Bandage", count = 2 },
    { type = "Base.Torch", count = 1 },
}

local function doesYuletideGiftItemTypeExist(fullType)
    if type(fullType) ~= "string" or fullType == "" then
        return false
    end
    local manager = ScriptManager and ScriptManager.instance or nil
    local getItem = manager and manager.getItem or nil
    if not getItem then
        return true
    end
    local ok, scriptItem = pcall(getItem, manager, fullType)
    return ok and scriptItem ~= nil
end

local function resolveYuletideGiftItemType(fullType)
    if type(fullType) ~= "string" or fullType == "" then
        return nil
    end
    if doesYuletideGiftItemTypeExist(fullType) then
        return fullType
    end
    local aliases = YULETIDE_GIFT_TYPE_ALIASES[fullType]
    if type(aliases) == "table" then
        for _, aliasType in ipairs(aliases) do
            if doesYuletideGiftItemTypeExist(aliasType) then
                return aliasType
            end
        end
    end
    return nil
end

local function sanitizeYuletideGiftList(gifts)
    if type(gifts) ~= "table" then
        return {}
    end
    local out = {}
    for _, gift in ipairs(gifts) do
        local resolvedType = type(gift) == "table" and resolveYuletideGiftItemType(gift.type) or nil
        if resolvedType then
            out[#out + 1] = {
                type = resolvedType,
                count = math.max(1, math.floor(tonumber(gift.count) or 1)),
            }
        end
    end
    return out
end

function BurdJournals.Server.buildGuaranteedYuletideGiftFallback()
    local fallback = {}
    for _, gift in ipairs(BurdJournals.Server.YULETIDE_GUARANTEED_GIFT_FALLBACKS or {}) do
        local resolvedType = resolveYuletideGiftItemType(gift.type)
        if resolvedType then
            fallback[#fallback + 1] = {
                type = resolvedType,
                count = math.max(1, math.floor(tonumber(gift.count) or 1)),
            }
        end
    end

    if #fallback == 0 then
        return {
            { type = "Base.Battery", count = 1 },
        }
    end

    local guaranteed = { fallback[1] }
    if #fallback >= 2 then
        guaranteed[#guaranteed + 1] = fallback[2]
    end
    return guaranteed
end

local function getYuletideGiftDisplayName(fullType, addedItem)
    if addedItem then
        local ok, displayName = pcall(function()
            return addedItem:getDisplayName()
        end)
        if ok and type(displayName) == "string" and displayName ~= "" then
            return displayName
        end
    end

    local scriptManager = getScriptManager and getScriptManager() or ScriptManager and ScriptManager.instance or nil
    if scriptManager and scriptManager.getItem then
        local ok, scriptItem = pcall(function()
            return scriptManager:getItem(fullType)
        end)
        if ok and scriptItem and scriptItem.getDisplayName then
            local okName, scriptDisplayName = pcall(function()
                return scriptItem:getDisplayName()
            end)
            if okName and type(scriptDisplayName) == "string" and scriptDisplayName ~= "" then
                return scriptDisplayName
            end
        end
    end

    local fallback = tostring(fullType or "Gift")
    if fallback:find("%.") then
        fallback = fallback:match("%.(.+)") or fallback
    end
    fallback = fallback:gsub("_", " ")
    fallback = fallback:gsub("(%l)(%u)", "%1 %2")
    return fallback
end

local shouldUseYuletideContainerObjectChange

local function getYuletideInventoryItemCount(inventory, fullType)
    if not inventory or type(fullType) ~= "string" or fullType == "" then
        return 0
    end

    if inventory.getItemsFromFullType then
        local ok, items = pcall(function()
            return inventory:getItemsFromFullType(fullType, false)
        end)
        if ok and items and items.size then
            return tonumber(items:size()) or 0
        end
    end

    if inventory.getItems then
        local ok, items = pcall(function()
            return inventory:getItems()
        end)
        if ok and items and items.size then
            local count = 0
            for i = 0, items:size() - 1 do
                local item = items:get(i)
                local itemType = item and item.getFullType and item:getFullType() or nil
                if itemType == fullType then
                    count = count + 1
                end
            end
            return count
        end
    end

    if inventory.getItemCount then
        local ok, count = pcall(function()
            return inventory:getItemCount(fullType)
        end)
        if ok then
            return tonumber(count) or 0
        end
    end

    return 0
end

local function isYuletideGiftItemInInventory(inventory, item, fullType, beforeCount)
    if not inventory or not item then
        return false
    end

    if inventory.contains then
        local okContains, contains = pcall(function()
            return inventory:contains(item)
        end)
        if okContains and contains == true then
            return true
        end
    end

    if item.getID and inventory.containsID then
        local itemId = item:getID()
        if itemId ~= nil then
            local okId, containsId = pcall(function()
                return inventory:containsID(itemId)
            end)
            if okId and containsId == true then
                return true
            end
        end
    end

    if type(fullType) == "string" and fullType ~= "" then
        return getYuletideInventoryItemCount(inventory, fullType) > (tonumber(beforeCount) or 0)
    end

    return false
end

local function syncYuletideGiftInventory(player, inventory, item, label)
    if not inventory then
        return
    end

    local sourceLabel = tostring(label or "Yuletide gift")
    if inventory.setDirty then
        BurdJournals.safePcall(function()
            inventory:setDirty(true)
        end)
    end
    if inventory.setDrawDirty then
        inventory:setDrawDirty(true)
    end
    if inventory.addItemsToProcessItems then
        BurdJournals.safePcall(function()
            inventory:addItemsToProcessItems()
        end)
    end
    if item and inventory.addItemOnServer then
        BurdJournals.safePcall(function()
            inventory:addItemOnServer(item)
        end)
    end
    if item and shouldUseYuletideContainerObjectChange() and sendAddItemToContainer then
        BurdJournals.safePcall(function()
            sendAddItemToContainer(inventory, item)
        end)
        BurdJournals.debugPrint("[BurdJournals] " .. sourceLabel .. " replicated through container add-item sync.")
    end
    if inventory.sync then
        BurdJournals.safePcall(function()
            inventory:sync()
        end)
    end
    if inventory.sendContentsToRemoteContainer then
        BurdJournals.safePcall(function()
            inventory:sendContentsToRemoteContainer()
        end)
    end
    if player and player.syncInventory then
        BurdJournals.safePcall(function()
            player:syncInventory()
        end)
    end
end

local function addYuletideGiftItem(player, inventory, fullType)
    if not inventory or type(fullType) ~= "string" or fullType == "" then
        return nil
    end

    local beforeCount = getYuletideInventoryItemCount(inventory, fullType)
    local attempts = {}

    if InventoryItemFactory and InventoryItemFactory.CreateItem then
        local okFactory, createdItem = pcall(function()
            return InventoryItemFactory.CreateItem(fullType)
        end)
        if okFactory and createdItem then
            attempts[#attempts + 1] = {
                label = "AddItem(item)",
                item = createdItem,
                run = function()
                    return inventory:AddItem(createdItem) or createdItem
                end,
            }
        end
    end

    attempts[#attempts + 1] = {
        label = "AddItem(string)",
        run = function()
            return inventory:AddItem(fullType)
        end,
    }

    attempts[#attempts + 1] = {
        label = "AddItems(string,1)",
        run = function()
            if not inventory.AddItems then
                return nil
            end
            local items = inventory:AddItems(fullType, 1)
            if items and items.size and items:size() > 0 then
                return items:get(0)
            end
            return nil
        end,
    }

    for _, attempt in ipairs(attempts) do
        local ok, candidate = pcall(attempt.run)
        if ok and candidate and isYuletideGiftItemInInventory(inventory, candidate, fullType, beforeCount) then
            syncYuletideGiftInventory(player, inventory, candidate, "Yuletide gift " .. tostring(attempt.label))
            return candidate
        end
        local afterCount = getYuletideInventoryItemCount(inventory, fullType)
        if afterCount > beforeCount then
            local recovered = nil
            if inventory.getItemsFromFullType then
                local okItems, items = pcall(function()
                    return inventory:getItemsFromFullType(fullType, false)
                end)
                if okItems and items and items.size and items:size() > 0 then
                    recovered = items:get(items:size() - 1)
                end
            end
            syncYuletideGiftInventory(player, inventory, recovered, "Yuletide gift " .. tostring(attempt.label))
            return recovered or candidate
        end

        if ok and candidate and inventory.Remove then
            -- Some B41 paths create a detached item object without inserting it.
            -- Do not report that as granted.
            BurdJournals.debugPrint("[BurdJournals] Yuletide gift add attempt "
                .. tostring(attempt.label) .. " returned an item that was not present in inventory for "
                .. tostring(fullType))
        elseif not ok then
            BurdJournals.debugPrint("[BurdJournals] Yuletide gift add attempt "
                .. tostring(attempt.label) .. " failed for " .. tostring(fullType)
                .. ": " .. tostring(candidate))
        end
    end

    if InventoryItemFactory and InventoryItemFactory.CreateItem and inventory.AddItemBlind then
        local okFactory, createdItem = pcall(function()
            return InventoryItemFactory.CreateItem(fullType)
        end)
        if okFactory and createdItem then
            local okBlind, blindResult = pcall(function()
                return inventory:AddItemBlind(createdItem) or createdItem
            end)
            if okBlind and blindResult and isYuletideGiftItemInInventory(inventory, blindResult, fullType, beforeCount) then
                syncYuletideGiftInventory(player, inventory, blindResult, "Yuletide gift AddItemBlind")
                return blindResult
            end
        end
    end

    return nil
end

shouldUseYuletideContainerObjectChange = function()
    return isServer and isServer()
end

local function transmitYuletideGiftItem(player, inventory, item, label)
    if not inventory or not item then
        return
    end
    syncYuletideGiftInventory(player, inventory, item, label)
end

local function ensureYuletideDeliveryStoreShape(store)
    if type(store) ~= "table" then
        return nil
    end
    if type(store.delivered) ~= "table" then
        store.delivered = {}
    end
    if type(store.pending) ~= "table" then
        store.pending = {}
    end
    if type(store.beacons) ~= "table" then
        store.beacons = {}
    end
    return store
end

function BurdJournals.Server.getYuletideDeliveryStore()
    if not ModData or not ModData.getOrCreate then
        return nil
    end
    local store = ModData.getOrCreate(BurdJournals.Server.YULETIDE_DELIVERY_MODDATA_KEY)
    return ensureYuletideDeliveryStoreShape(store)
end

function BurdJournals.Server.transmitYuletideDeliveryStore()
    if ModData and ModData.transmit then
        ModData.transmit(BurdJournals.Server.YULETIDE_DELIVERY_MODDATA_KEY)
    end
end

local function getYuletidePlayerKey(player)
    if not player then
        return nil
    end
    local username = player.getUsername and tostring(player:getUsername() or "") or ""
    if username ~= "" then
        return username
    end
    local characterId = BurdJournals.getPlayerCharacterId and BurdJournals.getPlayerCharacterId(player) or nil
    if characterId then
        return "character:" .. tostring(characterId)
    end
    local onlineId = player.getOnlineID and player:getOnlineID() or nil
    if onlineId ~= nil then
        return "online:" .. tostring(onlineId)
    end
    return nil
end

local function getYuletideJournalUUID(item, data)
    if type(data) ~= "table" and item and item.getModData then
        local modData = item:getModData()
        data = modData and modData.BurdJournals or nil
    end
    if BurdJournals.resolveJournalUUIDForRuntime then
        local uuid = BurdJournals.resolveJournalUUIDForRuntime(data, item, true)
        if uuid then
            return uuid
        end
    end
    return type(data) == "table" and data.uuid or nil
end

local function normalizeYuletideJournalStateForType(item, sourceTag)
    if not item or not item.getModData then
        return nil
    end
    local modData = item:getModData()
    modData.BurdJournals = modData.BurdJournals or {}
    local data = modData.BurdJournals
    local fullType = item.getFullType and item:getFullType() or ""
    if fullType == (BurdJournals.YULETIDE_ITEM_TYPE or "BurdJournals.YuletideJournal") then
        data.isYuletideJournal = true
        data.yuletideState = data.yuletideState == BurdJournals.YULETIDE_STATE_UNWRAPPED
            and BurdJournals.YULETIDE_STATE_UNWRAPPED
            or BurdJournals.YULETIDE_STATE_WRAPPED
        data.isWorn = false
        data.isBloody = false
        data.isCursedJournal = false
        data.isCursedReward = false
        data.cursedState = nil
    elseif data.isYuletideJournal == true and not data.yuletideState then
        data.yuletideState = BurdJournals.YULETIDE_STATE_WRAPPED
    end
    if sourceTag and data.isYuletideJournal == true then
        BurdJournals.debugPrint("[BurdJournals] normalizeYuletideJournalStateForType(" .. tostring(sourceTag)
            .. ") uuid=" .. tostring(data.uuid) .. " state=" .. tostring(data.yuletideState))
    end
    return data
end

local function buildYuletideGiftGrantSummary(player, grantedGifts)
    local summary = {}
    local inventory = player and player.getInventory and player:getInventory() or nil
    local requestedGiftCount = type(grantedGifts) == "table" and #grantedGifts or 0
    local grantedAnything = false
    if not inventory then
        BurdJournals.debugPrint("[BurdJournals] Yuletide gift grant aborted because player inventory was unavailable.")
        return summary
    end
    for _, gift in ipairs(grantedGifts or {}) do
        local requestedCount = math.max(1, math.floor(tonumber(gift.count) or 1))
        local grantedCount = 0
        local firstAdded = nil
        for _ = 1, requestedCount do
            local added = addYuletideGiftItem(player, inventory, gift.type)
            if added then
                firstAdded = firstAdded or added
                grantedCount = grantedCount + 1
                transmitYuletideGiftItem(player, inventory, added)
            else
                BurdJournals.debugPrint("[BurdJournals] Yuletide gift grant failed to add item type " .. tostring(gift.type))
                break
            end
        end

        if grantedCount > 0 then
            grantedAnything = true
            summary[#summary + 1] = {
                type = gift.type,
                count = grantedCount,
                displayName = getYuletideGiftDisplayName(gift.type, firstAdded),
            }
        end
    end
    if #summary == 0 and requestedGiftCount > 0 then
        BurdJournals.debugPrint("[BurdJournals] Yuletide gift grant produced no items for " .. tostring(requestedGiftCount) .. " requested gifts.")
    end
    if grantedAnything then
        if inventory.sync then
            BurdJournals.safePcall(function()
                inventory:sync()
            end)
        end
        if inventory.setDrawDirty then
            inventory:setDrawDirty(true)
        end
        if player and player.syncInventory then
            BurdJournals.safePcall(function()
                player:syncInventory()
            end)
        end
    end
    return summary
end

YULETIDE_GIFT_GRANT_VERSION = 2

function BurdJournals.Server.shouldClientMaterializeYuletideGifts()
    if isClient and isClient() then
        return false
    end
    -- B41 MP player inventories are safest when the owning client materializes
    -- the server-authorized reward. On a dedicated server getPlayer() is absent.
    if not (getPlayer and getPlayer()) then
        return true
    end
    return isServer and isServer()
end

local function hasVerifiedYuletideGiftGrant(data)
    return type(data) == "table"
        and data.yuletideGiftGranted == true
        and (tonumber(data.yuletideGiftGrantVersion) or 0) >= YULETIDE_GIFT_GRANT_VERSION
end

function BurdJournals.Server.buildYuletideGiftGrantToken(item, data)
    local uuid = getYuletideJournalUUID(item, data)
    if uuid then
        return "yuletide-gift:" .. tostring(uuid)
    end
    local itemId = item and item.getID and item:getID() or nil
    return "yuletide-gift:item:" .. tostring(itemId or ((getTimestampMs and getTimestampMs()) or os.time()))
end

function BurdJournals.Server.buildClientYuletideGiftGrantSummary(item, data, gifts)
    local summary = {}
    for _, gift in ipairs(gifts or {}) do
        local requestedCount = math.max(1, math.floor(tonumber(gift.count) or 1))
        summary[#summary + 1] = {
            type = gift.type,
            count = requestedCount,
            displayName = getYuletideGiftDisplayName(gift.type, nil),
        }
    end
    data.yuletideGiftGranted = false
    data.yuletideGiftGrantVersion = nil
    data.yuletidePendingClientGiftGrant = true
    data.yuletideGiftGrantToken = BurdJournals.Server.buildYuletideGiftGrantToken(item, data)
    data.yuletidePendingClientGifts = summary
    return summary
end

function BurdJournals.Server.grantYuletideImmediateGifts(player, item, data)
    if not player or type(data) ~= "table" then
        return {}
    end
    if hasVerifiedYuletideGiftGrant(data) then
        return {}
    end

    local sanitized = sanitizeYuletideGiftList(data.yuletideImmediateGifts)
    if #sanitized == 0 then
        for attempt = 1, 4 do
            local rerolledGifts, rerolledTier, rerolledRoll = rollYuletideImmediateGifts()
            sanitized = sanitizeYuletideGiftList(rerolledGifts)
            if #sanitized > 0 then
                data.yuletideGiftTier = rerolledTier or data.yuletideGiftTier or "practical"
                data.yuletideGiftRoll = rerolledRoll or data.yuletideGiftRoll
                BurdJournals.debugPrint("[BurdJournals] Yuletide gift grant rerolled gift bundle because the stored list was empty (attempt " .. tostring(attempt) .. ").")
                break
            end
        end
    end
    if #sanitized == 0 then
        sanitized = BurdJournals.Server.buildGuaranteedYuletideGiftFallback()
        data.yuletideGiftTier = data.yuletideGiftTier or "practical"
        BurdJournals.debugPrint("[BurdJournals] Yuletide gift grant fell back to guaranteed practical bundle because all rolled entries were unavailable.")
    end

    if BurdJournals.Server.shouldClientMaterializeYuletideGifts() then
        local clientGranted = BurdJournals.Server.buildClientYuletideGiftGrantSummary(item, data, sanitized)
        data.yuletideImmediateGifts = sanitized
        data.yuletideGiftTier = data.yuletideGiftTier or "practical"
        if item and item.transmitModData then
            item:transmitModData()
        end
        return clientGranted
    end

    local granted = buildYuletideGiftGrantSummary(player, sanitized)
    data.yuletideImmediateGifts = sanitized
    data.yuletideGiftGranted = #granted > 0
    if #granted > 0 then
        data.yuletideGiftGrantVersion = YULETIDE_GIFT_GRANT_VERSION
    else
        data.yuletideGiftGrantVersion = nil
    end
    data.yuletideGiftTier = data.yuletideGiftTier or "practical"
    if #granted == 0 then
        BurdJournals.debugPrint("[BurdJournals] Yuletide gift grant completed with zero granted items; journal will remain eligible for recovery.")
    end
    if item and item.transmitModData then
        item:transmitModData()
    end
    return granted
end

local function getYuletideSeasonNow()
    local dateTable = nil
    if os and os.date then
        dateTable = os.date("*t")
    end
    if BurdJournals.getYuletideSeasonContext then
        return BurdJournals.getYuletideSeasonContext(dateTable)
    end
    return nil
end

local function getYuletideSafehouseForPlayer(player)
    if not player then
        return nil
    end
    if SafeHouse and SafeHouse.hasSafehouse then
        local ok, safehouse = pcall(SafeHouse.hasSafehouse, player)
        if ok and safehouse then
            return safehouse
        end
    end
    if SafeHouse and SafeHouse.getSafeHouse and player.getSquare then
        local square = player:getSquare()
        if square then
            local ok, safehouse = pcall(SafeHouse.getSafeHouse, square)
            if ok and safehouse then
                return safehouse
            end
        end
    end
    return nil
end

local function getSafehouseDisplayLabel(safehouse)
    if not safehouse then
        return "Safehouse"
    end
    local title = safehouse.getTitle and safehouse:getTitle() or nil
    if type(title) == "string" and title ~= "" then
        return title
    end
    local owner = safehouse.getOwner and safehouse:getOwner() or nil
    if type(owner) == "string" and owner ~= "" then
        return owner .. "'s Safehouse"
    end
    return "Safehouse"
end

local function getYuletideContainerTypeName(container)
    if not container or not container.getType then
        return nil
    end
    local ok, value = pcall(function()
        return container:getType()
    end)
    if not ok or value == nil then
        return nil
    end
    local text = tostring(value)
    if text == "" then
        return nil
    end
    return string.lower(text)
end

local function forEachSafehouseSquare(safehouse, visitor)
    if not safehouse or not visitor then
        return
    end
    local cell = getCell and getCell() or nil
    if not cell or not cell.getGridSquare then
        return
    end

    local startX = tonumber(safehouse.getX and safehouse:getX()) or nil
    local startY = tonumber(safehouse.getY and safehouse:getY()) or nil
    local width = tonumber(safehouse.getW and safehouse:getW()) or nil
    local height = tonumber(safehouse.getH and safehouse:getH()) or nil
    if not startX or not startY or not width or not height then
        return
    end

    local maxX = startX + math.max(0, width - 1)
    local maxY = startY + math.max(0, height - 1)
    for z = 0, 7 do
        for x = startX, maxX do
            for y = startY, maxY do
                local square = cell:getGridSquare(x, y, z)
                if square then
                    visitor(square)
                end
            end
        end
    end
end

local function pickYuletideDeliveryTarget(safehouse)
    local preferred = nil
    local fallbackContainer = nil
    local fallbackSquare = nil

    forEachSafehouseSquare(safehouse, function(square)
        local room = square.getRoom and square:getRoom() or nil
        if not fallbackSquare and room then
            fallbackSquare = square
        end
        local objects = square.getObjects and square:getObjects() or nil
        if not objects then
            return
        end
        for i = 0, objects:size() - 1 do
            local obj = objects:get(i)
            local container = obj and obj.getContainer and obj:getContainer() or nil
            if container then
                local containerType = getYuletideContainerTypeName(container)
                if not preferred and containerType and YULETIDE_MAILBOX_CONTAINER_TYPES[containerType] then
                    preferred = {
                        square = square,
                        container = container,
                        containerType = containerType,
                        label = getYuletideServerText("UI_BurdJournals_YuletideDeliveryMailbox", "mailbox"),
                    }
                    return
                end
                if not fallbackContainer and room and containerType and YULETIDE_FALLBACK_CONTAINER_TYPES[containerType] then
                    fallbackContainer = {
                        square = square,
                        container = container,
                        containerType = containerType,
                        label = containerType,
                    }
                end
            end
        end
    end)

    if preferred then
        return preferred
    end
    if fallbackContainer then
        return fallbackContainer
    end
    if fallbackSquare then
        return {
            square = fallbackSquare,
            container = nil,
            containerType = nil,
            label = getYuletideServerText("UI_BurdJournals_YuletideDeliveryFallback", "near the front room"),
        }
    end
    return nil
end

local function findYuletideJournalAtBeaconSquare(square, journalUUID)
    if not square or not journalUUID then
        return nil
    end

    local objects = square.getObjects and square:getObjects() or nil
    if objects then
        for i = 0, objects:size() - 1 do
            local obj = objects:get(i)
            local container = obj and obj.getContainer and obj:getContainer() or nil
            if container and container.getItems then
                local items = container:getItems()
                if items then
                    for j = 0, items:size() - 1 do
                        local item = items:get(j)
                        local uuid = getYuletideJournalUUID(item)
                        if uuid == journalUUID then
                            return item
                        end
                    end
                end
            end
        end
    end

    local worldObjects = square.getWorldObjects and square:getWorldObjects() or nil
    if worldObjects then
        for i = 0, worldObjects:size() - 1 do
            local worldObj = worldObjects:get(i)
            local item = worldObj and worldObj.getItem and worldObj:getItem() or nil
            local uuid = getYuletideJournalUUID(item)
            if uuid == journalUUID then
                return item
            end
        end
    end

    return nil
end

local function clearYuletideBeacon(store, journalUUID)
    if type(store) ~= "table" or type(store.beacons) ~= "table" or not journalUUID then
        return false
    end
    if store.beacons[journalUUID] ~= nil then
        store.beacons[journalUUID] = nil
        return true
    end
    return false
end

local function recordYuletideBeacon(store, item, square, sourceLabel)
    local data = item and item.getModData and item:getModData().BurdJournals or nil
    local journalUUID = getYuletideJournalUUID(item, data)
    if type(store) ~= "table" or not square or not journalUUID then
        return false
    end

    store.beacons[journalUUID] = {
        uuid = journalUUID,
        x = square.getX and square:getX() or nil,
        y = square.getY and square:getY() or nil,
        z = square.getZ and square:getZ() or nil,
        label = sourceLabel,
        lastEmitHours = nil,
    }
    data.yuletideBeacon = {
        x = store.beacons[journalUUID].x,
        y = store.beacons[journalUUID].y,
        z = store.beacons[journalUUID].z,
        label = sourceLabel,
    }
    return true
end

local function markYuletideDelivered(store, playerKey, deliveryToken, payload)
    if type(store) ~= "table" or not playerKey or not deliveryToken then
        return
    end
    store.delivered[playerKey] = store.delivered[playerKey] or {}
    store.delivered[playerKey][deliveryToken] = payload or true
    store.pending[playerKey] = nil
end

local function hasYuletideDeliveryRecorded(store, playerKey, deliveryToken)
    if type(store) ~= "table" or not playerKey or not deliveryToken then
        return false
    end
    local delivered = store.delivered[playerKey]
    return type(delivered) == "table" and delivered[deliveryToken] ~= nil
end

local function setYuletidePendingDelivery(store, playerKey, pendingData)
    if type(store) ~= "table" or not playerKey then
        return
    end
    if type(pendingData) == "table" then
        store.pending[playerKey] = pendingData
    else
        store.pending[playerKey] = nil
    end
end

local function materializeYuletideDelivery(player, safehouse, seasonContext, store)
    local playerKey = getYuletidePlayerKey(player)
    local deliveryToken = seasonContext and seasonContext.eventToken or nil
    if not playerKey or not deliveryToken then
        return false
    end

    local target = pickYuletideDeliveryTarget(safehouse)
    if not target or not target.square then
        setYuletidePendingDelivery(store, playerKey, {
            deliveryToken = deliveryToken,
            safehouseLabel = getSafehouseDisplayLabel(safehouse),
        })
        return false
    end

    local journal = nil
    local targetContainer = target.container
    if targetContainer and targetContainer.AddItem then
        journal = addJournalItemToContainerCompat(targetContainer, BurdJournals.YULETIDE_ITEM_TYPE or "BurdJournals.YuletideJournal")
    end

    if not journal and InventoryItemFactory then
        journal = InventoryItemFactory.CreateItem(BurdJournals.YULETIDE_ITEM_TYPE or "BurdJournals.YuletideJournal")
        if journal and target.square and target.square.AddWorldInventoryItem then
            target.square:AddWorldInventoryItem(journal, 0.45, 0.45, 0)
        end
    end

    if not journal then
        setYuletidePendingDelivery(store, playerKey, {
            deliveryToken = deliveryToken,
            safehouseLabel = getSafehouseDisplayLabel(safehouse),
        })
        return false
    end

    local profile = BurdJournals.Server.generateYuletideJournalProfile({
        timestamp = getGameTime() and getGameTime():getWorldAgeHours() or 0,
        isZombieJournal = false,
        yuletideState = BurdJournals.YULETIDE_STATE_WRAPPED,
        yuletideDeliveryToken = deliveryToken,
        yuletideDeliveredBy = player and player.getUsername and player:getUsername() or nil,
        yuletideDeliveryLabel = getSafehouseDisplayLabel(safehouse),
        yuletidePendingDelivery = false,
        loreNoteTemplateVersion = LORE_DYNAMIC_VERSION,
        loreNoteTemplateFamily = "yuletide",
        lootNotesEligible = true,
    })
    local modData = journal:getModData()
    modData.BurdJournals = {}
    for key, value in pairs(profile) do
        modData.BurdJournals[key] = value
    end
    modData.BurdJournals.sourceType = "safehouse_delivery"

    BurdJournals.updateJournalName(journal)
    BurdJournals.updateJournalIcon(journal)
    if targetContainer and targetContainer.setDrawDirty then
        targetContainer:setDrawDirty(true)
    end
    if targetContainer and sendAddItemToContainer then
        sendAddItemToContainer(targetContainer, journal)
    end

    local beaconSourceLabel = target.label or getYuletideServerText("UI_BurdJournals_YuletideDeliveryFallback", "safehouse")
    recordYuletideBeacon(store, journal, target.square, beaconSourceLabel)
    if journal.transmitModData and not BurdJournals.Server.shouldDeferBatchTransmit() then
            journal:transmitModData()
    end

    local journalUUID = getYuletideJournalUUID(journal, modData.BurdJournals)
    markYuletideDelivered(store, playerKey, deliveryToken, {
        uuid = journalUUID,
        deliveredAtHours = getGameTime() and getGameTime():getWorldAgeHours() or 0,
        safehouseLabel = getSafehouseDisplayLabel(safehouse),
        deliveryLabel = beaconSourceLabel,
    })

    BurdJournals.Server.sendToClient(player, "yuletideDeliveryNotice", {
        journalId = journal.getID and journal:getID() or nil,
        journalUUID = journalUUID,
        deliveryToken = deliveryToken,
        safehouseLabel = getSafehouseDisplayLabel(safehouse),
        deliveryLabel = beaconSourceLabel,
        message = getYuletideServerText(
            "UI_BurdJournals_YuletideDeliveryNotice",
            "A wrapped Yuletide Journal has been delivered to your safehouse."
        ),
    })

    return true
end

function BurdJournals.Server.processYuletideBeacons(store)
    if type(store) ~= "table" or type(store.beacons) ~= "table" then
        return
    end

    local cell = getCell and getCell() or nil
    if not cell or not cell.getGridSquare then
        return
    end

    local changed = false
    local worldAge = getGameTime() and getGameTime():getWorldAgeHours() or 0
    for journalUUID, beacon in pairs(store.beacons) do
        local x = beacon and tonumber(beacon.x) or nil
        local y = beacon and tonumber(beacon.y) or nil
        local z = beacon and tonumber(beacon.z) or nil
        if not x or not y or not z then
            store.beacons[journalUUID] = nil
            changed = true
        else
            local square = cell:getGridSquare(x, y, z)
            local item = square and findYuletideJournalAtBeaconSquare(square, journalUUID) or nil
            local data = item and item.getModData and item:getModData().BurdJournals or nil
            if not item or type(data) ~= "table" or data.yuletideState == BurdJournals.YULETIDE_STATE_UNWRAPPED then
                store.beacons[journalUUID] = nil
                changed = true
            else
                beacon.lastEmitHours = tonumber(beacon.lastEmitHours) or -999999
                if (worldAge - beacon.lastEmitHours) >= 1 and addSound then
                    addSound(nil, x, y, z, YULETIDE_BEACON_SOUND_RADIUS, YULETIDE_BEACON_SOUND_VOLUME)
                    beacon.lastEmitHours = worldAge
                    changed = true
                end
            end
        end
    end

    if changed then
        BurdJournals.Server.transmitYuletideDeliveryStore()
    end
end

function BurdJournals.Server.processYuletideHourlyTasks()
    local yuletideEnabled = BurdJournals.Server.areYuletideJournalSpawnsEnabled and BurdJournals.Server.areYuletideJournalSpawnsEnabled()
    local store = BurdJournals.Server.getYuletideDeliveryStore()
    if not store then
        return
    end

    BurdJournals.Server.processYuletideBeacons(store)

    if yuletideEnabled == false then
        return
    end

    -- Christmas Day safehouse delivery is deprecated. Keep the underlying
    -- delivery pipeline dormant for potential future reuse, but do not run it in gameplay.
    return
end

local function getTraitName(traitId)
    if BurdJournals and BurdJournals.getTraitDisplayName then
        return BurdJournals.getTraitDisplayName(traitId)
    end
    return tostring(traitId)
end

local function normalizeForcedTraitId(traitId)
    if type(traitId) ~= "string" then
        return nil
    end
    local normalized = BurdJournals.normalizeTraitId and BurdJournals.normalizeTraitId(traitId) or traitId
    if type(normalized) ~= "string" or normalized == "" then
        return nil
    end
    return normalized
end

local function normalizeForcedSkillName(skillName)
    if type(skillName) ~= "string" then
        return nil
    end
    local trimmed = string.gsub(skillName, "^%s+", "")
    trimmed = string.gsub(trimmed, "%s+$", "")
    if trimmed == "" then
        return nil
    end
    return trimmed
end

local function normalizeLowerText(value)
    if value == nil then
        return nil
    end
    return string.lower(tostring(value))
end

local function traitIdInRemovableList(traitId)
    if type(traitId) ~= "string" or traitId == "" then
        return false
    end
    if BurdJournals.isTraitRemovable then
        return BurdJournals.isTraitRemovable(traitId) == true
    end
    local target = normalizeLowerText(traitId)
    for _, listed in ipairs((BurdJournals.REMOVABLE_TRAITS) or {}) do
        if normalizeLowerText(listed) == target then
            return true
        end
    end
    return false
end

local function canApplyCursedNegativeTrait(player, traitId)
    if not player or type(traitId) ~= "string" or traitId == "" then
        return false
    end
    if not traitIdInRemovableList(traitId) then
        return false
    end
    if BurdJournals.playerHasTrait(player, traitId) then
        return false
    end
    return true
end

-- Server-authoritative trait removal with verification.
-- Returns true only when the trait existed and is now fully removed.
local function removeTraitAuthoritatively(player, traitId, opts)
    if not player or type(traitId) ~= "string" or traitId == "" then
        return false
    end

    local resolvedTrait = nil
    local foundTraits = nil
    if BurdJournals.Server and BurdJournals.Server.resolveCharacterTrait then
        resolvedTrait, _, _, foundTraits = BurdJournals.Server.resolveCharacterTrait(traitId, player)
    end

    local function hasTraitNow()
        if player and player.hasTrait then
            if resolvedTrait and player:hasTrait(resolvedTrait) == true then
                return true
            end
            if type(foundTraits) == "table" then
                for _, entry in ipairs(foundTraits) do
                    local traitObj = entry and entry.trait
                    if traitObj and player:hasTrait(traitObj) == true then
                        return true
                    end
                end
            end
        end
        local hadResolvedCandidates = resolvedTrait ~= nil or (type(foundTraits) == "table" and #foundTraits > 0)
        if hadResolvedCandidates then
            return false
        end
        return BurdJournals.playerHasTrait and BurdJournals.playerHasTrait(player, traitId) == true
    end

    local hadBefore = hasTraitNow()
    if not hadBefore then
        return false
    end

    if BurdJournals.safeRemoveTrait then
        -- safeRemoveTrait performs stateful removal, XP reconciliation, and
        -- lifecycle dispatch. Let unexpected failures remain visible; falling
        -- through after a partial mutation can apply those effects twice.
        local removedBySafe = BurdJournals.safeRemoveTrait(player, traitId, opts)
        if removedBySafe == true then
            return true
        end
    end

    local charTraits = player.getCharacterTraits and player:getCharacterTraits() or nil
    if charTraits then
        local function removeTraitObject(traitObj)
            if not traitObj then
                return
            end
            if charTraits.remove then
                charTraits:remove(traitObj)
            elseif charTraits.set then
                charTraits:set(traitObj, false)
            end
        end

        removeTraitObject(resolvedTrait)
        if type(foundTraits) == "table" then
            for _, entry in ipairs(foundTraits) do
                removeTraitObject(entry and entry.trait)
            end
        end
    end

    local removed = not hasTraitNow()
    if removed and BurdJournals.applyTraitLifecycleSideEffects then
        local resolvedTraitName = nil
        if resolvedTrait and resolvedTrait.getName then
            resolvedTraitName = resolvedTrait:getName()
        end
        BurdJournals.applyTraitLifecycleSideEffects(player, traitId, "trait_removed", {
            traitObj = resolvedTrait,
            resolvedTraitName = resolvedTraitName,
            source = "removeTraitAuthoritatively_fallback",
        })
    end
    if removed and resolvedTrait and player.modifyTraitXPBoost then
        player:modifyTraitXPBoost(resolvedTrait, true)
    end
    if removed and not (opts and opts.skipSyncXp) and SyncXp then
        SyncXp(player)
    end

    return removed
end

local function removeTraitConflictsForCursedAdd(player, traitId, opts)
    local removed = {}
    if not player or not traitId then
        return removed
    end
    local targetLower = normalizeLowerText(normalizeForcedTraitId(traitId) or traitId)
    local seen = {}
    local candidates = {}

    local function addCandidate(rawTraitId)
        if not rawTraitId then
            return
        end
        local normalized = normalizeForcedTraitId(rawTraitId)
            or (BurdJournals.normalizeTraitId and BurdJournals.normalizeTraitId(rawTraitId))
            or tostring(rawTraitId)
        local key = normalizeLowerText(normalized)
        if targetLower and key == targetLower then
            return
        end
        if normalized and key and not seen[key] then
            seen[key] = true
            candidates[#candidates + 1] = normalized
        end
    end

    -- Primary path: only traits the player currently has.
    local conflicts = BurdJournals.getConflictingTraits and BurdJournals.getConflictingTraits(player, traitId) or {}
    for _, conflictId in ipairs(conflicts) do
        addCandidate(conflictId)
    end

    -- Fallback path: resolve mutual exclusions directly from trait definitions.
    if CharacterTraitDefinition and CharacterTraitDefinition.getTraits then
        local allTraits = CharacterTraitDefinition.getTraits()
        if allTraits and targetLower and allTraits.size and allTraits.get then
            local matchedDef = nil
            for i = 0, allTraits:size() - 1 do
                local def = allTraits:get(i)
                if def then
                    local defType = def:getType()
                    local defName = ""
                    if defType then
                        if defType.getName then
                            defName = defType:getName() or tostring(defType)
                        else
                            defName = tostring(defType)
                        end
                    end
                    local defLabel = def.getLabel and def:getLabel() or ""
                    local defNameLower = normalizeLowerText(normalizeForcedTraitId(defName) or defName)
                    local defLabelLower = normalizeLowerText(normalizeForcedTraitId(defLabel) or defLabel)
                    if defNameLower == targetLower or defLabelLower == targetLower then
                        matchedDef = def
                        break
                    end
                end
            end

            if matchedDef and matchedDef.getMutuallyExclusiveTraits then
                local exclusives = matchedDef:getMutuallyExclusiveTraits()
                if exclusives and exclusives.size and exclusives.get then
                    for i = 0, exclusives:size() - 1 do
                        local exTrait = exclusives:get(i)
                        local exId = nil
                        if type(exTrait) == "string" then
                            exId = exTrait
                        elseif exTrait and exTrait.getName then
                            exId = exTrait:getName()
                        elseif exTrait then
                            exId = tostring(exTrait)
                        end
                        addCandidate(exId)
                    end
                end
            end
        end
    end

    for _, conflictId in ipairs(candidates) do
        if removeTraitAuthoritatively(player, conflictId, opts) then
            removed[#removed + 1] = conflictId
        else
            BurdJournals.debugPrint("[BurdJournals] Conflict removal verification failed for trait: " .. tostring(conflictId))
        end
    end

    return removed
end

local function selectTraitFromPool(pool, forcedTraitId)
    if type(pool) ~= "table" or #pool == 0 then
        return nil
    end
    local forcedLower = normalizeLowerText(forcedTraitId)
    if forcedLower then
        for _, traitId in ipairs(pool) do
            if normalizeLowerText(traitId) == forcedLower then
                return traitId
            end
        end
    end
    return chooseRandom(pool)
end

local function getPerkTotalXpForLevel(perk, level)
    if not perk or not perk.getTotalXpForLevel then
        return nil
    end
    local ok, value = pcall(function()
        return perk:getTotalXpForLevel(level)
    end)
    if ok then
        return tonumber(value)
    end
    return nil
end

local function buildSkillDowngradePool(player)
    local pool = {}
    if not player or not player.getPerkLevel then
        return pool
    end
    local xpObj = player.getXp and player:getXp() or nil
    local allowed = (BurdJournals.getAllowedSkills and BurdJournals.getAllowedSkills()) or {}
    for _, skillName in ipairs(allowed) do
        local perk = BurdJournals.getPerkByName and BurdJournals.getPerkByName(skillName)
        if perk then
            local level = tonumber(player:getPerkLevel(perk)) or 0
            if level > 0 then
                local currentXP = xpObj and xpObj.getXP and tonumber(xpObj:getXP(perk)) or nil
                local levelStartXP = getPerkTotalXpForLevel(perk, level)
                local prevLevelStartXP = getPerkTotalXpForLevel(perk, math.max(0, level - 1))
                if currentXP ~= nil and levelStartXP ~= nil and prevLevelStartXP ~= nil then
                    pool[#pool + 1] = {
                        skillName = skillName,
                        perk = perk,
                        level = level,
                        currentXP = currentXP,
                        levelStartXP = levelStartXP,
                        prevLevelStartXP = prevLevelStartXP,
                    }
                end
            end
        end
    end
    return pool
end

local function pickForcedSkillEntry(pool, forcedSkillName)
    local forced = normalizeLowerText(forcedSkillName)
    if not forced then
        return nil
    end
    for _, entry in ipairs(pool) do
        if normalizeLowerText(entry.skillName) == forced then
            return entry
        end
    end
    return nil
end

local function computeDowngradedTargetXP(entry)
    local currentXP = tonumber(entry and entry.currentXP) or 0
    local levelStartXP = tonumber(entry and entry.levelStartXP) or 0
    local prevLevelStartXP = tonumber(entry and entry.prevLevelStartXP) or 0
    local currentLevel = tonumber(entry and entry.level) or 0
    local skillName = entry and entry.skillName
    local perk = entry and entry.perk

    if BurdJournals.computeLevelShiftTargetXP then
        local shiftedXP, shiftedLevel = BurdJournals.computeLevelShiftTargetXP(perk, skillName, currentXP, currentLevel, -1, {
            preserveProgressRatio = true,
        })
        shiftedXP = tonumber(shiftedXP)
        shiftedLevel = tonumber(shiftedLevel)
        if shiftedXP and shiftedLevel and shiftedLevel < currentLevel then
            local maxAllowed = levelStartXP - 1
            if maxAllowed < 0 then
                maxAllowed = 0
            end
            if shiftedXP > maxAllowed then
                shiftedXP = maxAllowed
            end
            if shiftedXP < 0 then
                shiftedXP = 0
            end
            if shiftedXP >= currentXP and currentXP > 0 then
                shiftedXP = math.max(0, currentXP - 1)
            end
            return shiftedXP
        end
    end

    if prevLevelStartXP > levelStartXP then
        prevLevelStartXP = levelStartXP
    end

    -- Fallback for older shared builds: drop to previous level start.
    local targetXP = prevLevelStartXP
    local maxAllowed = levelStartXP - 1
    if maxAllowed < 0 then
        maxAllowed = 0
    end
    if targetXP > maxAllowed then
        targetXP = maxAllowed
    end
    if targetXP < 0 then
        targetXP = 0
    end
    if targetXP >= currentXP and currentXP > 0 then
        targetXP = math.max(0, currentXP - 1)
    end
    return targetXP
end

local function applySkillXpDelta(xpObj, perk, delta)
    if not xpObj or not perk or not xpObj.AddXP or delta == 0 then
        return false
    end
    local ok = pcall(function()
        xpObj:AddXP(perk, delta)
    end)
    return ok
end

local function resolveTraitTypeId(defType)
    if not defType then
        return nil
    end

    if type(traitTypeToName) == "function" then
        local ok, resolved = pcall(function()
            return traitTypeToName(defType)
        end)
        if ok and type(resolved) == "string" and resolved ~= "" then
            return string.gsub(resolved, "^base:", "")
        end
    end

    if defType.getName then
        local ok, name = pcall(function()
            return defType:getName()
        end)
        if ok and type(name) == "string" and name ~= "" then
            return string.gsub(name, "^base:", "")
        end
    end

    local fallback = tostring(defType)
    if type(fallback) == "string" and fallback ~= "" then
        return string.gsub(fallback, "^base:", "")
    end
    return nil
end

local function getTraitDefinitionCost(traitId)
    if type(traitId) ~= "string" or traitId == "" then
        return nil
    end

    local normalized = BurdJournals.normalizeTraitId and BurdJournals.normalizeTraitId(traitId) or traitId
    normalized = string.gsub(tostring(normalized), "^base:", "")
    local targetLower = string.lower(normalized)

    if CharacterTraitDefinition and CharacterTraitDefinition.getTraits then
        local allTraits = CharacterTraitDefinition.getTraits()
        if allTraits and allTraits.size and allTraits.get then
            for i = 0, allTraits:size() - 1 do
                local def = allTraits:get(i)
                if def and def.getCost then
                    local defType = def.getType and def:getType() or nil
                    local defId = resolveTraitTypeId(defType)
                    local defLabel = def.getLabel and def:getLabel() or nil
                    local defIdLower = defId and string.lower(defId) or nil
                    local defLabelLower = defLabel and string.lower(tostring(BurdJournals.normalizeTraitId and BurdJournals.normalizeTraitId(defLabel) or defLabel)) or nil
                    if defIdLower == targetLower or defLabelLower == targetLower then
                        return tonumber(def:getCost()) or 0
                    end
                end
            end
        end
    end

    if TraitFactory and TraitFactory.getTrait then
        local traitDef = TraitFactory.getTrait(normalized) or TraitFactory.getTrait(traitId)
        if not traitDef and TraitFactory.getTraits then
            local allTraits = TraitFactory.getTraits()
            if allTraits and allTraits.size and allTraits.get then
                for i = 0, allTraits:size() - 1 do
                    local def = allTraits:get(i)
                    local defId = def and def.getType and def:getType() or nil
                    local defLabel = def and def.getLabel and def:getLabel() or nil
                    local defIdLower = defId and string.lower(tostring(BurdJournals.normalizeTraitId and BurdJournals.normalizeTraitId(defId) or defId)) or nil
                    local defLabelLower = defLabel and string.lower(tostring(BurdJournals.normalizeTraitId and BurdJournals.normalizeTraitId(defLabel) or defLabel)) or nil
                    if defIdLower == targetLower or defLabelLower == targetLower then
                        traitDef = def
                        break
                    end
                end
            end
        end

        if traitDef then
            if traitDef.getCost then
                return tonumber(traitDef:getCost()) or 0
            end
            if traitDef.cost ~= nil then
                return tonumber(traitDef.cost) or 0
            end
        end
    end

    return nil
end

local function collectCurrentTraitIds(player, includePassiveSkillTraits)
    local out = {}
    if not player or not BurdJournals.collectPlayerTraits then
        return out
    end

    local seen = {}
    local traits = BurdJournals.collectPlayerTraits(player, false) or {}
    for key, value in pairs(traits) do
        local traitId = nil
        if type(key) == "string" then
            traitId = key
        elseif type(value) == "string" then
            traitId = value
        elseif type(value) == "table" then
            traitId = value.id or value.type or value.name
        end

        if type(traitId) == "string" and traitId ~= "" then
            local normalized = BurdJournals.normalizeTraitId and BurdJournals.normalizeTraitId(traitId) or traitId
            normalized = string.gsub(tostring(normalized), "^base:", "")
            local keyLower = string.lower(normalized)
            local isPassive = BurdJournals.isPassiveSkillTrait and BurdJournals.isPassiveSkillTrait(normalized) or false
            if not seen[keyLower] and (includePassiveSkillTraits or not isPassive) then
                seen[keyLower] = true
                out[#out + 1] = normalized
            end
        end
    end

    table.sort(out)
    return out
end

local function isPositiveTraitDefinition(traitId)
    local cost = getTraitDefinitionCost(traitId)
    return tonumber(cost) ~= nil and tonumber(cost) > 0
end

function BurdJournals.Server.getDebugBulkTraitActionSpec(action)
    local specs = {
        addalltraits = {
            isAdd = true,
            resultLabel = "traits",
        },
        addallpositivetraits = {
            isAdd = true,
            resultLabel = "positive traits",
        },
        addallnegativetraits = {
            isAdd = true,
            resultLabel = "negative traits",
        },
        removealltraits = {
            isAdd = false,
            resultLabel = "traits",
        },
        removeallpositivetraits = {
            isAdd = false,
            resultLabel = "positive traits",
        },
        removeallnegativetraits = {
            isAdd = false,
            resultLabel = "negative traits",
        },
    }

    return specs[string.lower(tostring(action or ""))]
end

function BurdJournals.Server.getDebugBulkTraitBucket(traitId)
    local traitData = BurdJournals.getTraitMetadata and BurdJournals.getTraitMetadata(traitId) or nil
    if traitData and traitData.isPositive ~= nil then
        return traitData.isPositive and "positive" or "negative"
    end

    local cost = tonumber(getTraitDefinitionCost(traitId))
    local polarity = BurdJournals.determineTraitPolarity and BurdJournals.determineTraitPolarity(traitId, cost) or nil
    if polarity == true then
        return "positive"
    end
    if polarity == false then
        return "negative"
    end
    return "neutral"
end

function BurdJournals.Server.isDebugBulkPositiveTrait(traitId)
    return BurdJournals.Server.getDebugBulkTraitBucket(traitId) == "positive"
end

function BurdJournals.Server.matchesDebugBulkTraitAction(action, traitId)
    local spec = BurdJournals.Server.getDebugBulkTraitActionSpec(action)
    if not spec then
        return false
    end

    if spec.resultLabel == "traits" then
        return true
    end

    local bucket = BurdJournals.Server.getDebugBulkTraitBucket(traitId)
    if spec.resultLabel == "positive traits" then
        return bucket == "positive"
    end
    if spec.resultLabel == "negative traits" then
        return bucket == "negative"
    end

    return false
end

function BurdJournals.Server.collectAvailableTraitIdsForDebugBulkAction(action)
    local out = {}
    local seen = {}
    local seenDisplay = {}
    local availableTraits = BurdJournals.discoverGrantableTraits and BurdJournals.discoverGrantableTraits(true) or {}
    if type(availableTraits) ~= "table" then
        availableTraits = {}
    end

    for _, rawTraitId in ipairs(availableTraits) do
        local traitId = BurdJournals.normalizeTraitId and BurdJournals.normalizeTraitId(rawTraitId) or rawTraitId
        traitId = string.gsub(tostring(traitId or ""), "^base:", "")
        if traitId ~= ""
            and not (BurdJournals.isPassiveSkillTrait and BurdJournals.isPassiveSkillTrait(traitId))
            and BurdJournals.Server.matchesDebugBulkTraitAction(action, traitId) then
            local key = string.lower(traitId)
            local displayName = BurdJournals.getTraitDisplayName and BurdJournals.getTraitDisplayName(traitId) or traitId
            local displayKey = string.lower(tostring(displayName or traitId)):gsub("%s+", ""):gsub("_", "")
            if not seen[key] and not seenDisplay[displayKey] then
                seen[key] = true
                seenDisplay[displayKey] = true
                if BurdJournals.getTraitAliases then
                    for _, alias in ipairs(BurdJournals.getTraitAliases(traitId)) do
                        local aliasNorm = BurdJournals.normalizeTraitId and BurdJournals.normalizeTraitId(alias) or alias
                        seen[string.lower(tostring(aliasNorm or ""))] = true
                    end
                end
                out[#out + 1] = traitId
            end
        end
    end

    table.sort(out)
    return out
end

function BurdJournals.Server.collectOwnedTraitIdsForDebugBulkAction(targetPlayer, action)
    local out = {}
    local seen = {}

    local function queueTraitId(rawTraitId)
        local traitId = BurdJournals.normalizeTraitId and BurdJournals.normalizeTraitId(rawTraitId) or rawTraitId
        traitId = string.gsub(tostring(traitId or ""), "^base:", "")
        if traitId ~= ""
            and not (BurdJournals.isPassiveSkillTrait and BurdJournals.isPassiveSkillTrait(traitId))
            and BurdJournals.Server.matchesDebugBulkTraitAction(action, traitId) then
            local key = string.lower(traitId)
            if not seen[key] then
                seen[key] = true
                out[#out + 1] = traitId
            end
        end
    end

    local function extractTraitId(candidate)
        if candidate == nil then
            return nil
        end
        if type(candidate) == "string" then
            return candidate
        end
        if candidate.getResourceLocation then
            local location = candidate:getResourceLocation()
            if location ~= nil and tostring(location) ~= "" then
                return tostring(location)
            end
        end
        if candidate.getName then
            local name = candidate:getName()
            if name ~= nil and tostring(name) ~= "" then
                return tostring(name)
            end
        end
        return nil
    end

    local function queueTraitLike(traitLike)
        local traitId = extractTraitId(traitLike)
        if not traitId and traitLike and traitLike.getType then
            traitId = extractTraitId(traitLike:getType())
        end
        if not traitId and traitLike and traitLike.getTrait then
            traitId = extractTraitId(traitLike:getTrait())
        end
        if traitId then
            queueTraitId(traitId)
        end
    end

    for _, traitId in ipairs(collectCurrentTraitIds(targetPlayer, false)) do
        queueTraitId(traitId)
    end

    if targetPlayer then
        if targetPlayer.getTraits then
            local traits = targetPlayer:getTraits()
            if traits and traits.size and traits.get then
                for i = 0, traits:size() - 1 do
                    queueTraitLike(traits:get(i))
                end
            end
        end

        if targetPlayer.getCharacterTraits then
            local charTraits = targetPlayer:getCharacterTraits()
            if charTraits and charTraits.size and charTraits.get then
                for i = 0, charTraits:size() - 1 do
                    queueTraitLike(charTraits:get(i))
                end
            end
        end

        if CharacterTraitDefinition and CharacterTraitDefinition.getTraits and targetPlayer.hasTrait then
            local allDefs = CharacterTraitDefinition.getTraits()
            if allDefs then
                for i = 0, allDefs:size() - 1 do
                    local def = allDefs:get(i)
                    if def then
                        local probeTrait = (def.getType and def:getType())
                            or (def.getTrait and def:getTrait())
                            or nil
                        if probeTrait and targetPlayer:hasTrait(probeTrait) then
                            queueTraitLike(probeTrait)
                        end
                    end
                end
            end
        end
    end

    table.sort(out)
    return out
end

local function removeDebugTraitCompletelyAuthoritatively(targetPlayer, traitId)
    if not targetPlayer or not traitId then
        return 0, false
    end

    local removedCount = 0
    local remainingPasses = 32
    local traitIdsToTry = BurdJournals.Server.buildTraitIdsToTry
        and BurdJournals.Server.buildTraitIdsToTry(traitId)
        or { traitId }

    while remainingPasses > 0 do
        local activeTraitId = nil
        for _, candidateTraitId in ipairs(traitIdsToTry) do
            if BurdJournals.playerHasTrait and BurdJournals.playerHasTrait(targetPlayer, candidateTraitId) == true then
                activeTraitId = candidateTraitId
                break
            end
        end
        local hasTrait = activeTraitId ~= nil
        if not hasTrait then
            break
        end

        local okRemove, removed = pcall(function()
            return removeTraitAuthoritatively(targetPlayer, activeTraitId, { skipSyncXp = true })
        end)
        if not okRemove then
            return removedCount, false, tostring(removed)
        end
        if not removed then
            break
        end

        removedCount = removedCount + 1
        remainingPasses = remainingPasses - 1
    end

    local stillHas = false
    if BurdJournals.playerHasTrait then
        for _, candidateTraitId in ipairs(traitIdsToTry) do
            if BurdJournals.playerHasTrait(targetPlayer, candidateTraitId) == true then
                stillHas = true
                break
            end
        end
    end
    return removedCount, not stillHas, stillHas and "trait still present after authoritative bulk removal" or nil
end

function BurdJournals.Server.formatDebugBulkTraitActionMessage(action, count, skippedCount, failedCount)
    local spec = BurdJournals.Server.getDebugBulkTraitActionSpec(action)
    if not spec then
        return "Traits updated."
    end

    count = tonumber(count) or 0
    skippedCount = tonumber(skippedCount) or 0
    failedCount = tonumber(failedCount) or 0

    local parts = {}
    local verb = spec.isAdd and "Added " or "Removed "
    if count > 0 or skippedCount > 0 or failedCount > 0 then
        parts[#parts + 1] = verb .. tostring(count) .. " " .. spec.resultLabel
    else
        parts[#parts + 1] = "No matching " .. spec.resultLabel .. " found."
    end
    if skippedCount > 0 then
        parts[#parts + 1] = tostring(skippedCount) .. " already present"
    end
    if failedCount > 0 then
        parts[#parts + 1] = tostring(failedCount) .. " failed"
    end

    return table.concat(parts, " | ")
end

local function setPlayerPanicHard(player, targetPanic)
    local networkClient = isClient and isClient() and isServer and not isServer()
    if networkClient then
        return false
    end
    if not player or not player.getStats then
        return false
    end
    local stats = player:getStats()
    if not stats then
        return false
    end

    local function readPanic()
        if stats.getPanic then
            local ok, value = pcall(stats.getPanic, stats)
            if ok and tonumber(value) ~= nil then return tonumber(value) end
        end
        if CharacterStat and CharacterStat.PANIC and stats.get then
            local ok, value = pcall(stats.get, stats, CharacterStat.PANIC)
            if ok and tonumber(value) ~= nil then return tonumber(value) end
        end
        return nil
    end

    local currentPanic = readPanic() or 0
    local target = tonumber(targetPanic) or 100
    target = math.min(100, math.max(0, target))
    target = math.min(100, math.max(target, currentPanic + 60))

    local applied = false
    if stats.setPanic then
        -- Build compatibility boundary: verify the required mutation instead of
        -- treating a non-throwing optional API as success.
        local ok = pcall(stats.setPanic, stats, target)
        local actual = ok and readPanic() or nil
        applied = actual ~= nil and actual >= (target - 0.01)
    end

    if not applied and CharacterStat and CharacterStat.PANIC and stats.set then
        local ok = pcall(stats.set, stats, CharacterStat.PANIC, target)
        local actual = ok and readPanic() or nil
        applied = actual ~= nil and actual >= (target - 0.01)
    end

    if CharacterStat and CharacterStat.STRESS and stats.add then
        pcall(stats.add, stats, CharacterStat.STRESS, 0.25)
    elseif stats.setStress then
        local currentStress = 0
        if stats.getStress then
            local ok, value = pcall(stats.getStress, stats)
            if ok then currentStress = tonumber(value) or 0 end
        end
        pcall(stats.setStress, stats, math.max(currentStress, 0.25))
    end

    return applied
end

local function getCursedBarbedHandTarget()
    local function resolveEnumValueByKey(enumTable, key)
        if not enumTable or type(key) ~= "string" or key == "" then
            return nil
        end
        if enumTable[key] ~= nil then
            return enumTable[key]
        end
        for enumKey, enumValue in pairs(enumTable) do
            if tostring(enumKey) == key or tostring(enumValue) == key then
                return enumValue
            end
            if enumValue and enumValue.toString then
                local ok, enumText = pcall(function()
                    return tostring(enumValue:toString())
                end)
                if ok and enumText == key then
                    return enumValue
                end
            end
        end
        return nil
    end

    local targets = {
        {
            key = "Hand_L",
            bodyPartType = resolveEnumValueByKey(BodyPartType, "Hand_L"),
            bloodType = resolveEnumValueByKey(BloodBodyPartType, "Hand_L"),
            label = getText("IGUI_health_Left_Hand") or "Left Hand",
        },
        {
            key = "Hand_R",
            bodyPartType = resolveEnumValueByKey(BodyPartType, "Hand_R"),
            bloodType = resolveEnumValueByKey(BloodBodyPartType, "Hand_R"),
            label = getText("IGUI_health_Right_Hand") or "Right Hand",
        },
    }
    return chooseRandom(targets)
end

local function applyCursedBarbedHandLaceration(player)
    if not player or not player.getBodyDamage then
        return nil
    end

    local bodyDamage = player:getBodyDamage()
    local target = getCursedBarbedHandTarget()
    if not bodyDamage or not target or not target.bodyPartType then
        return nil
    end

    local bodyPart = bodyDamage.getBodyPart and bodyDamage:getBodyPart(target.bodyPartType) or nil
    if not bodyPart then
        return nil
    end

    local woundApplied = false
    if bodyPart.setCut then
        bodyPart:setCut(true)
        woundApplied = not bodyPart.isCut or bodyPart:isCut() == true
    elseif bodyPart.setScratched then
        bodyPart:setScratched(true, true)
        woundApplied = not bodyPart.isScratched or bodyPart:isScratched() == true
    else
        return nil
    end
    if not woundApplied then return nil end

    if bodyPart.getBleedingTime and bodyPart.setBleedingTime then
        local bleed = tonumber(bodyPart:getBleedingTime()) or 0
        bodyPart:setBleedingTime(math.max(bleed, 8))
    end
    if bodyPart.getAdditionalPain and bodyPart.setAdditionalPain then
        local pain = tonumber(bodyPart:getAdditionalPain()) or 0
        bodyPart:setAdditionalPain(math.max(pain, 12))
    end

    if syncBodyPart then
        -- Build-specific replication helper; body damage is also transmitted
        -- below, so a signature mismatch here has a deterministic fallback.
        pcall(syncBodyPart, bodyPart, 0xFFFFFFFFFFF)
    end
    if player.transmitBodyDamage then
        player:transmitBodyDamage()
    end

    if target.bloodType and player.addBlood then
        player:addBlood(target.bloodType, true, true, false)
    end

    local label = tostring(target.label or "Hand")
    if label == "" then
        label = "Hand"
    end
    return {
        label = label,
        bodyPart = target.key,
    }
end

local function curseBarbedSeal(player)
    local handData = applyCursedBarbedHandLaceration(player)
    if not handData then
        return nil
    end

    return {
        type = "barbed_seal",
        message = BurdJournals.formatText(
            getCursedServerText("UI_BurdJournals_CursedMsgBarbedSeal", "Barbed wire bites your %s as you tear the seal free."),
            handData.label
        ),
        focusText = handData.label,
        focusType = "body_part",
        compatEffect = {
            bodyPart = handData.bodyPart,
        },
    }
end

local function setCharacterStatValue(stats, statEnum, value)
    if not stats or not statEnum then
        return false
    end
    if stats.set and stats.get then
        -- CharacterStat differs across supported builds, so keep this narrow
        -- compatibility boundary but verify the postcondition before success.
        local ok = pcall(stats.set, stats, statEnum, value)
        if not ok then return false end
        local okRead, actual = pcall(stats.get, stats, statEnum)
        return okRead and tonumber(actual) ~= nil
            and math.abs(tonumber(actual) - (tonumber(value) or 0)) <= 0.01
    end
    return false
end

local function getCharacterStatValue(stats, statEnum)
    if not stats or not statEnum or not stats.get then
        return nil
    end
    local ok, value = pcall(stats.get, stats, statEnum)
    if not ok then
        return nil
    end
    return tonumber(value)
end

local function getCharacterStatBounds(statEnum)
    if not statEnum then
        return nil, nil
    end
    local minValue = nil
    local maxValue = nil
    if statEnum.getMinimumValue then
        local ok, minResult = pcall(function()
            return statEnum:getMinimumValue()
        end)
        if ok then
            minValue = tonumber(minResult)
        end
    end
    if statEnum.getMaximumValue then
        local ok, maxResult = pcall(function()
            return statEnum:getMaximumValue()
        end)
        if ok then
            maxValue = tonumber(maxResult)
        end
    end
    return minValue, maxValue
end

local function clampCharacterStatValue(statEnum, value)
    value = tonumber(value)
    if value == nil then
        return nil
    end
    local minValue, maxValue = getCharacterStatBounds(statEnum)
    if minValue ~= nil then
        value = math.max(minValue, value)
    end
    if maxValue ~= nil then
        value = math.min(maxValue, value)
    end
    return value
end

local function addCharacterStatValue(stats, statEnum, amount)
    if not stats or not statEnum then
        return false
    end
    local before = getCharacterStatValue(stats, statEnum)
    if before == nil or not stats.add then return false end
    local expected = clampCharacterStatValue(statEnum, before + (tonumber(amount) or 0))
    local ok = pcall(stats.add, stats, statEnum, amount)
    local actual = ok and getCharacterStatValue(stats, statEnum) or nil
    if actual ~= nil and expected ~= nil and math.abs(actual - expected) <= 0.01 then return true end
    return false
end

function BurdJournals.Server.setLegacyCharacterStatValue(stats, getterName, setterName, value)
    local getter = stats and stats[getterName] or nil
    local setter = stats and stats[setterName] or nil
    if not getter or not setter then return false end
    local ok = pcall(setter, stats, value)
    if not ok then return false end
    local okRead, actual = pcall(getter, stats)
    return okRead and tonumber(actual) ~= nil
        and math.abs(tonumber(actual) - (tonumber(value) or 0)) <= 0.01
end

local function curseJammedBreath(player)
    if not player or not player.getStats then
        return nil
    end

    local stats = player:getStats()
    if not stats then
        return nil
    end

    local enduranceNow = (stats.get and CharacterStat and CharacterStat.ENDURANCE and tonumber(stats:get(CharacterStat.ENDURANCE)))
        or (stats.getEndurance and tonumber(stats:getEndurance()))
        or 1
    local enduranceDrop = (30 + ZombRand(11)) / 100 -- 0.30-0.40
    local enduranceAfter = math.max(0, enduranceNow - enduranceDrop)

    local enduranceApplied = false
    if enduranceAfter < enduranceNow then
        enduranceApplied = CharacterStat and CharacterStat.ENDURANCE
            and setCharacterStatValue(stats, CharacterStat.ENDURANCE, enduranceAfter)
            or false
        if not enduranceApplied then
            enduranceApplied = BurdJournals.Server.setLegacyCharacterStatValue(stats, "getEndurance", "setEndurance", enduranceAfter)
        end
    end

    local panicAdd = 18 + ZombRand(13) -- 18-30
    local panicNow = (stats.get and CharacterStat and CharacterStat.PANIC and tonumber(stats:get(CharacterStat.PANIC)))
        or (stats.getPanic and tonumber(stats:getPanic()))
        or 0
    local panicAfter = math.min(100, panicNow + panicAdd)
    local panicApplied = false
    if panicAfter > panicNow then
        panicApplied = CharacterStat and CharacterStat.PANIC
            and addCharacterStatValue(stats, CharacterStat.PANIC, panicAdd)
            or false
        if not panicApplied then
            panicApplied = BurdJournals.Server.setLegacyCharacterStatValue(stats, "getPanic", "setPanic", panicAfter)
        end
    end

    local stressAdd = (10 + ZombRand(11)) / 100 -- 0.10-0.20
    local stressNow = (stats.get and CharacterStat and CharacterStat.STRESS and tonumber(stats:get(CharacterStat.STRESS)))
        or (stats.getStress and tonumber(stats:getStress()))
        or 0
    local stressAfter = math.min(1, stressNow + stressAdd)
    local stressApplied = false
    if stressAfter > stressNow then
        stressApplied = CharacterStat and CharacterStat.STRESS
            and addCharacterStatValue(stats, CharacterStat.STRESS, stressAdd)
            or false
        if not stressApplied then
            stressApplied = BurdJournals.Server.setLegacyCharacterStatValue(stats, "getStress", "setStress", stressAfter)
        end
    end

    if not enduranceApplied and not panicApplied and not stressApplied then
        return nil
    end

    local pct = math.max(1, math.floor((enduranceDrop * 100) + 0.5))
    return {
        type = "jammed_breath",
        message = getCursedServerText(
            "UI_BurdJournals_CursedMsgJammedBreath",
            "Your lungs seize as if something is gripping your chest."
        ),
        focusText = tostring(pct) .. "%",
        focusType = "endurance_drop",
        compatEffect = {
            endurance = enduranceAfter,
            panic = panicAfter,
            stress = stressAfter,
        },
    }
end

local function isEligibleHexedToolingItem(item)
    if not item or not item.getCondition or not item.getConditionMax or not item.setCondition then
        return false
    end

    local maxCondition = tonumber(item:getConditionMax()) or 0
    local condition = tonumber(item:getCondition()) or 0
    if maxCondition <= 0 or condition <= 1 then
        return false
    end

    if instanceof and instanceof(item, "HandWeapon") then
        return true
    end
    if item.IsWeapon and item:IsWeapon() then
        return true
    end

    local category = item.getCategory and tostring(item:getCategory() or "") or ""
    if category == "Weapon" or category == "Tool" then
        return true
    end

    local equipSlot = item.canBeEquipped and tostring(item:canBeEquipped() or "") or ""
    local equipLower = string.lower(equipSlot)
    if equipLower == "primary" or equipLower == "secondary" or equipLower == "bothhands" or equipLower == "bothhand" then
        return true
    end

    if item.getAttachmentType and item:getAttachmentType() ~= nil then
        return true
    end

    return false
end

local function collectHexedToolingCandidates(player)
    local out = {}
    local seen = {}
    local visitedContainers = {}
    if not player or not player.getInventory then
        return out
    end

    local function addCandidate(item)
        if not isEligibleHexedToolingItem(item) then
            return
        end
        local key = tostring((item.getID and item:getID()) or item)
        if key ~= "" and not seen[key] then
            seen[key] = true
            out[#out + 1] = item
        end
    end

    local function scanContainer(container)
        if not container or visitedContainers[container] then
            return
        end
        visitedContainers[container] = true

        local items = container.getItems and container:getItems() or nil
        if not items or not items.size or not items.get then
            return
        end

        for i = 0, items:size() - 1 do
            local item = items:get(i)
            addCandidate(item)

            local nested = item and item.getInventory and item:getInventory() or nil
            if not nested and item and item.getItemContainer then
                nested = item:getItemContainer()
            end
            if nested and nested ~= container then
                scanContainer(nested)
            end
        end
    end

    addCandidate(player.getPrimaryHandItem and player:getPrimaryHandItem() or nil)
    addCandidate(player.getSecondaryHandItem and player:getSecondaryHandItem() or nil)

    local wornItems = player.getWornItems and player:getWornItems() or nil
    if wornItems and wornItems.size and wornItems.get then
        for i = 0, wornItems:size() - 1 do
            local wornEntry = wornItems:get(i)
            local item = wornEntry and wornEntry.getItem and wornEntry:getItem() or nil
            addCandidate(item)
        end
    end

    scanContainer(player:getInventory())

    return out
end

local function getInventoryItemLabel(item)
    if not item then
        return "gear"
    end
    local label = item.getDisplayName and tostring(item:getDisplayName() or "") or ""
    if label ~= "" then
        return label
    end
    local fallback = item.getName and tostring(item:getName() or "") or ""
    if fallback ~= "" then
        return fallback
    end
    return "gear"
end

local function curseHexedTooling(player)
    local candidates = collectHexedToolingCandidates(player)
    local chosen = chooseRandom(candidates)
    if not chosen then
        return nil
    end

    local condition = tonumber(chosen:getCondition()) or 0
    local maxCondition = tonumber(chosen:getConditionMax()) or 0
    if condition <= 1 or maxCondition <= 0 then
        return nil
    end

    local minLoss = math.max(1, math.floor(maxCondition * 0.15))
    local maxLoss = math.max(minLoss, math.floor(maxCondition * 0.35))
    local loss = minLoss
    if maxLoss > minLoss then
        loss = ZombRand(minLoss, maxLoss + 1)
    end
    loss = math.min(loss, condition - 1)
    if loss <= 0 then
        return nil
    end

    local newCondition = math.max(1, condition - loss)
    chosen:setCondition(newCondition)
    local appliedCondition = tonumber(chosen:getCondition())
    if appliedCondition ~= newCondition then
        return nil
    end
    local chosenContainer = chosen.getContainer and chosen:getContainer() or (player and player.getInventory and player:getInventory()) or nil
    if chosenContainer and chosenContainer.setDrawDirty then
        pcall(function()
            chosenContainer:setDrawDirty(true)
        end)
    end
    if chosenContainer and chosenContainer.sync then
        pcall(function()
            chosenContainer:sync()
        end)
    end
    if player and player.syncInventory then
        pcall(function()
            player:syncInventory()
        end)
    end
    if player and player.resetEquippedHandsModels then
        pcall(function()
            player:resetEquippedHandsModels()
        end)
    end
    if player and player.resetModelNextFrame then
        pcall(function()
            player:resetModelNextFrame()
        end)
    end

    local itemLabel = getInventoryItemLabel(chosen)
    return {
        type = "hexed_tooling",
        message = BurdJournals.formatText(
            getCursedServerText(
                "UI_BurdJournals_CursedMsgHexedTooling",
                "Your %s dulls and cracks under a sudden malignant strain."
            ),
            itemLabel
        ),
        focusText = itemLabel,
        focusType = "item",
        compatEffect = {
            itemId = chosen.getID and chosen:getID() or nil,
            newCondition = newCondition,
            displayName = itemLabel,
            itemType = chosen.getFullType and chosen:getFullType() or nil,
        },
    }
end

local refreshPlayerClothingCompat

local function curseTornGear(player)
    if not player or (not player.addHole and not player.addHoleFromZombieAttacks) then
        return nil
    end

    local function collectTargets()
        local clothing = {}
        local parts = {}
        local seenParts = {}
        if not player or not player.getWornItems then
            return clothing, parts
        end

        local wornItems = player:getWornItems()
        if not wornItems or not wornItems.size or not wornItems.get then
            return clothing, parts
        end

        for i = 0, wornItems:size() - 1 do
            local wornEntry = wornItems:get(i)
            local item = wornEntry and wornEntry.getItem and wornEntry:getItem() or nil
            if item and item.getCoveredParts and item.getHolesNumber then
                local covered = item:getCoveredParts()
                if covered and covered.size and covered.get and covered:size() > 0 then
                    clothing[#clothing + 1] = item
                    for j = 0, covered:size() - 1 do
                        local part = covered:get(j)
                        local key = part and part.toString and tostring(part:toString()) or tostring(part)
                        if part and key and key ~= "" and not seenParts[key] then
                            seenParts[key] = true
                            parts[#parts + 1] = part
                        end
                    end
                end
            end
        end

        return clothing, parts
    end
    local function countClothingHoles(clothingItems)
        local total = 0
        if type(clothingItems) ~= "table" then
            return total
        end
        for _, item in ipairs(clothingItems) do
            if item and item.getHolesNumber then
                total = total + (tonumber(item:getHolesNumber()) or 0)
            end
        end
        return total
    end

    local clothing, parts = collectTargets()
    if #clothing == 0 or #parts == 0 then
        return nil
    end

    local targetTears = ZombRand(3, 6) -- 3-5
    local currentHoles = countClothingHoles(clothing)
    local applied = 0
    local tornParts = {}
    local attempts = 0
    local maxAttempts = math.max(12, targetTears * 8)
    local function resolveEnumValueByKey(enumTable, key)
        if not enumTable or type(key) ~= "string" or key == "" then
            return nil
        end
        if enumTable[key] ~= nil then
            return enumTable[key]
        end
        for enumKey, enumValue in pairs(enumTable) do
            if tostring(enumKey) == key or tostring(enumValue) == key then
                return enumValue
            end
            if enumValue and enumValue.toString then
                local ok, enumText = pcall(function()
                    return tostring(enumValue:toString())
                end)
                if ok and enumText == key then
                    return enumValue
                end
            end
        end
        return nil
    end
    local function applyClothingHoleCompat(part)
        if part == nil then
            return false, nil
        end

        local partKey = part and part.toString and tostring(part:toString()) or tostring(part)
        local targets = {}
        local seen = {}
        local function addTarget(target)
            if target == nil then
                return
            end
            local targetKey = tostring(target)
            if target.toString then
                local ok, resolvedText = pcall(function()
                    return tostring(target:toString())
                end)
                if ok and resolvedText and resolvedText ~= "" then
                    targetKey = resolvedText
                end
            end
            if targetKey ~= "" and not seen[targetKey] then
                seen[targetKey] = true
                targets[#targets + 1] = target
            end
        end

        addTarget(part)
        if partKey and partKey ~= "" then
            addTarget(resolveEnumValueByKey(BloodBodyPartType, partKey))
            addTarget(resolveEnumValueByKey(BodyPartType, partKey))
        end

        for _, target in ipairs(targets) do
            local appliedByApi = false
            if player.addHole then
                local ok, result = pcall(function()
                    return player:addHole(target)
                end)
                appliedByApi = ok and result ~= false
            end
            if not appliedByApi and player.addHoleFromZombieAttacks then
                local ok, result = pcall(function()
                    return player:addHoleFromZombieAttacks(target, true)
                end)
                appliedByApi = ok and result ~= false
            end
            if appliedByApi then
                return true, partKey
            end
        end

        return false, partKey
    end

    while applied < targetTears and attempts < maxAttempts do
        attempts = attempts + 1
        local part = chooseRandom(parts)
        if part then
            local appliedByApi, partKey = applyClothingHoleCompat(part)
            local after = countClothingHoles(clothing)
            if after > currentHoles then
                applied = applied + (after - currentHoles)
                currentHoles = after
                tornParts[#tornParts + 1] = partKey
            elseif appliedByApi and partKey and partKey ~= "" then
                tornParts[#tornParts + 1] = partKey
            end
        end
    end

    if applied <= 0 then
        return nil
    end

    refreshPlayerClothingCompat(player)

    return {
        type = "torn_gear",
        message = BurdJournals.formatText(
            getCursedServerText(
                "UI_BurdJournals_CursedMsgTornGear",
                "Something invisible rakes across your clothes, leaving %d fresh tears."
            ),
            applied
        ),
        focusText = tostring(applied),
        focusType = "tear_count",
        compatEffect = {
            tornParts = tornParts,
            tears = applied,
        },
    }
end

local function isWarmMonthForSeasonalWave(month)
    month = tonumber(month) or 1
    -- 3-8 => Spring/Summer leaning warm profile.
    return month >= 3 and month <= 8
end

local function curseSeasonalWave(player)
    if not player then
        return nil
    end

    local gameTime = getGameTime and getGameTime() or nil
    local month = (gameTime and gameTime.getMonth and (gameTime:getMonth() + 1)) or 1
    local warm = isWarmMonthForSeasonalWave(month)
    local applied = false
    local stats = player.getStats and player:getStats() or nil
    local bodyDamage = player.getBodyDamage and player:getBodyDamage() or nil
    local compatEffect = {
        warm = warm,
        wave = warm and "heat" or "cold",
    }

    -- Primary path: drive thermals through CharacterStat.TEMPERATURE for reliable moodle updates.
    if stats and CharacterStat and CharacterStat.TEMPERATURE then
        local currentTemp = getCharacterStatValue(stats, CharacterStat.TEMPERATURE)
        if currentTemp == nil and player.getTemperature then
            currentTemp = tonumber(player:getTemperature())
        end
        if currentTemp ~= nil then
            local delta = (warm and 4.5 or -4.5) + (ZombRand(0, 26) / 10) -- ~4.5 to 7.0
            local targetTemp = clampCharacterStatValue(
                CharacterStat.TEMPERATURE,
                currentTemp + delta
            )
            if targetTemp ~= nil and setCharacterStatValue(stats, CharacterStat.TEMPERATURE, targetTemp) then
                applied = true
                compatEffect.temperature = targetTemp
                if sendPlayerStat then
                    pcall(function()
                        sendPlayerStat(player, CharacterStat.TEMPERATURE)
                    end)
                end
            end
        end
    end
    if compatEffect.temperature == nil and player.getTemperature and player.setTemperature then
        local currentTemp = tonumber(player:getTemperature())
        if currentTemp ~= nil then
            local delta = (warm and 4.5 or -4.5) + (ZombRand(0, 26) / 10)
            local targetTemp = currentTemp + delta
            player:setTemperature(targetTemp)
            local actualTemp = tonumber(player:getTemperature())
            if actualTemp ~= nil and math.abs(actualTemp - targetTemp) <= 0.01 then
                applied = true
                compatEffect.temperature = actualTemp
            end
        end
    elseif compatEffect.temperature ~= nil and player.setTemperature then
        pcall(function()
            player:setTemperature(compatEffect.temperature)
        end)
    end

    if warm then
        -- Secondary warm pressure: wetness increase (0-100 scale in B42 stats UI).
        if stats and CharacterStat and CharacterStat.WETNESS then
            local wetnessNow = getCharacterStatValue(stats, CharacterStat.WETNESS) or 0
            local wetnessAdd = 30 + ZombRand(31) -- +30 to +60
            local wetnessTarget = clampCharacterStatValue(CharacterStat.WETNESS, wetnessNow + wetnessAdd)
            if wetnessTarget ~= nil and setCharacterStatValue(stats, CharacterStat.WETNESS, wetnessTarget) then
                applied = true
                compatEffect.wetness = wetnessTarget
                if sendPlayerStat then
                    pcall(function()
                        sendPlayerStat(player, CharacterStat.WETNESS)
                    end)
                end
            end
        end
        if compatEffect.wetness == nil and bodyDamage and bodyDamage.getWetness and bodyDamage.setWetness then
            local wetnessNow = tonumber(bodyDamage:getWetness()) or 0
            local wetnessTarget = math.max(0, math.min(100, wetnessNow + 30 + ZombRand(31)))
            bodyDamage:setWetness(wetnessTarget)
            local actualWetness = tonumber(bodyDamage:getWetness())
            if actualWetness ~= nil and math.abs(actualWetness - wetnessTarget) <= 0.01 then
                applied = true
                compatEffect.wetness = actualWetness
            end
        elseif compatEffect.wetness ~= nil and bodyDamage and bodyDamage.setWetness then
            pcall(function()
                bodyDamage:setWetness(compatEffect.wetness)
            end)
        end
        if bodyDamage and bodyDamage.getTemperature and bodyDamage.setTemperature then
            local tempNow = tonumber(bodyDamage:getTemperature())
            if tempNow ~= nil then
                local newBodyTemp = tempNow + 3.0 + (ZombRand(0, 19) / 10)
                bodyDamage:setTemperature(newBodyTemp) -- +3.0 to +4.8
                local actualBodyTemp = tonumber(bodyDamage:getTemperature())
                if actualBodyTemp ~= nil and math.abs(actualBodyTemp - newBodyTemp) <= 0.01 then
                    applied = true
                    compatEffect.bodyTemperature = actualBodyTemp
                end
            end
        end
    else
        -- Cold profile: push cold-strength high enough to visibly register.
        if bodyDamage and bodyDamage.getColdStrength and bodyDamage.setColdStrength then
            local coldNow = tonumber(bodyDamage:getColdStrength()) or 0
            local coldTarget = math.max(coldNow, 70 + ZombRand(31)) -- 70-100
            bodyDamage:setColdStrength(coldTarget)
            local actualCold = tonumber(bodyDamage:getColdStrength())
            if actualCold ~= nil and math.abs(actualCold - coldTarget) <= 0.01 then
                applied = true
                compatEffect.coldStrength = actualCold
            end
        end
        if bodyDamage and bodyDamage.getTemperature and bodyDamage.setTemperature then
            local tempNow = tonumber(bodyDamage:getTemperature())
            if tempNow ~= nil then
                local newBodyTemp = tempNow - (3.0 + (ZombRand(0, 19) / 10))
                bodyDamage:setTemperature(newBodyTemp) -- -3.0 to -4.8
                local actualBodyTemp = tonumber(bodyDamage:getTemperature())
                if actualBodyTemp ~= nil and math.abs(actualBodyTemp - newBodyTemp) <= 0.01 then
                    applied = true
                    compatEffect.bodyTemperature = actualBodyTemp
                end
            end
        end
        if bodyDamage and bodyDamage.setCatchACold then
            local catchColdTarget = math.max(45, tonumber(bodyDamage.getCatchACold and bodyDamage:getCatchACold() or 0) or 0)
            bodyDamage:setCatchACold(catchColdTarget)
            local actualCatchCold = tonumber(bodyDamage.getCatchACold and bodyDamage:getCatchACold() or nil)
            if actualCatchCold ~= nil and math.abs(actualCatchCold - catchColdTarget) <= 0.01 then
                applied = true
                compatEffect.catchACold = actualCatchCold
            end
        end
    end

    if applied and player.transmitBodyDamage then
        pcall(function()
            player:transmitBodyDamage()
        end)
    end

    if not applied then
        return nil
    end

    if warm then
        return {
            type = "seasonal_wave",
            message = getCursedServerText(
                "UI_BurdJournals_CursedMsgSeasonalHeat",
                "The air turns hostile in an instant. Heat claws at your skin."
            ),
            focusText = getCursedServerText("UI_BurdJournals_CursedFocusHeatWave", "Heat"),
            focusType = "seasonal_wave",
            compatEffect = compatEffect,
        }
    end

    return {
        type = "seasonal_wave",
        message = getCursedServerText(
            "UI_BurdJournals_CursedMsgSeasonalCold",
            "The air turns hostile in an instant. Cold sinks into your bones."
        ),
        focusText = getCursedServerText("UI_BurdJournals_CursedFocusColdWave", "Cold"),
        focusType = "seasonal_wave",
        compatEffect = compatEffect,
    }
end

CURSED_PANTSED_PREFERRED_LOCATIONS = {
    "Pants",
    "Bottoms",
    "Skirt",
    "UnderwearBottom",
    "Underwear",
    "UnderwearExtra1",
    "UnderwearExtra2",
}

CURSED_PANTSED_LOCATION_KEYS = {
    pants = true,
    bottoms = true,
    skirt = true,
    underwearbottom = true,
    underwear = true,
    underwearextra1 = true,
    underwearextra2 = true,
}

CURSED_PANTSED_COVERED_PART_KEYS = {
    groin = true,
    upperlegl = true,
    upperlegr = true,
    lowerlegl = true,
    lowerlegr = true,
}

local function normalizeWornLocationKey(location)
    if location == nil then
        return nil
    end
    local text = tostring(location)
    if text == "" then
        return nil
    end
    text = string.gsub(text, "[^%w]", "")
    if text == "" then
        return nil
    end
    return string.lower(text)
end

local function isPantsedWornLocation(location)
    local key = normalizeWornLocationKey(location)
    return key ~= nil and CURSED_PANTSED_LOCATION_KEYS[key] == true
end

local function isPantsedCoveredPart(part)
    local key = normalizeWornLocationKey(part)
    return key ~= nil and CURSED_PANTSED_COVERED_PART_KEYS[key] == true
end

local function addUniqueInventoryItem(list, seen, item)
    if not item then
        return false
    end
    local key = tostring((item.getID and item:getID()) or item)
    if key == "" or seen[key] then
        return false
    end
    seen[key] = true
    list[#list + 1] = item
    return true
end

local function collectPantsedTargets(player)
    local out = {}
    local seen = {}
    if not player then
        return out
    end
    local matchedByLocation = {}
    local wornItems = player.getWornItems and player:getWornItems() or nil
    if wornItems and wornItems.size and wornItems.get then
        for i = 0, wornItems:size() - 1 do
            local wornEntry = wornItems:get(i)
            local item = wornEntry and wornEntry.getItem and wornEntry:getItem()
                or (wornItems.getItemByIndex and wornItems:getItemByIndex(i))
                or nil
            local location = wornEntry and wornEntry.getLocation and wornEntry:getLocation()
                or (item and item.getBodyLocation and item:getBodyLocation())
                or nil
            local matchesBottomWear = item and isPantsedWornLocation(location)
            if not matchesBottomWear and item and item.getCoveredParts then
                local coveredParts = item:getCoveredParts()
                if coveredParts and coveredParts.size and coveredParts.get then
                    for j = 0, coveredParts:size() - 1 do
                        if isPantsedCoveredPart(coveredParts:get(j)) then
                            matchesBottomWear = true
                            break
                        end
                    end
                end
            end
            if item and matchesBottomWear then
                local locationKey = normalizeWornLocationKey(location) or "__unknown__"
                if matchedByLocation[locationKey] == nil then
                    matchedByLocation[locationKey] = {}
                end
                matchedByLocation[locationKey][#matchedByLocation[locationKey] + 1] = item
            end
        end
    end

    for _, location in ipairs(CURSED_PANTSED_PREFERRED_LOCATIONS) do
        local locationKey = normalizeWornLocationKey(location)
        local items = locationKey and matchedByLocation[locationKey] or nil
        if items then
            for _, item in ipairs(items) do
                addUniqueInventoryItem(out, seen, item)
            end
            matchedByLocation[locationKey] = nil
        end
    end

    for _, items in pairs(matchedByLocation) do
        for _, item in ipairs(items) do
            addUniqueInventoryItem(out, seen, item)
        end
    end

    return out
end

local getClothingSyncLocationToken
local resolveClothingSyncLocationToken

local function removeWornItemCompat(player, item)
    if not player or not item or not player.removeWornItem then
        if not (player and item) then
            return false
        end
    end

    if player.removeWornItem then
        local ok = pcall(function()
            player:removeWornItem(item, false)
        end)
        if ok then
            return true
        end

        ok = pcall(function()
            player:removeWornItem(item)
        end)
        if ok then
            return true
        end
    end

    local locationToken = resolveClothingSyncLocationToken(player, getClothingSyncLocationToken(nil, item))
    if player.setWornItem and locationToken then
        local ok = pcall(function()
            player:setWornItem(locationToken, nil)
        end)
        if ok then
            return true
        end
    end

    return false
end

getClothingSyncLocationToken = function(wornEntry, item)
    if item and item.canBeEquipped then
        local ok, equippedLocation = pcall(function()
            return item:canBeEquipped()
        end)
        if ok and equippedLocation then
            return equippedLocation
        end
    end

    if wornEntry and wornEntry.getLocation then
        local ok, wornLocation = pcall(function()
            return wornEntry:getLocation()
        end)
        if ok and wornLocation then
            return wornLocation
        end
    end

    if item and item.getBodyLocation then
        local ok, bodyLocation = pcall(function()
            return item:getBodyLocation()
        end)
        if ok and bodyLocation then
            return bodyLocation
        end
    end

    return nil
end

resolveClothingSyncLocationToken = function(player, locationToken)
    if locationToken == nil then
        return nil
    end
    if type(locationToken) ~= "string" then
        return locationToken
    end

    local locationText = tostring(locationToken or "")
    if locationText == "" then
        return nil
    end

    if ItemBodyLocation and ItemBodyLocation[locationText] ~= nil then
        return ItemBodyLocation[locationText]
    end

    local wornItems = player and player.getWornItems and player:getWornItems() or nil
    local bodyLocationGroup = wornItems and wornItems.getBodyLocationGroup and wornItems:getBodyLocationGroup() or nil
    if bodyLocationGroup then
        if bodyLocationGroup.getLocation then
            local ok, resolved = pcall(function()
                return bodyLocationGroup:getLocation(locationText)
            end)
            if ok and resolved then
                return resolved
            end
        end
        if bodyLocationGroup.getOrCreateLocation then
            local ok, resolved = pcall(function()
                return bodyLocationGroup:getOrCreateLocation(locationText)
            end)
            if ok and resolved then
                return resolved
            end
        end
    end

    if ItemBodyLocation and ItemBodyLocation.get and ResourceLocation and ResourceLocation.of then
        local ok, resolved = pcall(function()
            return ItemBodyLocation.get(ResourceLocation.of(locationText))
        end)
        if ok and resolved then
            return resolved
        end
    end

    return nil
end

refreshPlayerClothingCompat = function(player)
    if not player then
        return false
    end

    local sentAny = false
    if sendClothing and player.getWornItems then
        local wornItems = player:getWornItems()
        if wornItems and wornItems.size and wornItems.get then
            for i = 0, wornItems:size() - 1 do
                local wornEntry = wornItems:get(i)
                local item = wornEntry and wornEntry.getItem and wornEntry:getItem()
                    or (wornItems.getItemByIndex and wornItems:getItemByIndex(i))
                    or nil
                local bodyLocation = resolveClothingSyncLocationToken(player, getClothingSyncLocationToken(wornEntry, item))
                if item and bodyLocation then
                    local ok = pcall(function()
                        sendClothing(player, bodyLocation, item)
                    end)
                    sentAny = ok or sentAny
                end
            end
        end
    end

    if player.resetModelNextFrame then
        pcall(function()
            player:resetModelNextFrame()
        end)
    end
    if triggerEvent then
        pcall(function()
            triggerEvent("OnClothingUpdated", player)
        end)
    end

    return sentAny
end

local function cursePantsed(player)
    if not player or not player.removeWornItem then
        return nil
    end

    local targets = collectPantsedTargets(player)
    if #targets == 0 then
        return nil
    end

    local removed = 0
    for _, item in ipairs(targets) do
        if removeWornItemCompat(player, item) then
            removed = removed + 1
        end
    end

    if removed <= 0 then
        return nil
    end

    refreshPlayerClothingCompat(player)

    return {
        type = "pantsed",
        message = getCursedServerText(
            "UI_BurdJournals_CursedMsgPantsed",
            "Caught you with your pants down."
        ),
        focusText = getCursedServerText("UI_BurdJournals_CursedFocusPantsed", "Pantsed"),
        focusType = "pantsed",
    }
end

local function curseGainNegativeTrait(player, forcedTraitId)
    local forced = normalizeForcedTraitId(forcedTraitId)
    local pool = {}
    local seen = {}
    for _, traitId in ipairs((BurdJournals.REMOVABLE_TRAITS) or {}) do
        local normalized = normalizeForcedTraitId(traitId)
        local key = normalizeLowerText(normalized)
        if normalized and key and not seen[key] and canApplyCursedNegativeTrait(player, normalized) then
            pool[#pool + 1] = normalized
            seen[key] = true
        end
    end

    local chosen = selectTraitFromPool(pool, forced)
    if not chosen then
        return nil
    end

    if not canApplyCursedNegativeTrait(player, chosen) then
        return nil
    end

    -- Cursed adds force mutual-exclusion resolution (e.g. Conspicuous removes Inconspicuous).
    local cancelledTraits = removeTraitConflictsForCursedAdd(player, chosen)

    local added = BurdJournals.safeAddTrait(player, chosen)
    if not added then
        return nil
    end
    local traitLabel = tostring(getTraitName(chosen) or chosen or "Unknown")
    if traitLabel == "" then
        traitLabel = tostring(chosen or "Unknown")
    end
    return {
        type = "gain_negative_trait",
        message = BurdJournals.formatText(
            getCursedServerText("UI_BurdJournals_CursedMsgGainNegative", "The curse brands you with: %s"),
            traitLabel
        ),
        focusText = traitLabel,
        focusType = "trait",
        compatEffect = {
            traitId = chosen,
            cancelledTraits = cancelledTraits,
        },
    }
end

local function curseLosePositiveTrait(player, forcedTraitId)
    local forced = normalizeForcedTraitId(forcedTraitId)
    local pool = {}
    local seen = {}
    for _, traitId in ipairs(collectCurrentTraitIds(player, false)) do
        local key = normalizeLowerText(traitId)
        if key and not seen[key] and isPositiveTraitDefinition(traitId) then
            pool[#pool + 1] = traitId
            seen[key] = true
        end
    end
    local forcedCandidate = nil
    if forced and isPositiveTraitDefinition(forced) then
        for _, traitId in ipairs(pool) do
            if normalizeLowerText(traitId) == normalizeLowerText(forced) then
                forcedCandidate = traitId
                break
            end
        end
    end
    local chosen = forcedCandidate or chooseRandom(pool)
    if not chosen or not BurdJournals.playerHasTrait(player, chosen) then
        return nil
    end

    local removed = removeTraitAuthoritatively(player, chosen)
    if not removed then
        return nil
    end
    local traitLabel = tostring(getTraitName(chosen) or chosen or "Unknown")
    if traitLabel == "" then
        traitLabel = tostring(chosen or "Unknown")
    end
    return {
        type = "lose_positive_trait",
        message = BurdJournals.formatText(
            getCursedServerText("UI_BurdJournals_CursedMsgLosePositive", "The curse strips away: %s"),
            traitLabel
        ),
        focusText = traitLabel,
        focusType = "trait",
        compatEffect = {
            traitId = chosen,
        },
    }
end

local function curseLoseSkillLevel(player, _, forcedSkillName)
    local forcedSkill = normalizeForcedSkillName(forcedSkillName)
    local pool = buildSkillDowngradePool(player)
    local chosen = pickForcedSkillEntry(pool, forcedSkill) or chooseRandom(pool)
    if not chosen then
        return nil
    end

    local xpObj = player:getXp()
    local levelBefore = player:getPerkLevel(chosen.perk) or 0
    if levelBefore <= 0 then
        return nil
    end

    chosen.currentXP = xpObj and xpObj.getXP and tonumber(xpObj:getXP(chosen.perk)) or chosen.currentXP
    local targetXP = computeDowngradedTargetXP(chosen)
    local delta = targetXP - (chosen.currentXP or 0)
    if delta == 0 then
        delta = -1
    end

    if not applySkillXpDelta(xpObj, chosen.perk, delta) then
        return nil
    end

    local levelAfter = player:getPerkLevel(chosen.perk) or 0
    if levelAfter >= levelBefore then
        local currentXPAfter = xpObj and xpObj.getXP and tonumber(xpObj:getXP(chosen.perk)) or (chosen.currentXP or 0)
        local hardFloor = tonumber(chosen.prevLevelStartXP) or 0
        local hardDelta = hardFloor - currentXPAfter
        if hardDelta ~= 0 then
            applySkillXpDelta(xpObj, chosen.perk, hardDelta)
            levelAfter = player:getPerkLevel(chosen.perk) or 0
        end
    end

    if levelAfter >= levelBefore then
        return nil
    end

    if SyncXp then
        SyncXp(player)
    end

    local skillLabel = (BurdJournals.getPerkDisplayName and BurdJournals.getPerkDisplayName(chosen.skillName)) or chosen.skillName
    skillLabel = tostring(skillLabel or chosen.skillName or "Unknown")
    if skillLabel == "" then
        skillLabel = tostring(chosen.skillName or "Unknown")
    end
    return {
        type = "lose_skill_level",
        message = BurdJournals.formatText(
            getCursedServerText("UI_BurdJournals_CursedMsgLoseSkill", "Your %s knowledge decays."),
            tostring(skillLabel)
        ),
        focusText = tostring(skillLabel),
        focusType = "skill",
        compatEffect = {
            skillName = chosen.skillName,
        },
    }
end

CURSED_SEAL_WORLD_SOUND_RADIUS = 90
CURSED_SEAL_WORLD_SOUND_VOLUME = 90
CURSED_AMBUSH_NOISE_BASE_RADIUS = 35
CURSED_AMBUSH_NOISE_MAX_RADIUS = 140

local function emitCursedSealWorldSound(player, radius, volume)
    if not player then
        return
    end

    local px = math.floor((tonumber(player.getX and player:getX()) or 0) + 0.5)
    local py = math.floor((tonumber(player.getY and player:getY()) or 0) + 0.5)
    local pz = math.floor((tonumber(player.getZ and player:getZ()) or 0) + 0.5)
    local soundRadius = math.max(0, tonumber(radius) or CURSED_SEAL_WORLD_SOUND_RADIUS)
    local soundVolume = math.max(0, tonumber(volume) or soundRadius)
    if soundRadius <= 0 or soundVolume <= 0 then
        return
    end

    -- Primary zombie-AI noise channel (prefer concrete emitter, fallback to nil source).
    if addSound then
        local emitted = false
        local ok = pcall(function()
            addSound(player, px, py, pz, soundRadius, soundVolume)
        end)
        emitted = ok == true
        if not emitted then
            pcall(function()
                addSound(nil, px, py, pz, soundRadius, soundVolume)
            end)
        end
    end

    -- Secondary noise source for systems that consume world-sound hooks.
    if player.addWorldSoundUnlessInvisible then
        pcall(function()
            player:addWorldSoundUnlessInvisible(soundRadius, soundVolume, true)
        end)
    end
end

local function emitCursedSilentAIPull(player, radius, volume)
    if not player then
        return
    end

    local px = math.floor((tonumber(player.getX and player:getX()) or 0) + 0.5)
    local py = math.floor((tonumber(player.getY and player:getY()) or 0) + 0.5)
    local pz = math.floor((tonumber(player.getZ and player:getZ()) or 0) + 0.5)
    local soundRadius = math.max(0, tonumber(radius) or CURSED_AMBUSH_NOISE_BASE_RADIUS)
    local soundVolume = math.max(0, tonumber(volume) or soundRadius)
    if soundRadius <= 0 or soundVolume <= 0 then
        return
    end

    -- AI-only noise pulse (silent to players) to pull zombies to seal-break origin.
    if addSound then
        local emitted = false
        local ok = pcall(function()
            addSound(player, px, py, pz, soundRadius, soundVolume)
        end)
        emitted = ok == true
        if not emitted then
            pcall(function()
                addSound(nil, px, py, pz, soundRadius, soundVolume)
            end)
        end
    end
end

local function nudgeCursedAmbushZombiesToward(player, radius)
    if not player or not getCell then
        return 0
    end

    local cell = getCell()
    if not cell then
        return 0
    end

    local zombies = cell.getZombieList and cell:getZombieList() or nil
    if not zombies or not zombies.size then
        return 0
    end

    local px = tonumber(player.getX and player:getX()) or 0
    local py = tonumber(player.getY and player:getY()) or 0
    local pz = math.floor((tonumber(player.getZ and player:getZ()) or 0) + 0.5)
    local pullRadius = math.max(1, tonumber(radius) or 80)
    local pullRadiusSq = pullRadius * pullRadius
    local nudged = 0

    local function directZombieTowardPlayer(zombie)
        if not zombie then
            return false
        end
        local moved = false
        local investigateX = px + ZombRand(-2, 3)
        local investigateY = py + ZombRand(-2, 3)
        if zombie.setUseless then
            local ok = pcall(function() zombie:setUseless(false) end)
            moved = moved or ok
        end
        if zombie.setCanWalk then
            local ok = pcall(function() zombie:setCanWalk(true) end)
            moved = moved or ok
        end
        if zombie.spotted and player then
            local ok = pcall(function() zombie:spotted(player, true) end)
            moved = moved or ok
        end
        if zombie.addAggro and player then
            local ok = pcall(function() zombie:addAggro(player, 100.0) end)
            moved = moved or ok
        end
        if zombie.setTurnAlertedValues then
            local ok = pcall(function() zombie:setTurnAlertedValues(math.floor(px), math.floor(py)) end)
            moved = moved or ok
        end
        if zombie.pathToCharacter and player then
            local ok = pcall(function() zombie:pathToCharacter(player) end)
            moved = moved or ok
        end
        if zombie.setTarget and player then
            local ok = pcall(function() zombie:setTarget(player) end)
            moved = moved or ok
        end
        if zombie.setTargetSeenTime then
            local ok = pcall(function() zombie:setTargetSeenTime(0) end)
            moved = moved or ok
        end
        if zombie.pathToLocationF then
            local ok = pcall(function() zombie:pathToLocationF(investigateX, investigateY, pz) end)
            moved = moved or ok
        end
        return moved
    end

    for i = 0, zombies:size() - 1 do
        local zombie = zombies:get(i)
        if zombie and not zombie:isDead() and not zombie:isOnFloor() then
            local zx = tonumber(zombie.getX and zombie:getX()) or 0
            local zy = tonumber(zombie.getY and zombie:getY()) or 0
            local zz = math.floor((tonumber(zombie.getZ and zombie:getZ()) or 0) + 0.5)
            if zz == pz then
                local dx = zx - px
                local dy = zy - py
                if (dx * dx) + (dy * dy) <= pullRadiusSq then
                    if directZombieTowardPlayer(zombie) then
                        nudged = nudged + 1
                    end
                end
            end
        end
    end

    return nudged
end

local function nudgeCursedAmbushZombiesTowardDelayed(player, radius, delayMs)
    if not player then
        return
    end

    local delay = math.max(0, tonumber(delayMs) or 0)
    if delay <= 0 then
        nudgeCursedAmbushZombiesToward(player, radius)
        return
    end

    local events = Events and Events.OnTick or nil
    if not events or not events.Add then
        nudgeCursedAmbushZombiesToward(player, radius)
        return
    end

    local startedAt = getTimestampMs and getTimestampMs() or nil
    local waitedTicks = 0
    local onTickFn
    onTickFn = function()
        local ready = false
        if startedAt then
            local now = getTimestampMs and getTimestampMs() or startedAt
            ready = (now - startedAt) >= delay
        else
            waitedTicks = waitedTicks + 1
            ready = waitedTicks >= 60
        end
        if not ready then
            return
        end

        nudgeCursedAmbushZombiesToward(player, radius)
        if BurdJournals.safeRemoveEvent then
            BurdJournals.safeRemoveEvent(events, onTickFn)
        elseif events.Remove then
            events.Remove(onTickFn)
        end
    end

    events.Add(onTickFn)
end

local function emitCursedSealWorldSoundDelayed(player, radius, volume, delayMs)
    if not player then
        return
    end

    local delay = math.max(0, tonumber(delayMs) or 0)
    if delay <= 0 then
        emitCursedSealWorldSound(player, radius, volume)
        return
    end

    local events = Events and Events.OnTick or nil
    if not events or not events.Add then
        emitCursedSealWorldSound(player, radius, volume)
        return
    end

    local startedAt = getTimestampMs and getTimestampMs() or nil
    local waitedTicks = 0
    local onTickFn
    onTickFn = function()
        local ready = false
        if startedAt then
            local now = getTimestampMs and getTimestampMs() or startedAt
            ready = (now - startedAt) >= delay
        else
            waitedTicks = waitedTicks + 1
            -- Fallback if getTimestampMs is unavailable; 2 ticks ~= 33ms at 60fps.
            ready = waitedTicks >= 2
        end
        if not ready then
            return
        end

        emitCursedSealWorldSound(player, radius, volume)
        if BurdJournals.safeRemoveEvent then
            BurdJournals.safeRemoveEvent(events, onTickFn)
        elseif events.Remove then
            events.Remove(onTickFn)
        end
    end

    events.Add(onTickFn)
end

local function emitCursedSilentAIPullDelayed(player, radius, volume, delayMs)
    if not player then
        return
    end

    local delay = math.max(0, tonumber(delayMs) or 0)
    if delay <= 0 then
        emitCursedSilentAIPull(player, radius, volume)
        return
    end

    local events = Events and Events.OnTick or nil
    if not events or not events.Add then
        emitCursedSilentAIPull(player, radius, volume)
        return
    end

    local startedAt = getTimestampMs and getTimestampMs() or nil
    local waitedTicks = 0
    local onTickFn
    onTickFn = function()
        local ready = false
        if startedAt then
            local now = getTimestampMs and getTimestampMs() or startedAt
            ready = (now - startedAt) >= delay
        else
            waitedTicks = waitedTicks + 1
            -- Fallback if getTimestampMs is unavailable; 60 ticks ~= 1s at 60fps.
            ready = waitedTicks >= 60
        end
        if not ready then
            return
        end

        emitCursedSilentAIPull(player, radius, volume)
        if BurdJournals.safeRemoveEvent then
            BurdJournals.safeRemoveEvent(events, onTickFn)
        elseif events.Remove then
            events.Remove(onTickFn)
        end
    end

    events.Add(onTickFn)
end

local function getCursedAmbushNoiseRadius()
    return CURSED_AMBUSH_NOISE_BASE_RADIUS
end

local function getCursedAmbushPullProfile()
    local base = getCursedAmbushNoiseRadius()
    if base <= 0 then
        return 0, 0, 0
    end

    -- Give ambush pulls extra buffer so edge zombies reliably react.
    local pullRadius = math.max(1, math.min(CURSED_AMBUSH_NOISE_MAX_RADIUS, base + 20))
    local pullVolume = math.max(pullRadius, math.min(CURSED_AMBUSH_NOISE_MAX_RADIUS, base + 40))
    local nudgeRadius = math.max(pullRadius, math.min(CURSED_AMBUSH_NOISE_MAX_RADIUS, pullRadius + 10))
    return pullRadius, pullVolume, nudgeRadius
end

local function canSpawnCursedZombieAtSquare(square, playerSquare, playerRoom)
    if not square or not playerSquare then
        return false
    end
    if square == playerSquare then
        return false
    end
    if square.isSolidFloor and not square:isSolidFloor() then
        return false
    end
    if playerRoom and square.getRoom and square:getRoom() == playerRoom then
        return false
    end
    return true
end

local function collectCursedSpawnSquares(player, minRadius, maxRadius)
    local out = {}
    if not player or not getCell then
        return out
    end

    local playerSquare = player.getCurrentSquare and player:getCurrentSquare() or nil
    if not playerSquare then
        return out
    end

    local cell = getCell()
    if not cell then
        return out
    end

    local baseX = math.floor((tonumber(player:getX()) or 0) + 0.5)
    local baseY = math.floor((tonumber(player:getY()) or 0) + 0.5)
    local baseZ = player:getZ()
    local room = playerSquare.getRoom and playerSquare:getRoom() or nil

    local minSq = minRadius * minRadius
    local maxSq = maxRadius * maxRadius

    for dx = -maxRadius, maxRadius do
        for dy = -maxRadius, maxRadius do
            local distSq = (dx * dx) + (dy * dy)
            if distSq >= minSq and distSq <= maxSq then
                local square = cell:getGridSquare(baseX + dx, baseY + dy, baseZ)
                if canSpawnCursedZombieAtSquare(square, playerSquare, room) then
                    out[#out + 1] = square
                end
            end
        end
    end

    return out
end

local function spawnCursedZombieAtSquare(square)
    if not square or not addZombiesInOutfit then
        return nil
    end

    local zombieList = nil
    local ok, err = pcall(function()
        zombieList = addZombiesInOutfit(
            square:getX(), square:getY(), square:getZ(),
            1, nil, 50,
            false, false, false, false,
            false, false, 1.0,
            false, 0.0
        )
    end)

    if not ok then
        BurdJournals.debugPrint("[BurdJournals] Cursed panic spawn failed: " .. tostring(err))
        return nil
    end

    if zombieList and zombieList.size and zombieList:size() > 0 then
        return zombieList:get(0)
    end

    return nil
end

local function spawnCursedPanicHorde(player, minCount, maxCount, minRadius, maxRadius)
    if not player then
        return 0
    end

    local spawnMin = math.max(1, tonumber(minCount) or 8)
    local spawnMax = math.max(spawnMin, tonumber(maxCount) or 12)
    local ringMin = math.max(4, tonumber(minRadius) or 9)
    local ringMax = math.max(ringMin, tonumber(maxRadius) or 18)
    local targetCount = ZombRand(spawnMin, spawnMax + 1)
    local candidates = collectCursedSpawnSquares(player, ringMin, ringMax)
    local targetX = tonumber(player.getX and player:getX()) or 0
    local targetY = tonumber(player.getY and player:getY()) or 0
    local targetZ = math.floor((tonumber(player.getZ and player:getZ()) or 0) + 0.5)
    local function directSpawnTowardPlayer(zombie)
        if not zombie then
            return
        end
        local investigateX = targetX + ZombRand(-2, 3)
        local investigateY = targetY + ZombRand(-2, 3)
        if zombie.setUseless then
            pcall(function() zombie:setUseless(false) end)
        end
        if zombie.setCanWalk then
            pcall(function() zombie:setCanWalk(true) end)
        end
        if zombie.spotted and player then
            pcall(function() zombie:spotted(player, true) end)
        end
        if zombie.addAggro and player then
            pcall(function() zombie:addAggro(player, 100.0) end)
        end
        if zombie.setTurnAlertedValues then
            pcall(function() zombie:setTurnAlertedValues(math.floor(targetX), math.floor(targetY)) end)
        end
        if zombie.pathToCharacter and player then
            pcall(function() zombie:pathToCharacter(player) end)
        end
        if zombie.setTarget and player then
            pcall(function() zombie:setTarget(player) end)
        end
        if zombie.setTargetSeenTime then
            pcall(function() zombie:setTargetSeenTime(0) end)
        end
        if zombie.pathToLocationF then
            pcall(function() zombie:pathToLocationF(investigateX, investigateY, targetZ) end)
        end
    end
    if #candidates == 0 or targetCount <= 0 then
        return 0
    end

    local spawned = 0
    for _ = 1, targetCount do
        if #candidates == 0 then
            break
        end

        local idx = ZombRand(#candidates) + 1
        local square = candidates[idx]
        table.remove(candidates, idx)
        local spawnedZombie = spawnCursedZombieAtSquare(square)
        if spawnedZombie then
            spawned = spawned + 1
            directSpawnTowardPlayer(spawnedZombie)
        end
    end

    return spawned
end

local function cursePanic(player)
    local networkClient = isClient and isClient() and isServer and not isServer()
    local panicApplied = setPlayerPanicHard(player, 100)
    if not panicApplied and not networkClient and player and player.setPanic then
        pcall(player.setPanic, player, 100)
        panicApplied = setPlayerPanicHard(player, 100)
    end

    local spawned = spawnCursedPanicHorde(player, 8, 12, 9, 18)
    if not panicApplied and spawned <= 0 then return nil end
    local message
    if spawned > 0 then
        local template = getCursedServerText(
            "UI_BurdJournals_CursedMsgPanicHorde",
            "Ambush! A wave of panic grips you as %d dead answer the broken seal."
        )
        message = BurdJournals.formatText(template, spawned)
    else
        message = getCursedServerText("UI_BurdJournals_CursedMsgPanic", "Ambush! A wave of panic grips you.")
    end

    return {
        type = "panic",
        message = message,
        hordeCount = spawned,
        focusText = spawned > 0 and tostring(spawned) or getCursedServerText("UI_BurdJournals_CursedMsgPanic", "Ambush"),
        focusType = spawned > 0 and "horde_count" or "label",
    }
end

CURSE_EFFECT_HANDLERS = {
    barbed_seal = curseBarbedSeal,
    jammed_breath = curseJammedBreath,
    hexed_tooling = curseHexedTooling,
    torn_gear = curseTornGear,
    seasonal_wave = curseSeasonalWave,
    pantsed = cursePantsed,
    gain_negative_trait = curseGainNegativeTrait,
    lose_positive_trait = curseLosePositiveTrait,
    lose_skill_level = curseLoseSkillLevel,
    panic = cursePanic,
}

local function normalizeCurseEffectType(effectType)
    if type(effectType) ~= "string" then
        return nil
    end
    local normalized = string.lower(effectType)
    if normalized == "random" then
        return "random"
    end
    if CURSE_EFFECT_HANDLERS[normalized] then
        return normalized
    end
    return nil
end

do
CURSE_EFFECT_ORDER = {
    "barbed_seal",
    "jammed_breath",
    "hexed_tooling",
    "torn_gear",
    "seasonal_wave",
    "pantsed",
    "gain_negative_trait",
    "lose_positive_trait",
    "lose_skill_level",
    "panic",
}

CURSE_EFFECT_BASE_WEIGHTS = {
    barbed_seal = 1.10,
    jammed_breath = 1.10,
    hexed_tooling = 1.20,
    torn_gear = 1.20,
    seasonal_wave = 1.00,
    pantsed = 1.00,
    gain_negative_trait = 0.90,
    lose_positive_trait = 0.12,
    lose_skill_level = 0.10,
    panic = 0.95,
}

CURSE_EFFECT_MEMORY_KEY = "cursedRecentEffects"
CURSE_EFFECT_MEMORY_MAX = 8
CURSE_EFFECT_RECENT_LOOKBACK = 3
CURSE_EFFECT_REPEAT_MULTIPLIER = 0.20
CURSE_EFFECT_LAST_REPEAT_MULTIPLIER = 0.15
CURSE_EFFECT_STACKED_REPEAT_MULTIPLIER = 0.25
CURSE_EFFECT_PERSISTENT_FATIGUE_MULTIPLIER = 0.85
CURSE_EFFECT_SEVERE_STREAK_MULTIPLIER = 0.20

CURSE_PERSISTENT_EFFECT_TYPES = {
    gain_negative_trait = true,
    lose_positive_trait = true,
    lose_skill_level = true,
}

CURSE_SEVERE_EFFECT_TYPES = {
    lose_positive_trait = true,
    lose_skill_level = true,
}

function BurdJournals.Server.getCursedEffectCategory(effectType)
    if CURSE_SEVERE_EFFECT_TYPES[effectType] == true then
        return "severe"
    end
    if CURSE_PERSISTENT_EFFECT_TYPES[effectType] == true then
        return "persistent"
    end
    return "temporary"
end

function BurdJournals.Server.getWorldAgeHoursSafe()
    local gameTime = getGameTime and getGameTime() or nil
    if not gameTime or not gameTime.getWorldAgeHours then
        return nil
    end
    return tonumber(gameTime:getWorldAgeHours())
end

function BurdJournals.Server.getCursedSandboxPercent(optionName, fallback)
    local rawValue = tonumber(BurdJournals.getSandboxOption and BurdJournals.getSandboxOption(optionName))
    if rawValue == nil then
        return fallback
    end
    return math.max(0, rawValue) / 100
end

function BurdJournals.Server.getCursedEffectBaseWeight(effectType)
    if effectType == "gain_negative_trait" then
        return BurdJournals.Server.getCursedSandboxPercent("CursedJournalNegativeTraitCurseWeight", 0.90)
    end
    if effectType == "lose_positive_trait" then
        return BurdJournals.Server.getCursedSandboxPercent("CursedJournalLosePositiveTraitCurseWeight", 0.12)
    end
    if effectType == "lose_skill_level" then
        return BurdJournals.Server.getCursedSandboxPercent("CursedJournalLoseSkillCurseWeight", 0.10)
    end
    return tonumber(CURSE_EFFECT_BASE_WEIGHTS[effectType]) or 1
end

function BurdJournals.Server.getCursedSevereRepeatChanceMultiplier()
    return BurdJournals.Server.getCursedSandboxPercent("CursedJournalSevereRepeatChancePercent", 0.04)
end

function BurdJournals.Server.getCursedSevereCooldownHours()
    local rawValue = tonumber(BurdJournals.getSandboxOption and BurdJournals.getSandboxOption("CursedJournalSevereCooldownHours"))
    if rawValue == nil then
        return 72
    end
    return math.max(0, math.floor(rawValue))
end

function BurdJournals.Server.getPlayerCursedEffectHistory(player, create)
    if not player or not player.getModData then
        return nil, nil
    end
    local playerModData = player:getModData()
    if type(playerModData) ~= "table" then
        return nil, nil
    end
    if create then
        playerModData.BurdJournals = playerModData.BurdJournals or {}
    end
    local bj = playerModData.BurdJournals
    if type(bj) ~= "table" then
        return nil, nil
    end
    if create and type(bj[CURSE_EFFECT_MEMORY_KEY]) ~= "table" then
        bj[CURSE_EFFECT_MEMORY_KEY] = {}
    end
    return bj[CURSE_EFFECT_MEMORY_KEY], bj
end

function BurdJournals.Server.prunePlayerCursedEffectHistory(history, hoursNow)
    if type(history) ~= "table" then
        return false, {}
    end

    local changed = false
    local kept = {}
    local minHours = hoursNow and (hoursNow - BurdJournals.Server.getCursedSevereCooldownHours()) or nil

    for _, entry in ipairs(history) do
        if type(entry) == "table" then
            local effectType = normalizeCurseEffectType(entry.type)
            local entryHours = tonumber(entry.atHours)
            local keep = effectType ~= nil and effectType ~= "random"
            if keep and minHours ~= nil and entryHours ~= nil and entryHours < minHours then
                keep = false
            end
            if keep then
                kept[#kept + 1] = {
                    type = effectType,
                    atHours = entryHours,
                    category = BurdJournals.Server.getCursedEffectCategory(effectType),
                }
            else
                changed = true
            end
        else
            changed = true
        end
    end

    while #kept > CURSE_EFFECT_MEMORY_MAX do
        table.remove(kept, 1)
        changed = true
    end

    return changed, kept
end

function BurdJournals.Server.getRecentCursedEffectSummary(player)
    local history = BurdJournals.Server.getPlayerCursedEffectHistory(player, false)
    local hoursNow = BurdJournals.Server.getWorldAgeHoursSafe()
    local _, prunedHistory = BurdJournals.Server.prunePlayerCursedEffectHistory(history, hoursNow)
    local summary = {
        exactCounts = {},
        recentPersistentCount = 0,
        recentSevereCount = 0,
        lastEffectType = nil,
        history = prunedHistory,
    }

    local startIndex = math.max(1, #prunedHistory - CURSE_EFFECT_RECENT_LOOKBACK + 1)
    for index = startIndex, #prunedHistory do
        local entry = prunedHistory[index]
        local effectType = entry and entry.type
        if effectType then
            summary.exactCounts[effectType] = (summary.exactCounts[effectType] or 0) + 1
            if CURSE_PERSISTENT_EFFECT_TYPES[effectType] == true then
                summary.recentPersistentCount = summary.recentPersistentCount + 1
            end
            if CURSE_SEVERE_EFFECT_TYPES[effectType] == true then
                summary.recentSevereCount = summary.recentSevereCount + 1
            end
            summary.lastEffectType = effectType
        end
    end

    return summary
end

function BurdJournals.Server.getCursedEffectSelectionWeight(player, effectType, summary)
    effectType = normalizeCurseEffectType(effectType)
    if not effectType or effectType == "random" then
        return 0
    end

    summary = summary or BurdJournals.Server.getRecentCursedEffectSummary(player)
    local weight = BurdJournals.Server.getCursedEffectBaseWeight(effectType)
    local sameCount = (summary.exactCounts and summary.exactCounts[effectType]) or 0
    local isPersistent = CURSE_PERSISTENT_EFFECT_TYPES[effectType] == true
    local isSevere = CURSE_SEVERE_EFFECT_TYPES[effectType] == true

    if sameCount > 0 then
        weight = weight * CURSE_EFFECT_REPEAT_MULTIPLIER
        if summary.lastEffectType == effectType then
            weight = weight * CURSE_EFFECT_LAST_REPEAT_MULTIPLIER
        end
        if sameCount > 1 then
            weight = weight * CURSE_EFFECT_STACKED_REPEAT_MULTIPLIER
        end
    end

    if isPersistent and summary.recentPersistentCount > 0 then
        weight = weight * CURSE_EFFECT_PERSISTENT_FATIGUE_MULTIPLIER
    end
    if isSevere and summary.recentSevereCount > 0 then
        weight = weight * BurdJournals.Server.getCursedSevereRepeatChanceMultiplier()
    end
    if isSevere and summary.recentPersistentCount > 1 then
        weight = weight * CURSE_EFFECT_SEVERE_STREAK_MULTIPLIER
    end

    return math.max(0.01, weight)
end

function BurdJournals.Server.buildWeightedCursedEffectOrder(player, options)
    local summary = BurdJournals.Server.getRecentCursedEffectSummary(player)
    local remaining = {}
    local ordered = {}

    local omenCategory = type(options) == "table" and options.omenCategory or nil

    local function isEffectAllowed(effectType)
        if omenCategory ~= nil then
            if not (BurdJournals.Server.getCurseEffectOmenCategory
                and BurdJournals.Server.getCurseEffectOmenCategory(effectType) == omenCategory) then
                return false
            end
        end
        return true
    end

    -- Build the candidate pool. When an omen category is set, restrict to effects in
    -- that category; if that leaves nothing, fall back to the full pool (never fail).
    for _, effectType in ipairs(CURSE_EFFECT_ORDER) do
        if isEffectAllowed(effectType) then
            remaining[#remaining + 1] = effectType
        end
    end
    if #remaining == 0 then
        for _, effectType in ipairs(CURSE_EFFECT_ORDER) do
            remaining[#remaining + 1] = effectType
        end
    end

    while #remaining > 0 do
        local weightedEntries = {}
        local totalWeight = 0

        for index, effectType in ipairs(remaining) do
            local rawWeight = BurdJournals.Server.getCursedEffectSelectionWeight(player, effectType, summary)
            local scaledWeight = math.max(1, math.floor((rawWeight * 1000) + 0.5))
            totalWeight = totalWeight + scaledWeight
            weightedEntries[#weightedEntries + 1] = {
                index = index,
                scaledWeight = scaledWeight,
            }
        end

        local chosenIndex = nil
        if totalWeight > 0 then
            local roll = ZombRand(totalWeight)
            local cursor = 0
            for _, entry in ipairs(weightedEntries) do
                cursor = cursor + entry.scaledWeight
                if roll < cursor then
                    chosenIndex = entry.index
                    break
                end
            end
        end

        if not chosenIndex then
            chosenIndex = ZombRand(#remaining) + 1
        end

        ordered[#ordered + 1] = table.remove(remaining, chosenIndex)
    end

    return ordered
end

function BurdJournals.Server.recordAppliedCursedEffect(player, effectType)
    effectType = normalizeCurseEffectType(effectType)
    if not effectType or effectType == "random" then
        return
    end

    local history, bj = BurdJournals.Server.getPlayerCursedEffectHistory(player, true)
    if type(history) ~= "table" or type(bj) ~= "table" then
        return
    end

    local hoursNow = BurdJournals.Server.getWorldAgeHoursSafe()
    local _, prunedHistory = BurdJournals.Server.prunePlayerCursedEffectHistory(history, hoursNow)
    prunedHistory[#prunedHistory + 1] = {
        type = effectType,
        atHours = hoursNow,
        category = BurdJournals.Server.getCursedEffectCategory(effectType),
    }

    while #prunedHistory > CURSE_EFFECT_MEMORY_MAX do
        table.remove(prunedHistory, 1)
    end

    bj[CURSE_EFFECT_MEMORY_KEY] = prunedHistory
    if player.transmitModData then
        player:transmitModData()
    end
end

end

-- =====================================================================
-- Curse effect omen categories + add-on curse effect registry
-- Omen categories group curse effects for the binding-omen warning prompt.
-- =====================================================================

CURSE_EFFECT_OMEN_CATEGORY = CURSE_EFFECT_OMEN_CATEGORY or {
    barbed_seal = "pain",
    jammed_breath = "pain",
    seasonal_wave = "pain",
    panic = "pain",
    hexed_tooling = "ruin",
    torn_gear = "ruin",
    pantsed = "ruin",
    gain_negative_trait = "loss",
    lose_positive_trait = "loss",
    lose_skill_level = "loss",
}

function BurdJournals.Server.getCurseEffectOmenCategory(effectType)
    if type(effectType) ~= "string" then
        return nil
    end
    return CURSE_EFFECT_OMEN_CATEGORY[string.lower(effectType)] or "pain"
end

function BurdJournals.Server.setCurseEffectOmenCategory(effectType, omenCategory)
    if type(effectType) ~= "string" or type(omenCategory) ~= "string" then
        return
    end
    CURSE_EFFECT_OMEN_CATEGORY[string.lower(effectType)] = string.lower(omenCategory)
end

-- =====================================================================
-- Expired curses: older cursed journals can go dormant and open safely.
-- =====================================================================

-- Resolve the world-age hours a cursed journal was spawned. Falls back to
-- data.timestamp, then lazily stamps "now" (no expiration credit) if neither exists.
function BurdJournals.Server.getCursedJournalSpawnHours(data, stampIfMissing)
    if type(data) ~= "table" then
        return nil
    end
    local spawnHours = tonumber(data.cursedSpawnedAtHours)
    if spawnHours ~= nil then
        return spawnHours
    end
    spawnHours = tonumber(data.timestamp)
    if spawnHours ~= nil then
        return spawnHours
    end
    if stampIfMissing then
        local now = BurdJournals.Server.getWorldAgeHoursSafe()
        if now ~= nil then
            data.cursedSpawnedAtHours = now
            return now
        end
    end
    return nil
end

-- Compute expiration info for a cursed journal. Missing timestamps never expire.
function BurdJournals.Server.getCursedExpirationInfo(data)
    local info = { expired = false, enabled = false }
    if BurdJournals.getSandboxOption and BurdJournals.getSandboxOption("EnableCursedJournalExpiration") == false then
        return info
    end
    info.enabled = true

    local expirationDays = tonumber(BurdJournals.getSandboxOption and BurdJournals.getSandboxOption("CursedJournalExpirationDays"))
    if expirationDays == nil then
        expirationDays = 30
    end
    if expirationDays <= 0 then
        return info
    end
    info.thresholdDays = expirationDays

    local spawnHours = BurdJournals.Server.getCursedJournalSpawnHours(data, false)
    local now = BurdJournals.Server.getWorldAgeHoursSafe()
    if spawnHours == nil or now == nil then
        return info
    end
    info.spawnHours = spawnHours

    local ageHours = now - spawnHours
    if ageHours <= 0 then
        info.ageDays = 0
        return info
    end
    info.ageDays = ageHours / 24

    if ageHours >= (expirationDays * 24) then
        info.expired = true
    end
    return info
end

-- =====================================================================
-- Binding omens: insight-driven curse category warnings shown before the reader commits.
-- =====================================================================

function BurdJournals.Server.shouldShowCursedOmenForInsight(insightLevel)
    return (tonumber(insightLevel) or 0) >= 2
end

-- Roll a binding-omen category (pain|ruin|loss) using the weighted selection so the
-- omen respects player history. Returns the category string or nil.
function BurdJournals.Server.rollCursedOmenCategory(player)
    local ordered = BurdJournals.Server.buildWeightedCursedEffectOrder(player)
    if type(ordered) ~= "table" then
        return nil
    end
    for _, effectType in ipairs(ordered) do
        local category = BurdJournals.Server.getCurseEffectOmenCategory(effectType)
        if category then
            return category
        end
    end
    return nil
end

-- Localized omen line for a category (or the expired line for dormant journals).
function BurdJournals.Server.getCursedOmenLine(omenCategory, expired)
    if expired then
        return getCursedServerText("UI_BurdJournals_CursedOmenExpired", "The ink has dried and cracked. Whatever slept here has gone quiet.")
    end
    if omenCategory == "ruin" then
        return getCursedServerText("UI_BurdJournals_CursedOmenRuin", "An omen of ruin: your tools and garb will suffer.")
    elseif omenCategory == "loss" then
        return getCursedServerText("UI_BurdJournals_CursedOmenLoss", "An omen of loss: something you have earned may be stripped away.")
    end
    return getCursedServerText("UI_BurdJournals_CursedOmenPain", "An omen of pain: your body will bear the mark.")
end

function BurdJournals.Server.rollCursedInsightEffectType(player, omenCategory)
    local options = {}
    if type(omenCategory) == "string" and omenCategory ~= "" then
        options.omenCategory = omenCategory
    end
    local ordered = BurdJournals.Server.buildWeightedCursedEffectOrder(player, options)
    if type(ordered) == "table" and ordered[1] then
        return normalizeCurseEffectType(ordered[1])
    end
    return nil
end

function BurdJournals.Server.getCursedInsightLine(insightLevel, omenCategory, effectType, expired, hidden)
    insightLevel = tonumber(insightLevel) or 0
    if expired then
        return BurdJournals.Server.getCursedOmenLine(nil, true)
    end
    if insightLevel >= 3 and type(effectType) == "string" and effectType ~= "" then
        local effectName = BurdJournals.getCursedEffectInsightDisplayName
            and BurdJournals.getCursedEffectInsightDisplayName(effectType)
            or effectType
        return BurdJournals.formatText(
            getCursedServerText("UI_BurdJournals_CursedInsightExact", "Your fieldcraft reads the seal: %s."),
            effectName
        )
    end
    if insightLevel >= 2 and type(omenCategory) == "string" and omenCategory ~= "" then
        return BurdJournals.Server.getCursedOmenLine(omenCategory, false)
    end
    if insightLevel >= 1 and hidden == true then
        return getCursedServerText("UI_BurdJournals_CursedInsightHidden", "The blood is wrong. Something has been hidden under it.")
    end
    if insightLevel >= 1 then
        return getCursedServerText("UI_BurdJournals_CursedInsightFaint", "The seal feels wrong under your eyes.")
    end
    return nil
end

function BurdJournals.Server.getCursedInsightPreviewPlayerKey(player)
    if not player then
        return nil
    end
    local username = player.getUsername and player:getUsername() or nil
    if username and username ~= "" then
        return "u:" .. tostring(username)
    end
    local playerNum = player.getPlayerNum and player:getPlayerNum() or nil
    if playerNum ~= nil then
        return "p:" .. tostring(playerNum)
    end
    return tostring(player)
end

function BurdJournals.Server.getCursedInsightPreviewKey(player, journalUUID, journalId)
    local playerKey = BurdJournals.Server.getCursedInsightPreviewPlayerKey(player)
    if not playerKey then
        return nil
    end
    if type(journalUUID) == "string" and journalUUID ~= "" then
        return playerKey .. "|uuid:" .. journalUUID
    end
    if journalId ~= nil then
        return playerKey .. "|id:" .. tostring(journalId)
    end
    return nil
end

function BurdJournals.Server.getCachedCursedInsightPreview(player, journalUUID, journalId)
    local key = BurdJournals.Server.getCursedInsightPreviewKey(player, journalUUID, journalId)
    if not key then
        return nil
    end
    local preview = BurdJournals.Server._cursedInsightPreviewCache[key]
    if type(preview) ~= "table" then
        return nil
    end
    if type(journalUUID) == "string" and journalUUID ~= "" and preview.journalUUID ~= journalUUID then
        return nil
    end
    return preview
end

function BurdJournals.Server.storeCursedInsightPreview(player, preview)
    if type(preview) ~= "table" then
        return nil
    end
    local key = BurdJournals.Server.getCursedInsightPreviewKey(player, preview.journalUUID, preview.journalId)
    if not key then
        return nil
    end
    local cache = BurdJournals.Server._cursedInsightPreviewCache
    local nowMs = (getTimestampMs and getTimestampMs()) or ((os.time() or 0) * 1000)
    preview._bsjCachedAt = nowMs
    cache[key] = preview
    local entries = {}
    for cacheKey, cached in pairs(cache) do
        local storedAt = type(cached) == "table" and tonumber(cached._bsjCachedAt) or 0
        if storedAt > 0 and (nowMs - storedAt) > 1800000 then
            cache[cacheKey] = nil
        else
            entries[#entries + 1] = {key = cacheKey, storedAt = storedAt}
        end
    end
    local maxEntries = 2048
    if #entries > maxEntries then
        table.sort(entries, function(a, b) return a.storedAt < b.storedAt end)
        for index = 1, #entries - maxEntries do
            cache[entries[index].key] = nil
        end
    end
    return preview
end

function BurdJournals.Server.buildCursedInsightPreview(player, item, data, journalId, journalUUID)
    if not player or not item or type(data) ~= "table" then
        return nil
    end

    local insightLevel = BurdJournals.getCursedInsightLevel
        and select(1, BurdJournals.getCursedInsightLevel(player))
        or 0
    if insightLevel <= 0 then
        return nil
    end

    local isHiddenCursed = isHiddenCursedJournalState(item, data)
    local isSealedCursed = BurdJournals.isCursedJournalItem and BurdJournals.isCursedJournalItem(item) or false
    if not isHiddenCursed and not isSealedCursed then
        return nil
    end
    if isSealedCursed and insightLevel < 2 then
        return nil
    end

    local expirationInfo = BurdJournals.Server.getCursedExpirationInfo(data)
    local omenCategory = nil
    local effectType = nil
    local boundPreview = false
    if expirationInfo and expirationInfo.expired then
        effectType = "expired"
    elseif insightLevel >= 3 then
        effectType = normalizeCurseEffectType(data.cursedInsightEffectType)
            or normalizeCurseEffectType(data.cursedForcedEffectType)
        if not effectType then
            effectType = BurdJournals.Server.rollCursedInsightEffectType(player, data.cursedOmenCategory)
            if effectType then
                data.cursedInsightEffectType = effectType
                boundPreview = true
            end
        end
        omenCategory = effectType and BurdJournals.Server.getCurseEffectOmenCategory(effectType) or nil
        if omenCategory and data.cursedOmenCategory ~= omenCategory then
            data.cursedOmenCategory = omenCategory
            boundPreview = true
        end
    elseif insightLevel >= 2 then
        omenCategory = type(data.cursedOmenCategory) == "string"
            and data.cursedOmenCategory
            or BurdJournals.Server.rollCursedOmenCategory(player)
        if omenCategory and data.cursedOmenCategory ~= omenCategory then
            data.cursedOmenCategory = omenCategory
            boundPreview = true
        end
    end

    if boundPreview and item.transmitModData then
        item:transmitModData()
    end

    return BurdJournals.Server.storeCursedInsightPreview(player, {
        journalId = journalId,
        journalUUID = journalUUID,
        insightLevel = insightLevel,
        hiddenCursed = isHiddenCursed == true or nil,
        sealedCursed = isSealedCursed == true or nil,
        omenCategory = omenCategory,
        effectType = effectType,
        curseExpired = expirationInfo and expirationInfo.expired == true or nil,
        curseAgeDays = expirationInfo and expirationInfo.ageDays or nil,
        curseExpirationDays = expirationInfo and expirationInfo.thresholdDays or nil,
    })
end

-- Tooltip previews are the sole gameplay path allowed to inspect a journal
-- outside the requester's inventory. Resolve one exact identity within reach,
-- with a bounded container walk and a capped miss cache; never trust or apply
-- client journalData/fingerprint state.
function BurdJournals.Server.resolveCursedInsightPreviewJournal(player, args)
    if not player or type(args) ~= "table" then return nil end
    local requestedId = args.journalId ~= nil and tonumber(args.journalId) or nil
    local requestedUUID = type(args.journalUUID) == "string" and args.journalUUID ~= "" and args.journalUUID or nil
    if requestedUUID and #requestedUUID > 128 then return nil end
    if (args.journalId ~= nil and requestedId == nil) or (not requestedId and not requestedUUID) then return nil end

    local playerKey = BurdJournals.Server.getCursedInsightPreviewPlayerKey(player)
    if not playerKey then return nil end
    local missKey = playerKey .. "|" .. (requestedUUID and ("uuid:" .. requestedUUID) or ("id:" .. tostring(requestedId)))
    local nowMs = (getTimestampMs and getTimestampMs()) or ((os.time() or 0) * 1000)
    local misses = BurdJournals.Server._cursedInsightPreviewMisses
    local activeMisses = 0
    for key, entry in pairs(misses) do
        if type(entry) ~= "table" or (nowMs - (tonumber(entry.at) or 0)) >= 60000 then
            misses[key] = nil
        else
            activeMisses = activeMisses + 1
        end
    end
    local miss = misses[missKey]
    if type(miss) == "table" and (nowMs - (tonumber(miss.at) or 0)) < 60000 and (tonumber(miss.count) or 0) >= 3 then
        return nil
    elseif type(miss) == "table" and (nowMs - (tonumber(miss.at) or 0)) >= 60000 then
        misses[missKey] = nil
        miss = nil
    end
    if not miss and activeMisses >= 256 then return nil end

    local function matches(candidate)
        if not (candidate and candidate.getID and candidate.getFullType) then return false end
        local fullType = tostring(candidate:getFullType() or "")
        if not fullType:find("^BurdJournals%.") then return false end
        if requestedId and tonumber(candidate:getID()) ~= requestedId then return false end
        if requestedUUID then
            local data = BurdJournals.getJournalData and BurdJournals.getJournalData(candidate) or nil
            if type(data) ~= "table" or tostring(data.uuid or "") ~= requestedUUID then return false end
        end
        return true
    end

    local inventory = player.getInventory and player:getInventory() or nil
    local budget = { remaining = 512, seen = {} }
    local function scanContainer(container)
        if not (container and container.getItems) or budget.remaining <= 0 then return nil end
        local containerKey = tostring(container)
        if budget.seen[containerKey] then return nil end
        budget.seen[containerKey] = true
        local items = container:getItems()
        if not items then return nil end
        for i = 0, items:size() - 1 do
            if budget.remaining <= 0 then return nil end
            budget.remaining = budget.remaining - 1
            local candidate = items:get(i)
            if matches(candidate) then return candidate end
            local nested = candidate and candidate.getInventory and candidate:getInventory() or nil
            local found = nested and scanContainer(nested) or nil
            if found then return found end
        end
        return nil
    end

    local found = scanContainer(inventory)
    if not found then
        -- Keep a separate cap for reachable world containers so a huge modded
        -- player inventory cannot starve natural-loot discovery (1024 max
        -- candidates across both scopes).
        budget = { remaining = 512, seen = {} }
        local origin = player.getCurrentSquare and player:getCurrentSquare() or nil
        local cell = origin and getCell and getCell() or nil
        if origin and cell and cell.getGridSquare then
            local ox, oy, oz = origin:getX(), origin:getY(), origin:getZ()
            for dx = -1, 1 do
                for dy = -1, 1 do
                    local square = cell:getGridSquare(ox + dx, oy + dy, oz)
                    -- Project Zomboid's own container interaction handler uses
                    -- isBlockedTo for this same adjacent-square reachability
                    -- boundary. If an older API lacks it, fail closed off-square.
                    local reachable = square == origin
                    if not reachable and square and origin.isBlockedTo then
                        reachable = not origin:isBlockedTo(square)
                    end
                    local objects = reachable and square and square.getObjects and square:getObjects() or nil
                    if objects then
                        for i = 0, objects:size() - 1 do
                            local object = objects:get(i)
                            local worldItem = object and object.getItem and object:getItem() or nil
                            if matches(worldItem) then found = worldItem; break end
                            local container = object and object.getContainer and object:getContainer() or nil
                            found = container and scanContainer(container) or nil
                            if not found then
                                local objectInventory = object and object.getInventory and object:getInventory() or nil
                                found = objectInventory and scanContainer(objectInventory) or nil
                            end
                            if found then break end
                        end
                    end
                    if found then break end
                end
                if found then break end
            end
        end
    end

    if not found then
        misses[missKey] = { count = (type(miss) == "table" and (tonumber(miss.count) or 0) or 0) + 1, at = nowMs }
        return nil
    end
    misses[missKey] = nil
    local data = BurdJournals.getJournalData and BurdJournals.getJournalData(found) or nil
    return found, found:getID(), type(data) == "table" and data.uuid or nil
end

function BurdJournals.Server.handleRequestCursedInsightPreview(player, args)
    if not player or type(args) ~= "table" then
        return
    end

    local item, resolvedJournalId, resolvedJournalUUID = BurdJournals.Server.resolveCursedInsightPreviewJournal(player, args)
    if not item then
        return
    end
    local modData = item:getModData()
    modData.BurdJournals = modData.BurdJournals or {}
    local data = modData.BurdJournals
    normalizeCursedJournalStateForType(item, "requestCursedInsightPreview")
    resolvedJournalUUID = resolvedJournalUUID
        or (BurdJournals.resolveJournalUUIDForRuntime and BurdJournals.resolveJournalUUIDForRuntime(data, item, true))
        or data.uuid

    local preview = BurdJournals.Server.buildCursedInsightPreview(
        player,
        item,
        data,
        resolvedJournalId or (item.getID and item:getID()) or nil,
        resolvedJournalUUID
    )
    if preview then
        BurdJournals.Server.sendToClient(player, "cursedInsightPreview", preview)
    end
end

-- Register a new curse effect from an add-on. spec fields:
--   handler(context) -> resultTable|nil  (context: player, forcedTraitId, forcedSkillName, effectType, journalItem, journalData)
--   baseWeight (number, optional)
--   category "temporary"|"persistent"|"severe" (optional, default temporary)
--   omenCategory "pain"|"ruin"|"loss" (optional, default pain)
function BurdJournals.Server.registerCurseEffect(id, spec)
    if type(id) ~= "string" or id == "" or type(spec) ~= "table" then
        return false
    end
    local normalizedId = string.lower(id)
    local handler = spec.handler
    if type(handler) ~= "function" then
        return false
    end

    -- Adapter: core calls handlers as (player, forcedTraitId, forcedSkillName);
    -- registered handlers receive a single context table instead.
    CURSE_EFFECT_HANDLERS[normalizedId] = function(player, forcedTraitId, forcedSkillName, extraContext)
        local context = type(extraContext) == "table" and extraContext or {}
        context.player = player
        context.forcedTraitId = forcedTraitId
        context.forcedSkillName = forcedSkillName
        context.effectType = normalizedId
        return handler(context)
    end

    local inOrder = false
    for _, existing in ipairs(CURSE_EFFECT_ORDER) do
        if existing == normalizedId then
            inOrder = true
            break
        end
    end
    if not inOrder then
        CURSE_EFFECT_ORDER[#CURSE_EFFECT_ORDER + 1] = normalizedId
    end

    if tonumber(spec.baseWeight) then
        CURSE_EFFECT_BASE_WEIGHTS[normalizedId] = tonumber(spec.baseWeight)
    end

    local category = spec.category
    if category == "severe" then
        CURSE_SEVERE_EFFECT_TYPES[normalizedId] = true
        CURSE_PERSISTENT_EFFECT_TYPES[normalizedId] = true
    elseif category == "persistent" then
        CURSE_PERSISTENT_EFFECT_TYPES[normalizedId] = true
    end

    if type(spec.omenCategory) == "string" then
        BurdJournals.Server.setCurseEffectOmenCategory(normalizedId, spec.omenCategory)
    end

    return true
end

function BurdJournals.Server.normalizeCursedSealSoundEvent(soundEvent)
    if soundEvent == nil then
        return nil
    end
    if type(soundEvent) ~= "string" then
        return nil
    end

    local trimmed = string.gsub(soundEvent, "^%s+", "")
    trimmed = string.gsub(trimmed, "%s+$", "")
    if trimmed == "" then
        return nil
    end

    local lowered = string.lower(trimmed)
    if lowered == "default" then
        return nil
    end
    if lowered == "random" then
        return nil
    end
    if lowered == "none" or lowered == "off" or lowered == "silent" then
        return "none"
    end

    return trimmed
end

function BurdJournals.Server.applyCursedEffect(player, forcedType, forcedTraitId, forcedSkillName, context)
    context = type(context) == "table" and context or {}
    local function finish(result)
        if result and BurdJournals.runHooks then
            BurdJournals.runHooks("OnCurseApplied", player, result, context.journalItem)
        end
        return result
    end

    local normalizedForcedType = normalizeCurseEffectType(forcedType)
    if normalizedForcedType and normalizedForcedType ~= "random" then
        local forced = CURSE_EFFECT_HANDLERS[normalizedForcedType]
        if forced then
            local result = forced(player, forcedTraitId, forcedSkillName, context)
            if result then
                BurdJournals.Server.recordAppliedCursedEffect(player, result.type)
                return finish(result)
            end
        end
        if BurdJournals.debugPrint then
            BurdJournals.debugPrint("[BurdJournals] Forced cursed effect '" .. tostring(normalizedForcedType) .. "' had no valid target; falling back to panic")
        end
        local panicResult = cursePanic(player, nil, nil)
        if panicResult then
            BurdJournals.Server.recordAppliedCursedEffect(player, panicResult.type)
        end
        return finish(panicResult)
    end

    local effectTypes = BurdJournals.Server.buildWeightedCursedEffectOrder(player, {
        omenCategory = context.omenCategory,
    })
    for _, effectType in ipairs(effectTypes) do
        local fn = CURSE_EFFECT_HANDLERS[effectType]
        local result = fn and fn(player, nil, nil, context) or nil
        if result then
            BurdJournals.Server.recordAppliedCursedEffect(player, result.type)
            return finish(result)
        end
    end
    local panicResult = cursePanic(player, nil, nil)
    if panicResult then
        BurdJournals.Server.recordAppliedCursedEffect(player, panicResult.type)
    end
    return finish(panicResult)
end

function BurdJournals.Server.materializeCursedRewardJournal(player, sourceItem, rewardData, replacementFullType, allowSourceFallback)
    if not player or not sourceItem or type(rewardData) ~= "table" then
        return nil
    end

    local sourceModData = sourceItem:getModData()
    local sourceContainer = (sourceItem.getContainer and sourceItem:getContainer()) or nil
    local targetContainer = sourceContainer or (player and player.getInventory and player:getInventory()) or nil
    local inventory = player and player.getInventory and player:getInventory() or nil

    local targetFullType = type(replacementFullType) == "string" and replacementFullType ~= ""
        and replacementFullType
        or "BurdJournals.FilledSurvivalJournal_Bloody"
    local journal = nil
    if targetContainer and targetContainer.AddItem then
        journal = addJournalItemToContainerCompat(targetContainer, targetFullType)
    end
    if not journal and inventory and inventory ~= targetContainer then
        journal = addJournalItemToContainerCompat(inventory, targetFullType)
        if journal then
            targetContainer = inventory
        end
    end

    if not journal and allowSourceFallback == false then
        return nil
    end

    -- Keep the upgraded source item as a safe fallback so unexpected B41
    -- creation failures never strand or delete the player's journal.
    if not journal then
        sourceModData.BurdJournals = {}
        for key, value in pairs(rewardData) do
            sourceModData.BurdJournals[key] = value
        end
        if BurdJournals.updateJournalName then
            BurdJournals.updateJournalName(sourceItem, true)
        end
        if BurdJournals.updateJournalIcon then
            BurdJournals.updateJournalIcon(sourceItem)
        end
        if (isServer and isServer()) and sourceItem.transmitModData then
            sourceItem:transmitModData()
        end
        if targetContainer and targetContainer.setDrawDirty then
            targetContainer:setDrawDirty(true)
        end
        if inventory and inventory ~= targetContainer and inventory.setDrawDirty then
            inventory:setDrawDirty(true)
        end
        if player and player.syncInventory then
            BurdJournals.safePcall(function()
                player:syncInventory()
            end)
        end
        return sourceItem
    end

    local modData = journal:getModData()
    modData.BurdJournals = {}
    for key, value in pairs(rewardData) do
        modData.BurdJournals[key] = value
    end

    if BurdJournals.updateJournalName then
        BurdJournals.updateJournalName(journal, true)
    end
    if BurdJournals.updateJournalIcon then
        BurdJournals.updateJournalIcon(journal)
    end
    if targetContainer and targetContainer.setDrawDirty then
        targetContainer:setDrawDirty(true)
    end
    if targetContainer and sendAddItemToContainer then
        sendAddItemToContainer(targetContainer, journal)
    end
    if (isServer and isServer()) and journal.transmitModData then
        journal:transmitModData()
    end

    if sourceContainer and sourceContainer.Remove then
        sourceContainer:Remove(sourceItem)
        if sourceContainer.setDrawDirty then
            sourceContainer:setDrawDirty(true)
        end
        if sendRemoveItemFromContainer then
            sendRemoveItemFromContainer(sourceContainer, sourceItem)
        end
    end
    if inventory and inventory ~= sourceContainer and inventory.contains and inventory:contains(sourceItem) then
        inventory:Remove(sourceItem)
        if inventory.setDrawDirty then
            inventory:setDrawDirty(true)
        end
        if sendRemoveItemFromContainer then
            sendRemoveItemFromContainer(inventory, sourceItem)
        end
    end

    if player and player.syncInventory then
        BurdJournals.safePcall(function()
            player:syncInventory()
        end)
    end
    return journal
end

function BurdJournals.Server.applyCursedRewardDataToExistingJournal(journal, rewardData)
    if not journal or type(rewardData) ~= "table" then
        return false
    end

    local modData = journal:getModData()
    modData.BurdJournals = {}
    for key, value in pairs(rewardData) do
        modData.BurdJournals[key] = value
    end

    if BurdJournals.updateJournalName then
        BurdJournals.updateJournalName(journal, true)
    end
    if BurdJournals.updateJournalIcon then
        BurdJournals.updateJournalIcon(journal)
    end
    if journal.transmitModData and not BurdJournals.Server.shouldDeferBatchTransmit() then
            journal:transmitModData()
    end
    return true
end

function BurdJournals.Server.getHiddenCursedClaimBlockedMessage()
    return getCursedServerText("UI_BurdJournals_CursedHiddenClaimBlocked", "Open the journal first.")
end

function BurdJournals.Server.handleOpenCursedJournal(player, args)
    if not player or type(args) ~= "table" then
        BurdJournals.Server.sendToClient(player, "error", {message = "Invalid cursed journal request."})
        return
    end

    local journalId = tonumber(args.journalId)
    local journalUUID = normalizeCommandJournalUUID(args)
    local confirm = args.confirm == true
    if not journalId and not journalUUID then
        BurdJournals.Server.sendToClient(player, "error", {message = "Invalid cursed journal request."})
        return
    end

    local item, resolvedJournalId, resolvedJournalUUID = resolveServerCommandJournal(player, args, "openCursedJournal")
    if not item then
        BurdJournals.Server.sendToClient(player, "error", {message = "That cursed journal could not be found."})
        return
    end

    if not maybeMigrateRuntimeOnTouch(item, player, "openCursedJournal") then
        return
    end
    if not enforceJournalLightRequirement(player, "openCursedJournal") then
        return
    end
    local fullType = item.getFullType and item:getFullType() or ""
    local modData = item:getModData()
    modData.BurdJournals = modData.BurdJournals or {}
    local data = modData.BurdJournals
    normalizeCursedJournalStateForType(item, "openCursedJournal")
    local isHiddenCursed = isHiddenCursedJournalState(item, data)
    resolvedJournalUUID = resolvedJournalUUID
        or (BurdJournals.resolveJournalUUIDForRuntime and BurdJournals.resolveJournalUUIDForRuntime(data, item, true))
        or data.uuid

    if fullType ~= (BurdJournals.CURSED_ITEM_TYPE or "BurdJournals.CursedJournal") then
        if fullType == "BurdJournals.FilledSurvivalJournal_Bloody" and data.isCursedReward == true then
            local revealedExistingReward = markLootRewardsRevealed(player, item, data)
            if revealedExistingReward and item.transmitModData then
                item:transmitModData()
            end
            BurdJournals.Server.sendToClient(player, "cursedOpened", {
                journalId = item:getID(),
                journalUUID = resolvedJournalUUID,
                journalData = data,
                curseType = data.cursedEffectType or "none",
                curseMessage = getCursedServerText("UI_BurdJournals_CursedAlreadyUnleashed", "The curse has already taken its price."),
                soundEvent = nil,
            })
            return
        end
        if isHiddenCursed ~= true then
            BurdJournals.Server.sendToClient(player, "error", {message = "That item is not a cursed journal."})
            return
        end
    end

    if data.isCursedReward == true and data.cursedState == "unleashed" then
        BurdJournals.Server.sendToClient(player, "cursedOpened", {
            journalId = item:getID(),
            journalUUID = resolvedJournalUUID,
            journalData = data,
            curseType = data.cursedEffectType or "none",
            curseMessage = getCursedServerText("UI_BurdJournals_CursedAlreadyUnleashed", "The curse has already taken its price."),
            soundEvent = nil,
        })
        return
    end

    if not confirm and isHiddenCursed ~= true then
        local journalFingerprint = BurdJournals.buildJournalLookupFingerprint
            and BurdJournals.buildJournalLookupFingerprint(item, data)
            or nil
        local cursedInsightLevel = BurdJournals.getCursedInsightLevel
            and select(1, BurdJournals.getCursedInsightLevel(player))
            or 0
        local expirationInfo = BurdJournals.Server.getCursedExpirationInfo(data)
        local preview = BurdJournals.Server.getCachedCursedInsightPreview(player, resolvedJournalUUID, resolvedJournalId or journalId)
        if preview and tonumber(preview.insightLevel or 0) < cursedInsightLevel then
            preview = nil
        end
        if not preview and BurdJournals.Server.buildCursedInsightPreview then
            preview = BurdJournals.Server.buildCursedInsightPreview(
                player,
                item,
                data,
                resolvedJournalId or (item.getID and item:getID()) or journalId,
                resolvedJournalUUID
            )
        end
        local omenCategory = preview and type(preview.omenCategory) == "string" and preview.omenCategory
            or (type(data.cursedOmenCategory) == "string" and data.cursedOmenCategory or nil)
        local insightEffectType = preview and type(preview.effectType) == "string" and preview.effectType
            or normalizeCurseEffectType(data.cursedInsightEffectType)
            or normalizeCurseEffectType(data.cursedForcedEffectType)
        local promptBoundPreview = false
        if cursedInsightLevel >= 3 and not insightEffectType and not (expirationInfo and expirationInfo.expired) then
            insightEffectType = BurdJournals.Server.rollCursedInsightEffectType(player, omenCategory)
            if insightEffectType then
                data.cursedInsightEffectType = insightEffectType
                promptBoundPreview = true
            end
        end
        if insightEffectType and not omenCategory and BurdJournals.Server.getCurseEffectOmenCategory then
            omenCategory = BurdJournals.Server.getCurseEffectOmenCategory(insightEffectType)
            if omenCategory and data.cursedOmenCategory ~= omenCategory then
                data.cursedOmenCategory = omenCategory
                promptBoundPreview = true
            end
        end
        if promptBoundPreview then
            BurdJournals.Server.storeCursedInsightPreview(player, {
                journalId = (item.getID and item:getID()) or resolvedJournalId or journalId,
                journalUUID = resolvedJournalUUID,
                insightLevel = cursedInsightLevel,
                sealedCursed = true,
                omenCategory = omenCategory,
                effectType = insightEffectType,
                curseExpired = expirationInfo and expirationInfo.expired == true or nil,
                curseAgeDays = expirationInfo and expirationInfo.ageDays or nil,
                curseExpirationDays = expirationInfo and expirationInfo.thresholdDays or nil,
            })
            if item.transmitModData then
                item:transmitModData()
            end
        end
        local omenLine = nil
        if expirationInfo and expirationInfo.expired then
            omenLine = BurdJournals.Server.getCursedOmenLine(nil, true)
        else
            local canReadOmen = BurdJournals.Server.shouldShowCursedOmenForInsight(cursedInsightLevel)
            if not omenCategory and canReadOmen then
                local rolled = BurdJournals.Server.rollCursedOmenCategory(player)
                if rolled then
                    omenCategory = rolled
                    data.cursedOmenCategory = rolled
                    if item.transmitModData then item:transmitModData() end
                end
            end
            omenLine = BurdJournals.Server.getCursedInsightLine(
                cursedInsightLevel,
                omenCategory,
                insightEffectType,
                false,
                false
            )
            if not omenLine and canReadOmen then
                omenLine = BurdJournals.Server.getCursedOmenLine(omenCategory, false)
            end
        end

        BurdJournals.Server.sendToClient(player, "cursedOpenPrompt", {
            journalId = (item.getID and item:getID()) or resolvedJournalId or journalId,
            journalUUID = resolvedJournalUUID,
            journalFingerprint = journalFingerprint,
            exactJournalItem = true,
            loreLine = getCursedServerText("UI_BurdJournals_CursedPromptLore", "Ink writhes across the page. Something waits beneath these words."),
            consequenceLine = getCursedServerText("UI_BurdJournals_CursedPromptConsequence", "The first soul to read it will be marked."),
            omenLine = omenLine,
            omenCategory = omenCategory,
            cursedInsightLevel = cursedInsightLevel,
            cursedInsightEffectType = insightEffectType,
            curseExpired = expirationInfo and expirationInfo.expired == true or nil,
            curseAgeDays = expirationInfo and expirationInfo.ageDays or nil,
            curseExpirationDays = expirationInfo and expirationInfo.thresholdDays or nil,
        })
        return
    end

    -- Authoritative state re-check prevents double-curse races.
    local liveItem, liveJournalId, liveJournalUUID = resolveServerCommandJournal(player, {
        journalId = resolvedJournalId or journalId,
        journalUUID = resolvedJournalUUID or journalUUID,
        journalFingerprint = args.journalFingerprint,
        exactJournalItem = args.exactJournalItem == true,
    }, "openCursedJournalConfirm")
    if not liveItem then
        BurdJournals.Server.sendToClient(player, "error", {message = "That cursed journal could not be found."})
        return
    end
    local liveModData = liveItem:getModData()
    liveModData.BurdJournals = liveModData.BurdJournals or {}
    local liveData = liveModData.BurdJournals
    if not maybeMigrateRuntimeOnTouch(liveItem, player, "openCursedJournalConfirm") then
        return
    end
    if not enforceJournalLightRequirement(player, "openCursedJournalConfirm") then
        return
    end
    normalizeCursedJournalStateForType(liveItem, "openCursedJournalConfirm")
    local liveHiddenCursed = isHiddenCursedJournalState(liveItem, liveData)
    local liveCursedInsightLevel = BurdJournals.getCursedInsightLevel and select(1, BurdJournals.getCursedInsightLevel(player)) or 0
    liveJournalUUID = liveJournalUUID
        or (BurdJournals.resolveJournalUUIDForRuntime and BurdJournals.resolveJournalUUIDForRuntime(liveData, liveItem, true))
        or liveData.uuid
    if liveData.isCursedReward == true and liveData.cursedState == "unleashed" then
        local revealedLiveReward = markLootRewardsRevealed(player, liveItem, liveData)
        if revealedLiveReward and liveItem.transmitModData then
            liveItem:transmitModData()
        end
        BurdJournals.Server.sendToClient(player, "cursedOpened", {
            journalId = liveJournalId or (liveItem.getID and liveItem:getID()) or nil,
            journalUUID = liveJournalUUID,
            journalData = liveData,
            curseType = liveData.cursedEffectType or "none",
            curseMessage = getCursedServerText("UI_BurdJournals_CursedAlreadyUnleashed", "The curse has already taken its price."),
            soundEvent = nil,
        })
        return
    end

    local cachedInsightPreview = BurdJournals.Server.getCachedCursedInsightPreview(player, liveJournalUUID, liveJournalId)
    if cachedInsightPreview
        and tonumber(cachedInsightPreview.insightLevel or 0) ~= liveCursedInsightLevel
    then
        cachedInsightPreview = nil
    end

    local forcedCurseType = normalizeCurseEffectType(liveData.cursedForcedEffectType)
        or (cachedInsightPreview and normalizeCurseEffectType(cachedInsightPreview.effectType))
        or normalizeCurseEffectType(liveData.cursedInsightEffectType)
    local forcedCurseTraitId = normalizeForcedTraitId(liveData.cursedForcedTraitId)
    local forcedCurseSkillName = normalizeForcedSkillName(liveData.cursedForcedSkillName)
    -- Binding omen: constrain the roll to the pre-rolled omen category.
    -- Hidden journals stay
    -- omen-free (no stored category), preserving their surprise identity.
    local liveExpirationInfo = BurdJournals.Server.getCursedExpirationInfo(liveData)
    local curseContext = {
        journalItem = liveItem,
        journalData = liveData,
        curseExpired = liveExpirationInfo and liveExpirationInfo.expired == true or nil,
        curseAgeDays = liveExpirationInfo and liveExpirationInfo.ageDays or nil,
        curseExpirationDays = liveExpirationInfo and liveExpirationInfo.thresholdDays or nil,
    }
    local previewOmenCategory = cachedInsightPreview
        and type(cachedInsightPreview.omenCategory) == "string"
        and cachedInsightPreview.omenCategory
        or nil
    if previewOmenCategory then
        curseContext.omenCategory = previewOmenCategory
    elseif BurdJournals.Server.shouldShowCursedOmenForInsight(liveCursedInsightLevel)
        and type(liveData.cursedOmenCategory) == "string"
    then
        curseContext.omenCategory = liveData.cursedOmenCategory
    end
    local curseResult = nil
    if liveExpirationInfo and liveExpirationInfo.expired == true then
        curseResult = {
            type = "expired",
            expired = true,
            skipped = true,
            message = getCursedServerText("UI_BurdJournals_CursedMsgExpired", "The seal breaks, but the curse has already burned out."),
            ageDays = liveExpirationInfo.ageDays,
            thresholdDays = liveExpirationInfo.thresholdDays,
        }
    else
        curseResult = BurdJournals.Server.applyCursedEffect(
            player,
            forcedCurseType,
            forcedCurseTraitId,
            forcedCurseSkillName,
            curseContext
        )
    end
    local ambushNoiseBase = nil
    local ambushNoiseRadiusApplied = nil
    local ambushNoiseVolumeApplied = nil
    if curseResult and curseResult.type == "panic" then
        ambushNoiseBase = getCursedAmbushNoiseRadius()
        local pullRadius, pullVolume, nudgeRadius = getCursedAmbushPullProfile()
        ambushNoiseRadiusApplied = pullRadius
        ambushNoiseVolumeApplied = pullVolume
        if pullRadius > 0 then
            -- Multi-pulse AI noise + direct path nudges improve ambush reliability for freshly spawned zombies.
            emitCursedSilentAIPull(player, pullRadius, pullVolume)
            nudgeCursedAmbushZombiesToward(player, nudgeRadius)
            nudgeCursedAmbushZombiesTowardDelayed(player, nudgeRadius, 900)
            nudgeCursedAmbushZombiesTowardDelayed(player, nudgeRadius, 1700)
            emitCursedSilentAIPullDelayed(player, pullRadius, pullVolume, 650)
            emitCursedSilentAIPullDelayed(player, pullRadius, pullVolume, 1250)
            emitCursedSilentAIPullDelayed(player, pullRadius, pullVolume, 1850)
        end
    end

    local rewardSeed = nil
    if type(liveData.cursedPendingRewards) == "table" then
        rewardSeed = BurdJournals.normalizeJournalData and BurdJournals.normalizeJournalData(liveData.cursedPendingRewards) or liveData.cursedPendingRewards
    end
    rewardSeed = rewardSeed or {}
    rewardSeed.uuid = rewardSeed.uuid or liveData.uuid
    rewardSeed.author = rewardSeed.author or liveData.author
    rewardSeed.profession = rewardSeed.profession or liveData.profession
    rewardSeed.professionName = rewardSeed.professionName or liveData.professionName
    rewardSeed.flavorKey = rewardSeed.flavorKey or liveData.flavorKey
    rewardSeed.flavorText = rewardSeed.flavorText or liveData.flavorText
    rewardSeed.loreNoteText = rewardSeed.loreNoteText or liveData.loreNoteText
    rewardSeed.loreNoteTemplateVersion = rewardSeed.loreNoteTemplateVersion or liveData.loreNoteTemplateVersion
    rewardSeed.loreNoteTemplateFamily = rewardSeed.loreNoteTemplateFamily or liveData.loreNoteTemplateFamily
    rewardSeed.lootNotesEligible = rewardSeed.lootNotesEligible == true or liveData.lootNotesEligible == true
    rewardSeed.timestamp = rewardSeed.timestamp or liveData.timestamp
    rewardSeed.cursedSealSoundEvent = BurdJournals.Server.normalizeCursedSealSoundEvent(liveData.cursedSealSoundEvent)
    -- Severity compensation: tell the profile generator how harsh the applied curse was.
    rewardSeed.curseCategory = curseResult and curseResult.type
        and BurdJournals.Server.getCursedEffectCategory(curseResult.type)
        or nil

    local rewards = BurdJournals.Server.generateCursedRewardProfile(rewardSeed)
    if tonumber(rewards.loreNoteTemplateVersion) == LORE_DYNAMIC_VERSION
        and not BurdJournals.Server.normalizeJournalServerText(rewards.loreNoteText)
    then
        local loreNoteText = nil
        local openerName = nil
        if BurdJournals.Server.buildCursedLoreNote then
            loreNoteText, openerName, rewards.loreNoteTemplateFamily = BurdJournals.Server.buildCursedLoreNote(player, rewards)
        end
        rewards.loreNoteText = loreNoteText
        rewards.cursedUnleashedByName = openerName
    end
    rewards.isCursedJournal = false
    rewards.isHiddenCursedJournal = false
    rewards.cursedState = "unleashed"
    rewards.isCursedReward = true
    rewards.cursedEffectType = curseResult and curseResult.type or "panic"
    rewards.cursedUnleashedByCharacterId = BurdJournals.getPlayerCharacterId and BurdJournals.getPlayerCharacterId(player) or nil
    rewards.cursedUnleashedByUsername = player.getUsername and player:getUsername() or nil
    rewards.cursedUnleashedByName = rewards.cursedUnleashedByName
        or BurdJournals.Server.getJournalPlayerDisplayName(player)
    rewards.cursedUnleashedAtHours = getGameTime() and getGameTime():getWorldAgeHours() or 0
    if curseResult and curseResult.expired == true then
        rewards.curseExpired = true
        rewards.cursedAgeDaysAtOpen = curseResult.ageDays
        rewards.cursedExpirationDays = curseResult.thresholdDays
        rewards.cursedSourceTimestamp = liveData.timestamp or liveData.cursedSpawnedAtHours
    end
    rewards.cursedForcedEffectType = nil
    rewards.cursedForcedTraitId = nil
    rewards.cursedForcedSkillName = nil
    rewards.cursedInsightEffectType = nil
    rewards.cursedPendingRewards = nil

    local rewardJournal = nil
    local liveFullType = liveItem.getFullType and tostring(liveItem:getFullType() or "") or ""
    local canReuseHiddenCursedItem = liveHiddenCursed
        and liveFullType ~= (BurdJournals.CURSED_ITEM_TYPE or "BurdJournals.CursedJournal")
    if canReuseHiddenCursedItem then
        if not BurdJournals.Server.applyCursedRewardDataToExistingJournal(liveItem, rewards) then
            BurdJournals.Server.sendToClient(player, "error", {message = "Failed to materialize cursed rewards."})
            return
        end
        rewardJournal = liveItem
    else
        rewardJournal = BurdJournals.Server.materializeCursedRewardJournal(player, liveItem, rewards)
    end
    if not rewardJournal then
        BurdJournals.Server.sendToClient(player, "error", {message = "Failed to materialize cursed rewards."})
        return
    end

    local revealedReward = markLootRewardsRevealed(player, rewardJournal, rewards)
    if revealedReward and rewardJournal.transmitModData then
        rewardJournal:transmitModData()
    end

    if rewardJournal ~= liveItem then
        -- B41 inventory add/remove replication can arrive out of order, so send an
        -- explicit replacement notice to swap the sealed item for the bloody reward.
        BurdJournals.Server.sendToClient(player, "journalMaterialized", {
            oldJournalId = liveJournalId or (liveItem.getID and liveItem:getID()) or nil,
            oldJournalUUID = liveJournalUUID,
            newJournalId = rewardJournal.getID and rewardJournal:getID() or nil,
            journalUUID = rewards.uuid or liveJournalUUID,
            journalData = rewards,
            source = "openCursedJournal",
            exactOldJournalItem = true,
        })
        -- Add-on hook: a new reward journal item replaced the sealed one.
        if BurdJournals.runHooks then
            BurdJournals.runHooks("OnJournalMaterialized", player, liveItem, rewardJournal, rewards)
        end
    end

    local revealLead = nil
    local sealSoundEvent = nil
    if liveHiddenCursed then
        revealLead = getCursedServerText("UI_BurdJournals_CursedHiddenRevealLead", "The page turns. Something answers.")
    else
        sealSoundEvent = BurdJournals.Server.normalizeCursedSealSoundEvent(liveData.cursedSealSoundEvent)
        if sealSoundEvent == "none" then
            sealSoundEvent = nil
        end
        if not sealSoundEvent or sealSoundEvent == "" then
            if BurdJournals.getRandomCursedSealSoundEvent then
                sealSoundEvent = BurdJournals.getRandomCursedSealSoundEvent()
            end
        end
        if not sealSoundEvent or sealSoundEvent == "" then
            sealSoundEvent = BurdJournals.CURSED_DEFAULT_SOUND_EVENT or "PaperRip"
        end
    end

    -- Always emit a world sound when the seal breaks so nearby zombies react.
    emitCursedSealWorldSound(player, CURSED_SEAL_WORLD_SOUND_RADIUS, CURSED_SEAL_WORLD_SOUND_VOLUME)

    -- Add-on hook: the cursed journal has been fully opened and rewards materialized.
    if BurdJournals.runHooks then
        BurdJournals.runHooks("OnCursedJournalOpened", player, rewardJournal, rewards, curseResult)
    end

    BurdJournals.Server.sendToClient(player, "cursedOpened", {
        journalId = rewardJournal:getID(),
        journalUUID = rewards.uuid or liveJournalUUID,
        journalData = rewards,
        curseType = rewards.cursedEffectType or "panic",
        curseMessage = curseResult and curseResult.message or getCursedServerText("UI_BurdJournals_CursedMsgPanic", "A curse takes hold..."),
        curseExpired = curseResult and curseResult.expired == true or nil,
        curseAgeDays = curseResult and curseResult.ageDays or nil,
        curseExpirationDays = curseResult and curseResult.thresholdDays or nil,
        compatEffect = curseResult and curseResult.compatEffect or nil,
        soundEvent = sealSoundEvent,
        revealLead = revealLead,
        focusText = curseResult and curseResult.focusText or nil,
        focusType = curseResult and curseResult.focusType or nil,
        ambushNoiseRadius = ambushNoiseBase,
        ambushNoiseRadiusApplied = ambushNoiseRadiusApplied,
        ambushNoiseVolumeApplied = ambushNoiseVolumeApplied,
    })
end

function BurdJournals.Server.handleOpenYuletideJournal(player, args)
    if not player or type(args) ~= "table" then
        BurdJournals.Server.sendToClient(player, "error", {message = "Invalid Yuletide journal request."})
        return
    end

    local journalId = tonumber(args.journalId)
    local journalUUID = normalizeCommandJournalUUID(args)
    local confirm = args.confirm == true
    if not journalId and not journalUUID then
        BurdJournals.Server.sendToClient(player, "error", {message = "Invalid Yuletide journal request."})
        return
    end

    local item, resolvedJournalId, resolvedJournalUUID = resolveServerCommandJournal(player, args, "openYuletideJournal")
    if not item then
        BurdJournals.Server.sendToClient(player, "error", {message = "That Yuletide journal could not be found."})
        return
    end

    if not maybeMigrateRuntimeOnTouch(item, player, "openYuletideJournal") then
        return
    end
    if not enforceJournalLightRequirement(player, "openYuletideJournal") then
        return
    end
    local fullType = item.getFullType and item:getFullType() or ""
    local data = normalizeYuletideJournalStateForType(item, "openYuletideJournal")
    local healedPromptData = BurdJournals.Server.ensureYuletideJournalProfileData and BurdJournals.Server.ensureYuletideJournalProfileData(data)
    if fullType ~= (BurdJournals.YULETIDE_ITEM_TYPE or "BurdJournals.YuletideJournal")
        and not (type(data) == "table" and data.isYuletideJournal == true)
    then
        BurdJournals.Server.sendToClient(player, "error", {message = "That item is not a Yuletide journal."})
        return
    end

    resolvedJournalUUID = resolvedJournalUUID or getYuletideJournalUUID(item, data)
    if type(data) == "table" and data.yuletideState == BurdJournals.YULETIDE_STATE_UNWRAPPED then
        local recoveredGifts = {}
        local shouldRecoverGifts = not hasVerifiedYuletideGiftGrant(data)
        if shouldRecoverGifts then
            recoveredGifts = BurdJournals.Server.grantYuletideImmediateGifts(player, item, data)
        end
        local promptGifts = (#recoveredGifts > 0)
            and recoveredGifts
            or (shouldRecoverGifts and {} or sanitizeYuletideGiftList(data.yuletideImmediateGifts))
        local revealedPromptData = markLootRewardsRevealed(player, item, data)
        if (healedPromptData or revealedPromptData) and item.transmitModData then
            item:transmitModData()
        end
        BurdJournals.Server.sendToClient(player, "yuletideOpened", {
            journalId = item:getID(),
            journalUUID = resolvedJournalUUID,
            journalFingerprint = BurdJournals.buildJournalLookupFingerprint
                and BurdJournals.buildJournalLookupFingerprint(item, data)
                or nil,
            journalData = data,
            giftTier = data.yuletideGiftTier or "practical",
            gifts = promptGifts,
            clientMaterializeGifts = data.yuletidePendingClientGiftGrant == true and #promptGifts > 0,
            giftGrantToken = data.yuletideGiftGrantToken,
            messageKey = "UI_BurdJournals_YuletideAlreadyOpened",
            message = getYuletideServerText("UI_BurdJournals_YuletideAlreadyOpened", "The wrapping is already gone."),
        })
        return
    end

    if not confirm then
        local journalFingerprint = BurdJournals.buildJournalLookupFingerprint
            and BurdJournals.buildJournalLookupFingerprint(item, data)
            or nil
        BurdJournals.Server.sendToClient(player, "yuletideOpenPrompt", {
            journalId = (item.getID and item:getID()) or resolvedJournalId or journalId,
            journalUUID = resolvedJournalUUID,
            journalFingerprint = journalFingerprint,
            exactJournalItem = true,
            loreLine = getYuletideServerText(
                "UI_BurdJournals_YuletidePromptLore",
                "The wrapping is neat, the paper still bright. A gift waits inside."
            ),
            consequenceLine = getYuletideServerText(
                "UI_BurdJournals_YuletidePromptConsequence",
                "Unwrapping it will reveal the journal and its bundled supplies."
            ),
        })
        return
    end

    local liveItem, liveJournalId, liveJournalUUID = resolveServerCommandJournal(player, {
        journalId = resolvedJournalId or journalId,
        journalUUID = resolvedJournalUUID or journalUUID,
        journalFingerprint = args.journalFingerprint,
        exactJournalItem = args.exactJournalItem == true,
    }, "openYuletideJournalConfirm")
    if not liveItem then
        BurdJournals.Server.sendToClient(player, "error", {message = "That Yuletide journal could not be found."})
        return
    end

    if not maybeMigrateRuntimeOnTouch(liveItem, player, "openYuletideJournalConfirm") then
        return
    end
    if not enforceJournalLightRequirement(player, "openYuletideJournalConfirm") then
        return
    end
    local liveData = normalizeYuletideJournalStateForType(liveItem, "openYuletideJournalConfirm")
    local healedLiveData = BurdJournals.Server.ensureYuletideJournalProfileData and BurdJournals.Server.ensureYuletideJournalProfileData(liveData)
    liveJournalUUID = liveJournalUUID or getYuletideJournalUUID(liveItem, liveData)
    if type(liveData) ~= "table" or liveData.isYuletideJournal ~= true then
        BurdJournals.Server.sendToClient(player, "error", {message = "That item is not a Yuletide journal."})
        return
    end
    if liveData.yuletideState == BurdJournals.YULETIDE_STATE_UNWRAPPED then
        local recoveredLiveGifts = {}
        local shouldRecoverLiveGifts = not hasVerifiedYuletideGiftGrant(liveData)
        if shouldRecoverLiveGifts then
            recoveredLiveGifts = BurdJournals.Server.grantYuletideImmediateGifts(player, liveItem, liveData)
        end
        local livePromptGifts = (#recoveredLiveGifts > 0)
            and recoveredLiveGifts
            or (shouldRecoverLiveGifts and {} or sanitizeYuletideGiftList(liveData.yuletideImmediateGifts))
        local revealedLiveData = markLootRewardsRevealed(player, liveItem, liveData)
        if (healedLiveData or revealedLiveData) and liveItem.transmitModData then
            liveItem:transmitModData()
        end
        BurdJournals.Server.sendToClient(player, "yuletideOpened", {
            journalId = liveJournalId or (liveItem.getID and liveItem:getID()) or nil,
            journalUUID = liveJournalUUID,
            journalFingerprint = BurdJournals.buildJournalLookupFingerprint
                and BurdJournals.buildJournalLookupFingerprint(liveItem, liveData)
                or nil,
            journalData = liveData,
            giftTier = liveData.yuletideGiftTier or "practical",
            gifts = livePromptGifts,
            clientMaterializeGifts = liveData.yuletidePendingClientGiftGrant == true and #livePromptGifts > 0,
            giftGrantToken = liveData.yuletideGiftGrantToken,
            messageKey = "UI_BurdJournals_YuletideAlreadyOpened",
            message = getYuletideServerText("UI_BurdJournals_YuletideAlreadyOpened", "The wrapping is already gone."),
        })
        return
    end

    local grantedGifts = {}
    if not hasVerifiedYuletideGiftGrant(liveData) then
        grantedGifts = BurdJournals.Server.grantYuletideImmediateGifts(player, liveItem, liveData)
    end
    local unwrappedPayload = BurdJournals.normalizeJournalData and BurdJournals.normalizeJournalData(liveData) or nil
    if type(unwrappedPayload) ~= "table" then
        BurdJournals.Server.sendToClient(player, "error", {message = "Failed to prepare the Yuletide journal."})
        return
    end
    if tonumber(unwrappedPayload.loreNoteTemplateVersion) == LORE_DYNAMIC_VERSION
        and not BurdJournals.Server.normalizeJournalServerText(unwrappedPayload.loreNoteText)
    then
        local loreNoteText = nil
        local openerName = nil
        if BurdJournals.Server.buildYuletideLoreNote then
            loreNoteText, openerName, unwrappedPayload.loreNoteTemplateFamily = BurdJournals.Server.buildYuletideLoreNote(player, unwrappedPayload)
        end
        unwrappedPayload.loreNoteText = loreNoteText
        unwrappedPayload.yuletideOpenedByName = openerName
    end
    unwrappedPayload.yuletideOpenedByName = unwrappedPayload.yuletideOpenedByName
        or BurdJournals.Server.getJournalPlayerDisplayName(player)
    unwrappedPayload.yuletideState = BurdJournals.YULETIDE_STATE_UNWRAPPED
    unwrappedPayload.yuletidePendingDelivery = false
    markLootRewardsRevealed(player, liveItem, unwrappedPayload)

    unwrappedPayload.yuletideBeacon = nil

    local unwrappedItem = BurdJournals.Server.materializeCursedRewardJournal(
        player,
        liveItem,
        unwrappedPayload,
        BurdJournals.YULETIDE_ITEM_TYPE or "BurdJournals.YuletideJournal",
        false
    )
    if not unwrappedItem then
        BurdJournals.Server.sendToClient(player, "error", {message = "Failed to unwrap the Yuletide journal."})
        return
    end
    local store = BurdJournals.Server.getYuletideDeliveryStore()
    if store and clearYuletideBeacon(store, liveJournalUUID) then
        BurdJournals.Server.transmitYuletideDeliveryStore()
    end
    local unwrappedModData = unwrappedItem.getModData and unwrappedItem:getModData() or nil
    local unwrappedData = unwrappedModData and unwrappedModData.BurdJournals or unwrappedPayload
    if unwrappedItem ~= liveItem then
        BurdJournals.Server.sendToClient(player, "journalMaterialized", {
            oldJournalId = liveJournalId or (liveItem.getID and liveItem:getID()) or nil,
            oldJournalUUID = liveJournalUUID,
            newJournalId = unwrappedItem.getID and unwrappedItem:getID() or nil,
            journalUUID = liveJournalUUID,
            journalData = unwrappedData,
            source = "openYuletideJournal",
            exactOldJournalItem = true,
        })
    end

    BurdJournals.Server.sendToClient(player, "yuletideOpened", {
        journalId = unwrappedItem.getID and unwrappedItem:getID() or liveJournalId,
        journalUUID = liveJournalUUID,
        journalFingerprint = BurdJournals.buildJournalLookupFingerprint
            and BurdJournals.buildJournalLookupFingerprint(unwrappedItem, unwrappedData)
            or nil,
        journalData = unwrappedData,
        exactJournalItem = true,
        giftTier = unwrappedData.yuletideGiftTier or "practical",
        gifts = grantedGifts,
        clientMaterializeGifts = unwrappedData.yuletidePendingClientGiftGrant == true and #grantedGifts > 0,
        giftGrantToken = unwrappedData.yuletideGiftGrantToken,
        messageKey = "UI_BurdJournals_YuletideOpened",
        message = getYuletideServerText("UI_BurdJournals_YuletideOpened", "You unwrap the gift and find a journal inside."),
        soundEvent = (BurdJournals.getRandomYuletideUnwrapSoundEvent and BurdJournals.getRandomYuletideUnwrapSoundEvent())
            or BurdJournals.YULETIDE_DEFAULT_UNWRAP_SOUND_EVENT
            or BurdJournals.YULETIDE_UNWRAP_SOUND_EVENT
            or "PaperRip",
    })
end

-- =====================================================================
-- Generic sealed-journal "break the seal" handler.
-- ---------------------------------------------------------------------
-- Serves any journal registered via BurdJournals.registerSealedJournalType.
-- Flips sealedState sealed -> broken, reveals hidden reward contents, fires
-- the "OnJournalSealBroken" hook (add-ons inject their unseal payload here,
-- e.g. Curses & Blessings applies blessings), then refreshes name/icon and
-- notifies the client. Idempotent: re-breaking an already-broken seal just
-- re-sends the "opened" acknowledgement without re-firing the payload hook.
-- =====================================================================
function BurdJournals.Server.handleBreakJournalSeal(player, args)
    if not player or type(args) ~= "table" then
        BurdJournals.Server.sendToClient(player, "error", {message = "Invalid sealed journal request."})
        return
    end

    local item = resolveServerCommandJournal(player, args, "breakJournalSeal")
    if not item then
        BurdJournals.Server.sendToClient(player, "error", {message = "That sealed journal could not be found."})
        return
    end

    if not maybeMigrateRuntimeOnTouch(item, player, "breakJournalSeal") then
        return
    end
    if not enforceJournalLightRequirement(player, "breakJournalSeal") then
        return
    end

    local sealedEntry, data = BurdJournals.getSealedJournalType(item)
    if sealedEntry and type(data) ~= "table" and item.getModData then
        local md = item:getModData()
        md.BurdJournals = type(md.BurdJournals) == "table" and md.BurdJournals or {}
        data = md.BurdJournals
        data.journalTypeId = data.journalTypeId or sealedEntry.fullType
        data.journalKind = data.journalKind or "reward"
        data.sourceType = data.sourceType or "loot"
        data.originMode = data.originMode or "loot"
        data.sealedState = data.sealedState or BurdJournals.SEALED_STATE_SEALED
        data.sealedTypeKey = data.sealedTypeKey or sealedEntry.fullType
        data.lootRewardsRevealed = data.lootRewardsRevealed == true
        data.isPlayerCreated = false
        if type(sealedEntry.dataMarker) == "string" and sealedEntry.dataMarker ~= "" then
            data[sealedEntry.dataMarker] = true
        end
    end
    if not sealedEntry or type(data) ~= "table" then
        BurdJournals.Server.sendToClient(player, "error", {message = "That item is not a sealed journal."})
        return
    end

    local journalUUID = nil
    if BurdJournals.resolveJournalUUIDForRuntime then
        journalUUID = BurdJournals.resolveJournalUUIDForRuntime(data, item, true)
    end
    journalUUID = journalUUID or data.uuid

    local alreadyBroken = data.sealedState == BurdJournals.SEALED_STATE_BROKEN

    if not alreadyBroken then
        -- Flip the seal and reveal any concealed reward contents.
        BurdJournals.setSealedState(item, BurdJournals.SEALED_STATE_BROKEN)
        markLootRewardsRevealed(player, item, data)
        data.sealBrokenByName = data.sealBrokenByName
            or BurdJournals.Server.getJournalPlayerDisplayName(player)

        -- Add-on payload injection point. Handlers receive everything needed to
        -- mutate the live item's journalData and apply effects to the player.
        BurdJournals.runHooks("OnJournalSealBroken", player, item, data, sealedEntry)

        BurdJournals.updateJournalName(item, true)
        BurdJournals.updateJournalIcon(item)
        if item.transmitModData then
            item:transmitModData()
        end
    end

    BurdJournals.Server.sendToClient(player, "journalSealBroken", {
        journalId = item.getID and item:getID() or nil,
        journalUUID = journalUUID,
        journalFingerprint = BurdJournals.buildJournalLookupFingerprint
            and BurdJournals.buildJournalLookupFingerprint(item, data)
            or nil,
        journalData = data,
        sealedTypeKey = sealedEntry.fullType,
        themeKey = sealedEntry.themeKey,
        alreadyBroken = alreadyBroken,
        breakCommand = sealedEntry.breakCommand,
    })
end

function BurdJournals.Server.handleYuletideGiftMaterialized(player, args)
    if not player or type(args) ~= "table" then
        return
    end

    local item = resolveServerCommandJournal(player, args, "yuletideGiftMaterialized")
    if not item then
        BurdJournals.debugPrint("[BurdJournals] Yuletide gift materialize ack ignored because journal could not be found.")
        return
    end
    local data = normalizeYuletideJournalStateForType(item, "yuletideGiftMaterialized")
    if type(data) ~= "table" or data.isYuletideJournal ~= true then
        return
    end

    local expectedToken = tostring(data.yuletideGiftGrantToken or "")
    local receivedToken = tostring(args.giftGrantToken or "")
    if expectedToken == "" or receivedToken == "" or expectedToken ~= receivedToken then
        BurdJournals.debugPrint("[BurdJournals] Yuletide gift materialize ack rejected for token mismatch.")
        return
    end

    if data.yuletidePendingClientGiftGrant == true then
        data.yuletideGiftGranted = true
        data.yuletideGiftGrantVersion = YULETIDE_GIFT_GRANT_VERSION
        data.yuletidePendingClientGiftGrant = nil
        data.yuletidePendingClientGifts = nil
        if item.transmitModData then
            item:transmitModData()
        end
        BurdJournals.debugPrint("[BurdJournals] Yuletide client gift grant acknowledged for "
            .. tostring(getYuletideJournalUUID(item, data)))
    end
end

-- Server-side function to get skill baseline - checks server cache first, then player modData
function BurdJournals.Server.getSkillBaselineForPlayer(player, skillName)
    if not player or not skillName then return 0 end

    local cacheXP = nil
    -- First, try to get baseline from server cache (more reliable on dedicated servers)
    local characterId = BurdJournals.getPlayerCharacterId(player)
    if characterId then
        local cachedBaseline = BurdJournals.Server.getCachedBaseline(characterId, player)
        if cachedBaseline and cachedBaseline.skillBaseline then
            local xp = cachedBaseline.skillBaseline[skillName]
            if xp then
                cacheXP = math.max(0, tonumber(xp) or 0)
            end
        end
    end

    local storedBaselineXP = 0
    local modData = nil
    if player.getModData then
        modData = player:getModData()
    end
    if modData and modData.BurdJournals and modData.BurdJournals.skillBaseline then
        storedBaselineXP = math.max(0, tonumber(modData.BurdJournals.skillBaseline[skillName]) or 0)
    end

    local resolvedBaselineXP = cacheXP ~= nil and cacheXP or storedBaselineXP
    if resolvedBaselineXP > 0 then
        if cacheXP ~= nil then
            BurdJournals.debugPrint("[BurdJournals] Server: Got baseline for " .. skillName .. " from SERVER CACHE: " .. tostring(cacheXP))
        elseif storedBaselineXP > 0 then
            BurdJournals.debugPrint("[BurdJournals] Server: Got baseline for " .. skillName .. " from player/shared baseline: " .. tostring(storedBaselineXP))
        end
    end
    return resolvedBaselineXP
end

function BurdJournals.Server.getMediaSkillBaselineForPlayer(player, skillName)
    if not player or not skillName then return 0 end

    local characterId = BurdJournals.getPlayerCharacterId(player)
    if characterId then
        local cachedBaseline = BurdJournals.Server.getCachedBaseline(characterId, player)
        if cachedBaseline then
            if type(cachedBaseline.mediaSkillBaseline) ~= "table" then
                cachedBaseline.mediaSkillBaseline = BurdJournals.getPlayerVhsSkillXPMapCopy
                    and BurdJournals.getPlayerVhsSkillXPMapCopy(player)
                    or {}
                if characterId then
                    BurdJournals.Server.storeBaselineArchiveRecord(characterId, cachedBaseline, true)
                end
                if BurdJournals.Server.transmitBaselineStores then
                    BurdJournals.Server.transmitBaselineStores(true)
                elseif ModData.transmit and BurdJournals.Server.isTrueSinglePlayerContext and BurdJournals.Server.isTrueSinglePlayerContext() then
                    ModData.transmit("BurdJournals_PlayerBaselines")
                end
            end

            local xp = tonumber(cachedBaseline.mediaSkillBaseline[skillName]) or 0
            if xp > 0 then
                return xp
            end
        end
    end

    local modData = player:getModData()
    if modData and modData.BurdJournals and modData.BurdJournals.mediaSkillBaseline then
        local xp = tonumber(modData.BurdJournals.mediaSkillBaseline[skillName]) or 0
        if xp > 0 then
            return xp
        end
    end

    return 0
end

function BurdJournals.Server.getSkillExportBaselineForPlayer(player, skillName)
    if not player or not skillName then return 0 end

    local cacheXP = nil
    local characterId = BurdJournals.getPlayerCharacterId(player)
    if characterId then
        local cachedBaseline = BurdJournals.Server.getCachedBaseline(characterId, player)
        if cachedBaseline and cachedBaseline.skillExportBaseline then
            local xp = cachedBaseline.skillExportBaseline[skillName]
            if xp then
                cacheXP = math.max(0, tonumber(xp) or 0)
            end
        end
    end

    local storedXP = 0
    local modData = player.getModData and player:getModData() or nil
    if modData and modData.BurdJournals and modData.BurdJournals.skillExportBaseline then
        storedXP = math.max(0, tonumber(modData.BurdJournals.skillExportBaseline[skillName]) or 0)
    end

    return cacheXP ~= nil and cacheXP or storedXP
end

function BurdJournals.Server.getMediaSkillExportBaselineForPlayer(player, skillName)
    if not player or not skillName then return 0 end

    local characterId = BurdJournals.getPlayerCharacterId(player)
    if characterId then
        local cachedBaseline = BurdJournals.Server.getCachedBaseline(characterId, player)
        if cachedBaseline and type(cachedBaseline.mediaSkillExportBaseline) == "table" then
            return math.max(0, tonumber(cachedBaseline.mediaSkillExportBaseline[skillName]) or 0)
        end
    end

    local modData = player:getModData()
    if modData and modData.BurdJournals and type(modData.BurdJournals.mediaSkillExportBaseline) == "table" then
        return math.max(0, tonumber(modData.BurdJournals.mediaSkillExportBaseline[skillName]) or 0)
    end

    return 0
end

function BurdJournals.Server.getTrackedVhsSkillXPForPlayer(player, skillName)
    if not player or not skillName then
        return 0
    end

    if BurdJournals.getPlayerVhsSkillXP then
        return math.max(0, tonumber(BurdJournals.getPlayerVhsSkillXP(player, skillName)) or 0)
    end

    local modData = player:getModData()
    local xpMap = modData and modData.BurdJournals and modData.BurdJournals.vhsSkillXP
    if type(xpMap) == "table" then
        return math.max(0, tonumber(xpMap[skillName]) or 0)
    end

    return 0
end

function BurdJournals.Server.getTrackedVhsSkillXPDeltaForPlayer(player, skillName, useBaseline)
    local trackedTotal = BurdJournals.Server.getTrackedVhsSkillXPForPlayer(player, skillName)
    if trackedTotal <= 0 then
        return 0, trackedTotal, 0
    end

    if not useBaseline then
        return trackedTotal, trackedTotal, 0
    end

    local trackedBaseline = BurdJournals.Server.getMediaSkillBaselineForPlayer(player, skillName)
    local trackedDelta = math.max(0, trackedTotal - trackedBaseline)
    return trackedDelta, trackedTotal, trackedBaseline
end

function BurdJournals.Server.markJournalSkillMediaLinesKnown(player, skillData, skillName, context)
    if not player or type(skillData) ~= "table" then
        return 0
    end

    local lineMap = skillData.vhsMediaLines or skillData.mediaLines
    if type(lineMap) ~= "table" then
        return 0
    end

    local marked = 0
    local modData = nil
    if player.getModData then
        modData = player:getModData()
    end
    local bj = nil
    if modData then
        modData.BurdJournals = modData.BurdJournals or {}
        bj = modData.BurdJournals
        bj.vhsJournalKnownLines = bj.vhsJournalKnownLines or {}
    end

    for lineGuid, lineData in pairs(lineMap) do
        local guid = type(lineGuid) == "string" and lineGuid or nil
        if not guid and type(lineData) == "table" and type(lineData.lineGuid) == "string" then
            guid = lineData.lineGuid
        end

        if guid and guid ~= "" then
            local appliesToSkill = true
            if type(skillName) == "string" and type(lineData) == "table" and type(lineData.skills) == "table" then
                appliesToSkill = (tonumber(lineData.skills[skillName]) or 0) > 0
            end

            if appliesToSkill then
                local alreadyKnown = false
                if player.isKnownMediaLine then
                    alreadyKnown = player:isKnownMediaLine(guid) == true
                end
                if not alreadyKnown and player.addKnownMediaLine then
                    player:addKnownMediaLine(guid)
                    marked = marked + 1
                end
                if bj and type(bj.vhsJournalKnownLines) == "table" then
                    bj.vhsJournalKnownLines[guid] = true
                end
            end
        end
    end

    if marked > 0 and player.transmitModData and not BurdJournals.Server.shouldDeferBatchTransmit(context) then
        player:transmitModData()
    end

    return marked
end

-- Debug baseline edits are intentionally synthetic and should not force
-- minimum claim XP floors for player journals.
function BurdJournals.Server.isBaselineDebugModifiedForPlayer(player)
    if not player then return false end

    local characterId = BurdJournals.getPlayerCharacterId(player)
    if characterId then
        local cachedBaseline = BurdJournals.Server.getCachedBaseline(characterId, player)
        if cachedBaseline and cachedBaseline.debugModified ~= nil then
            return cachedBaseline.debugModified == true
        end
    end

    local modData = player:getModData()
    if modData and modData.BurdJournals and modData.BurdJournals.debugModified == true then
        return true
    end

    return false
end

-- Resolve the post-claim total XP target for a journal claim.
-- Baseline-aware player journals store earned/delta XP, so their effective
-- target is the player's current total plus that earned delta. Legacy absolute
-- entries remain exact/set targets for backward compatibility.
function BurdJournals.Server.getSkillClaimTargetXP(player, journalData, skillName, recordedXP, baselineXPHint)
    local targetXP = math.max(0, tonumber(recordedXP) or 0)
    local baselineXP = 0
    local baselineSuppressed = false
    local claimUsesEarnedDeltaGrant = false
    local recordedLevel = 0
    if type(journalData) == "table" and type(journalData.skills) == "table" then
        local storedSkillKey = BurdJournals.resolveSkillKey and BurdJournals.resolveSkillKey(journalData.skills, skillName) or skillName
        if type(journalData.skills[storedSkillKey]) == "table" then
            recordedLevel = tonumber(journalData.skills[storedSkillKey].level) or 0
        end
    end
    local useBaselineMode = BurdJournals.getJournalSkillRecordingMode
        and BurdJournals.getJournalSkillRecordingMode(journalData, player)
        or (type(journalData) == "table" and journalData.recordedWithBaseline == true)

    if player
        and type(journalData) == "table"
        and journalData.isPlayerCreated == true
        and useBaselineMode == true
    then
        baselineSuppressed = BurdJournals.Server.isBaselineDebugModifiedForPlayer
            and BurdJournals.Server.isBaselineDebugModifiedForPlayer(player)
            or false
        if not baselineSuppressed then
            baselineXP = math.max(0, tonumber(BurdJournals.Server.getSkillBaselineForPlayer(player, skillName)) or 0)
        end
        local legacyAbsolute = BurdJournals.isLikelyLegacyAbsoluteSkillEntry
            and BurdJournals.isLikelyLegacyAbsoluteSkillEntry(journalData, player, skillName, targetXP, recordedLevel, nil, baselineXP)
            or false
        if not legacyAbsolute then
            local perk = BurdJournals.getPerkByName and BurdJournals.getPerkByName(skillName)
            local currentXP = 0
            if perk and player and player.getXp then
                currentXP = math.max(
                    0,
                    tonumber(BurdJournals.getPlayerSkillTotalXP and BurdJournals.getPlayerSkillTotalXP(player, perk, skillName) or player:getXp():getXP(perk)) or 0
                )
            end
            if not baselineSuppressed then
                local hintedBaselineXP = math.max(0, tonumber(baselineXPHint) or 0)
                hintedBaselineXP = math.min(currentXP, hintedBaselineXP)
                if hintedBaselineXP > baselineXP then
                    BurdJournals.debugPrint("[BurdJournals] Server: Using restrictive claim baseline hint for "
                        .. tostring(skillName) .. " (server=" .. tostring(baselineXP)
                        .. ", hint=" .. tostring(hintedBaselineXP) .. ")")
                    baselineXP = hintedBaselineXP
                end
            end
            local currentLevel = 0
            if perk and player and player.getPerkLevel then
                currentLevel = math.max(0, tonumber(player:getPerkLevel(perk)) or 0)
            end
            -- Detect a genuinely MISSING baseline record (vs a legitimate baseline
            -- of 0). On long-running servers the cached baseline can be pruned/lost;
            -- with baselineXP defaulting to 0 the earned-delta math below would clamp
            -- the grant to the player's current XP and silently award nothing.
            local baselineRecordMissing = false
            if not baselineSuppressed and baselineXP == 0 then
                local hasRecord = false
                local cid = BurdJournals.getPlayerCharacterId and BurdJournals.getPlayerCharacterId(player)
                if cid and BurdJournals.Server.getCachedBaseline then
                    local cb = BurdJournals.Server.getCachedBaseline(cid, player)
                    if cb and cb.skillBaseline then hasRecord = true end
                end
                if not hasRecord then
                    local md = player.getModData and player:getModData()
                    if md and md.BurdJournals and md.BurdJournals.skillBaseline then hasRecord = true end
                end
                baselineRecordMissing = not hasRecord
            end
            local isPassiveSkill = (BurdJournals.isPassiveSkill and BurdJournals.isPassiveSkill(skillName))
                or (skillName == "Fitness" or skillName == "Strength")
            if recordedLevel > 0 and (currentLevel > recordedLevel or (isPassiveSkill and currentLevel >= recordedLevel)) then
                targetXP = currentXP
            elseif baselineRecordMissing then
                -- Cannot compute the earned delta without a baseline; grant the
                -- recorded earned XP additively (it is the player's own progress)
                -- rather than awarding nothing. The claim-lock check below still
                -- prevents re-granting on a reusable player journal.
                targetXP = currentXP + math.max(0, tonumber(targetXP) or 0)
                BurdJournals.debugPrint("[BurdJournals] Server: baseline record missing for "
                    .. tostring(skillName) .. "; granting recorded earned XP additively")
            else
                local currentEarnedXP = math.max(0, currentXP - baselineXP)
                local missingEarnedXP = math.max(0, targetXP - currentEarnedXP)
                targetXP = currentXP + missingEarnedXP
            end
            if BurdJournals.getActivePlayerJournalSkillClaimLock
                and BurdJournals.getActivePlayerJournalSkillClaimLock(journalData, player, skillName, recordedXP, targetXP, currentXP, currentLevel) then
                targetXP = currentXP
            end
            claimUsesEarnedDeltaGrant = true
        end
    end

    return targetXP, baselineXP, baselineSuppressed, claimUsesEarnedDeltaGrant
end

local function isLikelyAbsoluteSkillEntryForBaseline(storedXP, earnedXP, actualXP)
    local stored = math.max(0, tonumber(storedXP) or 0)
    local earned = math.max(0, tonumber(earnedXP) or 0)
    local actual = math.max(0, tonumber(actualXP) or 0)
    if stored <= 0 or actual <= 0 then
        return false
    end
    return stored > (earned + 0.001) and stored <= (actual + 0.001)
end

function BurdJournals.Server.validateSkillPayload(skills, player, useBaselineOverride, journalData)
    if skills == nil then return nil end
    if type(skills) ~= "table" then
        bsjWriteLogLine("[BurdJournals] WARNING: Invalid skills payload (not a table) from " .. tostring(player and player:getUsername() or "unknown"))
        return nil
    end

    local validSkills = {}
    local allowedSkills = BurdJournals.getAllowedSkills and BurdJournals.getAllowedSkills() or {}
    local allowedSet = {}
    for _, name in ipairs(allowedSkills) do allowedSet[name] = true end
    local playerJournalContext = {isPlayerCreated = true}

    -- Get baseline using the correct accessor
    local useBaseline
    if type(useBaselineOverride) == "boolean" then
        useBaseline = useBaselineOverride
    else
        useBaseline = BurdJournals.shouldUseBaselineForPlayerJournalRecording and BurdJournals.shouldUseBaselineForPlayerJournalRecording(player) or false
    end
    if useBaseline and BurdJournals.Server.isBaselineDebugModifiedForPlayer and BurdJournals.Server.isBaselineDebugModifiedForPlayer(player) then
        useBaseline = false
        BurdJournals.debugPrint("[BurdJournals] validateSkillPayload: baseline restriction suppressed because debug baseline edits are active")
    end
    local allowVhsSkillRecording = BurdJournals.isVhsSkillRecordingEnabled and BurdJournals.isVhsSkillRecordingEnabled() or false

    -- Debug: Check if we have cached baseline for this player
    local characterId = BurdJournals.getPlayerCharacterId(player)
    local hasCachedBaseline = false
    local hasModDataBaseline = false
    local cachedBaseline = nil

    if characterId then
        cachedBaseline = BurdJournals.Server.getCachedBaseline(characterId, player)
        hasCachedBaseline = cachedBaseline ~= nil and cachedBaseline.skillBaseline ~= nil
    end

    -- Also check player modData
    local modData = player:getModData()
    if modData and modData.BurdJournals and modData.BurdJournals.skillBaseline then
        hasModDataBaseline = true
    end

    BurdJournals.debugPrint("[BurdJournals] validateSkillPayload: useBaseline=" .. tostring(useBaseline) .. ", characterId=" .. tostring(characterId) .. ", hasCachedBaseline=" .. tostring(hasCachedBaseline) .. ", hasModDataBaseline=" .. tostring(hasModDataBaseline))

    -- WARNING: If baseline restriction is enabled but we have NO baseline data, log a warning
    -- This could cause skills to be rejected incorrectly
    if useBaseline and not hasCachedBaseline and not hasModDataBaseline then
        bsjWriteLogLine("[BurdJournals] WARNING: Baseline restriction enabled but NO baseline data found for player " .. tostring(player:getUsername()) .. "! This may cause skills to be rejected incorrectly.")
        BurdJournals.debugPrint("[BurdJournals] The player's baseline may not have been captured. Consider asking them to close and reopen the journal, or disable 'Only Record Earned Progress' sandbox option.")
    end

    for skillName, skillData in pairs(skills) do
        local requestedSkillName = skillName
        if type(skillName) == "string" and not allowedSet[skillName] and BurdJournals.resolveSkillKey then
            local resolvedAllowedSkillName = BurdJournals.resolveSkillKey(allowedSet, skillName)
            if resolvedAllowedSkillName and allowedSet[resolvedAllowedSkillName] then
                skillName = resolvedAllowedSkillName
            end
        end

        if type(requestedSkillName) ~= "string" then
            bsjWriteLogLine("[BurdJournals] WARNING: Invalid skill name type: " .. type(requestedSkillName))

        elseif not allowedSet[skillName] then
            bsjWriteLogLine("[BurdJournals] WARNING: Unknown skill name: " .. requestedSkillName)

        elseif BurdJournals.isSkillEnabledForJournal and not BurdJournals.isSkillEnabledForJournal(playerJournalContext, skillName) then
            BurdJournals.debugPrint("[BurdJournals] validateSkillPayload: Skipping disabled skill for player journals: " .. tostring(skillName))

        elseif type(skillData) ~= "table" then
            bsjWriteLogLine("[BurdJournals] WARNING: Invalid skill data type for " .. skillName .. ": " .. type(skillData))
        else
            -- SERVER-SIDE VALIDATION: Get actual player XP, don't trust client values
            local perk = BurdJournals.getPerkByName(skillName)
            if perk then
                local actualXP = BurdJournals.getPlayerSkillTotalXP and BurdJournals.getPlayerSkillTotalXP(player, perk, skillName) or player:getXp():getXP(perk)
                local actualLevel = player:getPerkLevel(perk)

                -- Apply baseline if enabled (Only Record Earned Progress)
                local earnedXP = actualXP
                local baselineXP = 0
                local baselineHintXP = math.max(0, tonumber(skillData and skillData.baselineXP) or 0)
                local exportGateXP = actualXP
                local exportBaselineXP = 0
                local trackedTotal = 0
                local trackedBaseline = 0
                local trackedExportBaseline = 0
                if useBaseline then
                    -- Use server-side baseline retrieval (checks cache first)
                    baselineXP = BurdJournals.Server.getSkillBaselineForPlayer(player, skillName) or 0
                    baselineHintXP = math.min(math.max(0, tonumber(actualXP) or 0), baselineHintXP)
                    if baselineHintXP > baselineXP then
                        BurdJournals.debugPrint("[BurdJournals] validateSkillPayload: " .. skillName
                            .. " using restrictive baseline hint fallback (server=" .. tostring(baselineXP)
                            .. ", hint=" .. tostring(baselineHintXP) .. ")")
                        baselineXP = baselineHintXP
                    end
                    earnedXP = math.max(0, actualXP - baselineXP)
                    exportBaselineXP = math.max(
                        baselineXP,
                        tonumber(BurdJournals.Server.getSkillExportBaselineForPlayer(player, skillName)) or 0
                    )
                    exportGateXP = math.max(0, actualXP - exportBaselineXP)
                    BurdJournals.debugPrint("[BurdJournals] validateSkillPayload: " .. skillName .. " actualXP=" .. tostring(actualXP) .. ", baselineXP=" .. tostring(baselineXP) .. ", earnedXP=" .. tostring(earnedXP))
                else
                    BurdJournals.debugPrint("[BurdJournals] validateSkillPayload: " .. skillName .. " actualXP=" .. tostring(actualXP) .. " (baseline disabled)")
                end

                local rawEarnedXP = math.max(0, tonumber(earnedXP) or 0)
                local vhsExcludedXP = 0
                local trackedDelta = 0
                trackedDelta, trackedTotal, trackedBaseline = BurdJournals.Server.getTrackedVhsSkillXPDeltaForPlayer(player, skillName, useBaseline)
                if not allowVhsSkillRecording then
                    if trackedDelta > 0 then
                        local earnedBeforeVhs = math.max(0, tonumber(earnedXP) or 0)
                        local safeTrackedDelta = math.max(0, tonumber(trackedDelta) or 0)

                        -- Guard against stale/duplicated VHS tracking data.
                        -- If tracked VHS exceeds currently earned XP, subtracting it would
                        -- incorrectly zero out legitimate progress and break recording.
                        if safeTrackedDelta > (earnedBeforeVhs + 0.001) then
                            BurdJournals.debugPrint("[BurdJournals] validateSkillPayload: " .. skillName
                                .. " VHS tracking out of sync; skipping subtraction (trackedDelta=" .. tostring(safeTrackedDelta)
                                .. ", earnedBeforeVhs=" .. tostring(earnedBeforeVhs)
                                .. ", total=" .. tostring(trackedTotal)
                                .. ", baseline=" .. tostring(trackedBaseline) .. ")")
                            safeTrackedDelta = 0
                        end

                        if safeTrackedDelta > 0 then
                            earnedXP = math.max(0, earnedBeforeVhs - safeTrackedDelta)
                            vhsExcludedXP = math.max(0, earnedBeforeVhs - earnedXP)
                            BurdJournals.debugPrint("[BurdJournals] validateSkillPayload: " .. skillName
                                .. " subtracting VHS XP delta=" .. tostring(safeTrackedDelta)
                                .. " (total=" .. tostring(trackedTotal)
                                .. ", baseline=" .. tostring(trackedBaseline)
                                .. "), finalEarned=" .. tostring(earnedXP))
                        end
                    end

                    if useBaseline and trackedTotal > 0 then
                        trackedExportBaseline = math.max(
                            trackedBaseline,
                            tonumber(BurdJournals.Server.getMediaSkillExportBaselineForPlayer(player, skillName)) or 0
                        )
                        local trackedExportDelta = math.max(0, trackedTotal - trackedExportBaseline)
                        if trackedExportDelta > (exportGateXP + 0.001) then
                            BurdJournals.debugPrint("[BurdJournals] validateSkillPayload: " .. skillName
                                .. " export VHS tracking out of sync; skipping export subtraction (trackedDelta=" .. tostring(trackedExportDelta)
                                .. ", exportGate=" .. tostring(exportGateXP)
                                .. ", total=" .. tostring(trackedTotal)
                                .. ", exportBaseline=" .. tostring(trackedExportBaseline) .. ")")
                            trackedExportDelta = 0
                        end
                        if trackedExportDelta > 0 then
                            exportGateXP = math.max(0, exportGateXP - trackedExportDelta)
                        end
                    end
                end

                local currentJournalXP = 0
                if type(journalData) == "table" and type(journalData.skills) == "table" then
                    local existingSkillKey = BurdJournals.resolveSkillKey and BurdJournals.resolveSkillKey(journalData.skills, skillName) or skillName
                    local existingSkill = journalData.skills[existingSkillKey]
                    currentJournalXP = math.max(0, tonumber(type(existingSkill) == "table" and existingSkill.xp or existingSkill) or 0)
                end

                -- Only record if there's actual earned XP
                local shouldAccept = earnedXP > 0
                if shouldAccept and useBaseline then
                    if currentJournalXP > 0 then
                        shouldAccept = earnedXP > (currentJournalXP + 0.001)
                    end
                end

                if shouldAccept then
                    if actualLevel <= 0
                        and (skillName == "Fitness" or skillName == "Strength")
                        and actualXP > 0
                        and BurdJournals.getSkillLevelFromXP
                    then
                        actualLevel = math.max(0, tonumber(BurdJournals.getSkillLevelFromXP(actualXP, skillName)) or 0)
                    end
                    local vhsMediaLines = nil
                    if allowVhsSkillRecording and trackedTotal > 0 and BurdJournals.getPlayerVhsMediaLinesForSkill then
                        vhsMediaLines = BurdJournals.getPlayerVhsMediaLinesForSkill(player, skillName, useBaseline and trackedBaseline or 0)
                    end
                    validSkills[skillName] = {
                        xp = earnedXP,
                        level = actualLevel,
                        rawXP = rawEarnedXP,
                        vhsExcludedXP = vhsExcludedXP,
                        exportSkillXP = math.max(0, tonumber(actualXP) or 0),
                        exportMediaSkillXP = math.max(0, tonumber(trackedTotal) or 0),
                        vhsMediaLines = vhsMediaLines
                    }
                    BurdJournals.debugPrint("[BurdJournals] validateSkillPayload: " .. skillName .. " ACCEPTED")
                else
                    BurdJournals.debugPrint("[BurdJournals] validateSkillPayload: " .. skillName .. " REJECTED (no new exportable earned XP)")
                end
            else
                bsjWriteLogLine("[BurdJournals] WARNING: Could not find perk for skill: " .. skillName)
            end
        end
    end

    return validSkills
end

function BurdJournals.Server.validateTraitPayload(traits, player, useBaselineOverride, journalData)
    if traits == nil then return nil end

    -- Check if trait recording is enabled for player journals
    if BurdJournals.getSandboxOption("EnableTraitRecordingPlayer") == false then
        BurdJournals.debugPrint("[BurdJournals] Trait recording disabled for player journals")
        return nil
    end

    if type(traits) ~= "table" then
        bsjWriteLogLine("[BurdJournals] WARNING: Invalid traits payload (not a table) from " .. tostring(player and player:getUsername() or "unknown"))
        return nil
    end

    local bsjTraitValidateStartMs = getTimestampMs and getTimestampMs() or nil
    local validTraits = {}
    local rejectedTraits = {}
    local incomingTraitCount = 0

    -- Check if baseline restriction is enabled
    local useBaseline
    if type(useBaselineOverride) == "boolean" then
        useBaseline = useBaselineOverride
    else
        useBaseline = BurdJournals.shouldUseBaselineForPlayerJournalRecording and BurdJournals.shouldUseBaselineForPlayerJournalRecording(player) or false
    end

    local playerJournalContext = { isPlayerCreated = true }
    local traitBaseline = useBaseline and BurdJournals.getTraitBaseline and BurdJournals.getTraitBaseline(player) or nil
    local traitExportBaseline = useBaseline and BurdJournals.getTraitExportBaseline and BurdJournals.getTraitExportBaseline(player) or nil
    local traitBaselineLookup = type(traitBaseline) == "table"
        and BurdJournals.buildTraitLookup
        and BurdJournals.buildTraitLookup(traitBaseline)
        or nil
    local journalTraitLookup = type(journalData) == "table"
        and BurdJournals.buildTraitLookup
        and BurdJournals.buildTraitLookup(journalData.traits or {})
        or nil

    local playerTraitLookup = {}
    local playerTraitLookupBuilt = false
    local function addTraitLookupValue(value)
        if value == nil then return end
        local normalized = BurdJournals.normalizeTraitId and BurdJournals.normalizeTraitId(value) or tostring(value)
        if not normalized or normalized == "" then return end
        playerTraitLookup[normalized] = true
        playerTraitLookup[string.lower(tostring(normalized))] = true
        if BurdJournals.getTraitAliases then
            for _, alias in ipairs(BurdJournals.getTraitAliases(normalized)) do
                if alias then
                    local aliasNorm = BurdJournals.normalizeTraitId and BurdJournals.normalizeTraitId(alias) or tostring(alias)
                    if aliasNorm and aliasNorm ~= "" then
                        playerTraitLookup[aliasNorm] = true
                        playerTraitLookup[string.lower(tostring(aliasNorm))] = true
                    end
                end
            end
        end
    end

    local runtimeTraits = nil
    local runtimeTraitShape = "none"
    if player and player.getTraits then
        local okTraits, traitsObj = BurdJournals.safePcall(function()
            return player:getTraits()
        end)
        runtimeTraits = okTraits and traitsObj or nil
        if runtimeTraits and runtimeTraits.size and runtimeTraits.get then
            runtimeTraitShape = "size_get"
            playerTraitLookupBuilt = true
            for i = 0, runtimeTraits:size() - 1 do
                addTraitLookupValue(runtimeTraits:get(i))
            end
        elseif runtimeTraits and runtimeTraits.contains then
            runtimeTraitShape = "contains"
            playerTraitLookupBuilt = true
        elseif runtimeTraits then
            runtimeTraitShape = "opaque"
        end
    end

    local submittedTraitDefinitionLookup = {}
    local function addTraitVerificationCandidate(list, seen, value)
        if value == nil then return end
        local key = tostring(value)
        if key == "" or seen[key] then return end
        seen[key] = true
        list[#list + 1] = value
    end
    local function addCharacterTraitLookupCandidate(list, seen, traitName)
        if not traitName or traitName == "" then return end
        if CharacterTrait and CharacterTrait.get and ResourceLocation and ResourceLocation.of then
            local lookupNames = { tostring(traitName) }
            local normalizedName = BurdJournals.normalizeTraitId and BurdJournals.normalizeTraitId(traitName) or tostring(traitName)
            if normalizedName and normalizedName ~= "" then
                lookupNames[#lookupNames + 1] = normalizedName
                lookupNames[#lookupNames + 1] = string.lower(tostring(normalizedName))
                if not string.find(tostring(normalizedName), ":") then
                    lookupNames[#lookupNames + 1] = "base:" .. tostring(normalizedName)
                    lookupNames[#lookupNames + 1] = "base:" .. string.lower(tostring(normalizedName))
                end
            end
            for _, lookupName in ipairs(lookupNames) do
                local okTrait, traitObj = BurdJournals.safePcall(function()
                    return CharacterTrait.get(ResourceLocation.of(lookupName))
                end)
                if okTrait and traitObj then
                    addTraitVerificationCandidate(list, seen, traitObj)
                end
            end
        end
    end
    local function addSubmittedTraitDefinition(traitId, traitObj, defName)
        if not traitId or not traitObj then return end
        local candidates = { __bsjTraitCandidates = true }
        local seen = {}
        addTraitVerificationCandidate(candidates, seen, traitObj)
        if traitObj and traitObj.getName then
            local okName, runtimeName = BurdJournals.safePcall(function()
                return traitObj:getName()
            end)
            if okName and runtimeName then
                addCharacterTraitLookupCandidate(candidates, seen, tostring(runtimeName))
            end
        end
        addCharacterTraitLookupCandidate(candidates, seen, defName)
        if defName then
            local normalizedName = BurdJournals.normalizeTraitId and BurdJournals.normalizeTraitId(defName) or tostring(defName)
            addCharacterTraitLookupCandidate(candidates, seen, normalizedName)
        end
        local normalized = BurdJournals.normalizeTraitId and BurdJournals.normalizeTraitId(traitId) or tostring(traitId)
        if normalized and normalized ~= "" then
            submittedTraitDefinitionLookup[normalized] = candidates
            submittedTraitDefinitionLookup[string.lower(tostring(normalized))] = candidates
        end
        if BurdJournals.getTraitAliases then
            for _, alias in ipairs(BurdJournals.getTraitAliases(normalized or traitId)) do
                local aliasNorm = BurdJournals.normalizeTraitId and BurdJournals.normalizeTraitId(alias) or tostring(alias)
                if aliasNorm and aliasNorm ~= "" then
                    submittedTraitDefinitionLookup[aliasNorm] = candidates
                    submittedTraitDefinitionLookup[string.lower(tostring(aliasNorm))] = candidates
                end
            end
        end
    end

    -- Resolve only submitted trait IDs once. This keeps server validation
    -- authoritative without repeatedly scanning the full trait registry.
    if CharacterTraitDefinition and CharacterTraitDefinition.getTraits then
        local allTraits = CharacterTraitDefinition.getTraits()
        if allTraits and allTraits.size and allTraits.get then
            local wanted = {}
            for traitId, _ in pairs(traits) do
                if type(traitId) == "string" then
                    local normalized = BurdJournals.normalizeTraitId and BurdJournals.normalizeTraitId(traitId) or tostring(traitId)
                    wanted[string.lower(tostring(normalized))] = true
                    if BurdJournals.getTraitAliases then
                        for _, alias in ipairs(BurdJournals.getTraitAliases(normalized)) do
                            local aliasNorm = BurdJournals.normalizeTraitId and BurdJournals.normalizeTraitId(alias) or tostring(alias)
                            wanted[string.lower(tostring(aliasNorm))] = true
                        end
                    end
                end
            end

            for i = 0, allTraits:size() - 1 do
                local def = allTraits:get(i)
                local defType = def and def.getType and def:getType() or nil
                local defLabel = def and def.getLabel and def:getLabel() or nil
                local defName = nil
                if defType then
                    if defType.getName then
                        defName = defType:getName()
                    else
                        defName = tostring(defType)
                    end
                end
                local candidates = { defLabel, defName }
                local matched = false
                for _, candidate in ipairs(candidates) do
                    local candidateNorm = BurdJournals.normalizeTraitId and BurdJournals.normalizeTraitId(candidate) or tostring(candidate or "")
                    if candidateNorm and wanted[string.lower(tostring(candidateNorm))] then
                        matched = true
                        break
                    end
                end
                if matched then
                    for traitId, _ in pairs(traits) do
                        if type(traitId) == "string" then
                            local traitNorm = BurdJournals.normalizeTraitId and BurdJournals.normalizeTraitId(traitId) or tostring(traitId)
                            local traitLower = string.lower(tostring(traitNorm))
                            local defLabelNorm = BurdJournals.normalizeTraitId and BurdJournals.normalizeTraitId(defLabel) or tostring(defLabel or "")
                            local defNameNorm = BurdJournals.normalizeTraitId and BurdJournals.normalizeTraitId(defName) or tostring(defName or "")
                            if traitLower == string.lower(tostring(defLabelNorm))
                                or traitLower == string.lower(tostring(defNameNorm))
                                or (BurdJournals.traitIdsMatch and (BurdJournals.traitIdsMatch(traitId, defLabel) or BurdJournals.traitIdsMatch(traitId, defName))) then
                                addSubmittedTraitDefinition(traitId, defType, defName)
                            end
                        end
                    end
                end
            end
        end
    end

    local function traitContainsFast(traitId)
        if not (runtimeTraits and runtimeTraits.contains) then
            return false
        end
        local candidates = { traitId }
        if BurdJournals.getTraitAliases then
            for _, alias in ipairs(BurdJournals.getTraitAliases(traitId)) do
                candidates[#candidates + 1] = alias
            end
        end
        for _, candidate in ipairs(candidates) do
            if candidate then
                local normalized = BurdJournals.normalizeTraitId and BurdJournals.normalizeTraitId(candidate) or tostring(candidate)
                local okContains, contains = BurdJournals.safePcall(function()
                    return runtimeTraits:contains(normalized)
                end)
                if okContains and contains == true then
                    return true
                end
                if normalized ~= candidate then
                    okContains, contains = BurdJournals.safePcall(function()
                        return runtimeTraits:contains(candidate)
                    end)
                    if okContains and contains == true then
                        return true
                    end
                end
            end
        end
        return false
    end

    local function hasPlayerTraitFast(traitId)
        if playerTraitLookupBuilt and BurdJournals.isTraitInLookup and BurdJournals.hasAnyEntries and BurdJournals.hasAnyEntries(playerTraitLookup) then
            return BurdJournals.isTraitInLookup(playerTraitLookup, traitId) == true
        end
        if traitContainsFast(traitId) then
            return true
        end
        if player and player.HasTrait then
            local candidates = { traitId }
            if BurdJournals.getTraitAliases then
                for _, alias in ipairs(BurdJournals.getTraitAliases(traitId)) do
                    candidates[#candidates + 1] = alias
                end
            end
            for _, candidate in ipairs(candidates) do
                local okHas, hasTrait = BurdJournals.safePcall(function()
                    return player:HasTrait(candidate)
                end)
                if okHas and hasTrait == true then
                    return true
                end
            end
        end
        if player and player.hasTrait then
            local normalized = BurdJournals.normalizeTraitId and BurdJournals.normalizeTraitId(traitId) or tostring(traitId)
            local traitObjects = submittedTraitDefinitionLookup[normalized] or submittedTraitDefinitionLookup[string.lower(tostring(normalized))]
            if traitObjects then
                if type(traitObjects) == "table" and traitObjects.__bsjTraitCandidates == true then
                    for _, traitObj in ipairs(traitObjects) do
                        local okHas, hasTrait = BurdJournals.safePcall(function()
                            return player:hasTrait(traitObj)
                        end)
                        if okHas and hasTrait == true then
                            return true
                        end
                    end
                else
                    local okHas, hasTrait = BurdJournals.safePcall(function()
                        return player:hasTrait(traitObjects)
                    end)
                    if okHas and hasTrait == true then
                        return true
                    end
                end
            end
        end
        if BurdJournals.playerHasTrait then
            local okShared, hasShared = BurdJournals.safePcall(function()
                return BurdJournals.playerHasTrait(player, traitId)
            end)
            if okShared and hasShared == true then
                return true
            end
        end
        return false
    end

    local function isBaselineTraitFast(traitId)
        if not useBaseline then
            return false
        end
        if BurdJournals.isPassiveSkillTrait and BurdJournals.isPassiveSkillTrait(traitId) then
            return false
        end
        if traitBaselineLookup and BurdJournals.isTraitInLookup then
            return BurdJournals.isTraitInLookup(traitBaselineLookup, traitId) == true
        end
        return BurdJournals.isStartingTrait and BurdJournals.isStartingTrait(player, traitId) == true
    end

    for traitId, _ in pairs(traits) do
        incomingTraitCount = incomingTraitCount + 1

        if type(traitId) ~= "string" then
            bsjWriteLogLine("[BurdJournals] WARNING: Invalid trait ID type: " .. type(traitId))

        elseif string.len(traitId) > 100 then
            bsjWriteLogLine("[BurdJournals] WARNING: Trait ID too long: " .. string.sub(traitId, 1, 50) .. "...")
        else
            if BurdJournals.isTraitEnabledForJournal and not BurdJournals.isTraitEnabledForJournal(playerJournalContext, traitId) then
                BurdJournals.debugPrint("[BurdJournals] validateTraitPayload: Rejected trait " .. traitId .. " - blocked by compatibility settings")
                rejectedTraits[#rejectedTraits + 1] = tostring(traitId) .. ":blocked"
            -- SERVER-SIDE VALIDATION: Verify player actually has this trait
            elseif hasPlayerTraitFast(traitId) then
                -- Check if trait was in baseline (shouldn't record starting traits if enabled)
                local isBaselineTrait = isBaselineTraitFast(traitId)
                -- Trait export markers are historical/audit state. The active
                -- journal contents and starting baseline are the authoritative
                -- duplicate guards for a record operation.
                local isExportedTrait = false
                local journalAlreadyHasTrait = journalTraitLookup
                    and BurdJournals.isTraitInLookup
                    and BurdJournals.isTraitInLookup(journalTraitLookup, traitId)
                if not isBaselineTrait and not isExportedTrait and not journalAlreadyHasTrait then
                    validTraits[traitId] = true
                else
                    local reason = isBaselineTrait and "baseline" or (isExportedTrait and "exported" or "already_recorded")
                    rejectedTraits[#rejectedTraits + 1] = tostring(traitId) .. ":" .. reason
                end
            else
                BurdJournals.debugPrint("[BurdJournals] Rejected trait " .. traitId .. " - player doesn't have it")
                rejectedTraits[#rejectedTraits + 1] = tostring(traitId) .. ":missing"
            end
        end
    end

    local validTraitCount = BurdJournals.countTable and BurdJournals.countTable(validTraits) or 0
    if incomingTraitCount > validTraitCount and #rejectedTraits > 0 then
        local sample = {}
        for i = 1, math.min(#rejectedTraits, 8) do
            sample[#sample + 1] = rejectedTraits[i]
        end
        bsjWriteLogLine("[BurdJournals] validateTraitPayload mismatch for "
            .. tostring(player and player:getUsername() or "unknown")
            .. ": incoming=" .. tostring(incomingTraitCount)
            .. ", accepted=" .. tostring(validTraitCount)
            .. ", rejectedSample=" .. table.concat(sample, ", "))
    end

    local bsjTraitValidateEndMs = getTimestampMs and getTimestampMs() or nil
    if bsjTraitValidateStartMs and bsjTraitValidateEndMs and bsjWriteLogLine then
        bsjWriteLogLine("[BurdJournals][TraitValidatePerf] player=" .. tostring(player and player:getUsername() or "unknown")
            .. " incoming=" .. tostring(incomingTraitCount)
            .. " accepted=" .. tostring(validTraitCount)
            .. " rejected=" .. tostring(math.max(0, incomingTraitCount - validTraitCount))
            .. " baseline=" .. tostring(useBaseline == true)
            .. " hasBaseline=" .. tostring(traitExportBaseline ~= nil)
            .. " hasJournalLookup=" .. tostring(journalTraitLookup ~= nil)
            .. " playerLookup=" .. tostring(playerTraitLookupBuilt == true)
            .. " traitShape=" .. tostring(runtimeTraitShape)
            .. " traitDefs=" .. tostring(BurdJournals.countTable and BurdJournals.countTable(submittedTraitDefinitionLookup) or 0)
            .. " ms=" .. tostring(math.max(0, bsjTraitValidateEndMs - bsjTraitValidateStartMs)))
    end

    return validTraits
end
function BurdJournals.Server.validateStatsPayload(stats, player)
    if stats == nil then return nil end
    if type(stats) ~= "table" then
        bsjWriteLogLine("[BurdJournals] WARNING: Invalid stats payload (not a table) from " .. tostring(player and player:getUsername() or "unknown"))
        return nil
    end

    local validStats = {}

    for statId, statData in pairs(stats) do

        if type(statId) ~= "string" then
            bsjWriteLogLine("[BurdJournals] WARNING: Invalid stat ID type: " .. type(statId))

        elseif string.len(statId) > 100 then
            bsjWriteLogLine("[BurdJournals] WARNING: Stat ID too long: " .. string.sub(statId, 1, 50) .. "...")

        elseif type(statData) ~= "table" then
            bsjWriteLogLine("[BurdJournals] WARNING: Invalid stat data type for " .. statId .. ": " .. type(statData))
        else

            -- Server-authoritative stat value (do not trust client)
            local value = nil
            if BurdJournals.getStatValue then
                value = BurdJournals.getStatValue(player, statId)
            end
            if value == nil then
                value = statData.value
                if type(value) ~= "number" and type(value) ~= "string" then
                    value = tostring(value)
                end
            end
            validStats[statId] = { value = value }
        end
    end

    return validStats
end

function BurdJournals.Server.validateRecipePayload(recipes, player, useBaselineOverride, journalData)
    if recipes == nil then return nil end

    -- Check if recipe recording is enabled for player journals
    if BurdJournals.getSandboxOption("EnableRecipeRecordingPlayer") == false then
        BurdJournals.debugPrint("[BurdJournals] Recipe recording disabled for player journals")
        return nil
    end

    if type(recipes) ~= "table" then
        bsjWriteLogLine("[BurdJournals] WARNING: Invalid recipes payload (not a table) from " .. tostring(player and player:getUsername() or "unknown"))
        return nil
    end

    local validRecipes = {}
    local rejectedRecipes = {}
    local incomingRecipeCount = 0

    -- Check if baseline restriction is enabled
    local useBaseline
    if type(useBaselineOverride) == "boolean" then
        useBaseline = useBaselineOverride
    else
        useBaseline = BurdJournals.shouldUseBaselineForPlayerJournalRecording and BurdJournals.shouldUseBaselineForPlayerJournalRecording(player) or false
    end

    local function isClientReportedTransferableRecipe(recipeName, recipeData)
        if type(recipeName) ~= "string" or recipeName == "" or type(recipeData) ~= "table" then
            return false
        end

        local source = type(recipeData.source) == "string" and recipeData.source or nil
        local expectedSource = BurdJournals.getMagazineForRecipe and BurdJournals.getMagazineForRecipe(recipeName) or nil
        if source and expectedSource and source == expectedSource then
            return true
        end

        local recipeScript = BurdJournals.getRecipeScript and BurdJournals.getRecipeScript(recipeName) or nil
        if recipeScript and recipeScript.needToBeLearn and recipeScript:needToBeLearn() then
            return true
        end

        return false
    end

    for recipeName, recipeData in pairs(recipes) do
        incomingRecipeCount = incomingRecipeCount + 1

        if type(recipeName) ~= "string" then
            bsjWriteLogLine("[BurdJournals] WARNING: Invalid recipe name type: " .. type(recipeName))

        elseif string.len(recipeName) > 200 then
            bsjWriteLogLine("[BurdJournals] WARNING: Recipe name too long: " .. string.sub(recipeName, 1, 50) .. "...")
        else
            local canonicalRecipeName = BurdJournals.getRecipeCanonicalName and BurdJournals.getRecipeCanonicalName(recipeName) or recipeName
            -- SERVER-SIDE VALIDATION: Verify player actually knows a transferable recipe
            local serverKnowsRecipe = BurdJournals.isTransferableRecipeKnown(player, canonicalRecipeName, false)
            local clientReportedTransferable = false
            if not serverKnowsRecipe then
                clientReportedTransferable = isClientReportedTransferableRecipe(canonicalRecipeName, recipeData)
            end
            if serverKnowsRecipe or clientReportedTransferable then
                -- Check if recipe was in baseline (shouldn't record starting recipes if enabled)
                local isBaselineRecipe = useBaseline and BurdJournals.isStartingRecipe(player, canonicalRecipeName)
                local recipeExportBaseline = useBaseline and BurdJournals.getRecipeExportBaseline and BurdJournals.getRecipeExportBaseline(player) or nil
                -- Recipe export markers are historical/audit state. They must not block
                -- recording into the active journal, especially across multi-batch record-all
                -- flows where a previous attempt may have exported the recipe before the
                -- current journal reflected it.
                local isExportedRecipe = false
                local journalAlreadyHasRecipe = type(journalData) == "table"
                    and type(journalData.recipes) == "table"
                    and BurdJournals.resolveRecipeKey
                    and BurdJournals.resolveRecipeKey(journalData.recipes, canonicalRecipeName) ~= nil
                if not isBaselineRecipe and not isExportedRecipe and not journalAlreadyHasRecipe then
                    validRecipes[canonicalRecipeName] = true
                else
                    local reason = isBaselineRecipe and "baseline" or (isExportedRecipe and "exported" or "already_recorded")
                    rejectedRecipes[#rejectedRecipes + 1] = tostring(recipeName) .. ":" .. reason
                end
            else
                BurdJournals.debugPrint("[BurdJournals] Rejected recipe " .. recipeName .. " - player doesn't know a transferable version of it")
                rejectedRecipes[#rejectedRecipes + 1] = tostring(recipeName) .. ":unknown"
            end
        end
    end

    local validRecipeCount = BurdJournals.countTable and BurdJournals.countTable(validRecipes) or 0
    if incomingRecipeCount > validRecipeCount and #rejectedRecipes > 0 then
        local sample = {}
        for i = 1, math.min(#rejectedRecipes, 8) do
            sample[#sample + 1] = rejectedRecipes[i]
        end
        bsjWriteLogLine("[BurdJournals] validateRecipePayload mismatch for "
            .. tostring(player and player:getUsername() or "unknown")
            .. ": incoming=" .. tostring(incomingRecipeCount)
            .. ", accepted=" .. tostring(validRecipeCount)
            .. ", rejectedSample=" .. table.concat(sample, ", "))
    end

    return validRecipes
end

function BurdJournals.Server.hasActiveBatchRewardJobs()
    local queue = BurdJournals.Server.BatchRewardQueue
    if not queue or type(queue.jobs) ~= "table" then
        return false
    end
    if queue.activeCount ~= nil then
        return (tonumber(queue.activeCount) or 0) > 0
    end
    for _, job in ipairs(queue.jobs) do
        if not job.completed then
            return true
        end
    end
    return false
end

function BurdJournals.Server.shouldDeferBatchTransmit(context)
    -- Batch deferral is carried explicitly by the handler request instead of a
    -- server-global execution flag. An exception therefore cannot strand all
    -- unrelated persistence in a deferred state.
    return type(context) == "table" and context._deferBatchTransmit == true
end

BurdJournals.Server.BATCH_INTERMEDIATE_RESPONSE_COMMANDS = {
    error = true,
    learnSuccess = true,
    claimSuccess = true,
    absorbSuccess = true,
    skillMaxed = true,
    traitAlreadyKnown = true,
    recipeAlreadyKnown = true,
    forgetSlotClaimed = true,
    journalDissolved = true,
}

BurdJournals.Server._batchResponseScopes = BurdJournals.Server._batchResponseScopes or {}

function BurdJournals.Server.invokeBatchRewardHandler(job, handler, ...)
    local callArgs = {...}
    local requestContext = type(callArgs[2]) == "table" and callArgs[2] or nil
    if requestContext then requestContext._deferBatchTransmit = true end
    local player = callArgs[1]
    local playerKey = BurdJournals.Server.getBatchRewardPlayerKey
        and BurdJournals.Server.getBatchRewardPlayerKey(player)
        or tostring(player)
    local token = {}
    BurdJournals.Server._batchResponseScopes[playerKey] = {
        token = token,
        expiresAt = ((getTimestampMs and getTimestampMs()) or ((os.time() or 0) * 1000)) + 1500,
    }
    local unpackArgs = table.unpack or unpack
    local resultA, resultB, resultC, resultD = handler(unpackArgs(callArgs))
    local activeScope = BurdJournals.Server._batchResponseScopes[playerKey]
    if activeScope and activeScope.token == token then
        BurdJournals.Server._batchResponseScopes[playerKey] = nil
    end
    return resultA, resultB, resultC, resultD
end

function BurdJournals.Server.sendToClient(player, command, args)
    local playerKey = BurdJournals.Server.getBatchRewardPlayerKey
        and BurdJournals.Server.getBatchRewardPlayerKey(player)
        or tostring(player)
    local scope = BurdJournals.Server._batchResponseScopes[playerKey]
    if scope then
        local nowMs = (getTimestampMs and getTimestampMs()) or ((os.time() or 0) * 1000)
        if nowMs >= (tonumber(scope.expiresAt) or 0) then
            BurdJournals.Server._batchResponseScopes[playerKey] = nil
            scope = nil
        end
    end
    if scope and BurdJournals.Server.BATCH_INTERMEDIATE_RESPONSE_COMMANDS[command] == true then return end
    -- Validate player before sending command (prevents crashes with mods that wrap sendServerCommand)
    if not player then
        bsjWriteLogLine("[BurdJournals] WARNING: sendToClient called with nil player for command: " .. tostring(command))
        return
    end

    -- Check if player is fully initialized (Username can be null during connection)
    if player.getUsername and player:getUsername() == nil then
        bsjWriteLogLine("[BurdJournals] WARNING: sendToClient called with uninitialized player for command: " .. tostring(command))
        return
    end

    if not sendServerCommand then
        bsjWriteLogLine("[BurdJournals] ERROR: sendServerCommand is not available for command '" .. tostring(command) .. "'")
        return
    end

    args = type(args) == "table" and args or {}
    -- OnServerCommand does not provide a player argument. Include stable target
    -- identity so local co-op clients route the response to the character whose
    -- command the server handled instead of blindly using player zero.
    if args.responseTargetUsername == nil and player.getUsername then
        args.responseTargetUsername = player:getUsername()
    end
    if args.responseTargetOnlineId == nil and player.getOnlineID then
        args.responseTargetOnlineId = player:getOnlineID()
    end
    if args.responseTargetCharacterId == nil and BurdJournals.getPlayerCharacterId then
        args.responseTargetCharacterId = BurdJournals.getPlayerCharacterId(player)
    end

    -- Legacy extensions may still omit a code. Keep their failure deterministic,
    -- but never infer gameplay semantics from mutable English prose.
    if command == "error" and type(args) == "table" and not args.errorCode then
        args.errorCode = "requestRejected"
        bsjWriteLogLine("[BurdJournals] WARNING: legacy error response omitted errorCode")
    end

    -- Data-shape hardening: claim/absorb responses carry ~10 debug_* diagnostic
    -- fields per event. On mod-heavy servers these add up across every claim. Only
    -- ship them when verbose logging is enabled; strip otherwise (no gameplay use).
    if type(args) == "table"
        and not (BurdJournals.shouldDebugLog and BurdJournals.shouldDebugLog()) then
        for key in pairs(args) do
            if type(key) == "string" and key:sub(1, 6) == "debug_" then
                args[key] = nil
            end
        end
    end

    sendServerCommand(player, "BurdJournals", command, args)

    local localPlayer = getPlayer and getPlayer()
    local isTrueSinglePlayer = localPlayer ~= nil and not isClient()

    if isTrueSinglePlayer then
        local ticksToWait = 1
        local ticksWaited = 0
        local invokeClient
        invokeClient = function()
            ticksWaited = ticksWaited + 1
            if ticksWaited >= ticksToWait then
                Events.OnTick.Remove(invokeClient)
                if BurdJournals.Client and BurdJournals.Client.onServerCommand then
                    BurdJournals.Client.onServerCommand("BurdJournals", command, args)
                end
            end
        end
        Events.OnTick.Add(invokeClient)
    end
end

function BurdJournals.Server.getAdminPolicyStore()
    local key = BurdJournals.ADMIN_POLICY_MODDATA_KEY or "BurdJournals_AdminPolicyV1"
    local store = nil
    if ModData and ModData.getOrCreate then
        store = ModData.getOrCreate(key)
    end
    if type(store) ~= "table" then
        store = BurdJournals.AdminPolicy or {}
    end
    store.skills = type(store.skills) == "table" and store.skills or {}
    store.traits = type(store.traits) == "table" and store.traits or {}
    store.recipes = type(store.recipes) == "table" and store.recipes or {}
    if BurdJournals.setAdminPolicy then
        BurdJournals.setAdminPolicy(store)
    end
    return store
end

function BurdJournals.Server.transmitAdminPolicyStore()
    local key = BurdJournals.ADMIN_POLICY_MODDATA_KEY or "BurdJournals_AdminPolicyV1"
    if ModData and ModData.transmit then
        ModData.transmit(key)
    end
end

function BurdJournals.Server.normalizeAdminPolicyMutation(args)
    args = type(args) == "table" and args or {}
    local kind = BurdJournals.normalizeAdminPolicyKind and BurdJournals.normalizeAdminPolicyKind(args.kind) or nil
    local scope = BurdJournals.normalizeAdminPolicyScope and BurdJournals.normalizeAdminPolicyScope(args.scope) or nil
    local id = args.id and tostring(args.id) or nil
    local policy = BurdJournals.normalizeAdminPolicyValue and BurdJournals.normalizeAdminPolicyValue(args.policy) or nil
    if not (kind and scope and id and id ~= "") then
        return nil
    end
    local rawPolicy = tostring(args.policy or "inherit")
    if rawPolicy ~= "inherit" and rawPolicy ~= "allow" and rawPolicy ~= "ban" and rawPolicy ~= "" then
        return nil
    end
    return kind, id, scope, policy
end

function BurdJournals.Server.applyAdminPolicyMutation(store, kind, id, scope, policy)
    if not (store and kind and id and scope) then return false end
    store[kind] = type(store[kind]) == "table" and store[kind] or {}
    local bucket = store[kind]
    bucket[id] = type(bucket[id]) == "table" and bucket[id] or {}
    bucket[id][scope] = policy
    if bucket[id].player == nil and bucket[id].loot == nil then
        bucket[id] = nil
    end
    return true
end

function BurdJournals.Server.sendAdminPolicyToClient(player, message)
    local store = BurdJournals.Server.getAdminPolicyStore()
    BurdJournals.Server.sendToClient(player, "debugAdminPolicy", {
        policy = BurdJournals.Server.deepCopy(store),
        message = message,
    })
end

function BurdJournals.Server.handleDebugRequestAdminPolicy(player, _args)
    if not BurdJournals.Server.isDebugAllowed(player) then
        BurdJournals.Server.sendToClient(player, "debugError", {message = "Debug commands not allowed"})
        return
    end
    BurdJournals.Server.sendAdminPolicyToClient(player)
end

function BurdJournals.Server.handleDebugSetAdminPolicy(player, args)
    if not BurdJournals.Server.isDebugAllowed(player) then
        BurdJournals.Server.sendToClient(player, "debugError", {message = "Debug commands not allowed"})
        return
    end
    local kind, id, scope, policy = BurdJournals.Server.normalizeAdminPolicyMutation(args)
    if not kind then
        BurdJournals.Server.sendToClient(player, "debugError", {message = "Invalid whitelist policy request"})
        return
    end
    local store = BurdJournals.Server.getAdminPolicyStore()
    BurdJournals.Server.applyAdminPolicyMutation(store, kind, id, scope, policy)
    BurdJournals.Server.transmitAdminPolicyStore()
    BurdJournals.Server.sendAdminPolicyToClient(player, BurdJournals.safeGetText and BurdJournals.safeGetText("UI_BurdJournals_DebugWhitelistPolicyUpdated", "Whitelist policy updated.") or "Whitelist policy updated.")
end

function BurdJournals.Server.handleDebugBulkSetAdminPolicy(player, args)
    if not BurdJournals.Server.isDebugAllowed(player) then
        BurdJournals.Server.sendToClient(player, "debugError", {message = "Debug commands not allowed"})
        return
    end
    args = type(args) == "table" and args or {}
    local kind = BurdJournals.normalizeAdminPolicyKind and BurdJournals.normalizeAdminPolicyKind(args.kind) or nil
    local scope = BurdJournals.normalizeAdminPolicyScope and BurdJournals.normalizeAdminPolicyScope(args.scope) or nil
    local policy = BurdJournals.normalizeAdminPolicyValue and BurdJournals.normalizeAdminPolicyValue(args.policy) or nil
    local rawPolicy = tostring(args.policy or "inherit")
    if not (kind and scope and type(args.ids) == "table") or (rawPolicy ~= "inherit" and rawPolicy ~= "allow" and rawPolicy ~= "ban" and rawPolicy ~= "") then
        BurdJournals.Server.sendToClient(player, "debugError", {message = "Invalid whitelist bulk request"})
        return
    end
    local store = BurdJournals.Server.getAdminPolicyStore()
    local count = 0
    for _, rawId in ipairs(args.ids) do
        local id = rawId and tostring(rawId) or nil
        if id and id ~= "" then
            if BurdJournals.Server.applyAdminPolicyMutation(store, kind, id, scope, policy) then
                count = count + 1
            end
        end
    end
    BurdJournals.Server.transmitAdminPolicyStore()
    BurdJournals.Server.sendAdminPolicyToClient(player, BurdJournals.formatText(BurdJournals.safeGetText and BurdJournals.safeGetText("UI_BurdJournals_DebugWhitelistPolicyUpdatedCount", "Whitelist policy updated: %1 entrie(s).") or "Whitelist policy updated: %1 entrie(s).", count))
end

function BurdJournals.Server.handleDebugResetAdminPolicy(player, _args)
    if not BurdJournals.Server.isDebugAllowed(player) then
        BurdJournals.Server.sendToClient(player, "debugError", {message = "Debug commands not allowed"})
        return
    end
    local store = BurdJournals.Server.getAdminPolicyStore()
    store.skills = {}
    store.traits = {}
    store.recipes = {}
    if BurdJournals.setAdminPolicy then
        BurdJournals.setAdminPolicy(store)
    end
    BurdJournals.Server.transmitAdminPolicyStore()
    BurdJournals.Server.sendAdminPolicyToClient(player, BurdJournals.safeGetText and BurdJournals.safeGetText("UI_BurdJournals_DebugWhitelistPolicyReset", "Whitelist policy reset.") or "Whitelist policy reset.")
end


function BurdJournals.Server.init()
    -- Server started - verify baseline cache is properly loaded
    BurdJournals.debugPrint("[BurdJournals] Server.init() called - checking baseline cache...")
    
    -- Reset the logged flag so we report cache state on next access
    BurdJournals.Server._baselineCacheLogged = false

    -- Fallback for environments where OnInitGlobalModData may not fire reliably.
    -- IMPORTANT: Don't force-create global keys here or we may mask persisted keys
    -- during startup ordering races (observed during workshop update restarts).
    if not BurdJournals.Server._modDataInitialized and BurdJournals.Server.ensureBaselineModDataReady then
        local ready = BurdJournals.Server.ensureBaselineModDataReady(false)
        BurdJournals.debugPrint("[BurdJournals] Server.init: baseline ModData ready before init event = " .. tostring(ready))
    end
    
    -- Trigger a cache access to log current state
    local cache = BurdJournals.Server.getBaselineCache()
    local archive = BurdJournals.Server.getBaselineArchive and BurdJournals.Server.getBaselineArchive() or nil
    if BurdJournals.Server.backfillBaselineArchiveFromCache then
        BurdJournals.Server.backfillBaselineArchiveFromCache("server_init")
        archive = BurdJournals.Server.getBaselineArchive and BurdJournals.Server.getBaselineArchive() or archive
    end
    if BurdJournals.Server.seedBaselineSnapshotsFromExistingStores then
        BurdJournals.Server.seedBaselineSnapshotsFromExistingStores("server_init")
    end
    local playerCount = 0
    for _ in pairs(cache.players or {}) do playerCount = playerCount + 1 end
    local archivedCount = 0
    if archive and archive.byCharacterId then
        for _ in pairs(archive.byCharacterId) do archivedCount = archivedCount + 1 end
    end
    local snapshotCount = 0
    local snapshots = BurdJournals.Server.getBaselineSnapshotStore and BurdJournals.Server.getBaselineSnapshotStore() or nil
    if snapshots and snapshots.bySnapshotId then
        for _ in pairs(snapshots.bySnapshotId) do snapshotCount = snapshotCount + 1 end
    end
    local mirrorCount = 0
    local mirrorField = BurdJournals.Server.BASELINE_SNAPSHOT_ARCHIVE_MIRROR_FIELD or "baselineSnapshotsMirrorV1"
    local legacyMirrorField = BurdJournals.Server.BASELINE_SNAPSHOT_ARCHIVE_MIRROR_FIELD_LEGACY or "_baselineSnapshotsMirrorV1"
    local mirror = archive and (archive[mirrorField] or archive[legacyMirrorField]) or nil
    if type(mirror) == "table" and type(mirror.bySnapshotId) == "table" then
        for _ in pairs(mirror.bySnapshotId) do
            mirrorCount = mirrorCount + 1
        end
    end
    BurdJournals.debugPrint("[BurdJournals] Server init complete. Baseline cache has " .. playerCount
        .. " player baseline(s), archive has " .. archivedCount .. " entry(ies), snapshots have "
        .. snapshotCount .. " entry(ies), snapshot mirror has " .. mirrorCount .. " entry(ies)")
end

-- Called when global ModData is initialized/loaded from disk
function BurdJournals.Server.onInitGlobalModData(isNewGame)
    BurdJournals.debugPrint("[BurdJournals] OnInitGlobalModData called (isNewGame=" .. tostring(isNewGame) .. ")")
    
    -- CRITICAL: Invalidate cached instance so we re-fetch from ModData
    -- This ensures we get the properly loaded data from disk
    BurdJournals.Server._baselineCacheInstance = nil
    BurdJournals.Server._baselineArchiveInstance = nil
    BurdJournals.Server._baselineSnapshotInstance = nil
    BurdJournals.Server._baselineSnapshotSeeded = false
    
    -- Mark that ModData has been initialized - this is critical for knowing when it's safe to trust cache state
    BurdJournals.Server._modDataInitialized = true
    
    -- Reset flag to ensure we log the loaded cache state
    BurdJournals.Server._baselineCacheLogged = false
    
    -- Access the cache to ensure it's properly loaded and logged
    local cache = BurdJournals.Server.getBaselineCache()
    local archive = BurdJournals.Server.getBaselineArchive and BurdJournals.Server.getBaselineArchive() or nil
    if BurdJournals.Server.backfillBaselineArchiveFromCache then
        BurdJournals.Server.backfillBaselineArchiveFromCache("onInitGlobalModData")
        archive = BurdJournals.Server.getBaselineArchive and BurdJournals.Server.getBaselineArchive() or archive
    end
    if BurdJournals.Server.seedBaselineSnapshotsFromExistingStores then
        BurdJournals.Server.seedBaselineSnapshotsFromExistingStores("onInitGlobalModData")
    end
    local playerCount = 0
    local debugModifiedCount = 0
    for id, data in pairs(cache.players or {}) do 
        playerCount = playerCount + 1
        if data and data.debugModified then
            debugModifiedCount = debugModifiedCount + 1
            BurdJournals.debugPrint("[BurdJournals]   - Player " .. tostring(id) .. " has debug-modified baseline")
        end
    end
    local archivedCount = 0
    if archive and archive.byCharacterId then
        for _ in pairs(archive.byCharacterId) do
            archivedCount = archivedCount + 1
        end
    end
    local snapshotCount = 0
    local snapshots = BurdJournals.Server.getBaselineSnapshotStore and BurdJournals.Server.getBaselineSnapshotStore() or nil
    if snapshots and snapshots.bySnapshotId then
        for _ in pairs(snapshots.bySnapshotId) do
            snapshotCount = snapshotCount + 1
        end
    end
    local mirrorCount = 0
    local mirrorField = BurdJournals.Server.BASELINE_SNAPSHOT_ARCHIVE_MIRROR_FIELD or "baselineSnapshotsMirrorV1"
    local legacyMirrorField = BurdJournals.Server.BASELINE_SNAPSHOT_ARCHIVE_MIRROR_FIELD_LEGACY or "_baselineSnapshotsMirrorV1"
    local mirror = archive and (archive[mirrorField] or archive[legacyMirrorField]) or nil
    if type(mirror) == "table" and type(mirror.bySnapshotId) == "table" then
        for _ in pairs(mirror.bySnapshotId) do
            mirrorCount = mirrorCount + 1
        end
    end
    
    if isNewGame then
        BurdJournals.debugPrint("[BurdJournals] New game detected - baseline cache is fresh")
    else
        BurdJournals.debugPrint("[BurdJournals] Existing game loaded - baseline cache has " .. playerCount
            .. " player(s), " .. debugModifiedCount .. " debug-modified, archive has " .. archivedCount
            .. " entry(ies), snapshots have " .. snapshotCount .. " entry(ies), snapshot mirror has "
            .. mirrorCount .. " entry(ies)")
        
        -- Ensure data persists by triggering a transmit (belt and suspenders approach)
        if (playerCount > 0 or archivedCount > 0) and BurdJournals.Server.transmitBaselineStores then
            BurdJournals.Server.transmitBaselineStores(true)
            BurdJournals.debugPrint("[BurdJournals] Triggered baseline cache + archive transmit to ensure persistence")
        elseif playerCount > 0 and ModData.transmit and BurdJournals.Server.isTrueSinglePlayerContext and BurdJournals.Server.isTrueSinglePlayerContext() then
            ModData.transmit("BurdJournals_PlayerBaselines")
            BurdJournals.debugPrint("[BurdJournals] Triggered ModData.transmit to ensure persistence")
        end
        if snapshotCount > 0 and BurdJournals.Server.transmitBaselineSnapshotStore then
            BurdJournals.Server.transmitBaselineSnapshotStore()
            BurdJournals.debugPrint("[BurdJournals] Triggered baseline snapshot transmit to ensure persistence")
        end
    end
end

PLAYER_JOURNAL_RESTRICTED_COMMANDS = {
    logSkills = true,
    learnSkills = true,
    initializeJournal = true,
    recordProgress = true,
    syncJournalData = true,
    claimSkill = true,
    claimTrait = true,
    claimForgetSlot = true,
    claimRecipe = true,
    claimStat = true,
    absorbSkill = true,
    absorbTrait = true,
    absorbRecipe = true,
    batchClaimRewards = true,
    batchAbsorbRewards = true,
    eraseJournal = true,
    eraseEntry = true,
    renameJournal = true,
    saveJournalNotes = true,
    dissolveJournal = true,
}

local function isPlayerJournalInteractionBlocked(command, player, args)
    if not PLAYER_JOURNAL_RESTRICTED_COMMANDS[command] then
        return false
    end
    if BurdJournals.isPlayerJournalsEnabled and BurdJournals.isPlayerJournalsEnabled() then
        return false
    end

    local journal = resolveServerCommandJournal(player, args, command)
    if not journal then
        return false
    end
    local modData = journal:getModData()
    local journalData = modData and modData.BurdJournals or nil
    return journalData and journalData.isPlayerCreated == true
end

function BurdJournals.Server.onPlayerDeathPrepareRespawnJournal(player)
    -- Deprecated: filled player-journal respawn restoration is intentionally disabled.
    return
end

function BurdJournals.Server.handleRequestRespawnJournalProvision(player, args)
    local spawnBlank = BurdJournals.getSandboxOption("SpawnWithBlankPlayerJournal") == true
    if not spawnBlank then
        return
    end

    if BurdJournals.isPlayerJournalsEnabled and not BurdJournals.isPlayerJournalsEnabled() then
        return
    end

    local playerModData = player and player.getModData and player:getModData() or nil
    if type(playerModData) ~= "table" then
        return
    end
    if type(playerModData.BurdJournals) ~= "table" then
        playerModData.BurdJournals = {}
    end
    local playerJournalState = playerModData.BurdJournals
    if playerJournalState.spawnBlankPlayerJournalProvisioned == true then
        return
    end

    local currentJournal = BurdJournals.Server.findRespawnPlayerJournalCandidate(player)
    local provisionedJournal = currentJournal
    if not provisionedJournal then
        provisionedJournal = BurdJournals.Server.createBlankRespawnPlayerJournal(player)
    end
    if not provisionedJournal then
        -- Do not latch a failure. A later OnCreatePlayer/request may safely retry.
        return
    end

    -- This marker lives on the character's own persisted ModData: reconnecting
    -- the same survivor remains idempotent, while a post-death character gets a
    -- fresh marker even when they reuse the same Steam account and name.
    playerJournalState.spawnBlankPlayerJournalProvisioned = true
    if player.transmitModData then
        player:transmitModData()
    end
end

function BurdJournals.Server.onClientCommand(module, command, player, args)
    -- Ignore other mods before touching BSJ response scopes or cleanup caches.
    if module ~= "BurdJournals" then return end
    if type(command) ~= "string" or not BurdJournals.Server.RECOGNIZED_CLIENT_COMMANDS[command] then
        return
    end
    -- Recover deterministically if a previous required handler raised before its
    -- batch invocation scope could be cleared. Never let that stale scope affect
    -- a later client command.
    local responseScopeKey = BurdJournals.Server.getBatchRewardPlayerKey
        and BurdJournals.Server.getBatchRewardPlayerKey(player)
        or tostring(player)
    BurdJournals.Server._batchResponseScopes[responseScopeKey] = nil
    local commandNowMs = (getTimestampMs and getTimestampMs()) or ((os.time() or 0) * 1000)
    local lastTransientCleanupAt = tonumber(BurdJournals.Server._lastTransientCommandCleanupAt) or 0
    if (commandNowMs - lastTransientCleanupAt) >= 60000 then
        BurdJournals.Server._lastTransientCommandCleanupAt = commandNowMs
        BurdJournals.Server.cleanupTransientCommandCaches(commandNowMs)
    end
    if type(args) ~= "table" then
        args = {}
    end

    if BurdJournals.flushPendingRuntimeShardTransmits then
        BurdJournals.flushPendingRuntimeShardTransmits(false)
    end

    BurdJournals.debugPrint("[BurdJournals] Server received command: " .. tostring(command) .. " from player: " .. tostring(player and player.getUsername and player:getUsername() or "unknown"))

    if not player then
        bsjWriteLogLine("[BurdJournals] ERROR: No player in command")
        return
    end

    -- Throttle the per-command compaction preamble to at most once per player per
    -- ~10s. It previously ran on every command, including the rapid-fire per-skill
    -- claim/absorb queue (~120ms apart), doing repeated full table scans each time.
    local preambleKey
    if player.getOnlineID then
        preambleKey = tostring(player:getOnlineID())
    elseif player.getUsername then
        preambleKey = tostring(player:getUsername())
    else
        preambleKey = "unknown"
    end
    BurdJournals.Server._lastPreambleCompactAt = BurdJournals.Server._lastPreambleCompactAt or {}
    local preambleNow = BurdJournals._getRuntimeNowMs and BurdJournals._getRuntimeNowMs() or ((os.time() or 0) * 1000)
    local lastPreambleAt = tonumber(BurdJournals.Server._lastPreambleCompactAt[preambleKey]) or 0
    local shouldRunPreambleCompaction = (preambleNow - lastPreambleAt) >= 10000
    if shouldRunPreambleCompaction then
        BurdJournals.Server._lastPreambleCompactAt[preambleKey] = preambleNow
    end

    local skipBaselineCompaction = command == "requestBaseline" or command == "registerBaseline"
    if shouldRunPreambleCompaction and BurdJournals.compactPlayerBurdJournalsData and not skipBaselineCompaction then
        local changed, removedLegacy, removedTransient, removedSkills, removedTraits, removedRecipes =
            BurdJournals.compactPlayerBurdJournalsData(player, true)
        if changed then
            BurdJournals.debugPrint("[BurdJournals] Server compacted player BurdJournals data for "
                .. tostring(player:getUsername())
                .. ": legacy=" .. tostring(removedLegacy)
                .. ", transient=" .. tostring(removedTransient)
                .. ", skills=" .. tostring(removedSkills)
                .. ", traits=" .. tostring(removedTraits)
                .. ", recipes=" .. tostring(removedRecipes))
        end
    end

    if shouldRunPreambleCompaction and BurdJournals.compactPlayerJournalDRCache then
        local changed, removedJournals, removedAliases = BurdJournals.compactPlayerJournalDRCache(player, true)
        if changed then
            BurdJournals.debugPrint("[BurdJournals] Server compacted player DR cache for "
                .. tostring(player:getUsername()) .. ": removed "
                .. tostring(removedJournals) .. " journals, "
                .. tostring(removedAliases) .. " aliases")
        end
    end

    -- Get player ID safely - getOnlineID may not exist on older builds
    local playerId
    if player.getOnlineID then
        playerId = tostring(player:getOnlineID())
    elseif player.getUsername then
        playerId = tostring(player:getUsername())
    else
        playerId = "unknown"
    end

    -- Rate limiting is command-scoped so one operation cannot block another.
    -- Timed-action commands use short explicit intervals; bulk commands use longer ones.
    local isDebugCommand = type(command) == "string" and string.sub(command, 1, 5) == "debug"
    if getTimestampMs then
        local now = getTimestampMs()
        local commandInterval = tonumber(COMMAND_RATE_LIMIT_MS[command]) or (isDebugCommand and 250 or 100)
        local rateLimitKey = playerId .. "|" .. tostring(command)
        local lastCmd = BurdJournals.Server._rateLimitCache[rateLimitKey] or 0
        if lastCmd > 0 and now - lastCmd < commandInterval then
            BurdJournals.debugPrint("[BurdJournals] Server: RATE LIMITED command " .. tostring(command)
                .. " (only " .. tostring(now - lastCmd) .. "ms since last; minimum=" .. tostring(commandInterval) .. "ms)")
            if command == "sanitizeJournal" then
                local requestId = type(args) == "table" and args.requestId or nil
                if type(requestId) ~= "string" or #requestId > 128 or requestId:find("%c") then requestId = nil end
                BurdJournals.Server.sendToClient(player, "syncSuccess", {
                    sanitizeRequestId = requestId,
                    sanitizeError = "rateLimited",
                    retryAfterMs = commandInterval - (now - lastCmd),
                    serverRuntimeMarker = BurdJournals.RUNTIME_LOAD_MARKER,
                })
            end
            return
        end
        BurdJournals.Server._rateLimitCache[rateLimitKey] = now

        -- Periodic cleanup (1% chance per command) - use ZombRand for PZ compatibility
        local rand = ZombRand and ZombRand(100) or 1
        if rand == 0 then
            BurdJournals.Server.cleanupRateLimitCache()
        end
    end

    local isEnabled = BurdJournals.isEnabled()
    BurdJournals.debugPrint("[BurdJournals] onClientCommand: isEnabled=" .. tostring(isEnabled))
    if not isEnabled then
        bsjWriteLogLine("[BurdJournals] onClientCommand ERROR: Journals disabled!")
        BurdJournals.Server.sendToClient(player, "error", {message = "Journals are disabled on this server."})
        return
    end
    if isPlayerJournalInteractionBlocked(command, player, args) then
        BurdJournals.debugPrint("[BurdJournals] Blocked player journal command while player journals are disabled: " .. tostring(command))
        BurdJournals.Server.sendToClient(player, "error", {
            message = (getText and getText("UI_BurdJournals_PlayerJournalsDisabled")) or "Player journals are disabled on this server."
        })
        return
    end

    BurdJournals.debugPrint("[BurdJournals] onClientCommand: Routing command '" .. tostring(command) .. "'")
    if command == "logSkills" then
        -- Retired legacy protocol: its remove-then-create transaction could
        -- destroy the source journal if materialization failed. Current clients
        -- use recordProgress, which is bounded and server-authoritative.
        BurdJournals.Server.sendToClient(player, "error", {message = "This journal recording request is no longer supported.", errorCode = "legacyLogSkillsRetired"})
    elseif command == "learnSkills" then
        BurdJournals.Server.handleLearnSkills(player, args)
    elseif command == "openCursedJournal" then
        BurdJournals.Server.handleOpenCursedJournal(player, args)
    elseif command == "requestCursedInsightPreview" then
        BurdJournals.Server.handleRequestCursedInsightPreview(player, args)
    elseif command == "openYuletideJournal" then
        BurdJournals.Server.handleOpenYuletideJournal(player, args)
    elseif command == "breakJournalSeal" then
        BurdJournals.Server.handleBreakJournalSeal(player, args)
    elseif command == "yuletideGiftMaterialized" then
        BurdJournals.Server.handleYuletideGiftMaterialized(player, args)
    elseif command == "absorbSkill" then
        BurdJournals.Server.handleAbsorbSkill(player, args)
    elseif command == "absorbTrait" then
        BurdJournals.Server.handleAbsorbTrait(player, args)
    elseif command == "claimSkill" then
        BurdJournals.Server.handleClaimSkill(player, args)
    elseif command == "claimTrait" then
        BurdJournals.Server.handleClaimTrait(player, args)
    elseif command == "claimForgetSlot" then
        BurdJournals.Server.handleClaimForgetSlot(player, args)
    elseif command == "eraseJournal" then
        BurdJournals.Server.handleEraseJournal(player, args)
    elseif command == "cleanBloody" then
        BurdJournals.Server.handleCleanBloody(player, args)
    elseif command == "convertToClean" then
        BurdJournals.Server.handleConvertToClean(player, args)
    elseif command == "initializeJournal" then
        BurdJournals.Server.handleInitializeJournal(player, args)
    elseif command == "recordProgress" then
        BurdJournals.debugPrint("[BurdJournals] ROUTING recordProgress to handler NOW")
        BurdJournals.Server.handleRecordProgress(player, args)
    elseif command == "syncJournalData" then
        BurdJournals.Server.handleSyncJournalData(player, args)
    elseif command == "requestJournalEntryChunk" then
        BurdJournals.Server.handleRequestJournalEntryChunk(player, args)
    elseif command == "claimRecipe" then
        BurdJournals.Server.handleClaimRecipe(player, args)
    elseif command == "claimStat" then
        BurdJournals.Server.handleClaimStat(player, args)
    elseif command == "absorbRecipe" then
        BurdJournals.Server.handleAbsorbRecipe(player, args)
    elseif command == "eraseEntry" then
        BurdJournals.Server.handleEraseEntry(player, args)
    elseif command == "registerBaseline" then
        BurdJournals.Server.handleRegisterBaseline(player, args)
    elseif command == "requestBaseline" then
        BurdJournals.Server.handleRequestBaseline(player, args)
    elseif command == "requestRespawnJournalProvision" then
        BurdJournals.Server.handleRequestRespawnJournalProvision(player, args)
    elseif command == "trackVhsSkillXP" then
        BurdJournals.Server.handleTrackVhsSkillXP(player, args)
    elseif command == "deleteBaseline" then
        BurdJournals.Server.handleDeleteBaseline(player, args)
    elseif command == "dissolveJournal" then
        BurdJournals.Server.handleDissolveJournal(player, args)
    elseif command == "sanitizeJournal" then
        BurdJournals.Server.handleSanitizeJournal(player, args)
    elseif command == "saveJournalNotes" then
        BurdJournals.Server.handleSaveJournalNotes(player, args)
    elseif command == "clearAllBaselines" then
        BurdJournals.Server.handleClearAllBaselines(player, args)
    elseif command == "renameJournal" then
        BurdJournals.Server.handleRenameJournal(player, args)
    elseif command == "removeCraftDuplicates" then
        BurdJournals.Server.handleRemoveCraftDuplicates(player, args)
    -- Debug commands (require debug permission)
    elseif command == "debugSetSkill" then
        BurdJournals.Server.handleDebugSetSkill(player, args)
    elseif command == "debugSetAllSkills" then
        BurdJournals.Server.handleDebugSetAllSkills(player, args)
    elseif command == "debugAddXP" then
        BurdJournals.Server.handleDebugAddXP(player, args)
    elseif command == "debugSetSkillToLevel" then
        BurdJournals.Server.handleDebugSetSkillToLevel(player, args)
    elseif command == "debugSetSkillXP" then
        BurdJournals.Server.handleDebugSetSkillXP(player, args)
    elseif command == "debugAddSkillXP" then
        BurdJournals.Server.handleDebugAddSkillXP(player, args)
    elseif command == "debugAddTrait" then
        BurdJournals.Server.handleDebugAddTrait(player, args)
    elseif command == "debugRemoveTrait" then
        BurdJournals.Server.handleDebugRemoveTrait(player, args)
    elseif command == "debugAddRecipe" then
        BurdJournals.Server.handleDebugAddRecipe(player, args)
    elseif command == "debugRemoveRecipe" then
        BurdJournals.Server.handleDebugRemoveRecipe(player, args)
    elseif command == "debugBulkRecipes" then
        BurdJournals.Server.handleDebugBulkRecipes(player, args)
    elseif command == "debugRemoveAllTraits" then
        BurdJournals.Server.handleDebugRemoveAllTraits(player, args)
    elseif command == "debugBulkTraits" then
        BurdJournals.Server.handleDebugBulkTraits(player, args)
    elseif command == "debugClearBaseline" then
        BurdJournals.Server.handleDebugClearBaseline(player, args)
    elseif command == "debugRecalcBaseline" then
        BurdJournals.Server.handleDebugRecalcBaseline(player, args)
    elseif command == "debugUpdateSkillBaseline" then
        BurdJournals.Server.handleDebugUpdateSkillBaseline(player, args)
    elseif command == "debugUpdateTraitBaseline" then
        BurdJournals.Server.handleDebugUpdateTraitBaseline(player, args)
    elseif command == "debugSaveBaselineDraft" then
        BurdJournals.Server.handleDebugSaveBaselineDraft(player, args)
    elseif command == "debugListBaselineCache" then
        BurdJournals.Server.handleDebugListBaselineCache(player, args)
    elseif command == "debugListBaselineSnapshots" then
        BurdJournals.Server.handleDebugListBaselineSnapshots(player, args)
    elseif command == "debugGetBaselineSnapshot" then
        BurdJournals.Server.handleDebugGetBaselineSnapshot(player, args)
    elseif command == "debugGetTargetBaselinePayload" then
        BurdJournals.Server.handleDebugGetTargetBaselinePayload(player, args)
    elseif command == "debugRequestCharacterData" then
        BurdJournals.Server.handleDebugRequestCharacterData(player, args)
    elseif command == "debugSaveBaselineSnapshot" then
        BurdJournals.Server.handleDebugSaveBaselineSnapshot(player, args)
    elseif command == "debugApplyBaselineSnapshot" then
        BurdJournals.Server.handleDebugApplyBaselineSnapshot(player, args)
    elseif command == "debugDeleteBaselineSnapshot" then
        BurdJournals.Server.handleDebugDeleteBaselineSnapshot(player, args)
    elseif command == "debugRequestAdminPolicy" then
        BurdJournals.Server.handleDebugRequestAdminPolicy(player, args)
    elseif command == "debugSetAdminPolicy" then
        BurdJournals.Server.handleDebugSetAdminPolicy(player, args)
    elseif command == "debugBulkSetAdminPolicy" then
        BurdJournals.Server.handleDebugBulkSetAdminPolicy(player, args)
    elseif command == "debugResetAdminPolicy" then
        BurdJournals.Server.handleDebugResetAdminPolicy(player, args)
    elseif command == "debugRequestLoreTemplateOptions" then
        BurdJournals.Server.handleDebugRequestLoreTemplateOptions(player, args)
    elseif command == "debugSpawnJournal" then
        BurdJournals.Server.handleDebugSpawnJournal(player, args)
    elseif command == "debugDissolveJournal" then
        BurdJournals.Server.handleDebugDissolveJournal(player, args)
    elseif command == "debugJournalCreated" then
        -- Client notifying server about a debug-spawned journal (for tracking)
        BurdJournals.debugPrint("[BurdJournals] DEBUG: Client created debug journal ID=" .. tostring(args.journalId) .. " type=" .. tostring(args.journalType))
    elseif command == "requestXpSync" then
        -- Client requesting XP sync after batch operations
        BurdJournals.Server.handleRequestXpSync(player, args)
    elseif command == "batchClaimRewards" then
        -- Process mixed claim rewards (skills/traits/recipes/stats) in one server-side call
        BurdJournals.Server.handleBatchClaimRewards(player, args)
    elseif command == "batchAbsorbRewards" then
        -- Process mixed absorb rewards (skills/traits/recipes/stats) in one server-side call
        BurdJournals.Server.handleBatchAbsorbRewards(player, args)
    elseif command == "saveDebugJournalBackup" then
        -- Save debug journal data to server-side global ModData for persistence
        BurdJournals.Server.handleSaveDebugJournalBackup(player, args)
    elseif command == "requestDebugJournalBackup" then
        -- Client requesting backup data for a debug journal
        BurdJournals.Server.handleRequestDebugJournalBackup(player, args)
    elseif command == "debugApplyJournalEdits" then
        -- Apply debug-edited journal data directly to server-side item ModData
        BurdJournals.Server.handleDebugApplyJournalEdits(player, args)
    elseif command == "debugMigrateOnlineJournals" then
        -- Admin one-shot migration for online players' journal data
        BurdJournals.Server.handleDebugMigrateOnlineJournals(player, args)
    elseif command == "debugLookupJournalByUUID" then
        -- Admin/debug lookup of live journal item by UUID
        BurdJournals.Server.handleDebugLookupJournalByUUID(player, args)
    elseif command == "debugRepairJournalByUUID" then
        -- Admin/debug repair pass for a live journal by UUID
        BurdJournals.Server.handleDebugRepairJournalByUUID(player, args)
    elseif command == "debugListJournalUUIDIndex" then
        -- Admin/debug fetch of UUID index metadata
        BurdJournals.Server.handleDebugListJournalUUIDIndex(player, args)
    elseif command == "debugDeleteJournalByUUID" then
        -- Admin/debug delete live journal item by UUID and purge cache/index entries
        BurdJournals.Server.handleDebugDeleteJournalByUUID(player, args)
    elseif command == "debugExportJournalJSON" then
        BurdJournals.Server.handleDebugExportJournalJSON(player, args)
    elseif command == "debugImportJournalJSON" then
        BurdJournals.Server.handleDebugImportJournalJSON(player, args)
    elseif command == "setVerboseLogging" then
        BurdJournals.Server.handleSetVerboseLogging(player, args)
    end

    if BurdJournals.flushPendingRuntimeShardTransmits then
        BurdJournals.flushPendingRuntimeShardTransmits(false)
    end
end

-- Admin-gated: propagate the client verbose-logging toggle to the server so that
-- server-side shouldDebugLog() (and the debug_* payload stripping in sendToClient)
-- respect the same flag. Non-admins are rejected.
function BurdJournals.Server.handleSetVerboseLogging(player, args)
    if not player then return end
    if not BurdJournals.Server.isDebugAdmin(player) then
        bsjWriteLogLine("[BurdJournals] WARNING: setVerboseLogging rejected for non-admin "
            .. tostring(player.getUsername and player:getUsername() or "?"))
        BurdJournals.Server.sendToClient(player, "setVerboseLoggingResult", {
            ok = false,
            message = "Admin access required.",
        })
        return
    end

    local enabled = args and args.enabled == true
    BurdJournals.verboseLogging = enabled
    bsjWriteLogLine("[BurdJournals] Verbose logging "
        .. (enabled and "ENABLED" or "DISABLED") .. " server-side by "
        .. tostring(player.getUsername and player:getUsername() or "?"))
    BurdJournals.Server.sendToClient(player, "setVerboseLoggingResult", {
        ok = true,
        enabled = enabled,
    })
end

-- Handle client request to sync XP (called at end of batch claiming)
function BurdJournals.Server.handleRequestXpSync(player, args)
    BurdJournals.debugPrint("[BurdJournals] Server: XP sync requested by " .. tostring(player:getUsername()))
    -- Try both sync methods - SyncXp (global) and player:syncXp (method)
    if SyncXp then
        SyncXp(player)
        BurdJournals.debugPrint("[BurdJournals] Server: XP synced via SyncXp global function")
    elseif player.syncXp then
        player:syncXp()
        BurdJournals.debugPrint("[BurdJournals] Server: XP synced via player:syncXp method")
    else
        bsjWriteLogLine("[BurdJournals] Server: WARNING - No sync method available!")
    end
    BurdJournals.Server.sendToClient(player, "xpSyncComplete", {})
end

-- Apply XP with compatibility fallbacks.
-- Some mods wrap/override XP APIs; this prevents one bad hook from breaking journal claims.
function BurdJournals.Server.applyXPWithFallback(player, perk, xpAmount, options)
    local amount = tonumber(xpAmount) or 0
    if not player or not perk or amount == 0 then
        return false, "invalid"
    end

    local skillName = options and options.skillName or "unknown"
    local xpObj = player.getXp and player:getXp() or nil
    local function getObservedXP()
        if BurdJournals.getPlayerSkillTotalXP then
            local totalXP = tonumber(BurdJournals.getPlayerSkillTotalXP(player, perk, skillName))
            if totalXP then
                return math.max(0, totalXP)
            end
        end
        if xpObj and xpObj.getXP then
            return math.max(0, tonumber(xpObj:getXP(perk)) or 0)
        end
        return 0
    end

    if amount > 0 and options and options.preferNativeNoMultiplier == true and addXpNoMultiplier then
        local beforeXP = getObservedXP()
        local ok = pcall(addXpNoMultiplier, player, perk, amount)
        local afterXP = getObservedXP()
        -- A third-party XP hook can mutate and then throw. Observed movement is
        -- already a committed mutation, so never fall through and award twice.
        if afterXP > beforeXP then
            return true, "addXpNoMultiplier"
        end
    end

    local compatVia = nil
    if BurdJournals.applyXPDeltaCompat then
        local ok, via = BurdJournals.applyXPDeltaCompat(player, perk, amount)
        if ok == true then
            return true, via or "compat"
        end
        compatVia = via or "compat"
        BurdJournals.debugPrint("[BurdJournals] XP apply failed via compat path for " .. tostring(skillName))
    end

    if amount > 0 and addXpNoMultiplier and xpObj and xpObj.getXP then
        local beforeXP = getObservedXP()
        local ok = pcall(addXpNoMultiplier, player, perk, amount)
        local afterXP = getObservedXP()
        if afterXP > beforeXP then
            return true, "addXpNoMultiplier"
        end
    end

    if xpObj and xpObj.AddXP then
        local beforeXP = getObservedXP()
        local ok = pcall(xpObj.AddXP, xpObj, perk, amount)
        local afterXP = getObservedXP()
        if (
            (amount > 0 and afterXP > beforeXP)
            or (amount < 0 and afterXP < beforeXP)
        ) then
            return true, "AddXP2"
        end
    end

    return false, compatVia or "none"
end

function BurdJournals.Server.sendExactSkillXPSync(player, skillName, totalXP)
    if not player or not skillName then
        return false
    end
    if not (BurdJournals.Server and BurdJournals.Server.sendToClient) then
        return false
    end

    local exactXP = math.max(0, tonumber(totalXP) or 0)
    BurdJournals.debugPrint("[BurdJournals] Server: Sending exact XP sync for " .. tostring(skillName) .. " => " .. tostring(exactXP))
    BurdJournals.Server.sendToClient(player, "applyXP", {
        skills = {
            [skillName] = {
                xp = exactXP,
                mode = "set"
            }
        },
        mode = "set",
        serverAuthoritative = true
    })
    return true
end

function BurdJournals.Server.applySetModeSkillTargetXP(player, perk, skillName, targetXP, options)
    if not player or not perk or not skillName then
        return false, 0, "invalid", 0
    end

    local desiredXP = math.max(0, tonumber(targetXP) or 0)
    local startingTotalXP = math.max(0, tonumber(BurdJournals.getPlayerSkillTotalXP and BurdJournals.getPlayerSkillTotalXP(player, perk, skillName) or player:getXp():getXP(perk)) or 0)
    if desiredXP <= startingTotalXP then
        return true, 0, "already", startingTotalXP
    end

    local isPassiveSkill = BurdJournals.isPassiveSkill and BurdJournals.isPassiveSkill(skillName) or (skillName == "Fitness" or skillName == "Strength")
    if isPassiveSkill and BurdJournals.Server.removePassiveSkillTraits then
        BurdJournals.Server.removePassiveSkillTraits(player, skillName)
    end

    local ok, appliedVia, finalXP = false, "none", startingTotalXP
    if BurdJournals.setSkillTotalXPCompat then
        ok, appliedVia, finalXP = BurdJournals.setSkillTotalXPCompat(player, perk, desiredXP, skillName)
    end
    if not ok then
        local afterFailedXP = math.max(0, tonumber(BurdJournals.getPlayerSkillTotalXP and BurdJournals.getPlayerSkillTotalXP(player, perk, skillName) or player:getXp():getXP(perk)) or 0)
        return false, math.max(0, afterFailedXP - startingTotalXP), appliedVia, afterFailedXP
    end

    local finalTotalXP = math.max(0, tonumber(finalXP) or tonumber(BurdJournals.getPlayerSkillTotalXP and BurdJournals.getPlayerSkillTotalXP(player, perk, skillName) or player:getXp():getXP(perk)) or 0)
    local actualAddedXP = math.max(0, finalTotalXP - startingTotalXP)
    local reachedTarget = finalTotalXP >= (desiredXP - 0.001)
    return reachedTarget or actualAddedXP > 0, actualAddedXP, appliedVia or "none", finalTotalXP
end

function BurdJournals.Server.handleTrackVhsSkillXP(player, args)
    if not player or type(args) ~= "table" then
        return
    end

    local skillPayload, tooLarge = nil, false
    if BurdJournals.normalizeTableLimited then
        skillPayload, tooLarge = BurdJournals.normalizeTableLimited(args.skills, 64)
    else
        skillPayload = BurdJournals.normalizeTable and BurdJournals.normalizeTable(args.skills) or args.skills
    end
    if tooLarge then
        bsjWriteLogLine("[BurdJournals] WARNING: Rejected oversized VHS tracking payload from "
            .. tostring(player.getUsername and player:getUsername() or "unknown"))
        return
    end
    if type(skillPayload) ~= "table" then
        return
    end

    local allowedSet = {}
    for _, allowedName in ipairs(BurdJournals.getAllowedSkills and BurdJournals.getAllowedSkills() or {}) do
        allowedSet[allowedName] = true
    end
    local normalized = {}
    local accepted = 0
    for skillName, xpDelta in pairs(skillPayload) do
        if type(skillName) == "string" and #skillName > 0 and #skillName <= 128 and not skillName:find("%c") then
            local resolvedCandidate = allowedSet[skillName] and skillName
                or (BurdJournals.resolveSkillKey and BurdJournals.resolveSkillKey(allowedSet, skillName))
            local resolvedName = resolvedCandidate and allowedSet[resolvedCandidate] and resolvedCandidate or nil
            local parsed = tonumber(xpDelta)
            if resolvedName and parsed and parsed == parsed and parsed > 0 and parsed <= 1000000 and parsed < math.huge then
                accepted = accepted + 1
                if accepted > 32 then
                    bsjWriteLogLine("[BurdJournals] WARNING: Rejected VHS tracking payload with too many valid skills from "
                        .. tostring(player.getUsername and player:getUsername() or "unknown"))
                    return
                end
                normalized[resolvedName] = parsed
            end
        end
    end

    if not BurdJournals.hasAnyEntries(normalized) then
        return
    end

    if BurdJournals.recordVhsSkillXP then
        local lineGuid = type(args.lineGuid) == "string" and #args.lineGuid <= 256 and not args.lineGuid:find("%c") and args.lineGuid or nil
        local category = type(args.category) == "string" and #args.category <= 128 and not args.category:find("%c") and args.category or nil
        BurdJournals.recordVhsSkillXP(player, normalized, lineGuid, category, "clientCommand")
    end
end

function BurdJournals.Server.syncRecipeKnowledgeForPlayer(player)
    if BurdJournals.invalidateAuthoritativeKnownRecipeCache then
        BurdJournals.invalidateAuthoritativeKnownRecipeCache(player)
    end
    if sendSyncPlayerFields then
        sendSyncPlayerFields(player, 0x00000001)
    end
end

function BurdJournals.Server.listContainsValueCompat(listObj, value)
    if not listObj or value == nil then
        return false
    end
    if listObj.contains then
        local okContains, contains = BurdJournals.safePcall(function()
            return listObj:contains(value)
        end)
        if okContains and contains then
            return true
        end
    end
    return false
end

function BurdJournals.Server.addKnownRecipeDirect(player, recipeName)
    if not player or type(recipeName) ~= "string" or recipeName == "" then
        return false
    end

    local knownRecipesList = player.getKnownRecipes and player:getKnownRecipes() or nil
    if not knownRecipesList or not knownRecipesList.add then
        return false
    end

    if BurdJournals.Server.listContainsValueCompat(knownRecipesList, recipeName) then
        return true
    end

    local okAdd = BurdJournals.safePcall(function()
        knownRecipesList:add(recipeName)
    end)
    return okAdd == true
end

function BurdJournals.Server.learnRecipeInBatch(player, recipeName, knownRecipes)
    if not player or type(recipeName) ~= "string" or recipeName == "" then
        return false
    end
    if type(knownRecipes) == "table" and knownRecipes[recipeName] == true then
        return true
    end

    local learnTarget = recipeName
    if BurdJournals.validateRecipeName then
        local validatedName = BurdJournals.validateRecipeName(recipeName)
        if validatedName and validatedName ~= "" then
            learnTarget = validatedName
        end
    end

    -- Match the single-recipe claim path: a non-throwing API call is not proof
    -- that a modded recipe became known. Only consume the journal reward after
    -- the authoritative recipe set confirms the mutation.
    local okLearn = BurdJournals.learnRecipeWithVerification
        and BurdJournals.learnRecipeWithVerification(player, learnTarget, "[BurdJournals Server Batch]") == true
        or false

    if okLearn and type(knownRecipes) == "table" then
        knownRecipes[recipeName] = true
        knownRecipes[learnTarget] = true
    end
    return okLearn
end

function BurdJournals.Server.syncMagazineReadStateForRecipe(player, recipeName)
    if not player or type(recipeName) ~= "string" or recipeName == "" then
        return false
    end
    local magazineType = BurdJournals.getMagazineForRecipe and BurdJournals.getMagazineForRecipe(recipeName) or nil
    if not magazineType then
        return false
    end

        local pageCount = 1
        local scriptManager = getScriptManager and getScriptManager() or nil
        if scriptManager and scriptManager.getItem then
            local script = scriptManager:getItem(magazineType)
            if script and script.getPageToLearn then
                pageCount = script:getPageToLearn() or 1
            end
        end

        if player.setAlreadyReadPages then
            BurdJournals.safePcall(function()
                player:setAlreadyReadPages(magazineType, pageCount)
            end)
        end

        local readBooks = player.getAlreadyReadBook and player:getAlreadyReadBook() or nil
        if readBooks and readBooks.add and not BurdJournals.Server.listContainsValueCompat(readBooks, magazineType) then
            BurdJournals.safePcall(function()
                readBooks:add(magazineType)
            end)
        end

        if player.learnRecipe then
            BurdJournals.safePcall(function()
                player:learnRecipe(recipeName)
            end)
        end
    return true
end

local removeJournalCompletely

BurdJournals.Server.MAX_BATCH_REWARD_ENTRIES = 128
BurdJournals.Server.MAX_BATCH_REWARD_INSPECTED_ENTRIES = 256

function BurdJournals.Server.isValidBatchRewardRequestId(requestId)
    return type(requestId) == "string"
        and #requestId > 0
        and #requestId <= 128
        and not requestId:find("%c")
end

function BurdJournals.Server.sanitizeBatchRewardEntries(args)
    if type(args) ~= "table" then return nil, "invalidRequest" end
    local result = {skills = {}, traits = {}, recipes = {}, stats = {}, total = 0}
    local seen = {skills = {}, traits = {}, recipes = {}, stats = {}}
    local inspected = 0

    local function isValidEntryText(value, maxLength)
        return type(value) == "string" and #value > 0 and #value <= maxLength
            and value:find("%S") ~= nil and value:find("%c") == nil
    end

    local function add(kind, value, key)
        inspected = inspected + 1
        if inspected > BurdJournals.Server.MAX_BATCH_REWARD_INSPECTED_ENTRIES then return false, "batchTooLarge" end
        if seen[kind][key] then return true end
        seen[kind][key] = true
        result.total = result.total + 1
        if result.total > BurdJournals.Server.MAX_BATCH_REWARD_ENTRIES then return false, "batchTooLarge" end
        result[kind][#result[kind] + 1] = value
        return true
    end

    for _, kind in ipairs({"skills", "traits", "recipes", "stats"}) do
        local source = args[kind]
        if source ~= nil and type(source) ~= "table" then return nil, "invalidEntries" end
        for _, entry in pairs(source or {}) do
            local value, key
            if kind == "skills" then
                local skillName = type(entry) == "table" and entry.skillName or nil
                if not isValidEntryText(skillName, 128) then return nil, "invalidEntries" end
                value, key = {skillName = skillName}, string.lower(skillName)
            elseif kind == "traits" then
                if not isValidEntryText(entry, 128) then return nil, "invalidEntries" end
                local normalized = BurdJournals.normalizeTraitId and BurdJournals.normalizeTraitId(entry) or entry
                value, key = entry, string.lower(tostring(normalized))
            elseif kind == "recipes" then
                if not isValidEntryText(entry, 512) then return nil, "invalidEntries" end
                value, key = entry, string.lower(entry)
            else
                local statId = type(entry) == "table" and entry.statId or nil
                if not isValidEntryText(statId, 128) then return nil, "invalidEntries" end
                value, key = {statId = statId}, string.lower(statId)
            end
            if key == "" then return nil, "invalidEntries" end
            local ok, reason = add(kind, value, key)
            if not ok then return nil, reason end
        end
    end
    return result
end

function BurdJournals.Server.sendBatchRewardFailure(player, mode, requestId, reason, message)
    if message then BurdJournals.Server.sendToClient(player, "error", {message = message}) end
    BurdJournals.Server.sendToClient(player, "batchClaimComplete", {
        count = 0, total = 0, failed = 0, mode = mode,
        requestId = requestId, reason = reason,
    })
end

BurdJournals.Server.BatchRewardQueue = BurdJournals.Server.BatchRewardQueue or {
    jobs = {},
    counter = 0,
    activeCount = 0,
    tickRegistered = false,
    tickBudget = 1,
    globalTickBudget = 2,
    asyncThreshold = 2,
    maxGlobalJobs = 24,
    maxPlayerJobs = 2,
    nextIndex = 1,
    maxJobAgeMs = 60000,
}
BurdJournals.Server.BatchRewardRequestCache = BurdJournals.Server.BatchRewardRequestCache or {}

function BurdJournals.Server.getBatchRewardRequestKey(player, requestId)
    if type(requestId) ~= "string" or requestId == "" then return nil end
    local playerKey = player and player.getUsername and player:getUsername() or tostring(player)
    return tostring(playerKey) .. "|" .. requestId
end

function BurdJournals.Server.getBatchRewardPlayerKey(player)
    if player and player.getOnlineID then
        local onlineId = player:getOnlineID()
        if onlineId ~= nil and tostring(onlineId) ~= "-1" then
            return "online:" .. tostring(onlineId)
        end
    end
    if player and player.getUsername then
        return "user:" .. tostring(player:getUsername() or "unknown")
    end
    return tostring(player)
end

function BurdJournals.Server.canEnqueueBatchRewardJob(player)
    local queue = BurdJournals.Server.BatchRewardQueue
    local globalLimit = math.max(1, tonumber(queue.maxGlobalJobs) or 24)
    local playerLimit = math.max(1, tonumber(queue.maxPlayerJobs) or 2)
    local activeGlobal = 0
    local activeForPlayer = 0
    local playerKey = BurdJournals.Server.getBatchRewardPlayerKey(player)
    for _, job in ipairs(queue.jobs or {}) do
        if type(job) == "table" and job.completed ~= true then
            activeGlobal = activeGlobal + 1
            if job.playerKey == playerKey then
                activeForPlayer = activeForPlayer + 1
            end
        end
    end
    if activeForPlayer >= playerLimit then return false, "playerQueueFull" end
    if activeGlobal >= globalLimit then return false, "serverQueueFull" end
    return true
end
function BurdJournals.Server.isBatchRewardPlayerAvailable(job)
    local player = job and job.player or nil
    if not player or (player.getUsername and player:getUsername() == nil) then return false end
    if not isStrictMPServer() then return true end
    local onlinePlayers = getOnlinePlayers and getOnlinePlayers() or nil
    if not (onlinePlayers and onlinePlayers.size) then return true end
    for index = 0, onlinePlayers:size() - 1 do
        if onlinePlayers:get(index) == player then return true end
    end
    return false
end

function BurdJournals.Server.cancelBatchRewardJob(job, reason)
    if not job or job.completed then return end
    -- Cancellation can happen after earlier entries have already committed. Use
    -- the normal completion path so those mutations are transmitted/flushed and
    -- every unprocessed entry receives an explicit failed outcome.
    if BurdJournals.Server.completeBatchRewardJob then
        BurdJournals.Server.completeBatchRewardJob(job, reason or "cancelled", true)
        return
    end
    job.completed = true
    job.cancelReason = reason or "cancelled"
    local queue = BurdJournals.Server.BatchRewardQueue
    queue.activeCount = math.max(0, (tonumber(queue.activeCount) or 1) - 1)
    local key = BurdJournals.Server.getBatchRewardRequestKey(job.player, job.requestId)
    if key then BurdJournals.Server.BatchRewardRequestCache[key] = nil end
end

function BurdJournals.Server.cancelBatchRewardJobsForPlayer(player, reason)
    if not player then return 0 end
    local queue = BurdJournals.Server.BatchRewardQueue
    local playerKey = BurdJournals.Server.getBatchRewardPlayerKey(player)
    local cancelled = 0
    for _, job in ipairs(queue and queue.jobs or {}) do
        if type(job) == "table" and job.completed ~= true
            and (job.player == player or job.playerKey == playerKey)
        then
            BurdJournals.Server.cancelBatchRewardJob(job, reason or "playerUnavailable")
            cancelled = cancelled + 1
        end
    end
    BurdJournals.Server._batchResponseScopes[playerKey] = nil
    return cancelled
end

function BurdJournals.Server.replayBatchRewardRequest(player, requestId)
    local key = BurdJournals.Server.getBatchRewardRequestKey(player, requestId)
    local cached = key and BurdJournals.Server.BatchRewardRequestCache[key] or nil
    if type(cached) ~= "table" then return false end
    if cached.payload then
        BurdJournals.Server.sendToClient(player, "batchClaimComplete", cached.payload)
    elseif cached.job and not cached.job.completed then
        BurdJournals.Server.sendToClient(player, "batchClaimQueued", {
            mode = cached.job.mode,
            jobId = cached.job.id,
            requestId = requestId,
            total = cached.job.total or 0,
        })
    else
        return false
    end
    return true
end

function BurdJournals.Server.cacheBatchRewardRequest(player, requestId, job, payload)
    local key = BurdJournals.Server.getBatchRewardRequestKey(player, requestId)
    if not key then return end
    local cache = BurdJournals.Server.BatchRewardRequestCache
    local nowMs = (getTimestampMs and getTimestampMs()) or ((os.time() or 0) * 1000)
    cache[key] = {job = job, payload = payload, storedAt = nowMs}
    local entries = {}
    for cacheKey, entry in pairs(cache) do
        local storedAt = type(entry) == "table" and tonumber(entry.storedAt) or 0
        local activeJob = type(entry) == "table" and entry.job or nil
        if activeJob and activeJob.completed ~= true then
            -- Active request identities are an idempotency boundary. Never evict
            -- them while their job is still queued, or a retry can enqueue the
            -- same mutation a second time.
            entries[#entries + 1] = {key = cacheKey, storedAt = storedAt, active = true}
        elseif storedAt > 0 and (nowMs - storedAt) > 600000 then
            cache[cacheKey] = nil
        else
            entries[#entries + 1] = {key = cacheKey, storedAt = storedAt, active = false}
        end
    end
    if #entries > 256 then
        table.sort(entries, function(a, b) return a.storedAt < b.storedAt end)
        local removeCount = #entries - 256
        for _, entry in ipairs(entries) do
            if removeCount <= 0 then break end
            if not entry.active then
                cache[entry.key] = nil
                removeCount = removeCount - 1
            end
        end
    end
end

function BurdJournals.Server.getBatchJobJournal(job)
    if not job or not job.player then
        return nil
    end
    -- Jobs span multiple ticks, so a reference that was valid when queued may
    -- since have been dropped, traded, or moved to another player. Re-resolve
    -- possession for every entry and never fall back to the nearby-world scan.
    local journal = nil
    local inventorySearched = false
    if BurdJournals.Server.findJournalInRequestersInventory then
        journal, inventorySearched = BurdJournals.Server.findJournalInRequestersInventory(
            job.player,
            job.journalId,
            job.journalUUID,
            job.journalFingerprint
        )
    end
    if not inventorySearched then
        journal = nil
    end
    job.journal = journal
    return journal
end

function BurdJournals.Server.addUniqueBatchTrait(target, seen, traitId)
    if type(traitId) ~= "string" or traitId == "" then
        return
    end
    local key = string.lower(tostring(BurdJournals.normalizeTraitId and BurdJournals.normalizeTraitId(traitId) or traitId))
    if key ~= "" and not seen[key] then
        seen[key] = true
        target[#target + 1] = traitId
    end
end

function BurdJournals.Server.buildQueuedBatchCompletePayload(job)
    local finalJournal = BurdJournals.Server.getBatchJobJournal(job)
    local finalJournalData = job.journalData
    if finalJournal and finalJournal.getModData then
        finalJournalData = finalJournal:getModData().BurdJournals or finalJournalData
    end

    local payload = {
        mode = job.mode,
        count = (job.processedSkills or 0) + (job.processedTraits or 0) + (job.processedRecipes or 0) + (job.processedStats or 0),
        total = job.total or 0,
        skillsProcessed = job.processedSkills or 0,
        traitsProcessed = job.processedTraits or 0,
        recipesProcessed = job.processedRecipes or 0,
        statsProcessed = job.processedStats or 0,
        failed = job.failedCount or 0,
        outcomes = job.outcomes or {},
        queued = true,
        jobId = job.id,
        requestId = job.requestId,
        dissolved = job.dissolved == true,
    }
    if job.mode == "claim" then
        payload.completedSkills = job.completedSkills or {}
        payload.completedSkillXP = job.completedSkillXP or {}
        payload.claimedTraits = job.claimedTraits or {}
        payload.completedTraits = job.completedTraits or {}
        payload.completedRecipes = job.completedRecipes or {}
    end
    payload.claimedStats = job.claimedStats or {}

    return buildBatchJournalResponse(payload, job.player, job.journalId, job.journalUUID, finalJournalData, job.mode == "absorb" and "batchAbsorbComplete" or "batchClaimComplete")
end

function BurdJournals.Server.getBatchRewardJobProcessedCount(job)
    return (job and tonumber(job.processedSkills) or 0)
        + (job and tonumber(job.processedTraits) or 0)
        + (job and tonumber(job.processedRecipes) or 0)
        + (job and tonumber(job.processedStats) or 0)
end

function BurdJournals.Server.maybeSendBatchRewardProgress(job, force)
    if not job or not job.player then
        return
    end
    local processed = BurdJournals.Server.getBatchRewardJobProcessedCount(job)
    local lastSent = tonumber(job.lastProgressSent) or 0
    if processed <= lastSent then
        return
    end
    -- At most about five tiny, targeted progress packets per job. This makes
    -- the authority wait visible without transmitting journal/global ModData.
    local total = math.max(0, tonumber(job.total) or 0)
    local progressInterval = math.max(1, math.ceil(total / 5))
    if not force and processed < total and (processed - lastSent) < progressInterval then
        return
    end
    job.lastProgressSent = processed
    BurdJournals.Server.sendToClient(job.player, "batchClaimProgress", {
        jobId = job.id,
        requestId = job.requestId,
        mode = job.mode,
        count = processed,
        total = job.total or 0,
        skillsProcessed = job.processedSkills or 0,
        traitsProcessed = job.processedTraits or 0,
        recipesProcessed = job.processedRecipes or 0,
        statsProcessed = job.processedStats or 0,
    })
end

function BurdJournals.Server.completeBatchRewardJob(job, reason, suppressClientResponse)
    if not job or job.completed then
        return
    end
    if reason ~= "complete" and BurdJournals.Server.peekBatchJobEntry then
        while true do
            local entryType, entry, indexKey, entryIndex = BurdJournals.Server.peekBatchJobEntry(job)
            if not entryType then break end
            BurdJournals.Server.recordBatchRewardOutcome(job, entryType, entry, "failed", reason or "batchStopped")
            BurdJournals.Server.advanceBatchJobEntry(job, indexKey, entryIndex)
        end
    end
    job.completed = true
    local queue = BurdJournals.Server.BatchRewardQueue
    if queue and queue.activeCount ~= nil then
        queue.activeCount = math.max(0, (tonumber(queue.activeCount) or 1) - 1)
    end
    if reason == "journalDissolved" then job.dissolved = true end
    if SyncXp then
        SyncXp(job.player)
    elseif job.player and job.player.syncXp then
        job.player:syncXp()
    end
    local finalJournal = BurdJournals.Server.getBatchJobJournal(job)
    if finalJournal and finalJournal.transmitModData then
        safeTransmitServerJournalModData(finalJournal, "batchRewardComplete")
    end
    if suppressClientResponse ~= true then
        BurdJournals.Server.maybeSendBatchRewardProgress(job, true)
    end
    local payload = BurdJournals.Server.buildQueuedBatchCompletePayload(job)
    payload.reason = reason
    payload.dissolved = job.dissolved == true
    BurdJournals.Server.cacheBatchRewardRequest(job.player, job.requestId, nil, payload)
    if suppressClientResponse ~= true then
        BurdJournals.Server.sendToClient(job.player, "batchClaimComplete", payload)
    end

    -- Once the final job drains, flush everything that was deferred for the batch
    -- duration in one pass (shard persistence + file backups + cache mirrors).
    if not BurdJournals.Server.hasActiveBatchRewardJobs() then
        if BurdJournals.flushPendingEntryStoreShardTransmits then
            BurdJournals.flushPendingEntryStoreShardTransmits(true)
        end
        if BurdJournals.flushPendingDRCacheTransmit then
            BurdJournals.flushPendingDRCacheTransmit(true)
        end
        if BurdJournals.Server.flushPendingBaselineTransmit then
            BurdJournals.Server.flushPendingBaselineTransmit(false)
        end
    end
end

function BurdJournals.Server.peekBatchJobEntry(job)
    local groups = {
        { key = "skills", indexKey = "skillIndex" },
        { key = "traits", indexKey = "traitIndex" },
        { key = "recipes", indexKey = "recipeIndex" },
        { key = "stats", indexKey = "statIndex" },
    }
    for _, group in ipairs(groups) do
        local list = job[group.key]
        local indexKey = group.indexKey
        local index = tonumber(job[indexKey]) or 1
        if type(list) == "table" and index <= #list then
            return group.key, list[index], indexKey, index
        end
    end
    return nil, nil
end

function BurdJournals.Server.advanceBatchJobEntry(job, indexKey, index)
    if job and indexKey then
        job[indexKey] = (tonumber(index) or tonumber(job[indexKey]) or 1) + 1
        job.currentRetryKey = nil
        job.currentRetryCount = 0
    end
end

function BurdJournals.Server.recordBatchRewardOutcome(job, entryType, entry, status, reason)
    job.outcomes = job.outcomes or {}
    local name = type(entry) == "table" and (entry.skillName or entry.statId) or entry
    job.outcomes[#job.outcomes + 1] = {
        type = entryType,
        name = name,
        status = status,
        reason = reason,
    }
    if status == "failed" then
        job.failedCount = (tonumber(job.failedCount) or 0) + 1
    end
end

function BurdJournals.Server.processBatchJobEntry(job, entryType, entry)
    local player = job.player
    local journal = BurdJournals.Server.getBatchJobJournal(job)
    if not player or not journal then
        BurdJournals.Server.completeBatchRewardJob(job, "journalMissing")
        return "stopped", "journalMissing"
    end

    local baseArgs = {
        journalId = job.journalId,
        journalUUID = job.journalUUID,
        journalFingerprint = job.journalFingerprint,
        _resolvedJournal = journal,
    }

    -- Hydrate the entry store once for the whole batch job; the per-entry claim
    -- handlers below would otherwise re-merge the entire entry store on every
    -- single skill/trait/recipe (25 claims => 25 full hydrations).
    if not job._entryStoreHydrated then
        job._entryStoreHydrated = true
        if journal.getModData and BurdJournals.hydrateJournalDataFromEntryStore then
            local hydrateModData = journal:getModData()
            if hydrateModData and type(hydrateModData.BurdJournals) == "table" then
                BurdJournals.hydrateJournalDataFromEntryStore(hydrateModData.BurdJournals, "batchJobHydrate")
            end
        end
    end
    local function finishEntry(status, reason)
        return status, reason
    end
    local function finishSuccessfulEntry()
        if not BurdJournals.Server.getBatchJobJournal(job) then
            job.dissolved = true
            return finishEntry("successStop", "journalDissolved")
        end
        return finishEntry("success")
    end

    if entryType == "skills" then
        local skillName = entry and entry.skillName
        if type(skillName) == "string" and skillName ~= "" then
            baseArgs.skillName = skillName
            baseArgs.claimSessionId = job.claimSessionId
            local skillCompleted = true
            local skillResult = nil
            if job.mode == "absorb" then
                skillResult = BurdJournals.Server.invokeBatchRewardHandler(job, BurdJournals.Server.handleAbsorbSkill, player, baseArgs)
                skillCompleted = type(skillResult) == "table" and skillResult.completed == true
            else
                skillResult = BurdJournals.Server.invokeBatchRewardHandler(job, BurdJournals.Server.handleClaimSkill, player, baseArgs)
                skillCompleted = type(skillResult) == "table" and skillResult.completed == true
                if BurdJournals.shouldDebugLog and BurdJournals.shouldDebugLog() then
                    bsjWriteLogLine("[BurdJournals][SkillClaimBatchResult] jobId=" .. tostring(job.id)
                        .. " mode=" .. tostring(job.mode)
                        .. " skill=" .. tostring(skillName)
                        .. " completed=" .. tostring(skillCompleted)
                        .. " claimed=" .. tostring(type(skillResult) == "table" and skillResult.claimed or nil)
                        .. " xpAdded=" .. tostring(type(skillResult) == "table" and skillResult.xpAdded or nil)
                        .. " alreadyAtLevel=" .. tostring(type(skillResult) == "table" and skillResult.alreadyAtLevel or nil)
                        .. " noXP=" .. tostring(type(skillResult) == "table" and skillResult.noXP or nil)
                        .. " xpAfterTotal=" .. tostring(type(skillResult) == "table" and skillResult.xpAfterTotal or nil))
                end
            end
            if skillCompleted then
                job.processedSkills = (job.processedSkills or 0) + 1
                if job.mode == "claim" then
                    if not job.completedSkills then job.completedSkills = {} end
                    if not job.completedSkillSeen then job.completedSkillSeen = {} end
                    local skillKey = string.lower(tostring(skillName))
                    if skillKey ~= "" and not job.completedSkillSeen[skillKey] then
                        job.completedSkillSeen[skillKey] = true
                        job.completedSkills[#job.completedSkills + 1] = skillName
                    end
                    if type(skillResult) == "table" and skillResult.xpAfterTotal ~= nil then
                        job.completedSkillXP[skillName] = skillResult.xpAfterTotal
                    end
                end
                return finishSuccessfulEntry()
            elseif type(skillResult) == "table" and skillResult.retryable == true then
                return finishEntry("retry", skillResult.reason or "retryable")
            elseif type(skillResult) == "table" and skillResult.alreadyClaimed == true then
                return finishEntry("already", "alreadyClaimed")
            elseif type(skillResult) == "table" and skillResult.diminishingZero == true then
                return finishEntry("deferred", "diminishingZero")
            end
            return finishEntry("failed", type(skillResult) == "table" and skillResult.reason or "notCompleted")
        end
    elseif entryType == "traits" then
        if type(entry) == "string" and entry ~= "" then
            baseArgs.traitId = entry
            if job.mode == "absorb" then
                local traitResult = BurdJournals.Server.invokeBatchRewardHandler(job, BurdJournals.Server.handleAbsorbTrait, player, baseArgs)
                if type(traitResult) == "table" and traitResult.completed == true then
                    job.processedTraits = (job.processedTraits or 0) + 1
                    return finishSuccessfulEntry()
                end
                return finishEntry(type(traitResult) == "table" and traitResult.retryable == true and "retry" or "failed",
                    type(traitResult) == "table" and traitResult.reason or "notCompleted")
            else
                local traitResult = BurdJournals.Server.invokeBatchRewardHandler(job, BurdJournals.Server.handleClaimTrait, player, baseArgs)
                local completedTraitId = type(traitResult) == "table" and traitResult.traitId or entry
                if type(traitResult) == "table" and traitResult.completed == true then
                    job.processedTraits = (job.processedTraits or 0) + 1
                    BurdJournals.Server.addUniqueBatchTrait(job.completedTraits, job.completedTraitSeen, completedTraitId)
                end
                if type(traitResult) == "table" and traitResult.claimed == true then
                    BurdJournals.Server.addUniqueBatchTrait(job.claimedTraits, job.claimedTraitSeen, completedTraitId)
                end
                if type(traitResult) == "table" and traitResult.completed == true then
                    return finishSuccessfulEntry()
                end
                if type(traitResult) == "table" and traitResult.alreadyClaimed == true then
                    return finishEntry("already", "alreadyClaimed")
                end
                return finishEntry(type(traitResult) == "table" and traitResult.retryable == true and "retry" or "failed",
                    type(traitResult) == "table" and traitResult.reason or "notCompleted")
            end
        end
    elseif entryType == "recipes" then
        if type(entry) == "string" and entry ~= "" then
            baseArgs.recipeName = entry
            local recipeHandler = job.mode == "absorb"
                and BurdJournals.Server.handleAbsorbRecipe
                or BurdJournals.Server.handleClaimRecipe
            local recipeResult = BurdJournals.Server.invokeBatchRewardHandler(job, recipeHandler, player, baseArgs)
            if type(recipeResult) == "table" and recipeResult.completed == true then
                job.processedRecipes = (job.processedRecipes or 0) + 1
            end
            if job.mode == "claim" and type(recipeResult) == "table" and recipeResult.completed == true then
                if not job.completedRecipes then job.completedRecipes = {} end
                if not job.completedRecipeSeen then job.completedRecipeSeen = {} end
                local completedRecipe = recipeResult.recipeName or entry
                local recipeKey = string.lower(tostring(completedRecipe))
                if recipeKey ~= "" and not job.completedRecipeSeen[recipeKey] then
                    job.completedRecipeSeen[recipeKey] = true
                    job.completedRecipes[#job.completedRecipes + 1] = completedRecipe
                end
            end
            if type(recipeResult) == "table" and recipeResult.completed == true then
                return finishSuccessfulEntry()
            end
            if type(recipeResult) == "table" and recipeResult.alreadyClaimed == true then
                return finishEntry("already", "alreadyClaimed")
            end
            return finishEntry(type(recipeResult) == "table" and recipeResult.retryable == true and "retry" or "failed",
                type(recipeResult) == "table" and recipeResult.reason or "notCompleted")
        end
    elseif entryType == "stats" then
        local statId = entry and entry.statId
        if type(statId) == "string" and statId ~= "" then
            baseArgs.statId = statId
            baseArgs.value = entry.value
            local statResult = BurdJournals.Server.invokeBatchRewardHandler(job, BurdJournals.Server.handleClaimStat, player, baseArgs)
            if type(statResult) == "table" and statResult.completed == true then
                job.processedStats = (job.processedStats or 0) + 1
                job.claimedStats = job.claimedStats or {}
                job.claimedStats[#job.claimedStats + 1] = {
                    statId = statResult.statId or statId,
                    value = statResult.value,
                }
                return finishSuccessfulEntry()
            end
            return finishEntry(type(statResult) == "table" and statResult.retryable == true and "retry" or "failed",
                type(statResult) == "table" and statResult.reason or "notCompleted")
        end
    end

    return finishEntry("failed", "invalidEntry")
end

function BurdJournals.Server.processBatchRewardJobsTick()
    local queue = BurdJournals.Server.BatchRewardQueue
    local jobCount = #queue.jobs
    if jobCount == 0 then
        if queue.tickRegistered and Events and Events.OnTick then
            Events.OnTick.Remove(BurdJournals.Server.processBatchRewardJobsTick)
            queue.tickRegistered = false
        end
        return
    end

    local removeIndexes = {}
    local processedGlobal = 0
    local globalTickBudget = math.max(1, tonumber(queue.globalTickBudget) or 2)
    -- A lone job may consume the already-configured global budget. With
    -- multiple jobs, retain the one-entry per-job cap for round-robin fairness.
    local perJobTickBudget = math.max(1, tonumber(queue.tickBudget) or 1)
    if jobCount == 1 then
        perJobTickBudget = math.max(perJobTickBudget, globalTickBudget)
    end
    local index = math.max(1, math.min(jobCount, tonumber(queue.nextIndex) or 1))
    local visited = 0
    local nowMs = (getTimestampMs and getTimestampMs()) or ((os.time() or 0) * 1000)

    while visited < jobCount and processedGlobal < globalTickBudget do
        local job = queue.jobs[index]
        if job.completed then
            removeIndexes[#removeIndexes + 1] = index
        elseif not BurdJournals.Server.isBatchRewardPlayerAvailable(job) then
            BurdJournals.Server.cancelBatchRewardJob(job, "playerDisconnected")
            removeIndexes[#removeIndexes + 1] = index
        elseif (nowMs - (tonumber(job.queuedAt) or nowMs)) >= math.max(1000, tonumber(queue.maxJobAgeMs) or 60000) then
            BurdJournals.Server.completeBatchRewardJob(job, "jobExpired")
            removeIndexes[#removeIndexes + 1] = index
        else
            local processedThisTick = 0
            while processedGlobal < globalTickBudget and processedThisTick < perJobTickBudget and not job.completed do
                local entryType, entry, indexKey, entryIndex = BurdJournals.Server.peekBatchJobEntry(job)
                if not entryType then
                    BurdJournals.Server.completeBatchRewardJob(job, "complete")
                    break
                end
                local status, reason = BurdJournals.Server.processBatchJobEntry(job, entryType, entry)
                if status == "retry" then
                    local retryKey = tostring(entryType) .. ":" .. tostring(entryIndex)
                    if job.currentRetryKey ~= retryKey then
                        job.currentRetryKey = retryKey
                        job.currentRetryCount = 0
                    end
                    job.currentRetryCount = (tonumber(job.currentRetryCount) or 0) + 1
                    if job.currentRetryCount >= 3 then
                        BurdJournals.Server.recordBatchRewardOutcome(job, entryType, entry, "failed", reason or "retryLimit")
                        BurdJournals.Server.advanceBatchJobEntry(job, indexKey, entryIndex)
                    end
                elseif status == "successStop" then
                    BurdJournals.Server.recordBatchRewardOutcome(job, entryType, entry, "success", reason)
                    BurdJournals.Server.advanceBatchJobEntry(job, indexKey, entryIndex)
                    BurdJournals.Server.completeBatchRewardJob(job, reason or "batchStopped")
                elseif status and status ~= "stopped" then
                    BurdJournals.Server.recordBatchRewardOutcome(job, entryType, entry, status, reason)
                    BurdJournals.Server.advanceBatchJobEntry(job, indexKey, entryIndex)
                end
                processedThisTick = processedThisTick + 1
                processedGlobal = processedGlobal + 1
                if not job.completed and not BurdJournals.Server.peekBatchJobEntry(job) then
                    BurdJournals.Server.completeBatchRewardJob(job, "complete")
                    break
                end
            end
            if job.completed then
                removeIndexes[#removeIndexes + 1] = index
            elseif processedThisTick > 0 then
                BurdJournals.Server.maybeSendBatchRewardProgress(job, false)
            end
        end
        visited = visited + 1
        index = (index % jobCount) + 1
    end

    queue.nextIndex = index
    table.sort(removeIndexes, function(a, b) return a > b end)
    for _, removeIndex in ipairs(removeIndexes) do table.remove(queue.jobs, removeIndex) end
    if #queue.jobs > 0 then
        queue.nextIndex = math.max(1, math.min(#queue.jobs, tonumber(queue.nextIndex) or 1))
    elseif queue.tickRegistered and Events and Events.OnTick then
        Events.OnTick.Remove(BurdJournals.Server.processBatchRewardJobsTick)
        queue.tickRegistered = false
    end
end

function BurdJournals.Server.ensureBatchRewardTickRegistered()
    local queue = BurdJournals.Server.BatchRewardQueue
    if queue.tickRegistered then
        return
    end
    if Events and Events.OnTick then
        Events.OnTick.Add(BurdJournals.Server.processBatchRewardJobsTick)
        queue.tickRegistered = true
    end
end

function BurdJournals.Server.enqueueBatchRewardJob(player, mode, args, journal, journalId, journalUUID, journalData, total, skills, traits, recipes, stats)
    local queue = BurdJournals.Server.BatchRewardQueue
    local allowed, rejectReason = BurdJournals.Server.canEnqueueBatchRewardJob(player)
    if not allowed then
        return nil, rejectReason
    end
    queue.counter = (tonumber(queue.counter) or 0) + 1
    local job = {
        id = "batch-" .. tostring(queue.counter),
        player = player,
        playerKey = BurdJournals.Server.getBatchRewardPlayerKey(player),
        mode = mode,
        args = args or {},
        journal = journal,
        journalId = journalId,
        journalUUID = journalUUID,
        journalFingerprint = args and args.journalFingerprint,
        journalData = journalData,
        queuedAt = (getTimestampMs and getTimestampMs()) or ((os.time() or 0) * 1000),
        total = total,
        skills = skills or {},
        traits = traits or {},
        recipes = recipes or {},
        stats = stats or {},
        skillIndex = 1,
        traitIndex = 1,
        recipeIndex = 1,
        statIndex = 1,
        processedSkills = 0,
        processedTraits = 0,
        processedRecipes = 0,
        processedStats = 0,
        completedSkills = {},
        completedSkillSeen = {},
        completedSkillXP = {},
        claimedTraits = {},
        claimedTraitSeen = {},
        completedTraits = {},
        completedTraitSeen = {},
        completedRecipes = {},
        completedRecipeSeen = {},
        claimedStats = {},
        outcomes = {},
        failedCount = 0,
        requestId = args and args.requestId,
        claimSessionId = args and args.claimSessionId,
    }
    queue.jobs[#queue.jobs + 1] = job
    queue.activeCount = (tonumber(queue.activeCount) or 0) + 1
    BurdJournals.Server.ensureBatchRewardTickRegistered()
    BurdJournals.Server.maybeSendBatchRewardProgress(job, true)
    return job
end

function BurdJournals.Server.handleBatchClaimRewards(player, args)
    local requestId = args and args.requestId or nil
    if not BurdJournals.Server.isValidBatchRewardRequestId(requestId) then
        BurdJournals.Server.sendBatchRewardFailure(player, "claim", requestId, "invalidRequestId", "Invalid batch claim request ID.")
        return
    end
    if BurdJournals.Server.replayBatchRewardRequest(player, requestId) then return end
    if not hasAnyJournalLookupArgs(args) then
        BurdJournals.Server.sendBatchRewardFailure(player, "claim", requestId, "invalidRequest", "Invalid batch claim request.")
        return
    end

    local sanitized, sanitizeError = BurdJournals.Server.sanitizeBatchRewardEntries(args)
    if not sanitized then
        BurdJournals.Server.sendBatchRewardFailure(player, "claim", requestId, sanitizeError, "Invalid or oversized batch claim payload.")
        return
    end

    local batchJournal, resolvedBatchJournalId, resolvedBatchJournalUUID = resolveServerCommandJournal(player, args, "batchClaimRewards")
    if not batchJournal then
        BurdJournals.Server.sendBatchRewardFailure(player, "claim", requestId, "journalNotFound", "Journal not found.")
        return
    end
    local batchJournalId = args.journalId
    batchJournalId, resolvedBatchJournalUUID = getResolvedJournalIdentity(batchJournal, resolvedBatchJournalId, resolvedBatchJournalUUID, batchJournalId)
    local batchJournalData = batchJournal.getModData and batchJournal:getModData().BurdJournals or nil

    local skills, traits = sanitized.skills, sanitized.traits
    local recipes, stats, total = sanitized.recipes, sanitized.stats, sanitized.total
    if total == 0 then
        BurdJournals.Server.sendBatchRewardFailure(player, "claim", requestId, "emptyBatch")
        return
    end

    local asyncThreshold = tonumber(BurdJournals.Server.BatchRewardQueue and BurdJournals.Server.BatchRewardQueue.asyncThreshold) or 12
    if total > asyncThreshold and BurdJournals.Server.enqueueBatchRewardJob then
        local job, queueError = BurdJournals.Server.enqueueBatchRewardJob(
            player,
            "claim",
            args,
            batchJournal,
            batchJournalId,
            resolvedBatchJournalUUID,
            batchJournalData,
            total,
            skills,
            traits,
            recipes,
            stats
        )
        if not job then
            BurdJournals.Server.sendBatchRewardFailure(player, "claim", requestId, queueError or "queueFull",
                "The server is already processing the maximum number of journal batches. Try again shortly.")
            return
        end
        BurdJournals.Server.sendToClient(player, "batchClaimQueued", {
            mode = "claim",
            jobId = job and job.id or nil,
            requestId = args.requestId,
            total = total,
        })
        BurdJournals.Server.cacheBatchRewardRequest(player, args.requestId, job, nil)
        return
    end


    local processedSkills = 0
    local processedTraits = 0
    local processedRecipes = 0
    local processedStats = 0
    local completedSkills = {}
    local completedSkillSeen = {}
    local completedSkillXP = {}
    local claimedStats = {}
    local claimedTraits = {}
    local claimedTraitSeen = {}
    local completedTraits = {}
    local completedTraitSeen = {}
    local completedRecipes = {}
    local completedRecipeSeen = {}
    local alreadyClaimedSkills = 0
    local diminishingZeroSkills = 0
    local outcomes = {}
    local failedCount = 0
    local claimSessionId = args.claimSessionId
    local batchExecutionJob = {
        player = player,
        requestId = requestId,
        journalId = batchJournalId,
        journalUUID = resolvedBatchJournalUUID,
    }

    local function recordOutcome(entryType, name, result)
        local status, reason = "failed", "notCompleted"
        if type(result) == "table" and result.completed == true then
            status = "success"
        elseif type(result) == "table" and result.alreadyClaimed == true then
            status, reason = "already", "alreadyClaimed"
        elseif type(result) == "table" and result.diminishingZero == true then
            status, reason = "deferred", "diminishingZero"
        elseif type(result) == "table" and result.reason then
            reason = result.reason
        end
        outcomes[#outcomes + 1] = {type = entryType, name = name, status = status, reason = reason}
        if status == "failed" then failedCount = failedCount + 1 end
    end


    for _, skillData in pairs(skills) do
        local skillName = skillData and skillData.skillName
        if type(skillName) == "string" and skillName ~= "" then
            local skillResult = BurdJournals.Server.invokeBatchRewardHandler(batchExecutionJob, BurdJournals.Server.handleClaimSkill, player, {
                journalId = batchJournalId,
                journalUUID = resolvedBatchJournalUUID,
                journalFingerprint = args.journalFingerprint,
                _resolvedJournal = batchJournal,
                skillName = skillName,
                claimSessionId = claimSessionId,
            })
            recordOutcome("skills", skillName, skillResult)
            if type(skillResult) == "table" and skillResult.completed == true then
                processedSkills = processedSkills + 1
                local completedSkillName = skillResult.skillName or skillName
                local completedSkillKey = string.lower(tostring(completedSkillName))
                if completedSkillKey ~= "" and not completedSkillSeen[completedSkillKey] then
                    completedSkillSeen[completedSkillKey] = true
                    completedSkills[#completedSkills + 1] = completedSkillName
                end
                if skillResult.xpAfterTotal ~= nil then
                    -- Final XP travels in the batchClaimComplete payload below
                    -- (completedSkillXP), which the client applies once; no need
                    -- for a per-skill applyXP packet here.
                    completedSkillXP[completedSkillName] = skillResult.xpAfterTotal
                end
            elseif type(skillResult) == "table" then
                -- Not granted: track why so the client can explain a "0/N" result
                -- instead of silently reporting nothing happened.
                if skillResult.alreadyClaimed then
                    alreadyClaimedSkills = alreadyClaimedSkills + 1
                elseif skillResult.diminishingZero then
                    diminishingZeroSkills = diminishingZeroSkills + 1
                end
            end
        end
    end

    for _, traitId in pairs(traits) do
        if type(traitId) == "string" and traitId ~= "" then
            local traitResult = BurdJournals.Server.invokeBatchRewardHandler(batchExecutionJob, BurdJournals.Server.handleClaimTrait, player, {
                journalId = batchJournalId,
                journalUUID = resolvedBatchJournalUUID,
                journalFingerprint = args.journalFingerprint,
                _resolvedJournal = batchJournal,
                traitId = traitId,
            })
            recordOutcome("traits", traitId, traitResult)
            local completedTraitId = type(traitResult) == "table" and traitResult.traitId or traitId
            if type(traitResult) == "table" and traitResult.completed == true then
                processedTraits = processedTraits + 1
                local completedTraitKey = string.lower(tostring(BurdJournals.normalizeTraitId and BurdJournals.normalizeTraitId(completedTraitId) or completedTraitId))
                if completedTraitKey ~= "" and not completedTraitSeen[completedTraitKey] then
                    completedTraitSeen[completedTraitKey] = true
                    completedTraits[#completedTraits + 1] = completedTraitId
                end
            end
            if type(traitResult) == "table" and traitResult.claimed == true then
                local traitKey = string.lower(tostring(BurdJournals.normalizeTraitId and BurdJournals.normalizeTraitId(completedTraitId) or completedTraitId))
                if traitKey ~= "" and not claimedTraitSeen[traitKey] then
                    claimedTraitSeen[traitKey] = true
                    claimedTraits[#claimedTraits + 1] = completedTraitId
                end
            end
        end
    end

    local recipesDissolved = false
    for _, recipeName in pairs(recipes) do
        if type(recipeName) == "string" and recipeName ~= "" then
            local recipeResult = BurdJournals.Server.invokeBatchRewardHandler(batchExecutionJob, BurdJournals.Server.handleClaimRecipe, player, {
                journalId = batchJournalId,
                journalUUID = resolvedBatchJournalUUID,
                journalFingerprint = args.journalFingerprint,
                _resolvedJournal = batchJournal,
                recipeName = recipeName,
            })
            recordOutcome("recipes", recipeName, recipeResult)
            if type(recipeResult) == "table" and recipeResult.completed == true then
                processedRecipes = processedRecipes + 1
                local completedRecipeName = recipeResult.recipeName or recipeName
                local completedRecipeKey = string.lower(tostring(completedRecipeName))
                if completedRecipeKey ~= "" and not completedRecipeSeen[completedRecipeKey] then
                    completedRecipeSeen[completedRecipeKey] = true
                    completedRecipes[#completedRecipes + 1] = completedRecipeName
                end
            end
            if not BurdJournals.Server.resolvePostActionJournal(player, batchJournalId, batchJournal) then
                recipesDissolved = true
                break
            end
        end
    end

    if not recipesDissolved then
        for _, statEntry in pairs(stats) do
            local statId = statEntry and statEntry.statId
            if type(statId) == "string" and statId ~= "" then
                local statResult = BurdJournals.Server.invokeBatchRewardHandler(batchExecutionJob, BurdJournals.Server.handleClaimStat, player, {
                journalId = batchJournalId,
                journalUUID = resolvedBatchJournalUUID,
                journalFingerprint = args.journalFingerprint,
                _resolvedJournal = batchJournal,
                statId = statId,
                value = statEntry.value,
            })
            recordOutcome("stats", statId, statResult)
                if type(statResult) == "table" and statResult.completed == true then
                    processedStats = processedStats + 1
                    claimedStats[#claimedStats + 1] = {
                        statId = statResult.statId or statId,
                        value = statResult.value,
                    }
                end
            end
        end
    end


    if SyncXp then
        SyncXp(player)
    elseif player.syncXp then
        player:syncXp()
    end

    local finalJournal = (not recipesDissolved)
        and BurdJournals.Server.resolvePostActionJournal(player, batchJournalId, batchJournal) or nil
    if not finalJournal and not recipesDissolved and BurdJournals.isValidItem(batchJournal) then
        finalJournal = batchJournal
    end
    local batchDissolved = recipesDissolved
        or (finalJournal == nil and (processedSkills + processedTraits + processedRecipes + processedStats) > 0)
    local finalJournalData = batchJournalData
    if finalJournal and finalJournal.getModData then
        finalJournalData = finalJournal:getModData().BurdJournals or finalJournalData
    end
    if finalJournal and finalJournal.transmitModData then
        safeTransmitServerJournalModData(finalJournal, "batchRewardComplete")
    end

    local completionPayload = buildBatchJournalResponse({
        mode = "claim",
        count = processedSkills + processedTraits + processedRecipes + processedStats,
        total = total,
        skillsProcessed = processedSkills,
        traitsProcessed = processedTraits,
        recipesProcessed = processedRecipes,
        statsProcessed = processedStats,
        completedSkills = completedSkills,
        completedSkillXP = completedSkillXP,
        claimedTraits = claimedTraits,
        completedTraits = completedTraits,
        completedRecipes = completedRecipes,
        claimedStats = claimedStats,
        -- Explain a "0/N" outcome instead of leaving the player guessing.
        alreadyClaimedSkills = alreadyClaimedSkills,
        diminishingZeroSkills = diminishingZeroSkills,
        failed = failedCount,
        outcomes = outcomes,
        requestId = args.requestId,
        dissolved = batchDissolved,
    }, player, batchJournalId, resolvedBatchJournalUUID, finalJournalData, "batchClaimComplete")
    BurdJournals.Server.cacheBatchRewardRequest(player, args.requestId, nil, completionPayload)
    BurdJournals.Server.sendToClient(player, "batchClaimComplete", completionPayload)
end

function BurdJournals.Server.handleBatchAbsorbRewards(player, args)
    local requestId = args and args.requestId or nil
    if not BurdJournals.Server.isValidBatchRewardRequestId(requestId) then
        BurdJournals.Server.sendBatchRewardFailure(player, "absorb", requestId, "invalidRequestId", "Invalid batch absorb request ID.")
        return
    end
    if BurdJournals.Server.replayBatchRewardRequest(player, requestId) then return end
    if not hasAnyJournalLookupArgs(args) then
        BurdJournals.Server.sendBatchRewardFailure(player, "absorb", requestId, "invalidRequest", "Invalid batch absorb request.")
        return
    end
    local sanitized, sanitizeError = BurdJournals.Server.sanitizeBatchRewardEntries(args)
    if not sanitized then
        BurdJournals.Server.sendBatchRewardFailure(player, "absorb", requestId, sanitizeError, "Invalid or oversized batch absorb payload.")
        return
    end

    local batchJournal, resolvedBatchJournalId, resolvedBatchJournalUUID = resolveServerCommandJournal(player, args, "batchAbsorbRewards")
    if not batchJournal then
        BurdJournals.Server.sendBatchRewardFailure(player, "absorb", requestId, "journalNotFound", "Journal not found.")
        return
    end
    local batchJournalId = args.journalId
    batchJournalId, resolvedBatchJournalUUID = getResolvedJournalIdentity(batchJournal, resolvedBatchJournalId, resolvedBatchJournalUUID, batchJournalId)
    local batchJournalData = batchJournal.getModData and batchJournal:getModData().BurdJournals or nil

    local skills, traits = sanitized.skills, sanitized.traits
    local recipes, stats, total = sanitized.recipes, sanitized.stats, sanitized.total
    if total == 0 then
        BurdJournals.Server.sendBatchRewardFailure(player, "absorb", requestId, "emptyBatch")
        return
    end

    local asyncThreshold = tonumber(BurdJournals.Server.BatchRewardQueue and BurdJournals.Server.BatchRewardQueue.asyncThreshold) or 12
    if total > asyncThreshold and BurdJournals.Server.enqueueBatchRewardJob then
        local job, queueError = BurdJournals.Server.enqueueBatchRewardJob(
            player,
            "absorb",
            args,
            batchJournal,
            batchJournalId,
            resolvedBatchJournalUUID,
            batchJournalData,
            total,
            skills,
            traits,
            recipes,
            stats
        )
        if not job then
            BurdJournals.Server.sendBatchRewardFailure(player, "absorb", requestId, queueError or "queueFull",
                "The server is already processing the maximum number of journal batches. Try again shortly.")
            return
        end
        BurdJournals.Server.sendToClient(player, "batchClaimQueued", {
            mode = "absorb",
            jobId = job and job.id or nil,
            requestId = args.requestId,
            total = total,
        })
        BurdJournals.Server.cacheBatchRewardRequest(player, args.requestId, job, nil)
        return
    end


    local processedSkills = 0
    local processedTraits = 0
    local processedRecipes = 0
    local processedStats = 0
    local completedSkills = {}
    local completedTraits = {}
    local completedRecipes = {}
    local claimedStats = {}
    local batchExecutionJob = {
        player = player,
        requestId = requestId,
        journalId = batchJournalId,
        journalUUID = resolvedBatchJournalUUID,
    }


    for _, skillData in pairs(skills) do
        local skillName = skillData and skillData.skillName
        if type(skillName) == "string" and skillName ~= "" then
            local result = BurdJournals.Server.invokeBatchRewardHandler(batchExecutionJob, BurdJournals.Server.handleAbsorbSkill, player, {
                journalId = batchJournalId,
                journalUUID = resolvedBatchJournalUUID,
                journalFingerprint = args.journalFingerprint,
                _resolvedJournal = batchJournal,
                skillName = skillName,
            })
            if type(result) == "table" and result.completed == true then
                processedSkills = processedSkills + 1
                completedSkills[#completedSkills + 1] = result.skillName or skillName
            end
        end
    end

    for _, traitId in pairs(traits) do
        if type(traitId) == "string" and traitId ~= "" then
            local result = BurdJournals.Server.invokeBatchRewardHandler(batchExecutionJob, BurdJournals.Server.handleAbsorbTrait, player, {
                journalId = batchJournalId,
                journalUUID = resolvedBatchJournalUUID,
                journalFingerprint = args.journalFingerprint,
                _resolvedJournal = batchJournal,
                traitId = traitId,
            })
            if type(result) == "table" and result.completed == true then
                processedTraits = processedTraits + 1
                completedTraits[#completedTraits + 1] = result.traitId or traitId
            end
        end
    end

    local recipesDissolved = false
    for _, recipeName in pairs(recipes) do
        if type(recipeName) == "string" and recipeName ~= "" then
            local result = BurdJournals.Server.invokeBatchRewardHandler(batchExecutionJob, BurdJournals.Server.handleAbsorbRecipe, player, {
                journalId = batchJournalId,
                journalUUID = resolvedBatchJournalUUID,
                journalFingerprint = args.journalFingerprint,
                _resolvedJournal = batchJournal,
                recipeName = recipeName,
            })
            if type(result) == "table" and result.completed == true then
                processedRecipes = processedRecipes + 1
                completedRecipes[#completedRecipes + 1] = result.recipeName or recipeName
            end
            if not BurdJournals.Server.resolvePostActionJournal(player, batchJournalId, batchJournal) then
                recipesDissolved = true
                break
            end
        end
    end

    for _, statEntry in pairs(stats) do
        local statId = statEntry and statEntry.statId
        if type(statId) == "string" and statId ~= "" then
            local statResult = BurdJournals.Server.invokeBatchRewardHandler(batchExecutionJob, BurdJournals.Server.handleClaimStat, player, {
                journalId = batchJournalId,
                journalUUID = resolvedBatchJournalUUID,
                journalFingerprint = args.journalFingerprint,
                _resolvedJournal = batchJournal,
                statId = statId,
                value = statEntry.value,
            })
            if type(statResult) == "table" and statResult.completed == true then
                processedStats = processedStats + 1
                claimedStats[#claimedStats + 1] = {
                    statId = statResult.statId or statId,
                    value = statResult.value,
                }
            end
        end
    end


    if SyncXp then
        SyncXp(player)
    elseif player.syncXp then
        player:syncXp()
    end

    local finalJournal = (not recipesDissolved)
        and BurdJournals.Server.resolvePostActionJournal(player, batchJournalId, batchJournal) or nil
    local batchDissolved = recipesDissolved
        or (finalJournal == nil and (processedSkills + processedTraits + processedRecipes + processedStats) > 0)
    local finalJournalData = batchJournalData
    if finalJournal and finalJournal.getModData then
        finalJournalData = finalJournal:getModData().BurdJournals or finalJournalData
    end
    if finalJournal and finalJournal.transmitModData then
        safeTransmitServerJournalModData(finalJournal, "batchRewardComplete")
    end

    local completionPayload = buildBatchJournalResponse({
        mode = "absorb",
        count = processedSkills + processedTraits + processedRecipes + processedStats,
        total = total,
        skillsProcessed = processedSkills,
        traitsProcessed = processedTraits,
        recipesProcessed = processedRecipes,
        statsProcessed = processedStats,
        completedSkills = completedSkills,
        completedTraits = completedTraits,
        completedRecipes = completedRecipes,
        claimedStats = claimedStats,
        requestId = args.requestId,
        dissolved = batchDissolved,
    }, player, batchJournalId, resolvedBatchJournalUUID, finalJournalData, "batchAbsorbComplete")
    BurdJournals.Server.cacheBatchRewardRequest(player, args.requestId, nil, completionPayload)
    BurdJournals.Server.sendToClient(player, "batchClaimComplete", completionPayload)
end

-- Server-side sanitization handler (called when client opens journal in MP)
function BurdJournals.Server.handleSanitizeJournal(player, args)
    local requestId = type(args) == "table" and args.requestId or nil
    if type(requestId) ~= "string" or #requestId > 128 or requestId:find("%c") then requestId = nil end
    if not hasAnyJournalLookupArgs(args) then
        BurdJournals.Server.sendToClient(player, "syncSuccess", {
            sanitizeRequestId = requestId,
            sanitizeError = "invalidRequest",
            serverRuntimeMarker = BurdJournals.RUNTIME_LOAD_MARKER,
        })
        return
    end

    local journal, resolvedJournalId, resolvedJournalUUID = resolveServerCommandJournal(player, args, "sanitizeJournal")
    if not journal then
        BurdJournals.Server.sendToClient(player, "syncSuccess", {
            sanitizeRequestId = requestId,
            sanitizeError = "journalNotFound",
            journalId = args.journalId,
            journalUUID = normalizeCommandJournalUUID(args),
            serverRuntimeMarker = BurdJournals.RUNTIME_LOAD_MARKER,
        })
        return
    end
    local function shouldTransmitSanitizeJournalModData()
        if BurdJournals.Server.shouldDeferBatchTransmit and BurdJournals.Server.shouldDeferBatchTransmit() then
            return false
        end
        local modData = journal and journal.getModData and journal:getModData() or nil
        local journalData = modData and modData.BurdJournals or nil
        if isStrictMPServer() and type(journalData) == "table" and journalData.entryStoreEnabled == true then
            BurdJournals.debugPrint("[BurdJournals] Server: Skipped sanitizeJournal offloaded shell transmit in strict MP")
            return false
        end
        return true
    end

    local modDataBefore = journal:getModData()
    if modDataBefore and type(modDataBefore.BurdJournals) == "table"
        and BurdJournals.Server.preferRicherEntryStoreData
    then
        local preferredData, preferredRicherStore = BurdJournals.Server.preferRicherEntryStoreData(
            modDataBefore.BurdJournals, args, "sanitizeJournalPartialResident")
        if preferredRicherStore then modDataBefore.BurdJournals = preferredData end
    end
    local dataBefore = modDataBefore and modDataBefore.BurdJournals or nil
    local sanitizeVersionBefore = type(dataBefore) == "table" and tonumber(dataBefore.sanitizedVersion) or nil
    local changed = false

    -- Run the whole maintenance pipeline without nested broadcasts. The final
    -- authoritative response below replaces the old sanitize + sync round trip.
    if BurdJournals.sanitizeJournalData then
        local sanitizeResult = BurdJournals.sanitizeJournalData(journal, player, {suppressTransmit = true})
        if sanitizeResult and sanitizeResult.cleaned then
            changed = true
            BurdJournals.debugPrint("[BurdJournals] Server: Sanitized journal " .. tostring(journal.getID and journal:getID() or args.journalId))
        end
    end

    if BurdJournals.migrateJournalIfNeeded then
        local migrated = BurdJournals.migrateJournalIfNeeded(journal, player, {
            suppressTransmit = true,
            suppressCompact = true,
        })
        changed = migrated == true or changed
    end

    -- Patch/update safety: if DR counters were dropped from item ModData, restore from cache.
    if (not isStrictMPServer()) and BurdJournals.restoreJournalDRStateIfMissing then
        changed = BurdJournals.restoreJournalDRStateIfMissing(journal, "sanitizeJournal", player) == true or changed
    end
    if (not isStrictMPServer()) and BurdJournals.captureJournalDRState then
        BurdJournals.captureJournalDRState(journal, "sanitizeJournalSeed", player)
    end
    
    -- Compact journal data to reduce ModData size (helps prevent 64KB player data limit issues)
    if BurdJournals.compactJournalData then
        changed = BurdJournals.compactJournalData(journal) == true or changed
    end

    local modDataAfter = journal:getModData()
    local journalData = modDataAfter and modDataAfter.BurdJournals or nil
    local sanitizeVersionAfter = type(journalData) == "table" and tonumber(journalData.sanitizedVersion) or nil
    changed = sanitizeVersionBefore ~= sanitizeVersionAfter or changed
    if changed and journal.transmitModData and shouldTransmitSanitizeJournalModData() then
        safeTransmitServerJournalModData(journal, "sanitizeJournalFinal")
    end

    BurdJournals.Server.updateJournalUUIDIndex(journal, player, "sanitizeJournal")
    local journalId, journalUUID = getResolvedJournalIdentity(
        journal,
        resolvedJournalId,
        resolvedJournalUUID,
        args.journalId
    )
    local sanitizePayload = buildSyncSuccessPayload(journalId, journalData, player)
    sanitizePayload.sanitizeRequestId = requestId
    sanitizePayload.sanitizeSuccess = true
    sanitizePayload.journalUUID = sanitizePayload.journalUUID
        or journalUUID
        or (type(journalData) == "table" and journalData.uuid or nil)
    sanitizePayload.sanitized = true
    sanitizePayload.unchanged = changed ~= true
    BurdJournals.Server.sendToClient(player, "syncSuccess", sanitizePayload)
end

local function serializeServerJournalNotesPages(notes)
    local parts = {}
    local pages = notes and notes.pages or {}
    if type(pages) == "table" then
        for i, pageText in ipairs(pages) do
            parts[i] = tostring(pageText or "")
        end
    end
    return table.concat(parts, "\30")
end

function BurdJournals.Server.handleSaveJournalNotes(player, args)
    if BurdJournals.isPlayerJournalNotesEnabled and not BurdJournals.isPlayerJournalNotesEnabled() then
        BurdJournals.Server.sendToClient(player, "error", {message = "Player journal notes are disabled."})
        return
    end

    if not hasAnyJournalLookupArgs(args) then
        BurdJournals.Server.sendToClient(player, "error", {message = "Invalid notes request."})
        return
    end

    local journal, resolvedJournalId, resolvedJournalUUID = resolveServerCommandJournal(player, args, "saveJournalNotes")
    if not journal then
        BurdJournals.Server.sendToClient(player, "error", {message = "Journal not found."})
        return
    end

    if not maybeMigrateRuntimeOnTouch(journal, player, "saveJournalNotes") then
        BurdJournals.Server.sendToClient(player, "error", {message = "Journal could not be prepared."})
        return
    end

    local modData = journal:getModData()
    modData.BurdJournals = modData.BurdJournals or {}
    local journalData = modData.BurdJournals
    local isPlayerJournal = journalData.isPlayerCreated == true
        or (BurdJournals.isEffectivelyPlayerJournal and BurdJournals.isEffectivelyPlayerJournal(journalData) == true)
    if not isPlayerJournal then
        BurdJournals.Server.sendToClient(player, "error", {message = "Only player journals can store notes."})
        return
    end
    -- Possession is enforced by resolveServerCommandJournal; opening policy still
    -- governs edits so holding a foreign journal cannot bypass sandbox rules.
    local canOpen, openReason = false, nil
    if BurdJournals.canPlayerOpenJournal then
        canOpen, openReason = BurdJournals.canPlayerOpenJournal(player, journal)
    end
    if not canOpen then
        BurdJournals.Server.sendToClient(player, "error", {message = openReason or "You cannot edit this journal."})
        return
    end

    local notes = BurdJournals.normalizeJournalNotes and BurdJournals.normalizeJournalNotes(args.notes, false) or nil
    if notes then
        -- Player-authored note requests never own server-generated lore metadata.
        notes.generated = false
        notes.generatedLore = nil
        notes.source = nil
        notes.family = nil
    end
    local existingNotes = BurdJournals.normalizeJournalNotes and BurdJournals.normalizeJournalNotes(journalData.notes, false) or nil
    local existingGeneratedMetadata = type(existingNotes) == "table"
        and (existingNotes.generated == true or existingNotes.generatedLore ~= nil
            or existingNotes.source ~= nil or existingNotes.family ~= nil)
    local notesChanged = serializeServerJournalNotesPages(notes) ~= serializeServerJournalNotesPages(existingNotes)
        or existingGeneratedMetadata
    local journalId = args.journalId
    journalId, resolvedJournalUUID = getResolvedJournalIdentity(journal, resolvedJournalId, resolvedJournalUUID, journalId)
    if not notesChanged then
        BurdJournals.Server.sendToClient(player, "notesSaved", {
            journalId = journalId,
            journalUUID = resolvedJournalUUID or BurdJournals.getJournalIdentityUUID(journalData),
            unchanged = true,
        })
        return
    end

    if notes and type(notes.pages) == "table" then
        notes.updatedAt = getTimestampMs and getTimestampMs() or os.time()
        notes.updatedBy = player and player.getUsername and player:getUsername() or nil
    end

    journalData.notes = notes
    journalData.lastModified = getTimestampMs and getTimestampMs() or os.time()
    journalData.isWritten = true
    local journalUUID = BurdJournals.getJournalIdentityUUID(journalData)
    if not journalUUID and BurdJournals.generateUUID then
        journalUUID = BurdJournals.generateUUID()
        journalData.uuid = journalUUID
    elseif not journalData.uuid and journalUUID then
        journalData.uuid = journalUUID
    end

    if journal.transmitModData then
        safeTransmitServerJournalModData(journal, "saveJournalNotes")
    end
    BurdJournals.Server.updateJournalUUIDIndex(journal, player, "saveJournalNotes")

    local payload = {
        journalId = journalId,
        journalUUID = resolvedJournalUUID or journalUUID,
        journalDelta = {
            notes = notes,
            lastModified = journalData.lastModified,
            isPlayerCreated = journalData.isPlayerCreated,
            isWritten = journalData.isWritten,
            uuid = journalUUID,
        },
    }
    BurdJournals.Server.sendToClient(player, "notesSaved", attachRuntimeDeltaOrLegacyJournalData(payload, journalData, player, "notesSaved"))
end

-- Dissolution handler - manual dissolve from UI button (no shouldDissolve check - user confirmed action)
function BurdJournals.Server.handleDissolveJournal(player, args)
    if not args or not args.journalId then
        BurdJournals.Server.sendToClient(player, "error", {message = "Invalid request."})
        return
    end

    BurdJournals.debugPrint("[BurdJournals] Server: Dissolve requested for journal ID " .. tostring(args.journalId))

    local requestedJournalId = args.journalId
    local journal, resolvedJournalId, resolvedJournalUUID = resolveServerCommandJournal(player, args, "dissolveJournal")
    local journalId = requestedJournalId
    journalId, resolvedJournalUUID = getResolvedJournalIdentity(journal, resolvedJournalId, resolvedJournalUUID, journalId)
    
    if not journal then
        BurdJournals.debugPrint("[BurdJournals] Server: Journal " .. tostring(requestedJournalId) .. " not found anywhere")
        BurdJournals.Server.sendToClient(player, "error", {message = "Journal not found."})
        return
    end

    -- Validate item is not a zombie object
    if not BurdJournals.isValidItem(journal) then
        BurdJournals.Server.sendToClient(player, "error", {message = "Journal is no longer valid."})
        return
    end

    -- Check journal data for debug-spawned or worn/bloody status
    local modData = journal:getModData()
    local data = modData and modData.BurdJournals
    local fullType = journal:getFullType()
    local isWornFromType = fullType and string.find(fullType, "_Worn") ~= nil
    local isBloodyFromType = fullType and string.find(fullType, "_Bloody") ~= nil
    local isWorn = (data and data.isWorn) or isWornFromType
    local isBloody = (data and data.isBloody) or isBloodyFromType
    local hasWornBloodyOrigin = data and (data.wasFromWorn or data.wasFromBloody)
    local isYuletide = data and data.isYuletideJournal == true
        and data.yuletideState == BurdJournals.YULETIDE_STATE_UNWRAPPED
    local isDebugSpawned = data and data.isDebugSpawned

    -- Allow debug-spawned journals to always be dissolved
    if not isDebugSpawned and not isWorn and not isBloody and not hasWornBloodyOrigin and not isYuletide then
        BurdJournals.Server.sendToClient(player, "error", {message = "Only worn or bloody journals can be manually dissolved."})
        return
    end

    BurdJournals.debugPrint("[BurdJournals] Server: Manual dissolve requested for journal " .. tostring(journalId) .. " (debug=" .. tostring(isDebugSpawned) .. ")")

    -- Remove the journal using the complete removal path
    local removed = BurdJournals.Server.dissolveJournal(player, journal)
    if not removed then
        return
    end

    -- Send dissolution notification
    local message = BurdJournals.getRandomDissolutionMessage and BurdJournals.getRandomDissolutionMessage() or "The journal crumbles to dust..."
    BurdJournals.Server.sendToClient(player, "journalDissolved", {
        message = message,
        journalId = journalId,
        journalUUID = resolvedJournalUUID,
    })
end

removeJournalCompletely = function(player, journal)

    if not journal then
        return false
    end

    local journalType = journal:getFullType()
    local journalID = journal:getID()
    local journalUUID = nil
    local journalEntryStoreShardKey = nil
    if journal.getModData then
        local modData = journal:getModData()
        local data = modData and modData.BurdJournals or nil
        journalEntryStoreShardKey = type(data) == "table" and data.entryStoreShardKey or nil
        local rawUUID = BurdJournals.getJournalIdentityUUID(data)
        if rawUUID ~= nil then
            local uuidText = tostring(rawUUID)
            uuidText = uuidText:gsub("^%s+", "")
            uuidText = uuidText:gsub("%s+$", "")
            journalUUID = (uuidText ~= "" and uuidText) or nil
        end
    end

    if player:getPrimaryHandItem() == journal then
        player:setPrimaryHandItem(nil)
    end
    if player:getSecondaryHandItem() == journal then
        player:setSecondaryHandItem(nil)
    end

    local container = journal:getContainer()
    if container then
        container:Remove(journal)
        if sendRemoveItemFromContainer then
            -- Optional cross-build replication API. The server-side removal is
            -- authoritative; keep this compatibility boundary narrow and visible.
            local replicated, replicationError = pcall(sendRemoveItemFromContainer, container, journal)
            if not replicated then
                bsjWriteLogLine("[BurdJournals] WARNING: journal removal replication failed: " .. tostring(replicationError))
            end
        end
        container:setDrawDirty(true)

    end

    local mainInv = player:getInventory()
    if mainInv then
        if mainInv:contains(journal) then
            mainInv:Remove(journal)
            mainInv:setDrawDirty(true)

        end

        local items = mainInv:getItems()
        for i = 0, items:size() - 1 do
            local item = items:get(i)
            -- Only check containers (bags, backpacks, etc.) - regular items don't have getInventory
            if item and item.getInventory then
                local subInv = item:getInventory()
                if subInv and subInv:contains(journal) then
                    subInv:Remove(journal)
                    subInv:setDrawDirty(true)
                end
            end
        end
    end

    local stillExists = mainInv and BurdJournals.Server.containerContainsItemRecursive
        and BurdJournals.Server.containerContainsItemRecursive(mainInv, journal)
        or (mainInv and mainInv:contains(journal))
    local removed = not stillExists

    if removed and journalUUID and BurdJournals.Server.purgeJournalUUIDTracking then
        local removedIndex, removedBackup = BurdJournals.Server.purgeJournalUUIDTracking(journalUUID, {
            removeBackup = true,
            entryStoreShardKey = journalEntryStoreShardKey,
        })
        if removedIndex > 0 or removedBackup > 0 then
            BurdJournals.debugPrint("[BurdJournals] Journal UUID tracking purged on removal: "
                .. tostring(journalUUID) .. " (index=" .. tostring(removedIndex)
                .. ", backup=" .. tostring(removedBackup) .. ")")
        end
    end

    return removed
end

-- Public dissolve function that uses complete removal
function BurdJournals.Server.dissolveJournal(player, journal)
    if not player or not journal then return false end
    local removed = removeJournalCompletely(player, journal)
    if not removed then
        bsjWriteLogLine("[BurdJournals] ERROR: authoritative journal removal failed")
        BurdJournals.Server.sendToClient(player, "error", {
            message = "The journal could not be removed. Its remaining rewards are unchanged; please retry."
        })
    end
    return removed
end

function BurdJournals.Server.findUniqueLegacyJournalForInitialization(container, itemType, matches)
    if not (container and container.getItems) then return end
    local items = container:getItems()
    for i = 0, items:size() - 1 do
        local item = items:get(i)
        if item then
            local fullType = item.getFullType and item:getFullType() or nil
            if fullType == itemType and BurdJournals.isAnyJournal(item) then
                local modData = item:getModData()
                local journalData = modData and modData.BurdJournals or nil
                local isFilled = string.find(fullType, "Filled", 1, true) ~= nil
                if type(journalData) ~= "table" or not BurdJournals.getJournalIdentityUUID(journalData)
                    or (isFilled and not journalData.skills)
                then
                    matches[#matches + 1] = item
                end
            end
            if item.getInventory then
                BurdJournals.Server.findUniqueLegacyJournalForInitialization(item:getInventory(), itemType, matches)
            end
        end
    end
end

function BurdJournals.Server.handleInitializeJournal(player, args)
    if type(args) ~= "table" then
        BurdJournals.Server.sendToClient(player, "error", {message = "Invalid request."})
        return
    end
    local initRequestId = tonumber(args.requestId)
    local function sendInitError(message, errorCode)
        BurdJournals.Server.sendToClient(player, "error", {
            message = message,
            errorCode = errorCode or "initializationFailed",
            initRequestId = initRequestId,
        })
    end

    local inheritedWasFromWorn = false
    local inheritedWasFromBloody = false
    local inheritedWasRestored = false
    local inheritedRestoredBy = nil

    local inventory = player and player:getInventory() or nil
    if not inventory then
        sendInitError("Inventory not found.", "inventoryNotFound")
        return
    end

    local journal = nil
    if hasAnyJournalLookupArgs(args) then
        journal = resolveServerCommandJournal(player, args, "initializeJournal")
    elseif type(args.itemType) == "string" and args.itemType ~= "" then
        -- Backward compatibility for older clients. Never guess when two identical
        -- uninitialized journals exist; a newer request identifies the exact item.
        local matches = {}
        BurdJournals.Server.findUniqueLegacyJournalForInitialization(inventory, args.itemType, matches)
        if #matches == 1 then
            journal = matches[1]
        elseif #matches > 1 then
            sendInitError("Multiple matching journals found; move the intended journal to the main inventory and retry.", "ambiguousJournal")
            return
        end
    end

    if not journal then

        sendInitError("Journal not found for initialization.", "journalNotFound")
        return
    end
    if not BurdJournals.isAnyJournal(journal) then
        sendInitError("Invalid journal.", "invalidJournal")
        return
    end

    local itemType = journal:getFullType()

    local sourceJournalData = nil
    do
        local sourceModData = journal:getModData()
        sourceJournalData = sourceModData and sourceModData.BurdJournals or nil
    end

    if type(sourceJournalData) == "table" then
        inheritedWasFromWorn = sourceJournalData.wasFromWorn == true or sourceJournalData.isWorn == true
        inheritedWasFromBloody = sourceJournalData.wasFromBloody == true or sourceJournalData.isBloody == true
        if BurdJournals.isRestoredJournalData then
            inheritedWasRestored = BurdJournals.isRestoredJournalData(sourceJournalData)
        else
            inheritedWasRestored = sourceJournalData.wasRestored == true
                or sourceJournalData.wasCleaned == true
                or inheritedWasFromWorn
                or inheritedWasFromBloody
        end
        inheritedRestoredBy = sourceJournalData.restoredBy
    end

    if inheritedWasRestored and (type(inheritedRestoredBy) ~= "string" or inheritedRestoredBy == "") then
        inheritedRestoredBy = player and player:getUsername() or "Unknown"
    end

    local modData = journal:getModData()
    if not modData.BurdJournals then
        modData.BurdJournals = {}
    end

    local uuid = BurdJournals.resolveJournalUUIDForRuntime(modData.BurdJournals, journal, true)
    if type(uuid) ~= "string" or uuid == "" then
        uuid = BurdJournals.generateUUID()
        modData.BurdJournals.uuid = uuid
    end

    local journalType = journal:getFullType()
    local isWorn = string.find(journalType, "_Worn") ~= nil
    local isBloody = string.find(journalType, "_Bloody") ~= nil
    local isFilled = string.find(journalType, "Filled") ~= nil

    -- Clean blank journals are player recording journals from the moment they
    -- are crafted. Repair older blanks that predate the recipe identity stamp.
    if not isFilled and not isWorn and not isBloody then
        modData.BurdJournals.isPlayerCreated = true
        if modData.BurdJournals.isWritten == nil then
            modData.BurdJournals.isWritten = false
        end
    end

    if isFilled and not modData.BurdJournals.skills then

        local minSkills, maxSkills, minXP, maxXP

        if isBloody then
            minSkills = BurdJournals.getSandboxOption("BloodyJournalMinSkills") or 2
            maxSkills = BurdJournals.getSandboxOption("BloodyJournalMaxSkills") or 4
            minXP = BurdJournals.getSandboxOption("BloodyJournalMinXP") or 50
            maxXP = BurdJournals.getSandboxOption("BloodyJournalMaxXP") or 150
        else
            minSkills = BurdJournals.getSandboxOption("WornJournalMinSkills") or 1
            maxSkills = BurdJournals.getSandboxOption("WornJournalMaxSkills") or 2
            minXP = BurdJournals.getSandboxOption("WornJournalMinXP") or 25
            maxXP = BurdJournals.getSandboxOption("WornJournalMaxXP") or 75
        end

        local numSkills = ZombRand(minSkills, maxSkills + 1)
        local skills = {}
        local availableSkills = BurdJournals.getAvailableSkills and BurdJournals.getAvailableSkills() or
                                {"Carpentry", "Cooking", "Farming", "Fishing", "Foraging", "Mechanics", "Electricity"}

        for i = 1, numSkills do
            if #availableSkills > 0 then
                local idx = ZombRand(1, #availableSkills + 1)
                local skill = availableSkills[idx]
                table.remove(availableSkills, idx)
                skills[skill] = {
                    xp = ZombRand(minXP, maxXP + 1),
                    level = 0
                }
            end
        end

        modData.BurdJournals.skills = skills
        modData.BurdJournals.claimedSkills = {}

        if isBloody then
            local traitChance = BurdJournals.getSandboxOption("BloodyJournalTraitChance") or 15
            if ZombRand(100) < traitChance then
                local grantableTraits = (BurdJournals.getGrantableTraitsForJournal
                    and BurdJournals.getGrantableTraitsForJournal({ isBloody = true, wasFromBloody = true, isPlayerCreated = false }))
                    or (BurdJournals.getGrantableTraits and BurdJournals.getGrantableTraits())
                    or BurdJournals.GRANTABLE_TRAITS or {
                    "Brave", "Organized", "FastLearner", "Wakeful", "Lucky",
                    "LightEater", "Dextrous", "Graceful", "Inconspicuous", "LowThirst"
                }
                local traits = {}
                if #grantableTraits > 0 then

                    local numTraits = ZombRand(1, 5)
                    local availableTraits = {}
                    for _, t in ipairs(grantableTraits) do
                        table.insert(availableTraits, t)
                    end

                    for i = 1, numTraits do
                        if #availableTraits == 0 then break end
                        local idx = ZombRand(#availableTraits) + 1
                        local randomTrait = availableTraits[idx]
                        if randomTrait then
                            traits[randomTrait] = true
                            table.remove(availableTraits, idx)
                        end
                    end
                end
                modData.BurdJournals.traits = traits
                modData.BurdJournals.claimedTraits = {}

            end

            -- Generate recipes for bloody journals
            local recipeChance = BurdJournals.getSandboxOption("BloodyJournalRecipeChance") or 35
            local recipeRoll = ZombRand(100)
            BurdJournals.debugPrint("[BurdJournals] Server init Bloody: recipeChance=" .. recipeChance .. ", roll=" .. recipeRoll)
            if recipeRoll < recipeChance then
                local maxRecipes = BurdJournals.getSandboxOption("BloodyJournalMaxRecipes") or 2
                local numRecipes = ZombRand(1, maxRecipes + 1)
                BurdJournals.debugPrint("[BurdJournals] Server init Bloody: Generating " .. numRecipes .. " recipes")
                local recipes = BurdJournals.generateRandomRecipes(numRecipes)
                local recipeCount = 0
                if recipes then
                    for _ in pairs(recipes) do recipeCount = recipeCount + 1 end
                end
                BurdJournals.debugPrint("[BurdJournals] Server init Bloody: Generated " .. recipeCount .. " recipes")
                if recipeCount > 0 then
                    modData.BurdJournals.recipes = recipes
                    modData.BurdJournals.claimedRecipes = {}
                end
            end
        elseif isWorn then
            -- Generate recipes for worn journals too
            local recipeChance = BurdJournals.getSandboxOption("WornJournalRecipeChance") or 20
            local recipeRoll = ZombRand(100)
            BurdJournals.debugPrint("[BurdJournals] Server init Worn: recipeChance=" .. recipeChance .. ", roll=" .. recipeRoll)
            if recipeRoll < recipeChance then
                local maxRecipes = BurdJournals.getSandboxOption("WornJournalMaxRecipes") or 1
                local numRecipes = ZombRand(1, maxRecipes + 1)
                BurdJournals.debugPrint("[BurdJournals] Server init Worn: Generating " .. numRecipes .. " recipes")
                local recipes = BurdJournals.generateRandomRecipes(numRecipes)
                local recipeCount = 0
                if recipes then
                    for _ in pairs(recipes) do recipeCount = recipeCount + 1 end
                end
                BurdJournals.debugPrint("[BurdJournals] Server init Worn: Generated " .. recipeCount .. " recipes")
                if recipeCount > 0 then
                    modData.BurdJournals.recipes = recipes
                    modData.BurdJournals.claimedRecipes = {}
                end
            end
        end

        modData.BurdJournals.author = BurdJournals.generateRandomName and BurdJournals.generateRandomName() or "Unknown Survivor"
        modData.BurdJournals.isWritten = true
    end

    if journal.transmitModData and not BurdJournals.Server.shouldDeferBatchTransmit() then
            journal:transmitModData()

    end

    if BurdJournals.updateJournalName then
        BurdJournals.updateJournalName(journal)
    end

    BurdJournals.Server.sendToClient(player, "journalInitialized", {
        uuid = uuid,
        journalUUID = uuid,
        journalId = journal.getID and journal:getID() or nil,
        journalDelta = {
            uuid = uuid,
            isPlayerCreated = modData.BurdJournals.isPlayerCreated,
            isWritten = modData.BurdJournals.isWritten,
        },
        itemType = itemType,
        skillCount = modData.BurdJournals.skills and BurdJournals.countTable(modData.BurdJournals.skills) or 0,
        requestId = args.requestId
    })
end

local function copyPositiveDRNumberMap(sourceMap)
    local out = {}
    local hasAny = false
    local normalized = sourceMap
    if type(normalized) ~= "table" and BurdJournals.normalizeTable then
        normalized = BurdJournals.normalizeTable(normalized)
    end
    if type(normalized) == "table" then
        for mapKey, rawValue in pairs(normalized) do
            if mapKey ~= nil then
                local numericValue = math.max(0, tonumber(rawValue) or 0)
                if numericValue > 0 then
                    out[mapKey] = numericValue
                    hasAny = true
                end
            end
        end
    end
    return out, hasAny
end

local function extractJournalDRCarryForward(sourceJournalData, player)
    if BurdJournals.getSandboxOption("PersistDROnErase") ~= true then
        return nil
    end
    if type(sourceJournalData) ~= "table" then
        return nil
    end

    local projected = sourceJournalData
    if BurdJournals.Server and BurdJournals.Server.deepCopy then
        local copied = BurdJournals.Server.deepCopy(sourceJournalData)
        if type(copied) == "table" then
            projected = copied
        end
    end
    if BurdJournals.applyRuntimeProjectionToJournalData then
        BurdJournals.applyRuntimeProjectionToJournalData(projected, player)
    end

    local skillReadCounts, hasSkillReadCounts = copyPositiveDRNumberMap(projected.skillReadCounts)
    local carry = {
        readCount = math.max(0, tonumber(projected.readCount) or 0),
        readSessionCount = math.max(0, tonumber(projected.readSessionCount) or 0),
        currentSessionReadCount = math.max(0, tonumber(projected.currentSessionReadCount) or 0),
        currentSessionId = type(projected.currentSessionId) == "string" and projected.currentSessionId or nil,
        skillReadCounts = hasSkillReadCounts and skillReadCounts or {},
        drLegacyMode3Migrated = projected.drLegacyMode3Migrated == true,
        migrationSchemaVersion = math.max(0, tonumber(projected.migrationSchemaVersion) or 0),
    }

    local hasCarry = carry.readCount > 0
        or carry.readSessionCount > 0
        or carry.currentSessionReadCount > 0
        or hasSkillReadCounts
    if not hasCarry then
        return nil
    end
    return carry
end

local function describeItemForWritingToolDebug(item, prefix, out, limit)
    if not item or type(out) ~= "table" then
        return
    end
    if #out >= limit then
        return
    end

    local fullType = item.getFullType and item:getFullType() or (item.getType and item:getType() or "unknown")
    out[#out + 1] = tostring(prefix) .. "=" .. tostring(fullType)

    local nestedInventory = nil
    if item.getInventory then
        nestedInventory = item:getInventory()
    end
    if not nestedInventory and item.getItemContainer then
        nestedInventory = item:getItemContainer()
    end
    if not nestedInventory or #out >= limit then
        return
    end

    local nestedItems = nestedInventory.getItems and nestedInventory:getItems() or nil
    if not nestedItems or not nestedItems.size or not nestedItems.get then
        return
    end

    local nestedCount = math.min(nestedItems:size(), math.max(0, limit - #out))
    for i = 0, nestedCount - 1 do
        local nestedItem = nestedItems:get(i)
        if nestedItem then
            local nestedType = nestedItem.getFullType and nestedItem:getFullType() or (nestedItem.getType and nestedItem:getType() or "unknown")
            out[#out + 1] = tostring(prefix) .. "[" .. tostring(i) .. "]=" .. tostring(nestedType)
            if #out >= limit then
                break
            end
        end
    end
end

local function buildWritingToolSearchDebugSnapshot(player)
    local out = {}
    local limit = 20
    if not player then
        return "player=nil"
    end

    describeItemForWritingToolDebug(player.getPrimaryHandItem and player:getPrimaryHandItem() or nil, "primary", out, limit)
    describeItemForWritingToolDebug(player.getSecondaryHandItem and player:getSecondaryHandItem() or nil, "secondary", out, limit)

    local inventory = player.getInventory and player:getInventory() or nil
    local items = inventory and inventory.getItems and inventory:getItems() or nil
    if items and items.size and items.get then
        local rootCount = math.min(items:size(), math.max(0, limit - #out))
        for i = 0, rootCount - 1 do
            describeItemForWritingToolDebug(items:get(i), "inventory[" .. tostring(i) .. "]", out, limit)
            if #out >= limit then
                break
            end
        end
    end

    local wornItems = player.getWornItems and player:getWornItems() or nil
    if wornItems and wornItems.size and wornItems.get and #out < limit then
        local wornCount = math.min(wornItems:size(), math.max(0, limit - #out))
        for i = 0, wornCount - 1 do
            local wornEntry = wornItems:get(i)
            local wornItem = wornEntry and wornEntry.getItem and wornEntry:getItem() or nil
            describeItemForWritingToolDebug(wornItem, "worn[" .. tostring(i) .. "]", out, limit)
            if #out >= limit then
                break
            end
        end
    end

    if #out == 0 then
        return "no-visible-items"
    end
    return table.concat(out, ", ")
end

function BurdJournals.Server.trimCommandString(value)
    if type(value) ~= "string" then
        return nil
    end

    local trimmed = value:gsub("^%s+", ""):gsub("%s+$", "")
    if trimmed == "" then
        return nil
    end
    return trimmed
end

function BurdJournals.Server.safeBuildWritingToolSearchDebugSnapshot(player)
    if type(buildWritingToolSearchDebugSnapshot) == "function" then
        return buildWritingToolSearchDebugSnapshot(player)
    end
    return "snapshot unavailable"
end

function BurdJournals.Server.normalizeCommandWritingToolId(args)
    if type(args) ~= "table" then
        return nil
    end

    local numericValue = tonumber(args.writingToolId)
    if numericValue ~= nil then
        return numericValue
    end

    return args.writingToolId
end

function BurdJournals.Server.normalizeCommandWritingToolFullType(args)
    if type(args) ~= "table" then
        return nil
    end

    return BurdJournals.Server.trimCommandString(args.writingToolFullType or args.writingToolType)
end

function BurdJournals.Server.shouldAllowClientWritingToolFallback(requestedJournalId, resolvedJournalId, journalResolvePath)
    if journalResolvePath == "materializedFromPayload"
        or journalResolvePath == "requesterByUUID"
        or journalResolvePath == "requesterByFingerprint"
        or journalResolvePath == "liveByUUID"
        or journalResolvePath == "indexOwnerById" then
        return true
    end

    if requestedJournalId ~= nil and resolvedJournalId ~= nil and tostring(requestedJournalId) ~= tostring(resolvedJournalId) then
        return true
    end

    return false
end

function BurdJournals.Server.resolveCommandWritingTool(player, args)
    local pen = BurdJournals.findWritingTool(player)
    if pen then
        local penType = pen.getFullType and pen:getFullType() or (pen.getType and pen:getType() or nil)
        return pen, "serverInventory", penType
    end

    return nil, "missing", nil
end

function BurdJournals.Server.enforceWritingToolRequirement(player, args, uses, sourceTag, requestedJournalId, resolvedJournalId, journalResolvePath, options)
    local normalizedUses = math.max(1, math.floor((tonumber(uses) or 1) + 0.5))
    local pen, penResolvePath, penType = BurdJournals.Server.resolveCommandWritingTool(player, args)
    if pen then
        if not (type(options) == "table" and options.deferConsumption == true) then
            BurdJournals.consumeItemUses(pen, normalizedUses, player)
        end
        return true, penResolvePath, penType, false, pen
    end

    bsjWriteLogLine("[BurdJournals] SERVER " .. tostring(sourceTag) .. " ERROR: No pen found!")
    BurdJournals.debugPrint("[BurdJournals] SERVER " .. tostring(sourceTag) .. " visible items: " .. tostring(BurdJournals.Server.safeBuildWritingToolSearchDebugSnapshot(player)))
    BurdJournals.debugPrint("[BurdJournals] SERVER " .. tostring(sourceTag) .. " writing tool hint: id="
        .. tostring(BurdJournals.Server.normalizeCommandWritingToolId(args))
        .. ", type=" .. tostring(BurdJournals.Server.normalizeCommandWritingToolFullType(args))
        .. ", journalResolvePath=" .. tostring(journalResolvePath))
    BurdJournals.Server.sendToClient(player, "error", {message = "You need a pen or pencil to write."})
    return false, penResolvePath, penType, false, nil
end

function BurdJournals.Server.handleLogSkills(player, args)
    if not args or not hasAnyJournalLookupArgs(args) then
        BurdJournals.Server.sendToClient(player, "error", {message = "Invalid request."})
        return
    end

    local journal = resolveServerCommandJournal(player, args, "logSkills")
    if not journal then
        BurdJournals.Server.sendToClient(player, "error", {message = "Journal not found."})
        return
    end
    if not maybeMigrateRuntimeOnTouch(journal, player, "logSkills") then
        return
    end
    if not enforceJournalLightRequirement(player, "logSkills") then
        return
    end
    local canMutateJournal, _, mutationMessage = BurdJournals.Server.validatePlayerJournalMutation(player, journal, {
        allowBlank = true,
    })
    if not canMutateJournal then
        BurdJournals.Server.sendToClient(player, "error", {message = mutationMessage})
        return
    end

    if not BurdJournals.isBlankJournal(journal) then
        BurdJournals.Server.sendToClient(player, "error", {message = "This journal already has content."})
        return
    end

    if BurdJournals.getSandboxOption("RequirePenToWrite") then
        local pen = BurdJournals.findWritingTool(player)
        if not pen then
            BurdJournals.Server.sendToClient(player, "error", {message = "You need a pen or pencil to write."})
            return
        end
        local usesPerLog = BurdJournals.getSandboxOption("PenUsesPerLog") or 1
        recordWritingTool = pen
        recordWritingToolUsesPerLog = math.max(1, tonumber(usesPerLog) or 1)
    end

    local sourceJournalData = nil
    do
        local sourceModData = journal:getModData()
        sourceJournalData = sourceModData and sourceModData.BurdJournals or nil
    end
    local inheritedWasFromWorn = type(sourceJournalData) == "table"
        and (sourceJournalData.wasFromWorn == true or sourceJournalData.isWorn == true) or false
    local inheritedWasFromBloody = type(sourceJournalData) == "table"
        and (sourceJournalData.wasFromBloody == true or sourceJournalData.isBloody == true) or false
    local inheritedWasRestored = type(sourceJournalData) == "table"
        and (sourceJournalData.wasRestored == true or sourceJournalData.isRestored == true) or false
    local inheritedRestoredBy = type(sourceJournalData) == "table" and sourceJournalData.restoredBy or nil
    local drCarryForward = extractJournalDRCarryForward(sourceJournalData, player)

    local journalContent = BurdJournals.collectAllPlayerData(player)
    local playerJournalContext = {isPlayerCreated = true}

    local selectedSkills = BurdJournals.normalizeTable and BurdJournals.normalizeTable(args.skills) or args.skills
    if BurdJournals.hasAnyEntries and BurdJournals.hasAnyEntries(selectedSkills) then
        local filteredSkills = {}
        for skillName, _ in pairs(selectedSkills) do
            local enabledForJournal = not BurdJournals.isSkillEnabledForJournal or BurdJournals.isSkillEnabledForJournal(playerJournalContext, skillName)
            if enabledForJournal and journalContent.skills[skillName] then
                filteredSkills[skillName] = journalContent.skills[skillName]
            end
        end
        journalContent.skills = filteredSkills
    elseif journalContent.skills and BurdJournals.isSkillEnabledForJournal then
        for skillName, _ in pairs(journalContent.skills) do
            if not BurdJournals.isSkillEnabledForJournal(playerJournalContext, skillName) then
                journalContent.skills[skillName] = nil
            end
        end
    end

    if journalContent.skills then
        for _, skillData in pairs(journalContent.skills) do
            if type(skillData) == "table" then
                local netXP = math.max(0, tonumber(skillData.xp) or 0)
                local rawXP = math.max(netXP, tonumber(skillData.rawXP) or netXP)
                local vhsExcludedXP = tonumber(skillData.vhsExcludedXP)
                if vhsExcludedXP == nil then
                    vhsExcludedXP = math.max(0, rawXP - netXP)
                else
                    vhsExcludedXP = math.max(0, vhsExcludedXP)
                end
                if vhsExcludedXP > rawXP then
                    vhsExcludedXP = rawXP
                end
                skillData.xp = netXP
                skillData.rawXP = rawXP
                skillData.vhsExcludedXP = vhsExcludedXP
            end
        end
    end

    local inventory = player:getInventory()
    local filledJournal = addJournalItemToContainerCompat(inventory, "BurdJournals.FilledSurvivalJournal")
    if not filledJournal then
        BurdJournals.Server.sendToClient(player, "error", {message = "Could not create the filled journal."})
        return
    end
    inventory:Remove(journal)
    if sendRemoveItemFromContainer then
        -- Optional cross-build replication API; the replacement already exists,
        -- so a replication failure is visible without hiding state mutations.
        local replicated, replicationError = pcall(sendRemoveItemFromContainer, inventory, journal)
        if not replicated then
            bsjWriteLogLine("[BurdJournals] WARNING: logSkills removal replication failed: " .. tostring(replicationError))
        end
    end
    if inventory.setDrawDirty then
        inventory:setDrawDirty(true)
    end

    if filledJournal then
        local modData = filledJournal:getModData()
        -- Track whether baseline was enforced when recording
        -- This affects how XP is applied on claim (add mode vs set mode)
        local baselineEnforced = BurdJournals.shouldUseBaselineForPlayerJournalRecording and BurdJournals.shouldUseBaselineForPlayerJournalRecording(player) or false

        local inheritedMigrationSchemaVersion = drCarryForward and tonumber(drCarryForward.migrationSchemaVersion) or 0
        local baseMigrationSchemaVersion = tonumber(BurdJournals.MIGRATION_SCHEMA_VERSION) or 0
        local seedReadCount = drCarryForward and drCarryForward.readCount or 0
        local seedReadSessionCount = drCarryForward and drCarryForward.readSessionCount or 0
        local seedCurrentSessionReadCount = drCarryForward and drCarryForward.currentSessionReadCount or 0
        local seedSkillReadCounts = drCarryForward and drCarryForward.skillReadCounts or {}
        modData.BurdJournals = {
            author = player:getDescriptor():getForename() .. " " .. player:getDescriptor():getSurname(),
            ownerUsername = player:getUsername(),
            ownerSteamId = BurdJournals.getPlayerSteamId(player),
            ownerCharacterName = player:getDescriptor():getForename() .. " " .. player:getDescriptor():getSurname(),
            timestamp = getGameTime():getWorldAgeHours(),
            uuid = (BurdJournals.generateUUID and BurdJournals.generateUUID())
                or ("journal-" .. tostring(getTimestampMs and getTimestampMs() or os.time()) .. "-" .. tostring(filledJournal:getID())),
            readCount = seedReadCount,
            readSessionCount = seedReadSessionCount,
            currentSessionReadCount = seedCurrentSessionReadCount,
            skillReadCounts = seedSkillReadCounts,
            migrationSchemaVersion = math.max(baseMigrationSchemaVersion, inheritedMigrationSchemaVersion),

            isWorn = false,
            isBloody = false,
            wasFromWorn = inheritedWasFromWorn,
            wasFromBloody = inheritedWasFromBloody,
            wasRestored = inheritedWasRestored,
            restoredBy = inheritedRestoredBy,
            isPlayerCreated = true,

            -- XP mode tracking: if baseline was enforced, XP values are deltas (earned XP)
            -- If baseline was NOT enforced, XP values are absolute (total XP)
            recordedWithBaseline = baselineEnforced,

            contributors = {},

            skills = journalContent.skills,
            traits = journalContent.traits,
        }
        if drCarryForward and type(drCarryForward.currentSessionId) == "string" and drCarryForward.currentSessionId ~= "" then
            modData.BurdJournals.currentSessionId = drCarryForward.currentSessionId
        end
        if drCarryForward and drCarryForward.drLegacyMode3Migrated == true then
            modData.BurdJournals.drLegacyMode3Migrated = true
        end
        BurdJournals.updateJournalName(filledJournal)
        BurdJournals.updateJournalIcon(filledJournal)

        local logEntryCount = BurdJournals.Server.countJournalPayloadEntries(modData.BurdJournals)
        if recordWritingTool and logEntryCount > 0 then
            BurdJournals.consumeItemUses(recordWritingTool, recordWritingToolUsesPerLog * logEntryCount, player)
        end
        local logFullSyncEntryLimit = math.max(1, tonumber(BurdJournals.FULL_SYNC_ENTRY_LIMIT) or 180)
        local logOmitFullJournalData = isStrictMPServer() and logEntryCount >= logFullSyncEntryLimit
        local logOffloadedToEntryStore = false
        if logOmitFullJournalData and BurdJournals.offloadJournalEntriesToStoreIfNeeded then
            logOffloadedToEntryStore = BurdJournals.offloadJournalEntriesToStoreIfNeeded(filledJournal, "logSkillsMaterialized", false) == true
        end
        if BurdJournals.Server.touchPlayerJournalRegistry then
            BurdJournals.Server.touchPlayerJournalRegistry(filledJournal, player, "logSkillsMaterialized")
        end

        if filledJournal.transmitModData and not logOmitFullJournalData then
            safeTransmitServerJournalModData(filledJournal, "logSkillsMaterialized")
            BurdJournals.debugPrint("[BurdJournals] Server: transmitModData called for filled journal in handleLogSkills")
        elseif logOmitFullJournalData then
            BurdJournals.debugPrint("[BurdJournals] Server: skipped full filled-journal transmit in handleLogSkills"
                .. " (entries=" .. tostring(logEntryCount) .. ")")
        end

        if inventory.setDrawDirty then
            inventory:setDrawDirty(true)
        end
        if player.syncInventory then
            -- Optional compatibility method on server player objects.
            local synced, syncError = pcall(player.syncInventory, player)
            if not synced then
                bsjWriteLogLine("[BurdJournals] WARNING: logSkills inventory sync failed: " .. tostring(syncError))
            end
        end
        BurdJournals.Server.sendToClient(player, "journalMaterialized", {
            oldJournalId = args.journalId,
            oldJournalUUID = sourceJournalData and sourceJournalData.uuid or nil,
            newJournalId = filledJournal.getID and filledJournal:getID() or nil,
            journalUUID = modData.BurdJournals and modData.BurdJournals.uuid or nil,
            journalData = logOmitFullJournalData and nil or modData.BurdJournals,
            fullJournalDataOmitted = logOmitFullJournalData == true,
            fullJournalDataOmittedReason = logOmitFullJournalData and "largeJournal" or nil,
            source = "handleLogSkills",
        })
        BurdJournals.debugPrint("[BurdJournals] Server: sendAddItemToContainer called for filled journal in handleLogSkills")
    end

    BurdJournals.Server.sendToClient(player, "logSuccess", {})
end

function BurdJournals.Server.detectLegacyAbsoluteRecordSkills(player, journalData, useBaseline)
    if not (useBaseline and type(journalData) == "table" and journalData.recordedWithBaseline == true and type(journalData.skills) == "table") then
        return false
    end
    local sampledSkills = 0
    local suspiciousAbsoluteSkills = 0
    for skillName, storedData in pairs(journalData.skills) do
        local storedXP = tonumber(type(storedData) == "table" and storedData.xp or storedData)
        if storedXP and storedXP > 0 then
            local perk = BurdJournals.getPerkByName and BurdJournals.getPerkByName(skillName)
            if perk then
                sampledSkills = sampledSkills + 1
                local actualXP = BurdJournals.getPlayerSkillTotalXP and BurdJournals.getPlayerSkillTotalXP(player, perk, skillName) or player:getXp():getXP(perk)
                local baselineXP = math.max(0, tonumber(BurdJournals.Server.getSkillBaselineForPlayer(player, skillName)) or 0)
                local earnedXP = math.max(0, actualXP - baselineXP)
                if isLikelyAbsoluteSkillEntryForBaseline(storedXP, earnedXP, actualXP) then
                    suspiciousAbsoluteSkills = suspiciousAbsoluteSkills + 1
                end
            end
        end
    end
    if sampledSkills > 0 and suspiciousAbsoluteSkills >= math.max(1, math.floor(sampledSkills * 0.5)) then
        BurdJournals.debugPrint("[BurdJournals] handleRecordProgress: detected legacy absolute entries while recordedWithBaseline=true; keeping baseline mode and allowing earned-only repair writes")
        return true
    end
    return false
end

function BurdJournals.Server.validateRecordProgressEntryLimits(player, journalData, validated, stores, countStoreBucket)
    -- Kept as explicit text for regression tests after splitting this block out
    -- of handleRecordProgress to stay below B41/Kahlua's 200-local ceiling:
    -- maxSkills > 0 and existingSkillCount + incomingSkillCount > maxSkills
    -- maxTraits > 0 and existingTraitCount + incomingTraitCount > maxTraits
    -- maxRecipes > 0 and existingRecipeCount + incomingRecipeCount > maxRecipes
    -- local storedRecipeCount = type(modData.BurdJournals.entryStoreEntryCounts) == "table"
    -- or countRecordStoreBucket(recordRecipeStore)
    local limits = BurdJournals.Limits or {}
    local maxSkills = tonumber(limits.MAX_SKILLS) or 0
    local maxTraits = tonumber(limits.MAX_TRAITS) or 0
    local maxRecipes = tonumber(limits.MAX_RECIPES) or 0

    local function storedCount(bucketName, storeBucket)
        local manifest = type(journalData.entryStoreEntryCounts) == "table"
            and math.max(0, tonumber(journalData.entryStoreEntryCounts[bucketName]) or 0)
            or nil
        if manifest ~= nil then
            return manifest
        end
        return countStoreBucket and countStoreBucket(storeBucket) or 0
    end

    if maxSkills > 0 then
        local existingSkillCount = 0
        for _ in pairs(journalData.skills) do existingSkillCount = existingSkillCount + 1 end
        existingSkillCount = math.max(existingSkillCount, storedCount("skills", stores.skills))
        local incomingSkillCount = 0
        if validated.skills then
            for skillName in pairs(validated.skills) do
                local storedSkillKey = stores.skills and BurdJournals.resolveSkillKey and BurdJournals.resolveSkillKey(stores.skills, skillName) or skillName
                if not journalData.skills[skillName] and not (stores.skills and stores.skills[storedSkillKey] ~= nil) then
                    incomingSkillCount = incomingSkillCount + 1
                end
            end
        end
        if existingSkillCount + incomingSkillCount > maxSkills then
            bsjWriteLogLine("[BurdJournals] SERVER handleRecordProgress ERROR: Skill limit reached")
            BurdJournals.Server.sendToClient(player, "error", {message = "Journal skill limit reached (" .. maxSkills .. " max)."})
            return false
        end
    end

    if maxTraits > 0 then
        local existingTraitCount = 0
        for _ in pairs(journalData.traits) do existingTraitCount = existingTraitCount + 1 end
        existingTraitCount = math.max(existingTraitCount, storedCount("traits", stores.traits))
        local incomingTraitCount = 0
        if validated.traits then
            for traitId in pairs(validated.traits) do
                if not journalData.traits[traitId] and not (stores.traits and stores.traits[traitId] ~= nil) then
                    incomingTraitCount = incomingTraitCount + 1
                end
            end
        end
        if existingTraitCount + incomingTraitCount > maxTraits then
            bsjWriteLogLine("[BurdJournals] SERVER handleRecordProgress ERROR: Trait limit reached")
            BurdJournals.Server.sendToClient(player, "error", {message = "Journal trait limit reached (" .. maxTraits .. " max)."})
            return false
        end
    end

    if maxRecipes > 0 then
        local existingRecipeCount = 0
        for _ in pairs(journalData.recipes) do existingRecipeCount = existingRecipeCount + 1 end
        existingRecipeCount = math.max(existingRecipeCount, storedCount("recipes", stores.recipes))
        local incomingRecipeCount = 0
        if validated.recipes then
            for recipeName in pairs(validated.recipes) do
                if not journalData.recipes[recipeName] and not (stores.recipes and stores.recipes[recipeName] ~= nil) then
                    incomingRecipeCount = incomingRecipeCount + 1
                end
            end
        end
        if existingRecipeCount + incomingRecipeCount > maxRecipes then
            bsjWriteLogLine("[BurdJournals] SERVER handleRecordProgress ERROR: Recipe limit reached")
            BurdJournals.Server.sendToClient(player, "error", {message = "Journal recipe limit reached (" .. maxRecipes .. " max)."})
            return false
        end
    end

    return true
end

function BurdJournals.Server.finishRecordProgressResponse(player, ctx)
    -- Split out of handleRecordProgress for Kahlua local pressure; preserve
    -- these regression anchors for strict-MP Record All perf diagnostics:
    -- local bsjShouldLogRecordPerf = bsjRecordPerfTotalMs
    -- or (isStrictMPServer() and isRecordAllRequest)
    -- or (isRecordAllRequest and bsjRecordPerfTotalMs >= bsjRecordPerfSlowThresholdMs)
    local finalJournal = ctx.newJournalId
        and BurdJournals.findItemByIdInPlayerInventory(player, ctx.newJournalId)
        or ctx.journal
    local journalData = nil
    local finalJournalId = ctx.newJournalId or ctx.journalId or (ctx.journal and ctx.journal:getID())

    local finalJournalUsesEntryStore = false
    if finalJournal and finalJournal.getModData then
        local finalModData = finalJournal:getModData()
        local finalData = finalModData and finalModData.BurdJournals or nil
        finalJournalUsesEntryStore = type(finalData) == "table" and finalData.entryStoreEnabled == true
    end
    -- An entry-store item only carries a compact manifest. Returning that shell
    -- as "full" journalData alongside a record delta makes the client prefer
    -- the empty shell and visually lose a successful single recording.
    if finalJournalUsesEntryStore then
        ctx.omitFullJournalData = true
    end
    if finalJournalUsesEntryStore
        and (ctx.recordAllSequenceClosed or (ctx.isRecordAllRequest and not ctx.recordAllHasMoreBatches))
        and not ctx.transmittedRecordJournalModData
        and finalJournal
        and finalJournal.transmitModData
    then
        if ctx.forceFlushRecordEntryStoreShard then
            ctx.forceFlushRecordEntryStoreShard()
        end
        if BurdJournals.refreshJournalEntryStoreCounts and finalJournal.getModData then
            local finalModData = finalJournal:getModData()
            if type(finalModData and finalModData.BurdJournals) == "table" then
                BurdJournals.refreshJournalEntryStoreCounts(finalModData.BurdJournals, "recordProgressFinalShell")
            end
        end
        ctx.transmittedRecordJournalModData = safeTransmitServerJournalModData(finalJournal, "recordProgressFinalShell")
        BurdJournals.debugPrint("[BurdJournals] transmitModData called for final offloaded journal shell")
    end
    local includeJournalData = not ctx.omitFullJournalData

    if finalJournal and BurdJournals.Server.touchPlayerJournalRegistry and not ctx.recordAllHasMoreBatches then
        local registryUUID = BurdJournals.Server.touchPlayerJournalRegistry(finalJournal, player, "recordProgress")
        if not registryUUID then
            BurdJournals.Server.sendToClient(player, "error", {
                operation = "recordProgress",
                errorCode = "registryPersistFailed",
                message = "Journal recovery registry unavailable.",
                journalId = finalJournalId,
                journalUUID = ctx.journalUUID,
            })
            return
        end
    end

    if includeJournalData and finalJournal then
        local modData = finalJournal:getModData()
        if modData and modData.BurdJournals then
            journalData = BurdJournals.Server.deepCopy(modData.BurdJournals)
        end
    end
    ctx.bsjRecordPerfSyncMs = getTimestampMs and getTimestampMs() or nil

    BurdJournals.debugPrint("[BurdJournals] Sending recordSuccess response, newJournalId=" .. tostring(ctx.newJournalId) .. ", journalId=" .. tostring(finalJournalId) .. ", includeJournalData=" .. tostring(includeJournalData))
    local noChangesRecorded = (ctx.skillsRecorded + ctx.traitsRecorded + ctx.statsRecorded + ctx.recipesRecorded) == 0
    local liveJournalData = nil
    if finalJournal and finalJournal.getModData then
        local liveModData = finalJournal:getModData()
        liveJournalData = liveModData and liveModData.BurdJournals or nil
    end
    local refreshedEntryStoreCount = nil
    local entryStoreUnavailable = false
    if type(liveJournalData) == "table"
        and liveJournalData.entryStoreEnabled == true
        and BurdJournals.refreshJournalEntryStoreCounts
    then
        local refreshedCounts = nil
        refreshedEntryStoreCount, refreshedCounts = BurdJournals.refreshJournalEntryStoreCounts(liveJournalData, "recordSuccess")
        refreshedEntryStoreCount = tonumber(refreshedEntryStoreCount) or 0
        if refreshedEntryStoreCount <= 0 and BurdJournals.Server.hasAnyDeltaEntries(ctx.recordDelta) then
            entryStoreUnavailable = true
            bsjWriteLogLine("[BurdJournals] SERVER recordSuccess ERROR: entry store manifest resolved empty after write"
                .. " journalId=" .. tostring(finalJournalId)
                .. " uuid=" .. tostring(liveJournalData.uuid or liveJournalData.entryStoreUUID or ctx.journalUUID)
                .. " counts=" .. bsjFormatEntryStoreCounts(refreshedCounts))
        end
    end
    if entryStoreUnavailable then
        BurdJournals.Server.sendToClient(player, "syncSuccess", {
            journalId = finalJournalId,
            journalUUID = type(liveJournalData) == "table"
                and (liveJournalData.uuid or liveJournalData.entryStoreUUID) or ctx.journalUUID,
            entryStoreUUID = type(liveJournalData) == "table" and liveJournalData.entryStoreUUID or nil,
            entryStoreEntryCounts = type(liveJournalData) == "table" and liveJournalData.entryStoreEntryCounts or nil,
            entryStoreUnavailable = true,
            terminal = true,
            syncError = "entryStoreUnavailable",
            serverRuntimeMarker = BurdJournals.RUNTIME_LOAD_MARKER,
        })
        return
    end
    if type(liveJournalData) == "table" and BurdJournals.Server.hasAnyDeltaEntries(ctx.recordDelta) then
        ctx.recordDelta.uuid = liveJournalData.uuid
        ctx.recordDelta.lastModified = liveJournalData.lastModified
        ctx.recordDelta.isPlayerCreated = liveJournalData.isPlayerCreated
        ctx.recordDelta.isWritten = liveJournalData.isWritten
        ctx.recordDelta.recordedWithBaseline = liveJournalData.recordedWithBaseline
        ctx.recordDelta.ownerUsername = liveJournalData.ownerUsername
        ctx.recordDelta.ownerSteamId = liveJournalData.ownerSteamId
        ctx.recordDelta.ownerCharacterName = liveJournalData.ownerCharacterName
        ctx.recordDelta.author = liveJournalData.author
    else
        ctx.recordDelta = nil
    end
    local fullJournalDataOmittedReason = nil
    if ctx.omitFullJournalData then
        if ctx.recordAllHasMoreBatches then
            fullJournalDataOmittedReason = "recordAllBatch"
        elseif ctx.recordAllSequenceClosed then
            fullJournalDataOmittedReason = "recordAllFinal"
        elseif finalJournalUsesEntryStore then
            fullJournalDataOmittedReason = "entryStore"
        else
            fullJournalDataOmittedReason = "largeJournal"
        end
    end
    local recordPayload = {
        skillsRecorded = ctx.skillsRecorded,
        traitsRecorded = ctx.traitsRecorded,
        statsRecorded = ctx.statsRecorded,
        recipesRecorded = ctx.recipesRecorded,
        skillNames = ctx.skillNames,
        traitNames = ctx.traitNames,
        statNames = ctx.statNames,
        recipeNames = ctx.recipeNames,
        newJournalId = ctx.newJournalId,
        journalId = finalJournalId,
        journalData = journalData,
        journalDelta = ctx.recordDelta,
        fullJournalDataOmitted = ctx.omitFullJournalData == true,
        fullJournalDataOmittedReason = fullJournalDataOmittedReason,
        entryStoreEnabled = type(liveJournalData) == "table" and liveJournalData.entryStoreEnabled == true or false,
        entryStoreUUID = type(liveJournalData) == "table" and liveJournalData.entryStoreUUID or nil,
        entryStoreEntryCounts = type(liveJournalData) == "table" and type(liveJournalData.entryStoreEntryCounts) == "table" and liveJournalData.entryStoreEntryCounts or nil,
        entryStoreUpdatedAt = type(liveJournalData) == "table" and liveJournalData.entryStoreUpdatedAt or nil,
        entryStoreActualCount = refreshedEntryStoreCount,
        entryStoreUnavailable = entryStoreUnavailable == true,
        recordQueueRemaining = ctx.recordQueueRemaining,
        isRecordAll = ctx.isRecordAllRequest == true,
        noChanges = noChangesRecorded,
        writingToolState = ctx.recordWritingToolState,
    }
    BurdJournals.debugPrint("[BurdJournals] recordSuccess counts: skillsRecorded=" .. tostring(ctx.skillsRecorded)
        .. ", traitsRecorded=" .. tostring(ctx.traitsRecorded)
        .. ", statsRecorded=" .. tostring(ctx.statsRecorded)
        .. ", recipesRecorded=" .. tostring(ctx.recipesRecorded)
        .. ", recordQueueRemaining=" .. tostring(ctx.recordQueueRemaining)
        .. ", noChanges=" .. tostring(noChangesRecorded))
    if attachRuntimeDeltaOrLegacyJournalData and type(journalData) == "table" then
        recordPayload = attachRuntimeDeltaOrLegacyJournalData(recordPayload, journalData, player, "recordSuccess")
    end
    if type(journalData) == "table" and recordPayload.journalUUID == nil then
        recordPayload.journalUUID = BurdJournals.getJournalIdentityUUID(journalData)
    elseif type(liveJournalData) == "table" and recordPayload.journalUUID == nil then
        recordPayload.journalUUID = BurdJournals.getJournalIdentityUUID(liveJournalData)
    elseif recordPayload.journalUUID == nil then
        recordPayload.journalUUID = ctx.journalUUID
    end
    local softLimit = math.max(1024, tonumber(BurdJournals.FULL_SYNC_SOFT_LIMIT_BYTES) or 48000)
    local estimate = 0
    if type(recordPayload.journalData) == "table" then
        estimate = estimatePayloadBytes(recordPayload)
        if estimate > softLimit then
            recordPayload.journalData = nil
            recordPayload.fullJournalDataOmitted = true
            recordPayload.fullJournalDataOmittedReason = "softLimit"
            BurdJournals.debugPrint("[BurdJournals] recordSuccess payload soft-limit exceeded: "
                .. tostring(estimate) .. " > " .. tostring(softLimit) .. ", omitting journalData")
        end
    end
    local bsjRecordPerfEndMs = getTimestampMs and getTimestampMs() or nil
    local bsjRecordPerfTotalMs = (ctx.bsjRecordPerfStartMs and bsjRecordPerfEndMs)
        and math.max(0, bsjRecordPerfEndMs - ctx.bsjRecordPerfStartMs)
        or nil
    local bsjRecordPerfSlowThresholdMs = math.max(0, tonumber(BurdJournals.MP_SLOW_RECORD_LOG_MS) or 250)
    local bsjShouldLogRecordPerf = bsjRecordPerfTotalMs
        and (
            (BurdJournals.shouldLogMPPerf and BurdJournals.shouldLogMPPerf())
            or (isStrictMPServer() and ctx.isRecordAllRequest)
            or (ctx.isRecordAllRequest and bsjRecordPerfTotalMs >= bsjRecordPerfSlowThresholdMs)
        )
    if ctx.bsjRecordPerfStartMs and bsjRecordPerfEndMs and bsjShouldLogRecordPerf then
        local validateMs = ctx.bsjRecordPerfValidateMs and (ctx.bsjRecordPerfValidateMs - ctx.bsjRecordPerfStartMs) or -1
        local writeMs = ctx.bsjRecordPerfWriteMs and (ctx.bsjRecordPerfWriteMs - (ctx.bsjRecordPerfValidateMs or ctx.bsjRecordPerfStartMs)) or -1
        local syncMs = ctx.bsjRecordPerfSyncMs and (ctx.bsjRecordPerfSyncMs - (ctx.bsjRecordPerfWriteMs or ctx.bsjRecordPerfStartMs)) or -1
        local payloadMs = bsjRecordPerfEndMs - (ctx.bsjRecordPerfSyncMs or ctx.bsjRecordPerfStartMs)
        bsjWriteLogLine("[BurdJournals][RecordPerf] player=" .. tostring(player and player.getUsername and player:getUsername() or "?")
            .. " pingMs=" .. tostring(BurdJournals.getPlayerPingMs and BurdJournals.getPlayerPingMs(player) or "nil")
            .. " in=" .. tostring(ctx.debugInSkills) .. "/" .. tostring(ctx.debugInTraits) .. "/" .. tostring(ctx.debugInStats) .. "/" .. tostring(ctx.debugInRecipes)
            .. " valid=" .. tostring(ctx.validSkillCount) .. "/" .. tostring(ctx.validTraitCount) .. "/" .. tostring(ctx.validStatCount) .. "/" .. tostring(ctx.validRecipeCount)
            .. " wrote=" .. tostring(ctx.skillsRecorded) .. "/" .. tostring(ctx.traitsRecorded) .. "/" .. tostring(ctx.statsRecorded) .. "/" .. tostring(ctx.recipesRecorded)
            .. " total=" .. tostring(ctx.totalItems)
            .. " itemEntries=" .. tostring(ctx.residentItemEntries)
            .. " storeEntries=" .. tostring(ctx.storedEntryManifestCount)
            .. " storeActual=" .. tostring(refreshedEntryStoreCount)
            .. " storeUnavailable=" .. tostring(entryStoreUnavailable)
            .. " preStripped=" .. tostring(ctx.strictMPRecordAllPreStrippedEntries)
            .. " remaining=" .. tostring(ctx.recordQueueRemaining)
            .. " omit=" .. tostring(recordPayload.fullJournalDataOmitted == true)
            .. " reason=" .. tostring(recordPayload.fullJournalDataOmittedReason)
            .. " offload=" .. tostring(ctx.offloadedToEntryStore)
            .. " itemTransmit=" .. tostring(ctx.transmittedRecordJournalModData)
            .. " estimate=" .. tostring(estimate)
            .. " ms=" .. tostring(bsjRecordPerfTotalMs)
            .. " writeDetail=entryStore:" .. tostring(ctx.bsjRecordPerfEntryStoreMs)
            .. ",baseline:" .. tostring(ctx.bsjRecordPerfBaselineMs)
            .. ",baselineUpdates:" .. tostring(ctx.bsjRecordPerfBaselineUpdates)
            .. " phases=" .. tostring(validateMs) .. "/" .. tostring(writeMs) .. "/" .. tostring(syncMs) .. "/" .. tostring(payloadMs))
    end
    BurdJournals.Server.sendToClient(player, "recordSuccess", recordPayload)
end

function BurdJournals.Server.handleRecordProgress(player, args)
    local bsjRecordPerfStartMs = getTimestampMs and getTimestampMs() or nil
    local bsjRecordPerfValidateMs = nil
    local bsjRecordPerfWriteMs = nil
    local bsjRecordPerfSyncMs = nil
    local bsjRecordPerfEntryStoreMs = 0
    local bsjRecordPerfBaselineMs = 0
    local bsjRecordPerfBaselineUpdates = 0
    local bsjRecordDebugEnabled = BurdJournals.shouldDebugLog and BurdJournals.shouldDebugLog() or false
    if bsjRecordDebugEnabled then
        BurdJournals.debugPrint("[BurdJournals] SERVER handleRecordProgress ENTRY")
        BurdJournals.debugPrint("[BurdJournals] Server: handleRecordProgress CALLED, player=" .. tostring(player and player:getUsername() or "nil"))
    end

    if not hasAnyJournalLookupArgs(args) then
        bsjWriteLogLine("[BurdJournals] SERVER handleRecordProgress ERROR: no journal lookup args")
        if bsjRecordDebugEnabled then
            BurdJournals.debugPrint("[BurdJournals] Server: handleRecordProgress - Invalid request (no journal lookup args)")
        end
        BurdJournals.Server.sendToClient(player, "error", {message = "Invalid request."})
        return
    end

    local requestedJournalId = tonumber(args and args.journalId) or (args and args.journalId)
    local requestedJournalUUID = normalizeCommandJournalUUID(args)
    if bsjRecordDebugEnabled then
        BurdJournals.debugPrint("[BurdJournals] SERVER handleRecordProgress: journalId=" .. tostring(requestedJournalId) .. ", journalUUID=" .. tostring(requestedJournalUUID))
        BurdJournals.debugPrint("[BurdJournals] Server: handleRecordProgress - journalId=" .. tostring(requestedJournalId) .. ", journalUUID=" .. tostring(requestedJournalUUID))
    end

    local journal, resolvedJournalId, resolvedJournalUUID, journalResolvePath = resolveServerCommandJournal(player, args, "recordProgress")
    if not journal then
        bsjWriteLogLine("[BurdJournals] SERVER handleRecordProgress ERROR: Journal not found for lookup id=" .. tostring(requestedJournalId) .. ", uuid=" .. tostring(requestedJournalUUID))
        if bsjRecordDebugEnabled then
            BurdJournals.debugPrint("[BurdJournals] Server: handleRecordProgress - Journal not found for lookup id=" .. tostring(requestedJournalId) .. ", uuid=" .. tostring(requestedJournalUUID))
        end
        BurdJournals.Server.sendToClient(player, "error", {message = "Journal not found."})
        return
    end
    local journalId, journalUUID = getResolvedJournalIdentity(journal, resolvedJournalId, resolvedJournalUUID, requestedJournalId)
    if not maybeMigrateRuntimeOnTouch(journal, player, "recordProgress") then
        return
    end
    if not enforceJournalLightRequirement(player, "recordProgress") then
        return
    end
    local canMutateJournal, _, mutationMessage = BurdJournals.Server.validatePlayerJournalMutation(player, journal, {
        allowBlank = true,
    })
    if not canMutateJournal then
        BurdJournals.Server.sendToClient(player, "error", {message = mutationMessage})
        return
    end

    if bsjRecordDebugEnabled then
        BurdJournals.debugPrint("[BurdJournals] SERVER handleRecordProgress: Journal found OK")
        BurdJournals.debugPrint("[BurdJournals] Server: handleRecordProgress - Journal found: " .. tostring(journal:getFullType()))

        BurdJournals.debugPrint("[BurdJournals] SERVER handleRecordProgress: Processing payload...")
    end

    local modData = journal:getModData()
    if not modData.BurdJournals then
        modData.BurdJournals = {}
    end
    if not modData.BurdJournals.skills then
        modData.BurdJournals.skills = {}
    end
    if not modData.BurdJournals.traits then
        modData.BurdJournals.traits = {}
    end
    if not modData.BurdJournals.stats then
        modData.BurdJournals.stats = {}
    end
    if not modData.BurdJournals.recipes then
        modData.BurdJournals.recipes = {}
    end
    BurdJournals.resolveJournalUUIDForRuntime(modData.BurdJournals, journal, true)
    local recordQueueRemainingEarly = math.max(0, tonumber(args and args.recordQueueRemaining) or 0)
    local isRecordAllRequest = args and args.isRecordAll == true
    local isStrictMPRecordAllWrite = isStrictMPServer()
        and args
        and (args.isRecordAll == true or modData.BurdJournals.entryStoreEnabled == true)
    -- Strict MP heavy recording can create hundreds of entries. Write those
    -- entries directly to the server entry store whenever the journal is already
    -- offloaded so the hot inventory/world item never carries a transient full
    -- journal payload while its chunk is loaded.

    local skillsRecorded = 0
    local traitsRecorded = 0
    local statsRecorded = 0
    local recipesRecorded = 0
    local skillNames = {}
    local traitNames = {}
    local statNames = {}
    local recipeNames = {}
    local recordDelta = {
        skills = {},
        traits = {},
        stats = {},
        recipes = {},
    }
    local recordEntryStoreCountDeltas = {
        skills = 0,
        traits = 0,
        stats = 0,
        recipes = 0,
    }
    local recordEntryStore = nil
    local directRecordEntryStoreShardKey = nil
    if BurdJournals.getOrCreateJournalEntryStoreEntryForData then
        if isStrictMPRecordAllWrite then
            recordEntryStore, directRecordEntryStoreShardKey = BurdJournals.getOrCreateJournalEntryStoreEntryForData(modData.BurdJournals, true, journal, "recordProgressDirectStore")
            if type(recordEntryStore) == "table" then
                modData.BurdJournals.entryStoreEnabled = true
                modData.BurdJournals.entryStoreVersion = BurdJournals.ENTRY_STORE_SCHEMA_VERSION
                modData.BurdJournals.entryStoreUUID = BurdJournals.getJournalIdentityUUID(modData.BurdJournals)
                modData.BurdJournals.entryStoreShardKey = directRecordEntryStoreShardKey
            end
        elseif modData.BurdJournals.entryStoreEnabled == true then
            recordEntryStore, directRecordEntryStoreShardKey = BurdJournals.getOrCreateJournalEntryStoreEntryForData(modData.BurdJournals, false, journal, "recordProgressLookup")
        end
    end
    local function getRecordStoreBucket(bucketName)
        return type(recordEntryStore) == "table" and type(recordEntryStore[bucketName]) == "table" and recordEntryStore[bucketName] or nil
    end
    local function getRecordWriteBucket(bucketName)
        if not isStrictMPRecordAllWrite or type(recordEntryStore) ~= "table" then
            return modData.BurdJournals[bucketName]
        end
        if type(recordEntryStore[bucketName]) ~= "table" then
            recordEntryStore[bucketName] = {}
        end
        return recordEntryStore[bucketName]
    end
    local function countRecordStoreBucket(bucket)
        if type(bucket) ~= "table" then
            return 0
        end
        local count = 0
        for _ in pairs(bucket) do
            count = count + 1
        end
        return count
    end
    local strictMPRecordAllPreStrippedEntries = 0
    if isStrictMPRecordAllWrite and BurdJournals.offloadJournalEntriesToStoreIfNeeded then
        local offloaded, shardKey, residentCount = BurdJournals.offloadJournalEntriesToStoreIfNeeded(journal, "recordProgressPreWrite", true, true)
        if offloaded == true then
            strictMPRecordAllPreStrippedEntries = math.max(0, tonumber(residentCount) or 0)
            directRecordEntryStoreShardKey = shardKey or directRecordEntryStoreShardKey
            if BurdJournals.getOrCreateJournalEntryStoreEntryForData then
                recordEntryStore, directRecordEntryStoreShardKey = BurdJournals.getOrCreateJournalEntryStoreEntryForData(modData.BurdJournals, false, journal, "recordProgressPreWriteLookup")
            end
        end
    end
    local recordSkillStore = getRecordStoreBucket("skills")
    local recordTraitStore = getRecordStoreBucket("traits")
    local recordStatStore = getRecordStoreBucket("stats")
    local recordRecipeStore = getRecordStoreBucket("recipes")

    -- Debug: Log baseline state
    local hasLegacyEntries = (BurdJournals.hasAnyEntries and BurdJournals.hasAnyEntries(modData.BurdJournals.skills))
        or (BurdJournals.hasAnyEntries and BurdJournals.hasAnyEntries(modData.BurdJournals.traits))
        or (BurdJournals.hasAnyEntries and BurdJournals.hasAnyEntries(modData.BurdJournals.stats))
        or (BurdJournals.hasAnyEntries and BurdJournals.hasAnyEntries(modData.BurdJournals.recipes))
        or false
    local useBaseline = BurdJournals.getJournalSkillRecordingMode
        and BurdJournals.getJournalSkillRecordingMode(modData.BurdJournals, player)
        or (BurdJournals.shouldEnforceBaseline and BurdJournals.shouldEnforceBaseline(player) or false)
    if useBaseline and BurdJournals.Server.isBaselineDebugModifiedForPlayer and BurdJournals.Server.isBaselineDebugModifiedForPlayer(player) then
        useBaseline = false
        if bsjRecordDebugEnabled then
            BurdJournals.debugPrint("[BurdJournals] handleRecordProgress: baseline restriction suppressed because debug baseline edits are active")
        end
    end
    local legacyAbsoluteSkillsDetected = BurdJournals.Server.detectLegacyAbsoluteRecordSkills(player, modData.BurdJournals, useBaseline)
    local hasBaselineCaptured = BurdJournals.hasBaselineCaptured and BurdJournals.hasBaselineCaptured(player) or false
    local isBaselineBypassed = BurdJournals.isBaselineBypassed and BurdJournals.isBaselineBypassed(player) or false
    if bsjRecordDebugEnabled then
        BurdJournals.debugPrint("[BurdJournals] handleRecordProgress: useBaseline=" .. tostring(useBaseline)
            .. ", hasBaselineCaptured=" .. tostring(hasBaselineCaptured)
            .. ", isBaselineBypassed=" .. tostring(isBaselineBypassed)
            .. ", legacyAbsoluteSkillsDetected=" .. tostring(legacyAbsoluteSkillsDetected))
    end

    -- Persist recording mode for correct future claim semantics.
    -- Legacy journals with existing entries but no flag are absolute/set mode.
    if modData.BurdJournals.recordedWithBaseline == nil then
        if hasLegacyEntries then
            modData.BurdJournals.recordedWithBaseline = false
            if bsjRecordDebugEnabled then
                BurdJournals.debugPrint("[BurdJournals] handleRecordProgress: inferred legacy journal XP mode (recordedWithBaseline=false)")
            end
        else
            modData.BurdJournals.recordedWithBaseline = useBaseline
        end
    end

    -- Count incoming items (before validation)
    local debugInSkills = args.skills and BurdJournals.countTable(args.skills) or 0
    local debugInTraits = args.traits and BurdJournals.countTable(args.traits) or 0
    local debugInRecipes = args.recipes and BurdJournals.countTable(args.recipes) or 0
    local debugInStats = args.stats and BurdJournals.countTable(args.stats) or 0
    if bsjRecordDebugEnabled then
        BurdJournals.debugPrint("[BurdJournals] handleRecordProgress: Incoming skills=" .. debugInSkills .. ", traits=" .. debugInTraits .. ", recipes=" .. debugInRecipes)
    end

    local validatedSkills = BurdJournals.Server.validateSkillPayload(args.skills, player, useBaseline, modData.BurdJournals)
    local validatedTraits = BurdJournals.Server.validateTraitPayload(args.traits, player, useBaseline, modData.BurdJournals)
    local validatedStats = BurdJournals.Server.validateStatsPayload(args.stats, player)
    local validatedRecipes = BurdJournals.Server.validateRecipePayload(args.recipes, player, useBaseline, modData.BurdJournals)

    -- Debug: Log validated counts
    local validSkillCount = validatedSkills and BurdJournals.countTable(validatedSkills) or 0
    local validTraitCount = validatedTraits and BurdJournals.countTable(validatedTraits) or 0
    local validStatCount = validatedStats and BurdJournals.countTable(validatedStats) or 0
    local validRecipeCount = validatedRecipes and BurdJournals.countTable(validatedRecipes) or 0
    if bsjRecordDebugEnabled then
        BurdJournals.debugPrint("[BurdJournals] handleRecordProgress: Validated skills=" .. validSkillCount .. ", traits=" .. validTraitCount .. ", stats=" .. validStatCount .. ", recipes=" .. validRecipeCount)
    end
    bsjRecordPerfValidateMs = getTimestampMs and getTimestampMs() or nil

    local recordWritingTool = nil
    local recordWritingToolUsesPerLog = 0
    local recordWritingTool = nil
    local recordWritingToolUsesPerLog = 0
    local recordWritingToolState = nil
    if BurdJournals.getSandboxOption("RequirePenToWrite") then
        local totalEntries = validSkillCount + validTraitCount + validStatCount + validRecipeCount
        if totalEntries > 0 then
            if bsjRecordDebugEnabled then
                BurdJournals.debugPrint("[BurdJournals] SERVER handleRecordProgress: Pen required for " .. tostring(totalEntries) .. " entries")
            end
            local usesPerLog = BurdJournals.getSandboxOption("PenUsesPerLog") or 1
            if usesPerLog < 1 then usesPerLog = 1 end
            local ok, _, _, _, resolvedWritingTool = BurdJournals.Server.enforceWritingToolRequirement(
                player,
                args,
                usesPerLog * totalEntries,
                "handleRecordProgress",
                requestedJournalId,
                journalId,
                journalResolvePath,
                {deferConsumption = true}
            )
            if not ok then
                return
            end
            recordWritingTool = resolvedWritingTool
            recordWritingToolUsesPerLog = usesPerLog
        end
    end

    if not BurdJournals.Server.validateRecordProgressEntryLimits(player, modData.BurdJournals, {
        skills = validatedSkills,
        traits = validatedTraits,
        recipes = validatedRecipes,
    }, {
        skills = recordSkillStore,
        traits = recordTraitStore,
        recipes = recordRecipeStore,
    }, countRecordStoreBucket) then
        return
    end
    BurdJournals.debugPrint("[BurdJournals] SERVER handleRecordProgress: Limits OK, recording data...")

    if validatedSkills then
        for skillName, skillData in pairs(validatedSkills) do
            local existingSkillKey = BurdJournals.resolveSkillKey and BurdJournals.resolveSkillKey(modData.BurdJournals.skills, skillName) or skillName
            local existingSkill = modData.BurdJournals.skills[existingSkillKey]
            if existingSkill == nil and recordSkillStore then
                local storedSkillKey = BurdJournals.resolveSkillKey and BurdJournals.resolveSkillKey(recordSkillStore, skillName) or skillName
                local storedSkill = recordSkillStore[storedSkillKey]
                if storedSkill ~= nil then
                    existingSkillKey = storedSkillKey
                    existingSkill = storedSkill
                end
            end
            local existingXP = existingSkill and tonumber(existingSkill.xp) or 0
            local incomingXP = math.max(0, tonumber(skillData.xp) or 0)
            local incomingRawXP = math.max(incomingXP, tonumber(skillData.rawXP) or incomingXP)
            local incomingVhsExcludedXP = tonumber(skillData.vhsExcludedXP)
            if incomingVhsExcludedXP == nil then
                incomingVhsExcludedXP = math.max(0, incomingRawXP - incomingXP)
            else
                incomingVhsExcludedXP = math.max(0, incomingVhsExcludedXP)
            end
            if incomingVhsExcludedXP > incomingRawXP then
                incomingVhsExcludedXP = incomingRawXP
            end

            local shouldUpdate = incomingXP > existingXP
            local repairedLegacyAbsolute = false
            if not shouldUpdate
                and useBaseline
                and modData.BurdJournals.recordedWithBaseline == true
                and existingSkill
            then
                local perk = BurdJournals.getPerkByName and BurdJournals.getPerkByName(skillName)
                if perk then
                    local actualXP = BurdJournals.getPlayerSkillTotalXP and BurdJournals.getPlayerSkillTotalXP(player, perk, skillName) or player:getXp():getXP(perk)
                    local baselineXP = math.max(0, tonumber(BurdJournals.Server.getSkillBaselineForPlayer(player, skillName)) or 0)
                    local earnedXP = math.max(0, actualXP - baselineXP)
                    if isLikelyAbsoluteSkillEntryForBaseline(existingXP, earnedXP, actualXP)
                        and incomingXP <= (earnedXP + 0.001)
                    then
                        shouldUpdate = true
                        repairedLegacyAbsolute = true
                        BurdJournals.debugPrint("[BurdJournals] handleRecordProgress: repairing legacy absolute skill entry for "
                            .. tostring(skillName)
                            .. " (stored=" .. tostring(existingXP)
                            .. ", earned=" .. tostring(earnedXP)
                            .. ", incoming=" .. tostring(incomingXP) .. ")")
                    end
                end
            end
            if not shouldUpdate and incomingXP == existingXP then
                local existingRawXP = math.max(existingXP, existingSkill and tonumber(existingSkill.rawXP) or existingXP)
                local existingVhsExcludedXP = existingSkill and tonumber(existingSkill.vhsExcludedXP)
                if existingVhsExcludedXP == nil then
                    existingVhsExcludedXP = math.max(0, existingRawXP - existingXP)
                else
                    existingVhsExcludedXP = math.max(0, existingVhsExcludedXP)
                end
                if existingVhsExcludedXP > existingRawXP then
                    existingVhsExcludedXP = existingRawXP
                end
                shouldUpdate = (incomingRawXP ~= existingRawXP) or (incomingVhsExcludedXP ~= existingVhsExcludedXP)
            end

            if shouldUpdate then
                if existingSkill == nil then
                    recordEntryStoreCountDeltas.skills = recordEntryStoreCountDeltas.skills + 1
                end
                if existingSkillKey ~= skillName then
                    modData.BurdJournals.skills[existingSkillKey] = nil
                    if recordSkillStore then
                        recordSkillStore[existingSkillKey] = nil
                    end
                end
                local incomingLevel = math.max(0, tonumber(skillData.level) or 0)
                local isPassiveSkill = (BurdJournals.isPassiveSkill and BurdJournals.isPassiveSkill(skillName))
                    or (skillName == "Fitness" or skillName == "Strength")
                if incomingLevel <= 0 and isPassiveSkill and BurdJournals.getSkillLevelFromXP then
                    local totalXPForLevel = tonumber(skillData.exportSkillXP)
                    if totalXPForLevel == nil or totalXPForLevel <= 0 then
                        local baselineXPForLevel = 0
                        if useBaseline and BurdJournals.Server.getSkillBaselineForPlayer then
                            baselineXPForLevel = math.max(0, tonumber(BurdJournals.Server.getSkillBaselineForPlayer(player, skillName)) or 0)
                        end
                        totalXPForLevel = baselineXPForLevel + incomingXP
                    end
                    incomingLevel = math.max(0, tonumber(BurdJournals.getSkillLevelFromXP(totalXPForLevel, skillName)) or 0)
                end
                local skillWriteBucket = getRecordWriteBucket("skills")
                skillWriteBucket[skillName] = {
                    xp = incomingXP,
                    level = incomingLevel,
                    rawXP = incomingRawXP,
                    vhsExcludedXP = incomingVhsExcludedXP,
                    vhsMediaLines = type(skillData.vhsMediaLines) == "table"
                        and BurdJournals.Server.deepCopy(skillData.vhsMediaLines)
                        or nil
                }
                if isStrictMPRecordAllWrite then
                    modData.BurdJournals.skills[skillName] = nil
                end
                recordDelta.skills[skillName] = BurdJournals.Server.deepCopy(skillWriteBucket[skillName])
                skillsRecorded = skillsRecorded + 1
                table.insert(skillNames, skillName)
                if repairedLegacyAbsolute then
                    BurdJournals.debugPrint("[BurdJournals] handleRecordProgress: baseline repair write committed for " .. tostring(skillName))
                end
            end
        end
    end

    if validatedTraits then
        for traitId, _ in pairs(validatedTraits) do
            if not modData.BurdJournals.traits[traitId] and not (recordTraitStore and recordTraitStore[traitId]) then
                local traitWriteBucket = getRecordWriteBucket("traits")
                traitWriteBucket[traitId] = true
                recordEntryStoreCountDeltas.traits = recordEntryStoreCountDeltas.traits + 1
                if isStrictMPRecordAllWrite then
                    modData.BurdJournals.traits[traitId] = nil
                end
                recordDelta.traits[traitId] = true
                traitsRecorded = traitsRecorded + 1
                table.insert(traitNames, traitId)
            end
        end
    end

    if validatedStats then
        for statId, statData in pairs(validatedStats) do

            local statWasRecorded = (modData.BurdJournals.stats and modData.BurdJournals.stats[statId] ~= nil)
                or (recordStatStore and recordStatStore[statId] ~= nil)
            local statWriteBucket = getRecordWriteBucket("stats")
            statWriteBucket[statId] = {
                value = statData.value
            }
            if not statWasRecorded then
                recordEntryStoreCountDeltas.stats = recordEntryStoreCountDeltas.stats + 1
            end
            if isStrictMPRecordAllWrite then
                modData.BurdJournals.stats[statId] = nil
            end
            recordDelta.stats[statId] = BurdJournals.Server.deepCopy(statWriteBucket[statId])
            statsRecorded = statsRecorded + 1
            table.insert(statNames, statId)
        end
    end

    if validatedRecipes then
        for recipeName, _ in pairs(validatedRecipes) do
            if not modData.BurdJournals.recipes[recipeName] and not (recordRecipeStore and recordRecipeStore[recipeName]) then
                local recipeWriteBucket = getRecordWriteBucket("recipes")
                recipeWriteBucket[recipeName] = true
                recordEntryStoreCountDeltas.recipes = recordEntryStoreCountDeltas.recipes + 1
                if isStrictMPRecordAllWrite then
                    modData.BurdJournals.recipes[recipeName] = nil
                end
                recordDelta.recipes[recipeName] = true
                recipesRecorded = recipesRecorded + 1
                table.insert(recipeNames, recipeName)
            end
        end
    end

    if isStrictMPRecordAllWrite and type(recordEntryStore) == "table" then
        recordEntryStore.updatedAt = getTimestampMs and getTimestampMs() or recordEntryStore.updatedAt
        local entryStorePerfStartMs = getTimestampMs and getTimestampMs() or nil
        if recordQueueRemainingEarly > 0 then
            modData.BurdJournals.entryStoreEnabled = true
            modData.BurdJournals.entryStoreVersion = BurdJournals.ENTRY_STORE_SCHEMA_VERSION
            modData.BurdJournals.entryStoreUUID = modData.BurdJournals.entryStoreUUID or modData.BurdJournals.uuid
            modData.BurdJournals.entryStoreShardKey = directRecordEntryStoreShardKey or modData.BurdJournals.entryStoreShardKey
            modData.BurdJournals.entryStoreUpdatedAt = recordEntryStore.updatedAt
            if type(modData.BurdJournals.entryStoreEntryCounts) ~= "table" then
                modData.BurdJournals.entryStoreEntryCounts = { skills = 0, traits = 0, stats = 0, recipes = 0 }
            end
            for bucketName, delta in pairs(recordEntryStoreCountDeltas) do
                modData.BurdJournals.entryStoreEntryCounts[bucketName] =
                    math.max(0, tonumber(modData.BurdJournals.entryStoreEntryCounts[bucketName]) or 0) + math.max(0, tonumber(delta) or 0)
            end
        elseif BurdJournals.refreshJournalEntryStoreCounts then
            BurdJournals.refreshJournalEntryStoreCounts(modData.BurdJournals, "recordProgressDirectStore")
        end
        if BurdJournals.markEntryStoreShardDirty then
            -- Final Record All batches force-flush below after final offload and
            -- shell work completes. Queue this write here so that batch only
            -- persists once, while non-Record-All writes retain their immediate
            -- final durability behavior.
            local forceDirectStorePersist = recordQueueRemainingEarly <= 0 and not (args and args.isRecordAll == true)
            local directStorePersisted = BurdJournals.markEntryStoreShardDirty(
                directRecordEntryStoreShardKey or modData.BurdJournals.entryStoreShardKey,
                forceDirectStorePersist,
                modData.BurdJournals.entryStoreUUID or modData.BurdJournals.uuid
            )
            if directStorePersisted ~= true then
                bsjWriteLogLine("[BurdJournals] SERVER recordProgress ERROR: authoritative entry-store persist failed"
                    .. " uuid=" .. tostring(modData.BurdJournals.entryStoreUUID or modData.BurdJournals.uuid))
                BurdJournals.Server.sendToClient(player, "error", {
                    operation = "recordProgress",
                    errorCode = "entryStoreUnavailable",
                    message = "Journal entry store unavailable.",
                    journalId = journalId,
                    journalUUID = modData.BurdJournals.entryStoreUUID or modData.BurdJournals.uuid,
                    entryStoreUnavailable = true,
                })
                return
            end
        end
        local entryStorePerfEndMs = getTimestampMs and getTimestampMs() or nil
        if entryStorePerfStartMs and entryStorePerfEndMs then
            bsjRecordPerfEntryStoreMs = bsjRecordPerfEntryStoreMs + math.max(0, entryStorePerfEndMs - entryStorePerfStartMs)
        end
    end

    -- Establish identity before first-record baseline promotion. Older clean
    -- blanks may not carry the recipe-time stamp yet.
    modData.BurdJournals.isPlayerCreated = true
    local isPlayerJournal = true
    if isPlayerJournal
        and useBaseline
        and BurdJournals.Server.promotePlayerJournalRecordBatchToExportBaseline
    then
        local exportBaselineUpdates = {}
        for _, skillName in ipairs(skillNames) do
            local skillData = validatedSkills and validatedSkills[skillName] or nil
            if type(skillData) == "table" then
                exportBaselineUpdates[#exportBaselineUpdates + 1] = {
                    skillName = skillName,
                    skillXP = skillData.exportSkillXP,
                    mediaSkillXP = skillData.exportMediaSkillXP,
                }
            end
        end
        for _, traitId in ipairs(traitNames) do
            exportBaselineUpdates[#exportBaselineUpdates + 1] = {
                traitId = traitId,
            }
        end
        for _, recipeName in ipairs(recipeNames) do
            exportBaselineUpdates[#exportBaselineUpdates + 1] = {
                recipeName = recipeName,
            }
        end
        if #exportBaselineUpdates > 0 then
            local baselinePerfStartMs = getTimestampMs and getTimestampMs() or nil
            local baselinePromotionUpdates = exportBaselineUpdates
            if isRecordAllRequest then
                local characterId = BurdJournals.getPlayerCharacterId and BurdJournals.getPlayerCharacterId(player) or "unknown"
                local queueKey = tostring(characterId) .. "|" .. tostring(modData.BurdJournals.uuid or journalUUID or journalId or "unknown")
                local pending = BurdJournals.Server._recordAllExportBaselineQueue[queueKey]
                if type(pending) ~= "table" then
                    pending = {}
                    BurdJournals.Server._recordAllExportBaselineQueue[queueKey] = pending
                end
                pending._updatedAt = (getTimestampMs and getTimestampMs()) or ((os.time() or 0) * 1000)
                for _, update in ipairs(exportBaselineUpdates) do
                    pending[#pending + 1] = update
                end
                if recordQueueRemainingEarly > 0 then
                    baselinePromotionUpdates = nil
                else
                    baselinePromotionUpdates = pending
                    BurdJournals.Server._recordAllExportBaselineQueue[queueKey] = nil
                end
            end
            if baselinePromotionUpdates and #baselinePromotionUpdates > 0 then
                BurdJournals.Server.promotePlayerJournalRecordBatchToExportBaseline(player, baselinePromotionUpdates)
                bsjRecordPerfBaselineUpdates = #baselinePromotionUpdates
            end
            local baselinePerfEndMs = getTimestampMs and getTimestampMs() or nil
            if baselinePerfStartMs and baselinePerfEndMs then
                bsjRecordPerfBaselineMs = math.max(0, baselinePerfEndMs - baselinePerfStartMs)
            end
        end
    end
    local committedRecordEntries = skillsRecorded + traitsRecorded + statsRecorded + recipesRecorded
    if recordWritingTool and committedRecordEntries > 0 then
        local writingToolId = recordWritingTool.getID and recordWritingTool:getID() or nil
        BurdJournals.consumeItemUses(recordWritingTool, recordWritingToolUsesPerLog * committedRecordEntries, player)
        local heldWritingTool = writingToolId and BurdJournals.findItemByIdInPlayerInventory
            and BurdJournals.findItemByIdInPlayerInventory(player, writingToolId) or nil
        recordWritingToolState = {
            itemId = writingToolId,
            removed = heldWritingTool == nil,
            usedDelta = heldWritingTool and heldWritingTool.getUsedDelta and heldWritingTool:getUsedDelta() or nil,
            drainableUses = heldWritingTool and heldWritingTool.getDrainableUsesFloat and heldWritingTool:getDrainableUsesFloat() or nil,
        }
    end
    bsjRecordPerfWriteMs = getTimestampMs and getTimestampMs() or nil


    local playerSteamId = BurdJournals.getPlayerSteamId(player)
    local playerCharName = player:getDescriptor():getForename() .. " " .. player:getDescriptor():getSurname()

    if not modData.BurdJournals.ownerSteamId then

        modData.BurdJournals.author = playerCharName
        modData.BurdJournals.ownerUsername = player:getUsername()
        modData.BurdJournals.ownerSteamId = playerSteamId
        modData.BurdJournals.ownerCharacterName = playerCharName
        modData.BurdJournals.contributors = {}
        BurdJournals.debugPrint("[BurdJournals] Journal owner set to: " .. playerCharName .. " (" .. playerSteamId .. ")")
    else

        if modData.BurdJournals.ownerSteamId ~= playerSteamId then

            if not modData.BurdJournals.contributors then
                modData.BurdJournals.contributors = {}
            end

            modData.BurdJournals.contributors[playerSteamId] = {
                characterName = playerCharName,
                username = player:getUsername(),
                addedAt = getGameTime():getWorldAgeHours()
            }
            BurdJournals.debugPrint("[BurdJournals] Added contributor: " .. playerCharName .. " (" .. playerSteamId .. ")")
        else

            if modData.BurdJournals.ownerCharacterName ~= playerCharName then
                local oldName = modData.BurdJournals.ownerCharacterName or "(none)"
                BurdJournals.debugPrint("[BurdJournals] Owner character name updated: " .. oldName .. " -> " .. playerCharName)
                modData.BurdJournals.ownerCharacterName = playerCharName
                modData.BurdJournals.author = playerCharName
            end
        end
    end

    modData.BurdJournals.lastModified = getGameTime():getWorldAgeHours()
    modData.BurdJournals.isWritten = true
    if BurdJournals.clearPlayerJournalDebugProvenance then
        BurdJournals.clearPlayerJournalDebugProvenance(modData.BurdJournals, "recordProgressSource")
    end

    local journalType = journal:getFullType()
    local isBlank = string.find(journalType, "Blank") ~= nil
    local totalItems = BurdJournals.Server.countJournalPayloadEntries(modData.BurdJournals)
    local residentItemEntries = BurdJournals.countJournalEntryStoreEntries
        and BurdJournals.countJournalEntryStoreEntries(modData.BurdJournals)
        or totalItems
    local storedEntryManifestCount = 0
    if type(modData.BurdJournals.entryStoreEntryCounts) == "table" then
        for _, bucketName in ipairs(BurdJournals.ENTRY_STORE_BUCKETS or {"skills", "traits", "stats", "recipes"}) do
            storedEntryManifestCount = storedEntryManifestCount + math.max(0, tonumber(modData.BurdJournals.entryStoreEntryCounts[bucketName]) or 0)
        end
    end
    -- Strict-MP Record All writes directly into the entry store and deliberately
    -- leaves the carried item buckets empty. Treat the authoritative manifest as
    -- recorded content so recipe-only/trait-only journals still materialize from
    -- Blank -> Filled on the final batch.
    totalItems = math.max(totalItems, storedEntryManifestCount)
    local recordQueueRemaining = recordQueueRemainingEarly
    local recordAllHasMoreBatches = isRecordAllRequest and recordQueueRemaining > 0
    local recordAllSequenceClosed = isRecordAllRequest and recordQueueRemaining <= 0
    local fullSyncEntryLimit = BurdJournals.getJournalFullSyncEntryLimit and BurdJournals.getJournalFullSyncEntryLimit(modData.BurdJournals)
        or math.max(1, tonumber(BurdJournals.FULL_SYNC_ENTRY_LIMIT) or 180)
    local largeJournalPayload = totalItems >= fullSyncEntryLimit
    local omitFullJournalData = isStrictMPServer()
        and (recordAllHasMoreBatches or largeJournalPayload)
    local offloadedToEntryStore = false
    local recordEntryStoreShardKey = directRecordEntryStoreShardKey
    local recordEntryStoreShardFlushed = false
    local transmittedRecordJournalModData = false
    local shouldOffloadRecordEntries = isStrictMPServer()
        and BurdJournals.offloadJournalEntriesToStoreIfNeeded ~= nil
        and (isRecordAllRequest or largeJournalPayload or modData.BurdJournals.entryStoreEnabled == true)
    local function forceFlushRecordEntryStoreShard()
        if recordEntryStoreShardFlushed then
            return
        end
        recordEntryStoreShardFlushed = true
        if recordEntryStoreShardKey and BurdJournals.markEntryStoreShardDirty then
            BurdJournals.markEntryStoreShardDirty(recordEntryStoreShardKey, true)
        elseif BurdJournals.flushPendingEntryStoreShardTransmits then
            BurdJournals.flushPendingEntryStoreShardTransmits(true)
        end
    end

    BurdJournals.debugPrint("[BurdJournals] handleRecordProgress: journalType=" .. tostring(journalType) .. ", isBlank=" .. tostring(isBlank) .. ", totalItems=" .. tostring(totalItems))

    local newJournalId = nil
    -- Materialize on the first authoritative write. Client continuation follows
    -- the replacement via journalMaterialized/newJournalId, so retaining a Blank
    -- shell between batches only misrepresents already-persisted journal data.
    if isBlank and totalItems > 0 then
        BurdJournals.debugPrint("[BurdJournals] Converting blank journal to filled...")

        local inventory = journal:getContainer()
        if inventory then
            BurdJournals.debugPrint("[BurdJournals] Got inventory container: " .. tostring(inventory))

            local savedData = BurdJournals.Server.deepCopy(modData.BurdJournals)
            if not savedData then
                bsjWriteLogLine("[BurdJournals] ERROR: Failed to deep copy journal data!")
                BurdJournals.Server.sendToClient(player, "error", {
                    operation = "recordProgress",
                    errorCode = "materializationFailed",
                    message = "Could not prepare the filled journal.",
                    journalId = journalId,
                    journalUUID = journalUUID,
                })
                return
            end
            if BurdJournals.clearPlayerJournalDebugProvenance then
                BurdJournals.clearPlayerJournalDebugProvenance(savedData, "recordProgressMaterialized")
            end

            -- Reset worn/bloody flags - preserve origin for "Restored" status logic
            -- The sandbox option controls display and dissolution behavior at runtime
            if savedData.isWorn then
                savedData.wasFromWorn = true
                savedData.isWorn = false
                BurdJournals.debugPrint("[BurdJournals] Reset isWorn flag, set wasFromWorn=true")
            end
            if savedData.isBloody then
                savedData.wasFromBloody = true
                savedData.isBloody = false
                BurdJournals.debugPrint("[BurdJournals] Reset isBloody flag, set wasFromBloody=true")
            end

            local filledJournal = addJournalItemToContainerCompat(inventory, "BurdJournals.FilledSurvivalJournal")
            if filledJournal then
                BurdJournals.debugPrint("[BurdJournals] Created filled journal: " .. tostring(filledJournal:getID()))

                local newModData = filledJournal:getModData()
                newModData.BurdJournals = savedData

                BurdJournals.updateJournalName(filledJournal)
                BurdJournals.updateJournalIcon(filledJournal)

                -- Compact journal data to reduce ModData size
                if BurdJournals.compactJournalData then
                    BurdJournals.compactJournalData(filledJournal)
                end

                if shouldOffloadRecordEntries and BurdJournals.offloadJournalEntriesToStoreIfNeeded then
                    local forceRecordOffload = recordAllHasMoreBatches or recordAllSequenceClosed or modData.BurdJournals.entryStoreEnabled == true
                    local offloaded, shardKey = BurdJournals.offloadJournalEntriesToStoreIfNeeded(filledJournal, "recordProgressMaterialized", forceRecordOffload, true)
                    offloadedToEntryStore = offloaded == true
                    recordEntryStoreShardKey = shardKey or recordEntryStoreShardKey
                end
                if recordAllSequenceClosed then
                    forceFlushRecordEntryStoreShard()
                end
                if BurdJournals.Server.touchPlayerJournalRegistry and not recordAllHasMoreBatches then
                    BurdJournals.Server.touchPlayerJournalRegistry(filledJournal, player, "recordProgressMaterialized")
                end

                if filledJournal.transmitModData
                    and (not recordAllHasMoreBatches)
                    and (not omitFullJournalData or offloadedToEntryStore or recordAllSequenceClosed)
                    and not (isStrictMPServer() and isRecordAllRequest and offloadedToEntryStore)
                then
                    transmittedRecordJournalModData = safeTransmitServerJournalModData(filledJournal, "recordProgressMaterialized")
                    BurdJournals.debugPrint("[BurdJournals] transmitModData called on filled journal")
                elseif omitFullJournalData then
                    BurdJournals.debugPrint("[BurdJournals] Skipped intermediate full filled-journal transmit for recordProgress"
                        .. " (totalItems=" .. tostring(totalItems)
                        .. ", remaining=" .. tostring(recordQueueRemaining) .. ")")
                end

                if player then
                    BurdJournals.safePcall(function()
                        if player.getPrimaryHandItem and player:getPrimaryHandItem() == journal then
                            player:setPrimaryHandItem(nil)
                        end
                        if player.getSecondaryHandItem and player:getSecondaryHandItem() == journal then
                            player:setSecondaryHandItem(nil)
                        end
                    end)
                end
                modData.BurdJournals = {
                    uuid = savedData and savedData.uuid or journalUUID,
                    replacedByJournalId = filledJournal.getID and filledJournal:getID() or nil,
                    replacedByJournalUUID = savedData and savedData.uuid or journalUUID,
                    replacedAt = getGameTime():getWorldAgeHours(),
                    replacementReason = "recordProgressMaterialized",
                }
                inventory:Remove(journal)
                if sendRemoveItemFromContainer then
                    BurdJournals.safePcall(function()
                        sendRemoveItemFromContainer(inventory, journal)
                    end)
                end
                if inventory.setDrawDirty then
                    inventory:setDrawDirty(true)
                end
                if sendAddItemToContainer then
                    BurdJournals.safePcall(function()
                        sendAddItemToContainer(inventory, filledJournal)
                    end)
                    BurdJournals.debugPrint("[BurdJournals] Server: sendAddItemToContainer called for filled journal in handleRecordProgress")
                end
                if player.syncInventory and not (isStrictMPServer() and isRecordAllRequest) then
                    BurdJournals.safePcall(function()
                        player:syncInventory()
                    end)
                end
                BurdJournals.Server.sendToClient(player, "journalMaterialized", {
                    oldJournalId = journalId,
                    oldJournalUUID = journalUUID,
                    newJournalId = filledJournal.getID and filledJournal:getID() or nil,
                    journalUUID = savedData and savedData.uuid or journalUUID,
                    journalData = omitFullJournalData and nil or savedData,
                    fullJournalDataOmitted = omitFullJournalData == true,
                    fullJournalDataOmittedReason = omitFullJournalData and (recordAllHasMoreBatches and "recordAllBatch" or (recordAllSequenceClosed and "recordAllFinal" or "largeJournal")) or nil,
                    source = "recordProgress",
                })
                BurdJournals.debugPrint("[BurdJournals] Replaced blank journal after filled journal replication")

                newJournalId = filledJournal:getID()
                BurdJournals.debugPrint("[BurdJournals] Conversion complete, newJournalId=" .. tostring(newJournalId))
            else
                bsjWriteLogLine("[BurdJournals] ERROR: Failed to create filled journal!")
                modData.BurdJournals.deferredBlankMaterialization = true
                modData.BurdJournals.deferredBlankMaterializationReason = "filledItemCreationFailed"
                BurdJournals.Server.sendToClient(player, "error", {
                    operation = "recordProgress",
                    errorCode = "materializationFailed",
                    message = "Could not create the filled journal.",
                    journalId = journalId,
                    journalUUID = journalUUID,
                })
                return
            end
        else
            bsjWriteLogLine("[BurdJournals] ERROR: No inventory container found!")
            modData.BurdJournals.deferredBlankMaterialization = true
            modData.BurdJournals.deferredBlankMaterializationReason = "sourceContainerUnavailable"
            BurdJournals.Server.sendToClient(player, "error", {
                operation = "recordProgress",
                errorCode = "materializationFailed",
                message = "Could not replace the blank journal.",
                journalId = journalId,
                journalUUID = journalUUID,
            })
            return
        end
    else
        BurdJournals.debugPrint("[BurdJournals] Not converting (isBlank=" .. tostring(isBlank)
            .. ", totalItems=" .. tostring(totalItems) .. ")")

        BurdJournals.updateJournalName(journal)
        BurdJournals.updateJournalIcon(journal)

        if shouldOffloadRecordEntries and BurdJournals.offloadJournalEntriesToStoreIfNeeded then
            local forceRecordOffload = recordAllHasMoreBatches or recordAllSequenceClosed or modData.BurdJournals.entryStoreEnabled == true
            local offloaded, shardKey = BurdJournals.offloadJournalEntriesToStoreIfNeeded(journal, "recordProgress", forceRecordOffload, true)
            offloadedToEntryStore = offloaded == true
            recordEntryStoreShardKey = shardKey or recordEntryStoreShardKey
        end
        if isStrictMPRecordAllWrite then
            forceFlushRecordEntryStoreShard()
        end

        -- Compact journal data to reduce ModData size
        if BurdJournals.compactJournalData then
            BurdJournals.compactJournalData(journal)
        end

        local shouldTransmitPreStrippedRecordAllShell = isStrictMPRecordAllWrite and strictMPRecordAllPreStrippedEntries > 0
        local shouldTransmitIntermediateRecordAllShell = isStrictMPRecordAllWrite and recordAllHasMoreBatches
        if journal.transmitModData
            and (
                shouldTransmitPreStrippedRecordAllShell
                or shouldTransmitIntermediateRecordAllShell
                or (not recordAllHasMoreBatches and (not omitFullJournalData or offloadedToEntryStore or recordAllSequenceClosed))
            )
            and not (isStrictMPServer() and isRecordAllRequest and offloadedToEntryStore
                and not shouldTransmitPreStrippedRecordAllShell and not shouldTransmitIntermediateRecordAllShell)
        then
            transmittedRecordJournalModData = safeTransmitServerJournalModData(
                journal,
                shouldTransmitPreStrippedRecordAllShell and "recordProgressPreStrippedShell"
                    or (shouldTransmitIntermediateRecordAllShell and "recordProgressIntermediateShell" or "recordProgress")
            )
            BurdJournals.debugPrint("[BurdJournals] transmitModData called on existing journal")
        elseif omitFullJournalData then
            BurdJournals.debugPrint("[BurdJournals] Skipped intermediate full journal transmit for recordProgress"
                .. " (totalItems=" .. tostring(totalItems)
                .. ", remaining=" .. tostring(recordQueueRemaining) .. ")")
        end
    end

    BurdJournals.Server.finishRecordProgressResponse(player, {
        journal = journal,
        journalId = journalId,
        journalUUID = journalUUID,
        newJournalId = newJournalId,
        skillsRecorded = skillsRecorded,
        traitsRecorded = traitsRecorded,
        statsRecorded = statsRecorded,
        recipesRecorded = recipesRecorded,
        skillNames = skillNames,
        traitNames = traitNames,
        statNames = statNames,
        recipeNames = recipeNames,
        recordDelta = recordDelta,
        recordQueueRemaining = recordQueueRemaining,
        isRecordAllRequest = isRecordAllRequest,
        recordAllHasMoreBatches = recordAllHasMoreBatches,
        recordAllSequenceClosed = recordAllSequenceClosed,
        omitFullJournalData = omitFullJournalData,
        transmittedRecordJournalModData = transmittedRecordJournalModData,
        forceFlushRecordEntryStoreShard = forceFlushRecordEntryStoreShard,
        debugInSkills = debugInSkills,
        debugInTraits = debugInTraits,
        debugInStats = debugInStats,
        debugInRecipes = debugInRecipes,
        validSkillCount = validSkillCount,
        validTraitCount = validTraitCount,
        validStatCount = validStatCount,
        validRecipeCount = validRecipeCount,
        totalItems = totalItems,
        residentItemEntries = residentItemEntries,
        storedEntryManifestCount = storedEntryManifestCount,
        strictMPRecordAllPreStrippedEntries = strictMPRecordAllPreStrippedEntries,
        offloadedToEntryStore = offloadedToEntryStore,
        recordWritingToolState = recordWritingToolState,
        bsjRecordPerfStartMs = bsjRecordPerfStartMs,
        bsjRecordPerfValidateMs = bsjRecordPerfValidateMs,
        bsjRecordPerfWriteMs = bsjRecordPerfWriteMs,
        bsjRecordPerfEntryStoreMs = bsjRecordPerfEntryStoreMs,
        bsjRecordPerfBaselineMs = bsjRecordPerfBaselineMs,
        bsjRecordPerfBaselineUpdates = bsjRecordPerfBaselineUpdates,
    })
end

function BurdJournals.Server.materializeDeferredBlankAfterRecordCancellation(player, journal)
    if not (player and journal and journal.getFullType and journal.getModData) then
        return journal, false, nil
    end

    local journalType = tostring(journal:getFullType() or "")
    local sourceModData = journal:getModData()
    local sourceData = sourceModData and sourceModData.BurdJournals or nil
    if not string.find(journalType, "Blank", 1, true)
        or type(sourceData) ~= "table"
        or sourceData.deferredBlankMaterialization ~= true
    then
        return journal, false, nil
    end

    if BurdJournals.refreshJournalEntryStoreCounts and sourceData.entryStoreEnabled == true then
        BurdJournals.refreshJournalEntryStoreCounts(sourceData, "recordAllCancelledMaterialize")
    end
    local committedEntries = BurdJournals.Server.countJournalPayloadEntries(sourceData)
    if type(sourceData.entryStoreEntryCounts) == "table" then
        for _, bucketName in ipairs(BurdJournals.ENTRY_STORE_BUCKETS or {"skills", "traits", "stats", "recipes"}) do
            committedEntries = committedEntries
                + math.max(0, tonumber(sourceData.entryStoreEntryCounts[bucketName]) or 0)
        end
    end
    if committedEntries <= 0 then
        return journal, false, nil
    end

    local sourceContainer = journal:getContainer()
    if not sourceContainer then
        return journal, false, "sourceContainerUnavailable"
    end
    local savedData = BurdJournals.Server.deepCopy(sourceData)
    if type(savedData) ~= "table" then
        return journal, false, "materializationFailed"
    end
    savedData.deferredBlankMaterialization = nil
    savedData.deferredBlankMaterializationReason = nil
    savedData.isWritten = true
    if BurdJournals.clearPlayerJournalDebugProvenance then
        BurdJournals.clearPlayerJournalDebugProvenance(savedData, "recordAllCancelledMaterialized")
    end

    local filledJournal = addJournalItemToContainerCompat(sourceContainer, "BurdJournals.FilledSurvivalJournal")
    if not filledJournal then
        return journal, false, "materializationFailed"
    end
    local filledModData = filledJournal:getModData()
    if not filledModData then
        sourceContainer:Remove(filledJournal)
        return journal, false, "materializationFailed"
    end
    filledModData.BurdJournals = savedData
    BurdJournals.updateJournalName(filledJournal)
    BurdJournals.updateJournalIcon(filledJournal)
    if BurdJournals.compactJournalData then
        BurdJournals.compactJournalData(filledJournal)
    end
    if BurdJournals.Server.touchPlayerJournalRegistry
        and not BurdJournals.Server.touchPlayerJournalRegistry(filledJournal, player, "recordAllCancelledMaterialized")
    then
        sourceContainer:Remove(filledJournal)
        return journal, false, "registryPersistFailed"
    end

    local oldJournalId = journal.getID and journal:getID() or nil
    local journalUUID = savedData.uuid or savedData.entryStoreUUID
    sourceModData.BurdJournals = {
        uuid = journalUUID,
        replacedByJournalId = filledJournal.getID and filledJournal:getID() or nil,
        replacedByJournalUUID = journalUUID,
        replacedAt = getGameTime():getWorldAgeHours(),
        replacementReason = "recordAllCancelledMaterialized",
    }
    if player.getPrimaryHandItem and player:getPrimaryHandItem() == journal then
        player:setPrimaryHandItem(nil)
    end
    if player.getSecondaryHandItem and player:getSecondaryHandItem() == journal then
        player:setSecondaryHandItem(nil)
    end
    sourceContainer:Remove(journal)
    if sendRemoveItemFromContainer then
        sendRemoveItemFromContainer(sourceContainer, journal)
    end
    if sendAddItemToContainer then
        sendAddItemToContainer(sourceContainer, filledJournal)
    end
    if sourceContainer.setDrawDirty then
        sourceContainer:setDrawDirty(true)
    end
    BurdJournals.Server.sendToClient(player, "journalMaterialized", {
        oldJournalId = oldJournalId,
        oldJournalUUID = journalUUID,
        newJournalId = filledJournal.getID and filledJournal:getID() or nil,
        journalUUID = journalUUID,
        journalData = savedData.entryStoreEnabled == true and nil or savedData,
        fullJournalDataOmitted = savedData.entryStoreEnabled == true,
        fullJournalDataOmittedReason = savedData.entryStoreEnabled == true and "recordAllCancelled" or nil,
        source = "recordAllCancelled",
    })
    return filledJournal, true, nil
end

function BurdJournals.Server.handleSyncJournalData(player, args)
    local function sendSyncFailure(errorCode, journalId, journalUUID)
        BurdJournals.Server.sendToClient(player, "syncSuccess", {
            journalId = journalId or (type(args) == "table" and args.journalId or nil),
            journalUUID = journalUUID or normalizeCommandJournalUUID(args),
            serverRuntimeMarker = BurdJournals.RUNTIME_LOAD_MARKER,
            syncReason = type(args) == "table" and args.reason or nil,
            syncError = errorCode or "syncFailed",
            terminal = true,
        })
    end
    if not hasAnyJournalLookupArgs(args) then
        BurdJournals.debugPrint("[BurdJournals] handleSyncJournalData: Invalid request (no journal lookup args)")
        sendSyncFailure("invalidRequest")
        return
    end

    local journal, requestedJournalId = resolveServerCommandJournal(player, args, "syncJournalData")
    if not journal then
        BurdJournals.debugPrint("[BurdJournals] handleSyncJournalData: Journal not found: " .. tostring(requestedJournalId or args.journalId))
        sendSyncFailure("journalNotFound", requestedJournalId or args.journalId, normalizeCommandJournalUUID(args))
        return
    end
    local syncReason = tostring(args.reason or "")
    if not maybeMigrateRuntimeOnTouch(journal, player, "syncJournalData") then
        sendSyncFailure("staleJournalInstance", requestedJournalId or args.journalId, normalizeCommandJournalUUID(args))
        return
    end
    if not enforceJournalLightRequirement(player, "syncJournalData") then
        sendSyncFailure("lightRequired", requestedJournalId or args.journalId, normalizeCommandJournalUUID(args))
        return
    end

    if syncReason == "hiddenCursedPresentation"
        and BurdJournals.Server.materializeSandboxHiddenCursedBloodyItemIfNeeded
    then
        local replacement, materialized, oldJournalId, oldJournalUUID, materializedData =
            BurdJournals.Server.materializeSandboxHiddenCursedBloodyItemIfNeeded(player, journal, "syncJournalData:hiddenCursedPresentation")
        if materialized and replacement then
            journal = replacement
            requestedJournalId = replacement.getID and replacement:getID() or requestedJournalId
            BurdJournals.Server.sendToClient(player, "journalMaterialized", {
                oldJournalId = oldJournalId or args.journalId,
                oldJournalUUID = oldJournalUUID or args.journalUUID,
                newJournalId = requestedJournalId,
                journalUUID = materializedData and materializedData.uuid or oldJournalUUID or args.journalUUID,
                journalData = materializedData,
                source = "hiddenCursedPresentation",
            })
        end
    end

    local resolvedJournalId = journal.getID and journal:getID() or requestedJournalId or args.journalId
    local skipLootReveal = syncReason == "hiddenCursedPresentation" or syncReason == "recordAllCancelled"
    local syncOffloadedToEntryStore = false
    local syncPromotedEntryStoreShell = false
    local syncEntryStoreShardKey = nil
    BurdJournals.debugPrint("[BurdJournals] handleSyncJournalData: Syncing journal " .. tostring(resolvedJournalId))

    local modData = journal:getModData()
    if modData and type(modData.BurdJournals) == "table" and BurdJournals.Server.preferRicherEntryStoreData then
        local preferredData, preferredRicherStore = BurdJournals.Server.preferRicherEntryStoreData(
            modData.BurdJournals,
            args,
            "syncJournalDataPartialResident"
        )
        if preferredRicherStore then
            modData.BurdJournals = preferredData
            syncPromotedEntryStoreShell = true
        end
    end
    if modData and modData.BurdJournals then
        BurdJournals.Server.ensureGeneratedLootLoreNote(player, journal, modData.BurdJournals)
        BurdJournals.Server.syncHiddenCursedPendingLoreIdentity(modData.BurdJournals)
        if not skipLootReveal then
            markLootRewardsRevealed(player, journal, modData.BurdJournals)
        end
        if syncReason == "recordAllCancelled"
            and modData.BurdJournals.isPlayerCreated == true
            and BurdJournals.Server.promotePlayerJournalRecordBatchToExportBaseline
        then
            local characterId = BurdJournals.getPlayerCharacterId and BurdJournals.getPlayerCharacterId(player) or "unknown"
            local queueKey = tostring(characterId) .. "|" .. tostring(modData.BurdJournals.uuid or args.journalUUID or resolvedJournalId or "unknown")
            local pending = BurdJournals.Server._recordAllExportBaselineQueue[queueKey]
            if type(pending) == "table" and #pending > 0 then
                BurdJournals.Server._recordAllExportBaselineQueue[queueKey] = nil
                BurdJournals.Server.promotePlayerJournalRecordBatchToExportBaseline(player, pending)
                BurdJournals.debugPrint("[BurdJournals] handleSyncJournalData: flushed deferred record-all baseline updates after cancellation count=" .. tostring(#pending))
            end
        end
        if isStrictMPServer() and BurdJournals.offloadJournalEntriesToStoreIfNeeded then
            local entryCount = BurdJournals.countJournalEntryStoreEntries
                and BurdJournals.countJournalEntryStoreEntries(modData.BurdJournals)
                or BurdJournals.Server.countJournalPayloadEntries(modData.BurdJournals)
            local fullSyncEntryLimit = BurdJournals.getJournalFullSyncEntryLimit and BurdJournals.getJournalFullSyncEntryLimit(modData.BurdJournals)
                or math.max(1, tonumber(BurdJournals.FULL_SYNC_ENTRY_LIMIT) or 180)
            local allowReadOffload = not BurdJournals.shouldAutoOffloadJournalEntriesForSource
                or BurdJournals.shouldAutoOffloadJournalEntriesForSource(modData.BurdJournals, "syncJournalData")
            if allowReadOffload and (entryCount >= fullSyncEntryLimit or modData.BurdJournals.entryStoreEnabled == true or syncReason == "recordAllCancelled") then
                local offloaded, shardKey = BurdJournals.offloadJournalEntriesToStoreIfNeeded(journal, "syncJournalData", true, true)
                syncOffloadedToEntryStore = offloaded == true
                syncEntryStoreShardKey = shardKey or syncEntryStoreShardKey
            elseif not allowReadOffload and entryCount >= fullSyncEntryLimit then
                BurdJournals.debugPrint("[BurdJournals] handleSyncJournalData: preserved inline legacy journal data during read sync; entries="
                    .. tostring(entryCount))
            end
        end
    end

    if syncReason == "recordAllCancelled"
        and BurdJournals.Server.materializeDeferredBlankAfterRecordCancellation
    then
        local replacement, materialized, materializeError =
            BurdJournals.Server.materializeDeferredBlankAfterRecordCancellation(player, journal)
        if materializeError then
            sendSyncFailure(materializeError, resolvedJournalId, normalizeCommandJournalUUID(args))
            return
        end
        if materialized and replacement then
            journal = replacement
            resolvedJournalId = replacement.getID and replacement:getID() or resolvedJournalId
            modData = replacement:getModData()
            syncPromotedEntryStoreShell = true
        end
    end
    if isStrictMPServer()
        and modData
        and type(modData.BurdJournals) == "table"
        and modData.BurdJournals.entryStoreEnabled ~= true
        and not (modData.BurdJournals.isPlayerCreated == true and modData.BurdJournals.isWritten == false)
        and BurdJournals.Server.countJournalPayloadEntries(modData.BurdJournals) <= 0
    then
        local fallbackUUID = normalizeCommandJournalUUID(args) or resolveCommandJournalUUIDFromIndex(player, args)
            or (type(modData.BurdJournals.uuid) == "string" and modData.BurdJournals.uuid ~= "" and modData.BurdJournals.uuid)
            or nil
        local fallbackJournalData = fallbackUUID
            and BurdJournals.Server.buildEntryStoreOnlyJournalData
            and BurdJournals.Server.buildEntryStoreOnlyJournalData(fallbackUUID, "syncJournalDataEmptyShell")
            or nil
        if type(fallbackJournalData) == "table" then
            modData.BurdJournals = fallbackJournalData
            syncPromotedEntryStoreShell = true
            BurdJournals.debugPrint("[BurdJournals] handleSyncJournalData: promoted empty item shell to entry-store manifest for uuid="
                .. tostring(fallbackUUID))
        end
    end

    BurdJournals.Server.updateJournalUUIDIndex(journal, player, "syncJournalData")

    local syncTransmittedJournalModData = false
    local syncJournalUsesEntryStore = type(modData and modData.BurdJournals) == "table" and modData.BurdJournals.entryStoreEnabled == true
    if syncOffloadedToEntryStore and syncEntryStoreShardKey and BurdJournals.markEntryStoreShardDirty then
        BurdJournals.markEntryStoreShardDirty(syncEntryStoreShardKey, true)
    end
    if journal.transmitModData
        and syncReason ~= "recordAllCancelled"
        and not (isStrictMPServer() and syncJournalUsesEntryStore and not syncOffloadedToEntryStore and not syncPromotedEntryStoreShell)
        and not BurdJournals.Server.shouldDeferBatchTransmit()
    then
        syncTransmittedJournalModData = safeTransmitServerJournalModData(journal, "syncJournalData")
        BurdJournals.debugPrint("[BurdJournals] handleSyncJournalData: transmitModData called for journal " .. tostring(resolvedJournalId))
    elseif journal.transmitModData and syncReason == "recordAllCancelled" then
        if syncJournalUsesEntryStore and BurdJournals.refreshJournalEntryStoreCounts then
            BurdJournals.refreshJournalEntryStoreCounts(modData.BurdJournals, "syncJournalDataCancelled")
        end
        syncTransmittedJournalModData = safeTransmitServerJournalModData(journal, "syncJournalDataCancelled")
        BurdJournals.debugPrint("[BurdJournals] handleSyncJournalData: transmitted durable journal shell for record-all cancellation")
    elseif isStrictMPServer() and syncJournalUsesEntryStore then
        BurdJournals.debugPrint("[BurdJournals] handleSyncJournalData: skipped offloaded journal shell transmit in strict MP")
    elseif syncReason == "recordAllCancelled" then
        BurdJournals.debugPrint("[BurdJournals] handleSyncJournalData: skipped transmitModData for record-all cancellation")
    end

    -- Send back the journal data so client can update UI
    if modData and modData.BurdJournals then
        if syncReason == "recordAllCancelled" then
            if BurdJournals.shouldLogMPPerf and BurdJournals.shouldLogMPPerf() then
                bsjWriteLogLine("[BurdJournals][SyncPerf] player=" .. tostring(player and player.getUsername and player:getUsername() or "?")
                    .. " reason=recordAllCancelled"
                    .. " itemTransmit=" .. tostring(syncTransmittedJournalModData)
                    .. " argEstimate=" .. tostring(estimatePayloadBytes and estimatePayloadBytes(args) or "nil"))
            end
            local cancelledPayload = buildSyncSuccessPayload(resolvedJournalId, modData.BurdJournals, player)
            cancelledPayload.syncReason = "recordAllCancelled"
            cancelledPayload.forceAuthoritativeEntrySync = true
            BurdJournals.Server.sendToClient(player, "syncSuccess", cancelledPayload)
            return
        end
        BurdJournals.Server.sendToClient(player, "syncSuccess",
            buildSyncSuccessPayload(resolvedJournalId, modData.BurdJournals, player))
    else
        BurdJournals.Server.sendToClient(player, "syncSuccess", {
            journalId = resolvedJournalId,
            journalUUID = normalizeCommandJournalUUID(args),
            serverRuntimeMarker = BurdJournals.RUNTIME_LOAD_MARKER,
            needsSync = true,
            syncReason = syncReason,
            syncError = "journalDataUnavailable",
            terminal = true,
        })
    end
end

function BurdJournals.Server.handleRequestJournalEntryChunk(player, args)
    if not hasAnyJournalLookupArgs(args) then
        BurdJournals.debugPrint("[BurdJournals] handleRequestJournalEntryChunk: Invalid request (no journal lookup args)")
        BurdJournals.Server.sendToClient(player, "journalEntryChunk", {
            requestId = type(args) == "table" and args.requestId or nil,
            serverRuntimeMarker = BurdJournals.RUNTIME_LOAD_MARKER,
            journalId = type(args) == "table" and args.journalId or nil,
            journalUUID = type(args) == "table" and args.journalUUID or nil,
            error = "Invalid request.",
            errorCode = "invalidRequest",
            terminal = true,
        })
        return
    end

    local journal, requestedJournalId = resolveServerCommandJournal(player, args, "requestJournalEntryChunk")
    if not journal then
        BurdJournals.debugPrint("[BurdJournals] handleRequestJournalEntryChunk: Journal not found: "
            .. tostring(requestedJournalId or args.journalId))
        BurdJournals.Server.sendToClient(player, "journalEntryChunk", {
            requestId = args and args.requestId or nil,
            serverRuntimeMarker = BurdJournals.RUNTIME_LOAD_MARKER,
            journalId = args and args.journalId or nil,
            journalUUID = args and args.journalUUID or nil,
            error = "Journal not found.",
            terminal = true,
        })
        return
    end
    if not maybeMigrateRuntimeOnTouch(journal, player, "requestJournalEntryChunk") then
        BurdJournals.Server.sendToClient(player, "journalEntryChunk", {
            requestId = args and args.requestId or nil,
            serverRuntimeMarker = BurdJournals.RUNTIME_LOAD_MARKER,
            journalId = requestedJournalId or (args and args.journalId or nil),
            journalUUID = args and args.journalUUID or nil,
            error = "Stale journal instance.",
            errorCode = "staleJournalInstance",
            terminal = true,
        })
        return
    end
    if not enforceJournalLightRequirement(player, "requestJournalEntryChunk") then
        BurdJournals.Server.sendToClient(player, "journalEntryChunk", {
            requestId = args and args.requestId or nil,
            serverRuntimeMarker = BurdJournals.RUNTIME_LOAD_MARKER,
            journalId = requestedJournalId or (args and args.journalId or nil),
            journalUUID = args and args.journalUUID or nil,
            error = "Light required.",
            errorCode = "lightRequired",
            terminal = true,
        })
        return
    end

    local resolvedJournalId = journal.getID and journal:getID() or requestedJournalId or args.journalId
    local modData = journal:getModData()
    local journalData = modData and modData.BurdJournals or nil
    if type(journalData) ~= "table" then
        local fallbackUUID = normalizeCommandJournalUUID(args) or resolveCommandJournalUUIDFromIndex(player, args)
        journalData = fallbackUUID
            and BurdJournals.Server.buildEntryStoreOnlyJournalData
            and BurdJournals.Server.buildEntryStoreOnlyJournalData(fallbackUUID, "requestJournalEntryChunkNoItemData")
            or nil
    end
    if type(journalData) ~= "table" then
        BurdJournals.Server.sendToClient(player, "journalEntryChunk", {
            requestId = args and args.requestId or nil,
            serverRuntimeMarker = BurdJournals.RUNTIME_LOAD_MARKER,
            journalId = resolvedJournalId,
            journalUUID = args and args.journalUUID or nil,
            error = "Journal data unavailable.",
            terminal = true,
        })
        return
    end
    if BurdJournals.Server.preferEntryStoreDataForEmptyShell then
        journalData = BurdJournals.Server.preferEntryStoreDataForEmptyShell(journalData, args, "requestJournalEntryChunkEmptyShell")
    end

    if isStrictMPServer() and BurdJournals.offloadJournalEntriesToStoreIfNeeded then
        local entryCount = BurdJournals.countJournalEntryStoreEntries
            and BurdJournals.countJournalEntryStoreEntries(journalData)
            or BurdJournals.Server.countJournalPayloadEntries(journalData)
        local fullSyncEntryLimit = BurdJournals.getJournalFullSyncEntryLimit and BurdJournals.getJournalFullSyncEntryLimit(journalData)
            or math.max(1, tonumber(BurdJournals.FULL_SYNC_ENTRY_LIMIT) or 180)
        local allowReadOffload = not BurdJournals.shouldAutoOffloadJournalEntriesForSource
            or BurdJournals.shouldAutoOffloadJournalEntriesForSource(journalData, "requestJournalEntryChunk")
        if allowReadOffload and (entryCount >= fullSyncEntryLimit or journalData.entryStoreEnabled == true) then
            BurdJournals.offloadJournalEntriesToStoreIfNeeded(journal, "requestJournalEntryChunk", false, true)
        elseif not allowReadOffload and entryCount >= fullSyncEntryLimit then
            BurdJournals.debugPrint("[BurdJournals] handleRequestJournalEntryChunk: served inline legacy journal without read offload; entries="
                .. tostring(entryCount))
        end
    end

    local payload = BurdJournals.Server.buildJournalEntryChunkPayload(resolvedJournalId, journalData, args)
    if BurdJournals.shouldLogMPPerf and BurdJournals.shouldLogMPPerf() then
        bsjWriteLogLine("[BurdJournals][EntryChunkServer] journalId=" .. tostring(resolvedJournalId)
            .. " uuid=" .. tostring(payload and payload.journalUUID)
            .. " bucket=" .. tostring(payload and payload.bucket)
            .. " offset=" .. tostring(payload and payload.offset)
            .. " nextOffset=" .. tostring(payload and payload.nextOffset)
            .. " total=" .. tostring(payload and payload.total)
            .. " done=" .. tostring(payload and payload.done)
            .. " counts=" .. bsjFormatEntryStoreCounts(journalData.entryStoreEntryCounts))
    end
    BurdJournals.Server.sendToClient(player, "journalEntryChunk", payload)
end

function BurdJournals.Server.handleLearnSkills(player, args)
    if not args or not hasAnyJournalLookupArgs(args) then
        BurdJournals.Server.sendToClient(player, "error", {message = "Invalid request."})
        return
    end

    local journal = resolveServerCommandJournal(player, args, "learnSkills")
    if not journal then
        BurdJournals.Server.sendToClient(player, "error", {message = "Journal not found."})
        return
    end
    if not maybeMigrateRuntimeOnTouch(journal, player, "learnSkills") then
        return
    end
    if not enforceJournalLightRequirement(player, "learnSkills") then
        return
    end

    if not BurdJournals.canSetXP(journal) then
        BurdJournals.Server.sendToClient(player, "error", {message = "Cannot learn from this journal."})
        return
    end

    local modData = journal:getModData()
    if not modData.BurdJournals or not modData.BurdJournals.skills then
        BurdJournals.Server.sendToClient(player, "error", {message = "This journal has no skill data."})
        return
    end

    local selectedSkills = args.skills
    local journalSkills = modData.BurdJournals.skills

    local skillsToSet = {}
    local fallbackSkillsToSet = {}
    local skillsApplied = 0
    local applyFailedAny = false
    local normalizedAny = false
    local consumedAny = false
    local claimSessionId = args and args.claimSessionId
    local learnModeBefore, learnModeAfter, learnModeChanged = nil, nil, false
    local shouldNormalizeLearnMode = not (modData.BurdJournals and modData.BurdJournals.isPlayerCreated == true and modData.BurdJournals.recordedWithBaseline == true)
    if shouldNormalizeLearnMode and normalizeDebugJournalXPMode then
        learnModeBefore, learnModeAfter, learnModeChanged = normalizeDebugJournalXPMode(modData.BurdJournals, player)
        if learnModeChanged then
            BurdJournals.debugPrint("[BurdJournals] handleLearnSkills: normalized journal XP mode " .. tostring(learnModeBefore) .. " -> " .. tostring(learnModeAfter))
        end
    end
    local hasSelectedSkills = BurdJournals.hasAnyEntries and BurdJournals.hasAnyEntries(selectedSkills)
    for skillName, storedData in pairs(journalSkills) do

        if not hasSelectedSkills or selectedSkills[skillName] then
            local normalizedXP = tonumber(storedData.xp) or 0
            local normalizedLevel = tonumber(storedData.level) or 0
            local normalized = false
            if BurdJournals.normalizeLegacySkillEntry then
                normalizedXP, normalizedLevel, normalized = BurdJournals.normalizeLegacySkillEntry(skillName, storedData, modData.BurdJournals.recordedWithBaseline)
                if normalized then
                    storedData.xp = normalizedXP
                    storedData.level = normalizedLevel
                    normalizedAny = true
                end
            end

            local multiplier = 1.0
            local shouldConsumeClaimRead = BurdJournals.shouldConsumeJournalClaimRead == nil
                or BurdJournals.shouldConsumeJournalClaimRead(modData.BurdJournals, "skills") == true
            if shouldConsumeClaimRead and BurdJournals.consumeJournalClaimRead then
                multiplier = BurdJournals.consumeJournalClaimRead(modData.BurdJournals, skillName, claimSessionId, player)
                consumedAny = true
            end
            local effectiveRecordedXP = math.floor(normalizedXP * multiplier)
            local targetXP = effectiveRecordedXP
            local claimUsesEarnedDeltaGrant = false
            if BurdJournals.Server.getSkillClaimTargetXP then
                targetXP, _, _, claimUsesEarnedDeltaGrant = BurdJournals.Server.getSkillClaimTargetXP(player, modData.BurdJournals, skillName, effectiveRecordedXP, nil)
            end
            -- Compute level from XP instead of reading stored level (for backward compatibility)
            -- Pass skillName for proper Fitness/Strength XP thresholds
            local computedLevel = normalizedLevel > 0 and normalizedLevel
                or (BurdJournals.getSkillLevelFromXP and BurdJournals.getSkillLevelFromXP(targetXP, skillName))
                or math.floor(targetXP / 75)
            local perk = BurdJournals.getPerkByName(skillName)
            if perk then
                local currentXP = BurdJournals.getPlayerSkillTotalXP and BurdJournals.getPlayerSkillTotalXP(player, perk, skillName) or player:getXp():getXP(perk)
                local xpToAdd = math.max(0, targetXP - currentXP)
                skillsToSet[skillName] = {
                    xp = claimUsesEarnedDeltaGrant and xpToAdd or targetXP,
                    level = computedLevel,
                    mode = claimUsesEarnedDeltaGrant and "add" or "set"
                }

                -- Apply XP directly on server using vanilla addXp function (42.13.2+ compatible)
                if xpToAdd > 0 then
                    local applied, method = BurdJournals.Server.applyXPWithFallback(player, perk, xpToAdd, {
                        skillName = skillName,
                        useMultipliers = false,
                    })
                    if applied then
                        BurdJournals.debugPrint("[BurdJournals] Server: LearnSkills - Applied " .. tostring(xpToAdd)
                            .. " XP to " .. skillName .. " via " .. tostring(method))
                        skillsApplied = skillsApplied + 1
                    else
                        applyFailedAny = true
                        fallbackSkillsToSet[skillName] = skillsToSet[skillName]
                    end
                end
            end

            -- Legacy "Learn Skills" restores XP in one action.  It must also
            -- restore the corresponding VHS media-line history, otherwise a
            -- respawned character can reclaim journal XP and replay those VHS
            -- lines for a second reward.
            if BurdJournals.Server.markJournalSkillMediaLinesKnown then
                BurdJournals.Server.markJournalSkillMediaLinesKnown(player, storedData, skillName, args)
            end
        end
    end

    local shouldTransmitLearnChanges = normalizedAny or (consumedAny and not isStrictMPServer())
    if shouldTransmitLearnChanges and journal.transmitModData then
        safeTransmitServerJournalModData(journal, "learnSkills")
    end
    if consumedAny and (not isStrictMPServer()) and BurdJournals.captureJournalDRState then
        BurdJournals.captureJournalDRState(journal, "learnSkills", player)
    end

    if applyFailedAny and BurdJournals.hasAnyEntries and BurdJournals.hasAnyEntries(fallbackSkillsToSet) then
        BurdJournals.debugPrint("[BurdJournals] Server: LearnSkills fallback - sending applyXP to client")
        BurdJournals.Server.sendToClient(player, "applyXP", {skills = fallbackSkillsToSet, mode = "set"})
    end
    -- Notify client of success (for UI update)
    BurdJournals.Server.sendToClient(player, "learnSuccess", {skillCount = skillsApplied})
end

function BurdJournals.Server.handleClaimSkill(player, args)
    BurdJournals.debugPrint("[BurdJournals] Server: handleClaimSkill called - skillName=" .. tostring(args and args.skillName) .. ", journalId=" .. tostring(args and args.journalId) .. ", journalUUID=" .. tostring(args and args.journalUUID))

    if not args or not args.skillName then

        BurdJournals.Server.sendToClient(player, "error", {message = "Invalid request."})
        return
    end

    local journalId = args.journalId
    local skillName = args.skillName

    local journal, resolvedJournalId, resolvedJournalUUID = resolveServerCommandJournal(player, args, "claimSkill")
    if not journal then
        BurdJournals.Server.sendToClient(player, "error", {message = "Journal not found."})
        return
    end
    journalId, resolvedJournalUUID = getResolvedJournalIdentity(journal, resolvedJournalId, resolvedJournalUUID, journalId)
    if not maybeMigrateRuntimeOnTouch(journal, player, "claimSkill") then
        return
    end
    if not enforceJournalLightRequirement(player, "claimSkill") then
        return
    end

    if not BurdJournals.canSetXP(journal) then
        BurdJournals.Server.sendToClient(player, "error", {message = "Cannot claim set XP from this journal type."})
        return
    end

    local canClaim, reason = BurdJournals.canPlayerClaimFromJournal(player, journal)
    if not canClaim then
        BurdJournals.Server.sendToClient(player, "error", {message = reason or "Permission denied."})
        return
    end

    local modData = journal:getModData()
    local journalData = modData.BurdJournals
    if BurdJournals.Server.preferEntryStoreDataForEmptyShell then
        journalData = BurdJournals.Server.preferEntryStoreDataForEmptyShell(journalData, args, "claimSkillEmptyShell")
    end
    if isHiddenCursedJournalState(journal, journalData) then
        BurdJournals.Server.sendToClient(player, "error", {message = BurdJournals.Server.getHiddenCursedClaimBlockedMessage()})
        return
    end

    if not journalData or not journalData.skills then
        BurdJournals.Server.sendToClient(player, "error", {message = "This journal has no skill data."})
        return
    end

    -- Patch/update safety: restore DR counters if item ModData lost them.
    if (not isStrictMPServer()) and BurdJournals.restoreJournalDRStateIfMissing then
        BurdJournals.restoreJournalDRStateIfMissing(journal, "handleClaimSkill", player)
        journalData = modData.BurdJournals
    end

    local storedSkillName = BurdJournals.resolveSkillKey and BurdJournals.resolveSkillKey(journalData.skills, skillName) or skillName
    if journalData.skills[storedSkillName] then
        skillName = storedSkillName
    end
    if (not journalData.skills[skillName]) and BurdJournals.Server.preferEntryStoreDataForMissingEntry then
        journalData = BurdJournals.Server.preferEntryStoreDataForMissingEntry(journalData, args, "skills", skillName, "claimSkillMissingEntry")
        if journalData and journalData.skills then
            storedSkillName = BurdJournals.resolveSkillKey and BurdJournals.resolveSkillKey(journalData.skills, skillName) or skillName
            if journalData.skills[storedSkillName] then
                skillName = storedSkillName
            end
        end
    end

    if BurdJournals.isSkillEnabledForJournal and not BurdJournals.isSkillEnabledForJournal(journalData, skillName) then
        BurdJournals.Server.sendToClient(player, "error", {message = "That skill is disabled by sandbox settings for this journal."})
        return
    end

    if not journalData.skills[skillName] then
        bsjWriteLogLine("[BurdJournals] Server ERROR: Skill '" .. skillName .. "' not found in journal")
        BurdJournals.Server.sendToClient(player, "error", {message = "Skill not found in journal.", errorCode = "entryNotFound"})
        return
    end

    -- Keep claim semantics stable for found journals even if debug edits changed flags.
    normalizeFoundJournalClaimFlags(journal, journalData, "handleClaimSkill")
    local limitedClaimMode = BurdJournals.Server.limitedLootClaimModeActive(journal)
    if BurdJournals.Server.enforceLimitedLootClaimBudget(player, journal, journalData) then
        return
    end

    -- Check if already claimed by this character (only for non-player journals).
    -- Player journals allow repeated claims (subject to target XP / sandbox policy).
    local isPlayerJournal = (BurdJournals.isEffectivelyPlayerJournal and BurdJournals.isEffectivelyPlayerJournal(journalData))
        or journalData.isPlayerCreated == true
    if not isPlayerJournal and BurdJournals.hasCharacterClaimedSkill(journalData, player, skillName) then
        BurdJournals.debugPrint("[BurdJournals] Server: Skill '" .. skillName .. "' already claimed by this character from found journal, skipping")
        BurdJournals.Server.sendToClient(player, "claimSuccess", attachRuntimeDeltaOrLegacyJournalData({
            skillName = skillName,
            xpAdded = 0,
            journalId = journalId,
            alreadyClaimed = true
        }, journalData, player, "claimSkillAlreadyClaimed"))
        return {
            skillName = skillName,
            completed = false,
            claimed = false,
            alreadyClaimed = true,
        }
    end

    local skillData = journalData.skills[skillName]
    local claimModeBefore, claimModeAfter, claimModeChanged, claimModeAutoRepaired = nil, nil, false, false
    local shouldNormalizeClaimMode = not (isPlayerJournal and journalData.recordedWithBaseline == true)
    if shouldNormalizeClaimMode and normalizeDebugJournalXPMode then
        claimModeBefore, claimModeAfter, claimModeChanged, claimModeAutoRepaired = normalizeDebugJournalXPMode(journalData, player)
        if claimModeChanged then
            BurdJournals.debugPrint("[BurdJournals] handleClaimSkill: normalized journal XP mode " .. tostring(claimModeBefore) .. " -> " .. tostring(claimModeAfter))
        end
        if claimModeAutoRepaired then
            BurdJournals.debugPrint("[BurdJournals] handleClaimSkill: auto-repaired mismatched baseline flag using legacy absolute-entry detection")
        end
    end

    local recordedXP = tonumber(skillData.xp) or 0
    local recordedLevel = tonumber(skillData.level) or 0
    local normalizedLegacy = false
    if BurdJournals.normalizeLegacySkillEntry then
        recordedXP, recordedLevel, normalizedLegacy = BurdJournals.normalizeLegacySkillEntry(skillName, skillData, journalData.recordedWithBaseline)
        if normalizedLegacy then
            skillData.xp = recordedXP
            skillData.level = recordedLevel
            journalData.skills[skillName] = skillData
            BurdJournals.debugPrint("[BurdJournals] Normalized legacy journal XP entry for " .. tostring(skillName) .. ": xp=" .. tostring(recordedXP) .. ", level=" .. tostring(recordedLevel))
        end
    end
    -- Compute level from XP instead of reading stored level (for backward compatibility)
    -- Pass skillName for proper Fitness/Strength XP thresholds
    if recordedLevel <= 0 then
        recordedLevel = (BurdJournals.getSkillLevelFromXP and BurdJournals.getSkillLevelFromXP(recordedXP, skillName)) or math.floor(recordedXP / 75)
    end

    -- Compute diminishing returns without mutating the journal. The read is
    -- committed only after the authoritative claim finishes successfully, so a
    -- retryable MP XP failure cannot spend one or more reads for no reward.
    local claimMultiplier, claimReadCount = 1.0, tonumber(journalData.readCount) or 0
    local shouldConsumeClaimRead = BurdJournals.shouldConsumeJournalClaimRead == nil
        or BurdJournals.shouldConsumeJournalClaimRead(journalData, "skills") == true
    if shouldConsumeClaimRead then
        claimMultiplier, claimReadCount = BurdJournals.getJournalClaimMultiplier(
            journalData,
            0,
            skillName,
            args and args.claimSessionId,
            player
        )
    end
    local effectiveRecordedXP = math.max(0, math.floor(recordedXP * claimMultiplier))
    local claimTargetXP, baselineXPForClaim, baselineSuppressedForClaim, claimUsesEarnedDeltaGrant = effectiveRecordedXP, 0, false, false
    if BurdJournals.Server.getSkillClaimTargetXP then
        claimTargetXP, baselineXPForClaim, baselineSuppressedForClaim, claimUsesEarnedDeltaGrant = BurdJournals.Server.getSkillClaimTargetXP(player, journalData, skillName, effectiveRecordedXP, args and args.baselineXP)
    end
    local claimTargetLevel = recordedLevel
    if claimTargetXP > 0 then
        claimTargetLevel = (BurdJournals.getSkillLevelFromXP and BurdJournals.getSkillLevelFromXP(claimTargetXP, skillName)) or claimTargetLevel
    end

    local perk = BurdJournals.getPerkByName(skillName)
    if not perk then
        bsjWriteLogLine("[BurdJournals] Server ERROR: Could not find perk for skill '" .. skillName .. "'")
        BurdJournals.Server.sendToClient(player, "error", {message = "Invalid skill: " .. skillName})
        return
    end

    -- Baseline-aware player journals award their earned delta additively and remain
    -- reusable. Legacy absolute entries and non-baseline journals still use set mode.
    local journalUsesBaselineMode = BurdJournals.getJournalSkillRecordingMode
        and BurdJournals.getJournalSkillRecordingMode(journalData, player)
        or (journalData.recordedWithBaseline == true)
    local useAddMode = claimUsesEarnedDeltaGrant or ((not isPlayerJournal) and journalUsesBaselineMode == true)
    local strictMPClaimFlow = isStrictMPServer and isStrictMPServer()
    local reusableSkillClaim = isPlayerJournal and (claimUsesEarnedDeltaGrant or strictMPClaimFlow)
    local debugLoggingEnabled = BurdJournals.shouldDebugLog and BurdJournals.shouldDebugLog() or false
    -- isPlayerJournal already declared above
    -- Debug logging
    if debugLoggingEnabled then
        BurdJournals.debugPrint("[BurdJournals] Server ClaimSkill DEBUG:")
        BurdJournals.debugPrint("  - skillName: " .. tostring(skillName))
        BurdJournals.debugPrint("  - recordedXP: " .. tostring(recordedXP))
        BurdJournals.debugPrint("  - recordedLevel: " .. tostring(recordedLevel))
        BurdJournals.debugPrint("  - effectiveRecordedXP: " .. tostring(effectiveRecordedXP))
        BurdJournals.debugPrint("  - claimTargetXP: " .. tostring(claimTargetXP))
        BurdJournals.debugPrint("  - claimTargetLevel: " .. tostring(claimTargetLevel))
        BurdJournals.debugPrint("  - baselineXPForClaim: " .. tostring(baselineXPForClaim))
        BurdJournals.debugPrint("  - baselineSuppressedForClaim: " .. tostring(baselineSuppressedForClaim))
        BurdJournals.debugPrint("  - claimMultiplier: " .. tostring(claimMultiplier))
        BurdJournals.debugPrint("  - claimReadCount: " .. tostring(claimReadCount))
        BurdJournals.debugPrint("  - claimSessionId: " .. tostring(args and args.claimSessionId))
        BurdJournals.debugPrint("  - isPlayerCreated: " .. tostring(isPlayerJournal))
        BurdJournals.debugPrint("  - recordedWithBaseline: " .. tostring(journalData.recordedWithBaseline))
        BurdJournals.debugPrint("  - journalUsesBaselineMode: " .. tostring(journalUsesBaselineMode))
        BurdJournals.debugPrint("  - claimModeChanged: " .. tostring(claimModeChanged))
        BurdJournals.debugPrint("  - claimUsesEarnedDeltaGrant: " .. tostring(claimUsesEarnedDeltaGrant))
        BurdJournals.debugPrint("  - useAddMode: " .. tostring(useAddMode))
    end

    if effectiveRecordedXP > 0 then
        local currentXP = BurdJournals.getPlayerSkillTotalXP and BurdJournals.getPlayerSkillTotalXP(player, perk, skillName) or player:getXp():getXP(perk)
        local currentLevel = player:getPerkLevel(perk)
        local preClaimXP = math.max(0, tonumber(currentXP) or 0)
        local preClaimLevel = math.max(0, tonumber(currentLevel) or 0)
        local actualXpAdded = 0
        local appliedVia = nil
        local totalAfterGrant = math.max(0, tonumber(currentXP) or 0)
        local xpToApply = math.max(0, claimTargetXP - currentXP)
        local claimLockProbe = nil
        if BurdJournals.getActivePlayerJournalSkillClaimLock then
            claimLockProbe = BurdJournals.getActivePlayerJournalSkillClaimLock(journalData, player, skillName, effectiveRecordedXP, claimTargetXP, currentXP, currentLevel)
        end
        if BurdJournals.shouldDebugLog and BurdJournals.shouldDebugLog() then
            bsjWriteLogLine("[BurdJournals][SkillClaimDecision] player=" .. tostring(player and player.getUsername and player:getUsername() or "?")
                .. " skill=" .. tostring(skillName)
                .. " journalUUID=" .. tostring(journalData and journalData.uuid or args and args.journalUUID)
                .. " isPlayerJournal=" .. tostring(isPlayerJournal)
                .. " recordedXP=" .. tostring(recordedXP)
                .. " effectiveRecordedXP=" .. tostring(effectiveRecordedXP)
                .. " currentXP=" .. tostring(currentXP)
                .. " currentLevel=" .. tostring(currentLevel)
                .. " claimTargetXP=" .. tostring(claimTargetXP)
                .. " xpToApply=" .. tostring(xpToApply)
                .. " baselineXP=" .. tostring(baselineXPForClaim)
                .. " baselineSuppressed=" .. tostring(baselineSuppressedForClaim)
                .. " earnedDelta=" .. tostring(claimUsesEarnedDeltaGrant)
                .. " useAddMode=" .. tostring(useAddMode)
                .. " reusable=" .. tostring(reusableSkillClaim)
                .. " activeLock=" .. tostring(type(claimLockProbe) == "table")
                .. " fallbackSource=" .. tostring(journalData and journalData._entryStoreFallbackSource or nil))
        end

        if currentXP >= claimTargetXP then
            bsjWriteLogLine("[BurdJournals][SkillClaimAlreadyAtTarget] skill=" .. tostring(skillName)
                .. " currentXP=" .. tostring(currentXP)
                .. " claimTargetXP=" .. tostring(claimTargetXP)
                .. " isPlayerJournal=" .. tostring(isPlayerJournal)
                .. " reusable=" .. tostring(reusableSkillClaim))
            -- Player already has equal or more comparable XP than the journal can grant.
            -- Only mark as claimed for non-player journals (player journals allow reusable delta claims).
            if not useAddMode and not isPlayerJournal and not limitedClaimMode then
                BurdJournals.markSkillClaimedByCharacter(journalData, player, skillName)
            end
            if BurdJournals.Server.markJournalSkillMediaLinesKnown then
                BurdJournals.Server.markJournalSkillMediaLinesKnown(player, skillData, skillName, args)
            end
            if journal.transmitModData and not BurdJournals.Server.shouldDeferBatchTransmit(args) then
            safeTransmitServerJournalModData(journal, "claimSkill")
            end
            BurdJournals.Server.sendToClient(player, "skillMaxed", attachRuntimeDeltaOrLegacyJournalData({
                skillName = skillName,
                journalId = journalId,
                alreadyAtLevel = true,
                claimTargetXP = claimTargetXP,
                debug_targetXP = claimTargetXP,
                debug_xpAfter = currentXP,
                reusableDeltaSkillClaim = reusableSkillClaim,
                message = "You already have this much XP in " .. skillName .. "."
            }, journalData, player, "claimSkillAlreadyAtLevel"))
            -- Re-fetch journal by ID before calling shouldDissolve to avoid zombie object errors
            -- Only dissolve non-player journals
            if not isPlayerJournal then
                local freshJournal = BurdJournals.Server.resolvePostActionJournal(player, journalId, journal)
                if freshJournal and BurdJournals.isValidItem(freshJournal) and BurdJournals.shouldDissolve and BurdJournals.shouldDissolve(freshJournal, player) then
                    BurdJournals.Server.dissolveJournal(player, freshJournal)
                end
            end
            return {
                skillName = skillName,
                completed = true,
                claimed = false,
                alreadyAtLevel = true,
            }
        end

        -- For "set" mode (absolute XP), cap at recorded value to prevent over-grant
        -- Player should end up with AT MOST the recorded XP, not more
        if not useAddMode then
            BurdJournals.debugPrint("  - currentXP: " .. tostring(currentXP))
            BurdJournals.debugPrint("  - currentLevel: " .. tostring(currentLevel))
            if strictMPClaimFlow then
                BurdJournals.debugPrint("  - xpToApply (strict MP SET fallback): " .. tostring(xpToApply))

                local success, addModeVia = BurdJournals.Server.applyXPWithFallback(player, perk, xpToApply, {
                    skillName = skillName,
                    useMultipliers = false,
                })
                appliedVia = addModeVia
                if success then
                    BurdJournals.debugPrint("[BurdJournals] Server: Applied strict MP additive claim " .. tostring(xpToApply)
                        .. " XP to " .. skillName .. " via " .. tostring(appliedVia))
                else
                    bsjWriteLogLine("[BurdJournals] Server WARNING: strict MP claim did not apply XP for " .. tostring(skillName))
                    return {
                        skillName = skillName,
                        completed = false,
                        claimed = false,
                        retryable = true,
                        reason = "xpApplyFailed",
                    }
                end
                local totalAfterAdd = BurdJournals.getPlayerSkillTotalXP and BurdJournals.getPlayerSkillTotalXP(player, perk, skillName) or player:getXp():getXP(perk)
                actualXpAdded = math.max(0, tonumber(totalAfterAdd) or 0) - math.max(0, tonumber(currentXP) or 0)
                totalAfterGrant = math.max(0, tonumber(totalAfterAdd) or 0)
            else
                local success, setModeXpAdded, setModeVia = BurdJournals.Server.applySetModeSkillTargetXP(player, perk, skillName, claimTargetXP, {
                    useMultipliers = false,
                })
                actualXpAdded = math.max(0, tonumber(setModeXpAdded) or 0)
                appliedVia = setModeVia
                currentXP = BurdJournals.getPlayerSkillTotalXP and BurdJournals.getPlayerSkillTotalXP(player, perk, skillName) or player:getXp():getXP(perk)
                totalAfterGrant = math.max(0, tonumber(currentXP) or 0)
                BurdJournals.debugPrint("  - xpToApply (after SET calc): " .. tostring(math.max(0, claimTargetXP - currentXP)))
            if success then
                BurdJournals.debugPrint("[BurdJournals] Server: Applied set-mode target " .. tostring(claimTargetXP)
                        .. " XP to " .. skillName .. " via " .. tostring(appliedVia) .. " (actualAdded=" .. tostring(actualXpAdded) .. ")")
                    -- In a batch the final XP is synced once via batchClaimComplete.
                    if not BurdJournals.Server.shouldDeferBatchTransmit(args) then
                        BurdJournals.Server.sendExactSkillXPSync(player, skillName, totalAfterGrant)
                    end
                else
                    BurdJournals.debugPrint("[BurdJournals] Server: Fallback - sending applyXP to client for " .. skillName)
                    BurdJournals.Server.sendToClient(player, "applyXP", {
                        skills = {
                            [skillName] = {
                                xp = claimTargetXP,
                                mode = "set"
                            }
                        },
                        mode = "set"
                    })
                end
            end
        else
            BurdJournals.debugPrint("  - FINAL xpToApply: " .. tostring(xpToApply))

            local success, addModeVia = BurdJournals.Server.applyXPWithFallback(player, perk, xpToApply, {
                skillName = skillName,
                useMultipliers = false,
                preferNativeNoMultiplier = true,
            })
            appliedVia = addModeVia
            if success then
                BurdJournals.debugPrint("[BurdJournals] Server: Applied " .. tostring(xpToApply)
                    .. " XP to " .. skillName .. " via " .. tostring(appliedVia))
            else
                if strictMPClaimFlow then
                    bsjWriteLogLine("[BurdJournals] Server WARNING: strict MP claim did not apply XP for " .. tostring(skillName))
                    return {
                        skillName = skillName,
                        completed = false,
                        claimed = false,
                        retryable = true,
                        reason = "xpApplyFailed",
                    }
                elseif not claimUsesEarnedDeltaGrant then
                    BurdJournals.debugPrint("[BurdJournals] Server: Fallback - sending applyXP to client for " .. skillName)
                    BurdJournals.Server.sendToClient(player, "applyXP", {
                        skills = {
                            [skillName] = {
                                xp = xpToApply,
                                mode = "add"
                            }
                        },
                        mode = "add"
                    })
                end
            end
            local totalAfterAdd = BurdJournals.getPlayerSkillTotalXP and BurdJournals.getPlayerSkillTotalXP(player, perk, skillName) or player:getXp():getXP(perk)
            actualXpAdded = math.max(0, tonumber(totalAfterAdd) or 0) - math.max(0, tonumber(currentXP) or 0)
            totalAfterGrant = math.max(0, tonumber(totalAfterAdd) or 0)
            if strictMPClaimFlow and xpToApply > 0 and actualXpAdded <= 0 then
                bsjWriteLogLine("[BurdJournals] Server WARNING: strict MP claim reported success without observable XP for " .. tostring(skillName))
                return {
                    skillName = skillName,
                    completed = false,
                    claimed = false,
                    retryable = true,
                    reason = "xpNotObserved",
                }
            end
            if not strictMPClaimFlow and not BurdJournals.Server.shouldDeferBatchTransmit(args) then
                BurdJournals.Server.sendExactSkillXPSync(player, skillName, totalAfterGrant)
            end
        end

        -- NOTE: Don't call syncXp here - it disrupts batch command processing
        -- Client will request sync at end of batch via requestXpSync command

        -- Get player state AFTER XP application for debug comparison
        local xpObj = player:getXp()
        local levelAfter = player:getPerkLevel(perk)
        local xpAfter = xpObj:getXP(perk)
        bsjWriteLogLine("[BurdJournals][SkillClaimApplied] skill=" .. tostring(skillName)
            .. " xpToApply=" .. tostring(xpToApply)
            .. " actualXpAdded=" .. tostring(actualXpAdded)
            .. " totalAfterGrant=" .. tostring(totalAfterGrant)
            .. " xpAfter=" .. tostring(xpAfter)
            .. " appliedVia=" .. tostring(appliedVia)
            .. " strictMP=" .. tostring(strictMPClaimFlow)
            .. " useAddMode=" .. tostring(useAddMode)
            .. " reusable=" .. tostring(reusableSkillClaim))

        if debugLoggingEnabled then
            BurdJournals.debugPrint("================================================================================")
            BurdJournals.debugPrint("[BurdJournals CLAIM RESULT] Skill: " .. tostring(skillName))
            BurdJournals.debugPrint("[BurdJournals CLAIM RESULT]   JOURNAL EXPECTED: Level " .. tostring(claimTargetLevel) .. ", XP " .. tostring(claimTargetXP))
            BurdJournals.debugPrint("[BurdJournals CLAIM RESULT]   PLAYER AFTER:     Level " .. tostring(levelAfter) .. ", XP " .. tostring(xpAfter))
            BurdJournals.debugPrint("[BurdJournals CLAIM RESULT]   XP Applied: " .. tostring(xpToApply))
            if levelAfter < claimTargetLevel then
                bsjWriteLogLine("[BurdJournals CLAIM RESULT]   WARNING: Player level (" .. levelAfter .. ") is LESS than recorded level (" .. claimTargetLevel .. ")!")
            elseif levelAfter > claimTargetLevel then
                BurdJournals.debugPrint("[BurdJournals CLAIM RESULT]   NOTE: Player level (" .. levelAfter .. ") exceeds recorded level (" .. claimTargetLevel .. ")")
            else
                BurdJournals.debugPrint("[BurdJournals CLAIM RESULT]   SUCCESS: Player reached recorded level " .. claimTargetLevel)
            end
            BurdJournals.debugPrint("================================================================================")
        elseif levelAfter < claimTargetLevel then
            bsjWriteLogLine("[BurdJournals CLAIM RESULT]   WARNING: Player level (" .. levelAfter .. ") is LESS than recorded level (" .. claimTargetLevel .. ")!")
        end

        if shouldConsumeClaimRead then
            BurdJournals.consumeJournalClaimRead(journalData, skillName, args and args.claimSessionId, player)
            if (not isStrictMPServer()) and BurdJournals.captureJournalDRState then
                BurdJournals.captureJournalDRState(journal, "claimSkill", player)
            end
        end
        if not reusableSkillClaim then
            BurdJournals.markSkillClaimedByCharacter(journalData, player, skillName)
        end
        if reusableSkillClaim and claimUsesEarnedDeltaGrant and BurdJournals.markPlayerJournalSkillClaimLock then
            BurdJournals.markPlayerJournalSkillClaimLock(journalData, player, skillName, effectiveRecordedXP, claimTargetXP, preClaimXP, preClaimLevel, claimTargetLevel)
            if player and player.transmitModData and not BurdJournals.Server.shouldDeferBatchTransmit(args) then
                player:transmitModData()
            end
        end
        if BurdJournals.Server.markJournalSkillMediaLinesKnown then
            BurdJournals.Server.markJournalSkillMediaLinesKnown(player, skillData, skillName, args)
        end

        if journal.transmitModData
            and not BurdJournals.Server.shouldDeferBatchTransmit(args)
            and (not BurdJournals.shouldTransmitJournalItemModData
                or BurdJournals.shouldTransmitJournalItemModData(journal, "updateJournalUUIDIndexUUID"))
        then
            safeTransmitServerJournalModData(journal, "claimSkill")
        end

            local claimPayload = {
                skillName = skillName,
                xpAdded = strictMPClaimFlow and math.max(0, tonumber(xpToApply) or 0) or actualXpAdded,
                journalId = journalId,
                journalUUID = BurdJournals.getJournalIdentityUUID(journalData) or args and args.journalUUID,
                claimTargetXP = claimTargetXP,
                -- Include debug info for client
                debug_recordedLevel = recordedLevel,
            debug_recordedXP = recordedXP,
            debug_effectiveRecordedLevel = claimTargetLevel,
            debug_effectiveRecordedXP = effectiveRecordedXP,
            debug_targetXP = claimTargetXP,
            debug_baselineXP = baselineXPForClaim,
            debug_baselineSuppressed = baselineSuppressedForClaim,
            debug_claimMultiplier = claimMultiplier,
            debug_claimReadCount = claimReadCount,
            debug_levelAfter = levelAfter,
            debug_xpAfter = xpAfter,
            reusableDeltaSkillClaim = reusableSkillClaim,
            claimLock = reusableSkillClaim and claimUsesEarnedDeltaGrant and {
                recordedXP = effectiveRecordedXP,
                targetXP = claimTargetXP,
                preClaimXP = preClaimXP,
                preClaimLevel = preClaimLevel,
                targetLevel = claimTargetLevel,
            } or nil,
            }
            claimPayload.xpAfterTotal = math.max(0, tonumber(totalAfterGrant) or tonumber(claimTargetXP) or 0)
            BurdJournals.Server.sendToClient(player, "claimSuccess", attachRuntimeDeltaOrLegacyJournalData(
                claimPayload,
                journalData,
                player,
                "claimSkillSuccess"
            ))
        -- Re-fetch journal by ID before calling shouldDissolve to avoid zombie object errors
        local freshJournal = BurdJournals.Server.resolvePostActionJournal(player, journalId, journal)
        BurdJournals.debugPrint("[BurdJournals] Server: Post-claim skill check - freshJournal=" .. tostring(freshJournal ~= nil) .. ", journalId=" .. tostring(journalId))
        if freshJournal then
            local isValid = BurdJournals.isValidItem(freshJournal)
            local hasShouldDissolve = BurdJournals.shouldDissolve ~= nil
            local shouldDis = hasShouldDissolve and BurdJournals.shouldDissolve(freshJournal, player)
            BurdJournals.debugPrint("[BurdJournals] Server: isValid=" .. tostring(isValid) .. ", hasShouldDissolve=" .. tostring(hasShouldDissolve) .. ", shouldDis=" .. tostring(shouldDis))
            if isValid and shouldDis then
                BurdJournals.debugPrint("[BurdJournals] Server: DISSOLVING JOURNAL after skill claim!")
                BurdJournals.Server.dissolveJournal(player, freshJournal)
            end
        end
        return {
            skillName = skillName,
            completed = true,
            claimed = true,
            xpAdded = strictMPClaimFlow and math.max(0, tonumber(xpToApply) or 0) or actualXpAdded,
            xpAfterTotal = math.max(0, tonumber(totalAfterGrant) or tonumber(claimTargetXP) or 0),
        }
    else
        -- effectiveRecordedXP <= 0. Distinguish a genuinely empty entry from one
        -- whose XP was floored to zero purely by the diminishing-returns multiplier:
        -- in the latter case do NOT mark it claimed (that would permanently consume
        -- the entry for nothing), and tell the player why.
        local zeroedByDiminishing = (tonumber(recordedXP) or 0) > 0
        bsjWriteLogLine("[BurdJournals][SkillClaimNoXP] skill=" .. tostring(skillName)
            .. " recordedXP=" .. tostring(recordedXP)
            .. " effectiveRecordedXP=" .. tostring(effectiveRecordedXP)
            .. " zeroedByDiminishing=" .. tostring(zeroedByDiminishing)
            .. " isPlayerJournal=" .. tostring(isPlayerJournal)
            .. " reusable=" .. tostring(reusableSkillClaim))
        if not limitedClaimMode and not reusableSkillClaim and not zeroedByDiminishing then
            BurdJournals.markSkillClaimedByCharacter(journalData, player, skillName)
        end
        if journal.transmitModData and not BurdJournals.Server.shouldDeferBatchTransmit(args) then
            safeTransmitServerJournalModData(journal, "claimSkill")
        end
        BurdJournals.Server.sendToClient(player, "skillMaxed", attachRuntimeDeltaOrLegacyJournalData({
            skillName = skillName,
            journalId = journalId,
            diminishingZero = zeroedByDiminishing,
            message = zeroedByDiminishing
                and ("Diminishing returns leave no XP to claim from " .. skillName .. " right now.")
                or "No XP to claim from this skill."
        }, journalData, player, "claimSkillNoXP"))
        -- Re-fetch journal by ID before calling shouldDissolve to avoid zombie object errors
        local freshJournal = BurdJournals.Server.resolvePostActionJournal(player, journalId, journal)
        BurdJournals.debugPrint("[BurdJournals] Server: Post-skillMaxed check - freshJournal=" .. tostring(freshJournal ~= nil) .. ", journalId=" .. tostring(journalId))
        if freshJournal then
            local isValid = BurdJournals.isValidItem(freshJournal)
            local hasShouldDissolve = BurdJournals.shouldDissolve ~= nil
            local shouldDis = hasShouldDissolve and BurdJournals.shouldDissolve(freshJournal, player)
            BurdJournals.debugPrint("[BurdJournals] Server: isValid=" .. tostring(isValid) .. ", hasShouldDissolve=" .. tostring(hasShouldDissolve) .. ", shouldDis=" .. tostring(shouldDis))
            if isValid and shouldDis then
                BurdJournals.debugPrint("[BurdJournals] Server: DISSOLVING JOURNAL after skillMaxed!")
                BurdJournals.Server.dissolveJournal(player, freshJournal)
            end
        end
        return {
            skillName = skillName,
            completed = true,
            claimed = false,
            noXP = true,
            diminishingZero = zeroedByDiminishing,
        }
    end
end

function BurdJournals.Server.limitedLootClaimModeActive(journal)
    return journal
        and BurdJournals.isLimitedClaimLootJournalActive
        and BurdJournals.isLimitedClaimLootJournalActive(journal)
end

function BurdJournals.Server.buildLimitedLootClaimPayload(journalData, player, journalId)
    local payload = {
        journalId = journalId,
        message = (getText and getText("UI_BurdJournals_LimitedLootClaimsSpent"))
            or "This journal has no claims left."
    }

    if attachRuntimeDeltaOrLegacyJournalData then
        return attachRuntimeDeltaOrLegacyJournalData(payload, journalData, player, "limitedLootClaimBlocked")
    end

    return payload
end

function BurdJournals.Server.enforceLimitedLootClaimBudget(player, journal, journalData)
    if not BurdJournals.Server.limitedLootClaimModeActive(journal) then
        return false
    end

    local canTake = BurdJournals.canPlayerTakeLimitedLootClaim and BurdJournals.canPlayerTakeLimitedLootClaim(journal, player)
    if canTake then
        return false
    end

    BurdJournals.Server.sendToClient(player, "error", BurdJournals.Server.buildLimitedLootClaimPayload(
        journalData,
        player,
        journal and journal.getID and journal:getID() or nil
    ))
    return true
end

function BurdJournals.Server.resolveAndRemoveTraitConflicts(player, traitId, opts)
    local cancelledTraits = {}

    local allowCancellation = BurdJournals.getSandboxOption("AllowMutualExclusionCancellation")
    if allowCancellation == nil then
        allowCancellation = true
    end
    if not allowCancellation then
        return cancelledTraits
    end

    local conflicts = removeTraitConflictsForCursedAdd(player, traitId, opts)
    for _, conflictId in ipairs(conflicts) do
        cancelledTraits[#cancelledTraits + 1] = conflictId
        BurdJournals.debugPrint("[BurdJournals] Cancelled conflicting trait: " .. tostring(conflictId))
    end

    return cancelledTraits
end

function BurdJournals.Server.handleClaimTrait(player, args)

    if not args or not args.traitId then
        BurdJournals.Server.sendToClient(player, "error", {message = "Invalid request."})
        return {
            traitId = args and args.traitId or nil,
            completed = false,
            claimed = false,
        }
    end

    local journalId = args.journalId
    local traitId = args.traitId
    local journal, resolvedJournalId, resolvedJournalUUID = resolveServerCommandJournal(player, args, "claimTrait")
    if not journal then
        BurdJournals.Server.sendToClient(player, "error", {message = "Journal not found."})
        return {
            traitId = traitId,
            completed = false,
            claimed = false,
        }
    end
    journalId, resolvedJournalUUID = getResolvedJournalIdentity(journal, resolvedJournalId, resolvedJournalUUID, journalId)
    if not maybeMigrateRuntimeOnTouch(journal, player, "claimTrait") then
        return {
            traitId = traitId,
            completed = false,
            claimed = false,
        }
    end
    if not enforceJournalLightRequirement(player, "claimTrait") then
        return {
            traitId = traitId,
            completed = false,
            claimed = false,
        }
    end

    local canClaim, reason = BurdJournals.canPlayerClaimFromJournal(player, journal)
    if not canClaim then
        BurdJournals.Server.sendToClient(player, "error", {message = reason or "Permission denied."})
        return {
            traitId = traitId,
            completed = false,
            claimed = false,
        }
    end

    local modData = journal:getModData()
    local journalData = modData.BurdJournals
    if BurdJournals.Server.preferEntryStoreDataForEmptyShell then
        journalData = BurdJournals.Server.preferEntryStoreDataForEmptyShell(journalData, args, "claimTraitEmptyShell")
    end
    if isHiddenCursedJournalState(journal, journalData) then
        BurdJournals.Server.sendToClient(player, "error", {message = BurdJournals.Server.getHiddenCursedClaimBlockedMessage()})
        return {
            traitId = traitId,
            completed = false,
            claimed = false,
        }
    end

    if not journalData or not journalData.traits then
        BurdJournals.Server.sendToClient(player, "error", {message = "This journal has no trait data."})
        return {
            traitId = traitId,
            completed = false,
            claimed = false,
        }
    end
    normalizeFoundJournalClaimFlags(journal, journalData, "handleClaimTrait")
    local limitedClaimMode = BurdJournals.Server.limitedLootClaimModeActive(journal)
    if BurdJournals.Server.enforceLimitedLootClaimBudget(player, journal, journalData) then
        return {
            traitId = traitId,
            completed = false,
            claimed = false,
        }
    end

    -- Check if traits are enabled for this journal type
    if not BurdJournals.isTraitEnabledForJournal(journalData, traitId) then
        BurdJournals.Server.sendToClient(player, "error", {message = "Trait claiming is disabled for this journal type."})
        return {
            traitId = traitId,
            completed = false,
            claimed = false,
        }
    end

    if (not journalData.traits[traitId]) and BurdJournals.Server.preferEntryStoreDataForMissingEntry then
        journalData = BurdJournals.Server.preferEntryStoreDataForMissingEntry(journalData, args, "traits", traitId, "claimTraitMissingEntry")
    end

    if not journalData.traits[traitId] then
        bsjWriteLogLine("[BurdJournals] Server ERROR: Trait '" .. traitId .. "' not found in journal")
        BurdJournals.Server.sendToClient(player, "error", {message = "Trait not found in journal.", errorCode = "entryNotFound"})
        return {
            traitId = traitId,
            completed = false,
            claimed = false,
        }
    end

    local isPlayerJournalTraitClaim = journalData.isPlayerCreated == true
        or (BurdJournals.isEffectivelyPlayerJournal and BurdJournals.isEffectivelyPlayerJournal(journalData))
    local playerAlreadyHasTrait = BurdJournals.playerHasTrait(player, traitId)

    if BurdJournals.hasCharacterClaimedTrait(journalData, player, traitId)
        and not (isPlayerJournalTraitClaim and not playerAlreadyHasTrait) then
        BurdJournals.Server.sendToClient(player, "error", {message = "This trait has already been claimed."})
        return {
            traitId = traitId,
            completed = true,
            claimed = false,
            alreadyClaimed = true,
        }
    end

    if playerAlreadyHasTrait then
        -- In limited-claim mode, already-owned rewards never consume the journal.
        if not limitedClaimMode then
            BurdJournals.markTraitClaimedByCharacter(journalData, player, traitId)
        end
        if journal.transmitModData and not limitedClaimMode and not BurdJournals.Server.shouldDeferBatchTransmit(args) then
            safeTransmitServerJournalModData(journal, "claimTrait")
        end
        BurdJournals.Server.sendToClient(player, "traitAlreadyKnown", attachRuntimeDeltaOrLegacyJournalData({
            traitId = traitId,
            journalId = journalId,
        }, journalData, player, "claimTraitAlreadyKnown"))
        -- Re-fetch journal by ID before calling shouldDissolve to avoid zombie object errors
        if not limitedClaimMode then
            local freshJournal = BurdJournals.Server.resolvePostActionJournal(player, journalId, journal)
            if freshJournal and BurdJournals.isValidItem(freshJournal) and BurdJournals.shouldDissolve and BurdJournals.shouldDissolve(freshJournal, player) then
                local dissolutionMessage = BurdJournals.getRandomDissolutionMessage()
                if BurdJournals.Server.dissolveJournal(player, freshJournal) then
                    BurdJournals.Server.sendToClient(player, "journalDissolved", {
                        message = dissolutionMessage,
                        journalId = journalId,
                        journalUUID = resolvedJournalUUID,
                    })
                    return {
                        traitId = traitId,
                        completed = true,
                        claimed = false,
                        alreadyKnown = true,
                        dissolved = true,
                    }
                end
            end
        end
        return {
            traitId = traitId,
            completed = true,
            claimed = false,
            alreadyKnown = true,
        }
    end

    local traitWasAdded = BurdJournals.safeAddTrait(player, traitId)

    if traitWasAdded then
        local cancelledTraits = BurdJournals.Server.resolveAndRemoveTraitConflicts(player, traitId)

        BurdJournals.markTraitClaimedByCharacter(journalData, player, traitId)

        if journal.transmitModData and not BurdJournals.Server.shouldDeferBatchTransmit(args) then
            safeTransmitServerJournalModData(journal, "claimTrait")
        end

        BurdJournals.Server.sendToClient(player, "claimSuccess", attachRuntimeDeltaOrLegacyJournalData({
            traitId = traitId,
            journalId = journalId,
            cancelledTraits = cancelledTraits,
        }, journalData, player, "claimTraitSuccess"))
        -- Re-fetch journal by ID before calling shouldDissolve to avoid zombie object errors
        local freshJournal = BurdJournals.Server.resolvePostActionJournal(player, journalId, journal)
        BurdJournals.debugPrint("[BurdJournals] Server: Post-trait claim check - freshJournal=" .. tostring(freshJournal ~= nil) .. ", journalId=" .. tostring(journalId))
        if freshJournal then
            local isValid = BurdJournals.isValidItem(freshJournal)
            local hasShouldDissolve = BurdJournals.shouldDissolve ~= nil
            local shouldDis = hasShouldDissolve and BurdJournals.shouldDissolve(freshJournal, player)
            BurdJournals.debugPrint("[BurdJournals] Server: isValid=" .. tostring(isValid) .. ", hasShouldDissolve=" .. tostring(hasShouldDissolve) .. ", shouldDis=" .. tostring(shouldDis))
            if isValid and shouldDis then
                BurdJournals.debugPrint("[BurdJournals] Server: DISSOLVING JOURNAL after trait claim!")
                local dissolutionMessage = BurdJournals.getRandomDissolutionMessage()
                if BurdJournals.Server.dissolveJournal(player, freshJournal) then
                    BurdJournals.Server.sendToClient(player, "journalDissolved", {
                        message = dissolutionMessage,
                        journalId = journalId,
                        journalUUID = resolvedJournalUUID,
                    })
                    return {
                        traitId = traitId,
                        completed = true,
                        claimed = true,
                        dissolved = true,
                    }
                end
            end
        end
        return {
            traitId = traitId,
            completed = true,
            claimed = true,
        }
    else
        BurdJournals.Server.sendToClient(player, "error", {message = "Could not learn trait."})
        return {
            traitId = traitId,
            completed = false,
            claimed = false,
        }
    end
end

function BurdJournals.Server.handleClaimForgetSlot(player, args)
    if not args or not hasAnyJournalLookupArgs(args) or not args.traitId then
        BurdJournals.Server.sendToClient(player, "error", {message = "Invalid forget request."})
        return
    end

    local journalId = args.journalId
    local traitId = args.traitId

    local journal, resolvedJournalId, resolvedJournalUUID = resolveServerCommandJournal(player, args, "claimForgetSlot")
    if not journal then
        BurdJournals.Server.sendToClient(player, "error", {message = "Journal not found."})
        return
    end
    journalId, resolvedJournalUUID = getResolvedJournalIdentity(journal, resolvedJournalId, resolvedJournalUUID, journalId)
    if not maybeMigrateRuntimeOnTouch(journal, player, "claimForgetSlot") then
        return
    end
    if not enforceJournalLightRequirement(player, "claimForgetSlot") then
        return
    end

    local canClaim, reason = BurdJournals.canPlayerClaimFromJournal(player, journal)
    if not canClaim then
        BurdJournals.Server.sendToClient(player, "error", {message = reason or "Permission denied."})
        return
    end

    local modData = journal:getModData()
    local journalData = modData and modData.BurdJournals
    if isHiddenCursedJournalState(journal, journalData) then
        BurdJournals.Server.sendToClient(player, "error", {message = BurdJournals.Server.getHiddenCursedClaimBlockedMessage()})
        return
    end
    local forgetSlotCount = journalData and BurdJournals.getForgetSlotCount and BurdJournals.getForgetSlotCount(journalData) or 0
    if not journalData or forgetSlotCount < 1 then
        BurdJournals.Server.sendToClient(player, "error", {message = "This journal has no forget slot."})
        return
    end
    normalizeFoundJournalClaimFlags(journal, journalData, "handleClaimForgetSlot")
    if BurdJournals.Server.enforceLimitedLootClaimBudget(player, journal, journalData) then
        return
    end

    if not BurdJournals.isForgetSlotEnabledForJournal or not BurdJournals.isForgetSlotEnabledForJournal(journalData) then
        BurdJournals.Server.sendToClient(player, "error", {message = "Forget slots are disabled for this journal type."})
        return
    end

    if BurdJournals.hasCharacterClaimedForgetSlot and BurdJournals.hasCharacterClaimedForgetSlot(journalData, player) then
        BurdJournals.Server.sendToClient(player, "error", {message = "Forget slot already used."})
        return
    end

    if not (BurdJournals.isTraitRemovable and BurdJournals.isTraitRemovable(traitId)) then
        BurdJournals.Server.sendToClient(player, "error", {message = "That trait cannot be forgotten."})
        return
    end

    if not BurdJournals.playerHasTrait(player, traitId) then
        BurdJournals.Server.sendToClient(player, "error", {message = "You do not have that trait."})
        return
    end

    local removed = removeTraitAuthoritatively(player, traitId)
    if not removed then
        BurdJournals.Server.sendToClient(player, "error", {message = "Could not forget trait."})
        return
    end

    if BurdJournals.markForgetSlotClaimedByCharacter then
        BurdJournals.markForgetSlotClaimedByCharacter(journalData, player, traitId)
    end

    if journal.transmitModData and not BurdJournals.Server.shouldDeferBatchTransmit(args) then
            safeTransmitServerJournalModData(journal, "claimForgetSlot")
    end

    BurdJournals.Server.sendToClient(player, "forgetSlotClaimed", attachRuntimeDeltaOrLegacyJournalData({
        journalId = journalId,
        traitId = traitId,
    }, journalData, player, "claimForgetSlotSuccess"))

    -- Check for dissolution after forget-slot claim.
    local freshJournal = BurdJournals.Server.resolvePostActionJournal(player, journalId, journal)
    BurdJournals.debugPrint("[BurdJournals] Server: Post-forget claim check - freshJournal=" .. tostring(freshJournal ~= nil) .. ", journalId=" .. tostring(journalId))
    if freshJournal then
        local isValid = BurdJournals.isValidItem(freshJournal)
        local hasShouldDissolve = BurdJournals.shouldDissolve ~= nil
        local shouldDis = hasShouldDissolve and BurdJournals.shouldDissolve(freshJournal, player)
        BurdJournals.debugPrint("[BurdJournals] Server: forget check isValid=" .. tostring(isValid) .. ", hasShouldDissolve=" .. tostring(hasShouldDissolve) .. ", shouldDis=" .. tostring(shouldDis))
        if isValid and shouldDis then
            BurdJournals.debugPrint("[BurdJournals] Server: DISSOLVING JOURNAL after forget-slot claim!")
            if BurdJournals.Server.dissolveJournal(player, freshJournal) then
                BurdJournals.Server.sendToClient(player, "journalDissolved", {
                    message = BurdJournals.getRandomDissolutionMessage and BurdJournals.getRandomDissolutionMessage() or "The journal crumbles to dust...",
                    journalId = journalId,
                })
            end
        end
    end
end

-- Server handler for claiming stats (zombie kills, hours survived) from journals
function BurdJournals.Server.handleClaimStat(player, args)
    if not args or not args.statId then
        BurdJournals.Server.sendToClient(player, "error", {message = "Invalid request."})
        return false
    end

    local journalId = args.journalId
    local statId = args.statId
    local journal, resolvedJournalId, resolvedJournalUUID = resolveServerCommandJournal(player, args, "claimStat")
    if not journal then
        BurdJournals.Server.sendToClient(player, "error", {message = "Journal not found."})
        return false
    end
    journalId, resolvedJournalUUID = getResolvedJournalIdentity(journal, resolvedJournalId, resolvedJournalUUID, journalId)
    if not maybeMigrateRuntimeOnTouch(journal, player, "claimStat") then
        return false
    end
    if not enforceJournalLightRequirement(player, "claimStat") then
        return false
    end

    local canClaim, reason = BurdJournals.canPlayerClaimFromJournal(player, journal)
    if not canClaim then
        BurdJournals.Server.sendToClient(player, "error", {message = reason or "Permission denied."})
        return false
    end

    local modData = journal:getModData()
    local journalData = modData.BurdJournals
    if BurdJournals.Server.preferEntryStoreDataForEmptyShell then
        journalData = BurdJournals.Server.preferEntryStoreDataForEmptyShell(journalData, args, "claimStatEmptyShell")
    end
    if isHiddenCursedJournalState(journal, journalData) then
        BurdJournals.Server.sendToClient(player, "error", {message = BurdJournals.Server.getHiddenCursedClaimBlockedMessage()})
        return false
    end

    if not journalData or not journalData.stats then
        BurdJournals.Server.sendToClient(player, "error", {message = "This journal has no stat data."})
        return false
    end
    normalizeFoundJournalClaimFlags(journal, journalData, "handleClaimStat")
    if BurdJournals.Server.enforceLimitedLootClaimBudget(player, journal, journalData) then
        return false
    end

    -- Validate that this stat can be absorbed
    if not BurdJournals.canAbsorbStat then
        BurdJournals.Server.sendToClient(player, "error", {message = "Stat absorption not available."})
        return false
    end

    if (not journalData.stats[statId]) and BurdJournals.Server.preferEntryStoreDataForMissingEntry then
        journalData = BurdJournals.Server.preferEntryStoreDataForMissingEntry(journalData, args, "stats", statId, "claimStatMissingEntry")
    end

    local canAbsorb, recordedValue, previousValue, absorbReason = BurdJournals.canAbsorbStat(journalData, player, statId)
    if not canAbsorb then
        -- Convert reason codes to user-friendly messages
        local message = "Cannot absorb this stat."
        local errorCode = "statAbsorbRejected"
        if absorbReason == "not_absorbable" then
            message = "This stat cannot be absorbed."
            errorCode = "statNotAbsorbable"
        elseif absorbReason == "already_claimed" then
            message = "Already claimed from this journal."
            errorCode = "statAlreadyClaimed"
        elseif absorbReason == "no_benefit" then
            message = "Your current value is already higher or equal."
            errorCode = "statNoBenefit"
        end
        BurdJournals.Server.sendToClient(player, "error", {message = message, errorCode = errorCode})
        return false
    end

    -- Server-authoritative value: always apply the recorded journal value.
    -- Never trust client-provided args.value for stat application.
    local statApplied = BurdJournals.applyStatAbsorption(player, statId, recordedValue)

    if statApplied then
        if statId == "hoursSurvived" and player.getModData then
            local playerModData = player:getModData()
            playerModData.BurdJournals = type(playerModData.BurdJournals) == "table" and playerModData.BurdJournals or {}
            local playerJournalData = playerModData.BurdJournals
            playerJournalData.restoredHoursSurvived = math.max(
                tonumber(playerJournalData.restoredHoursSurvived) or 0,
                tonumber(recordedValue) or 0
            )
            playerJournalData.hoursSurvivedBeforeRestore = tonumber(playerJournalData.hoursSurvivedBeforeRestore)
                or tonumber(previousValue)
                or 0
            if player.transmitModData then
                player:transmitModData()
            end
        end
        -- Mark the stat as claimed
        BurdJournals.markStatClaimedByCharacter(journalData, player, statId)

        if journal.transmitModData and not BurdJournals.Server.shouldDeferBatchTransmit(args) then
            safeTransmitServerJournalModData(journal, "claimStat")
        end

        BurdJournals.Server.sendToClient(player, "claimSuccess", attachRuntimeDeltaOrLegacyJournalData({
            statId = statId,
            journalId = journalId,
            value = recordedValue,
        }, journalData, player, "claimStatSuccess"))

        -- Check for dissolution after claiming
        local freshJournal = BurdJournals.Server.resolvePostActionJournal(player, journalId, journal)
        BurdJournals.debugPrint("[BurdJournals] Server: Post-stat claim check - freshJournal=" .. tostring(freshJournal ~= nil) .. ", journalId=" .. tostring(journalId))
        if freshJournal then
            local isValid = BurdJournals.isValidItem(freshJournal)
            local hasShouldDissolve = BurdJournals.shouldDissolve ~= nil
            local shouldDis = hasShouldDissolve and BurdJournals.shouldDissolve(freshJournal, player)
            BurdJournals.debugPrint("[BurdJournals] Server: isValid=" .. tostring(isValid) .. ", hasShouldDissolve=" .. tostring(hasShouldDissolve) .. ", shouldDis=" .. tostring(shouldDis))
            if isValid and shouldDis then
                BurdJournals.debugPrint("[BurdJournals] Server: DISSOLVING JOURNAL after stat claim!")
                BurdJournals.Server.dissolveJournal(player, freshJournal)
            end
        end
        return { completed = true, statId = statId, value = recordedValue }
    else
        BurdJournals.Server.sendToClient(player, "error", {message = "Could not apply stat."})
        return false
    end
end

function BurdJournals.Server.handleAbsorbSkill(player, args)
    if not args or not args.skillName then

        BurdJournals.Server.sendToClient(player, "error", {message = "Invalid request."})
        return
    end

    local journalId = args.journalId
    local skillName = args.skillName

    local journal, resolvedJournalId, resolvedJournalUUID = resolveServerCommandJournal(player, args, "absorbSkill")

    if not journal then
        BurdJournals.Server.sendToClient(player, "error", {message = "Journal not found."})
        return
    end
    journalId, resolvedJournalUUID = getResolvedJournalIdentity(journal, resolvedJournalId, resolvedJournalUUID, journalId)
    if not maybeMigrateRuntimeOnTouch(journal, player, "absorbSkill") then
        return
    end
    if not enforceJournalLightRequirement(player, "absorbSkill") then
        return
    end

    if not BurdJournals.canAbsorbXP(journal) then
        BurdJournals.Server.sendToClient(player, "error", {message = "Cannot absorb from this journal."})
        return
    end

    local modData = journal:getModData()

    if modData and BurdJournals.isDebug() then
        for k, v in pairs(modData) do
            BurdJournals.debugPrint("  - " .. tostring(k) .. " = " .. type(v))
        end
    end

    local journalData = modData.BurdJournals

    if not journalData then
        BurdJournals.Server.sendToClient(player, "error", {message = "This journal has no data."})
        return
    end
    normalizeFoundJournalClaimFlags(journal, journalData, "handleAbsorbSkill")
    local limitedClaimMode = BurdJournals.Server.limitedLootClaimModeActive(journal)
    if BurdJournals.Server.enforceLimitedLootClaimBudget(player, journal, journalData) then
        return
    end

    if BurdJournals.isDebug() then
        for k, v in pairs(journalData) do
            local valueStr = tostring(v)
            if type(v) == "table" then
                valueStr = "table with " .. BurdJournals.countTable(v) .. " entries"
            end
            BurdJournals.debugPrint("  - " .. tostring(k) .. " = " .. valueStr)
        end
    end

    if not journalData.skills then
        BurdJournals.Server.sendToClient(player, "error", {message = "This journal has no skill data."})
        return
    end

    local storedSkillName = BurdJournals.resolveSkillKey and BurdJournals.resolveSkillKey(journalData.skills, skillName) or skillName
    if journalData.skills[storedSkillName] then
        skillName = storedSkillName
    end

    if BurdJournals.isSkillEnabledForJournal and not BurdJournals.isSkillEnabledForJournal(journalData, skillName) then
        BurdJournals.Server.sendToClient(player, "error", {message = "That skill is disabled by sandbox settings for this journal."})
        return
    end

    local skillCount = BurdJournals.countTable(journalData.skills)

    if BurdJournals.isDebug() then
        for skillKey, skillVal in pairs(journalData.skills) do
            if type(skillVal) == "table" then
                BurdJournals.debugPrint("  - '" .. tostring(skillKey) .. "': xp=" .. tostring(skillVal.xp) .. ", level=" .. tostring(skillVal.level))
            else
                BurdJournals.debugPrint("  - '" .. tostring(skillKey) .. "': INVALID (not a table, is " .. type(skillVal) .. ")")
            end
        end
    end

    if not journalData.skills[skillName] then
        bsjWriteLogLine("[BurdJournals] Server ERROR: Skill '" .. tostring(skillName) .. "' not found in journal!")

        if BurdJournals.isDebug() then
            for k, _ in pairs(journalData.skills) do
                BurdJournals.debugPrint("  - '" .. tostring(k) .. "'")
            end
        end
        BurdJournals.Server.sendToClient(player, "error", {message = "Skill not found in journal.", errorCode = "entryNotFound"})
        return
    end

    if BurdJournals.hasCharacterClaimedSkill(journalData, player, skillName) then
        BurdJournals.Server.sendToClient(player, "error", {message = "This skill has already been claimed."})
        return {skillName = skillName, completed = false, alreadyClaimed = true}
    end

    local skillData = journalData.skills[skillName]

    if type(skillData) ~= "table" then
        bsjWriteLogLine("[BurdJournals] Server ERROR: skillData is not a table! It's: " .. type(skillData) .. " = " .. tostring(skillData))
        BurdJournals.Server.sendToClient(player, "error", {message = "Invalid skill data."})
        return
    end

    local baseXP = skillData.xp

    if baseXP == nil then
        if BurdJournals.isDebug() then
            for k, v in pairs(skillData) do
                BurdJournals.debugPrint("  - " .. tostring(k) .. " = " .. tostring(v))
            end
        end
        baseXP = 0
    end

    if type(baseXP) ~= "number" then

        baseXP = tonumber(baseXP) or 0
    end

    local journalMultiplier = tonumber(BurdJournals.getSandboxOption("JournalXPMultiplier")) or 1.0
    if journalMultiplier < 0 then
        journalMultiplier = 0
    end

    -- Resolve skill-book multiplier server-side for correctness/security.
    -- Keep a client multiplier fallback only if server cannot detect a boost.
    local skillBookMultiplier = 1.0
    local cap = tonumber(BurdJournals.getSandboxOption("SkillBookMultiplierCap")) or 2.0
    if cap < 1.0 then cap = 1.0 end
    local featureEnabled = BurdJournals.getSandboxOption("SkillBookMultiplierForJournals")
    local clientReportedMultiplier = tonumber(args and args.skillBookMultiplier)

    if featureEnabled
        and BurdJournals.getSkillBookMultiplier
        and BurdJournals.shouldApplySkillBookMultiplierForJournal(journalData) then
        local serverMultiplier, serverHasBoost = BurdJournals.getSkillBookMultiplier(player, skillName)
        serverMultiplier = tonumber(serverMultiplier) or 1.0
        serverMultiplier = math.max(1.0, math.min(serverMultiplier, cap))

        if serverHasBoost and serverMultiplier > 1.0 then
            skillBookMultiplier = serverMultiplier
        elseif clientReportedMultiplier and clientReportedMultiplier > 1.0 then
            -- Fallback for edge cases where server-side multiplier is unavailable.
            skillBookMultiplier = math.max(1.0, math.min(clientReportedMultiplier, cap))
        end
    end
    BurdJournals.debugPrint("[BurdJournals] Server: absorb skill multipliers - journal=" .. tostring(journalMultiplier) .. ", book=" .. tostring(skillBookMultiplier) .. ", clientReported=" .. tostring(clientReportedMultiplier))
    
    local xpToAdd = baseXP * journalMultiplier * skillBookMultiplier
    BurdJournals.debugPrint("[BurdJournals] Server: baseXP=" .. tostring(baseXP) .. ", journalMult=" .. tostring(journalMultiplier) .. ", bookMult=" .. tostring(skillBookMultiplier) .. ", xpToAdd=" .. tostring(xpToAdd))

    -- Fitness and Strength use different XP scaling in PZ.
    -- Compensate so journal rewards match configured values.
    local isPassiveSkill = (skillName == "Fitness" or skillName == "Strength")
    if isPassiveSkill then
        xpToAdd = xpToAdd * 5
        BurdJournals.debugPrint("[BurdJournals] Server: Applied 5x passive skill multiplier for " .. skillName .. ", new xpToAdd: " .. tostring(xpToAdd))
    end

    -- AddXP adds the specified amount directly for all skills
    local perk = BurdJournals.getPerkByName(skillName)

    if not perk then

        perk = Perks[skillName]

        if not perk and BurdJournals.SKILL_TO_PERK and BurdJournals.SKILL_TO_PERK[skillName] then
            local mappedName = BurdJournals.SKILL_TO_PERK[skillName]

            perk = Perks[mappedName]
        end
    end

    if not perk then
        bsjWriteLogLine("[BurdJournals] Server ERROR: Could not find perk for skill '" .. skillName .. "'")
        BurdJournals.Server.sendToClient(player, "error", {message = "Invalid skill: " .. skillName})
        return
    end

    if xpToAdd > 0 then
        local totalAfterAbsorb = nil

        BurdJournals.markSkillClaimedByCharacter(journalData, player, skillName)

        -- Re-fetch journal by ID before calling shouldDissolve to avoid zombie object errors
        local freshJournal = BurdJournals.Server.resolvePostActionJournal(player, journalId, journal)
        local shouldDis = false
        if freshJournal and BurdJournals.isValidItem(freshJournal) then
            shouldDis = BurdJournals.shouldDissolve(freshJournal, player)
        end

        local applied, appliedVia = BurdJournals.Server.applyXPWithFallback(player, perk, xpToAdd, {
            skillName = skillName,
            useMultipliers = false,
            preferNativeNoMultiplier = true,
        })
        if applied then
            totalAfterAbsorb = BurdJournals.getPlayerSkillTotalXP and BurdJournals.getPlayerSkillTotalXP(player, perk, skillName) or player:getXp():getXP(perk)
            BurdJournals.debugPrint("[BurdJournals] Server: Absorb - Applied " .. tostring(xpToAdd)
                .. " XP to " .. skillName .. " via " .. tostring(appliedVia))
            -- Absorb XP reaches the client only through this sync (the batch payload
            -- applies completedSkillXP for claim mode only), so it must not be gated.
            BurdJournals.Server.sendExactSkillXPSync(player, skillName, totalAfterAbsorb)
            -- NOTE: Don't call syncXp here - it disrupts batch command processing
            -- Client will request sync at end of batch via requestXpSync command
        else
            -- Last resort fallback for SP/edge cases
            BurdJournals.debugPrint("[BurdJournals] Server: Absorb fallback - sending applyXP to client for " .. skillName)
            BurdJournals.Server.sendToClient(player, "applyXP", {
                skills = {
                    [skillName] = {
                        xp = xpToAdd,
                        mode = "add"
                    }
                },
                mode = "add"
            })
        end

        if shouldDis and freshJournal then
            local dissolutionMessage = BurdJournals.getRandomDissolutionMessage()
            if BurdJournals.Server.dissolveJournal(player, freshJournal) then
                BurdJournals.Server.sendToClient(player, "absorbSuccess", attachRuntimeDeltaOrLegacyJournalData({
                    skillName = skillName,
                    xpGained = xpToAdd,
                    xpAfterTotal = totalAfterAbsorb,
                    remaining = 0,
                    total = 0,
                    journalId = journalId,
                    dissolved = true,
                    dissolutionMessage = dissolutionMessage,
                    -- Debug info
                    debug_baseXP = baseXP,
                    debug_journalMult = journalMultiplier,
                    debug_bookMult = skillBookMultiplier,
                    debug_receivedMult = clientReportedMultiplier,
                }, journalData, player, "absorbSkillDissolved"))
                return {skillName = skillName, completed = true, claimed = true, dissolved = true, xpGained = xpToAdd}
            end
        end

            if freshJournal and freshJournal.transmitModData and not BurdJournals.Server.shouldDeferBatchTransmit(args) then
                safeTransmitServerJournalModData(freshJournal, "absorbSkill")
            end

            -- Use per-character unclaimed counts (use freshJournal if available)
            local jnl = freshJournal or journal
            local remainingRewards = 0
            local totalRewards = 0
            if jnl then
                remainingRewards = BurdJournals.getUnclaimedSkillCount(jnl, player) +
                                   BurdJournals.getUnclaimedTraitCount(jnl, player) +
                                   BurdJournals.getUnclaimedRecipeCount(jnl, player)
                totalRewards = BurdJournals.getTotalRewards(jnl)
            end
            BurdJournals.Server.sendToClient(player, "absorbSuccess", attachRuntimeDeltaOrLegacyJournalData({
                skillName = skillName,
                xpGained = xpToAdd,
                xpAfterTotal = totalAfterAbsorb,
                remaining = remainingRewards,
                total = totalRewards,
                journalId = journalId,
                -- Debug info
                debug_baseXP = baseXP,
                debug_journalMult = journalMultiplier,
                debug_bookMult = skillBookMultiplier,
                debug_receivedMult = clientReportedMultiplier,
                }, journalData, player, "absorbSkillSuccess"))
            return {skillName = skillName, completed = true, claimed = true, xpGained = xpToAdd}
    else
        -- Still mark as claimed even if no XP to add (allows journal dissolution)
        if not limitedClaimMode then
            BurdJournals.markSkillClaimedByCharacter(journalData, player, skillName)
        end

        -- Re-fetch journal by ID to avoid zombie object errors
        local freshJournal = BurdJournals.Server.resolvePostActionJournal(player, journalId, journal)
        if freshJournal and freshJournal.transmitModData and not BurdJournals.Server.shouldDeferBatchTransmit(args) then
                safeTransmitServerJournalModData(freshJournal, "absorbSkill")
        end
        BurdJournals.Server.sendToClient(player, "skillMaxed", attachRuntimeDeltaOrLegacyJournalData({
            skillName = skillName,
            journalId = journalId,
        }, journalData, player, "absorbSkillMaxed"))
        -- Check if journal should dissolve after marking this claim
        if freshJournal and BurdJournals.isValidItem(freshJournal) and BurdJournals.shouldDissolve and BurdJournals.shouldDissolve(freshJournal, player) then
            BurdJournals.Server.dissolveJournal(player, freshJournal)
        end
        return {skillName = skillName, completed = true, claimed = true, xpGained = 0}
    end

end

function BurdJournals.Server.handleAbsorbTrait(player, args)
    if not args or not args.traitId then
        BurdJournals.Server.sendToClient(player, "error", {message = "Invalid request."})
        return
    end

    local journalId = args.journalId
    local traitId = args.traitId

    local journal, resolvedJournalId, resolvedJournalUUID = resolveServerCommandJournal(player, args, "absorbTrait")
    if not journal then
        BurdJournals.Server.sendToClient(player, "error", {message = "Journal not found."})
        return
    end
    journalId, resolvedJournalUUID = getResolvedJournalIdentity(journal, resolvedJournalId, resolvedJournalUUID, journalId)
    if not maybeMigrateRuntimeOnTouch(journal, player, "absorbTrait") then
        return
    end
    if not enforceJournalLightRequirement(player, "absorbTrait") then
        return
    end

    if not BurdJournals.canAbsorbXP(journal) then
        BurdJournals.Server.sendToClient(player, "error", {message = "Cannot absorb from this journal."})
        return
    end

    local modData = journal:getModData()
    local journalData = modData.BurdJournals
    if not journalData or not journalData.traits then
        BurdJournals.Server.sendToClient(player, "error", {message = "This journal has no trait data."})
        return
    end
    normalizeFoundJournalClaimFlags(journal, journalData, "handleAbsorbTrait")
    local limitedClaimMode = BurdJournals.Server.limitedLootClaimModeActive(journal)
    if BurdJournals.Server.enforceLimitedLootClaimBudget(player, journal, journalData) then
        return
    end

    if not BurdJournals.isTraitEnabledForJournal(journalData, traitId) then
        BurdJournals.Server.sendToClient(player, "error", {message = "Trait absorbing is disabled for this journal type."})
        return
    end

    if not journalData.traits[traitId] then
        BurdJournals.Server.sendToClient(player, "error", {message = "Trait not found in journal.", errorCode = "entryNotFound"})
        return
    end

    if BurdJournals.hasCharacterClaimedTrait(journalData, player, traitId) then
        BurdJournals.Server.sendToClient(player, "error", {message = "This trait has already been claimed."})
        return {traitId = traitId, completed = false, alreadyClaimed = true}
    end

    if BurdJournals.playerHasTrait(player, traitId) then
        if not limitedClaimMode then
            BurdJournals.markTraitClaimedByCharacter(journalData, player, traitId)

            local freshJournal = BurdJournals.Server.resolvePostActionJournal(player, journalId, journal)
            if freshJournal and freshJournal.transmitModData and not BurdJournals.Server.shouldDeferBatchTransmit(args) then
                safeTransmitServerJournalModData(freshJournal, "absorbTrait")
            end

            if BurdJournals.Server.safeShouldDissolve(player, journalId) then
                local jnl = BurdJournals.Server.resolvePostActionJournal(player, journalId, nil)
                if jnl then
                    if BurdJournals.Server.dissolveJournal(player, jnl) then
                        BurdJournals.Server.sendToClient(player, "journalDissolved", {
                            message = BurdJournals.getRandomDissolutionMessage(),
                            journalId = journalId,
                        })
                    end
                end
            end
        end

        BurdJournals.Server.sendToClient(player, "traitAlreadyKnown", attachRuntimeDeltaOrLegacyJournalData({
            traitId = traitId,
            journalId = journalId,
        }, journalData, player, "absorbTraitAlreadyKnown"))
        return {traitId = traitId, completed = true, claimed = true, alreadyKnown = true}
    end

    local traitWasAdded = BurdJournals.safeAddTrait and BurdJournals.safeAddTrait(player, traitId)
    local hasAfter = BurdJournals.playerHasTrait and BurdJournals.playerHasTrait(player, traitId) == true
    if not traitWasAdded or not hasAfter then
        bsjWriteLogLine("[BurdJournals] Server: ERROR - Trait add verification failed for '" .. tostring(traitId) .. "'")
        BurdJournals.Server.sendToClient(player, "error", {message = "Could not learn trait."})
        return
    end

    local cancelledTraits = BurdJournals.Server.resolveAndRemoveTraitConflicts(player, traitId)

    BurdJournals.markTraitClaimedByCharacter(journalData, player, traitId)

    local freshJournal = BurdJournals.Server.resolvePostActionJournal(player, journalId, journal)
    local shouldDis = false
    if freshJournal and BurdJournals.isValidItem(freshJournal) then
        shouldDis = BurdJournals.shouldDissolve(freshJournal, player)
    end

    if shouldDis and freshJournal then
        local dissolutionMessage = BurdJournals.getRandomDissolutionMessage()
        if BurdJournals.Server.dissolveJournal(player, freshJournal) then
            BurdJournals.Server.sendToClient(player, "absorbSuccess", attachRuntimeDeltaOrLegacyJournalData({
                traitId = traitId,
                remaining = 0,
                total = 0,
                journalId = journalId,
                cancelledTraits = cancelledTraits,
                dissolved = true,
                dissolutionMessage = dissolutionMessage,
            }, journalData, player, "absorbTraitDissolved"))
            return {traitId = traitId, completed = true, claimed = true, dissolved = true}
        end
    end

    if freshJournal and freshJournal.transmitModData and not BurdJournals.Server.shouldDeferBatchTransmit(args) then
                safeTransmitServerJournalModData(freshJournal, "absorbTrait")
    end

    local jnl = freshJournal or journal
    local remainingRewards = 0
    local totalRewards = 0
    if jnl then
        remainingRewards = BurdJournals.getUnclaimedSkillCount(jnl, player) +
                           BurdJournals.getUnclaimedTraitCount(jnl, player) +
                           BurdJournals.getUnclaimedRecipeCount(jnl, player)
        totalRewards = BurdJournals.getTotalRewards(jnl)
    end

    BurdJournals.Server.sendToClient(player, "absorbSuccess", attachRuntimeDeltaOrLegacyJournalData({
        traitId = traitId,
        remaining = remainingRewards,
        total = totalRewards,
        journalId = journalId,
        cancelledTraits = cancelledTraits,
    }, journalData, player, "absorbTraitSuccess"))
    return {traitId = traitId, completed = true, claimed = true}
end

function BurdJournals.Server.handleEraseJournal(player, args)
    if not args or not hasAnyJournalLookupArgs(args) then
        BurdJournals.Server.sendToClient(player, "error", {message = "Invalid request."})
        return
    end

    local journal = resolveServerCommandJournal(player, args, "eraseJournal")
    if not journal then
        BurdJournals.Server.sendToClient(player, "error", {message = "Journal not found."})
        return
    end
    if not maybeMigrateRuntimeOnTouch(journal, player, "eraseJournal") then
        return
    end
    local canMutateJournal, _, mutationMessage = BurdJournals.Server.validatePlayerJournalMutation(player, journal, {
        allowBlank = false,
    })
    if not canMutateJournal then
        BurdJournals.Server.sendToClient(player, "error", {message = mutationMessage})
        return
    end

    if BurdJournals.getSandboxOption("RequireEraserToErase") then
        local eraser = BurdJournals.findEraser(player)
        if not eraser then
            BurdJournals.Server.sendToClient(player, "error", {message = "You need an eraser to wipe the journal."})
            return
        end
    end

    local sourceJournalData = nil
    do
        local sourceModData = journal:getModData()
        sourceJournalData = sourceModData and sourceModData.BurdJournals or nil
    end

    local drCarryForward = extractJournalDRCarryForward(sourceJournalData, player)

    local inheritedWasFromBloody = false
    local inheritedWasCleaned = false
    local inheritedRestoredBy = player and player:getUsername() or "Unknown"
    if type(sourceJournalData) == "table" then
        inheritedWasFromBloody = sourceJournalData.wasFromBloody == true or sourceJournalData.isBloody == true
        inheritedWasCleaned = sourceJournalData.wasCleaned == true
        if type(sourceJournalData.restoredBy) == "string" and sourceJournalData.restoredBy ~= "" then
            inheritedRestoredBy = sourceJournalData.restoredBy
        end
    end

    local inventory = player:getInventory()
    local blankJournal = addJournalItemToContainerCompat(inventory, "BurdJournals.BlankSurvivalJournal")
    if not blankJournal then
        BurdJournals.Server.sendToClient(player, "error", {message = "Could not create a replacement blank journal."})
        return
    end

    local modData = blankJournal:getModData()
    local inheritedMigrationSchemaVersion = drCarryForward and tonumber(drCarryForward.migrationSchemaVersion) or 0
    local baseMigrationSchemaVersion = tonumber(BurdJournals.MIGRATION_SCHEMA_VERSION) or 0
    local seedReadCount = drCarryForward and drCarryForward.readCount or 0
    local seedReadSessionCount = drCarryForward and drCarryForward.readSessionCount or 0
    local seedCurrentSessionReadCount = drCarryForward and drCarryForward.currentSessionReadCount or 0
    local seedSkillReadCounts = drCarryForward and drCarryForward.skillReadCounts or {}
    modData.BurdJournals = {
        isWorn = false,
        isBloody = false,
        isPlayerCreated = true,
        readCount = seedReadCount,
        readSessionCount = seedReadSessionCount,
        currentSessionReadCount = seedCurrentSessionReadCount,
        skillReadCounts = seedSkillReadCounts,
        migrationSchemaVersion = math.max(baseMigrationSchemaVersion, inheritedMigrationSchemaVersion),
    }
    if drCarryForward and type(drCarryForward.currentSessionId) == "string" and drCarryForward.currentSessionId ~= "" then
        modData.BurdJournals.currentSessionId = drCarryForward.currentSessionId
    end
    if drCarryForward and drCarryForward.drLegacyMode3Migrated == true then
        modData.BurdJournals.drLegacyMode3Migrated = true
    end
    BurdJournals.updateJournalName(blankJournal)
    BurdJournals.updateJournalIcon(blankJournal)

    local sourceUUID = type(sourceJournalData) == "table"
        and trimUUID(sourceJournalData.uuid or sourceJournalData.entryStoreUUID)
        or nil
    inventory:Remove(journal)
    if sendRemoveItemFromContainer then
        local replicated, replicationError = pcall(sendRemoveItemFromContainer, inventory, journal)
        if not replicated then
            bsjWriteLogLine("[BurdJournals] WARNING: erase journal removal replication failed: " .. tostring(replicationError))
        end
    end
    if sourceUUID and BurdJournals.Server.purgeJournalUUIDTracking then
        BurdJournals.Server.purgeJournalUUIDTracking(sourceUUID, {
            removeBackup = true,
            removeEntryStore = true,
            removeEntryFileBackup = true,
            entryStoreShardKey = type(sourceJournalData) == "table" and sourceJournalData.entryStoreShardKey or nil,
        })
    end

    if blankJournal.transmitModData then
        blankJournal:transmitModData()
        BurdJournals.debugPrint("[BurdJournals] Server: transmitModData called for blank journal in handleEraseJournal")
    end
    if sendAddItemToContainer then
        sendAddItemToContainer(inventory, blankJournal)
        BurdJournals.debugPrint("[BurdJournals] Server: sendAddItemToContainer called for blank journal in handleEraseJournal")
    end

    BurdJournals.Server.sendToClient(player, "eraseSuccess", {
        journalId = blankJournal.getID and blankJournal:getID() or nil,
    })
end

function BurdJournals.Server.handleCleanBloody(player, args)

    BurdJournals.Server.sendToClient(player, "error", {
        message = "Bloody journals can now be read directly. Right-click to open and absorb XP."
    })
end

function BurdJournals.Server.handleConvertToClean(player, args)
    if not args or not hasAnyJournalLookupArgs(args) then
        BurdJournals.Server.sendToClient(player, "error", {message = "Invalid request."})
        return
    end
    if BurdJournals.isPlayerJournalCraftingEnabled and not BurdJournals.isPlayerJournalCraftingEnabled() then
        BurdJournals.Server.sendToClient(player, "error", {
            message = (getText and getText("UI_BurdJournals_PlayerJournalCraftingDisabled"))
                or "Player journal crafting is disabled on this server."
        })
        return
    end

    local journal = resolveServerCommandJournal(player, args, "convertToClean")
    if not journal then
        BurdJournals.Server.sendToClient(player, "error", {message = "Journal not found."})
        return
    end

    if not BurdJournals.isWorn(journal) then
        BurdJournals.Server.sendToClient(player, "error", {message = "Only worn journals can be converted."})
        return
    end

    if not BurdJournals.canConvertToClean(player) then
        BurdJournals.Server.sendToClient(player, "error", {message = "You need leather, thread, needle, and Tailoring Lv1."})
        return
    end

    local leather = BurdJournals.findRepairItem(player, "leather")
    local thread = BurdJournals.findRepairItem(player, "thread")
    local needle = BurdJournals.findRepairItem(player, "needle")
    if not leather or not thread or not needle then
        BurdJournals.Server.sendToClient(player, "error", {message = "You need leather, thread, needle, and Tailoring Lv1."})
        return
    end

    local sourceJournalData = nil
    do
        local sourceModData = journal:getModData()
        sourceJournalData = sourceModData and sourceModData.BurdJournals or nil
    end
    local drCarryForward = extractJournalDRCarryForward(sourceJournalData, player)
    local inheritedWasFromBloody = type(sourceJournalData) == "table"
        and (sourceJournalData.wasFromBloody == true or sourceJournalData.isBloody == true)
        or false
    local inheritedWasCleaned = type(sourceJournalData) == "table" and sourceJournalData.wasCleaned == true or false
    local inheritedRestoredBy = type(sourceJournalData) == "table" and sourceJournalData.restoredBy or nil
    if type(inheritedRestoredBy) ~= "string" or inheritedRestoredBy == "" then
        inheritedRestoredBy = player and player:getUsername() or "Unknown"
    end
    local sourceUUID = type(sourceJournalData) == "table"
        and trimUUID(sourceJournalData.uuid or sourceJournalData.entryStoreUUID)
        or nil

    local inventory = player:getInventory()
    local cleanJournal = addJournalItemToContainerCompat(inventory, "BurdJournals.BlankSurvivalJournal")
    if not cleanJournal then
        BurdJournals.Server.sendToClient(player, "error", {message = "Could not create a replacement blank journal."})
        return
    end

    local modData = cleanJournal:getModData()
    if not modData then
        inventory:Remove(cleanJournal)
        BurdJournals.Server.sendToClient(player, "error", {message = "Could not create a replacement blank journal."})
        return
    end
    local inheritedMigrationSchemaVersion = drCarryForward and tonumber(drCarryForward.migrationSchemaVersion) or 0
    local baseMigrationSchemaVersion = tonumber(BurdJournals.MIGRATION_SCHEMA_VERSION) or 0
    local seedReadCount = drCarryForward and drCarryForward.readCount or 0
    local seedReadSessionCount = drCarryForward and drCarryForward.readSessionCount or 0
    local seedCurrentSessionReadCount = drCarryForward and drCarryForward.currentSessionReadCount or 0
    local seedSkillReadCounts = drCarryForward and drCarryForward.skillReadCounts or {}
    modData.BurdJournals = {
        uuid = (BurdJournals.generateUUID and BurdJournals.generateUUID())
            or ("journal-" .. tostring(getTimestampMs and getTimestampMs() or os.time()) .. "-" .. tostring(cleanJournal:getID())),
        isWorn = false,
        isBloody = false,
        wasFromWorn = true,
        wasFromBloody = inheritedWasFromBloody,
        wasRestored = true,
        wasCleaned = inheritedWasCleaned,
        restoredBy = inheritedRestoredBy,
        isPlayerCreated = true,
        readCount = seedReadCount,
        readSessionCount = seedReadSessionCount,
        currentSessionReadCount = seedCurrentSessionReadCount,
        skillReadCounts = seedSkillReadCounts,
        migrationSchemaVersion = math.max(baseMigrationSchemaVersion, inheritedMigrationSchemaVersion),
    }
    if drCarryForward and type(drCarryForward.currentSessionId) == "string" and drCarryForward.currentSessionId ~= "" then
        modData.BurdJournals.currentSessionId = drCarryForward.currentSessionId
    end
    if drCarryForward and drCarryForward.drLegacyMode3Migrated == true then
        modData.BurdJournals.drLegacyMode3Migrated = true
    end
    BurdJournals.updateJournalName(cleanJournal)
    BurdJournals.updateJournalIcon(cleanJournal)

    -- Commit only after the replacement exists and is fully initialized. This
    -- keeps every failure above non-destructive for both the journal and tools.
    inventory:Remove(leather)
    BurdJournals.consumeItemUses(thread, 1, player)
    BurdJournals.consumeItemUses(needle, 1, player)
    inventory:Remove(journal)
    sendRemoveItemFromContainer(inventory, journal)
    if sourceUUID and BurdJournals.Server.purgeJournalUUIDTracking then
        BurdJournals.Server.purgeJournalUUIDTracking(sourceUUID, {
            removeBackup = true,
            removeRuntime = true,
            removeEntryStore = true,
            removeEntryFileBackup = true,
            entryStoreShardKey = type(sourceJournalData) == "table" and sourceJournalData.entryStoreShardKey or nil,
        })
    end

    if cleanJournal.transmitModData then
        cleanJournal:transmitModData()
        BurdJournals.debugPrint("[BurdJournals] Server: transmitModData called for clean journal in handleConvertToClean")
    end

    sendAddItemToContainer(inventory, cleanJournal)
    BurdJournals.debugPrint("[BurdJournals] Server: sendAddItemToContainer called for clean journal in handleConvertToClean")

    BurdJournals.Server.sendToClient(player, "convertSuccess", {
        message = "The worn journal has been restored to a clean blank journal."
    })
end

function BurdJournals.Server.handleClaimRecipe(player, args)
    if not args or not args.recipeName then
        BurdJournals.Server.sendToClient(player, "error", {message = "Invalid request."})
        return
    end

    local journalId = args.journalId
    local recipeName = args.recipeName
    local journal, resolvedJournalId, resolvedJournalUUID = resolveServerCommandJournal(player, args, "claimRecipe")
    if not journal then
        BurdJournals.Server.sendToClient(player, "error", {message = "Journal not found."})
        return
    end
    journalId = resolvedJournalId or (journal.getID and journal:getID()) or journalId
    if not maybeMigrateRuntimeOnTouch(journal, player, "claimRecipe") then
        return
    end
    if not enforceJournalLightRequirement(player, "claimRecipe") then
        return
    end

    local canClaim, reason = BurdJournals.canPlayerClaimFromJournal(player, journal)
    if not canClaim then
        BurdJournals.Server.sendToClient(player, "error", {message = reason or "Permission denied."})
        return
    end

    local modData = journal:getModData()
    local journalData = modData.BurdJournals
    if BurdJournals.Server.preferEntryStoreDataForEmptyShell then
        journalData = BurdJournals.Server.preferEntryStoreDataForEmptyShell(journalData, args, "claimRecipeEmptyShell")
    end
    if isHiddenCursedJournalState(journal, journalData) then
        BurdJournals.Server.sendToClient(player, "error", {message = BurdJournals.Server.getHiddenCursedClaimBlockedMessage()})
        return
    end

    if not journalData or not journalData.recipes then
        BurdJournals.Server.sendToClient(player, "error", {message = "This journal has no recipe data."})
        return
    end
    normalizeFoundJournalClaimFlags(journal, journalData, "handleClaimRecipe")
    local limitedClaimMode = BurdJournals.Server.limitedLootClaimModeActive(journal)
    if BurdJournals.Server.enforceLimitedLootClaimBudget(player, journal, journalData) then
        return
    end

    if (not journalData.recipes[recipeName]) and BurdJournals.Server.preferEntryStoreDataForMissingEntry then
        journalData = BurdJournals.Server.preferEntryStoreDataForMissingEntry(journalData, args, "recipes", recipeName, "claimRecipeMissingEntry")
    end

    local storedRecipeName = BurdJournals.resolveRecipeKey and BurdJournals.resolveRecipeKey(journalData.recipes, recipeName) or recipeName
    local learnRecipeName = BurdJournals.getRecipeCanonicalName and BurdJournals.getRecipeCanonicalName(recipeName) or recipeName
    if not storedRecipeName or not journalData.recipes[storedRecipeName] then
        bsjWriteLogLine("[BurdJournals] Server ERROR: Recipe '" .. recipeName .. "' not found in journal")
        BurdJournals.Server.sendToClient(player, "error", {message = "Recipe not found in journal.", errorCode = "entryNotFound"})
        return
    end

    if BurdJournals.isRecipeEnabledForJournal and not BurdJournals.isRecipeEnabledForJournal(journalData, recipeName) then
        BurdJournals.Server.sendToClient(player, "error", {message = "Recipe claiming is disabled for this journal type."})
        return
    end

    local isPlayerJournalRecipeClaim = journalData.isPlayerCreated == true
        or (BurdJournals.isEffectivelyPlayerJournal and BurdJournals.isEffectivelyPlayerJournal(journalData))
    local playerAlreadyKnowsRecipe = BurdJournals.playerKnowsRecipe(player, learnRecipeName)

    if BurdJournals.hasCharacterClaimedRecipe(journalData, player, storedRecipeName)
        and not (isPlayerJournalRecipeClaim and not playerAlreadyKnowsRecipe) then
        BurdJournals.Server.sendToClient(player, "error", {message = "Recipe already claimed."})
        return {recipeName = recipeName, completed = false, alreadyClaimed = true}
    end

    if playerAlreadyKnowsRecipe then
        if not limitedClaimMode then
            -- Mark as claimed even though player already knows the recipe (allows journal dissolution)
            BurdJournals.markRecipeClaimedByCharacter(journalData, player, storedRecipeName)
            if journal.transmitModData and not BurdJournals.Server.shouldDeferBatchTransmit(args) then
            safeTransmitServerJournalModData(journal, "claimRecipe")
            end
        end
        BurdJournals.Server.sendToClient(player, "recipeAlreadyKnown", attachRuntimeDeltaOrLegacyJournalData({
            recipeName = recipeName,
            journalId = journalId,
        }, journalData, player, "claimRecipeAlreadyKnown"))
        if not limitedClaimMode then
            -- Re-fetch journal by ID before calling shouldDissolve to avoid zombie object errors
            local freshJournal = BurdJournals.Server.resolvePostActionJournal(player, journalId, journal)
            if freshJournal and BurdJournals.isValidItem(freshJournal) and BurdJournals.shouldDissolve and BurdJournals.shouldDissolve(freshJournal, player) then
                BurdJournals.Server.dissolveJournal(player, freshJournal)
            end
        end
        return {
            recipeName = recipeName,
            completed = true,
            claimed = false,
            alreadyKnown = true,
        }
    end

    local recipeWasLearned = BurdJournals.learnRecipeWithVerification(player, learnRecipeName, "[BurdJournals Server]")

    if recipeWasLearned then

        BurdJournals.markRecipeClaimedByCharacter(journalData, player, storedRecipeName)

        if journal.transmitModData and not BurdJournals.Server.shouldDeferBatchTransmit(args) then
            safeTransmitServerJournalModData(journal, "claimRecipe")
        end

        if sendSyncPlayerFields then
            -- Only sync recipes (0x4), not skills/traits (0x7 would sync all three)
            -- Match vanilla research-recipe sync (PF_Recipes).
            sendSyncPlayerFields(player, 0x00000001)
        end

        BurdJournals.Server.sendToClient(player, "claimSuccess", attachRuntimeDeltaOrLegacyJournalData({
            recipeName = recipeName,
            journalId = journalId,
        }, journalData, player, "claimRecipeSuccess"))
        -- Re-fetch journal by ID before calling shouldDissolve to avoid zombie object errors
        local freshJournal = BurdJournals.Server.resolvePostActionJournal(player, journalId, journal)
        BurdJournals.debugPrint("[BurdJournals] Server: Post-recipe claim check - freshJournal=" .. tostring(freshJournal ~= nil) .. ", journalId=" .. tostring(journalId))
        if freshJournal then
            local isValid = BurdJournals.isValidItem(freshJournal)
            local hasShouldDissolve = BurdJournals.shouldDissolve ~= nil
            local shouldDis = hasShouldDissolve and BurdJournals.shouldDissolve(freshJournal, player)
            BurdJournals.debugPrint("[BurdJournals] Server: isValid=" .. tostring(isValid) .. ", hasShouldDissolve=" .. tostring(hasShouldDissolve) .. ", shouldDis=" .. tostring(shouldDis))
            if isValid and shouldDis then
                BurdJournals.debugPrint("[BurdJournals] Server: DISSOLVING JOURNAL after recipe claim!")
                BurdJournals.Server.dissolveJournal(player, freshJournal)
            end
        end
        return {
            recipeName = recipeName,
            completed = true,
            claimed = true,
        }
    else
        BurdJournals.Server.sendToClient(player, "error", {message = "Could not learn recipe."})
        return {
            recipeName = recipeName,
            completed = false,
            claimed = false,
        }
    end
end

function BurdJournals.Server.handleAbsorbRecipe(player, args)
    if not args or not args.recipeName then
        BurdJournals.Server.sendToClient(player, "error", {message = "Invalid request."})
        return
    end

    local journalId = args.journalId
    local recipeName = args.recipeName

    local journal, resolvedJournalId, resolvedJournalUUID = resolveServerCommandJournal(player, args, "absorbRecipe")
    if not journal then
        BurdJournals.Server.sendToClient(player, "error", {message = "Journal not found."})
        return
    end
    journalId = resolvedJournalId or (journal.getID and journal:getID()) or journalId
    if not maybeMigrateRuntimeOnTouch(journal, player, "absorbRecipe") then
        return
    end
    if not enforceJournalLightRequirement(player, "absorbRecipe") then
        return
    end

    if not BurdJournals.canAbsorbXP(journal) then
        BurdJournals.Server.sendToClient(player, "error", {message = "Cannot absorb from this journal."})
        return
    end

    local modData = journal:getModData()
    local journalData = modData.BurdJournals

    if not journalData or not journalData.recipes then
        BurdJournals.Server.sendToClient(player, "error", {message = "This journal has no recipe data."})
        return
    end
    normalizeFoundJournalClaimFlags(journal, journalData, "handleAbsorbRecipe")
    local limitedClaimMode = BurdJournals.Server.limitedLootClaimModeActive(journal)
    if BurdJournals.Server.enforceLimitedLootClaimBudget(player, journal, journalData) then
        return
    end

    local storedRecipeName = BurdJournals.resolveRecipeKey and BurdJournals.resolveRecipeKey(journalData.recipes, recipeName) or recipeName
    local learnRecipeName = BurdJournals.getRecipeCanonicalName and BurdJournals.getRecipeCanonicalName(recipeName) or recipeName
    if not storedRecipeName or not journalData.recipes[storedRecipeName] then
        bsjWriteLogLine("[BurdJournals] Server ERROR: Recipe '" .. recipeName .. "' not found in journal")
        BurdJournals.Server.sendToClient(player, "error", {message = "Recipe not found in journal.", errorCode = "entryNotFound"})
        return
    end

    if BurdJournals.isRecipeEnabledForJournal and not BurdJournals.isRecipeEnabledForJournal(journalData, recipeName) then
        BurdJournals.Server.sendToClient(player, "error", {message = "Recipe absorbing is disabled for this journal type."})
        return
    end

    local isPlayerJournalRecipeClaim = journalData.isPlayerCreated == true
        or (journalData.isPlayerCreated == nil
            and (journalData.ownerUsername or journalData.ownerSteamId or journalData.ownerCharacterName)
            and journalData.isWorn ~= true
            and journalData.isBloody ~= true)
    local playerAlreadyKnowsRecipe = BurdJournals.playerKnowsRecipe(player, learnRecipeName)

    if BurdJournals.hasCharacterClaimedRecipe(journalData, player, storedRecipeName)
        and not (isPlayerJournalRecipeClaim and not playerAlreadyKnowsRecipe) then
        BurdJournals.Server.sendToClient(player, "error", {message = "Recipe already claimed."})
        return
    end

    if playerAlreadyKnowsRecipe then
        if not limitedClaimMode then
            BurdJournals.markRecipeClaimedByCharacter(journalData, player, storedRecipeName)

            if journal.transmitModData and not BurdJournals.Server.shouldDeferBatchTransmit(args) then
            safeTransmitServerJournalModData(journal, "absorbRecipe")
            end
        end

        BurdJournals.Server.sendToClient(player, "recipeAlreadyKnown", attachRuntimeDeltaOrLegacyJournalData({
            recipeName = recipeName,
            journalId = journalId,
        }, journalData, player, "absorbRecipeAlreadyKnown"))

        if not limitedClaimMode then
            -- Re-fetch journal by ID before calling shouldDissolve to avoid zombie object errors
            local freshJournal = BurdJournals.Server.resolvePostActionJournal(player, journalId, journal)
            if freshJournal and BurdJournals.isValidItem(freshJournal) and BurdJournals.shouldDissolve(freshJournal, player) then
                local dissolutionMessage = BurdJournals.getRandomDissolutionMessage()
                if removeJournalCompletely(player, freshJournal) then
                    BurdJournals.Server.sendToClient(player, "journalDissolved", {
                        message = dissolutionMessage
                    })
                else
                    BurdJournals.Server.sendToClient(player, "error", {message = "The journal could not be removed; please retry."})
                end
            end
        end
        return {
            recipeName = recipeName,
            completed = true,
            claimed = false,
            alreadyKnown = true,
        }
    end

    local recipeWasLearned = BurdJournals.learnRecipeWithVerification(player, learnRecipeName, "[BurdJournals Server]")

    if recipeWasLearned then

        BurdJournals.markRecipeClaimedByCharacter(journalData, player, storedRecipeName)

        if journal.transmitModData and not BurdJournals.Server.shouldDeferBatchTransmit(args) then
            safeTransmitServerJournalModData(journal, "absorbRecipe")
        end

        if sendSyncPlayerFields then
            -- Only sync recipes (0x4), not skills/traits (0x7 would sync all three)
            -- Match vanilla research-recipe sync (PF_Recipes).
            sendSyncPlayerFields(player, 0x00000001)
        end

        -- Re-fetch journal by ID before calling shouldDissolve to avoid zombie object errors
        local freshJournal = BurdJournals.Server.resolvePostActionJournal(player, journalId, journal)
        local shouldDis = freshJournal and BurdJournals.isValidItem(freshJournal) and BurdJournals.shouldDissolve(freshJournal, player)

        if shouldDis then
            local dissolutionMessage = BurdJournals.getRandomDissolutionMessage()
            if removeJournalCompletely(player, freshJournal) then
                BurdJournals.Server.sendToClient(player, "absorbSuccess", attachRuntimeDeltaOrLegacyJournalData({
                    recipeName = recipeName,
                    journalId = journalId,
                    dissolved = true,
                    dissolutionMessage = dissolutionMessage
                }, journalData, player, "absorbRecipeDissolved"))
                return {recipeName = recipeName, completed = true, claimed = true, dissolved = true}
            end
            BurdJournals.Server.sendToClient(player, "error", {message = "The journal could not be removed; please retry."})
            return {recipeName = recipeName, completed = true, claimed = true, dissolved = false}
        else
            BurdJournals.Server.sendToClient(player, "absorbSuccess", attachRuntimeDeltaOrLegacyJournalData({
                recipeName = recipeName,
                journalId = journalId,
                dissolved = false
            }, journalData, player, "absorbRecipeSuccess"))
            return {recipeName = recipeName, completed = true, claimed = true}
        end
    else
        BurdJournals.Server.sendToClient(player, "error", {message = "Could not learn recipe."})
        return {recipeName = recipeName, completed = false, claimed = false}
    end
end

function BurdJournals.Server.handleEraseEntry(player, args)
    if not args then
        BurdJournals.debugPrint("[BurdJournals] Server: EraseEntry - No args provided")
        return
    end

    local journalId = args.journalId
    local entryType = args.entryType
    local entryName = args.entryName

    if not hasAnyJournalLookupArgs(args) or not entryType or not entryName then
        BurdJournals.debugPrint("[BurdJournals] Server: EraseEntry - Missing required args")
        BurdJournals.Server.sendToClient(player, "error", {message = "Invalid erase request."})
        return
    end

    BurdJournals.debugPrint("[BurdJournals] Server: Processing erase request - type: " .. entryType .. ", name: " .. entryName)

    local journal = resolveServerCommandJournal(player, args, "eraseEntry")
    if not journal then
        BurdJournals.debugPrint("[BurdJournals] Server: EraseEntry - Journal not found: " .. tostring(journalId))
        BurdJournals.Server.sendToClient(player, "error", {message = "Journal not found."})
        return
    end
    if not maybeMigrateRuntimeOnTouch(journal, player, "eraseEntry") then
        return
    end
    local canMutateJournal, _, mutationMessage = BurdJournals.Server.validatePlayerJournalMutation(player, journal, {
        allowBlank = false,
    })
    if not canMutateJournal then
        BurdJournals.Server.sendToClient(player, "error", {message = mutationMessage})
        return
    end

    if BurdJournals.getSandboxOption("RequireEraserToErase") then
        local eraser = BurdJournals.findEraser(player)
        if not eraser then
            BurdJournals.Server.sendToClient(player, "error", {message = "You need an eraser to wipe the journal."})
            return
        end
    end

    local modData = journal:getModData()
    if not modData or not modData.BurdJournals then
        BurdJournals.debugPrint("[BurdJournals] Server: EraseEntry - No journal data")
        BurdJournals.Server.sendToClient(player, "error", {message = "Journal has no data."})
        return
    end

    local journalData = modData.BurdJournals
    local erased = false

    if entryType == "skill" then
        -- Resolve case/alias skill key variants (e.g. Woodwork vs Carpentry)
        -- the same way handleClaimSkill does before touching the buckets.
        local storedSkillName = BurdJournals.resolveSkillKey and BurdJournals.resolveSkillKey(journalData.skills, entryName) or entryName
        if journalData.skills and journalData.skills[storedSkillName] then
            entryName = storedSkillName
        end
        if journalData.skills and journalData.skills[entryName] then
            journalData.skills[entryName] = nil
            erased = true
            BurdJournals.debugPrint("[BurdJournals] Server: Erased skill entry: " .. entryName)
        end

        -- Clear legacy claims
        if journalData.claimedSkills and journalData.claimedSkills[entryName] then
            journalData.claimedSkills[entryName] = nil
        end
        -- Clear per-character claims structure for ALL characters
        if journalData.claims and type(journalData.claims) == "table" then
            for charId, charClaims in pairs(journalData.claims) do
                if charClaims and charClaims.skills and charClaims.skills[entryName] then
                    charClaims.skills[entryName] = nil
                    BurdJournals.debugPrint("[BurdJournals] Server: Cleared skill claim for character: " .. tostring(charId))
                end
            end
        end
    elseif entryType == "trait" then
        if journalData.traits and journalData.traits[entryName] then
            journalData.traits[entryName] = nil
            erased = true
            BurdJournals.debugPrint("[BurdJournals] Server: Erased trait entry: " .. entryName)
        end

        -- Clear legacy claims
        if journalData.claimedTraits and journalData.claimedTraits[entryName] then
            journalData.claimedTraits[entryName] = nil
        end
        -- Clear per-character claims structure for ALL characters
        if journalData.claims and type(journalData.claims) == "table" then
            for charId, charClaims in pairs(journalData.claims) do
                if charClaims and charClaims.traits and charClaims.traits[entryName] then
                    charClaims.traits[entryName] = nil
                    BurdJournals.debugPrint("[BurdJournals] Server: Cleared trait claim for character: " .. tostring(charId))
                end
            end
        end
    elseif entryType == "recipe" then
        if journalData.recipes and journalData.recipes[entryName] then
            journalData.recipes[entryName] = nil
            erased = true
            BurdJournals.debugPrint("[BurdJournals] Server: Erased recipe entry: " .. entryName)
        end

        -- Clear legacy claims
        if journalData.claimedRecipes and journalData.claimedRecipes[entryName] then
            journalData.claimedRecipes[entryName] = nil
        end
        -- Clear per-character claims structure for ALL characters
        if journalData.claims and type(journalData.claims) == "table" then
            for charId, charClaims in pairs(journalData.claims) do
                if charClaims and charClaims.recipes and charClaims.recipes[entryName] then
                    charClaims.recipes[entryName] = nil
                    BurdJournals.debugPrint("[BurdJournals] Server: Cleared recipe claim for character: " .. tostring(charId))
                end
            end
        end
    elseif entryType == "stat" then
        if journalData.stats and journalData.stats[entryName] then
            journalData.stats[entryName] = nil
            erased = true
            BurdJournals.debugPrint("[BurdJournals] Server: Erased stat entry: " .. entryName)
        end

        -- Clear legacy claims
        if journalData.claimedStats and journalData.claimedStats[entryName] then
            journalData.claimedStats[entryName] = nil
        end
        -- Clear per-character claims structure for ALL characters
        if journalData.claims and type(journalData.claims) == "table" then
            for charId, charClaims in pairs(journalData.claims) do
                if charClaims and charClaims.stats and charClaims.stats[entryName] then
                    charClaims.stats[entryName] = nil
                    BurdJournals.debugPrint("[BurdJournals] Server: Cleared stat claim for character: " .. tostring(charId))
                end
            end
        end
    end

    -- Strict MP player journals keep their entry buckets in the server-side
    -- entry store. Write the erase through to the store so hydration, chunk
    -- requests, and restarts cannot resurrect the erased entry.
    if BurdJournals.getOrCreateJournalEntryStoreEntryForData then
        local storeEntry, storeShardKey, storeUUID = BurdJournals.getOrCreateJournalEntryStoreEntryForData(modData.BurdJournals, false, journal, "eraseEntry")
        if type(storeEntry) == "table" then
            local storeBucketName = nil
            if entryType == "skill" then
                storeBucketName = "skills"
            elseif entryType == "trait" then
                storeBucketName = "traits"
            elseif entryType == "recipe" then
                storeBucketName = "recipes"
            elseif entryType == "stat" then
                storeBucketName = "stats"
            end
            local storeBucket = storeBucketName and storeEntry[storeBucketName] or nil
            if type(storeBucket) == "table" then
                local storeKey = entryName
                if entryType == "skill" and BurdJournals.resolveSkillKey then
                    storeKey = BurdJournals.resolveSkillKey(storeBucket, entryName) or entryName
                end
                if storeBucket[storeKey] ~= nil then
                    storeBucket[storeKey] = nil
                    erased = true
                    storeEntry.updatedAt = BurdJournals._getRuntimeNowMs()
                    if BurdJournals.refreshJournalEntryStoreCounts then
                        BurdJournals.refreshJournalEntryStoreCounts(modData.BurdJournals, "eraseEntry")
                    end
                    if storeShardKey and BurdJournals.markEntryStoreShardDirty then
                        BurdJournals.markEntryStoreShardDirty(storeShardKey, false, storeUUID)
                    end
                    BurdJournals.debugPrint("[BurdJournals] Server: Erased entry store " .. tostring(storeBucketName) .. " entry: " .. tostring(storeKey))
                end
            end
        end
    end

    if erased then

        if journal.transmitModData and not BurdJournals.Server.shouldDeferBatchTransmit() then
            safeTransmitServerJournalModData(journal, "eraseEntry")
        end

        local updatedJournalData = BurdJournals.Server.copyJournalData(journal)
        local liveJournalData = modData.BurdJournals
        local erasePayload = {
            entryType = entryType,
            entryName = entryName,
            journalId = journal:getID(),
            entryStoreEnabled = type(liveJournalData) == "table" and liveJournalData.entryStoreEnabled == true,
            entryStoreUUID = type(liveJournalData) == "table" and liveJournalData.entryStoreUUID or nil,
            entryStoreEntryCounts = type(liveJournalData) == "table" and type(liveJournalData.entryStoreEntryCounts) == "table" and liveJournalData.entryStoreEntryCounts or nil,
            entryStoreUpdatedAt = type(liveJournalData) == "table" and tonumber(liveJournalData.entryStoreUpdatedAt) or nil,
        }
        if attachRuntimeDeltaOrLegacyJournalData and type(updatedJournalData) == "table" then
            erasePayload = attachRuntimeDeltaOrLegacyJournalData(erasePayload, updatedJournalData, player, "eraseEntry")
        end

        BurdJournals.Server.sendToClient(player, "eraseSuccess", erasePayload)
        BurdJournals.debugPrint("[BurdJournals] Server: Erase successful, sent confirmation to client")
    else
        BurdJournals.debugPrint("[BurdJournals] Server: Entry not found to erase: " .. entryType .. " - " .. entryName)
        BurdJournals.Server.sendToClient(player, "error", {message = "Entry not found.", errorCode = "entryNotFound"})
    end
end

-- Server-side handler for renaming journals (MP custom name persistence fix)
-- This ensures the server has the correct name so it persists during item transfers
function BurdJournals.Server.handleRenameJournal(player, args)
    local function sendRenameResult(payload)
        payload = type(payload) == "table" and payload or {}
        payload.renameRequestId = args and args.renameRequestId or nil
        BurdJournals.Server.sendToClient(player, "renameResult", payload)
    end
    if not args then
        BurdJournals.debugPrint("[BurdJournals] Server: RenameJournal - No args provided")
        sendRenameResult({ok = false, errorCode = "invalidRequest"})
        return
    end

    local journalId = args.journalId
    local newName = args.newName

    if not hasAnyJournalLookupArgs(args) or not newName then
        BurdJournals.debugPrint("[BurdJournals] Server: RenameJournal - Missing required args")
        sendRenameResult({ok = false, errorCode = "invalidRequest"})
        return
    end

    -- The 100-byte limit applies after surrounding whitespace is trimmed.
    -- Control characters are rejected rather than stripped so an attacker
    -- cannot smuggle line breaks into item names or server logs.
    if type(newName) ~= "string" or newName:find("%c") then
        BurdJournals.debugPrint("[BurdJournals] Server: RenameJournal - Invalid name shape")
        sendRenameResult({ok = false, errorCode = "invalidName"})
        return
    end
    newName = newName:match("^%s*(.-)%s*$") or ""
    if newName == "" or #newName > 100 then
        BurdJournals.debugPrint("[BurdJournals] Server: RenameJournal - Invalid normalized name")
        sendRenameResult({ok = false, errorCode = "invalidName"})
        return
    end

    local journal = resolveServerCommandJournal(player, args, "renameJournal")
    if not journal then
        BurdJournals.debugPrint("[BurdJournals] Server: RenameJournal - Journal not found: " .. tostring(journalId))
        sendRenameResult({ok = false, errorCode = "journalNotFound"})
        return
    end
    if not maybeMigrateRuntimeOnTouch(journal, player, "renameJournal") then
        sendRenameResult({ok = false, errorCode = "journalMigrationFailed"})
        return
    end
    local canMutateJournal, mutationCode = BurdJournals.Server.validatePlayerJournalMutation(player, journal, {
        allowBlank = true,
    })
    if not canMutateJournal then
        sendRenameResult({ok = false, errorCode = mutationCode or "permissionDenied"})
        return
    end

    -- Update the item's display name on the server
    journal:setName(newName)
    
    -- Mark as custom name so PZ preserves it during serialization
    if journal.setCustomName then
        journal:setCustomName(true)
    end

    -- Store in ModData as backup
    local modData = journal:getModData()
    if not modData.BurdJournals then
        modData.BurdJournals = {}
    end
    modData.BurdJournals.customName = newName

    -- Transmit the updated data to all clients
    if journal.transmitModData and not BurdJournals.Server.shouldDeferBatchTransmit() then
        safeTransmitServerJournalModData(journal, "renameJournal")
    end

    BurdJournals.debugPrint("[BurdJournals] Server: Journal renamed successfully (bytes=" .. tostring(#newName) .. ")")

    -- Send confirmation to client
    sendRenameResult({
        ok = true,
        journalId = journal.getID and journal:getID() or journalId,
        journalUUID = modData.BurdJournals.uuid,
        newName = newName
    })
end

-- Authoritatively remove duplicate journals produced by a craft recipe. The
-- client removes them locally for immediate feedback, but that removal is not
-- persisted in MP, so without this the extras reappear on relog. Only journal
-- items found in/near the requesting player are removed (anti-grief).
function BurdJournals.Server.handleRemoveCraftDuplicates(player, args)
    if not player or not args or type(args.journalIds) ~= "table" then
        return
    end

    local journalIds, tooLarge = BurdJournals.normalizeTableLimited(args.journalIds, 16)
    if tooLarge or type(journalIds) ~= "table" then
        BurdJournals.Server.sendToClient(player, "error", {message = "Invalid duplicate journal request.", errorCode = "invalidDuplicateJournalRequest"})
        return
    end
    local removed = 0
    local seenIds = {}
    for _, rawId in ipairs(journalIds) do
        local id = tonumber(rawId)
        if id and id >= 0 and id == math.floor(id) and not seenIds[id] then
            seenIds[id] = true
        local item = BurdJournals.Server.findJournalInRequestersInventory(player, id, nil, nil)
        if item and BurdJournals.isAnyJournal and BurdJournals.isAnyJournal(item) then
            local container = item.getContainer and item:getContainer() or nil
            if container and container.Remove then
                container:Remove(item)
                removed = removed + 1
            end
        end
        end
    end

    if removed > 0 then
        BurdJournals.debugPrint("[BurdJournals] Server: removed " .. tostring(removed)
            .. " duplicate crafted journal(s) for " .. tostring(player.getUsername and player:getUsername() or "?"))
    end
end

-- Track if we've already logged the cache state this session
BurdJournals.Server._baselineCacheLogged = false
BurdJournals.Server._modDataInitialized = false
BurdJournals.Server._baselineCacheInstance = nil  -- Cache the reference to avoid re-creating
BurdJournals.Server._baselineArchiveInstance = nil
BurdJournals.Server._baselineSnapshotInstance = nil
BurdJournals.Server._baselineSnapshotSeeded = false
BurdJournals.Server.BASELINE_CACHE_MODDATA_KEY = "BurdJournals_PlayerBaselines"
BurdJournals.Server.BASELINE_ARCHIVE_MODDATA_KEY = "BurdJournals_PlayerBaselinesArchive"
BurdJournals.Server.PLAYER_BASELINE_BACKUP_KEY = "serverBaselineBackup"
BurdJournals.Server.BASELINE_SNAPSHOT_MODDATA_KEY = BurdJournals.BASELINE_SNAPSHOT_STORE_MODDATA_KEY
    or "BurdJournals_BaselineSnapshotsV1"
BurdJournals.Server.BASELINE_SNAPSHOT_ARCHIVE_MIRROR_FIELD = "baselineSnapshotsMirrorV1"
BurdJournals.Server.BASELINE_SNAPSHOT_ARCHIVE_MIRROR_FIELD_LEGACY = "_baselineSnapshotsMirrorV1"
BurdJournals.Server.PLAYER_BASELINE_SNAPSHOT_HISTORY_KEY = "serverBaselineSnapshotHistory"
-- Player-ModData snapshot history rides along with every player:transmitModData(),
-- so keep it tiny; the authoritative store lives in global ModData.
BurdJournals.Server.PLAYER_BASELINE_SNAPSHOT_HISTORY_LIMIT = 5

function BurdJournals.Server.copyBaselineTableEntries(sourceTable)
    local copied = {}
    local normalized = BurdJournals.normalizeTable and BurdJournals.normalizeTable(sourceTable) or nil
    if type(normalized) ~= "table" then
        return copied
    end
    for key, value in pairs(normalized) do
        if type(key) == "number" and type(value) == "string" and value ~= "" then
            copied[value] = true
        elseif key ~= nil and value ~= nil then
            copied[key] = value
        end
    end
    return copied
end

function BurdJournals.Server.cloneBaselineRecordForStorage(baselineData)
    local nowHours = getGameTime and getGameTime():getWorldAgeHours() or 0
    local cloned = {
        skillBaseline = BurdJournals.Server.copyBaselineTableEntries(baselineData and baselineData.skillBaseline),
        skillExportBaseline = BurdJournals.Server.copyBaselineTableEntries(baselineData and baselineData.skillExportBaseline),
        mediaSkillBaseline = BurdJournals.Server.copyBaselineTableEntries(baselineData and baselineData.mediaSkillBaseline),
        mediaSkillExportBaseline = BurdJournals.Server.copyBaselineTableEntries(baselineData and baselineData.mediaSkillExportBaseline),
        traitBaseline = BurdJournals.Server.copyBaselineTableEntries(baselineData and baselineData.traitBaseline),
        traitExportBaseline = BurdJournals.Server.copyBaselineTableEntries(baselineData and baselineData.traitExportBaseline),
        recipeBaseline = BurdJournals.Server.copyBaselineTableEntries(baselineData and baselineData.recipeBaseline),
        recipeExportBaseline = BurdJournals.Server.copyBaselineTableEntries(baselineData and baselineData.recipeExportBaseline),
        baselineVersion = tonumber(baselineData and baselineData.baselineVersion) or tonumber(BurdJournals.BASELINE_VERSION) or 5,
        capturedAt = tonumber(baselineData and baselineData.capturedAt) or nowHours,
        steamId = baselineData and baselineData.steamId or nil,
        characterName = baselineData and baselineData.characterName or nil,
        debugModified = (baselineData and baselineData.debugModified == true) or false
    }
    if BurdJournals.sanitizeBaselinePayloadForSnapshot then
        local sanitized = BurdJournals.sanitizeBaselinePayloadForSnapshot(cloned)
        cloned.traitBaseline = sanitized.traitBaseline
    end
    if baselineData and baselineData.recoveredFromPlayerModData ~= nil then
        cloned.recoveredFromPlayerModData = baselineData.recoveredFromPlayerModData
    end
    if baselineData and baselineData.migrationSource ~= nil then
        cloned.migrationSource = baselineData.migrationSource
    end
    return cloned
end

function BurdJournals.Server.mergeRestrictiveBaselineNumberMap(existingMap, hintedMap, allowedSet)
    local merged = BurdJournals.Server.copyBaselineTableEntries(existingMap)
    local normalizedHints = BurdJournals.normalizeTable and BurdJournals.normalizeTable(hintedMap) or hintedMap
    local mergedCount = 0

    if type(normalizedHints) ~= "table" then
        return merged, mergedCount
    end

    for key, value in pairs(normalizedHints) do
        if key ~= nil and (not allowedSet or allowedSet[key] == true) then
            local incoming = math.max(0, tonumber(value) or 0)
            if incoming > 0 then
                local existing = math.max(0, tonumber(merged[key]) or 0)
                if incoming > existing then
                    merged[key] = incoming
                    mergedCount = mergedCount + 1
                end
            end
        end
    end

    return merged, mergedCount
end

function BurdJournals.Server.mergeRestrictiveBaselineBoolMap(existingMap, hintedMap)
    local merged = BurdJournals.Server.copyBaselineTableEntries(existingMap)
    local normalizedHints = BurdJournals.normalizeTable and BurdJournals.normalizeTable(hintedMap) or hintedMap
    local mergedCount = 0

    if type(normalizedHints) ~= "table" then
        return merged, mergedCount
    end

    for key, value in pairs(normalizedHints) do
        if key ~= nil and value == true and merged[key] ~= true then
            merged[key] = true
            mergedCount = mergedCount + 1
        end
    end

    return merged, mergedCount
end

function BurdJournals.Server.mergeRestrictiveClientBaselineHints(serverBaseline, clientArgs)
    if type(serverBaseline) ~= "table" or type(clientArgs) ~= "table" then
        return serverBaseline
    end

    local hintedSkills = clientArgs.skillBaseline
    local hintedMediaSkills = clientArgs.mediaSkillBaseline
    local hintedTraits = clientArgs.traitBaseline
    local hintedRecipes = clientArgs.recipeBaseline
    if type(hintedSkills) ~= "table"
        and type(hintedMediaSkills) ~= "table"
        and type(hintedTraits) ~= "table"
        and type(hintedRecipes) ~= "table"
    then
        return serverBaseline
    end

    local mergedBaseline = BurdJournals.Server.cloneBaselineRecordForStorage(serverBaseline)
    local allowedSkills = BurdJournals.getAllowedSkills and BurdJournals.getAllowedSkills() or {}
    local allowedSkillSet = {}
    for _, skillName in ipairs(allowedSkills) do
        allowedSkillSet[skillName] = true
    end

    local mergedSkills = 0
    local mergedMediaSkills = 0
    local mergedTraits = 0
    local mergedRecipes = 0

    mergedBaseline.skillBaseline, mergedSkills = BurdJournals.Server.mergeRestrictiveBaselineNumberMap(
        mergedBaseline.skillBaseline,
        hintedSkills,
        allowedSkillSet
    )
    mergedBaseline.mediaSkillBaseline, mergedMediaSkills = BurdJournals.Server.mergeRestrictiveBaselineNumberMap(
        mergedBaseline.mediaSkillBaseline,
        hintedMediaSkills,
        nil
    )
    mergedBaseline.traitBaseline, mergedTraits = BurdJournals.Server.mergeRestrictiveBaselineBoolMap(
        mergedBaseline.traitBaseline,
        hintedTraits
    )
    mergedBaseline.recipeBaseline, mergedRecipes = BurdJournals.Server.mergeRestrictiveBaselineBoolMap(
        mergedBaseline.recipeBaseline,
        hintedRecipes
    )

    local totalMerged = mergedSkills + mergedMediaSkills + mergedTraits + mergedRecipes
    if totalMerged > 0 then
        BurdJournals.debugPrint("[BurdJournals] registerBaseline: merged restrictive client baseline hints"
            .. " (skills=" .. tostring(mergedSkills)
            .. ", mediaSkills=" .. tostring(mergedMediaSkills)
            .. ", traits=" .. tostring(mergedTraits)
            .. ", recipes=" .. tostring(mergedRecipes) .. ")")
    end

    return mergedBaseline
end

function BurdJournals.Server.baselineTableHasEntries(tbl)
    local normalized = BurdJournals.normalizeTable and BurdJournals.normalizeTable(tbl) or nil
    if type(normalized) ~= "table" then
        return false
    end
    for key, value in pairs(normalized) do
        if key ~= nil and value ~= nil and value ~= false then
            return true
        end
    end
    return false
end

function BurdJournals.Server.getPlayerCharacterDisplayName(player)
    if not player then
        return nil
    end
    if BurdJournals.getPlayerCharacterName then
        local name = BurdJournals.getPlayerCharacterName(player)
        if name and name ~= "" then
            return name
        end
    end
    if player.getDescriptor then
        local descriptor = player:getDescriptor()
        if descriptor then
            local forename = descriptor:getForename() or "Unknown"
            local surname = descriptor:getSurname() or ""
            return forename .. " " .. surname
        end
    end
    return nil
end

function BurdJournals.Server.ensureBaselineModDataReady(allowCreate)
    if BurdJournals.Server._modDataInitialized then
        return true
    end

    -- In Build 42, OnInitGlobalModData is the reliable point where persisted
    -- global ModData is loaded. Allowing early create/read-write before that
    -- can clobber persisted stores during workshop update restarts.
    if Events and Events.OnInitGlobalModData then
        return false
    end

    -- Fallback only for environments without OnInitGlobalModData.
    local existingCache = nil
    local existingArchive = nil
    local existingSnapshots = nil
    if ModData.get then
        existingCache = ModData.get(BurdJournals.Server.BASELINE_CACHE_MODDATA_KEY)
        existingArchive = ModData.get(BurdJournals.Server.BASELINE_ARCHIVE_MODDATA_KEY)
        existingSnapshots = ModData.get(BurdJournals.Server.BASELINE_SNAPSHOT_MODDATA_KEY)
    end

    if existingCache or existingArchive or existingSnapshots then
        BurdJournals.Server._modDataInitialized = true
        BurdJournals.debugPrint("[BurdJournals] Baseline ModData detected before init event; enabling baseline/snapshot store access")
        return true
    end

    if allowCreate and ModData.getOrCreate then
        ModData.getOrCreate(BurdJournals.Server.BASELINE_CACHE_MODDATA_KEY)
        ModData.getOrCreate(BurdJournals.Server.BASELINE_ARCHIVE_MODDATA_KEY)
        ModData.getOrCreate(BurdJournals.Server.BASELINE_SNAPSHOT_MODDATA_KEY)
        BurdJournals.Server._modDataInitialized = true
        BurdJournals.debugPrint("[BurdJournals] Baseline ModData fallback created cache/archive/snapshot keys before init event")
        return true
    end

    return false
end

function BurdJournals.Server.isBaselineModDataReady()
    local ready = BurdJournals.Server._modDataInitialized == true
    if (not ready) and BurdJournals.Server.ensureBaselineModDataReady then
        ready = BurdJournals.Server.ensureBaselineModDataReady(false)
    end
    return ready == true
end

function BurdJournals.Server.clonePlayerBaselineBackupRecord(player, characterId, baselineData)
    local cloned = BurdJournals.Server.cloneBaselineRecordForStorage(baselineData or {})
    cloned.characterId = characterId or (BurdJournals.getPlayerCharacterId and BurdJournals.getPlayerCharacterId(player)) or nil
    if not cloned.steamId and BurdJournals.getPlayerSteamId then
        cloned.steamId = BurdJournals.getPlayerSteamId(player)
    end
    if not cloned.characterName then
        cloned.characterName = BurdJournals.Server.getPlayerCharacterDisplayName(player)
    end
    cloned._schema = 1
    cloned.updatedAt = getGameTime and getGameTime():getWorldAgeHours() or 0
    return cloned
end

function BurdJournals.Server.writePlayerBaselineBackup(player, characterId, baselineData, skipTransmit)
    if not player or not player.getModData or type(baselineData) ~= "table" then
        return false
    end

    local playerModData = player:getModData()
    if type(playerModData) ~= "table" then
        return false
    end
    playerModData.BurdJournals = playerModData.BurdJournals or {}

    local backup = BurdJournals.Server.clonePlayerBaselineBackupRecord(player, characterId, baselineData)
    playerModData.BurdJournals[BurdJournals.Server.PLAYER_BASELINE_BACKUP_KEY] = backup
    playerModData.BurdJournals.baselineCaptured = true
    playerModData.BurdJournals.baselineVersion = tonumber(backup and backup.baselineVersion)
        or tonumber(playerModData.BurdJournals.baselineVersion)
        or tonumber(BurdJournals.BASELINE_VERSION)
        or 5

    if not skipTransmit and player.transmitModData then
        player:transmitModData()
    end
    return true
end

function BurdJournals.Server.clearPlayerBaselineBackup(player, skipTransmit)
    if not player or not player.getModData then
        return false
    end
    local playerModData = player:getModData()
    local bj = type(playerModData) == "table" and playerModData.BurdJournals or nil
    if type(bj) ~= "table" then
        return false
    end
    if bj[BurdJournals.Server.PLAYER_BASELINE_BACKUP_KEY] == nil then
        return false
    end

    bj[BurdJournals.Server.PLAYER_BASELINE_BACKUP_KEY] = nil
    if not skipTransmit and player.transmitModData then
        player:transmitModData()
    end
    return true
end

function BurdJournals.Server.readPlayerBaselineBackup(player, requestedCharacterId)
    if not player or not player.getModData then
        return nil
    end

    local playerModData = player:getModData()
    local bj = type(playerModData) == "table" and playerModData.BurdJournals or nil
    local backup = type(bj) == "table" and bj[BurdJournals.Server.PLAYER_BASELINE_BACKUP_KEY] or nil
    if type(backup) ~= "table" then
        return nil
    end

    -- Respect global "clear all baselines" epochs so offline player backups don't resurrect wiped data.
    local backupUpdatedAt = tonumber(backup.updatedAt) or tonumber(backup.capturedAt) or 0
    local cache = BurdJournals.Server.getBaselineCache()
    local backupResetEpoch = cache and tonumber(cache._backupResetEpochHours) or 0
    if backupResetEpoch > 0 and backupUpdatedAt > 0 and backupUpdatedAt < backupResetEpoch then
        return nil
    end

    local backupCharacterId = backup.characterId and tostring(backup.characterId) or nil
    if requestedCharacterId and backupCharacterId and backupCharacterId ~= tostring(requestedCharacterId) then
        BurdJournals.debugPrint("[BurdJournals] Baseline backup characterId mismatch (backup="
            .. tostring(backupCharacterId) .. ", request=" .. tostring(requestedCharacterId)
            .. ") - using backup payload for same player")
    end

    local restored = BurdJournals.Server.cloneBaselineRecordForStorage(backup)
    restored.characterId = requestedCharacterId or backupCharacterId
    restored._schema = tonumber(backup._schema) or 1
    restored.updatedAt = tonumber(backup.updatedAt) or (getGameTime and getGameTime():getWorldAgeHours() or 0)
    if not restored.steamId and BurdJournals.getPlayerSteamId then
        restored.steamId = BurdJournals.getPlayerSteamId(player)
    end
    if not restored.characterName then
        restored.characterName = BurdJournals.Server.getPlayerCharacterDisplayName(player)
    end

    local hasData = BurdJournals.Server.baselineTableHasEntries(restored.skillBaseline)
        or BurdJournals.Server.baselineTableHasEntries(restored.mediaSkillBaseline)
        or BurdJournals.Server.baselineTableHasEntries(restored.traitBaseline)
        or BurdJournals.Server.baselineTableHasEntries(restored.recipeBaseline)
    if not hasData then
        return nil
    end

    return restored
end

BurdJournals.Server.BASELINE_TRANSMIT_DEBOUNCE_MS = 30000
BurdJournals.Server._baselineTransmitPendingAt = BurdJournals.Server._baselineTransmitPendingAt or nil
BurdJournals.Server._baselineTransmitPendingArchive = BurdJournals.Server._baselineTransmitPendingArchive or false
BurdJournals.Server._baselineTransmitLastAt = BurdJournals.Server._baselineTransmitLastAt or 0

local function baselineTransmitNowMs()
    if BurdJournals._getRuntimeNowMs then
        return BurdJournals._getRuntimeNowMs()
    end
    return (os.time() or 0) * 1000
end

-- Actually push the coalesced baseline cache/archive mirror to clients if the
-- debounce window has elapsed (or forced). Called from the server tick and at
-- batch end so a burst of register/delete/prune events collapses into one send.
function BurdJournals.Server.flushPendingBaselineTransmit(force)
    if type(ModData) ~= "table" or type(ModData.transmit) ~= "function" then
        return
    end
    if BurdJournals.Server._baselineTransmitPendingAt == nil then
        return
    end
    local now = baselineTransmitNowMs()
    local debounceMs = math.max(0, tonumber(BurdJournals.Server.BASELINE_TRANSMIT_DEBOUNCE_MS) or 30000)
    local lastAt = tonumber(BurdJournals.Server._baselineTransmitLastAt) or 0
    if not force and (now - lastAt) < debounceMs then
        return
    end
    BurdJournals.Server._baselineTransmitPendingAt = nil
    BurdJournals.Server._baselineTransmitPendingArchive = false
    BurdJournals.Server._baselineTransmitLastAt = now
    -- Only the cache is ever transmitted: clients keep a best-effort fallback reader
    -- for it (getClientGlobalCachedBaseline). The archive has no client readers and
    -- carries the snapshot mirror, so broadcasting it shipped the entire snapshot
    -- history to every client. Global ModData persists with the world save without
    -- transmitting, so the archive needs no transmit at all.
    if BurdJournals.Server.isTrueSinglePlayerContext and BurdJournals.Server.isTrueSinglePlayerContext() then
        ModData.transmit(BurdJournals.Server.BASELINE_CACHE_MODDATA_KEY)
    end
end

-- Global baseline ModData is a cache/recovery mirror only. Gameplay-critical
-- baseline delivery still goes through requestBaseline/registerBaseline and
-- the explicit baselineResponse client command. Broadcasting the entire cache
-- (+archive) to every client on each register/delete/prune froze mod-heavy
-- servers (recipeBaseline scales with installed mods), so coalesce into a
-- debounced flush instead of transmitting inline from command handlers.
function BurdJournals.Server.transmitBaselineStores(includeArchive)
    if type(ModData) ~= "table" or type(ModData.transmit) ~= "function" then
        return
    end
    BurdJournals.Server._baselineTransmitPendingAt = baselineTransmitNowMs()
    if includeArchive then
        BurdJournals.Server._baselineTransmitPendingArchive = true
    end
    BurdJournals.Server.flushPendingBaselineTransmit(false)
end

function BurdJournals.Server.transmitBaselineSnapshotStore()
    -- Never ModData.transmit the snapshot store or archive. No client code reads
    -- either table (clients get baselines via baselineResponse), and global ModData
    -- persists with the world save without transmitting. Broadcasting them shipped
    -- up to 50 full baselines per SteamID (recipeBaseline scales with installed
    -- mods) to every client on each death/register/prune, freezing mod-heavy
    -- servers for minutes. Only the load-time recovery mirror is kept fresh here.
    local store = BurdJournals.Server._baselineSnapshotInstance
    if type(store) ~= "table" and BurdJournals.Server.getBaselineSnapshotStore then
        store = BurdJournals.Server.getBaselineSnapshotStore()
    end
    if BurdJournals.Server.syncBaselineSnapshotMirrorToArchive then
        BurdJournals.Server.syncBaselineSnapshotMirrorToArchive(store, true)
    end
end

function BurdJournals.Server.getBaselineCache()
    -- Return cached instance if we have one (prevents multiple getOrCreate calls)
    if BurdJournals.Server._baselineCacheInstance then
        return BurdJournals.Server._baselineCacheInstance
    end
    
    -- Try to get existing ModData first (may return nil if not yet loaded from disk)
    local cache = nil
    
    -- Use ModData.get first to check if data exists without creating empty table
    local hasModDataApi = type(ModData) == "table"
    if hasModDataApi and ModData.get then
        cache = ModData.get(BurdJournals.Server.BASELINE_CACHE_MODDATA_KEY)
        if cache then
            BurdJournals.debugPrint("[BurdJournals] ModData.get returned existing cache")
        end
    end
    
    -- If no cache found, use getOrCreate only after ModData init.
    if not cache then
        local ready = BurdJournals.Server._modDataInitialized
        if (not ready) and BurdJournals.Server.ensureBaselineModDataReady then
            ready = BurdJournals.Server.ensureBaselineModDataReady(false)
        end
        if not ready or not hasModDataApi or type(ModData.getOrCreate) ~= "function" then
            bsjWriteLogLine("[BurdJournals] WARNING: getBaselineCache called before ModData initialized or without global ModData access - returning temporary non-persistent cache")
            return { players = {} }
        end
        cache = ModData.getOrCreate(BurdJournals.Server.BASELINE_CACHE_MODDATA_KEY)
    end
    
    -- Initialize players table if missing (but preserve existing data)
    if not cache.players then
        cache.players = {}
        -- Mark this as a newly created cache
        cache._createdAt = getGameTime and getGameTime():getWorldAgeHours() or 0
        cache._version = 1
        BurdJournals.debugPrint("[BurdJournals] Created new baseline cache (no existing data found)")
    elseif not BurdJournals.Server._baselineCacheLogged then
        -- Log once per session to help debug persistence issues
        local playerCount = 0
        local debugModifiedCount = 0
        for id, data in pairs(cache.players) do 
            playerCount = playerCount + 1
            if data.debugModified then
                debugModifiedCount = debugModifiedCount + 1
            end
        end
        BurdJournals.debugPrint("[BurdJournals] Loaded existing baseline cache: " .. playerCount .. " player(s), " .. debugModifiedCount .. " debug-modified")
        BurdJournals.Server._baselineCacheLogged = true
    end
    
    -- Store reference to avoid re-creating
    BurdJournals.Server._baselineCacheInstance = cache
    
    return cache
end

function BurdJournals.Server.getBaselineArchive()
    if BurdJournals.Server._baselineArchiveInstance then
        return BurdJournals.Server._baselineArchiveInstance
    end

    local archive = nil
    local hasModDataApi = type(ModData) == "table"
    if hasModDataApi and ModData.get then
        archive = ModData.get(BurdJournals.Server.BASELINE_ARCHIVE_MODDATA_KEY)
    end

    if not archive then
        local ready = BurdJournals.Server._modDataInitialized
        if (not ready) and BurdJournals.Server.ensureBaselineModDataReady then
            ready = BurdJournals.Server.ensureBaselineModDataReady(false)
        end
        if not ready or not hasModDataApi or type(ModData.getOrCreate) ~= "function" then
            bsjWriteLogLine("[BurdJournals] WARNING: getBaselineArchive called before ModData initialized or without global ModData access - returning temporary non-persistent archive")
            return { byCharacterId = {}, bySteamId = {}, _version = 1 }
        end
        archive = ModData.getOrCreate(BurdJournals.Server.BASELINE_ARCHIVE_MODDATA_KEY)
    end

    if type(archive.byCharacterId) ~= "table" then
        archive.byCharacterId = {}
    end
    if type(archive.bySteamId) ~= "table" then
        archive.bySteamId = {}
    end
    archive._version = tonumber(archive._version) or 1
    archive._updatedAt = getGameTime and getGameTime():getWorldAgeHours() or 0

    BurdJournals.Server._baselineArchiveInstance = archive
    return archive
end

function BurdJournals.Server.baselineSnapshotNowHours()
    return (getGameTime and getGameTime():getWorldAgeHours()) or 0
end

function BurdJournals.Server.baselineSnapshotNowEpochMs()
    local ts = getTimestampMs and tonumber(getTimestampMs()) or nil
    if ts and ts > 0 then
        return math.floor(ts)
    end
    local unix = os and os.time and tonumber(os.time()) or 0
    return math.floor(math.max(0, unix) * 1000)
end

function BurdJournals.Server.baselineSnapshotFormatEpochMs(epochMs, utc)
    local ms = tonumber(epochMs)
    if not ms or ms <= 0 then
        return nil
    end
    if not (os and os.date) then
        return tostring(math.floor(ms))
    end
    local seconds = math.floor(ms / 1000)
    local format = utc and "!%Y-%m-%dT%H:%M:%SZ" or "%Y-%m-%d %H:%M:%S %Z"
    local ok, formatted = pcall(os.date, format, seconds)
    if ok and formatted and formatted ~= "" then
        return tostring(formatted)
    end
    return tostring(math.floor(ms))
end

function BurdJournals.Server.normalizeSnapshotString(value, maxLen)
    if value == nil then
        return nil
    end
    local s = tostring(value)
    if s == "" then
        return nil
    end
    local limit = math.max(8, tonumber(maxLen) or 128)
    if string.len(s) > limit then
        s = string.sub(s, 1, limit)
    end
    return s
end

function BurdJournals.Server.ensureSnapshotIndexList(indexMap, key)
    if type(indexMap) ~= "table" or not key then
        return nil
    end
    local keyStr = tostring(key)
    if type(indexMap[keyStr]) ~= "table" then
        indexMap[keyStr] = {}
    end
    return indexMap[keyStr], keyStr
end

function BurdJournals.Server.snapshotListContains(list, snapshotId)
    if type(list) ~= "table" or not snapshotId then
        return false
    end
    local id = tostring(snapshotId)
    for i = 1, #list do
        if tostring(list[i]) == id then
            return true
        end
    end
    return false
end

function BurdJournals.Server.snapshotRemoveFromIndex(indexMap, key, snapshotId)
    if type(indexMap) ~= "table" or not key or not snapshotId then
        return false
    end
    local list = indexMap[tostring(key)]
    if type(list) ~= "table" then
        return false
    end
    local removed = false
    local id = tostring(snapshotId)
    for i = #list, 1, -1 do
        if tostring(list[i]) == id then
            table.remove(list, i)
            removed = true
        end
    end
    if #list == 0 then
        indexMap[tostring(key)] = nil
    end
    return removed
end

function BurdJournals.Server.sanitizeBaselineSnapshotRecord(record)
    local source = type(record) == "table" and record or {}
    local nowHours = BurdJournals.Server.baselineSnapshotNowHours()
    local payload = BurdJournals.sanitizeBaselinePayloadForSnapshot
        and BurdJournals.sanitizeBaselinePayloadForSnapshot(source)
        or {
            skillBaseline = BurdJournals.Server.copyBaselineTableEntries(source.skillBaseline),
            skillExportBaseline = BurdJournals.Server.copyBaselineTableEntries(source.skillExportBaseline),
            mediaSkillBaseline = BurdJournals.Server.copyBaselineTableEntries(source.mediaSkillBaseline),
            mediaSkillExportBaseline = BurdJournals.Server.copyBaselineTableEntries(source.mediaSkillExportBaseline),
            traitBaseline = BurdJournals.Server.copyBaselineTableEntries(source.traitBaseline),
            traitExportBaseline = BurdJournals.Server.copyBaselineTableEntries(source.traitExportBaseline),
            recipeBaseline = BurdJournals.Server.copyBaselineTableEntries(source.recipeBaseline),
            recipeExportBaseline = BurdJournals.Server.copyBaselineTableEntries(source.recipeExportBaseline),
        }
    local counts = BurdJournals.getBaselineSnapshotCounts and BurdJournals.getBaselineSnapshotCounts(payload) or {
        skills = BurdJournals.countTable(payload.skillBaseline),
        mediaSkills = BurdJournals.countTable(payload.mediaSkillBaseline),
        traits = BurdJournals.countTable(payload.traitBaseline),
        recipes = BurdJournals.countTable(payload.recipeBaseline),
    }

    local capturedAtHours = tonumber(source.capturedAtHours or source.capturedAt) or nowHours
    local endedAtHours = tonumber(source.endedAtHours or source.endedAt) or nil
    if endedAtHours and endedAtHours < capturedAtHours then
        endedAtHours = capturedAtHours
    end

    local capturedAtEpochMs = tonumber(
        source.capturedAtEpochMs
        or source.capturedAtRealEpochMs
        or source.capturedAtUnixMs
    ) or nil
    local endedAtEpochMs = tonumber(
        source.endedAtEpochMs
        or source.endedAtRealEpochMs
        or source.endedAtUnixMs
    ) or nil
    if capturedAtEpochMs and capturedAtEpochMs <= 0 then
        capturedAtEpochMs = nil
    end
    if endedAtEpochMs and endedAtEpochMs <= 0 then
        endedAtEpochMs = nil
    end
    if endedAtEpochMs and capturedAtEpochMs and endedAtEpochMs < capturedAtEpochMs then
        endedAtEpochMs = capturedAtEpochMs
    end

    local capturedAtIsoUtc = BurdJournals.Server.normalizeSnapshotString(source.capturedAtIsoUtc, 64)
        or (capturedAtEpochMs and BurdJournals.Server.baselineSnapshotFormatEpochMs(capturedAtEpochMs, true))
    local capturedAtLocal = BurdJournals.Server.normalizeSnapshotString(source.capturedAtLocal, 96)
        or (capturedAtEpochMs and BurdJournals.Server.baselineSnapshotFormatEpochMs(capturedAtEpochMs, false))
    local endedAtIsoUtc = BurdJournals.Server.normalizeSnapshotString(source.endedAtIsoUtc, 64)
        or (endedAtEpochMs and BurdJournals.Server.baselineSnapshotFormatEpochMs(endedAtEpochMs, true))
    local endedAtLocal = BurdJournals.Server.normalizeSnapshotString(source.endedAtLocal, 96)
        or (endedAtEpochMs and BurdJournals.Server.baselineSnapshotFormatEpochMs(endedAtEpochMs, false))

    return {
        snapshotId = BurdJournals.Server.normalizeSnapshotString(source.snapshotId, 196),
        steamId = BurdJournals.Server.normalizeSnapshotString(source.steamId, 96),
        characterId = BurdJournals.Server.normalizeSnapshotString(source.characterId, 160),
        characterName = BurdJournals.Server.normalizeSnapshotString(source.characterName, 160),
        username = BurdJournals.Server.normalizeSnapshotString(source.username, 96),
        capturedAtHours = capturedAtHours,
        capturedAtEpochMs = capturedAtEpochMs,
        capturedAtIsoUtc = capturedAtIsoUtc,
        capturedAtLocal = capturedAtLocal,
        endedAtHours = endedAtHours,
        endedAtEpochMs = endedAtEpochMs,
        endedAtIsoUtc = endedAtIsoUtc,
        endedAtLocal = endedAtLocal,
        endedReason = BurdJournals.Server.normalizeSnapshotString(source.endedReason, 64),
        source = BurdJournals.Server.normalizeSnapshotString(source.source, 64) or "unknown",
        note = BurdJournals.Server.normalizeSnapshotString(source.note, 256),
        isProtected = source.isProtected == true,
        debugModified = source.debugModified == true,
        skillBaseline = payload.skillBaseline or {},
        mediaSkillBaseline = payload.mediaSkillBaseline or {},
        traitBaseline = payload.traitBaseline or {},
        recipeBaseline = payload.recipeBaseline or {},
        counts = {
            skills = math.max(0, tonumber(counts.skills) or 0),
            mediaSkills = math.max(0, tonumber(counts.mediaSkills) or 0),
            traits = math.max(0, tonumber(counts.traits) or 0),
            recipes = math.max(0, tonumber(counts.recipes) or 0),
        },
    }
end

function BurdJournals.Server.snapshotHasEntries(record)
    if type(record) ~= "table" then
        return false
    end
    if BurdJournals.baselineHasEntries then
        return BurdJournals.baselineHasEntries(record)
    end
    return false
end

function BurdJournals.Server.getSnapshotHistoryLimit()
    local configured = tonumber(BurdJournals.Server.PLAYER_BASELINE_SNAPSHOT_HISTORY_LIMIT) or 25
    local sandboxLimit = BurdJournals.getBaselineSnapshotsPerSteamLimit and BurdJournals.getBaselineSnapshotsPerSteamLimit() or configured
    configured = math.max(1, math.min(100, math.floor(configured)))
    sandboxLimit = math.max(1, math.min(500, math.floor(tonumber(sandboxLimit) or configured)))
    return math.max(1, math.min(configured, sandboxLimit))
end

function BurdJournals.Server.normalizeSnapshotHistoryMap(history)
    local normalized = {}
    if type(history) ~= "table" then
        return normalized
    end
    local resetEpochHours = 0
    if BurdJournals.Server.getBaselineCache then
        local cache = BurdJournals.Server.getBaselineCache()
        resetEpochHours = cache and tonumber(cache._backupResetEpochHours) or 0
    end

    for key, value in pairs(history) do
        local cleaned = BurdJournals.Server.sanitizeBaselineSnapshotRecord(value)
        local snapshotId = cleaned.snapshotId or BurdJournals.Server.normalizeSnapshotString(key, 196)
        local historyEpochHours = tonumber(value and (value.endedAtHours or value.endedAt or value.capturedAtHours or value.capturedAt)) or 0
        if historyEpochHours <= 0 then
            historyEpochHours = tonumber(cleaned.endedAtHours or cleaned.capturedAtHours or cleaned.capturedAt) or 0
        end
        if snapshotId
            and BurdJournals.Server.snapshotHasEntries(cleaned)
            and (resetEpochHours <= 0 or historyEpochHours <= 0 or historyEpochHours >= resetEpochHours)
        then
            cleaned.snapshotId = snapshotId
            normalized[snapshotId] = cleaned
        end
    end
    return normalized
end

function BurdJournals.Server.pruneSnapshotHistoryMap(history, limit)
    local maxEntries = math.max(1, math.floor(tonumber(limit) or BurdJournals.Server.getSnapshotHistoryLimit()))
    local entries = {}
    for snapshotId, record in pairs(history or {}) do
        if type(record) == "table" then
            entries[#entries + 1] = {
                snapshotId = tostring(snapshotId),
                record = record,
                capturedAtHours = tonumber(record.capturedAtHours) or 0,
            }
        end
    end
    table.sort(entries, function(a, b)
        if a.capturedAtHours == b.capturedAtHours then
            return tostring(a.snapshotId) > tostring(b.snapshotId)
        end
        return a.capturedAtHours > b.capturedAtHours
    end)

    local pruned = {}
    local kept = 0
    for i = 1, #entries do
        if kept >= maxEntries then
            break
        end
        local entry = entries[i]
        pruned[entry.snapshotId] = entry.record
        kept = kept + 1
    end
    return pruned
end

function BurdJournals.Server.getPlayerSnapshotHistoryMap(player)
    if not player or not player.getModData then
        return {}
    end
    local modData = player:getModData()
    if type(modData) ~= "table" then
        return {}
    end
    modData.BurdJournals = modData.BurdJournals or {}
    local historyKey = BurdJournals.Server.PLAYER_BASELINE_SNAPSHOT_HISTORY_KEY or "serverBaselineSnapshotHistory"
    local raw = modData.BurdJournals[historyKey]
    local normalized = BurdJournals.Server.normalizeSnapshotHistoryMap(raw)
    modData.BurdJournals[historyKey] = BurdJournals.Server.pruneSnapshotHistoryMap(normalized, BurdJournals.Server.getSnapshotHistoryLimit())
    return modData.BurdJournals[historyKey]
end

function BurdJournals.Server.persistSnapshotToPlayerHistory(player, snapshotRecord, skipTransmit)
    if not player or not player.getModData or type(snapshotRecord) ~= "table" then
        return false
    end

    local cleaned = BurdJournals.Server.sanitizeBaselineSnapshotRecord(snapshotRecord)
    if not BurdJournals.Server.snapshotHasEntries(cleaned) then
        return false
    end
    if not cleaned.snapshotId then
        return false
    end

    local history = BurdJournals.Server.getPlayerSnapshotHistoryMap(player)
    history[cleaned.snapshotId] = cleaned
    local pruned = BurdJournals.Server.pruneSnapshotHistoryMap(history, BurdJournals.Server.getSnapshotHistoryLimit())
    local modData = player:getModData()
    modData.BurdJournals = modData.BurdJournals or {}
    modData.BurdJournals[BurdJournals.Server.PLAYER_BASELINE_SNAPSHOT_HISTORY_KEY] = pruned

    if not skipTransmit and player.transmitModData then
        player:transmitModData()
    end
    return true
end

function BurdJournals.Server.mergeBaselineSnapshotsFromPlayerHistory(player, skipTransmit)
    if not player or not player.getModData then
        return 0
    end
    if BurdJournals.Server.isBaselineModDataReady and not BurdJournals.Server.isBaselineModDataReady() then
        return 0
    end

    local history = BurdJournals.Server.getPlayerSnapshotHistoryMap(player)
    if type(history) ~= "table" then
        return 0
    end
    local store = BurdJournals.Server.getBaselineSnapshotStore and BurdJournals.Server.getBaselineSnapshotStore() or nil
    if type(store) ~= "table" then
        return 0
    end

    local merged = 0
    local touchedSteamIds = {}
    for snapshotId, rawRecord in pairs(history) do
        local cleaned = BurdJournals.Server.sanitizeBaselineSnapshotRecord(rawRecord)
        cleaned.snapshotId = BurdJournals.Server.normalizeSnapshotString(snapshotId, 196) or cleaned.snapshotId
        if cleaned.snapshotId and BurdJournals.Server.snapshotHasEntries(cleaned) and not store.bySnapshotId[cleaned.snapshotId] then
            store.bySnapshotId[cleaned.snapshotId] = cleaned
            if cleaned.steamId then
                local steamList = BurdJournals.Server.ensureSnapshotIndexList(store.bySteamId, cleaned.steamId)
                if steamList and not BurdJournals.Server.snapshotListContains(steamList, cleaned.snapshotId) then
                    steamList[#steamList + 1] = cleaned.snapshotId
                end
                touchedSteamIds[tostring(cleaned.steamId)] = true
            end
            if cleaned.characterId then
                local characterList = BurdJournals.Server.ensureSnapshotIndexList(store.byCharacterId, cleaned.characterId)
                if characterList and not BurdJournals.Server.snapshotListContains(characterList, cleaned.snapshotId) then
                    characterList[#characterList + 1] = cleaned.snapshotId
                end
            end
            if not cleaned.endedAtHours then
                if cleaned.steamId then
                    store.activeBySteamId[tostring(cleaned.steamId)] = cleaned.snapshotId
                end
                if cleaned.characterId then
                    store.activeByCharacterId[tostring(cleaned.characterId)] = cleaned.snapshotId
                end
            end
            merged = merged + 1
        end
    end

    if merged > 0 then
        store._updatedAt = BurdJournals.Server.baselineSnapshotNowHours()
        for steamId in pairs(touchedSteamIds) do
            BurdJournals.Server.pruneBaselineSnapshotsForSteamId(steamId, nil, true)
        end
        local playerName = player and player.getUsername and player:getUsername() or "unknown"
        BurdJournals.debugPrint("[BurdJournals] Recovered " .. tostring(merged)
            .. " baseline snapshot(s) from player history for " .. tostring(playerName))
        if not skipTransmit and BurdJournals.Server.transmitBaselineSnapshotStore then
            BurdJournals.Server.transmitBaselineSnapshotStore()
        end
    end

    return merged
end

function BurdJournals.Server.clearPlayerBaselineSnapshotHistory(player, skipTransmit)
    if not player or not player.getModData then
        return 0
    end
    local modData = player:getModData()
    if type(modData) ~= "table" then
        return 0
    end
    local bj = modData.BurdJournals
    if type(bj) ~= "table" then
        return 0
    end
    local historyKey = BurdJournals.Server.PLAYER_BASELINE_SNAPSHOT_HISTORY_KEY or "serverBaselineSnapshotHistory"
    local history = bj[historyKey]
    local removed = 0
    if type(history) == "table" then
        for _ in pairs(history) do
            removed = removed + 1
        end
    elseif history ~= nil then
        removed = 1
    end
    bj[historyKey] = nil
    if removed > 0 and not skipTransmit and player.transmitModData then
        player:transmitModData()
    end
    return removed
end

function BurdJournals.Server.purgeBaselineSnapshotsForIdentity(steamId, characterId, skipTransmit)
    local normalizedSteamId = BurdJournals.Server.normalizeSnapshotString(steamId, 96)
    local normalizedCharacterId = BurdJournals.Server.normalizeSnapshotString(characterId, 160)
    if not normalizedSteamId and not normalizedCharacterId then
        return 0
    end
    if BurdJournals.Server.isBaselineModDataReady and not BurdJournals.Server.isBaselineModDataReady() then
        return 0
    end

    local store = BurdJournals.Server.getBaselineSnapshotStore and BurdJournals.Server.getBaselineSnapshotStore() or nil
    if type(store) ~= "table" or type(store.bySnapshotId) ~= "table" then
        return 0
    end

    local toDelete = {}
    for snapshotId, record in pairs(store.bySnapshotId) do
        if type(record) == "table" then
            local matches = false
            if normalizedCharacterId and tostring(record.characterId or "") == normalizedCharacterId then
                matches = true
            end
            if normalizedSteamId and tostring(record.steamId or "") == normalizedSteamId then
                matches = true
            end
            if matches then
                toDelete[#toDelete + 1] = tostring(snapshotId)
            end
        end
    end

    local removed = 0
    for i = 1, #toDelete do
        if BurdJournals.Server.deleteBaselineSnapshot(toDelete[i], true) then
            removed = removed + 1
        end
    end

    if removed > 0 then
        store._updatedAt = BurdJournals.Server.baselineSnapshotNowHours()
        if not skipTransmit and BurdJournals.Server.transmitBaselineSnapshotStore then
            BurdJournals.Server.transmitBaselineSnapshotStore()
        end
    end
    return removed
end

function BurdJournals.Server.snapshotStoreCount(bySnapshotId)
    if type(bySnapshotId) ~= "table" then
        return 0
    end
    local count = 0
    for _ in pairs(bySnapshotId) do
        count = count + 1
    end
    return count
end

function BurdJournals.Server.buildSanitizedSnapshotStoreCopy(sourceStore)
    local copy = {
        _version = tonumber(sourceStore and sourceStore._version)
            or tonumber(BurdJournals.BASELINE_SNAPSHOT_SCHEMA_VERSION)
            or 1,
        _updatedAt = BurdJournals.Server.baselineSnapshotNowHours(),
        bySnapshotId = {},
        bySteamId = {},
        byCharacterId = {},
        activeBySteamId = {},
        activeByCharacterId = {},
    }

    local sourceBySnapshotId = sourceStore and sourceStore.bySnapshotId
    if type(sourceBySnapshotId) == "table" then
        for _, record in pairs(sourceBySnapshotId) do
            local cleaned = BurdJournals.Server.sanitizeBaselineSnapshotRecord(record)
            local cleanedId = cleaned and cleaned.snapshotId
            if cleanedId then
                copy.bySnapshotId[cleanedId] = cleaned

                if cleaned.steamId then
                    local steamList = BurdJournals.Server.ensureSnapshotIndexList(copy.bySteamId, cleaned.steamId)
                    if steamList and not BurdJournals.Server.snapshotListContains(steamList, cleanedId) then
                        steamList[#steamList + 1] = cleanedId
                    end
                end
                if cleaned.characterId then
                    local characterList = BurdJournals.Server.ensureSnapshotIndexList(copy.byCharacterId, cleaned.characterId)
                    if characterList and not BurdJournals.Server.snapshotListContains(characterList, cleanedId) then
                        characterList[#characterList + 1] = cleanedId
                    end
                end
            end
        end
    end

    local sourceActiveSteam = sourceStore and sourceStore.activeBySteamId
    if type(sourceActiveSteam) == "table" then
        for steamId, snapshotId in pairs(sourceActiveSteam) do
            local id = BurdJournals.Server.normalizeSnapshotString(snapshotId, 196)
            local key = BurdJournals.Server.normalizeSnapshotString(steamId, 96)
            if key and id and copy.bySnapshotId[id] then
                copy.activeBySteamId[key] = id
            end
        end
    end

    local sourceActiveCharacter = sourceStore and sourceStore.activeByCharacterId
    if type(sourceActiveCharacter) == "table" then
        for characterId, snapshotId in pairs(sourceActiveCharacter) do
            local id = BurdJournals.Server.normalizeSnapshotString(snapshotId, 196)
            local key = BurdJournals.Server.normalizeSnapshotString(characterId, 160)
            if key and id and copy.bySnapshotId[id] then
                copy.activeByCharacterId[key] = id
            end
        end
    end

    return copy
end

function BurdJournals.Server.restoreSnapshotStoreFromArchiveMirror(store)
    if type(store) ~= "table" then
        return 0
    end
    local archive = BurdJournals.Server.getBaselineArchive and BurdJournals.Server.getBaselineArchive() or nil
    local mirrorField = BurdJournals.Server.BASELINE_SNAPSHOT_ARCHIVE_MIRROR_FIELD or "baselineSnapshotsMirrorV1"
    local legacyMirrorField = BurdJournals.Server.BASELINE_SNAPSHOT_ARCHIVE_MIRROR_FIELD_LEGACY
        or "_baselineSnapshotsMirrorV1"
    local mirror = archive and archive[mirrorField] or nil
    if type(mirror) ~= "table" then
        mirror = archive and archive[legacyMirrorField] or nil
    end
    if type(mirror) ~= "table" then
        return 0
    end

    local liveCopy = BurdJournals.Server.buildSanitizedSnapshotStoreCopy(store)
    local mirrorCopy = BurdJournals.Server.buildSanitizedSnapshotStoreCopy(mirror)
    local liveCount = BurdJournals.Server.snapshotStoreCount(liveCopy.bySnapshotId)
    local mirrorCount = BurdJournals.Server.snapshotStoreCount(mirrorCopy.bySnapshotId)
    if mirrorCount <= liveCount then
        return 0
    end

    local maxSnapshots = BurdJournals.getBaselineSnapshotsPerSteamLimit and BurdJournals.getBaselineSnapshotsPerSteamLimit() or 50
    local suspiciousShrink = (liveCount <= 1) and (mirrorCount > liveCount) and (tonumber(maxSnapshots) or 50) > 1
    if not suspiciousShrink then
        return 0
    end

    local recovered = mirrorCopy
    for snapshotId, record in pairs(liveCopy.bySnapshotId or {}) do
        recovered.bySnapshotId[snapshotId] = record
    end
    recovered = BurdJournals.Server.buildSanitizedSnapshotStoreCopy(recovered)
    local recoveredCount = BurdJournals.Server.snapshotStoreCount(recovered.bySnapshotId)
    if recoveredCount <= liveCount then
        return 0
    end

    store._version = tonumber(recovered._version) or tonumber(BurdJournals.BASELINE_SNAPSHOT_SCHEMA_VERSION) or 1
    store._updatedAt = BurdJournals.Server.baselineSnapshotNowHours()
    store.bySnapshotId = recovered.bySnapshotId or {}
    store.bySteamId = recovered.bySteamId or {}
    store.byCharacterId = recovered.byCharacterId or {}
    store.activeBySteamId = recovered.activeBySteamId or {}
    store.activeByCharacterId = recovered.activeByCharacterId or {}
    return recoveredCount - liveCount
end

function BurdJournals.Server.syncBaselineSnapshotMirrorToArchive(sourceStore, skipTransmit)
    if BurdJournals.Server.isBaselineModDataReady and not BurdJournals.Server.isBaselineModDataReady() then
        return false
    end
    local store = type(sourceStore) == "table" and sourceStore or BurdJournals.Server._baselineSnapshotInstance
    if type(store) ~= "table" then
        return false
    end

    local archive = BurdJournals.Server.getBaselineArchive and BurdJournals.Server.getBaselineArchive() or nil
    if type(archive) ~= "table" then
        return false
    end

    local mirrorField = BurdJournals.Server.BASELINE_SNAPSHOT_ARCHIVE_MIRROR_FIELD or "baselineSnapshotsMirrorV1"
    local legacyMirrorField = BurdJournals.Server.BASELINE_SNAPSHOT_ARCHIVE_MIRROR_FIELD_LEGACY
        or "_baselineSnapshotsMirrorV1"
    local incoming = BurdJournals.Server.buildSanitizedSnapshotStoreCopy(store)
    local existing = BurdJournals.Server.buildSanitizedSnapshotStoreCopy(archive[mirrorField] or archive[legacyMirrorField])
    local incomingCount = BurdJournals.Server.snapshotStoreCount(incoming.bySnapshotId)
    local existingCount = BurdJournals.Server.snapshotStoreCount(existing.bySnapshotId)
    local maxSnapshots = BurdJournals.getBaselineSnapshotsPerSteamLimit and BurdJournals.getBaselineSnapshotsPerSteamLimit() or 50
    local suspiciousShrink = (incomingCount <= 1) and (existingCount > incomingCount) and (tonumber(maxSnapshots) or 50) > 1
    if suspiciousShrink then
        for snapshotId, record in pairs(incoming.bySnapshotId or {}) do
            existing.bySnapshotId[snapshotId] = record
        end
        incoming = BurdJournals.Server.buildSanitizedSnapshotStoreCopy(existing)
        BurdJournals.debugPrint("[BurdJournals] Preserved snapshot archive mirror history during suspicious shrink (incoming="
            .. tostring(incomingCount) .. ", existing=" .. tostring(existingCount) .. ")")
    end

    archive[mirrorField] = incoming
    -- Drop the legacy duplicate: serializing the same store under two fields doubled
    -- the archive's save size. Readers already fall back to the legacy field for old saves.
    archive[legacyMirrorField] = nil
    archive._updatedAt = BurdJournals.Server.baselineSnapshotNowHours()

    if not skipTransmit and BurdJournals.Server.transmitBaselineStores then
        BurdJournals.Server.transmitBaselineStores(true)
    end
    return true
end

function BurdJournals.Server.getBaselineSnapshotStore()
    if BurdJournals.Server._baselineSnapshotInstance then
        return BurdJournals.Server._baselineSnapshotInstance
    end

    local store = nil
    if ModData.get then
        store = ModData.get(BurdJournals.Server.BASELINE_SNAPSHOT_MODDATA_KEY)
    end
    if not store then
        local ready = BurdJournals.Server._modDataInitialized
        if (not ready) and BurdJournals.Server.ensureBaselineModDataReady then
            ready = BurdJournals.Server.ensureBaselineModDataReady(false)
        end
        if not ready then
            bsjWriteLogLine("[BurdJournals] WARNING: getBaselineSnapshotStore called before ModData initialized - returning temporary non-persistent store")
            return {
                _version = BurdJournals.BASELINE_SNAPSHOT_SCHEMA_VERSION or 1,
                _updatedAt = BurdJournals.Server.baselineSnapshotNowHours(),
                bySnapshotId = {},
                bySteamId = {},
                byCharacterId = {},
                activeBySteamId = {},
                activeByCharacterId = {},
            }
        end
        store = ModData.getOrCreate(BurdJournals.Server.BASELINE_SNAPSHOT_MODDATA_KEY)
    end

    store._version = tonumber(store._version) or tonumber(BurdJournals.BASELINE_SNAPSHOT_SCHEMA_VERSION) or 1
    store._updatedAt = tonumber(store._updatedAt) or BurdJournals.Server.baselineSnapshotNowHours()
    if type(store.bySnapshotId) ~= "table" then store.bySnapshotId = {} end
    if type(store.bySteamId) ~= "table" then store.bySteamId = {} end
    if type(store.byCharacterId) ~= "table" then store.byCharacterId = {} end
    if type(store.activeBySteamId) ~= "table" then store.activeBySteamId = {} end
    if type(store.activeByCharacterId) ~= "table" then store.activeByCharacterId = {} end
    local recoveredCount = BurdJournals.Server.restoreSnapshotStoreFromArchiveMirror(store)
    if recoveredCount > 0 then
        BurdJournals.debugPrint("[BurdJournals] Recovered " .. tostring(recoveredCount)
            .. " baseline snapshot(s) from archive mirror")
        -- No transmit: clients never read the snapshot store, and the recovered
        -- table persists with the world save on its own.
    end

    BurdJournals.Server._baselineSnapshotInstance = store
    return store
end

function BurdJournals.Server.createBaselineSnapshotId(steamId, characterId, nowHours)
    local sSteam = BurdJournals.Server.normalizeSnapshotString(steamId, 64) or "nosteam"
    local sCharacter = BurdJournals.Server.normalizeSnapshotString(characterId, 96) or "unknown"
    local stamp = math.max(0, math.floor((tonumber(nowHours) or BurdJournals.Server.baselineSnapshotNowHours()) * 1000))
    local nonce = (ZombRand and ZombRand(1000000)) or math.floor((os.time() or 0) % 1000000)
    return tostring(sSteam) .. ":" .. tostring(sCharacter) .. ":" .. tostring(stamp) .. ":" .. tostring(nonce)
end

function BurdJournals.Server.baselineSnapshotDedupeKey(steamId, characterId, capturedAtHours, source)
    return tostring(steamId or "")
        .. "|" .. tostring(characterId or "")
        .. "|" .. tostring(math.max(0, math.floor((tonumber(capturedAtHours) or 0) * 1000)))
        .. "|" .. tostring(source or "")
end

function BurdJournals.Server.compareBaselineNumberMap(left, right)
    local l = type(left) == "table" and left or {}
    local r = type(right) == "table" and right or {}
    for key, value in pairs(l) do
        local a = tonumber(value) or 0
        local b = tonumber(r[key]) or 0
        if math.abs(a - b) > 0.0001 then
            return false
        end
    end
    for key, value in pairs(r) do
        local a = tonumber(value) or 0
        local b = tonumber(l[key]) or 0
        if math.abs(a - b) > 0.0001 then
            return false
        end
    end
    return true
end

function BurdJournals.Server.compareBaselineBoolMap(left, right)
    local l = type(left) == "table" and left or {}
    local r = type(right) == "table" and right or {}
    for key, value in pairs(l) do
        local a = value == true
        local b = r[key] == true
        if a ~= b then
            return false
        end
    end
    for key, value in pairs(r) do
        local a = value == true
        local b = l[key] == true
        if a ~= b then
            return false
        end
    end
    return true
end

function BurdJournals.Server.areBaselinePayloadsEquivalent(left, right)
    return BurdJournals.Server.compareBaselineNumberMap(left and left.skillBaseline, right and right.skillBaseline)
        and BurdJournals.Server.compareBaselineNumberMap(left and left.skillExportBaseline, right and right.skillExportBaseline)
        and BurdJournals.Server.compareBaselineNumberMap(left and left.mediaSkillBaseline, right and right.mediaSkillBaseline)
        and BurdJournals.Server.compareBaselineNumberMap(left and left.mediaSkillExportBaseline, right and right.mediaSkillExportBaseline)
        and BurdJournals.Server.compareBaselineBoolMap(left and left.traitBaseline, right and right.traitBaseline)
        and BurdJournals.Server.compareBaselineBoolMap(left and left.traitExportBaseline, right and right.traitExportBaseline)
        and BurdJournals.Server.compareBaselineBoolMap(left and left.recipeBaseline, right and right.recipeBaseline)
        and BurdJournals.Server.compareBaselineBoolMap(left and left.recipeExportBaseline, right and right.recipeExportBaseline)
end

function BurdJournals.Server.shouldReplaceCachedBaselineForFreshCharacter(existingBaseline, newBaseline)
    if type(existingBaseline) ~= "table" or type(newBaseline) ~= "table" then
        return false
    end
    if existingBaseline.debugModified == true then
        return false
    end
    return not BurdJournals.Server.areBaselinePayloadsEquivalent(existingBaseline, newBaseline)
end

function BurdJournals.Server.getActiveBaselineSnapshotRecord(store, steamId, characterId)
    if type(store) ~= "table" or type(store.bySnapshotId) ~= "table" then
        return nil, nil
    end

    local function pickLatestOpenSnapshot(list)
        if type(list) ~= "table" then
            return nil, nil
        end
        local bestId, bestRecord, bestCaptured = nil, nil, -1
        for _, snapshotId in ipairs(list) do
            local record = snapshotId and store.bySnapshotId[snapshotId] or nil
            if type(record) == "table" and not tonumber(record.endedAtHours) then
                local captured = tonumber(record.capturedAtHours) or 0
                if (not bestRecord)
                    or captured > bestCaptured
                    or (captured == bestCaptured and tostring(snapshotId) > tostring(bestId))
                then
                    bestId = snapshotId
                    bestRecord = record
                    bestCaptured = captured
                end
            end
        end
        return bestId, bestRecord
    end

    local activeId = nil
    if characterId and type(store.activeByCharacterId) == "table" then
        activeId = store.activeByCharacterId[tostring(characterId)]
    end
    if (not activeId) and steamId and type(store.activeBySteamId) == "table" then
        activeId = store.activeBySteamId[tostring(steamId)]
    end
    if not activeId then
        return nil, nil
    end

    local record = store.bySnapshotId[activeId]
    if type(record) == "table" then
        return activeId, record
    end

    local byCharacter = characterId and type(store.byCharacterId) == "table" and store.byCharacterId[tostring(characterId)] or nil
    local fallbackId, fallbackRecord = pickLatestOpenSnapshot(byCharacter)
    if fallbackRecord then
        return fallbackId, fallbackRecord
    end
    local bySteam = steamId and type(store.bySteamId) == "table" and store.bySteamId[tostring(steamId)] or nil
    return pickLatestOpenSnapshot(bySteam)
end

function BurdJournals.Server.removeSnapshotFromIndexes(store, snapshotId, record)
    if type(store) ~= "table" or not snapshotId or type(record) ~= "table" then
        return
    end
    if record.steamId then
        BurdJournals.Server.snapshotRemoveFromIndex(store.bySteamId, record.steamId, snapshotId)
        if store.activeBySteamId[record.steamId] == snapshotId then
            store.activeBySteamId[record.steamId] = nil
        end
    end
    if record.characterId then
        BurdJournals.Server.snapshotRemoveFromIndex(store.byCharacterId, record.characterId, snapshotId)
        if store.activeByCharacterId[record.characterId] == snapshotId then
            store.activeByCharacterId[record.characterId] = nil
        end
    end
end

function BurdJournals.Server.deleteBaselineSnapshot(snapshotId, skipTransmit)
    local id = BurdJournals.Server.normalizeSnapshotString(snapshotId, 196)
    if not id then
        return false
    end
    local store = BurdJournals.Server.getBaselineSnapshotStore()
    local record = store.bySnapshotId[id]
    if type(record) ~= "table" then
        return false
    end
    BurdJournals.Server.removeSnapshotFromIndexes(store, id, record)
    store.bySnapshotId[id] = nil
    store._updatedAt = BurdJournals.Server.baselineSnapshotNowHours()
    if not skipTransmit then
        BurdJournals.Server.transmitBaselineSnapshotStore()
    end
    return true
end

function BurdJournals.Server.getBaselineSnapshot(snapshotId)
    local id = BurdJournals.Server.normalizeSnapshotString(snapshotId, 196)
    if not id then
        return nil
    end
    local store = BurdJournals.Server.getBaselineSnapshotStore()
    local record = store.bySnapshotId and store.bySnapshotId[id] or nil
    if type(record) ~= "table" then
        return nil
    end
    return BurdJournals.Server.sanitizeBaselineSnapshotRecord(record)
end

function BurdJournals.Server.pruneBaselineSnapshotsForSteamId(steamId, limit, skipTransmit)
    local steamKey = BurdJournals.Server.normalizeSnapshotString(steamId, 96)
    if not steamKey then
        return 0
    end
    local store = BurdJournals.Server.getBaselineSnapshotStore()
    local list = store.bySteamId and store.bySteamId[steamKey] or nil
    if type(list) ~= "table" then
        return 0
    end

    local maxSnapshots = tonumber(limit)
        or (BurdJournals.getBaselineSnapshotsPerSteamLimit and BurdJournals.getBaselineSnapshotsPerSteamLimit())
        or 50
    maxSnapshots = math.max(1, math.floor(maxSnapshots))

    if #list <= maxSnapshots then
        return 0
    end

    local activeSnapshotId = store.activeBySteamId and store.activeBySteamId[steamKey] or nil
    local sorted = {}
    for i = 1, #list do
        local snapshotId = list[i]
        local record = store.bySnapshotId[snapshotId]
        if type(record) == "table" then
            sorted[#sorted + 1] = {
                snapshotId = snapshotId,
                capturedAtHours = tonumber(record.capturedAtHours) or 0
            }
        end
    end
    table.sort(sorted, function(a, b)
        if a.capturedAtHours == b.capturedAtHours then
            return tostring(a.snapshotId) < tostring(b.snapshotId)
        end
        return a.capturedAtHours < b.capturedAtHours
    end)

    local removed = 0
    local overflow = math.max(0, #sorted - maxSnapshots)
    for i = 1, #sorted do
        if overflow <= 0 then
            break
        end
        local snapshotId = sorted[i].snapshotId
        if activeSnapshotId and snapshotId == activeSnapshotId then
            -- Preserve active pointer snapshot when pruning retention.
        else
            if BurdJournals.Server.deleteBaselineSnapshot(snapshotId, true) then
                removed = removed + 1
                overflow = overflow - 1
            end
        end
    end

    if removed > 0 and not skipTransmit then
        BurdJournals.Server.transmitBaselineSnapshotStore()
    end
    return removed
end

function BurdJournals.Server.saveBaselineSnapshot(record, options)
    options = type(options) == "table" and options or {}
    local force = options.force == true
    if not force and BurdJournals.isBaselineSnapshotsEnabled and not BurdJournals.isBaselineSnapshotsEnabled() then
        return false, nil, "disabled"
    end
    if BurdJournals.Server.isBaselineModDataReady and not BurdJournals.Server.isBaselineModDataReady() then
        BurdJournals.debugPrint("[BurdJournals] Snapshot save deferred: baseline ModData not ready")
        return false, nil, "moddata_not_ready"
    end

    local cleaned = BurdJournals.Server.sanitizeBaselineSnapshotRecord(record)
    if not BurdJournals.Server.snapshotHasEntries(cleaned) then
        return false, nil, "empty"
    end
    if not cleaned.snapshotId then
        cleaned.snapshotId = BurdJournals.Server.createBaselineSnapshotId(cleaned.steamId, cleaned.characterId, cleaned.capturedAtHours)
    end

    local store = BurdJournals.Server.getBaselineSnapshotStore()
    local previous = store.bySnapshotId[cleaned.snapshotId]
    if type(previous) == "table" then
        if not cleaned.capturedAtEpochMs then
            cleaned.capturedAtEpochMs = tonumber(previous.capturedAtEpochMs) or nil
        end
        if not cleaned.capturedAtIsoUtc then
            cleaned.capturedAtIsoUtc = BurdJournals.Server.normalizeSnapshotString(previous.capturedAtIsoUtc, 64)
        end
        if not cleaned.capturedAtLocal then
            cleaned.capturedAtLocal = BurdJournals.Server.normalizeSnapshotString(previous.capturedAtLocal, 96)
        end
        if not cleaned.endedAtEpochMs then
            cleaned.endedAtEpochMs = tonumber(previous.endedAtEpochMs) or nil
        end
        if not cleaned.endedAtIsoUtc then
            cleaned.endedAtIsoUtc = BurdJournals.Server.normalizeSnapshotString(previous.endedAtIsoUtc, 64)
        end
        if not cleaned.endedAtLocal then
            cleaned.endedAtLocal = BurdJournals.Server.normalizeSnapshotString(previous.endedAtLocal, 96)
        end
    end

    if not cleaned.capturedAtEpochMs then
        cleaned.capturedAtEpochMs = BurdJournals.Server.baselineSnapshotNowEpochMs()
    end
    if not cleaned.capturedAtIsoUtc then
        cleaned.capturedAtIsoUtc = BurdJournals.Server.baselineSnapshotFormatEpochMs(cleaned.capturedAtEpochMs, true)
    end
    if not cleaned.capturedAtLocal then
        cleaned.capturedAtLocal = BurdJournals.Server.baselineSnapshotFormatEpochMs(cleaned.capturedAtEpochMs, false)
    end
    if cleaned.endedAtHours then
        if not cleaned.endedAtEpochMs then
            cleaned.endedAtEpochMs = BurdJournals.Server.baselineSnapshotNowEpochMs()
        end
        if cleaned.endedAtEpochMs < cleaned.capturedAtEpochMs then
            cleaned.endedAtEpochMs = cleaned.capturedAtEpochMs
        end
        if not cleaned.endedAtIsoUtc then
            cleaned.endedAtIsoUtc = BurdJournals.Server.baselineSnapshotFormatEpochMs(cleaned.endedAtEpochMs, true)
        end
        if not cleaned.endedAtLocal then
            cleaned.endedAtLocal = BurdJournals.Server.baselineSnapshotFormatEpochMs(cleaned.endedAtEpochMs, false)
        end
    else
        cleaned.endedAtEpochMs = nil
        cleaned.endedAtIsoUtc = nil
        cleaned.endedAtLocal = nil
    end

    if type(previous) == "table" then
        BurdJournals.Server.removeSnapshotFromIndexes(store, cleaned.snapshotId, previous)
    end

    store.bySnapshotId[cleaned.snapshotId] = cleaned

    if cleaned.steamId then
        local steamList = BurdJournals.Server.ensureSnapshotIndexList(store.bySteamId, cleaned.steamId)
        if steamList and not BurdJournals.Server.snapshotListContains(steamList, cleaned.snapshotId) then
            steamList[#steamList + 1] = cleaned.snapshotId
        end
    end
    if cleaned.characterId then
        local characterList = BurdJournals.Server.ensureSnapshotIndexList(store.byCharacterId, cleaned.characterId)
        if characterList and not BurdJournals.Server.snapshotListContains(characterList, cleaned.snapshotId) then
            characterList[#characterList + 1] = cleaned.snapshotId
        end
    end

    local updateActive = options.updateActive ~= false
    if updateActive then
        if cleaned.endedAtHours then
            if cleaned.steamId and store.activeBySteamId[cleaned.steamId] == cleaned.snapshotId then
                store.activeBySteamId[cleaned.steamId] = nil
            end
            if cleaned.characterId and store.activeByCharacterId[cleaned.characterId] == cleaned.snapshotId then
                store.activeByCharacterId[cleaned.characterId] = nil
            end
        else
            if cleaned.steamId then
                store.activeBySteamId[cleaned.steamId] = cleaned.snapshotId
            end
            if cleaned.characterId then
                store.activeByCharacterId[cleaned.characterId] = cleaned.snapshotId
            end
        end
    end

    store._version = tonumber(store._version) or tonumber(BurdJournals.BASELINE_SNAPSHOT_SCHEMA_VERSION) or 1
    store._updatedAt = BurdJournals.Server.baselineSnapshotNowHours()

    local pruned = 0
    if cleaned.steamId and not options.skipPrune then
        pruned = BurdJournals.Server.pruneBaselineSnapshotsForSteamId(cleaned.steamId, options.limit, true)
    end

    if not options.skipTransmit then
        BurdJournals.Server.transmitBaselineSnapshotStore()
    end

    local historyPlayer = options.snapshotHistoryPlayer or options.player
    if historyPlayer and BurdJournals.Server.persistSnapshotToPlayerHistory then
        BurdJournals.Server.persistSnapshotToPlayerHistory(
            historyPlayer,
            cleaned,
            options.skipPlayerHistoryTransmit == true
        )
    end

    return true, cleaned.snapshotId, cleaned, pruned
end

function BurdJournals.Server.captureBaselineSnapshotForPlayer(player, characterId, baselineData, source, note, options)
    options = type(options) == "table" and options or {}
    local force = options.force == true
    if not force then
        if BurdJournals.isBaselineSnapshotsEnabled and not BurdJournals.isBaselineSnapshotsEnabled() then
            return false, nil, "disabled"
        end
        if BurdJournals.getBaselineSnapshotsAutoCaptureEnabled
            and not BurdJournals.getBaselineSnapshotsAutoCaptureEnabled()
        then
            return false, nil, "autocapture_disabled"
        end
    end

    local resolvedCharacterId = characterId
    if not resolvedCharacterId and player and BurdJournals.getPlayerCharacterId then
        resolvedCharacterId = BurdJournals.getPlayerCharacterId(player)
    end

    local baseline = type(baselineData) == "table" and baselineData or nil
    if type(baseline) ~= "table" and resolvedCharacterId then
        baseline = BurdJournals.Server.getCachedBaseline(resolvedCharacterId, player)
    end
    if type(baseline) ~= "table" then
        return false, nil, "missing_baseline"
    end

    local payload = BurdJournals.sanitizeBaselinePayloadForSnapshot
        and BurdJournals.sanitizeBaselinePayloadForSnapshot(baseline)
        or {
            skillBaseline = BurdJournals.Server.copyBaselineTableEntries(baseline.skillBaseline),
            skillExportBaseline = BurdJournals.Server.copyBaselineTableEntries(baseline.skillExportBaseline),
            mediaSkillBaseline = BurdJournals.Server.copyBaselineTableEntries(baseline.mediaSkillBaseline),
            mediaSkillExportBaseline = BurdJournals.Server.copyBaselineTableEntries(baseline.mediaSkillExportBaseline),
            traitBaseline = BurdJournals.Server.copyBaselineTableEntries(baseline.traitBaseline),
            traitExportBaseline = BurdJournals.Server.copyBaselineTableEntries(baseline.traitExportBaseline),
            recipeBaseline = BurdJournals.Server.copyBaselineTableEntries(baseline.recipeBaseline),
            recipeExportBaseline = BurdJournals.Server.copyBaselineTableEntries(baseline.recipeExportBaseline),
        }
    if not (BurdJournals.baselineHasEntries and BurdJournals.baselineHasEntries(payload)) then
        return false, nil, "empty"
    end

    local steamId = baseline.steamId
        or (player and BurdJournals.getPlayerSteamId and BurdJournals.getPlayerSteamId(player))
        or nil
    local username = baseline.username
        or (player and player.getUsername and player:getUsername())
        or nil
    local characterName = baseline.characterName
        or BurdJournals.Server.getPlayerCharacterDisplayName(player)
    local sourceTag = BurdJournals.Server.normalizeSnapshotString(source, 64) or "unknown"

    -- Prevent login/recovery spam: if the active snapshot payload is identical,
    -- skip auto register/recovery captures unless explicitly forced.
    if options.force ~= true and options.skipDuplicate ~= false then
        local store = BurdJournals.Server.getBaselineSnapshotStore and BurdJournals.Server.getBaselineSnapshotStore() or nil
        local activeSnapshotId, activeSnapshot = BurdJournals.Server.getActiveBaselineSnapshotRecord(store, steamId, resolvedCharacterId)
        if activeSnapshot and not tonumber(activeSnapshot.endedAtHours) then
            local activePayload = BurdJournals.sanitizeBaselinePayloadForSnapshot
                and BurdJournals.sanitizeBaselinePayloadForSnapshot(activeSnapshot)
                or {
                    skillBaseline = BurdJournals.Server.copyBaselineTableEntries(activeSnapshot.skillBaseline),
                    skillExportBaseline = BurdJournals.Server.copyBaselineTableEntries(activeSnapshot.skillExportBaseline),
                    mediaSkillBaseline = BurdJournals.Server.copyBaselineTableEntries(activeSnapshot.mediaSkillBaseline),
                    mediaSkillExportBaseline = BurdJournals.Server.copyBaselineTableEntries(activeSnapshot.mediaSkillExportBaseline),
                    traitBaseline = BurdJournals.Server.copyBaselineTableEntries(activeSnapshot.traitBaseline),
                    traitExportBaseline = BurdJournals.Server.copyBaselineTableEntries(activeSnapshot.traitExportBaseline),
                    recipeBaseline = BurdJournals.Server.copyBaselineTableEntries(activeSnapshot.recipeBaseline),
                    recipeExportBaseline = BurdJournals.Server.copyBaselineTableEntries(activeSnapshot.recipeExportBaseline),
                }
            if BurdJournals.Server.areBaselinePayloadsEquivalent(payload, activePayload) then
                if sourceTag == "request_recovery" or sourceTag == "register" then
                    return false, activeSnapshotId, "duplicate_active_payload"
                end
            end
        end
    end

    local capturedAtHours = tonumber(options.capturedAtHours)
        or tonumber(baseline.capturedAtHours)
        or tonumber(baseline.capturedAt)
        or BurdJournals.Server.baselineSnapshotNowHours()
    local capturedAtEpochMs = tonumber(options.capturedAtEpochMs)
        or tonumber(baseline.capturedAtEpochMs)
        or BurdJournals.Server.baselineSnapshotNowEpochMs()
    local capturedAtIsoUtc = BurdJournals.Server.normalizeSnapshotString(options.capturedAtIsoUtc, 64)
        or BurdJournals.Server.normalizeSnapshotString(baseline.capturedAtIsoUtc, 64)
        or BurdJournals.Server.baselineSnapshotFormatEpochMs(capturedAtEpochMs, true)
    local capturedAtLocal = BurdJournals.Server.normalizeSnapshotString(options.capturedAtLocal, 96)
        or BurdJournals.Server.normalizeSnapshotString(baseline.capturedAtLocal, 96)
        or BurdJournals.Server.baselineSnapshotFormatEpochMs(capturedAtEpochMs, false)

    local snapshotRecord = {
        steamId = steamId,
        characterId = resolvedCharacterId,
        characterName = characterName,
        username = username,
        capturedAtHours = capturedAtHours,
        capturedAtEpochMs = capturedAtEpochMs,
        capturedAtIsoUtc = capturedAtIsoUtc,
        capturedAtLocal = capturedAtLocal,
        source = sourceTag,
        note = note,
        isProtected = baseline.debugModified == true or options.isProtected == true,
        debugModified = baseline.debugModified == true,
        skillBaseline = payload.skillBaseline,
        mediaSkillBaseline = payload.mediaSkillBaseline,
        traitBaseline = payload.traitBaseline,
        recipeBaseline = payload.recipeBaseline,
    }

    if options.snapshotId then
        snapshotRecord.snapshotId = options.snapshotId
    end
    if options.endedAtHours then
        snapshotRecord.endedAtHours = tonumber(options.endedAtHours)
    end
    if options.endedReason then
        snapshotRecord.endedReason = tostring(options.endedReason)
    end

    local saveOptions = {}
    for key, value in pairs(options) do
        saveOptions[key] = value
    end
    if player then
        saveOptions.snapshotHistoryPlayer = player
    end
    return BurdJournals.Server.saveBaselineSnapshot(snapshotRecord, saveOptions)
end

function BurdJournals.Server.finalizeActiveBaselineSnapshot(characterId, player, endedReason, note, options)
    options = type(options) == "table" and options or {}
    if BurdJournals.isBaselineSnapshotsEnabled and not BurdJournals.isBaselineSnapshotsEnabled() then
        return false, nil
    end

    local store = BurdJournals.Server.getBaselineSnapshotStore()
    local resolvedCharacterId = characterId
        or (player and BurdJournals.getPlayerCharacterId and BurdJournals.getPlayerCharacterId(player))
    local steamId = player and BurdJournals.getPlayerSteamId and BurdJournals.getPlayerSteamId(player) or nil
    local snapshotId = nil
    if resolvedCharacterId then
        snapshotId = store.activeByCharacterId and store.activeByCharacterId[resolvedCharacterId] or nil
    end
    if not snapshotId and steamId then
        snapshotId = store.activeBySteamId and store.activeBySteamId[tostring(steamId)] or nil
    end
    if not snapshotId then
        return false, nil
    end

    local record = store.bySnapshotId and store.bySnapshotId[snapshotId] or nil
    if type(record) ~= "table" then
        return false, nil
    end
    if tonumber(record.endedAtHours) then
        return false, snapshotId
    end

    record.endedAtHours = tonumber(options.endedAtHours) or BurdJournals.Server.baselineSnapshotNowHours()
    record.endedAtEpochMs = tonumber(options.endedAtEpochMs) or BurdJournals.Server.baselineSnapshotNowEpochMs()
    if record.capturedAtEpochMs and tonumber(record.endedAtEpochMs) and tonumber(record.endedAtEpochMs) < tonumber(record.capturedAtEpochMs) then
        record.endedAtEpochMs = tonumber(record.capturedAtEpochMs)
    end
    record.endedAtIsoUtc = BurdJournals.Server.normalizeSnapshotString(options.endedAtIsoUtc, 64)
        or BurdJournals.Server.baselineSnapshotFormatEpochMs(record.endedAtEpochMs, true)
    record.endedAtLocal = BurdJournals.Server.normalizeSnapshotString(options.endedAtLocal, 96)
        or BurdJournals.Server.baselineSnapshotFormatEpochMs(record.endedAtEpochMs, false)
    record.endedReason = BurdJournals.Server.normalizeSnapshotString(endedReason, 64) or "unknown"
    if note and note ~= "" then
        record.note = BurdJournals.Server.normalizeSnapshotString(note, 256)
    end
    record.counts = BurdJournals.getBaselineSnapshotCounts and BurdJournals.getBaselineSnapshotCounts(record) or record.counts
    local skipHistoryTransmit = options.skipPlayerHistoryTransmit == true
    if options.skipTransmit == true and options.skipPlayerHistoryTransmit == nil then
        skipHistoryTransmit = true
    end
    if player and BurdJournals.Server.persistSnapshotToPlayerHistory then
        BurdJournals.Server.persistSnapshotToPlayerHistory(player, record, skipHistoryTransmit)
    end

    if record.steamId and store.activeBySteamId[record.steamId] == snapshotId then
        store.activeBySteamId[record.steamId] = nil
    end
    if record.characterId and store.activeByCharacterId[record.characterId] == snapshotId then
        store.activeByCharacterId[record.characterId] = nil
    end
    store._updatedAt = BurdJournals.Server.baselineSnapshotNowHours()
    if not options.skipTransmit then
        BurdJournals.Server.transmitBaselineSnapshotStore()
    end
    return true, snapshotId
end

function BurdJournals.Server.snapshotMatchesQuery(record, queryLower)
    if not queryLower or queryLower == "" then
        return true
    end
    local haystack = table.concat({
        tostring(record.snapshotId or ""),
        tostring(record.steamId or ""),
        tostring(record.characterId or ""),
        tostring(record.characterName or ""),
        tostring(record.username or ""),
        tostring(record.source or ""),
        tostring(record.note or ""),
        tostring(record.endedReason or ""),
        tostring(record.capturedAtIsoUtc or ""),
        tostring(record.capturedAtLocal or ""),
        tostring(record.endedAtIsoUtc or ""),
        tostring(record.endedAtLocal or ""),
    }, " ")
    return string.find(string.lower(haystack), queryLower, 1, true) ~= nil
end

function BurdJournals.Server.listBaselineSnapshots(filter)
    filter = type(filter) == "table" and filter or {}
    local store = BurdJournals.Server.getBaselineSnapshotStore()
    local bySnapshotId = store.bySnapshotId or {}

    local steamIdFilter = BurdJournals.Server.normalizeSnapshotString(filter.steamId, 96)
    local characterIdFilter = BurdJournals.Server.normalizeSnapshotString(filter.characterId, 160)
    local includeDead = filter.includeDead == true
    local queryLower = filter.query and string.lower(tostring(filter.query)) or ""
    local page = math.max(1, math.floor(tonumber(filter.page) or 1))
    local pageSize = math.max(5, math.min(100, math.floor(tonumber(filter.pageSize) or 20)))

    local matched = {}
    for _, record in pairs(bySnapshotId) do
        if type(record) == "table" then
            local include = true
            if steamIdFilter and tostring(record.steamId or "") ~= steamIdFilter then
                include = false
            end
            if include and characterIdFilter and tostring(record.characterId or "") ~= characterIdFilter then
                include = false
            end
            if include and not includeDead and tostring(record.endedReason or "") == "death" then
                include = false
            end
            if include and not BurdJournals.Server.snapshotMatchesQuery(record, queryLower) then
                include = false
            end
            if include then
                local counts = record.counts
                if type(counts) ~= "table" and BurdJournals.getBaselineSnapshotCounts then
                    counts = BurdJournals.getBaselineSnapshotCounts(record)
                end
                matched[#matched + 1] = {
                    snapshotId = record.snapshotId,
                    steamId = record.steamId,
                    characterId = record.characterId,
                    characterName = record.characterName,
                    username = record.username,
                    capturedAtHours = tonumber(record.capturedAtHours) or 0,
                    capturedAtEpochMs = tonumber(record.capturedAtEpochMs) or nil,
                    capturedAtIsoUtc = record.capturedAtIsoUtc,
                    capturedAtLocal = record.capturedAtLocal,
                    endedAtHours = tonumber(record.endedAtHours) or nil,
                    endedAtEpochMs = tonumber(record.endedAtEpochMs) or nil,
                    endedAtIsoUtc = record.endedAtIsoUtc,
                    endedAtLocal = record.endedAtLocal,
                    endedReason = record.endedReason,
                    source = record.source,
                    note = record.note,
                    isProtected = record.isProtected == true,
                    debugModified = record.debugModified == true,
                    counts = {
                        skills = math.max(0, tonumber(counts and counts.skills) or 0),
                        mediaSkills = math.max(0, tonumber(counts and counts.mediaSkills) or 0),
                        traits = math.max(0, tonumber(counts and counts.traits) or 0),
                        recipes = math.max(0, tonumber(counts and counts.recipes) or 0),
                    },
                }
            end
        end
    end

    table.sort(matched, function(a, b)
        local ah = tonumber(a.capturedAtHours) or 0
        local bh = tonumber(b.capturedAtHours) or 0
        if ah == bh then
            return tostring(a.snapshotId or "") > tostring(b.snapshotId or "")
        end
        return ah > bh
    end)

    local total = #matched
    local startIndex = ((page - 1) * pageSize) + 1
    local endIndex = math.min(total, startIndex + pageSize - 1)
    local items = {}
    if startIndex <= total then
        for i = startIndex, endIndex do
            items[#items + 1] = matched[i]
        end
    end

    return {
        items = items,
        total = total,
        page = page,
        pageSize = pageSize,
    }
end

function BurdJournals.Server.seedBaselineSnapshotsFromExistingStores(reasonTag)
    if BurdJournals.Server._baselineSnapshotSeeded then
        return 0
    end

    if BurdJournals.isBaselineSnapshotsEnabled and not BurdJournals.isBaselineSnapshotsEnabled() then
        return 0
    end
    if BurdJournals.Server.isBaselineModDataReady and not BurdJournals.Server.isBaselineModDataReady() then
        BurdJournals.debugPrint("[BurdJournals] Baseline snapshot seed deferred ("
            .. tostring(reasonTag or "unknown") .. "): baseline ModData not ready")
        return 0
    end

    local store = BurdJournals.Server.getBaselineSnapshotStore()
    if type(store) ~= "table" or type(store.bySnapshotId) ~= "table" then
        return 0
    end
    local dedupe = {}
    for _, record in pairs(store.bySnapshotId or {}) do
        if type(record) == "table" then
            local key = BurdJournals.Server.baselineSnapshotDedupeKey(
                record.steamId,
                record.characterId,
                record.capturedAtHours,
                record.source
            )
            dedupe[key] = true
        end
    end

    local seeded = 0
    local function seedFromRecord(characterId, baselineData, sourceLabel)
        if type(characterId) ~= "string" or characterId == "" or type(baselineData) ~= "table" then
            return
        end
        local payload = BurdJournals.sanitizeBaselinePayloadForSnapshot and BurdJournals.sanitizeBaselinePayloadForSnapshot(baselineData) or nil
        if not (payload and BurdJournals.baselineHasEntries and BurdJournals.baselineHasEntries(payload)) then
            return
        end
        local capturedAt = tonumber(baselineData.capturedAt) or BurdJournals.Server.baselineSnapshotNowHours()
        local steamId = baselineData.steamId and tostring(baselineData.steamId) or nil
        local source = "migration_seed"
        local key = BurdJournals.Server.baselineSnapshotDedupeKey(steamId, characterId, capturedAt, source)
        if dedupe[key] then
            return
        end

        local ok = BurdJournals.Server.saveBaselineSnapshot({
            steamId = steamId,
            characterId = characterId,
            characterName = baselineData.characterName,
            username = baselineData.username,
            capturedAtHours = capturedAt,
            source = source,
            note = "seed:" .. tostring(sourceLabel or "unknown"),
            isProtected = baselineData.debugModified == true,
            debugModified = baselineData.debugModified == true,
            skillBaseline = payload.skillBaseline,
            mediaSkillBaseline = payload.mediaSkillBaseline,
            traitBaseline = payload.traitBaseline,
            recipeBaseline = payload.recipeBaseline,
        }, {
            skipTransmit = true,
            skipPrune = false,
            force = true,
        })
        if ok then
            dedupe[key] = true
            seeded = seeded + 1
        end
    end

    local cache = BurdJournals.Server.getBaselineCache()
    if cache and type(cache.players) == "table" then
        for characterId, baselineData in pairs(cache.players) do
            seedFromRecord(characterId, baselineData, "cache")
        end
    end

    local archive = BurdJournals.Server.getBaselineArchive()
    if archive and type(archive.byCharacterId) == "table" then
        for characterId, baselineData in pairs(archive.byCharacterId) do
            seedFromRecord(characterId, baselineData, "archive")
        end
    end

    if seeded > 0 then
        BurdJournals.Server.transmitBaselineSnapshotStore()
        BurdJournals.debugPrint("[BurdJournals] Baseline snapshot seed (" .. tostring(reasonTag or "unknown")
            .. "): created " .. tostring(seeded) .. " snapshot(s)")
    end
    BurdJournals.Server._baselineSnapshotSeeded = true
    return seeded
end

function BurdJournals.Server.findCachedBaselineBySteamId(steamId, requestedCharacterId)
    if not steamId then
        return nil, nil
    end

    local cache = BurdJournals.Server.getBaselineCache()
    local players = cache and cache.players
    if type(players) ~= "table" then
        return nil, nil
    end

    local steamIdStr = tostring(steamId)
    local bestKey = nil
    local bestBaseline = nil
    local bestCapturedAt = -1

    for key, baseline in pairs(players) do
        if type(key) == "string" and type(baseline) == "table" then
            local baselineSteamId = baseline.steamId and tostring(baseline.steamId) or nil
            local matchesSteam = baselineSteamId == steamIdStr
            if not matchesSteam then
                local prefix = steamIdStr .. "_"
                matchesSteam = string.sub(key, 1, string.len(prefix)) == prefix
            end

            if matchesSteam then
                if requestedCharacterId and key == requestedCharacterId then
                    return baseline, key
                end

                local capturedAt = tonumber(baseline.capturedAt) or 0
                if bestBaseline == nil or capturedAt > bestCapturedAt then
                    bestBaseline = baseline
                    bestKey = key
                    bestCapturedAt = capturedAt
                end
            end
        end
    end

    return bestBaseline, bestKey
end

function BurdJournals.Server.findArchivedBaselineBySteamId(steamId, requestedCharacterId)
    if not steamId then
        return nil, nil
    end

    local archive = BurdJournals.Server.getBaselineArchive()
    local byCharacterId = archive and archive.byCharacterId
    if type(byCharacterId) ~= "table" then
        return nil, nil
    end

    local steamIdStr = tostring(steamId)
    local bySteamId = archive.bySteamId
    if type(bySteamId) == "table" then
        local mappedCharacterId = bySteamId[steamIdStr]
        local mappedEntry = mappedCharacterId and byCharacterId[mappedCharacterId] or nil
        if type(mappedEntry) == "table" then
            return mappedEntry, mappedCharacterId
        end
    end

    local bestKey = nil
    local bestBaseline = nil
    local bestCapturedAt = -1
    local prefix = steamIdStr .. "_"

    for key, baseline in pairs(byCharacterId) do
        if type(key) == "string" and type(baseline) == "table" then
            local baselineSteamId = baseline.steamId and tostring(baseline.steamId) or nil
            local matchesSteam = baselineSteamId == steamIdStr
            if not matchesSteam then
                matchesSteam = string.sub(key, 1, string.len(prefix)) == prefix
            end

            if matchesSteam then
                if requestedCharacterId and key == requestedCharacterId then
                    return baseline, key
                end

                local capturedAt = tonumber(baseline.capturedAt) or 0
                if bestBaseline == nil or capturedAt > bestCapturedAt then
                    bestBaseline = baseline
                    bestKey = key
                    bestCapturedAt = capturedAt
                end
            end
        end
    end

    return bestBaseline, bestKey
end

function BurdJournals.Server.storeBaselineArchiveRecord(characterId, baselineData, skipTransmit, canonicalAlready)
    if not characterId or type(baselineData) ~= "table" then
        return false
    end

    if BurdJournals.Server.isBaselineModDataReady and not BurdJournals.Server.isBaselineModDataReady() then
        BurdJournals.debugPrint("[BurdJournals] Archive baseline write skipped (ModData not ready): "
            .. tostring(characterId))
        return false
    end

    local archive = BurdJournals.Server.getBaselineArchive()
    archive.byCharacterId = archive.byCharacterId or {}
    archive.bySteamId = archive.bySteamId or {}

    -- Data-shape hardening: with 200+ mods recipeBaseline holds thousands of keys.
    -- Re-cloning it here on every register doubles CPU/memory for the archive mirror.
    -- When the caller passes a freshly-built canonical record (register path), share
    -- it directly. The two ModData root keys serialize independently on save, so this
    -- only aliases in-memory, and baseline records are always replaced (never mutated
    -- in place) by the register/promote paths.
    local archivedRecord = canonicalAlready == true and baselineData
        or BurdJournals.Server.cloneBaselineRecordForStorage(baselineData)
    local existingRecord = archive.byCharacterId[characterId]
    if not archivedRecord.steamId and type(existingRecord) == "table" and existingRecord.steamId then
        archivedRecord.steamId = existingRecord.steamId
    end

    archive.byCharacterId[characterId] = archivedRecord
    local steamId = archivedRecord.steamId and tostring(archivedRecord.steamId) or nil
    if steamId then
        archive.bySteamId[steamId] = characterId
    end
    archive._updatedAt = getGameTime and getGameTime():getWorldAgeHours() or 0

    if not skipTransmit then
        BurdJournals.Server.transmitBaselineStores(true)
    end
    return true
end

function BurdJournals.Server.backfillBaselineArchiveFromCache(reasonTag)
    local cache = BurdJournals.Server.getBaselineCache()
    local archive = BurdJournals.Server.getBaselineArchive()
    local players = cache and cache.players
    if type(players) ~= "table" then
        return 0
    end

    archive.byCharacterId = archive.byCharacterId or {}
    archive.bySteamId = archive.bySteamId or {}

    local seeded = 0
    for characterId, baseline in pairs(players) do
        if type(characterId) == "string" and characterId ~= "" and type(baseline) == "table" then
            local existing = archive.byCharacterId[characterId]
            local baselineCapturedAt = tonumber(baseline.capturedAt) or 0
            local existingCapturedAt = tonumber(existing and existing.capturedAt) or -1
            if type(existing) ~= "table" or baselineCapturedAt > existingCapturedAt then
                BurdJournals.Server.storeBaselineArchiveRecord(characterId, baseline, true)
                seeded = seeded + 1
            end
        end
    end

    if seeded > 0 then
        BurdJournals.Server.transmitBaselineStores(true)
        BurdJournals.debugPrint("[BurdJournals] Baseline archive backfill ("
            .. tostring(reasonTag or "unknown") .. "): mirrored " .. tostring(seeded) .. " cache entry(ies)")
    end
    return seeded
end

function BurdJournals.Server.removeArchivedBaseline(characterId, skipTransmit)
    if not characterId then
        return false
    end

    local archive = BurdJournals.Server.getBaselineArchive()
    local byCharacterId = archive and archive.byCharacterId
    local bySteamId = archive and archive.bySteamId
    if type(byCharacterId) ~= "table" then
        return false
    end

    local removed = false
    local existing = byCharacterId[characterId]
    local steamId = existing and existing.steamId and tostring(existing.steamId) or nil

    if existing ~= nil then
        byCharacterId[characterId] = nil
        removed = true
    end

    if type(bySteamId) == "table" then
        if steamId and bySteamId[steamId] == characterId then
            bySteamId[steamId] = nil
            removed = true
        end
        for steamKey, mappedCharacterId in pairs(bySteamId) do
            if mappedCharacterId == characterId then
                bySteamId[steamKey] = nil
                removed = true
            end
        end
    end

    if removed then
        archive._updatedAt = getGameTime and getGameTime():getWorldAgeHours() or 0
        if not skipTransmit then
            BurdJournals.Server.transmitBaselineStores(true)
        end
    end
    return removed
end

function BurdJournals.Server.restoreCachedBaselineFromArchive(characterId, player, allowSteamFallback)
    if not characterId then
        return nil
    end

    local archive = BurdJournals.Server.getBaselineArchive()
    local byCharacterId = archive and archive.byCharacterId
    if type(byCharacterId) ~= "table" then
        return nil
    end

    local steamId = player and BurdJournals.getPlayerSteamId and BurdJournals.getPlayerSteamId(player) or nil
    local archiveSourceKey = characterId
    local archivedBaseline = byCharacterId[characterId]

    if type(archivedBaseline) ~= "table" and allowSteamFallback and steamId then
        archivedBaseline, archiveSourceKey = BurdJournals.Server.findArchivedBaselineBySteamId(steamId, characterId)
    end

    if type(archivedBaseline) ~= "table" then
        return nil
    end

    local restoredBaseline = BurdJournals.Server.cloneBaselineRecordForStorage(archivedBaseline)
    if steamId then
        restoredBaseline.steamId = tostring(steamId)
    end

    if player and player.getDescriptor then
        local descriptor = player:getDescriptor()
        if descriptor then
            restoredBaseline.characterName = (descriptor:getForename() or "Unknown") .. " " .. (descriptor:getSurname() or "")
        end
    end
    if not tonumber(restoredBaseline.capturedAt) or tonumber(restoredBaseline.capturedAt) <= 0 then
        restoredBaseline.capturedAt = getGameTime and getGameTime():getWorldAgeHours() or 0
    end

    local cache = BurdJournals.Server.getBaselineCache()
    cache.players = cache.players or {}
    cache.players[characterId] = restoredBaseline

    BurdJournals.Server.storeBaselineArchiveRecord(characterId, restoredBaseline, true)
    BurdJournals.Server.transmitBaselineStores(true)
    if BurdJournals.Server.captureBaselineSnapshotForPlayer then
        BurdJournals.Server.captureBaselineSnapshotForPlayer(
            player,
            characterId,
            restoredBaseline,
            "request_recovery",
            "archive_restore"
        )
    end

    BurdJournals.debugPrint("[BurdJournals] Restored baseline from archive for "
        .. tostring(characterId) .. " (source key " .. tostring(archiveSourceKey) .. ")")
    return restoredBaseline
end

function BurdJournals.Server.recoverCachedBaselineFromPlayerBackup(characterId, player, options)
    options = type(options) == "table" and options or {}
    if not characterId or not player or not BurdJournals.Server.readPlayerBaselineBackup then
        return nil
    end

    local backupBaseline = BurdJournals.Server.readPlayerBaselineBackup(player, characterId)
    if type(backupBaseline) ~= "table" then
        return nil
    end

    local recoveredBaseline = BurdJournals.Server.cloneBaselineRecordForStorage(backupBaseline)
    recoveredBaseline.debugModified = backupBaseline.debugModified == true
    recoveredBaseline.steamId = backupBaseline.steamId
        or (BurdJournals.getPlayerSteamId and BurdJournals.getPlayerSteamId(player))
    recoveredBaseline.characterName = backupBaseline.characterName
        or BurdJournals.Server.getPlayerCharacterDisplayName(player)
    recoveredBaseline.capturedAt = tonumber(backupBaseline.capturedAt)
        or (getGameTime and getGameTime():getWorldAgeHours() or 0)
    recoveredBaseline.recoveredFromPlayerModData = true
    recoveredBaseline.migrationSource = "playerBackup"

    BurdJournals.debugPrint("[BurdJournals] RECOVERY (serverBaselineBackup) for " .. tostring(characterId))

    local hasLiveModData = type(ModData) == "table"
        and (type(ModData.getOrCreate) == "function" or type(ModData.get) == "function")
    local stored = false
    if hasLiveModData then
        stored = BurdJournals.Server.storeCachedBaseline(characterId, recoveredBaseline, true)
    else
        local cache = BurdJournals.Server.getBaselineCache()
        cache.players = cache.players or {}
        cache.players[characterId] = recoveredBaseline
        stored = true
    end
    if not stored then
        local cache = BurdJournals.Server.getBaselineCache()
        local existing = cache and cache.players and cache.players[characterId] or nil
        if type(existing) == "table" then
            return existing
        end
        return nil
    end

    if hasLiveModData then
        BurdJournals.Server.storeBaselineArchiveRecord(characterId, recoveredBaseline, true)
    end
    if BurdJournals.Server.writePlayerBaselineBackup then
        BurdJournals.Server.writePlayerBaselineBackup(player, characterId, recoveredBaseline, true)
    end
    if hasLiveModData and BurdJournals.Server.captureBaselineSnapshotForPlayer then
        BurdJournals.Server.captureBaselineSnapshotForPlayer(
            player,
            characterId,
            recoveredBaseline,
            "request_recovery",
            "player_backup"
        )
    end
    if hasLiveModData and options.transmitStores ~= false then
        BurdJournals.Server.transmitBaselineStores(true)
    end
    if options.transmitPlayer == true and player.transmitModData then
        player:transmitModData()
    end

    return recoveredBaseline
end

function BurdJournals.Server.getCachedBaseline(characterId, player)
    if not characterId then return nil end
    local cache = BurdJournals.Server.getBaselineCache()
    cache.players = cache.players or {}
    local cachedBaseline = cache.players[characterId]
    if cachedBaseline then
        if BurdJournals.sanitizeBaselinePayloadForSnapshot then
            local oldTraitCount = BurdJournals.countTable(cachedBaseline.traitBaseline or {})
            local sanitized = BurdJournals.sanitizeBaselinePayloadForSnapshot(cachedBaseline)
            cachedBaseline.traitBaseline = sanitized.traitBaseline
            if BurdJournals.countTable(cachedBaseline.traitBaseline or {}) < oldTraitCount then
                BurdJournals.Server.storeBaselineArchiveRecord(characterId, cachedBaseline, true, true)
                BurdJournals.Server.transmitBaselineStores(true)
                if player then
                    BurdJournals.Server.applyBaselinePayloadToPlayerModData(player, cachedBaseline, cachedBaseline.debugModified == true)
                    if player.transmitModData then player:transmitModData() end
                end
            end
        end
        return cachedBaseline
    end

    local allowSteamFallback = false
    if player then
        local hoursAlive = BurdJournals.getBaselineLifecycleHours and BurdJournals.getBaselineLifecycleHours(player) or 0
        local snapshotWindowHours = BurdJournals.getBaselineSnapshotMaxHours and BurdJournals.getBaselineSnapshotMaxHours() or 0.5
        allowSteamFallback = hoursAlive > snapshotWindowHours
    end

    -- Recovery path: character name/descriptor timing can create key drift.
    -- For established characters (or temporary *_Unknown IDs), recover by steamId and migrate.
    if allowSteamFallback and player then
        local steamId = BurdJournals.getPlayerSteamId and BurdJournals.getPlayerSteamId(player) or nil
        if steamId then
            local fallbackBaseline, fallbackKey = BurdJournals.Server.findCachedBaselineBySteamId(steamId, characterId)
            if fallbackBaseline then
                cache.players[characterId] = fallbackBaseline
                if fallbackKey and fallbackKey ~= characterId then
                    cache.players[fallbackKey] = nil
                end

                fallbackBaseline.steamId = tostring(steamId)
                local descriptor = player.getDescriptor and player:getDescriptor() or nil
                if descriptor then
                    fallbackBaseline.characterName = (descriptor:getForename() or "Unknown") .. " " .. (descriptor:getSurname() or "")
                end
                if not tonumber(fallbackBaseline.capturedAt) or tonumber(fallbackBaseline.capturedAt) <= 0 then
                    fallbackBaseline.capturedAt = getGameTime():getWorldAgeHours()
                end

                BurdJournals.Server.storeBaselineArchiveRecord(characterId, fallbackBaseline, true)
                BurdJournals.Server.transmitBaselineStores(true)
                if BurdJournals.Server.captureBaselineSnapshotForPlayer then
                    BurdJournals.Server.captureBaselineSnapshotForPlayer(
                        player,
                        characterId,
                        fallbackBaseline,
                        "request_recovery",
                        "steam_alias"
                    )
                end

                BurdJournals.debugPrint("[BurdJournals] Recovered baseline alias by steamId for "
                    .. tostring(characterId) .. " (from key " .. tostring(fallbackKey) .. ")")
                return fallbackBaseline
            end
        end
    end

    local restoredFromBackup = BurdJournals.Server.recoverCachedBaselineFromPlayerBackup(characterId, player, {
        transmitStores = true,
        transmitPlayer = false,
    })
    if restoredFromBackup then
        return restoredFromBackup
    end

    local restoredFromArchive = BurdJournals.Server.restoreCachedBaselineFromArchive(characterId, player, allowSteamFallback)
    if restoredFromArchive then
        return restoredFromArchive
    end

    return nil
end

function BurdJournals.Server.storeCachedBaseline(characterId, baselineData, forceOverwrite)
    if not characterId or not baselineData then return false end

    if BurdJournals.Server.isBaselineModDataReady and not BurdJournals.Server.isBaselineModDataReady() then
        BurdJournals.debugPrint("[BurdJournals] Cached baseline write skipped (ModData not ready): "
            .. tostring(characterId))
        return false
    end

    local cache = BurdJournals.Server.getBaselineCache()
    cache.players = cache.players or {}
    local existingBaseline = cache.players[characterId]

    -- IMPORTANT: Never overwrite debug-modified baselines unless explicitly forced
    if existingBaseline then
        if existingBaseline.debugModified and not forceOverwrite then
            bsjWriteLogLine("[BurdJournals] PROTECTED: Baseline for " .. characterId .. " was debug-modified, refusing automatic overwrite")
            return false
        end
        if not forceOverwrite then
            BurdJournals.debugPrint("[BurdJournals] Baseline already cached for " .. characterId .. ", ignoring new registration")
            return false
        end
    end

    -- Preserve the debugModified flag if it was set and we're force-overwriting
    local preserveDebugFlag = existingBaseline and existingBaseline.debugModified

    local debugFlag = baselineData.debugModified
    if debugFlag == nil then
        debugFlag = preserveDebugFlag
    end

    cache.players[characterId] = BurdJournals.Server.cloneBaselineRecordForStorage({
        skillBaseline = baselineData.skillBaseline or {},
        skillExportBaseline = baselineData.skillExportBaseline or {},
        mediaSkillBaseline = baselineData.mediaSkillBaseline or {},
        mediaSkillExportBaseline = baselineData.mediaSkillExportBaseline or {},
        traitBaseline = baselineData.traitBaseline or {},
        traitExportBaseline = baselineData.traitExportBaseline or {},
        recipeBaseline = baselineData.recipeBaseline or {},
        recipeExportBaseline = baselineData.recipeExportBaseline or {},
        baselineVersion = tonumber(baselineData.baselineVersion) or tonumber(BurdJournals.BASELINE_VERSION) or 5,
        capturedAt = tonumber(baselineData.capturedAt) or getGameTime():getWorldAgeHours(),
        steamId = baselineData.steamId,
        characterName = baselineData.characterName,
        debugModified = debugFlag,  -- Preserve unless explicitly overridden
        recoveredFromPlayerModData = baselineData.recoveredFromPlayerModData,
        migrationSource = baselineData.migrationSource
    })

    -- Persist to disk so baseline survives server restart. The cache record was just
    -- freshly cloned above, so let the archive share it (canonicalAlready=true) instead
    -- of deep-copying the (potentially thousands-of-keys) recipeBaseline a second time.
    BurdJournals.Server.storeBaselineArchiveRecord(characterId, cache.players[characterId], true, true)
    BurdJournals.Server.transmitBaselineStores(true)

    BurdJournals.debugPrint("[BurdJournals] Baseline cached and persisted for " .. characterId)
    return true
end

function BurdJournals.Server.extractBaselinePayloadFromPlayerModData(player)
    if not player or not player.getModData then
        return nil
    end
    local modData = player:getModData()
    local bj = type(modData) == "table" and modData.BurdJournals or nil
    if type(bj) ~= "table" then
        return nil
    end
    return {
        skillBaseline = BurdJournals.Server.copyBaselineTableEntries(bj.skillBaseline),
        skillExportBaseline = BurdJournals.Server.copyBaselineTableEntries(bj.skillExportBaseline),
        mediaSkillBaseline = BurdJournals.Server.copyBaselineTableEntries(bj.mediaSkillBaseline),
        mediaSkillExportBaseline = BurdJournals.Server.copyBaselineTableEntries(bj.mediaSkillExportBaseline),
        traitBaseline = BurdJournals.Server.copyBaselineTableEntries(bj.traitBaseline),
        traitExportBaseline = BurdJournals.Server.copyBaselineTableEntries(bj.traitExportBaseline),
        recipeBaseline = BurdJournals.Server.copyBaselineTableEntries(bj.recipeBaseline),
        recipeExportBaseline = BurdJournals.Server.copyBaselineTableEntries(bj.recipeExportBaseline),
        debugModified = bj.debugModified == true,
        baselineCaptured = bj.baselineCaptured == true,
        baselineVersion = tonumber(bj.baselineVersion) or tonumber(BurdJournals.BASELINE_VERSION) or 5,
    }
end

function BurdJournals.Server.applyBaselinePayloadToPlayerModData(targetPlayer, payload, debugModified)
    if not targetPlayer or not targetPlayer.getModData then
        return false
    end
    local modData = targetPlayer:getModData()
    modData.BurdJournals = modData.BurdJournals or {}
    modData.BurdJournals.skillBaseline = BurdJournals.Server.copyBaselineTableEntries(payload.skillBaseline)
    modData.BurdJournals.skillExportBaseline = BurdJournals.Server.copyBaselineTableEntries(payload.skillExportBaseline)
    modData.BurdJournals.mediaSkillBaseline = BurdJournals.Server.copyBaselineTableEntries(payload.mediaSkillBaseline)
    modData.BurdJournals.mediaSkillExportBaseline = BurdJournals.Server.copyBaselineTableEntries(payload.mediaSkillExportBaseline)
    modData.BurdJournals.traitBaseline = BurdJournals.Server.copyBaselineTableEntries(payload.traitBaseline)
    modData.BurdJournals.traitExportBaseline = BurdJournals.Server.copyBaselineTableEntries(payload.traitExportBaseline)
    modData.BurdJournals.recipeBaseline = BurdJournals.Server.copyBaselineTableEntries(payload.recipeBaseline)
    modData.BurdJournals.recipeExportBaseline = BurdJournals.Server.copyBaselineTableEntries(payload.recipeExportBaseline)
    modData.BurdJournals.debugModified = debugModified == true
    modData.BurdJournals.baselineCaptured = true
    modData.BurdJournals.baselineVersion = tonumber(payload and payload.baselineVersion)
        or tonumber(modData.BurdJournals.baselineVersion)
        or tonumber(BurdJournals.BASELINE_VERSION)
        or 5
    if BurdJournals.getPlayerCharacterId then
        local currentCharacterId = BurdJournals.getPlayerCharacterId(targetPlayer)
        modData.BurdJournals.characterId = currentCharacterId
        modData.BurdJournals.lastSeenCharacterId = currentCharacterId
        if tostring(modData.BurdJournals.deferBaselineUntilNewCharacterId or "") == tostring(currentCharacterId or "") then
            modData.BurdJournals.deferBaselineUntilNewCharacterId = nil
        end
    end
    if BurdJournals.getPlayerSteamId then
        modData.BurdJournals.steamId = BurdJournals.getPlayerSteamId(targetPlayer)
    end
    modData.BurdJournals_Baseline = nil
    return true
end

function BurdJournals.Server.promotePlayerJournalRecordToExportBaseline(targetPlayer, update)
    if not targetPlayer or not targetPlayer.getModData or type(update) ~= "table" then
        return false
    end
    if not (BurdJournals.shouldUseBaselineForPlayerJournalRecording
        and BurdJournals.shouldUseBaselineForPlayerJournalRecording(targetPlayer)) then
        return false
    end

    local characterId = BurdJournals.getPlayerCharacterId and BurdJournals.getPlayerCharacterId(targetPlayer) or nil
    if not characterId then
        return false
    end

    local payload = BurdJournals.Server.buildBaselinePayloadFromTarget
        and BurdJournals.Server.buildBaselinePayloadFromTarget(targetPlayer, characterId)
        or nil
    if type(payload) ~= "table" then
        payload = {}
    end
    payload.skillBaseline = BurdJournals.Server.copyBaselineTableEntries(payload.skillBaseline)
    payload.skillExportBaseline = BurdJournals.Server.copyBaselineTableEntries(payload.skillExportBaseline)
    payload.mediaSkillBaseline = BurdJournals.Server.copyBaselineTableEntries(payload.mediaSkillBaseline)
    payload.mediaSkillExportBaseline = BurdJournals.Server.copyBaselineTableEntries(payload.mediaSkillExportBaseline)
    payload.traitBaseline = BurdJournals.Server.copyBaselineTableEntries(payload.traitBaseline)
    payload.traitExportBaseline = BurdJournals.Server.copyBaselineTableEntries(payload.traitExportBaseline)
    payload.recipeBaseline = BurdJournals.Server.copyBaselineTableEntries(payload.recipeBaseline)
    payload.recipeExportBaseline = BurdJournals.Server.copyBaselineTableEntries(payload.recipeExportBaseline)

    local changed = false

    local skillName = type(update.skillName) == "string" and update.skillName or nil
    if skillName and skillName ~= "" then
        local skillXP = math.max(
            math.max(0, tonumber(payload.skillBaseline[skillName]) or 0),
            math.max(0, tonumber(update.skillXP) or 0)
        )
        if tonumber(payload.skillExportBaseline[skillName]) ~= skillXP then
            payload.skillExportBaseline[skillName] = skillXP
            changed = true
        end

        local mediaSkillXP = math.max(
            math.max(0, tonumber(payload.mediaSkillBaseline[skillName]) or 0),
            math.max(0, tonumber(update.mediaSkillXP) or 0)
        )
        if mediaSkillXP > 0 then
            if tonumber(payload.mediaSkillExportBaseline[skillName]) ~= mediaSkillXP then
                payload.mediaSkillExportBaseline[skillName] = mediaSkillXP
                changed = true
            end
        elseif payload.mediaSkillExportBaseline[skillName] ~= nil then
            payload.mediaSkillExportBaseline[skillName] = nil
            changed = true
        end
    end

    local traitId = type(update.traitId) == "string" and update.traitId or nil
    if traitId and traitId ~= "" then
        local aliases = BurdJournals.getTraitAliases and BurdJournals.getTraitAliases(traitId) or { traitId }
        for _, alias in ipairs(aliases) do
            if alias and alias ~= "" and payload.traitExportBaseline[alias] ~= true then
                payload.traitExportBaseline[alias] = true
                changed = true
            end
        end
    end

    local recipeName = type(update.recipeName) == "string" and update.recipeName or nil
    if recipeName and recipeName ~= "" and payload.recipeExportBaseline[recipeName] ~= true then
        payload.recipeExportBaseline[recipeName] = true
        changed = true
    end

    if not changed then
        return false
    end

    local cache = BurdJournals.Server.getBaselineCache and BurdJournals.Server.getBaselineCache() or nil
    if type(cache) == "table" then
        cache.players = cache.players or {}
        local existing = type(cache.players[characterId]) == "table" and cache.players[characterId] or {}
        cache.players[characterId] = {
            skillBaseline = BurdJournals.Server.copyBaselineTableEntries(payload.skillBaseline),
            skillExportBaseline = BurdJournals.Server.copyBaselineTableEntries(payload.skillExportBaseline),
            mediaSkillBaseline = BurdJournals.Server.copyBaselineTableEntries(payload.mediaSkillBaseline),
            mediaSkillExportBaseline = BurdJournals.Server.copyBaselineTableEntries(payload.mediaSkillExportBaseline),
            traitBaseline = BurdJournals.Server.copyBaselineTableEntries(payload.traitBaseline),
            traitExportBaseline = BurdJournals.Server.copyBaselineTableEntries(payload.traitExportBaseline),
            recipeBaseline = BurdJournals.Server.copyBaselineTableEntries(payload.recipeBaseline),
            recipeExportBaseline = BurdJournals.Server.copyBaselineTableEntries(payload.recipeExportBaseline),
            capturedAt = tonumber(existing.capturedAt) or ((getGameTime and getGameTime() and getGameTime():getWorldAgeHours()) or 0),
            steamId = existing.steamId or (BurdJournals.getPlayerSteamId and BurdJournals.getPlayerSteamId(targetPlayer)),
            characterName = existing.characterName or (getPlayerCharacterDisplayName and BurdJournals.Server.getPlayerCharacterDisplayName(targetPlayer)),
            debugModified = existing.debugModified == true,
            baselineCaptured = true,
            baselineVersion = tonumber(payload.baselineVersion) or tonumber(existing.baselineVersion) or tonumber(BurdJournals.BASELINE_VERSION) or 5,
        }
        if BurdJournals.Server.storeBaselineArchiveRecord then
            BurdJournals.Server.storeBaselineArchiveRecord(characterId, cache.players[characterId], cache.players[characterId].debugModified == true)
        end
        if BurdJournals.Server.writePlayerBaselineBackup then
            BurdJournals.Server.writePlayerBaselineBackup(targetPlayer, characterId, cache.players[characterId], cache.players[characterId].debugModified == true)
        end
        if BurdJournals.Server.transmitBaselineStores then
            BurdJournals.Server.transmitBaselineStores(true)
        end
    end

    BurdJournals.Server.applyBaselinePayloadToPlayerModData(targetPlayer, payload, false)
    if targetPlayer.transmitModData then
        targetPlayer:transmitModData()
    end
    return true
end

function BurdJournals.Server.promotePlayerJournalRecordBatchToExportBaseline(targetPlayer, updates)
    if not targetPlayer or not targetPlayer.getModData or type(updates) ~= "table" or #updates <= 0 then
        return false
    end
    if not (BurdJournals.shouldUseBaselineForPlayerJournalRecording
        and BurdJournals.shouldUseBaselineForPlayerJournalRecording(targetPlayer)) then
        return false
    end

    local characterId = BurdJournals.getPlayerCharacterId and BurdJournals.getPlayerCharacterId(targetPlayer) or nil
    if not characterId then
        return false
    end

    local payload = BurdJournals.Server.buildBaselinePayloadFromTarget
        and BurdJournals.Server.buildBaselinePayloadFromTarget(targetPlayer, characterId)
        or nil
    if type(payload) ~= "table" then
        payload = {}
    end
    payload.skillBaseline = BurdJournals.Server.copyBaselineTableEntries(payload.skillBaseline)
    payload.skillExportBaseline = BurdJournals.Server.copyBaselineTableEntries(payload.skillExportBaseline)
    payload.mediaSkillBaseline = BurdJournals.Server.copyBaselineTableEntries(payload.mediaSkillBaseline)
    payload.mediaSkillExportBaseline = BurdJournals.Server.copyBaselineTableEntries(payload.mediaSkillExportBaseline)
    payload.traitBaseline = BurdJournals.Server.copyBaselineTableEntries(payload.traitBaseline)
    payload.traitExportBaseline = BurdJournals.Server.copyBaselineTableEntries(payload.traitExportBaseline)
    payload.recipeBaseline = BurdJournals.Server.copyBaselineTableEntries(payload.recipeBaseline)
    payload.recipeExportBaseline = BurdJournals.Server.copyBaselineTableEntries(payload.recipeExportBaseline)

    local changed = false
    for _, update in ipairs(updates) do
        if type(update) == "table" then
            local skillName = type(update.skillName) == "string" and update.skillName or nil
            if skillName and skillName ~= "" then
                local skillXP = math.max(
                    math.max(0, tonumber(payload.skillBaseline[skillName]) or 0),
                    math.max(0, tonumber(update.skillXP) or 0)
                )
                if tonumber(payload.skillExportBaseline[skillName]) ~= skillXP then
                    payload.skillExportBaseline[skillName] = skillXP
                    changed = true
                end

                local mediaSkillXP = math.max(
                    math.max(0, tonumber(payload.mediaSkillBaseline[skillName]) or 0),
                    math.max(0, tonumber(update.mediaSkillXP) or 0)
                )
                if mediaSkillXP > 0 then
                    if tonumber(payload.mediaSkillExportBaseline[skillName]) ~= mediaSkillXP then
                        payload.mediaSkillExportBaseline[skillName] = mediaSkillXP
                        changed = true
                    end
                elseif payload.mediaSkillExportBaseline[skillName] ~= nil then
                    payload.mediaSkillExportBaseline[skillName] = nil
                    changed = true
                end
            end

            local traitId = type(update.traitId) == "string" and update.traitId or nil
            if traitId and traitId ~= "" then
                local aliases = BurdJournals.getTraitAliases and BurdJournals.getTraitAliases(traitId) or { traitId }
                for _, alias in ipairs(aliases) do
                    if alias and alias ~= "" and payload.traitExportBaseline[alias] ~= true then
                        payload.traitExportBaseline[alias] = true
                        changed = true
                    end
                end
            end

            local recipeName = type(update.recipeName) == "string" and update.recipeName or nil
            if recipeName and recipeName ~= "" and payload.recipeExportBaseline[recipeName] ~= true then
                payload.recipeExportBaseline[recipeName] = true
                changed = true
            end
        end
    end

    if not changed then
        return false
    end

    local cache = BurdJournals.Server.getBaselineCache and BurdJournals.Server.getBaselineCache() or nil
    if type(cache) == "table" then
        cache.players = cache.players or {}
        local existing = type(cache.players[characterId]) == "table" and cache.players[characterId] or {}
        cache.players[characterId] = {
            skillBaseline = BurdJournals.Server.copyBaselineTableEntries(payload.skillBaseline),
            skillExportBaseline = BurdJournals.Server.copyBaselineTableEntries(payload.skillExportBaseline),
            mediaSkillBaseline = BurdJournals.Server.copyBaselineTableEntries(payload.mediaSkillBaseline),
            mediaSkillExportBaseline = BurdJournals.Server.copyBaselineTableEntries(payload.mediaSkillExportBaseline),
            traitBaseline = BurdJournals.Server.copyBaselineTableEntries(payload.traitBaseline),
            traitExportBaseline = BurdJournals.Server.copyBaselineTableEntries(payload.traitExportBaseline),
            recipeBaseline = BurdJournals.Server.copyBaselineTableEntries(payload.recipeBaseline),
            recipeExportBaseline = BurdJournals.Server.copyBaselineTableEntries(payload.recipeExportBaseline),
            capturedAt = tonumber(existing.capturedAt) or ((getGameTime and getGameTime() and getGameTime():getWorldAgeHours()) or 0),
            steamId = existing.steamId or (BurdJournals.getPlayerSteamId and BurdJournals.getPlayerSteamId(targetPlayer)),
            characterName = existing.characterName or (getPlayerCharacterDisplayName and BurdJournals.Server.getPlayerCharacterDisplayName(targetPlayer)),
            debugModified = existing.debugModified == true,
            baselineCaptured = true,
            baselineVersion = tonumber(payload.baselineVersion) or tonumber(existing.baselineVersion) or tonumber(BurdJournals.BASELINE_VERSION) or 5,
        }
        if BurdJournals.Server.storeBaselineArchiveRecord then
            BurdJournals.Server.storeBaselineArchiveRecord(characterId, cache.players[characterId], cache.players[characterId].debugModified == true)
        end
        if BurdJournals.Server.writePlayerBaselineBackup then
            BurdJournals.Server.writePlayerBaselineBackup(targetPlayer, characterId, cache.players[characterId], cache.players[characterId].debugModified == true)
        end
        if BurdJournals.Server.transmitBaselineStores then
            BurdJournals.Server.transmitBaselineStores(true)
        end
    end

    BurdJournals.Server.applyBaselinePayloadToPlayerModData(targetPlayer, payload, false)
    if targetPlayer.transmitModData then
        targetPlayer:transmitModData()
    end
    return true
end

BurdJournals.Server.BASELINE_PLAYER_MUTATION_KEYS = {
    "skillBaseline", "skillExportBaseline", "mediaSkillBaseline", "mediaSkillExportBaseline",
    "traitBaseline", "traitExportBaseline", "recipeBaseline", "recipeExportBaseline",
    "debugModified", "baselineCaptured", "baselineVersion", "characterId", "lastSeenCharacterId",
    "deferBaselineUntilNewCharacterId", "steamId",
}

function BurdJournals.Server.captureBaselinePlayerMutationState(targetPlayer)
    if not targetPlayer or not targetPlayer.getModData then return nil end
    local modData = targetPlayer:getModData()
    if type(modData) ~= "table" then return nil end
    local bj = modData.BurdJournals
    local state = {
        modData = modData,
        hadBurdJournals = type(bj) == "table",
        values = {},
        present = {},
        legacyBaseline = modData.BurdJournals_Baseline,
        hadLegacyBaseline = modData.BurdJournals_Baseline ~= nil,
    }
    if type(bj) == "table" then
        for _, key in ipairs(BurdJournals.Server.BASELINE_PLAYER_MUTATION_KEYS) do
            state.present[key] = bj[key] ~= nil
            state.values[key] = bj[key]
        end
        local backupKey = BurdJournals.Server.PLAYER_BASELINE_BACKUP_KEY
        state.present[backupKey] = bj[backupKey] ~= nil
        state.values[backupKey] = bj[backupKey]
    end
    return state
end

function BurdJournals.Server.restoreBaselinePlayerMutationState(state)
    if type(state) ~= "table" or type(state.modData) ~= "table" then return false end
    local modData = state.modData
    if state.hadBurdJournals ~= true then
        modData.BurdJournals = nil
    else
        modData.BurdJournals = modData.BurdJournals or {}
        local bj = modData.BurdJournals
        for _, key in ipairs(BurdJournals.Server.BASELINE_PLAYER_MUTATION_KEYS) do
            bj[key] = state.present[key] and state.values[key] or nil
        end
        local backupKey = BurdJournals.Server.PLAYER_BASELINE_BACKUP_KEY
        bj[backupKey] = state.present[backupKey] and state.values[backupKey] or nil
    end
    modData.BurdJournals_Baseline = state.hadLegacyBaseline and state.legacyBaseline or nil
    return true
end

function BurdJournals.Server.copyBaselineSteamIndex(source)
    local copied = {}
    if type(source) == "table" then
        for steamId, characterId in pairs(source) do copied[steamId] = characterId end
    end
    return copied
end

function BurdJournals.Server.applyBaselineSnapshotToPlayer(targetPlayer, snapshot, restoreMode)
    if not targetPlayer then
        return false, "Target player not found"
    end
    if type(snapshot) ~= "table" then
        return false, "Snapshot not found"
    end

    local characterId = BurdJournals.getPlayerCharacterId and BurdJournals.getPlayerCharacterId(targetPlayer) or nil
    if not characterId then
        return false, "Could not resolve target character ID"
    end

    local defaultRestoreMode = BurdJournals.getDefaultBaselineRestoreMode
        and BurdJournals.getDefaultBaselineRestoreMode()
        or BurdJournals.BASELINE_SNAPSHOT_RESTORE_UNLOCKED
    local normalizedMode = BurdJournals.normalizeBaselineRestoreMode and BurdJournals.normalizeBaselineRestoreMode(restoreMode)
        or defaultRestoreMode
    if not restoreMode or tostring(restoreMode) == "" then
        normalizedMode = defaultRestoreMode
    end
    local debugModified = normalizedMode ~= BurdJournals.BASELINE_SNAPSHOT_RESTORE_UNLOCKED

    local steamId = BurdJournals.getPlayerSteamId and BurdJournals.getPlayerSteamId(targetPlayer) or snapshot.steamId
    local characterName = BurdJournals.Server.getPlayerCharacterDisplayName(targetPlayer) or snapshot.characterName
    local baselinePayload = BurdJournals.sanitizeBaselinePayloadForSnapshot and BurdJournals.sanitizeBaselinePayloadForSnapshot(snapshot) or {
        skillBaseline = BurdJournals.Server.copyBaselineTableEntries(snapshot.skillBaseline),
        skillExportBaseline = BurdJournals.Server.copyBaselineTableEntries(snapshot.skillExportBaseline),
        mediaSkillBaseline = BurdJournals.Server.copyBaselineTableEntries(snapshot.mediaSkillBaseline),
        mediaSkillExportBaseline = BurdJournals.Server.copyBaselineTableEntries(snapshot.mediaSkillExportBaseline),
        traitBaseline = BurdJournals.Server.copyBaselineTableEntries(snapshot.traitBaseline),
        traitExportBaseline = BurdJournals.Server.copyBaselineTableEntries(snapshot.traitExportBaseline),
        recipeBaseline = BurdJournals.Server.copyBaselineTableEntries(snapshot.recipeBaseline),
        recipeExportBaseline = BurdJournals.Server.copyBaselineTableEntries(snapshot.recipeExportBaseline),
    }

    if not (BurdJournals.baselineHasEntries and BurdJournals.baselineHasEntries(baselinePayload)) then
        return false, "Snapshot has no baseline payload"
    end

    if BurdJournals.Server.isBaselineModDataReady and not BurdJournals.Server.isBaselineModDataReady() then
        return false, "Baseline storage is not ready"
    end
    local cache = BurdJournals.Server.getBaselineCache()
    local archive = BurdJournals.Server.getBaselineArchive()
    if type(cache) ~= "table" or type(archive) ~= "table" then
        return false, "Baseline storage is unavailable"
    end
    cache.players = cache.players or {}
    archive.byCharacterId = archive.byCharacterId or {}
    archive.bySteamId = archive.bySteamId or {}
    local stagedCacheRecord = cache.players[characterId]
    local stagedArchiveRecord = archive.byCharacterId[characterId]
    local stagedArchiveSteamIndex = BurdJournals.Server.copyBaselineSteamIndex(archive.bySteamId)
    local stagedPlayerState = BurdJournals.Server.captureBaselinePlayerMutationState(targetPlayer)
    if not stagedPlayerState then return false, "Player ModData is unavailable" end

    local appliedBaseline = {
        skillBaseline = baselinePayload.skillBaseline,
        skillExportBaseline = baselinePayload.skillExportBaseline,
        mediaSkillBaseline = baselinePayload.mediaSkillBaseline,
        mediaSkillExportBaseline = baselinePayload.mediaSkillExportBaseline,
        traitBaseline = baselinePayload.traitBaseline,
        traitExportBaseline = baselinePayload.traitExportBaseline,
        recipeBaseline = baselinePayload.recipeBaseline,
        recipeExportBaseline = baselinePayload.recipeExportBaseline,
        steamId = steamId,
        characterName = characterName,
        debugModified = debugModified,
        capturedAt = BurdJournals.Server.baselineSnapshotNowHours(),
    }

    local function rollbackApply()
        cache.players[characterId] = stagedCacheRecord
        archive.byCharacterId[characterId] = stagedArchiveRecord
        archive.bySteamId = BurdJournals.Server.copyBaselineSteamIndex(stagedArchiveSteamIndex)
        BurdJournals.Server.restoreBaselinePlayerMutationState(stagedPlayerState)
    end

    local appliedRecord = BurdJournals.Server.cloneBaselineRecordForStorage(appliedBaseline)
    cache.players[characterId] = appliedRecord
    if not BurdJournals.Server.storeBaselineArchiveRecord(characterId, appliedRecord, true, true) then
        rollbackApply()
        return false, "Failed to write baseline archive"
    end
    if BurdJournals.Server.writePlayerBaselineBackup
        and not BurdJournals.Server.writePlayerBaselineBackup(targetPlayer, characterId, appliedRecord, true)
    then
        rollbackApply()
        return false, "Failed to write player baseline backup"
    end
    if not BurdJournals.Server.applyBaselinePayloadToPlayerModData(targetPlayer, baselinePayload, debugModified) then
        rollbackApply()
        return false, "Failed to apply player baseline"
    end

    -- Replication happens only after every persistent representation has committed.
    -- A transport failure remains visible but cannot trigger a partial rollback.
    BurdJournals.Server.transmitBaselineStores(true)
    if targetPlayer.transmitModData then targetPlayer:transmitModData() end

    local counts = BurdJournals.getBaselineSnapshotCounts and BurdJournals.getBaselineSnapshotCounts(baselinePayload) or {
        skills = BurdJournals.countTable(baselinePayload.skillBaseline),
        mediaSkills = BurdJournals.countTable(baselinePayload.mediaSkillBaseline),
        traits = BurdJournals.countTable(baselinePayload.traitBaseline),
        recipes = BurdJournals.countTable(baselinePayload.recipeBaseline),
    }
    return true, nil, {
        characterId = characterId,
        restoreMode = normalizedMode,
        debugModified = debugModified,
        counts = counts,
    }
end

function BurdJournals.Server.handleRegisterBaseline(player, args)
    if not player or not args then return end

    local characterId = args.characterId
    if not characterId then
        bsjWriteLogLine("[BurdJournals] ERROR: No characterId in registerBaseline")
        return
    end

    local serverCharacterId = BurdJournals.getPlayerCharacterId(player)
    if serverCharacterId ~= characterId then
        bsjWriteLogLine("[BurdJournals] WARNING: Character ID mismatch! Client sent: " .. characterId .. ", Server computed: " .. tostring(serverCharacterId))

        characterId = serverCharacterId
    end

    local playerModData = player:getModData()
    playerModData.BurdJournals = playerModData.BurdJournals or {}
    playerModData.BurdJournals.lastSeenCharacterId = characterId

    local hoursAlive = BurdJournals.getBaselineLifecycleHours and BurdJournals.getBaselineLifecycleHours(player) or 0
    local snapshotWindowHours = BurdJournals.getBaselineSnapshotMaxHours and BurdJournals.getBaselineSnapshotMaxHours() or 0.5
    if tostring(playerModData.BurdJournals.deferBaselineUntilNewCharacterId or "") == tostring(characterId) then
        local deferredRecoveryPayload = BurdJournals.sanitizeBaselinePayloadForSnapshot
            and BurdJournals.sanitizeBaselinePayloadForSnapshot(args)
            or nil
        local hasDeferredRecoveryPayload = deferredRecoveryPayload ~= nil
            and BurdJournals.baselineHasEntries
            and BurdJournals.baselineHasEntries(deferredRecoveryPayload)
        if hasDeferredRecoveryPayload then
            BurdJournals.debugPrint("[BurdJournals] Clearing established-character baseline deferral for recovered client payload on " .. tostring(characterId))
            playerModData.BurdJournals.deferBaselineUntilNewCharacterId = nil
        else
        BurdJournals.debugPrint("[BurdJournals] Deferring registerBaseline for active character "
            .. tostring(characterId) .. " until next new character")
        BurdJournals.Server.sendToClient(player, "baselineRegistered", {
            success = false,
            characterId = characterId,
            skippedEstablished = true,
            deferredUntilNewCharacter = true,
            hoursAlive = hoursAlive,
        })
        return
        end
    end
    if hoursAlive > snapshotWindowHours then
        local existingBaseline = BurdJournals.Server.getCachedBaseline(characterId, player)
        if not existingBaseline then
            local recoveredPayload = BurdJournals.sanitizeBaselinePayloadForSnapshot
                and BurdJournals.sanitizeBaselinePayloadForSnapshot(args)
                or nil
            local hasRecoveredPayload = recoveredPayload ~= nil
                and BurdJournals.baselineHasEntries
                and BurdJournals.baselineHasEntries(recoveredPayload)
            if hasRecoveredPayload then
                local recoveredBaseline = BurdJournals.Server.cloneBaselineRecordForStorage(recoveredPayload)
                recoveredBaseline.baselineVersion = tonumber(args.baselineVersion)
                    or tonumber(recoveredBaseline.baselineVersion)
                    or tonumber(BurdJournals.BASELINE_VERSION)
                    or 5
                recoveredBaseline.steamId = BurdJournals.getPlayerSteamId(player)
                local descriptor = player.getDescriptor and player:getDescriptor() or nil
                recoveredBaseline.characterName = BurdJournals.getPlayerCharacterName and BurdJournals.getPlayerCharacterName(player)
                    or (descriptor and (descriptor:getForename() .. " " .. descriptor:getSurname()) or nil)
                recoveredBaseline.capturedAt = getGameTime and getGameTime():getWorldAgeHours() or 0
                recoveredBaseline.recoveredFromPlayerModData = true
                recoveredBaseline.migrationSource = "register_client_payload"
                BurdJournals.debugPrint("[BurdJournals] Recovering established baseline from client payload for " .. tostring(characterId))

                local stored = BurdJournals.Server.storeCachedBaseline(characterId, recoveredBaseline, true)
                local activeBaseline = BurdJournals.Server.getCachedBaseline(characterId, player) or recoveredBaseline
                local recovered = stored or type(activeBaseline) == "table"

                if recovered and BurdJournals.Server.captureBaselineSnapshotForPlayer then
                    BurdJournals.Server.captureBaselineSnapshotForPlayer(
                        player,
                        characterId,
                        recoveredBaseline,
                        "register_recovery",
                        "client_payload"
                    )
                end

                local strictMPServer = isStrictMPServer()
                local backupWritten = false
                if strictMPServer and BurdJournals.Server.writePlayerBaselineBackup then
                    backupWritten = BurdJournals.Server.writePlayerBaselineBackup(player, characterId, activeBaseline, true)
                end
                if strictMPServer then
                    local removed = 0
                    local bj = playerModData.BurdJournals
                    local keysToStrip = {
                        "skillBaseline",
                        "skillExportBaseline",
                        "mediaSkillBaseline",
                        "mediaSkillExportBaseline",
                        "traitBaseline",
                        "traitExportBaseline",
                        "recipeBaseline",
                        "recipeExportBaseline",
                    }
                    for _, key in ipairs(keysToStrip) do
                        if bj[key] ~= nil then
                            bj[key] = nil
                            removed = removed + 1
                        end
                    end
                    if removed > 0 or backupWritten then
                        BurdJournals.debugPrint("[BurdJournals] Strict MP recovered baseline updated player ModData backup; stripped legacy baseline tables: " .. tostring(removed))
                        if player.transmitModData then
                            player:transmitModData()
                        end
                    end
                else
                    BurdJournals.Server.applyBaselinePayloadToPlayerModData(player, activeBaseline, activeBaseline.debugModified == true)
                    if player.transmitModData then
                        player:transmitModData()
                    end
                end

                playerModData.BurdJournals.deferBaselineUntilNewCharacterId = nil
                playerModData.BurdJournals.lastSeenCharacterId = characterId

                BurdJournals.Server.sendToClient(player, "baselineRegistered", {
                    success = recovered,
                    characterId = characterId,
                    alreadyExisted = false,
                    recoveredEstablished = true,
                    recoveredFromClientPayload = true,
                    steamId = activeBaseline and activeBaseline.steamId or nil,
                    skillBaseline = activeBaseline and activeBaseline.skillBaseline or {},
                    skillExportBaseline = activeBaseline and activeBaseline.skillExportBaseline or {},
                    mediaSkillBaseline = activeBaseline and activeBaseline.mediaSkillBaseline or {},
                    mediaSkillExportBaseline = activeBaseline and activeBaseline.mediaSkillExportBaseline or {},
                    traitBaseline = activeBaseline and activeBaseline.traitBaseline or {},
                    traitExportBaseline = activeBaseline and activeBaseline.traitExportBaseline or {},
                    recipeBaseline = activeBaseline and activeBaseline.recipeBaseline or {},
                    recipeExportBaseline = activeBaseline and activeBaseline.recipeExportBaseline or {},
                    baselineVersion = tonumber(activeBaseline and activeBaseline.baselineVersion) or tonumber(BurdJournals.BASELINE_VERSION) or 5,
                    debugModified = activeBaseline and activeBaseline.debugModified == true or false,
                    baselineCaptured = true,
                })
                return
            end

            bsjWriteLogLine("[BurdJournals] WARNING: Established character has no recoverable baseline cache: "
                .. tostring(characterId) .. " (hoursAlive=" .. tostring(hoursAlive) .. ").")
            BurdJournals.debugPrint("[BurdJournals] Established-character baseline register skipped with missing cache/archive entry.")
            playerModData.BurdJournals.deferBaselineUntilNewCharacterId = characterId
            if player.transmitModData then
                player:transmitModData()
            end
        end
        BurdJournals.debugPrint("[BurdJournals] Skipping registerBaseline snapshot for established character "
            .. tostring(characterId) .. " (hoursAlive=" .. tostring(hoursAlive)
            .. ", snapshotWindow=" .. tostring(snapshotWindowHours) .. ")")
        BurdJournals.Server.sendToClient(player, "baselineRegistered", {
            success = false,
            characterId = characterId,
            alreadyExisted = existingBaseline ~= nil,
            skippedEstablished = true,
            hoursAlive = hoursAlive,
            missingCache = existingBaseline == nil,
        })
        return
    end

    -- Build the first baseline from authoritative server state. Client payloads are
    -- only transport hints and must never add live/debug-granted progression.
    local baselineData = BurdJournals.Server.buildBaselineForPlayer(player)
    baselineData.steamId = BurdJournals.getPlayerSteamId(player)
    local descriptor = player.getDescriptor and player:getDescriptor() or nil
    baselineData.characterName = BurdJournals.getPlayerCharacterName and BurdJournals.getPlayerCharacterName(player) or (descriptor and (descriptor:getForename() .. " " .. descriptor:getSurname()) or nil)

    local existingBaseline = BurdJournals.Server.getCachedBaseline(characterId, player)
    local replacingExisting = false
    local protectedExisting = existingBaseline and existingBaseline.debugModified == true or false
    local equivalentExisting = existingBaseline and BurdJournals.Server.areBaselinePayloadsEquivalent(existingBaseline, baselineData) or false
    local stored = false
    if existingBaseline then
        -- A baseline is immutable for the life of a character. Re-registering
        -- while the survivor is still inside the snapshot window must not absorb
        -- traits, recipes, or XP granted after creation. Death cleanup removes the
        -- old cache before the next life is allowed to register.
        BurdJournals.debugPrint("[BurdJournals] Fresh-character baseline already exists for "
            .. tostring(characterId) .. "; keeping the first authoritative capture")
    else
        stored = BurdJournals.Server.storeCachedBaseline(characterId, baselineData, false)
    end
    if stored and BurdJournals.Server.captureBaselineSnapshotForPlayer then
        BurdJournals.Server.captureBaselineSnapshotForPlayer(
            player,
            characterId,
            BurdJournals.Server.getCachedBaseline(characterId, player) or baselineData,
            replacingExisting and "register_replacement" or "register"
        )
    end

    local activeBaseline = BurdJournals.Server.getCachedBaseline(characterId, player) or baselineData

    playerModData.BurdJournals = playerModData.BurdJournals or {}
    local strictMPServer = isStrictMPServer()
    local backupWritten = false
    if strictMPServer and BurdJournals.Server.writePlayerBaselineBackup then
        local backupSource = activeBaseline
        backupWritten = BurdJournals.Server.writePlayerBaselineBackup(player, characterId, backupSource, true)
    end
    if strictMPServer then
        local removed = 0
        local bj = playerModData.BurdJournals
        local keysToStrip = {
            "skillBaseline",
            "mediaSkillBaseline",
            "traitBaseline",
            "recipeBaseline",
        }
        for _, key in ipairs(keysToStrip) do
            if bj[key] ~= nil then
                bj[key] = nil
                removed = removed + 1
            end
        end
        if removed > 0 or backupWritten then
            BurdJournals.debugPrint("[BurdJournals] Strict MP baseline register updated player ModData backup; stripped legacy baseline tables: " .. tostring(removed))
            if player.transmitModData then
                player:transmitModData()
            end
        end
    else
        BurdJournals.Server.applyBaselinePayloadToPlayerModData(player, activeBaseline, activeBaseline.debugModified == true)
        if player.transmitModData then
            player:transmitModData()
        end
    end

    playerModData.BurdJournals.lastSeenCharacterId = characterId
    playerModData.BurdJournals.deferBaselineUntilNewCharacterId = nil

    BurdJournals.Server.sendToClient(player, "baselineRegistered", {
        success = stored,
        characterId = characterId,
        alreadyExisted = not stored,
        replacedExisting = replacingExisting,
        protectedExisting = protectedExisting,
        equivalentExisting = equivalentExisting,
        steamId = activeBaseline and activeBaseline.steamId or nil,
        skillBaseline = activeBaseline and activeBaseline.skillBaseline or {},
        skillExportBaseline = activeBaseline and activeBaseline.skillExportBaseline or {},
        mediaSkillBaseline = activeBaseline and activeBaseline.mediaSkillBaseline or {},
        mediaSkillExportBaseline = activeBaseline and activeBaseline.mediaSkillExportBaseline or {},
        traitBaseline = activeBaseline and activeBaseline.traitBaseline or {},
        traitExportBaseline = activeBaseline and activeBaseline.traitExportBaseline or {},
        recipeBaseline = activeBaseline and activeBaseline.recipeBaseline or {},
        recipeExportBaseline = activeBaseline and activeBaseline.recipeExportBaseline or {},
        baselineVersion = tonumber(activeBaseline and activeBaseline.baselineVersion) or tonumber(BurdJournals.BASELINE_VERSION) or 5,
        debugModified = activeBaseline and activeBaseline.debugModified == true or false,
        baselineCaptured = true,
    })
end

-- Build baseline data from server-authoritative player state
function BurdJournals.Server.buildBaselineForPlayer(player)
    local baseline = {
        skillBaseline = {},
        mediaSkillBaseline = {},
        traitBaseline = {},
        recipeBaseline = {},
        baselineVersion = tonumber(BurdJournals.BASELINE_VERSION) or 5,
    }

    if not player then return baseline end

    -- Skills: capture current total XP for all allowed skills
    local allowedSkills = BurdJournals.getAllowedSkills and BurdJournals.getAllowedSkills() or {}
    local xpObj = player.getXp and player:getXp()
    if xpObj then
        for _, skillName in ipairs(allowedSkills) do
            local perk = BurdJournals.getPerkByName and BurdJournals.getPerkByName(skillName)
            if perk then
                local xp = BurdJournals.getPlayerSkillTotalXP and BurdJournals.getPlayerSkillTotalXP(player, perk, skillName) or xpObj:getXP(perk)
                xp = math.max(0, tonumber(xp) or 0)
                if xp and xp > 0 then
                    baseline.skillBaseline[skillName] = xp
                end
            end
        end
    end

    -- Traits: record current traits as baseline
    local traits = BurdJournals.collectPlayerTraits and BurdJournals.collectPlayerTraits(player, false) or {}
    for traitId, _ in pairs(traits) do
        baseline.traitBaseline[traitId] = true
    end

    -- Recipes: only baseline for new characters (avoid baselining existing saves)
    local hoursAlive = BurdJournals.getBaselineLifecycleHours and BurdJournals.getBaselineLifecycleHours(player) or 0
    local snapshotWindowHours = BurdJournals.getBaselineSnapshotMaxHours
        and BurdJournals.getBaselineSnapshotMaxHours()
        or 0.5
    if hoursAlive <= snapshotWindowHours then
        local recipes = BurdJournals.collectPlayerMagazineRecipes and BurdJournals.collectPlayerMagazineRecipes(player, false, true) or {}
        for recipeName, _ in pairs(recipes) do
            baseline.recipeBaseline[recipeName] = true
        end
    end

    if BurdJournals.getPlayerVhsSkillXPMapCopy then
        baseline.mediaSkillBaseline = BurdJournals.getPlayerVhsSkillXPMapCopy(player)
    end

    return baseline
end

function BurdJournals.Server.buildRecoveredBaselineFromSnapshot(player, snapshotRecord)
    if type(snapshotRecord) ~= "table" then
        return nil
    end

    local recoveredBaseline = BurdJournals.Server.cloneBaselineRecordForStorage({
        skillBaseline = snapshotRecord.skillBaseline,
        skillExportBaseline = snapshotRecord.skillExportBaseline,
        mediaSkillBaseline = snapshotRecord.mediaSkillBaseline,
        mediaSkillExportBaseline = snapshotRecord.mediaSkillExportBaseline,
        traitBaseline = snapshotRecord.traitBaseline,
        traitExportBaseline = snapshotRecord.traitExportBaseline,
        recipeBaseline = snapshotRecord.recipeBaseline,
        recipeExportBaseline = snapshotRecord.recipeExportBaseline,
        capturedAt = tonumber(snapshotRecord.capturedAtHours) or tonumber(snapshotRecord.capturedAt),
        steamId = snapshotRecord.steamId
            or (BurdJournals.getPlayerSteamId and BurdJournals.getPlayerSteamId(player))
            or nil,
        characterName = snapshotRecord.characterName or BurdJournals.Server.getPlayerCharacterDisplayName(player),
        debugModified = snapshotRecord.debugModified == true,
        migrationSource = "activeSnapshot",
    })
    if not BurdJournals.Server.snapshotHasEntries(recoveredBaseline) then
        return nil
    end
    recoveredBaseline.recoveredFromSnapshot = true
    recoveredBaseline.recoverySnapshotId = snapshotRecord.snapshotId
    return recoveredBaseline
end

function BurdJournals.Server.buildRecoveredBaselineFromLivePlayer(player)
    if not player or not BurdJournals.Server.buildBaselineForPlayer then
        return nil
    end

    local baseline = BurdJournals.Server.buildBaselineForPlayer(player)
    if type(baseline) ~= "table" or not BurdJournals.Server.snapshotHasEntries(baseline) then
        return nil
    end

    local nowHours = getGameTime and getGameTime():getWorldAgeHours() or 0
    local recoveredBaseline = BurdJournals.Server.cloneBaselineRecordForStorage({
        skillBaseline = baseline.skillBaseline,
        mediaSkillBaseline = baseline.mediaSkillBaseline,
        traitBaseline = baseline.traitBaseline,
        recipeBaseline = baseline.recipeBaseline,
        capturedAt = nowHours,
        steamId = BurdJournals.getPlayerSteamId and BurdJournals.getPlayerSteamId(player) or nil,
        characterName = BurdJournals.Server.getPlayerCharacterDisplayName(player),
        debugModified = false,
        migrationSource = "livePlayerState",
    })
    if not BurdJournals.Server.snapshotHasEntries(recoveredBaseline) then
        return nil
    end
    recoveredBaseline.recoveredFromLivePlayer = true
    return recoveredBaseline
end

function BurdJournals.Server.sendRecoveredBaselineResponse(player, characterId, baselineData, extra)
    extra = type(extra) == "table" and extra or {}
    local response = {
        found = true,
        characterId = characterId,
        skillBaseline = baselineData and baselineData.skillBaseline or {},
        skillExportBaseline = baselineData and baselineData.skillExportBaseline or {},
        mediaSkillBaseline = baselineData and baselineData.mediaSkillBaseline or {},
        mediaSkillExportBaseline = baselineData and baselineData.mediaSkillExportBaseline or {},
        traitBaseline = baselineData and baselineData.traitBaseline or {},
        traitExportBaseline = baselineData and baselineData.traitExportBaseline or {},
        recipeBaseline = baselineData and baselineData.recipeBaseline or {},
        recipeExportBaseline = baselineData and baselineData.recipeExportBaseline or {},
        baselineVersion = tonumber(baselineData and baselineData.baselineVersion) or tonumber(BurdJournals.BASELINE_VERSION) or 5,
        debugModified = baselineData and baselineData.debugModified == true or false,
    }
    for key, value in pairs(extra) do
        response[key] = value
    end
    BurdJournals.Server.sendToClient(player, "baselineResponse", response)
end

function BurdJournals.Server.handleDeleteBaseline(player, args)
    if not player then return end

    local characterId = args and args.characterId
    if not characterId then
        bsjWriteLogLine("[BurdJournals] ERROR: No characterId in deleteBaseline")
        return
    end

    local reason = args and args.reason
    local serverCharacterId = BurdJournals.getPlayerCharacterId(player)
    if serverCharacterId ~= characterId then
        bsjWriteLogLine("[BurdJournals] WARNING: Character ID mismatch in deleteBaseline! Client sent: " .. characterId .. ", Server computed: " .. tostring(serverCharacterId))
        characterId = serverCharacterId
    end

    -- Security:
    -- - Admins can always clear baselines (debug/tools)
    -- - Non-admins may only clear their own baseline during death cleanup
    local accessLevel = player:getAccessLevel()
    local isAdmin = accessLevel and accessLevel ~= "None"
    local isDeathCleanup = reason == "death"
    if not isAdmin then
        local isOwnDeathCleanup = isDeathCleanup and serverCharacterId and characterId and serverCharacterId == characterId
        if not isOwnDeathCleanup then
            bsjWriteLogLine("[BurdJournals] WARNING: Non-admin player attempted deleteBaseline: " .. tostring(player.getUsername and player:getUsername() or "unknown"))
            BurdJournals.Server.sendToClient(player, "error", {message = "Admin access required."})
            return
        end
    end

    local finalizedSnapshot = false
    if isDeathCleanup
        and BurdJournals.getBaselineSnapshotsCaptureOnDeathEnabled
        and BurdJournals.getBaselineSnapshotsCaptureOnDeathEnabled()
        and BurdJournals.Server.finalizeActiveBaselineSnapshot
    then
        finalizedSnapshot = BurdJournals.Server.finalizeActiveBaselineSnapshot(
            characterId,
            player,
            "death",
            "death_cleanup",
            { skipTransmit = true }
        )
    end

    -- A replacement character must be eligible for its own one-time blank
    -- journal even if the engine temporarily carries the dead IsoPlayer's
    -- ModData through the respawn transition.
    local clearedSpawnJournalProvision = false
    if isDeathCleanup and player.getModData then
        local playerModData = player:getModData()
        local playerJournalState = type(playerModData) == "table" and playerModData.BurdJournals or nil
        if type(playerJournalState) == "table"
            and playerJournalState.spawnBlankPlayerJournalProvisioned ~= nil
        then
            playerJournalState.spawnBlankPlayerJournalProvisioned = nil
            clearedSpawnJournalProvision = true
        end
    end

    local cache = BurdJournals.Server.getBaselineCache()
    cache.players = cache.players or {}
    local removedFromCache = false
    local removedFromArchive = false
    local removedBackup = false
    if cache.players[characterId] then
        cache.players[characterId] = nil
        removedFromCache = true
    end
    removedFromArchive = BurdJournals.Server.removeArchivedBaseline(characterId, true)
    if serverCharacterId and characterId and tostring(serverCharacterId) == tostring(characterId)
        and BurdJournals.Server.clearPlayerBaselineBackup then
        removedBackup = BurdJournals.Server.clearPlayerBaselineBackup(player, true)
    end

    if removedFromCache or removedFromArchive or removedBackup then
        BurdJournals.Server.transmitBaselineStores(true)
        if BurdJournals.Server.transmitBaselineSnapshotStore then
            BurdJournals.Server.transmitBaselineSnapshotStore()
        end
        if removedBackup and player.transmitModData then
            player:transmitModData()
        end
        BurdJournals.debugPrint("[BurdJournals] Deleted baseline for: " .. characterId
            .. " (cache=" .. tostring(removedFromCache)
            .. ", archive=" .. tostring(removedFromArchive)
            .. ", backup=" .. tostring(removedBackup) .. ")")
    else
        BurdJournals.debugPrint("[BurdJournals] No cached/archive baseline to delete for: " .. characterId)
        if finalizedSnapshot and BurdJournals.Server.transmitBaselineSnapshotStore then
            BurdJournals.Server.transmitBaselineSnapshotStore()
        end
    end
    if clearedSpawnJournalProvision and player.transmitModData then
        player:transmitModData()
    end
end

-- Admin command to clear ALL baseline caches server-wide
-- This allows a fresh start for all players - baselines will be captured on next character creation
function BurdJournals.Server.handleClearAllBaselines(player, _args)
    if not player then return end

    -- Check if player is admin
    local accessLevel = player:getAccessLevel()
    if not accessLevel or accessLevel == "None" then
        bsjWriteLogLine("[BurdJournals] WARNING: Non-admin player attempted clearAllBaselines: " .. tostring(player:getUsername()))
        BurdJournals.Server.sendToClient(player, "error", {message = "Admin access required."})
        return
    end

    local cache = BurdJournals.Server.getBaselineCache()
    local archive = BurdJournals.Server.getBaselineArchive()
    local snapshotStore = BurdJournals.Server.getBaselineSnapshotStore and BurdJournals.Server.getBaselineSnapshotStore() or nil
    cache.players = cache.players or {}
    archive.byCharacterId = archive.byCharacterId or {}
    archive.bySteamId = archive.bySteamId or {}
    local clearedCount = 0
    local archivedClearedCount = 0
    local snapshotClearedCount = 0
    local backupClearedCount = 0
    local historyClearedCount = 0

    -- Count entries before clearing
    for _ in pairs(cache.players) do
        clearedCount = clearedCount + 1
    end
    for _ in pairs(archive.byCharacterId) do
        archivedClearedCount = archivedClearedCount + 1
    end
    if snapshotStore and type(snapshotStore.bySnapshotId) == "table" then
        for _ in pairs(snapshotStore.bySnapshotId) do
            snapshotClearedCount = snapshotClearedCount + 1
        end
    end

    -- Clear all cached baselines
    local nowHours = getGameTime and getGameTime():getWorldAgeHours() or 0
    cache.players = {}
    cache._backupResetEpochHours = nowHours
    archive.byCharacterId = {}
    archive.bySteamId = {}
    local mirrorField = BurdJournals.Server.BASELINE_SNAPSHOT_ARCHIVE_MIRROR_FIELD or "baselineSnapshotsMirrorV1"
    local legacyMirrorField = BurdJournals.Server.BASELINE_SNAPSHOT_ARCHIVE_MIRROR_FIELD_LEGACY or "_baselineSnapshotsMirrorV1"
    archive[mirrorField] = nil
    archive[legacyMirrorField] = nil
    archive._updatedAt = nowHours
    if snapshotStore then
        snapshotStore.bySnapshotId = {}
        snapshotStore.bySteamId = {}
        snapshotStore.byCharacterId = {}
        snapshotStore.activeBySteamId = {}
        snapshotStore.activeByCharacterId = {}
        snapshotStore._updatedAt = nowHours
    end

    -- Persist to disk
    BurdJournals.Server.transmitBaselineStores(true)
    if BurdJournals.Server.transmitBaselineSnapshotStore then
        BurdJournals.Server.transmitBaselineSnapshotStore()
    end

    -- Clear connected players' per-player baseline backups
    local onlinePlayers = getOnlinePlayers and getOnlinePlayers()
    if onlinePlayers and BurdJournals.Server.clearPlayerBaselineBackup then
        for i = 0, onlinePlayers:size() - 1 do
            local onlinePlayer = onlinePlayers:get(i)
            if onlinePlayer then
                if BurdJournals.Server.clearPlayerBaselineBackup(onlinePlayer, true) then
                    backupClearedCount = backupClearedCount + 1
                end
                if BurdJournals.Server.clearPlayerBaselineSnapshotHistory then
                    historyClearedCount = historyClearedCount
                        + (BurdJournals.Server.clearPlayerBaselineSnapshotHistory(onlinePlayer, true) or 0)
                end
                if onlinePlayer.getModData then
                    local playerModData = onlinePlayer:getModData()
                    playerModData.BurdJournals = playerModData.BurdJournals or {}
                    playerModData.BurdJournals.skillBaseline = nil
                    playerModData.BurdJournals.mediaSkillBaseline = nil
                    playerModData.BurdJournals.traitBaseline = nil
                    playerModData.BurdJournals.recipeBaseline = nil
                    playerModData.BurdJournals.baselineCaptured = false
                    playerModData.BurdJournals.debugModified = false
                end
                if onlinePlayer.transmitModData then
                    onlinePlayer:transmitModData()
                end
            end
        end
    end

    bsjWriteLogLine("[BurdJournals] ADMIN " .. tostring(player:getUsername()) .. " cleared ALL baseline caches (cache="
        .. clearedCount .. ", archive=" .. archivedClearedCount .. ", snapshots=" .. snapshotClearedCount
        .. ", playerBackups=" .. backupClearedCount .. ", playerSnapshotHistory=" .. historyClearedCount .. ")")

    -- Notify the admin
    BurdJournals.Server.sendToClient(player, "allBaselinesCleared", {
        clearedCount = clearedCount,
        archivedClearedCount = archivedClearedCount,
        snapshotClearedCount = snapshotClearedCount,
        backupClearedCount = backupClearedCount,
        historyClearedCount = historyClearedCount,
    })
end

function BurdJournals.Server.handleRequestBaseline(player, args)
    if not player then return end

    local characterId = BurdJournals.getPlayerCharacterId(player)
    if not characterId then
        bsjWriteLogLine("[BurdJournals] ERROR: Could not compute characterId for baseline request")
        BurdJournals.Server.sendToClient(player, "baselineResponse", {
            found = false,
            characterId = nil
        })
        return
    end

    if BurdJournals.Server.mergeBaselineSnapshotsFromPlayerHistory then
        BurdJournals.Server.mergeBaselineSnapshotsFromPlayerHistory(player, false)
    end

    local playerModData = player.getModData and player:getModData() or nil
    if playerModData then
        playerModData.BurdJournals = playerModData.BurdJournals or {}
    end
    local playerBaseline = playerModData and playerModData.BurdJournals or nil
    local hadSeenCharacterBefore = type(playerBaseline) == "table"
        and tostring(playerBaseline.lastSeenCharacterId or "") == tostring(characterId)
    local wasDeferredForCharacter = type(playerBaseline) == "table"
        and tostring(playerBaseline.deferBaselineUntilNewCharacterId or "") == tostring(characterId)
    if type(playerBaseline) == "table" then
        playerBaseline.lastSeenCharacterId = characterId
    end

    local cachedBaseline = BurdJournals.Server.getCachedBaseline(characterId, player)

    if cachedBaseline then
        BurdJournals.debugPrint("[BurdJournals] Found cached baseline for " .. characterId)
        if type(playerBaseline) == "table"
            and tostring(playerBaseline.deferBaselineUntilNewCharacterId or "") == tostring(characterId) then
            playerBaseline.deferBaselineUntilNewCharacterId = nil
            if player.transmitModData then
                player:transmitModData()
            end
        end
        if isStrictMPServer() and BurdJournals.Server.writePlayerBaselineBackup then
            local hasBackup = BurdJournals.Server.readPlayerBaselineBackup
                and BurdJournals.Server.readPlayerBaselineBackup(player, characterId) ~= nil
            BurdJournals.Server.writePlayerBaselineBackup(player, characterId, cachedBaseline, hasBackup == true)
        end
        BurdJournals.Server.sendToClient(player, "baselineResponse", {
            found = true,
            characterId = characterId,
            skillBaseline = cachedBaseline.skillBaseline,
            skillExportBaseline = cachedBaseline.skillExportBaseline or {},
            mediaSkillBaseline = cachedBaseline.mediaSkillBaseline or {},
            mediaSkillExportBaseline = cachedBaseline.mediaSkillExportBaseline or {},
            traitBaseline = cachedBaseline.traitBaseline,
            traitExportBaseline = cachedBaseline.traitExportBaseline or {},
            recipeBaseline = cachedBaseline.recipeBaseline,
            recipeExportBaseline = cachedBaseline.recipeExportBaseline or {},
            baselineVersion = tonumber(cachedBaseline.baselineVersion) or tonumber(BurdJournals.BASELINE_VERSION) or 5,
            debugModified = cachedBaseline.debugModified  -- Include debug flag
        })
    else
        -- CRITICAL: Server cache is empty, but player's own ModData might have baseline!
        -- This handles THREE scenarios:
        -- 1. MIGRATION: Old production version stored baseline in player ModData - migrate to new system
        -- 2. RECOVERY: After mod update, global ModData was lost but player ModData was preserved
        -- 3. STRICT MP RECOVERY: serverBaselineBackup snapshot in player ModData
        local normalizedPlayerBaseline = BurdJournals.Server.extractBaselinePayloadFromPlayerModData(player)

        -- Log what we found for diagnostics
        local hasBaselineFlag = playerBaseline and playerBaseline.baselineCaptured == true
        local hasSkillData = normalizedPlayerBaseline and BurdJournals.Server.baselineTableHasEntries(normalizedPlayerBaseline.skillBaseline)
        local hasMediaSkillData = normalizedPlayerBaseline and BurdJournals.Server.baselineTableHasEntries(normalizedPlayerBaseline.mediaSkillBaseline)
        local hasTraitData = normalizedPlayerBaseline and BurdJournals.Server.baselineTableHasEntries(normalizedPlayerBaseline.traitBaseline)
        local hasRecipeData = normalizedPlayerBaseline and BurdJournals.Server.baselineTableHasEntries(normalizedPlayerBaseline.recipeBaseline)
        local hasDebugFlag = normalizedPlayerBaseline and normalizedPlayerBaseline.debugModified == true
        local hasAnyBaselineData = hasSkillData or hasMediaSkillData or hasTraitData or hasRecipeData
        local backupBaseline = BurdJournals.Server.readPlayerBaselineBackup
            and BurdJournals.Server.readPlayerBaselineBackup(player, characterId)
            or nil
        
        BurdJournals.debugPrint("[BurdJournals] No server cache for " .. characterId .. " - checking player ModData...")
        BurdJournals.debugPrint("[BurdJournals]   baselineCaptured: " .. tostring(hasBaselineFlag))
        BurdJournals.debugPrint("[BurdJournals]   skillBaseline: " .. tostring(hasSkillData))
        BurdJournals.debugPrint("[BurdJournals]   mediaSkillBaseline: " .. tostring(hasMediaSkillData))
        BurdJournals.debugPrint("[BurdJournals]   traitBaseline: " .. tostring(hasTraitData))
        BurdJournals.debugPrint("[BurdJournals]   recipeBaseline: " .. tostring(hasRecipeData))
        BurdJournals.debugPrint("[BurdJournals]   debugModified: " .. tostring(hasDebugFlag))
        BurdJournals.debugPrint("[BurdJournals]   serverBaselineBackup: " .. tostring(backupBaseline ~= nil))

        -- Strict-MP recovery path: restore from compact player-level backup if global cache/archive is missing.
        if not hasAnyBaselineData and backupBaseline then
            local recoveredBaseline = BurdJournals.Server.recoverCachedBaselineFromPlayerBackup(characterId, player, {
                transmitStores = true,
                transmitPlayer = true,
            })
            if recoveredBaseline then
                BurdJournals.Server.sendToClient(player, "baselineResponse", {
                    found = true,
                    characterId = characterId,
                    skillBaseline = recoveredBaseline.skillBaseline or {},
                    skillExportBaseline = recoveredBaseline.skillExportBaseline or {},
                    mediaSkillBaseline = recoveredBaseline.mediaSkillBaseline or {},
                    mediaSkillExportBaseline = recoveredBaseline.mediaSkillExportBaseline or {},
                    traitBaseline = recoveredBaseline.traitBaseline or {},
                    traitExportBaseline = recoveredBaseline.traitExportBaseline or {},
                    recipeBaseline = recoveredBaseline.recipeBaseline or {},
                    recipeExportBaseline = recoveredBaseline.recipeExportBaseline or {},
                    baselineVersion = tonumber(recoveredBaseline.baselineVersion) or tonumber(BurdJournals.BASELINE_VERSION) or 5,
                    debugModified = recoveredBaseline.debugModified == true,
                    recovered = true,
                    recoverySource = "playerBackup"
                })
                return
            end
        end
        
        if playerBaseline and (hasBaselineFlag or hasAnyBaselineData) then
            if hasAnyBaselineData then
                -- Determine if this is migration from old version or recovery from mod update
                local recoveredWithoutFlag = (not hasBaselineFlag) and hasAnyBaselineData
                local recoveryType = hasDebugFlag and "RECOVERY (debug-modified baseline)"
                    or (recoveredWithoutFlag and "RECOVERY (baseline tables without captured flag)")
                    or "MIGRATION (from player ModData)"
                
                local restoredBaseline = BurdJournals.Server.cloneBaselineRecordForStorage(normalizedPlayerBaseline or {})
                restoredBaseline.debugModified = hasDebugFlag
                if not BurdJournals.Server.baselineTableHasEntries(restoredBaseline.mediaSkillBaseline)
                    and BurdJournals.getPlayerVhsSkillXPMapCopy
                then
                    restoredBaseline.mediaSkillBaseline = BurdJournals.Server.copyBaselineTableEntries(
                        BurdJournals.getPlayerVhsSkillXPMapCopy(player)
                    )
                end
                restoredBaseline.steamId = BurdJournals.getPlayerSteamId(player)
                restoredBaseline.characterName = BurdJournals.Server.getPlayerCharacterDisplayName(player)
                restoredBaseline.capturedAt = getGameTime():getWorldAgeHours()
                restoredBaseline.recoveredFromPlayerModData = true
                restoredBaseline.migrationSource = hasDebugFlag and "recovery" or "migration"
                local counts = BurdJournals.getBaselineSnapshotCounts
                    and BurdJournals.getBaselineSnapshotCounts(restoredBaseline)
                    or {
                        skills = BurdJournals.countTable(restoredBaseline.skillBaseline),
                        mediaSkills = BurdJournals.countTable(restoredBaseline.mediaSkillBaseline),
                        traits = BurdJournals.countTable(restoredBaseline.traitBaseline),
                        recipes = BurdJournals.countTable(restoredBaseline.recipeBaseline),
                    }
                
                BurdJournals.debugPrint("[BurdJournals] " .. recoveryType .. " for " .. characterId)
                BurdJournals.debugPrint("[BurdJournals]   Restoring " .. tostring(counts.skills)
                    .. " skill baselines, " .. tostring(counts.mediaSkills) .. " media skill baselines, "
                    .. tostring(counts.traits) .. " trait baselines, " .. tostring(counts.recipes)
                    .. " recipe baselines")
                
                -- Store in server cache (force overwrite since cache is empty)
                local cache = BurdJournals.Server.getBaselineCache()
                cache.players[characterId] = restoredBaseline
                BurdJournals.Server.storeBaselineArchiveRecord(characterId, restoredBaseline, true)
                if BurdJournals.Server.writePlayerBaselineBackup then
                    BurdJournals.Server.writePlayerBaselineBackup(player, characterId, restoredBaseline, true)
                end

                -- Ensure player ModData also carries a recoverable baseline backup.
                playerBaseline.skillBaseline = restoredBaseline.skillBaseline
                playerBaseline.skillExportBaseline = restoredBaseline.skillExportBaseline
                playerBaseline.mediaSkillBaseline = restoredBaseline.mediaSkillBaseline
                playerBaseline.mediaSkillExportBaseline = restoredBaseline.mediaSkillExportBaseline
                playerBaseline.traitBaseline = restoredBaseline.traitBaseline
                playerBaseline.traitExportBaseline = restoredBaseline.traitExportBaseline
                playerBaseline.recipeBaseline = restoredBaseline.recipeBaseline
                playerBaseline.recipeExportBaseline = restoredBaseline.recipeExportBaseline
                playerBaseline.baselineCaptured = true
                playerBaseline.baselineVersion = tonumber(restoredBaseline.baselineVersion)
                    or tonumber(playerBaseline.baselineVersion)
                    or tonumber(BurdJournals.BASELINE_VERSION)
                    or 5
                
                -- Persist the recovered data
                BurdJournals.Server.transmitBaselineStores(true)
                if BurdJournals.Server.captureBaselineSnapshotForPlayer then
                    BurdJournals.Server.captureBaselineSnapshotForPlayer(
                        player,
                        characterId,
                        restoredBaseline,
                        "request_recovery",
                        tostring(restoredBaseline.migrationSource or "player_moddata")
                    )
                end
                if player.transmitModData then
                    player:transmitModData()
                end
                
                BurdJournals.debugPrint("[BurdJournals] SUCCESS: Baseline restored to server cache for " .. characterId)
                
                -- Return the recovered baseline
                BurdJournals.Server.sendToClient(player, "baselineResponse", {
                    found = true,
                    characterId = characterId,
                    skillBaseline = restoredBaseline.skillBaseline,
                    skillExportBaseline = restoredBaseline.skillExportBaseline or {},
                    mediaSkillBaseline = restoredBaseline.mediaSkillBaseline or {},
                    mediaSkillExportBaseline = restoredBaseline.mediaSkillExportBaseline or {},
                    traitBaseline = restoredBaseline.traitBaseline,
                    traitExportBaseline = restoredBaseline.traitExportBaseline or {},
                    recipeBaseline = restoredBaseline.recipeBaseline,
                    recipeExportBaseline = restoredBaseline.recipeExportBaseline or {},
                    baselineVersion = tonumber(restoredBaseline.baselineVersion) or tonumber(BurdJournals.BASELINE_VERSION) or 5,
                    debugModified = restoredBaseline.debugModified,
                    recovered = true  -- Let client know this was recovered
                })
                return
            else
                BurdJournals.debugPrint("[BurdJournals] Player baseline marker/data found but no actual recoverable entries")
            end
        elseif playerBaseline then
            -- Player has BurdJournals data but no recoverable baseline tables.
            BurdJournals.debugPrint("[BurdJournals] Player has BurdJournals data but no recoverable baseline tables")
        else
            BurdJournals.debugPrint("[BurdJournals] No BurdJournals data in player ModData")
        end

        local steamId = BurdJournals.getPlayerSteamId and BurdJournals.getPlayerSteamId(player) or nil
        local snapshotStore = BurdJournals.Server.getBaselineSnapshotStore
            and BurdJournals.Server.getBaselineSnapshotStore()
            or nil
        local activeSnapshotId, activeSnapshot = BurdJournals.Server.getActiveBaselineSnapshotRecord(snapshotStore, steamId, characterId)
        if activeSnapshotId and type(activeSnapshot) == "table" then
            local recoveredFromSnapshot = BurdJournals.Server.buildRecoveredBaselineFromSnapshot(player, activeSnapshot)
            if recoveredFromSnapshot then
                BurdJournals.debugPrint("[BurdJournals] RECOVERY (active snapshot) for " .. characterId
                    .. " using " .. tostring(activeSnapshotId))
                local stored = BurdJournals.Server.storeCachedBaseline(characterId, recoveredFromSnapshot, true)
                if stored then
                    BurdJournals.Server.storeBaselineArchiveRecord(characterId, recoveredFromSnapshot, true)
                    if BurdJournals.Server.writePlayerBaselineBackup then
                        BurdJournals.Server.writePlayerBaselineBackup(player, characterId, recoveredFromSnapshot, true)
                    end
                    if player.transmitModData then
                        player:transmitModData()
                    end

                    BurdJournals.Server.sendRecoveredBaselineResponse(player, characterId, recoveredFromSnapshot, {
                        recovered = true,
                        recoverySource = "activeSnapshot",
                        recoveredSnapshotId = activeSnapshotId,
                    })
                    return
                end
            end
        end

        local hoursAlive = BurdJournals.getBaselineLifecycleHours and BurdJournals.getBaselineLifecycleHours(player) or 0
        local snapshotWindowHours = BurdJournals.getBaselineSnapshotMaxHours
            and BurdJournals.getBaselineSnapshotMaxHours()
            or 0.5
        if type(playerBaseline) == "table"
            and (hoursAlive > snapshotWindowHours or hadSeenCharacterBefore or wasDeferredForCharacter) then
            playerBaseline.deferBaselineUntilNewCharacterId = characterId
            if player.transmitModData then
                player:transmitModData()
            end
            BurdJournals.debugPrint("[BurdJournals] Deferring baseline capture for active character "
                .. tostring(characterId) .. " (hoursAlive=" .. tostring(hoursAlive)
                .. ", seenBefore=" .. tostring(hadSeenCharacterBefore)
                .. ", deferred=" .. tostring(wasDeferredForCharacter) .. ")")
            BurdJournals.Server.sendToClient(player, "baselineResponse", {
                found = false,
                characterId = characterId,
                deferUntilNewCharacter = true,
                hoursAlive = hoursAlive,
            })
            return
        end

        if player.transmitModData and type(playerBaseline) == "table" then
            player:transmitModData()
        end
        BurdJournals.debugPrint("[BurdJournals] No baseline found for " .. characterId .. " (new player)")
        BurdJournals.Server.sendToClient(player, "baselineResponse", {
            found = false,
            characterId = characterId
        })
    end
end

BurdJournals.Server.BASELINE_CACHE_TTL_HOURS = 720

BurdJournals.Server._lastBaselineCleanup = 0

BurdJournals.Server.BASELINE_CLEANUP_INTERVAL = 24

function BurdJournals.Server.pruneBaselineCache()
    local cache = BurdJournals.Server.getBaselineCache()
    if not cache.players then return 0 end

    local currentHours = getGameTime():getWorldAgeHours()
    local ttl = BurdJournals.Server.BASELINE_CACHE_TTL_HOURS
    local prunedCount = 0
    local backfilledCapturedAt = 0
    local archivePrunedCount = 0
    local archiveTouched = false
    local toRemove = {}

    for characterId, baseline in pairs(cache.players) do
        if type(baseline) ~= "table" then
            table.insert(toRemove, characterId)
        else
            local capturedAt = tonumber(baseline.capturedAt)
            if not capturedAt or capturedAt <= 0 then
                -- Legacy entries may not have capturedAt; backfill instead of pruning.
                baseline.capturedAt = currentHours
                backfilledCapturedAt = backfilledCapturedAt + 1
                BurdJournals.Server.storeBaselineArchiveRecord(characterId, baseline, true)
                archiveTouched = true
            else
                local age = currentHours - capturedAt
                if age > ttl then
                    table.insert(toRemove, characterId)
                end
            end
        end
    end

    for _, characterId in ipairs(toRemove) do
        cache.players[characterId] = nil
        if BurdJournals.Server.removeArchivedBaseline(characterId, true) then
            archivePrunedCount = archivePrunedCount + 1
            archiveTouched = true
        end
        prunedCount = prunedCount + 1
        BurdJournals.debugPrint("[BurdJournals] Pruned stale baseline for: " .. characterId)
    end

    if prunedCount > 0 or backfilledCapturedAt > 0 or archiveTouched then
        -- Persist pruned/backfilled cache to disk
        BurdJournals.Server.transmitBaselineStores(true)
        BurdJournals.debugPrint("[BurdJournals] Baseline cache cleanup: removed "
            .. prunedCount .. " stale entries, backfilled capturedAt for " .. backfilledCapturedAt
            .. " entries, pruned archive entries " .. archivePrunedCount)
    end

    return prunedCount
end

function BurdJournals.Server.checkBaselineCleanup()
    local currentHours = getGameTime():getWorldAgeHours()
    local timeSinceCleanup = currentHours - BurdJournals.Server._lastBaselineCleanup

    if BurdJournals.Server.processYuletideHourlyTasks then
        BurdJournals.Server.processYuletideHourlyTasks()
    end

    if timeSinceCleanup >= BurdJournals.Server.BASELINE_CLEANUP_INTERVAL then
        BurdJournals.Server._lastBaselineCleanup = currentHours
        BurdJournals.Server.pruneBaselineCache()
    end
end

function BurdJournals.Server.forceBaselineCleanup()
    BurdJournals.debugPrint("[BurdJournals] Admin: Forcing baseline cache cleanup...")
    local pruned = BurdJournals.Server.pruneBaselineCache()
    BurdJournals.debugPrint("[BurdJournals] Admin: Cleanup complete, removed " .. pruned .. " entries")
    return pruned
end

-- One-shot admin migration: proactively migrate all online players' journals.
function BurdJournals.Server.forEachInventoryItemRecursive(container, visitor)
    if not container or not visitor or not container.getItems then
        return
    end
    local items = container:getItems()
    if not items then
        return
    end
    for i = 0, items:size() - 1 do
        local item = items:get(i)
        if item then
            visitor(item)
            if item.getInventory then
                local subInventory = item:getInventory()
                if subInventory then
                    BurdJournals.Server.forEachInventoryItemRecursive(subInventory, visitor)
                end
            end
        end
    end
end

function BurdJournals.Server.handleDebugMigrateOnlineJournals(player, args)
    if not BurdJournals.Server.isDebugAllowed(player) then
        BurdJournals.Server.sendToClient(player, "debugError", {message = "Debug commands not allowed"})
        return
    end

    local stats = {
        players = 0,
        journalsScanned = 0,
        journalsUpdated = 0,
    }

    local function processPlayer(targetPlayer)
        if not targetPlayer or not targetPlayer.getInventory then
            return
        end
        local inventory = targetPlayer:getInventory()
        if not inventory then
            return
        end

        stats.players = stats.players + 1
        BurdJournals.Server.forEachInventoryItemRecursive(inventory, function(item)
            if not item or not item.getModData then
                return
            end
            local modData = item:getModData()
            local data = modData and modData.BurdJournals
            if type(data) ~= "table" then
                return
            end

            stats.journalsScanned = stats.journalsScanned + 1
            local beforeSchema = tonumber(data.migrationSchemaVersion) or 0
            local beforeSanitize = tonumber(data.sanitizedVersion) or 0
            local beforeCompact = tonumber(data.compactVersion) or 0
            local beforeDrLegacy = data.drLegacyMode3Migrated == true

            BurdJournals.migrateJournalIfNeeded(item, targetPlayer)

            local afterData = modData.BurdJournals or data
            local afterSchema = tonumber(afterData.migrationSchemaVersion) or 0
            local afterSanitize = tonumber(afterData.sanitizedVersion) or 0
            local afterCompact = tonumber(afterData.compactVersion) or 0
            local afterDrLegacy = afterData.drLegacyMode3Migrated == true
            if afterSchema > beforeSchema
                or afterSanitize > beforeSanitize
                or afterCompact > beforeCompact
                or (afterDrLegacy and not beforeDrLegacy) then
                stats.journalsUpdated = stats.journalsUpdated + 1
            end
        end)
    end

    local onlinePlayers = getOnlinePlayers and getOnlinePlayers() or nil
    if onlinePlayers and onlinePlayers.size then
        for i = 0, onlinePlayers:size() - 1 do
            processPlayer(onlinePlayers:get(i))
        end
    else
        processPlayer(player)
    end

    local message = "Journal migration complete: "
        .. tostring(stats.journalsUpdated)
        .. " updated / "
        .. tostring(stats.journalsScanned)
        .. " scanned across "
        .. tostring(stats.players)
        .. " player(s)."
    BurdJournals.debugPrint("[BurdJournals] " .. message)
    BurdJournals.Server.sendToClient(player, "debugSuccess", {message = message, stats = stats})
end

-- ============================================================================
-- Debug Journal Backup System (Server-side persistence for MP)
-- ============================================================================
-- These functions mirror the baseline system to provide server-side persistence
-- for debug-edited journals on dedicated servers where client ModData.transmit
-- doesn't persist to the global ModData cache.

-- Get or create the debug journal backup cache (server-side global ModData)
function BurdJournals.Server.getDebugJournalCache()
    local cache = ModData.getOrCreate("BurdJournals_DebugJournalCache")
    if not cache.journals then
        cache.journals = {}
    end
    return cache
end

BurdJournals.Server.DEBUG_JOURNAL_CACHE_MAX = 128
function BurdJournals.Server.pruneDebugJournalCache(cache)
    cache = cache or BurdJournals.Server.getDebugJournalCache()
    local journals = cache and cache.journals
    if type(journals) ~= "table" then return 0 end
    local unique = {}
    for key, snapshot in pairs(journals) do
        if type(snapshot) == "table" then
            local uuid = trimUUID(snapshot.uuid or key) or tostring(key)
            local entry = unique[uuid]
            if not entry then unique[uuid] = {uuid = uuid, timestamp = tonumber(snapshot.timestamp) or 0, pending = snapshot.pendingApply == true}
            else entry.pending = entry.pending or snapshot.pendingApply == true; entry.timestamp = math.max(entry.timestamp, tonumber(snapshot.timestamp) or 0) end
        end
    end
    local entries = {}; for _, entry in pairs(unique) do table.insert(entries, entry) end
    local maxEntries = math.max(1, tonumber(BurdJournals.Server.DEBUG_JOURNAL_CACHE_MAX) or 128)
    if #entries <= maxEntries then return 0 end
    table.sort(entries, function(a, b) return a.timestamp < b.timestamp end)
    local removed = 0
    for _, entry in ipairs(entries) do
        if #entries - removed <= maxEntries then break end
        if not entry.pending then
            for key, snapshot in pairs(journals) do
                if type(snapshot) == "table" and (trimUUID(snapshot.uuid or key) or tostring(key)) == entry.uuid then journals[key] = nil end
            end
            removed = removed + 1
        end
    end
    return removed
end

function BurdJournals.Server.getJournalUUIDIndex()
    local cache = ModData.getOrCreate("BurdJournals_JournalUUIDIndex")
    if type(cache.journals) ~= "table" then
        cache.journals = {}
    end
    return cache
end

function BurdJournals.Server.getRespawnJournalCache()
    local cache = ModData.getOrCreate("BurdJournals_RespawnJournalCache")
    if type(cache.bySteamId) ~= "table" then
        cache.bySteamId = {}
    end
    return cache
end

function BurdJournals.Server.getPlayerJournalRegistry()
    local registry = nil
    if ModData and ModData.getOrCreate then
        registry = ModData.getOrCreate("BurdJournals_PlayerJournalRegistry")
    else
        BurdJournals.Server._playerJournalRegistryFallback = BurdJournals.Server._playerJournalRegistryFallback or {}
        registry = BurdJournals.Server._playerJournalRegistryFallback
    end
    if type(registry.bySteamId) ~= "table" then
        registry.bySteamId = {}
    end
    if type(registry.byUUID) ~= "table" then
        registry.byUUID = {}
    end
    registry.schemaVersion = math.max(1, tonumber(registry.schemaVersion) or 0)
    return registry
end

function BurdJournals.Server.isTrueSinglePlayerContext()
    local localPlayer = getPlayer and getPlayer() or nil
    return localPlayer ~= nil and not (isClient and isClient())
end

function BurdJournals.Server.persistPlayerJournalRegistry(registry)
    if not (ModData and ModData.add and type(registry) == "table") then
        return false
    end
    ModData.add("BurdJournals_PlayerJournalRegistry", registry)
    -- This is a server recovery cache.  Broadcasting it sends every player's
    -- journal snapshot to every client, including strict-MP entry-store data.
    -- Keep the SP compatibility behavior, but never globally publish it in MP.
    if BurdJournals.Server.isTrueSinglePlayerContext() and ModData.transmit then
        ModData.transmit("BurdJournals_PlayerJournalRegistry")
    end
    return true
end

function BurdJournals.Server.persistJournalUUIDIndex(indexCache)
    if not (ModData and type(indexCache) == "table") then return false end
    if ModData.add then ModData.add("BurdJournals_JournalUUIDIndex", indexCache) end
    -- Admin clients receive an explicit, targeted debugJournalUUIDIndexList response.
    if BurdJournals.Server.isTrueSinglePlayerContext() and ModData.transmit then ModData.transmit("BurdJournals_JournalUUIDIndex") end
    return true
end

function BurdJournals.Server.getRespawnJournalTimestamp()
    return getTimestampMs and getTimestampMs() or os.time()
end

function BurdJournals.Server.getRespawnJournalSquareSnapshot(square)
    if not square then
        return nil
    end
    local x = square.getX and square:getX() or nil
    local y = square.getY and square:getY() or nil
    local z = square.getZ and square:getZ() or nil
    if x == nil or y == nil or z == nil then
        return nil
    end
    return {
        x = tonumber(x),
        y = tonumber(y),
        z = tonumber(z),
    }
end

function BurdJournals.Server.containerContainsItemRecursive(container, targetItem, seen)
    if not container or not targetItem or not container.getItems then
        return false
    end
    seen = seen or {}
    local containerKey = tostring(container)
    if seen[containerKey] then
        return false
    end
    seen[containerKey] = true

    local items = container:getItems()
    if not items then
        return false
    end
    for i = 0, items:size() - 1 do
        local item = items:get(i)
        if item == targetItem then
            return true
        end
        if item and item.getInventory then
            local subInventory = item:getInventory()
            if subInventory and BurdJournals.Server.containerContainsItemRecursive(subInventory, targetItem, seen) then
                return true
            end
        end
    end
    return false
end

function BurdJournals.Server.getContainerRespawnLocationSnapshot(container, sourceTag)
    if not container then
        return nil
    end

    local location = {
        kind = "container",
        sourceTag = tostring(sourceTag or "unknown"),
        containerType = container.getType and tostring(container:getType()) or nil,
    }

    local parent = container.getParent and container:getParent() or nil
    if parent then
        location.parentType = parent.getObjectName and tostring(parent:getObjectName())
            or parent.getName and tostring(parent:getName())
            or parent.getType and tostring(parent:getType())
            or tostring(parent)
    end

    local square = nil
    if parent and parent.getSquare then
        square = parent:getSquare()
    end
    if not square and parent and parent.getCurrentSquare then
        square = parent:getCurrentSquare()
    end
    if not square and container.getSourceGrid then
        square = container:getSourceGrid()
    end

    local squareSnapshot = BurdJournals.Server.getRespawnJournalSquareSnapshot(square)
    if squareSnapshot then
        location.x = squareSnapshot.x
        location.y = squareSnapshot.y
        location.z = squareSnapshot.z

        local objects = square.getObjects and square:getObjects() or nil
        if objects and parent then
            for i = 0, objects:size() - 1 do
                if objects:get(i) == parent then
                    location.objectIndex = i
                    break
                end
            end
        end
    end

    return location
end

function BurdJournals.Server.getJournalRespawnLocationSnapshot(journal, ownerPlayer, sourceTag, options)
    if not journal then
        return nil
    end

    options = type(options) == "table" and options or {}
    local sourceContainer = journal.getContainer and journal:getContainer() or nil
    if ownerPlayer and ownerPlayer.getInventory then
        local inventory = ownerPlayer:getInventory()
        -- Routine touches use direct container information; only recovery opts into a deep search.
        local inOwnerInventory = inventory and sourceContainer == inventory
        local parent = sourceContainer and sourceContainer.getParent and sourceContainer:getParent() or nil
        inOwnerInventory = inOwnerInventory or parent == ownerPlayer
        if not inOwnerInventory and options.allowInventorySearch == true and inventory then
            inOwnerInventory = BurdJournals.Server.containerContainsItemRecursive(inventory, journal)
        end
        if inOwnerInventory then
            local location = {
                kind = "playerInventory",
                sourceTag = tostring(sourceTag or "unknown"),
                ownerUsername = ownerPlayer.getUsername and ownerPlayer:getUsername() or nil,
                ownerSteamId = BurdJournals.getPlayerSteamId and BurdJournals.getPlayerSteamId(ownerPlayer) or nil,
            }
            local square = ownerPlayer.getCurrentSquare and ownerPlayer:getCurrentSquare() or nil
            local squareSnapshot = BurdJournals.Server.getRespawnJournalSquareSnapshot(square)
            if squareSnapshot then
                location.x = squareSnapshot.x
                location.y = squareSnapshot.y
                location.z = squareSnapshot.z
            end
            return location
        end
    end

    local containerLocation = BurdJournals.Server.getContainerRespawnLocationSnapshot(sourceContainer, sourceTag)
    if containerLocation then
        return containerLocation
    end

    local worldItem = journal.getWorldItem and journal:getWorldItem() or nil
    local square = worldItem and worldItem.getSquare and worldItem:getSquare() or nil
    local squareSnapshot = BurdJournals.Server.getRespawnJournalSquareSnapshot(square)
    if squareSnapshot then
        return {
            kind = "worldItem",
            sourceTag = tostring(sourceTag or "unknown"),
            x = squareSnapshot.x,
            y = squareSnapshot.y,
            z = squareSnapshot.z,
        }
    end

    if ownerPlayer and ownerPlayer.getCurrentSquare then
        local fallbackSquare = ownerPlayer:getCurrentSquare()
        local fallbackSnapshot = BurdJournals.Server.getRespawnJournalSquareSnapshot(fallbackSquare)
        if fallbackSnapshot then
            return {
                kind = "playerSquare",
                sourceTag = tostring(sourceTag or "unknown"),
                x = fallbackSnapshot.x,
                y = fallbackSnapshot.y,
                z = fallbackSnapshot.z,
            }
        end
    end

    return {
        kind = "unknown",
        sourceTag = tostring(sourceTag or "unknown"),
    }
end

function BurdJournals.Server.getJournalRespawnGeneration(data, registryEntry)
    local dataGeneration = tonumber(type(data) == "table" and data.respawnGeneration or nil) or 0
    local registryGeneration = tonumber(type(registryEntry) == "table" and registryEntry.respawnGeneration or nil) or 0
    if registryGeneration > dataGeneration then
        return registryGeneration
    end
    return dataGeneration
end

function BurdJournals.Server.isStaleRespawnJournalData(data)
    if type(data) ~= "table" or data.isPlayerCreated ~= true then
        return false
    end
    local uuid = trimUUID(BurdJournals.getJournalIdentityUUID and BurdJournals.getJournalIdentityUUID(data)
        or data.uuid or data.entryStoreUUID)
    if not uuid then
        return false
    end
    local registry = BurdJournals.Server.getPlayerJournalRegistry()
    local entry = registry and registry.byUUID and registry.byUUID[uuid] or nil
    if type(entry) ~= "table" then
        return false
    end
    local registryGeneration = tonumber(entry.respawnGeneration) or 0
    local itemGeneration = tonumber(data.respawnGeneration) or 0
    return registryGeneration > itemGeneration
end

function BurdJournals.Server.rejectStaleRespawnJournalInstance(player, journal, sourceTag)
    if not journal or not journal.getModData then
        return false
    end
    local modData = journal:getModData()
    local data = modData and modData.BurdJournals or nil
    if not BurdJournals.Server.isStaleRespawnJournalData(data) then
        return false
    end

    local message = (getText and getText("UI_BurdJournals_RespawnJournalDuplicateStale"))
        or "This journal has already returned to its owner."
    if message == "UI_BurdJournals_RespawnJournalDuplicateStale" then
        message = "This journal has already returned to its owner."
    end
    BurdJournals.debugPrint("[BurdJournals] Blocked stale respawn journal duplicate during "
        .. tostring(sourceTag or "unknown") .. ": uuid=" .. tostring(data and data.uuid))
    BurdJournals.Server.sendToClient(player, "error", { message = message })
    return true
end

local function isRegistryEntryBucketKey(key)
    return key == "skills" or key == "traits" or key == "stats" or key == "recipes"
end

local function countRegistryBucketEntries(bucket)
    if type(bucket) ~= "table" then
        return 0
    end
    local count = 0
    for _ in pairs(bucket) do
        count = count + 1
    end
    return count
end

function BurdJournals.Server.buildPlayerJournalRegistrySnapshot(data)
    if type(data) ~= "table" then
        return nil
    end

    if not (isStrictMPServer() and data.entryStoreEnabled == true) then
        return BurdJournals.Server.deepCopy(data)
    end

    -- Strict MP entries remain in the authoritative entry store until recovery needs them.
    local snapshot = BurdJournals.Server.deepCopy(data)

    for _, bucketName in ipairs(BurdJournals.ENTRY_STORE_BUCKETS or {"skills", "traits", "stats", "recipes"}) do
        snapshot[bucketName] = {}
    end

    if type(snapshot.entryStoreEntryCounts) ~= "table" then
        snapshot.entryStoreEntryCounts = {
            skills = countRegistryBucketEntries(data.skills),
            traits = countRegistryBucketEntries(data.traits),
            stats = countRegistryBucketEntries(data.stats),
            recipes = countRegistryBucketEntries(data.recipes),
        }
    end

    return snapshot
end

BurdJournals.Server.PLAYER_JOURNAL_REGISTRY_DEFAULT_MAX_ENTRIES = 128
BurdJournals.Server.PLAYER_JOURNAL_REGISTRY_DEFAULT_MAX_AGE_MS = 30 * 24 * 60 * 60 * 1000
BurdJournals.Server.JOURNAL_UUID_INDEX_DEFAULT_MAX_ENTRIES = 1024
BurdJournals.Server.JOURNAL_UUID_INDEX_DEFAULT_MAX_AGE_MS = 90 * 24 * 60 * 60 * 1000

function BurdJournals.Server.prunePlayerJournalRegistry(registry, nowTs, currentUUID)
    if type(registry) ~= "table" or type(registry.byUUID) ~= "table" then return false end
    nowTs = tonumber(nowTs) or BurdJournals.Server.getRespawnJournalTimestamp()
    local maxEntries = tonumber(BurdJournals.Server.PLAYER_JOURNAL_REGISTRY_MAX_ENTRIES) or BurdJournals.Server.PLAYER_JOURNAL_REGISTRY_DEFAULT_MAX_ENTRIES
    local maxAge = tonumber(BurdJournals.Server.PLAYER_JOURNAL_REGISTRY_MAX_AGE_MS) or BurdJournals.Server.PLAYER_JOURNAL_REGISTRY_DEFAULT_MAX_AGE_MS
    if nowTs < 100000000000 then maxAge = math.floor(maxAge / 1000) end
    local latestByOwner, entries = {}, {}
    for uuid, entry in pairs(registry.byUUID) do
        if type(entry) == "table" then
            local owner = BurdJournals.Server.normalizeRespawnIdentityValue(entry.ownerSteamId)
            local touched = tonumber(entry.updatedAt or entry.lastTouchedAt) or 0
            entries[#entries + 1] = { uuid = uuid, owner = owner, touched = touched }
            if owner and (not latestByOwner[owner] or touched > latestByOwner[owner].touched) then latestByOwner[owner] = { uuid = uuid, touched = touched } end
        end
    end
    table.sort(entries, function(a, b) return a.touched > b.touched end)
    local keep, kept = {}, 0
    for _, candidate in ipairs(entries) do
        local pinned = candidate.uuid == currentUUID or (candidate.owner and latestByOwner[candidate.owner] and latestByOwner[candidate.owner].uuid == candidate.uuid)
        local expired = candidate.touched > 0 and (nowTs - candidate.touched) > maxAge
        if pinned or ((not expired) and kept < maxEntries) then keep[candidate.uuid] = true; kept = kept + 1 end
    end
    local changed = false
    for uuid in pairs(registry.byUUID) do if not keep[uuid] then registry.byUUID[uuid] = nil; changed = true end end
    registry.bySteamId = registry.bySteamId or {}
    for owner, latest in pairs(latestByOwner) do if keep[latest.uuid] then registry.bySteamId[owner] = { uuid = latest.uuid, updatedAt = latest.touched, sourceTag = "pruned" } end end
    for owner, pointer in pairs(registry.bySteamId) do if type(pointer) ~= "table" or not keep[trimUUID(pointer.uuid)] then registry.bySteamId[owner] = nil; changed = true end end
    return changed
end

function BurdJournals.Server.pruneJournalUUIDIndex(indexCache, nowTs, currentUUID)
    if type(indexCache) ~= "table" or type(indexCache.journals) ~= "table" then return false end
    nowTs = tonumber(nowTs) or BurdJournals.Server.getRespawnJournalTimestamp()
    local maxEntries = tonumber(BurdJournals.Server.JOURNAL_UUID_INDEX_MAX_ENTRIES) or BurdJournals.Server.JOURNAL_UUID_INDEX_DEFAULT_MAX_ENTRIES
    local maxAge = tonumber(BurdJournals.Server.JOURNAL_UUID_INDEX_MAX_AGE_MS) or BurdJournals.Server.JOURNAL_UUID_INDEX_DEFAULT_MAX_AGE_MS
    if nowTs < 100000000000 then maxAge = math.floor(maxAge / 1000) end
    local onlineOwners, onlinePlayers = {}, getOnlinePlayers and getOnlinePlayers() or nil
    if onlinePlayers and onlinePlayers.size then for i = 0, onlinePlayers:size() - 1 do local p = onlinePlayers:get(i); local id = p and BurdJournals.getPlayerSteamId and BurdJournals.getPlayerSteamId(p) or nil; id = BurdJournals.Server.normalizeRespawnIdentityValue(id); if id then onlineOwners[id] = true end end end
    local entries = {}
    for uuid, entry in pairs(indexCache.journals) do if type(entry) == "table" then entries[#entries + 1] = { uuid = uuid, touched = tonumber(entry.lastSeenTs) or 0, owner = BurdJournals.Server.normalizeRespawnIdentityValue(entry.ownerSteamId) } end end
    table.sort(entries, function(a, b) return a.touched > b.touched end)
    local keep, kept = {}, 0
    for _, candidate in ipairs(entries) do
        local protected = candidate.uuid == currentUUID or (candidate.owner and onlineOwners[candidate.owner])
        local expired = candidate.touched > 0 and (nowTs - candidate.touched) > maxAge
        if protected or ((not expired) and kept < maxEntries) then keep[candidate.uuid] = true; kept = kept + 1 end
    end
    local changed = false
    for uuid in pairs(indexCache.journals) do if not keep[uuid] then indexCache.journals[uuid] = nil; changed = true end end
    return changed
end

function BurdJournals.Server.getPlayerJournalRegistryEntryByUUID(uuid)
    local normalizedUUID = trimUUID(uuid)
    if not normalizedUUID then
        return nil
    end
    local registry = BurdJournals.Server.getPlayerJournalRegistry
        and BurdJournals.Server.getPlayerJournalRegistry()
        or nil
    local entry = type(registry) == "table"
        and type(registry.byUUID) == "table"
        and registry.byUUID[normalizedUUID]
        or nil
    if type(entry) == "table" and type(entry.journalData) == "table" then
        return entry
    end
    return nil
end

function BurdJournals.Server.touchPlayerJournalRegistry(journal, ownerPlayer, sourceTag, options)
    if not journal or not journal.getModData then
        return nil
    end
    options = type(options) == "table" and options or {}
    local modData = journal:getModData()
    local data = modData and modData.BurdJournals or nil
    if type(data) ~= "table" or data.isPlayerCreated ~= true then
        return nil
    end

    if BurdJournals.Server.preferRicherEntryStoreData then
        local preferredData, preferredRicherStore = BurdJournals.Server.preferRicherEntryStoreData(
            data,
            { journalUUID = data.uuid or data.entryStoreUUID },
            "playerJournalRegistry:" .. tostring(sourceTag or "unknown")
        )
        if preferredRicherStore then
            modData.BurdJournals = preferredData
            data = preferredData
        end
    end

    local itemType = BurdJournals.Server.normalizeRespawnJournalItemType(journal.getFullType and journal:getFullType() or data.fullType)
    if not itemType then
        return nil
    end

    local ownerSteamId = BurdJournals.Server.normalizeRespawnIdentityValue(data.ownerSteamId)
        or BurdJournals.Server.normalizeRespawnIdentityValue(ownerPlayer and BurdJournals.getPlayerSteamId and BurdJournals.getPlayerSteamId(ownerPlayer))
    if not ownerSteamId then
        return nil
    end

    local uuid = trimUUID(BurdJournals.getJournalIdentityUUID and BurdJournals.getJournalIdentityUUID(data)
        or data.uuid or data.entryStoreUUID)
    if not uuid then
        uuid = (BurdJournals.generateUUID and BurdJournals.generateUUID())
            or ("journal-" .. tostring(BurdJournals.Server.getRespawnJournalTimestamp()) .. "-" .. tostring(journal.getID and journal:getID() or "unknown"))
    end
    if data.uuid ~= uuid then
        data.uuid = uuid
        if journal.transmitModData
            and not BurdJournals.Server.shouldDeferBatchTransmit()
            and (not BurdJournals.shouldTransmitJournalItemModData
                or BurdJournals.shouldTransmitJournalItemModData(journal, "touchPlayerJournalRegistryUUID"))
        then
            safeTransmitServerJournalModData(journal, "touchPlayerJournalRegistryUUID")
        end
    end

    local nowTs = tonumber(options.timestamp) or BurdJournals.Server.getRespawnJournalTimestamp()
    local registry = BurdJournals.Server.getPlayerJournalRegistry()
    local previousEntry = registry.byUUID and registry.byUUID[uuid] or nil
    local generation = BurdJournals.Server.getJournalRespawnGeneration(data, previousEntry)

    data.ownerSteamId = data.ownerSteamId or ownerSteamId
    data.ownerUsername = data.ownerUsername or (ownerPlayer and ownerPlayer.getUsername and ownerPlayer:getUsername() or nil)
    data.ownerCharacterName = data.ownerCharacterName or (ownerPlayer and BurdJournals.Server.getRespawnPlayerFullName and BurdJournals.Server.getRespawnPlayerFullName(ownerPlayer) or nil)
    data.respawnGeneration = generation
    data.lastPlayerJournalUseTs = nowTs

    local snapshot = BurdJournals.Server.buildPlayerJournalRegistrySnapshot(data)
    if type(snapshot) ~= "table" then
        return nil
    end
    snapshot.respawnGeneration = generation
    snapshot.lastPlayerJournalUseTs = nowTs

    local location = BurdJournals.Server.getJournalRespawnLocationSnapshot(journal, ownerPlayer, sourceTag, { allowInventorySearch = options.allowInventorySearch == true })
    local entry = {
        uuid = uuid,
        ownerSteamId = ownerSteamId,
        ownerUsername = data.ownerUsername,
        ownerCharacterName = data.ownerCharacterName,
        itemFullType = itemType,
        itemName = journal.getName and journal:getName() or nil,
        itemId = journal.getID and journal:getID() or nil,
        journalData = snapshot,
        lastKnownLocation = location,
        updatedAt = nowTs,
        lastTouchedAt = nowTs,
        respawnGeneration = generation,
        sourceTag = tostring(sourceTag or "unknown"),
    }

    registry.byUUID[uuid] = entry
    local previousSteamEntry = registry.bySteamId[ownerSteamId]
    local previousUpdatedAt = tonumber(type(previousSteamEntry) == "table" and previousSteamEntry.updatedAt or nil) or 0
    if nowTs >= previousUpdatedAt then
        registry.bySteamId[ownerSteamId] = {
            uuid = uuid,
            updatedAt = nowTs,
            sourceTag = tostring(sourceTag or "unknown"),
        }
    end

    BurdJournals.Server.prunePlayerJournalRegistry(registry, nowTs, uuid)
    if BurdJournals.Server.persistPlayerJournalRegistry(registry) ~= true then
        bsjWriteLogLine("[BurdJournals] ERROR: Player journal registry persistence failed"
            .. " uuid=" .. tostring(uuid) .. " source=" .. tostring(sourceTag or "unknown"))
        return nil
    end

    return uuid, entry
end

function BurdJournals.Server.getLatestPlayerJournalRegistryEntryForPlayer(player)
    local steamKey = BurdJournals.Server.getRespawnJournalCacheKeyForPlayer(player)
    if not steamKey then
        return nil
    end
    local registry = BurdJournals.Server.getPlayerJournalRegistry()
    if not registry then
        return nil
    end

    local steamEntry = type(registry.bySteamId) == "table" and registry.bySteamId[steamKey] or nil
    local uuid = type(steamEntry) == "table" and trimUUID(steamEntry.uuid) or nil
    local directEntry = uuid and registry.byUUID and registry.byUUID[uuid] or nil
    if type(directEntry) == "table" and type(directEntry.journalData) == "table" then
        return directEntry
    end

    local bestEntry = nil
    local bestTs = -1
    if type(registry.byUUID) == "table" then
        for _, entry in pairs(registry.byUUID) do
            if type(entry) == "table"
                and BurdJournals.Server.normalizeRespawnIdentityValue(entry.ownerSteamId) == steamKey
                and type(entry.journalData) == "table"
            then
                local itemType = BurdJournals.Server.normalizeRespawnJournalItemType(entry.itemFullType)
                    or BurdJournals.Server.normalizeRespawnJournalItemType(entry.journalData.fullType)
                local ts = tonumber(entry.updatedAt or entry.lastTouchedAt or entry.journalData.lastPlayerJournalUseTs) or 0
                if itemType and ts > bestTs then
                    bestEntry = entry
                    bestTs = ts
                end
            end
        end
    end
    return bestEntry
end

function BurdJournals.Server.buildRespawnPendingEntryFromRegistry(player, registryEntry, reason)
    if not player or type(registryEntry) ~= "table" or type(registryEntry.journalData) ~= "table" then
        return nil
    end
    local steamKey = BurdJournals.Server.getRespawnJournalCacheKeyForPlayer(player)
    if not steamKey then
        return nil
    end

    local normalized = BurdJournals.normalizeJournalData and BurdJournals.normalizeJournalData(registryEntry.journalData) or registryEntry.journalData
    if type(normalized) ~= "table" then
        return nil
    end
    normalized = BurdJournals.Server.deepCopy(normalized)
    if normalized.entryStoreEnabled == true and BurdJournals.hydrateJournalDataFromEntryStore then
        BurdJournals.hydrateJournalDataFromEntryStore(normalized, "playerRegistryHydrate")
    end
    normalized.isPlayerCreated = true
    normalized.ownerSteamId = normalized.ownerSteamId or steamKey
    normalized.ownerUsername = normalized.ownerUsername or (player.getUsername and player:getUsername() or nil)
    normalized.ownerCharacterName = normalized.ownerCharacterName or BurdJournals.Server.getRespawnPlayerFullName(player)

    local journalUUID = trimUUID(registryEntry.uuid or normalized.uuid)
    local itemType = BurdJournals.Server.normalizeRespawnJournalItemType(registryEntry.itemFullType)
        or BurdJournals.Server.normalizeRespawnJournalItemType(normalized.fullType)
        or "BurdJournals.FilledSurvivalJournal"
    normalized.fullType = itemType

    return {
        ownerSteamId = steamKey,
        characterId = BurdJournals.Server.normalizeRespawnIdentityValue(BurdJournals.getPlayerCharacterId and BurdJournals.getPlayerCharacterId(player)),
        reservedAt = BurdJournals.Server.getRespawnJournalTimestamp(),
        sourceTag = tostring(reason or "registry"),
        itemFullType = itemType,
        journalUUID = journalUUID,
        journalData = normalized,
        lastKnownLocation = registryEntry.lastKnownLocation,
        respawnGeneration = tonumber(registryEntry.respawnGeneration) or tonumber(normalized.respawnGeneration) or 0,
        registryFallback = true,
    }
end

function trimUUID(value)
    if value == nil then return nil end
    local uuid = tostring(value)
    uuid = uuid:gsub("^%s+", "")
    uuid = uuid:gsub("%s+$", "")
    if uuid == "" then
        return nil
    end
    return uuid
end

function BurdJournals.Server.normalizeRespawnIdentityValue(value)
    if value == nil then
        return nil
    end
    local normalized = tostring(value)
    normalized = normalized:gsub("^%s+", "")
    normalized = normalized:gsub("%s+$", "")
    if normalized == "" then
        return nil
    end
    return normalized
end

function BurdJournals.Server.getRespawnJournalCacheKeyForPlayer(player)
    if not player or not BurdJournals.getPlayerSteamId then
        return nil
    end
    return BurdJournals.Server.normalizeRespawnIdentityValue(BurdJournals.getPlayerSteamId(player))
end

function BurdJournals.Server.getRespawnPlayerFullName(player)
    if not player or not player.getDescriptor then
        return nil
    end
    local descriptor = player:getDescriptor()
    if not descriptor then
        return nil
    end
    local forename = descriptor.getForename and descriptor:getForename() or nil
    local surname = descriptor.getSurname and descriptor:getSurname() or nil
    if not forename and not surname then
        return nil
    end
    return tostring(forename or "Unknown") .. " " .. tostring(surname or "")
end

function BurdJournals.Server.normalizeRespawnJournalItemType(fullType)
    if type(fullType) ~= "string" then
        return nil
    end
    if fullType == "BurdJournals.BlankSurvivalJournal" then
        return fullType
    end
    if fullType == "BurdJournals.FilledSurvivalJournal" then
        return fullType
    end
    return nil
end

function BurdJournals.Server.scoreRespawnJournalCandidate(player, item, data)
    if not player or not item or type(data) ~= "table" or data.isPlayerCreated ~= true then
        return nil, nil
    end
    if BurdJournals.Server.isStaleRespawnJournalData and BurdJournals.Server.isStaleRespawnJournalData(data) then
        return nil, nil
    end

    local itemType = BurdJournals.Server.normalizeRespawnJournalItemType(item.getFullType and item:getFullType() or nil)
    if not itemType then
        return nil, nil
    end

    local playerSteamId = BurdJournals.Server.normalizeRespawnIdentityValue(BurdJournals.getPlayerSteamId and BurdJournals.getPlayerSteamId(player))
    local playerUsername = BurdJournals.Server.normalizeRespawnIdentityValue(player.getUsername and player:getUsername() or nil)
    local playerFullName = BurdJournals.Server.normalizeRespawnIdentityValue(BurdJournals.Server.getRespawnPlayerFullName(player))
    local ownerSteamId = BurdJournals.Server.normalizeRespawnIdentityValue(data.ownerSteamId)
    local ownerUsername = BurdJournals.Server.normalizeRespawnIdentityValue(data.ownerUsername)
    local ownerCharacterName = BurdJournals.Server.normalizeRespawnIdentityValue(data.ownerCharacterName)

    local ownerMatched = false
    local score = 0

    if playerSteamId and ownerSteamId and playerSteamId == ownerSteamId then
        ownerMatched = true
        score = score + 100
    elseif playerUsername and ownerUsername and playerUsername == ownerUsername then
        ownerMatched = true
        score = score + 80
    elseif playerFullName and ownerCharacterName and playerFullName == ownerCharacterName then
        ownerMatched = true
        score = score + 60
    elseif itemType == "BurdJournals.BlankSurvivalJournal"
        and not ownerSteamId and not ownerUsername and not ownerCharacterName
    then
        ownerMatched = true
        score = score + 20
    elseif BurdJournals.isJournalOwner and BurdJournals.isJournalOwner(player, item) then
        ownerMatched = true
        score = score + 10
    end

    if not ownerMatched then
        return nil, nil
    end

    if itemType == "BurdJournals.FilledSurvivalJournal" then
        score = score + 20
    else
        score = score + 10
    end

    if trimUUID(data.uuid) then
        score = score + 5
    end

    return score, itemType
end

function BurdJournals.Server.collectRespawnJournalCandidatesFromContainer(player, container, state)
    if not player or not container or not state or type(state.items) ~= "table" then
        return
    end

    local items = container.getItems and container:getItems() or nil
    if not items then
        return
    end

    for i = 0, items:size() - 1 do
        local item = items:get(i)
        if item then
            local itemId = item.getID and item:getID() or nil
            local itemKey = itemId and tostring(itemId) or tostring(item)
            if not state.seen[itemKey] then
                state.seen[itemKey] = true
                local data = BurdJournals.getJournalData and BurdJournals.getJournalData(item) or nil
                local score, itemType = BurdJournals.Server.scoreRespawnJournalCandidate(player, item, data)
                if score then
                    table.insert(state.items, {
                        item = item,
                        data = data,
                        score = score,
                        itemType = itemType,
                    })
                end

                if item.getInventory then
                    local subInventory = item:getInventory()
                    if subInventory then
                        BurdJournals.Server.collectRespawnJournalCandidatesFromContainer(player, subInventory, state)
                    end
                end
            end
        end
    end
end

function BurdJournals.Server.findRespawnPlayerJournalCandidate(player)
    if not player or not player.getInventory then
        return nil, nil, nil
    end

    local inventory = player:getInventory()
    if not inventory then
        return nil, nil, nil
    end

    local state = {
        items = {},
        seen = {},
    }
    BurdJournals.Server.collectRespawnJournalCandidatesFromContainer(player, inventory, state)
    if #state.items == 0 then
        return nil, nil, nil
    end

    table.sort(state.items, function(a, b)
        local scoreA = tonumber(a.score) or 0
        local scoreB = tonumber(b.score) or 0
        if scoreA ~= scoreB then
            return scoreA > scoreB
        end
        local touchedA = tonumber(a.data and a.data.lastPlayerJournalUseTs) or tonumber(a.data and a.data.lastModified) or 0
        local touchedB = tonumber(b.data and b.data.lastPlayerJournalUseTs) or tonumber(b.data and b.data.lastModified) or 0
        if touchedA ~= touchedB then
            return touchedA > touchedB
        end
        local uuidA = trimUUID(a.data and a.data.uuid) or ""
        local uuidB = trimUUID(b.data and b.data.uuid) or ""
        return uuidA < uuidB
    end)

    local best = state.items[1]
    return best and best.item or nil, best and best.data or nil, best and best.itemType or nil
end

function BurdJournals.Server.findPlayerInventoryJournalByUUID(uuid)
    local targetUUID = trimUUID(uuid)
    if not targetUUID then
        return nil, nil
    end

    local onlinePlayers = getOnlinePlayers and getOnlinePlayers() or nil
    if onlinePlayers and onlinePlayers.size then
        for i = 0, onlinePlayers:size() - 1 do
            local targetPlayer = onlinePlayers:get(i)
            local inventory = targetPlayer and targetPlayer.getInventory and targetPlayer:getInventory() or nil
            local found = inventory and BurdJournals.findJournalByUUIDInContainer and BurdJournals.findJournalByUUIDInContainer(inventory, targetUUID) or nil
            if found then
                local data = BurdJournals.getJournalData and BurdJournals.getJournalData(found) or nil
                if not (BurdJournals.Server.isStaleRespawnJournalData and BurdJournals.Server.isStaleRespawnJournalData(data)) then
                    return found, targetPlayer
                end
            end
        end
    end

    return nil, nil
end

function BurdJournals.Server.findJournalByUUIDInSquare(square, uuid)
    local targetUUID = trimUUID(uuid)
    if not square or not targetUUID then
        return nil
    end

    local objects = square.getObjects and square:getObjects() or nil
    if not objects then
        return nil
    end

    for i = 0, objects:size() - 1 do
        local obj = objects:get(i)
        if obj and obj.getItem then
            local worldItem = obj:getItem()
            if worldItem then
                local fullType = getItemFullTypeEarly(worldItem)
                if fullType and fullType:find("^BurdJournals%.") then
                    local journalData = BurdJournals.getJournalData and BurdJournals.getJournalData(worldItem) or nil
                    local journalUUID = journalData and trimUUID(BurdJournals.getJournalIdentityUUID
                        and BurdJournals.getJournalIdentityUUID(journalData)
                        or journalData.uuid or journalData.entryStoreUUID) or nil
                    if journalUUID == targetUUID then
                        return worldItem
                    end
                end
            end
        end

        if obj and obj.getContainer then
            local container = obj:getContainer()
            local found = container and BurdJournals.findJournalByUUIDInContainer and BurdJournals.findJournalByUUIDInContainer(container, targetUUID) or nil
            if found then
                return found
            end
        end

        if obj and obj.getInventory then
            local inventory = obj:getInventory()
            local found = inventory and BurdJournals.findJournalByUUIDInContainer and BurdJournals.findJournalByUUIDInContainer(inventory, targetUUID) or nil
            if found then
                return found
            end
        end
    end

    return nil
end

function BurdJournals.Server.findRegisteredRespawnJournalInLoadedWorld(uuid, entry)
    local targetUUID = trimUUID(uuid)
    if not targetUUID or type(entry) ~= "table" then
        return nil
    end

    local location = type(entry.lastKnownLocation) == "table" and entry.lastKnownLocation or nil
    local x = tonumber(location and location.x)
    local y = tonumber(location and location.y)
    local z = tonumber(location and location.z)
    if x == nil or y == nil or z == nil then
        return nil
    end

    local cell = getCell and getCell() or nil
    local square = cell and cell.getGridSquare and cell:getGridSquare(x, y, z) or nil
    if not square then
        return nil
    end

    return BurdJournals.Server.findJournalByUUIDInSquare(square, targetUUID)
end

local FILLED_RESPAWN_RESTORE_RETIRED = true

function BurdJournals.Server.moveRespawnJournalItemToPlayerInventory(player, journal, sourceTag)
    if FILLED_RESPAWN_RESTORE_RETIRED then return nil, "deprecated" end
    if not player or not journal or not player.getInventory then
        return nil, "invalid"
    end

    local inventory = player:getInventory()
    if not inventory then
        return nil, "missingInventory"
    end
    if inventory.contains and inventory:contains(journal) then
        return journal, "alreadyInInventory"
    end

    local sourceContainer = journal.getContainer and journal:getContainer() or nil
    if not sourceContainer or not sourceContainer.Remove then
        return nil, "unmovable"
    end

    sourceContainer:Remove(journal)
    if sourceContainer.setDrawDirty then
        sourceContainer:setDrawDirty(true)
    end
    if sendRemoveItemFromContainer then
        sendRemoveItemFromContainer(sourceContainer, journal)
    end

    local added = nil
    if inventory.AddItem then
        local ok, result = BurdJournals.safePcall(function()
            return inventory:AddItem(journal)
        end)
        if ok then
            added = result or journal
        end
    end

    if not added then
        return nil, "addFailed"
    end

    if sendAddItemToContainer then
        sendAddItemToContainer(inventory, added)
    end
    if inventory.setDrawDirty then
        inventory:setDrawDirty(true)
    end
    if player.syncInventory then
        BurdJournals.safePcall(function()
            player:syncInventory()
        end)
    end

    BurdJournals.debugPrint("[BurdJournals] Moved respawn journal from loaded source into player inventory during "
        .. tostring(sourceTag or "unknown")
        .. ": uuid=" .. tostring((BurdJournals.getJournalData and BurdJournals.getJournalData(added) or {}).uuid))

    return added, "moved"
end

function BurdJournals.Server.restoreRespawnJournalFromLoadedLocation(player, uuid, entry)
    if FILLED_RESPAWN_RESTORE_RETIRED then return nil, "deprecated" end
    local targetUUID = trimUUID(uuid)
    if not player or not targetUUID then
        return nil, "invalid"
    end

    local heldJournal, holder = BurdJournals.Server.findPlayerInventoryJournalByUUID(targetUUID)
    if heldJournal then
        if holder and holder ~= player then
            return nil, "heldByOnlinePlayer"
        end
        return BurdJournals.Server.moveRespawnJournalItemToPlayerInventory(player, heldJournal, "respawnRestoreInventory")
    end

    local registeredJournal = BurdJournals.Server.findRegisteredRespawnJournalInLoadedWorld(targetUUID, entry)
    if registeredJournal then
        return BurdJournals.Server.moveRespawnJournalItemToPlayerInventory(player, registeredJournal, "respawnRestoreLoadedLocation")
    end

    return nil, "notLoaded"
end

function BurdJournals.Server.clearRespawnJournalHands(player, journal)
    if not player or not journal then
        return
    end

    BurdJournals.safePcall(function()
        if player.getPrimaryHandItem and player:getPrimaryHandItem() == journal then
            player:setPrimaryHandItem(nil)
        end
        if player.getSecondaryHandItem and player:getSecondaryHandItem() == journal then
            player:setSecondaryHandItem(nil)
        end
    end)
end

function BurdJournals.Server.reserveRespawnPlayerJournal(player, reason)
    if FILLED_RESPAWN_RESTORE_RETIRED then return nil end
    local steamKey = BurdJournals.Server.getRespawnJournalCacheKeyForPlayer(player)
    if not steamKey then
        return nil
    end

    local cache = BurdJournals.Server.getRespawnJournalCache()
    local pending = cache and cache.bySteamId or nil
    if type(pending) ~= "table" then
        return nil
    end

    local journal, data, itemType = BurdJournals.Server.findRespawnPlayerJournalCandidate(player)
    if not journal or type(data) ~= "table" or not itemType then
        local registryEntry = BurdJournals.Server.getLatestPlayerJournalRegistryEntryForPlayer(player)
        local registryPending = BurdJournals.Server.buildRespawnPendingEntryFromRegistry(player, registryEntry, reason or "deathRegistryFallback")
        if type(registryPending) == "table" then
            pending[steamKey] = registryPending
            if ModData.transmit then
                ModData.transmit("BurdJournals_RespawnJournalCache")
            end
            BurdJournals.debugPrint("[BurdJournals] Reserved respawn journal from registry fallback for steamId="
                .. tostring(steamKey) .. ", uuid=" .. tostring(registryPending.journalUUID))
            return registryPending
        end
        pending[steamKey] = nil
        if ModData.transmit then
            ModData.transmit("BurdJournals_RespawnJournalCache")
        end
        return nil
    end

    local normalized = BurdJournals.normalizeJournalData and BurdJournals.normalizeJournalData(data) or data
    if type(normalized) ~= "table" then
        return nil
    end

    normalized.isPlayerCreated = true
    normalized.ownerUsername = normalized.ownerUsername or (player.getUsername and player:getUsername() or nil)
    normalized.ownerSteamId = normalized.ownerSteamId or steamKey
    normalized.ownerCharacterName = normalized.ownerCharacterName or BurdJournals.Server.getRespawnPlayerFullName(player)

    local journalUUID = trimUUID(normalized.uuid)
    local _, registryEntry = BurdJournals.Server.touchPlayerJournalRegistry(journal, player, tostring(reason or "deathReserve"), {
        skipTransmit = true,
        allowInventorySearch = true,
    })
    pending[steamKey] = {
        ownerSteamId = steamKey,
        characterId = BurdJournals.Server.normalizeRespawnIdentityValue(BurdJournals.getPlayerCharacterId and BurdJournals.getPlayerCharacterId(player)),
        reservedAt = getTimestampMs and getTimestampMs() or os.time(),
        sourceTag = tostring(reason or "death"),
        itemFullType = itemType,
        journalUUID = journalUUID,
        journalData = normalized,
        lastKnownLocation = registryEntry and registryEntry.lastKnownLocation or BurdJournals.Server.getJournalRespawnLocationSnapshot(journal, player, "deathReserve", { allowInventorySearch = true }),
        respawnGeneration = tonumber(normalized.respawnGeneration) or tonumber(registryEntry and registryEntry.respawnGeneration) or 0,
    }

    BurdJournals.Server.clearRespawnJournalHands(player, journal)

    local sourceContainer = journal.getContainer and journal:getContainer() or nil
    if sourceContainer and sourceContainer.Remove then
        sourceContainer:Remove(journal)
        if sourceContainer.setDrawDirty then
            sourceContainer:setDrawDirty(true)
        end
        if sendRemoveItemFromContainer then
            sendRemoveItemFromContainer(sourceContainer, journal)
        end
    end

    local inventory = player.getInventory and player:getInventory() or nil
    if inventory and inventory:contains(journal) then
        inventory:Remove(journal)
        if inventory.setDrawDirty then
            inventory:setDrawDirty(true)
        end
        if sendRemoveItemFromContainer then
            sendRemoveItemFromContainer(inventory, journal)
        end
    end

    if journalUUID and BurdJournals.Server.purgeJournalUUIDTracking then
        BurdJournals.Server.purgeJournalUUIDTracking(journalUUID, {
            removeBackup = false,
            removeRuntime = false,
            removeEntryStore = false,
            removeEntryFileBackup = false,
            skipTransmit = true,
        })
    end

    if player.syncInventory then
        BurdJournals.safePcall(function()
            player:syncInventory()
        end)
    end

    if ModData.transmit then
        ModData.transmit("BurdJournals_RespawnJournalCache")
    end
    BurdJournals.Server.persistJournalUUIDIndex(BurdJournals.Server.getJournalUUIDIndex())

    return pending[steamKey]
end

function BurdJournals.Server.createBlankRespawnPlayerJournal(player)
    if not player or not player.getInventory then
        return nil
    end

    local inventory = player:getInventory()
    if not inventory or not inventory.AddItem then
        return nil
    end

    local blankJournal = inventory:AddItem("BurdJournals.BlankSurvivalJournal")
    if not blankJournal then
        return nil
    end

    local modData = blankJournal:getModData()
    modData.BurdJournals = modData.BurdJournals or {}
    local bj = modData.BurdJournals
    bj.isPlayerCreated = true
    bj.ownerUsername = player.getUsername and player:getUsername() or bj.ownerUsername
    bj.ownerSteamId = (BurdJournals.getPlayerSteamId and BurdJournals.getPlayerSteamId(player)) or bj.ownerSteamId
    bj.ownerCharacterName = BurdJournals.Server.getRespawnPlayerFullName(player) or bj.ownerCharacterName
    bj.uuid = trimUUID(bj.uuid) or ((BurdJournals.generateUUID and BurdJournals.generateUUID()) or ("journal-" .. tostring(blankJournal:getID())))
    bj.readCount = tonumber(bj.readCount) or 0
    bj.readSessionCount = tonumber(bj.readSessionCount) or 0
    bj.currentSessionReadCount = tonumber(bj.currentSessionReadCount) or 0
    bj.skillReadCounts = type(bj.skillReadCounts) == "table" and bj.skillReadCounts or {}
    bj.migrationSchemaVersion = tonumber(bj.migrationSchemaVersion) or (tonumber(BurdJournals.MIGRATION_SCHEMA_VERSION) or 0)

    BurdJournals.updateJournalName(blankJournal)
    BurdJournals.updateJournalIcon(blankJournal)
    if blankJournal.transmitModData then
        blankJournal:transmitModData()
    end
    if sendAddItemToContainer then
        sendAddItemToContainer(inventory, blankJournal)
    end
    if inventory.setDrawDirty then
        inventory:setDrawDirty(true)
    end
    if player.syncInventory then
        BurdJournals.safePcall(function()
            player:syncInventory()
        end)
    end
    if BurdJournals.Server.updateJournalUUIDIndex then
        BurdJournals.Server.updateJournalUUIDIndex(blankJournal, player, "respawnBlank")
    end
    if BurdJournals.Server.touchPlayerJournalRegistry then
        BurdJournals.Server.touchPlayerJournalRegistry(blankJournal, player, "respawnBlank")
    end

    return blankJournal
end

function BurdJournals.Server.restoreReservedPlayerJournal(player, entry)
    if FILLED_RESPAWN_RESTORE_RETIRED then return nil end
    if not player or type(entry) ~= "table" or not player.getInventory then
        return nil
    end

    local inventory = player:getInventory()
    if not inventory or not inventory.AddItem then
        return nil
    end

    local requestedUUID = trimUUID(entry.journalUUID or (type(entry.journalData) == "table" and entry.journalData.uuid))
    if requestedUUID and BurdJournals.Server.restoreRespawnJournalFromLoadedLocation then
        local movedJournal, moveReason = BurdJournals.Server.restoreRespawnJournalFromLoadedLocation(player, requestedUUID, entry)
        if movedJournal then
            local movedData = BurdJournals.getJournalData and BurdJournals.getJournalData(movedJournal) or nil
            if type(movedData) == "table" then
                movedData.isPlayerCreated = true
                movedData.ownerUsername = movedData.ownerUsername or (player.getUsername and player:getUsername() or nil)
                movedData.ownerSteamId = movedData.ownerSteamId or BurdJournals.Server.getRespawnJournalCacheKeyForPlayer(player)
                movedData.ownerCharacterName = movedData.ownerCharacterName or BurdJournals.Server.getRespawnPlayerFullName(player)
                movedData.wasRespawnRestored = true
                movedData.respawnRestoredAt = BurdJournals.Server.getRespawnJournalTimestamp()
                local registry = BurdJournals.Server.getPlayerJournalRegistry()
                local registryEntry = registry and registry.byUUID and registry.byUUID[requestedUUID] or nil
                movedData.respawnGeneration = BurdJournals.Server.getJournalRespawnGeneration(movedData, registryEntry)
            end
            BurdJournals.updateJournalName(movedJournal)
            BurdJournals.updateJournalIcon(movedJournal)
            if movedJournal.transmitModData then
                movedJournal:transmitModData()
            end
            if BurdJournals.Server.updateJournalUUIDIndex then
                BurdJournals.Server.updateJournalUUIDIndex(movedJournal, player, "respawnRestoreMove:" .. tostring(moveReason or "moved"))
            end
            if BurdJournals.Server.touchPlayerJournalRegistry then
                BurdJournals.Server.touchPlayerJournalRegistry(movedJournal, player, "respawnRestoreMove:" .. tostring(moveReason or "moved"))
            end
            return movedJournal
        elseif moveReason == "heldByOnlinePlayer" then
            BurdJournals.debugPrint("[BurdJournals] Respawn journal restore skipped because UUID is held by another online player: "
                .. tostring(requestedUUID))
            return nil
        end
    end

    local itemType = BurdJournals.Server.normalizeRespawnJournalItemType(entry.itemFullType) or "BurdJournals.FilledSurvivalJournal"
    local journal = inventory:AddItem(itemType)
    if not journal then
        return nil
    end

    local normalized = BurdJournals.normalizeJournalData and BurdJournals.normalizeJournalData(entry.journalData) or entry.journalData
    if type(normalized) ~= "table" then
        inventory:Remove(journal)
        return nil
    end

    normalized.isPlayerCreated = true
    normalized.ownerUsername = normalized.ownerUsername or (player.getUsername and player:getUsername() or nil)
    normalized.ownerSteamId = normalized.ownerSteamId or BurdJournals.Server.getRespawnJournalCacheKeyForPlayer(player)
    normalized.ownerCharacterName = normalized.ownerCharacterName or BurdJournals.Server.getRespawnPlayerFullName(player)
    do
        local registry = BurdJournals.Server.getPlayerJournalRegistry()
        local registryEntry = requestedUUID and registry and registry.byUUID and registry.byUUID[requestedUUID] or nil
        local generation = math.max(
            tonumber(entry.respawnGeneration) or 0,
            BurdJournals.Server.getJournalRespawnGeneration(normalized, registryEntry)
        ) + 1
        normalized.respawnGeneration = generation
        normalized.wasRespawnReconstructed = true
        normalized.wasRespawnRestored = true
        normalized.respawnRestoredAt = BurdJournals.Server.getRespawnJournalTimestamp()
    end

    local bj = BurdJournals.Server.applyNormalizedDebugJournalDataToItem
        and BurdJournals.Server.applyNormalizedDebugJournalDataToItem(journal, normalized, entry.journalUUID)
    if type(bj) ~= "table" then
        inventory:Remove(journal)
        return nil
    end

    BurdJournals.updateJournalName(journal)
    BurdJournals.updateJournalIcon(journal)
    if journal.transmitModData and not BurdJournals.Server.shouldDeferBatchTransmit() then
            journal:transmitModData()
    end
    if sendAddItemToContainer then
        sendAddItemToContainer(inventory, journal)
    end
    if inventory.setDrawDirty then
        inventory:setDrawDirty(true)
    end
    if player.syncInventory then
        BurdJournals.safePcall(function()
            player:syncInventory()
        end)
    end
    if BurdJournals.Server.updateJournalUUIDIndex then
        BurdJournals.Server.updateJournalUUIDIndex(journal, player, "respawnRestore")
    end
    if BurdJournals.Server.touchPlayerJournalRegistry then
        BurdJournals.Server.touchPlayerJournalRegistry(journal, player, "respawnRestore")
    end

    return journal
end

function BurdJournals.Server.purgeJournalUUIDTracking(uuid, options)
    local targetUUID = trimUUID(uuid)
    if not targetUUID then
        return 0, 0
    end
    options = type(options) == "table" and options or {}

    local removedIndexEntries = 0
    local indexCache = BurdJournals.Server.getJournalUUIDIndex()
    local indexTable = indexCache and indexCache.journals or {}
    for key, entry in pairs(indexTable) do
        local entryUUID = trimUUID((type(entry) == "table" and entry.uuid) or key)
        if entryUUID == targetUUID then
            indexTable[key] = nil
            removedIndexEntries = removedIndexEntries + 1
        end
    end

    local removedBackupEntries = 0
    if options.removeBackup ~= false then
        local backupCache = BurdJournals.Server.getDebugJournalCache()
        local backupTable = backupCache and backupCache.journals or {}
        for key, entry in pairs(backupTable) do
            local entryUUID = trimUUID((type(entry) == "table" and entry.uuid) or key)
            if entryUUID == targetUUID then
                backupTable[key] = nil
                removedBackupEntries = removedBackupEntries + 1
            end
        end
    end

    local removedRegistryEntries = 0
    local registry = BurdJournals.Server.getPlayerJournalRegistry and BurdJournals.Server.getPlayerJournalRegistry() or nil
    if type(registry) == "table" then
        registry.byUUID = type(registry.byUUID) == "table" and registry.byUUID or {}
        if registry.byUUID[targetUUID] ~= nil then
            registry.byUUID[targetUUID] = nil
            removedRegistryEntries = removedRegistryEntries + 1
        end
        registry.bySteamId = type(registry.bySteamId) == "table" and registry.bySteamId or {}
        for ownerKey, pointer in pairs(registry.bySteamId) do
            if type(pointer) == "table" and trimUUID(pointer.uuid) == targetUUID then
                registry.bySteamId[ownerKey] = nil
                removedRegistryEntries = removedRegistryEntries + 1
            end
        end
        if removedRegistryEntries > 0 and BurdJournals.Server.persistPlayerJournalRegistry then
            BurdJournals.Server.persistPlayerJournalRegistry(registry)
        end
    end

    local removedRuntimeEntries = 0
    if options.removeRuntime ~= false and BurdJournals.purgeJournalRuntimeUUID then
        removedRuntimeEntries = BurdJournals.purgeJournalRuntimeUUID(targetUUID, options.runtimeShardKey)
    end

    local removedEntryStoreEntries = 0
    if options.removeEntryStore ~= false and BurdJournals.purgeJournalEntryStoreUUID then
        removedEntryStoreEntries = BurdJournals.purgeJournalEntryStoreUUID(targetUUID, options.entryStoreShardKey)
    end
    if type(BurdJournals.Server._pendingEntryFileBackups) == "table" then
        BurdJournals.Server._pendingEntryFileBackups[targetUUID] = nil
    end
    local invalidatedFileBackup = false
    if options.removeEntryFileBackup ~= false and BurdJournals.Server.invalidateJournalEntryFileBackup then
        invalidatedFileBackup = BurdJournals.Server.invalidateJournalEntryFileBackup(targetUUID, "uuidPurge") == true
    end

    if removedIndexEntries > 0 then BurdJournals.Server.persistJournalUUIDIndex(indexCache) end
    return removedIndexEntries, removedBackupEntries, {
        registry = removedRegistryEntries,
        runtime = removedRuntimeEntries,
        entryStore = removedEntryStoreEntries,
        fileBackup = invalidatedFileBackup,
    }
end

function getDebugXPModeLabel(mode)
    if mode == true then
        return "baseline"
    end
    if mode == false then
        return "absolute"
    end
    return "auto"
end

function normalizeDebugJournalXPMode(data, targetPlayer)
    if type(data) ~= "table" then
        return nil, nil, false, false
    end

    local modeBefore = data.recordedWithBaseline
    local modeAfter = BurdJournals.getJournalSkillRecordingMode
        and BurdJournals.getJournalSkillRecordingMode(data, targetPlayer)
        or (modeBefore == true)
    local autoRepaired = false

    if modeAfter and data.recordedWithBaseline == true and type(data.skills) == "table" and targetPlayer and targetPlayer.getXp then
        local sampledSkills = 0
        local suspiciousAbsoluteSkills = 0
        for skillName, storedData in pairs(data.skills) do
            local storedXP = tonumber(type(storedData) == "table" and storedData.xp or storedData)
            if storedXP and storedXP > 0 then
                local perk = BurdJournals.getPerkByName and BurdJournals.getPerkByName(skillName)
                if perk then
                    sampledSkills = sampledSkills + 1
                    local actualXP = BurdJournals.getPlayerSkillTotalXP and BurdJournals.getPlayerSkillTotalXP(targetPlayer, perk, skillName) or targetPlayer:getXp():getXP(perk)
                    local baselineXP = math.max(0, tonumber(BurdJournals.Server.getSkillBaselineForPlayer(targetPlayer, skillName)) or 0)
                    local storedLevel = tonumber(type(storedData) == "table" and storedData.level or 0) or 0
                    local looksLegacyAbsolute = BurdJournals.isLikelyLegacyAbsoluteSkillEntry
                        and BurdJournals.isLikelyLegacyAbsoluteSkillEntry(data, targetPlayer, skillName, storedXP, storedLevel, actualXP, baselineXP)
                        or false
                    if looksLegacyAbsolute then
                        suspiciousAbsoluteSkills = suspiciousAbsoluteSkills + 1
                    end
                end
            end
        end
        if sampledSkills > 0 and suspiciousAbsoluteSkills >= math.max(1, math.floor(sampledSkills * 0.5)) then
            modeAfter = false
            autoRepaired = true
        end
    end

    if data.recordedWithBaseline ~= modeAfter then
        data.recordedWithBaseline = modeAfter
        return modeBefore, modeAfter, true, autoRepaired
    end

    return modeBefore, modeAfter, false, autoRepaired
end

function normalizeIdentityValue(value)
    return trimUUID(value)
end

function safeLower(value)
    if value == nil then
        return nil
    end
    return string.lower(tostring(value))
end

function BurdJournals.Server.getPlayerJournalIdentity(player)
    if not player then
        return nil, nil
    end

    local username = player.getUsername and normalizeIdentityValue(player:getUsername()) or nil
    local steamId = nil
    if BurdJournals.getPlayerSteamId then
        steamId = normalizeIdentityValue(BurdJournals.getPlayerSteamId(player))
    end

    return username, steamId
end

function BurdJournals.Server.isServerJournalScopeAdmin(player)
    if not player then
        return false
    end
    if BurdJournals.Server and BurdJournals.Server.isDebugAdmin then
        return BurdJournals.Server.isDebugAdmin(player)
    end
    return false
end

function BurdJournals.Server.canPlayerAccessJournalSnapshot(player, ownerUsername, ownerSteamId, cachedPlayerUsername, cachedPlayerSteamId)
    if BurdJournals.Server.isServerJournalScopeAdmin(player) then
        return true
    end

    local playerUsername = cachedPlayerUsername
    local playerSteamId = cachedPlayerSteamId
    if not playerUsername and not playerSteamId then
        playerUsername, playerSteamId = BurdJournals.Server.getPlayerJournalIdentity(player)
    end

    local normalizedOwnerSteamId = normalizeIdentityValue(ownerSteamId)
    if normalizedOwnerSteamId and playerSteamId and normalizedOwnerSteamId == playerSteamId then
        return true
    end

    local normalizedOwnerUsername = normalizeIdentityValue(ownerUsername)
    if normalizedOwnerUsername and playerUsername and safeLower(normalizedOwnerUsername) == safeLower(playerUsername) then
        return true
    end

    return false
end

function BurdJournals.Server.sendJournalScopeDenied(player, actionLabel)
    BurdJournals.Server.sendToClient(player, "debugError", {
        message = tostring(actionLabel or "Journal action") .. " denied: admin scope required or journal ownership mismatch."
    })
end

function BurdJournals.Server.persistDebugJournalSnapshot(player, journalKey, sourceData, journalRef, options)
    options = options or {}
    if type(sourceData) ~= "table" then
        return nil, nil
    end

    local normalized = BurdJournals.normalizeJournalData and BurdJournals.normalizeJournalData(sourceData) or sourceData
    if type(normalized) ~= "table" then
        return nil, nil
    end

    local nowTs = getTimestampMs and getTimestampMs() or os.time()
    local backupUUID = trimUUID(normalized.uuid or journalKey)
    local backupKey = trimUUID(journalKey) or backupUUID
    if not backupKey then
        return nil, nil
    end

    local existingKey, existingBackup = BurdJournals.Server.findDebugBackupByUUID(backupUUID or backupKey)
    local existingRevision = tonumber(existingBackup and (existingBackup.revision or existingBackup.debugRevision)) or 0
    local incomingRevision = tonumber(normalized.debugRevision) or tonumber(normalized.revision) or 0
    local nextRevision = math.max(existingRevision, incomingRevision) + 1
    normalized.debugRevision = nextRevision

    local cache = BurdJournals.Server.getDebugJournalCache()
    local itemType = normalized.itemType or (journalRef and journalRef.getFullType and journalRef:getFullType() or nil)
    local isWornType = type(itemType) == "string" and string.find(itemType, "_Worn", 1, true) ~= nil
    local isBloodyType = type(itemType) == "string" and string.find(itemType, "_Bloody", 1, true) ~= nil
    local isCursedType = itemType == (BurdJournals.CURSED_ITEM_TYPE or "BurdJournals.CursedJournal")
    local isYuletideType = itemType == (BurdJournals.YULETIDE_ITEM_TYPE or "BurdJournals.YuletideJournal")
    local explicitPersonalOrigin = tostring(normalized.originMode or normalized.sourceType or "") == "personal"
    local forceFoundClaimMode = (not explicitPersonalOrigin) and (
        isWornType
        or isBloodyType
        or isCursedType
        or isYuletideType
        or normalized.isWorn == true
        or normalized.isBloody == true
        or normalized.isCursedJournal == true
        or normalized.isCursedReward == true
        or normalized.isYuletideJournal == true
    )
    local useDebugProfile = normalized.isDebugSpawned == true
    local snapshot = {
        skills = {},
        traits = {},
        recipes = {},
        stats = {},
        claims = {},
        claimedSkills = {},
        claimedTraits = {},
        claimedRecipes = {},
        claimedStats = {},
        claimedForgetSlot = {},
        skillReadCounts = {},
        forgetSlot = normalized.forgetSlot == true,
        forgetSlotCount = normalized.forgetSlot == true and tonumber(normalized.forgetSlotCount) or nil,
        author = normalized.author,
        profession = normalized.profession,
        professionName = normalized.professionName,
        flavorKey = normalized.flavorKey,
        flavorText = normalized.flavorText,
        loreNoteText = normalized.loreNoteText,
        isCursedJournal = normalized.isCursedJournal == true,
        cursedState = normalized.cursedState,
        isCursedReward = normalized.isCursedReward == true,
        cursedEffectType = normalized.cursedEffectType,
        cursedUnleashedByCharacterId = normalized.cursedUnleashedByCharacterId,
        cursedUnleashedByUsername = normalized.cursedUnleashedByUsername,
        cursedUnleashedByName = normalized.cursedUnleashedByName,
        cursedUnleashedAtHours = tonumber(normalized.cursedUnleashedAtHours) or nil,
        cursedSealSoundEvent = normalized.cursedSealSoundEvent,
        cursedForcedEffectType = normalized.cursedForcedEffectType,
        cursedForcedTraitId = normalized.cursedForcedTraitId,
        cursedForcedSkillName = normalized.cursedForcedSkillName,
        cursedPendingRewards = nil,
        isYuletideJournal = normalized.isYuletideJournal == true or isYuletideType,
        yuletideState = normalized.yuletideState,
        yuletideImmediateGifts = type(normalized.yuletideImmediateGifts) == "table" and normalized.yuletideImmediateGifts or {},
        yuletideGiftGranted = normalized.yuletideGiftGranted == true,
        yuletideGiftTier = normalized.yuletideGiftTier,
        yuletideGiftRoll = tonumber(normalized.yuletideGiftRoll) or nil,
        yuletideManualRewards = normalized.yuletideManualRewards == true,
        yuletideWrappedVariant = BurdJournals.normalizeYuletideWrappedVariant
            and BurdJournals.normalizeYuletideWrappedVariant(normalized.yuletideWrappedVariant)
            or tostring(normalized.yuletideWrappedVariant or "1"),
        yuletideOpenedByName = normalized.yuletideOpenedByName,
        yuletideDeliveryToken = normalized.yuletideDeliveryToken,
        yuletideDeliveredBy = normalized.yuletideDeliveredBy,
        yuletideDeliveryLabel = normalized.yuletideDeliveryLabel,
        yuletidePendingDelivery = normalized.yuletidePendingDelivery == true,
        yuletideBeacon = type(normalized.yuletideBeacon) == "table" and normalized.yuletideBeacon or nil,
        drLegacyMode3Migrated = normalized.drLegacyMode3Migrated == true,
        migrationSchemaVersion = tonumber(normalized.migrationSchemaVersion) or 0,
        isDebugSpawned = useDebugProfile,
        isDebugEdited = useDebugProfile and (normalized.isDebugEdited == true) or nil,
        isPlayerCreated = forceFoundClaimMode and false or (normalized.isPlayerCreated == true),
        isWorn = normalized.isWorn == true or isWornType,
        isBloody = normalized.isBloody == true or isBloodyType,
        sanitizedVersion = normalized.sanitizedVersion,
        uuid = backupUUID or normalized.uuid,
        itemType = itemType,
        itemID = normalized.itemID or normalized.itemId or (journalRef and journalRef.getID and journalRef:getID() or nil),
        itemName = normalized.itemName or (journalRef and journalRef.getName and journalRef:getName() or nil),
        ownerUsername = normalized.ownerUsername,
        ownerSteamId = normalized.ownerSteamId,
        ownerCharacterName = normalized.ownerCharacterName,
        sourceType = normalized.sourceType,
        originMode = normalized.originMode,
        wasFromWorn = normalized.wasFromWorn == true,
        wasFromBloody = normalized.wasFromBloody == true,
        wasRestored = normalized.wasRestored == true,
        readCount = tonumber(normalized.readCount) or 0,
        readSessionCount = tonumber(normalized.readSessionCount) or 0,
        currentSessionId = normalized.currentSessionId,
        currentSessionReadCount = tonumber(normalized.currentSessionReadCount) or 0,
        timestamp = nowTs,
        savedBy = options.savedBy or (player and player.getUsername and player:getUsername() or nil),
        revision = nextRevision,
        debugRevision = nextRevision,
        pendingApply = options.pendingApply == true,
        pendingReason = options.pendingReason,
        pendingRequestedBy = options.pendingApply == true and (options.pendingRequestedBy or (player and player.getUsername and player:getUsername() or nil)) or nil,
        pendingRequestedTs = options.pendingApply == true and nowTs or nil,
        lastAppliedTs = options.lastAppliedTs,
        sourceTag = options.sourceTag or "debugBackup",
    }

    if normalized.skills then
        for skillName, skillData in pairs(normalized.skills) do
            if skillName and skillData then
                snapshot.skills[skillName] = {
                    xp = tonumber(type(skillData) == "table" and skillData.xp or skillData) or 0,
                    level = tonumber(type(skillData) == "table" and skillData.level) or 0
                }
            end
        end
    end

    if normalized.traits then
        for traitId, value in pairs(normalized.traits) do
            if traitId then
                snapshot.traits[traitId] = value
            end
        end
    end

    if normalized.recipes then
        for recipeName, value in pairs(normalized.recipes) do
            if recipeName then
                snapshot.recipes[recipeName] = value
            end
        end
    end

    if normalized.stats then
        for statId, statData in pairs(normalized.stats) do
            if statId then
                snapshot.stats[statId] = statData
            end
        end
    end

    if normalized.claims then
        for characterId, claimData in pairs(normalized.claims) do
            if characterId then
                snapshot.claims[characterId] = claimData
            end
        end
    end

    if normalized.claimedSkills then
        for skillName, value in pairs(normalized.claimedSkills) do
            if skillName then
                snapshot.claimedSkills[skillName] = value
            end
        end
    end

    if normalized.claimedTraits then
        for traitId, value in pairs(normalized.claimedTraits) do
            if traitId then
                snapshot.claimedTraits[traitId] = value
            end
        end
    end

    if normalized.claimedRecipes then
        for recipeName, value in pairs(normalized.claimedRecipes) do
            if recipeName then
                snapshot.claimedRecipes[recipeName] = value
            end
        end
    end

    if normalized.claimedStats then
        for statId, value in pairs(normalized.claimedStats) do
            if statId then
                snapshot.claimedStats[statId] = value
            end
        end
    end

    if normalized.claimedForgetSlot then
        for characterId, value in pairs(normalized.claimedForgetSlot) do
            if characterId then
                snapshot.claimedForgetSlot[characterId] = value
            end
        end
    end

    if type(normalized.cursedPendingRewards) == "table" then
        snapshot.cursedPendingRewards = BurdJournals.normalizeJournalData
            and BurdJournals.normalizeJournalData(normalized.cursedPendingRewards)
            or normalized.cursedPendingRewards
    end

    local normalizedSkillReadCounts = normalized.skillReadCounts
    if type(normalizedSkillReadCounts) ~= "table" and BurdJournals.normalizeTable then
        normalizedSkillReadCounts = BurdJournals.normalizeTable(normalizedSkillReadCounts)
    end
    if type(normalizedSkillReadCounts) == "table" then
        for skillName, count in pairs(normalizedSkillReadCounts) do
            if skillName then
                snapshot.skillReadCounts[skillName] = tonumber(count) or 0
            end
        end
    end

    cache.journals[backupKey] = snapshot
    if backupUUID then
        cache.journals[backupUUID] = snapshot
    end
    if existingKey and existingKey ~= backupKey and existingKey ~= backupUUID then
        cache.journals[existingKey] = nil
    end
    BurdJournals.Server.pruneDebugJournalCache(cache)

    if backupUUID then
        local uuidIndex = BurdJournals.Server.getJournalUUIDIndex()
        local existingIndex = type(uuidIndex.journals[backupUUID]) == "table" and uuidIndex.journals[backupUUID] or nil
        uuidIndex.journals[backupUUID] = {
            uuid = backupUUID,
            itemName = snapshot.itemName or (existingIndex and existingIndex.itemName) or nil,
            itemType = snapshot.itemType or (existingIndex and existingIndex.itemType) or nil,
            itemId = snapshot.itemID or (existingIndex and existingIndex.itemId) or nil,
            ownerUsername = snapshot.ownerUsername or (existingIndex and existingIndex.ownerUsername) or nil,
            ownerSteamId = snapshot.ownerSteamId or (existingIndex and existingIndex.ownerSteamId) or nil,
            ownerCharacterName = snapshot.ownerCharacterName or (existingIndex and existingIndex.ownerCharacterName) or nil,
            isDebugSpawned = snapshot.isDebugSpawned == true,
            isPlayerCreated = snapshot.isPlayerCreated == true,
            wasFromWorn = snapshot.wasFromWorn == true,
            wasFromBloody = snapshot.wasFromBloody == true,
            wasRestored = snapshot.wasRestored == true,
            skillCount = BurdJournals.countTable and BurdJournals.countTable(snapshot.skills) or 0,
            traitCount = BurdJournals.countTable and BurdJournals.countTable(snapshot.traits) or 0,
            recipeCount = BurdJournals.countTable and BurdJournals.countTable(snapshot.recipes) or 0,
            statCount = BurdJournals.countTable and BurdJournals.countTable(snapshot.stats) or 0,
            sourceTag = options.sourceTag or (snapshot.pendingApply and "deferredDebugEdit" or "debugBackup"),
            lastSeenTs = nowTs,
            pendingApply = snapshot.pendingApply == true,
            revision = nextRevision,
        }
    end

    return backupUUID or backupKey, snapshot
end

function BurdJournals.Server.findLiveJournalByUUID(uuid)
    local targetUUID = trimUUID(uuid)
    if not targetUUID then
        return nil, nil
    end

    local held, holder = BurdJournals.Server.findPlayerInventoryJournalByUUID(targetUUID)
    if held then return held, holder end
    local index = BurdJournals.Server.getJournalUUIDIndex()
    local entry = index and index.journals and index.journals[targetUUID] or nil
    if type(entry) == "table" then
        local loaded = BurdJournals.Server.findRegisteredRespawnJournalInLoadedWorld(targetUUID, entry)
        if loaded then
            local owner = entry.ownerUsername and BurdJournals.Server.findPlayerByUsername
                and BurdJournals.Server.findPlayerByUsername(entry.ownerUsername) or nil
            return loaded, owner
        end
    end

    return nil, nil
end

function BurdJournals.Server.findDebugBackupByUUID(uuid)
    local targetUUID = trimUUID(uuid)
    if not targetUUID then
        return nil, nil
    end

    local cache = BurdJournals.Server.getDebugJournalCache()
    local journals = cache and cache.journals
    if type(journals) ~= "table" then
        return nil, nil
    end

    local direct = journals[targetUUID]
    if type(direct) == "table" then
        return targetUUID, direct
    end

    for journalKey, backup in pairs(journals) do
        if type(backup) == "table" and tostring(backup.uuid or "") == targetUUID then
            return journalKey, backup
        end
    end

    return nil, nil
end

function BurdJournals.Server.seedDebugSnapshotFromLiveJournal(journal, ownerPlayer, sourceTag)
    if not journal or not journal.getModData then
        return nil
    end

    local modData = journal:getModData()
    local data = modData and modData.BurdJournals or nil
    if type(data) ~= "table" then
        return nil
    end
    if isStrictMPServer() and data.entryStoreEnabled == true then
        return nil
    end

    local uuid = trimUUID(data.uuid)
    if not uuid then
        return nil
    end

    local _, existingBackup = BurdJournals.Server.findDebugBackupByUUID(uuid)
    if type(existingBackup) == "table" then
        return uuid
    end

    local storedUUID, snapshot = BurdJournals.Server.persistDebugJournalSnapshot(ownerPlayer, uuid, data, journal, {
        savedBy = "__autoOpen",
        pendingApply = false,
        sourceTag = sourceTag or "autoOpenSnapshot",
    })

    if storedUUID and type(snapshot) == "table" then
        local revision = tonumber(snapshot.revision or snapshot.debugRevision)
        if revision and revision > (tonumber(data.debugRevision) or 0) then
            data.debugRevision = revision
            if journal.transmitModData and not BurdJournals.Server.shouldDeferBatchTransmit() then
            journal:transmitModData()
            end
        end
        BurdJournals.Server.persistJournalUUIDIndex(BurdJournals.Server.getJournalUUIDIndex())
    end

    return storedUUID
end

function BurdJournals.Server.updateJournalUUIDIndex(journal, ownerPlayer, sourceTag)
    if not journal or not journal.getModData then
        return nil
    end

    local modData = journal:getModData()
    modData.BurdJournals = modData.BurdJournals or {}
    local data = modData.BurdJournals

    if not data.uuid and isStrictMPEmptyJournalShellData(data) then
        BurdJournals.debugPrint("[BurdJournals] Skipped UUID repair for empty strict-MP journal shell during "
            .. tostring(sourceTag or "unknown"))
        return nil
    end

    if not data.uuid then
        data.uuid = (BurdJournals.getJournalIdentityUUID and BurdJournals.getJournalIdentityUUID(data))
            or (BurdJournals.generateUUID and BurdJournals.generateUUID())
            or ("journal-" .. tostring(getTimestampMs and getTimestampMs() or os.time()) .. "-" .. tostring(journal:getID()))
        if journal.transmitModData and not BurdJournals.Server.shouldDeferBatchTransmit() then
            journal:transmitModData()
        end
    end

    local uuid = trimUUID(data.uuid)
    if not uuid then
        return nil
    end

    local nowTs = getTimestampMs and getTimestampMs() or os.time()
    local backupDirty = false
    local backupKey, backup = BurdJournals.Server.findDebugBackupByUUID(uuid)
    local pendingApplied = false
    if type(backup) == "table" and backup.pendingApply == true and BurdJournals.Server.applyNormalizedDebugJournalDataToItem then
        local liveRevision = tonumber(data.debugRevision) or 0
        local pendingRevision = tonumber(backup.revision or backup.debugRevision) or 0
        local shouldApplyPending = pendingRevision > liveRevision

        if shouldApplyPending then
            local normalizedBackup = BurdJournals.normalizeJournalData and BurdJournals.normalizeJournalData(backup) or backup
            local appliedData = BurdJournals.Server.applyNormalizedDebugJournalDataToItem(journal, normalizedBackup, uuid)
            if type(appliedData) == "table" then
                appliedData.debugRevision = math.max(tonumber(appliedData.debugRevision) or 0, pendingRevision)
                data = appliedData
                pendingApplied = true
                if journal.transmitModData and not BurdJournals.Server.shouldDeferBatchTransmit() then
            journal:transmitModData()
                end
            end
        end

        backup.pendingApply = false
        backup.pendingReason = nil
        backup.pendingRequestedBy = nil
        backup.pendingRequestedTs = nil
        backup.lastAppliedTs = nowTs
        backup.appliedBy = pendingApplied and "__autoSync" or (backup.appliedBy or "__autoSync")
        backup.itemID = journal.getID and journal:getID() or backup.itemID
        backup.itemType = journal.getFullType and journal:getFullType() or backup.itemType
        backup.itemName = journal.getName and journal:getName() or backup.itemName
        local effectiveRevision = math.max(tonumber(backup.revision or backup.debugRevision) or 0, tonumber(data.debugRevision) or 0)
        backup.revision = effectiveRevision
        backup.debugRevision = effectiveRevision
        backup.timestamp = nowTs
        backup.sourceTag = pendingApplied and "autoApplied" or (backup.sourceTag or "debugBackup")
        backupDirty = true
    end

    local indexCache = BurdJournals.Server.getJournalUUIDIndex()
    indexCache.journals[uuid] = {
        uuid = uuid,
        itemName = journal.getName and journal:getName() or nil,
        itemType = journal.getFullType and journal:getFullType() or nil,
        itemId = journal.getID and journal:getID() or nil,
        ownerUsername = (type(data.ownerUsername) == "string" and data.ownerUsername ~= "") and data.ownerUsername or (ownerPlayer and ownerPlayer:getUsername() or nil),
        ownerSteamId = data.ownerSteamId,
        ownerCharacterName = data.ownerCharacterName,
        isDebugSpawned = data.isDebugSpawned == true,
        isPlayerCreated = data.isPlayerCreated == true,
        wasFromWorn = data.wasFromWorn == true,
        wasFromBloody = data.wasFromBloody == true,
        wasRestored = BurdJournals.isRestoredJournalData and BurdJournals.isRestoredJournalData(data) or (data.wasRestored == true),
        skillCount = BurdJournals.countTable and BurdJournals.countTable(data.skills) or 0,
        traitCount = BurdJournals.countTable and BurdJournals.countTable(data.traits) or 0,
        recipeCount = BurdJournals.countTable and BurdJournals.countTable(data.recipes) or 0,
        statCount = BurdJournals.countTable and BurdJournals.countTable(data.stats) or 0,
        sourceTag = pendingApplied and ("autoApplied:" .. tostring(sourceTag or "unknown")) or (sourceTag or "unknown"),
        lastSeenTs = nowTs,
        pendingApply = false,
        revision = tonumber(data.debugRevision) or tonumber(backup and backup.revision) or 0,
    }

    if BurdJournals.Server.touchPlayerJournalRegistry
        and not shouldSkipPlayerJournalRegistryTouchOnInitialServerTouch(sourceTag)
    then
        BurdJournals.Server.touchPlayerJournalRegistry(journal, ownerPlayer, sourceTag or "uuidIndex", {
            skipTransmit = true,
            timestamp = nowTs,
        })
    end

    BurdJournals.Server.pruneJournalUUIDIndex(indexCache, nowTs, uuid)
    BurdJournals.Server.persistJournalUUIDIndex(indexCache)
    return uuid
end

function BurdJournals.Server.refreshJournalUUIDIndexFromOnlineInventories()
    local stats = {
        playersScanned = 0,
        journalsScanned = 0,
        journalsIndexed = 0,
    }

    local function scanPlayer(targetPlayer)
        if not targetPlayer or not targetPlayer.getInventory then
            return
        end

        local inventory = targetPlayer:getInventory()
        if not inventory then
            return
        end

        stats.playersScanned = stats.playersScanned + 1

        BurdJournals.Server.forEachInventoryItemRecursive(inventory, function(item)
            if not item or not item.getModData then
                return
            end
            local isFilledJournal = BurdJournals.isFilledJournal and BurdJournals.isFilledJournal(item)
            local isCursedJournal = BurdJournals.isCursedJournalItem and BurdJournals.isCursedJournalItem(item)
            if not isFilledJournal and not isCursedJournal then
                return
            end

            stats.journalsScanned = stats.journalsScanned + 1

            local modData = item:getModData()
            local journalData = modData and modData.BurdJournals or nil
            if type(journalData) ~= "table" then
                return
            end

            local indexedUUID = BurdJournals.Server.updateJournalUUIDIndex(item, targetPlayer, "inventoryScan")
            if indexedUUID then
                stats.journalsIndexed = stats.journalsIndexed + 1
            end
        end)
    end

    local onlinePlayers = getOnlinePlayers and getOnlinePlayers() or nil
    if onlinePlayers and onlinePlayers.size then
        for i = 0, onlinePlayers:size() - 1 do
            scanPlayer(onlinePlayers:get(i))
        end
    end

    return stats
end

-- Handle client request to save debug journal backup to server-side storage
function BurdJournals.Server.handleSaveDebugJournalBackup(player, args)
    if not player or not args then
        bsjWriteLogLine("[BurdJournals] ERROR: handleSaveDebugJournalBackup - missing player or args")
        return
    end
    if not BurdJournals.Server.isDebugAllowed(player) then
        BurdJournals.Server.sendToClient(player, "debugError", {message = "Debug commands not allowed"})
        return
    end

    local journalKey = args.journalKey
    if not journalKey then
        bsjWriteLogLine("[BurdJournals] ERROR: handleSaveDebugJournalBackup - no journalKey")
        return
    end

    local journalData = args.journalData
    if not journalData then
        bsjWriteLogLine("[BurdJournals] ERROR: handleSaveDebugJournalBackup - no journalData")
        return
    end
    local normalizedJournalData = BurdJournals.normalizeJournalData and BurdJournals.normalizeJournalData(journalData) or journalData
    local storedUUID = BurdJournals.Server.persistDebugJournalSnapshot(player, journalKey, normalizedJournalData, nil, {
        savedBy = player:getUsername(),
        pendingApply = false,
        sourceTag = "debugBackup",
    })
    if not storedUUID then
        BurdJournals.Server.sendToClient(player, "debugError", {message = "Failed to save debug journal backup"})
        return
    end

    -- Global ModData persists server-side; the response below is targeted to the admin.
    BurdJournals.Server.persistJournalUUIDIndex(BurdJournals.Server.getJournalUUIDIndex())

    BurdJournals.debugPrint("[BurdJournals] Server: Saved debug journal backup for key=" .. tostring(journalKey) .. " by " .. tostring(player:getUsername()))

    -- Notify client of success
    BurdJournals.Server.sendToClient(player, "debugJournalBackupSaved", {
        journalKey = journalKey,
        success = true
    })
end

-- Handle client request for debug journal backup data (for restoration)
function BurdJournals.Server.handleRequestDebugJournalBackup(player, args)
    if not player or not args then return end
    if not BurdJournals.Server.isDebugAllowed(player) then
        BurdJournals.Server.sendToClient(player, "debugError", {message = "Debug commands not allowed"})
        return
    end

    local journalKey = args.journalKey
    if not journalKey then
        bsjWriteLogLine("[BurdJournals] ERROR: handleRequestDebugJournalBackup - no journalKey")
        return
    end

    local cache = BurdJournals.Server.getDebugJournalCache()
    local backup = cache.journals and cache.journals[journalKey]

    if backup then
        BurdJournals.debugPrint("[BurdJournals] Server: Found debug journal backup for key=" .. tostring(journalKey))
        BurdJournals.Server.sendToClient(player, "debugJournalBackupResponse", {
            journalKey = journalKey,
            found = true,
            journalData = backup
        })
    else
        BurdJournals.debugPrint("[BurdJournals] Server: No debug journal backup found for key=" .. tostring(journalKey))
        BurdJournals.Server.sendToClient(player, "debugJournalBackupResponse", {
            journalKey = journalKey,
            found = false
        })
    end
end

function BurdJournals.Server.handleDebugLookupJournalByUUID(player, args)
    if not BurdJournals.Server.isDebugAllowed(player) then
        BurdJournals.Server.sendToClient(player, "debugError", {message = "Debug commands not allowed"})
        return
    end

    local uuid = trimUUID(args and args.uuid)
    if not uuid then
        BurdJournals.Server.sendToClient(player, "debugError", {message = "UUID is required"})
        return
    end

    local journal, ownerPlayer = BurdJournals.Server.findLiveJournalByUUID(uuid)
    if journal then
        local modData = journal:getModData()
        local data = modData and modData.BurdJournals or {}
        local ownerUsername = data.ownerUsername or (ownerPlayer and ownerPlayer:getUsername()) or nil
        local ownerSteamId = data.ownerSteamId or (ownerPlayer and BurdJournals.getPlayerSteamId and BurdJournals.getPlayerSteamId(ownerPlayer)) or nil
        if not BurdJournals.Server.canPlayerAccessJournalSnapshot(player, ownerUsername, ownerSteamId) then
            BurdJournals.Server.sendJournalScopeDenied(player, "Journal lookup")
            return
        end

        local appliedCachedEdits = false
        local backupKey, backup = BurdJournals.Server.findDebugBackupByUUID(uuid)
        if backup then
            local normalizedBackup = BurdJournals.normalizeJournalData(backup) or backup
            if type(normalizedBackup) == "table"
                and BurdJournals.Server.applyNormalizedDebugJournalDataToItem(journal, normalizedBackup, uuid)
            then
                if journal.transmitModData and not BurdJournals.Server.shouldDeferBatchTransmit() then
            journal:transmitModData()
                end
                appliedCachedEdits = true
            end
        end

        local liveSnapshot = BurdJournals.Server.copyJournalData and BurdJournals.Server.copyJournalData(journal) or nil
        if type(liveSnapshot) == "table" and BurdJournals.normalizeJournalData then
            liveSnapshot = BurdJournals.normalizeJournalData(liveSnapshot) or liveSnapshot
        end

        BurdJournals.Server.updateJournalUUIDIndex(journal, ownerPlayer, "lookup")
        local indexCache = BurdJournals.Server.getJournalUUIDIndex()
        local indexEntry = indexCache and indexCache.journals and indexCache.journals[uuid] or nil

        BurdJournals.Server.sendToClient(player, "debugJournalUUIDLookupResult", {
            uuid = uuid,
            found = true,
            live = true,
            journalId = journal:getID(),
            itemType = journal:getFullType(),
            itemName = journal.getName and journal:getName() or nil,
            ownerUsername = ownerUsername or "Unknown",
            ownerSteamId = ownerSteamId,
            ownerCharacterName = data.ownerCharacterName,
            isDebugSpawned = data.isDebugSpawned == true,
            isPlayerCreated = data.isPlayerCreated == true,
            isRestored = BurdJournals.isRestoredJournalData and BurdJournals.isRestoredJournalData(data) or (data.wasRestored == true),
            wasFromWorn = data.wasFromWorn == true,
            wasFromBloody = data.wasFromBloody == true,
            skillCount = BurdJournals.countTable and BurdJournals.countTable(data.skills) or 0,
            traitCount = BurdJournals.countTable and BurdJournals.countTable(data.traits) or 0,
            recipeCount = BurdJournals.countTable and BurdJournals.countTable(data.recipes) or 0,
            statCount = BurdJournals.countTable and BurdJournals.countTable(data.stats) or 0,
            hasIndex = indexEntry ~= nil,
            indexEntry = indexEntry,
            hasBackup = backup ~= nil,
            appliedCachedEdits = appliedCachedEdits,
            backupKey = backupKey,
            backupData = backup,
            backupSavedBy = backup and backup.savedBy or nil,
            snapshotData = liveSnapshot,
            message = appliedCachedEdits and "Found live journal by UUID (applied cached edits)." or "Found live journal by UUID."
        })
        return
    end

    local indexCache = BurdJournals.Server.getJournalUUIDIndex()
    local indexEntry = indexCache and indexCache.journals and indexCache.journals[uuid] or nil
    local backupKey, backup = BurdJournals.Server.findDebugBackupByUUID(uuid)
    if not BurdJournals.Server.isServerJournalScopeAdmin(player) and (indexEntry or backup) then
        local cachedOwnerUsername = (type(indexEntry) == "table" and indexEntry.ownerUsername) or (type(backup) == "table" and backup.ownerUsername) or nil
        local cachedOwnerSteamId = (type(indexEntry) == "table" and indexEntry.ownerSteamId) or (type(backup) == "table" and backup.ownerSteamId) or nil
        if not BurdJournals.Server.canPlayerAccessJournalSnapshot(player, cachedOwnerUsername, cachedOwnerSteamId) then
            BurdJournals.Server.sendJournalScopeDenied(player, "Journal lookup")
            return
        end
    end

    BurdJournals.Server.sendToClient(player, "debugJournalUUIDLookupResult", {
        uuid = uuid,
        found = false,
        live = false,
        hasIndex = indexEntry ~= nil,
        indexEntry = indexEntry,
        hasBackup = backup ~= nil,
        backupKey = backupKey,
        backupData = backup,
        backupSavedBy = backup and backup.savedBy or nil,
        message = (indexEntry or backup) and "No live journal found. Cached metadata available." or "UUID not found in live items or cache."
    })
end

function BurdJournals.Server.handleDebugRepairJournalByUUID(player, args)
    if not BurdJournals.Server.isDebugAllowed(player) then
        BurdJournals.Server.sendToClient(player, "debugError", {message = "Debug commands not allowed"})
        return
    end
    if not BurdJournals.Server.isServerJournalScopeAdmin(player) then
        BurdJournals.Server.sendJournalScopeDenied(player, "Journal repair")
        return
    end

    local uuid = trimUUID(args and args.uuid)
    if not uuid then
        BurdJournals.Server.sendToClient(player, "debugError", {message = "UUID is required"})
        return
    end

    local journal, ownerPlayer = BurdJournals.Server.findLiveJournalByUUID(uuid)
    if not journal then
        BurdJournals.Server.sendToClient(player, "debugJournalUUIDRepairResult", {
            uuid = uuid,
            found = false,
            message = "No live journal found for UUID. Move near the container or have owner online."
        })
        return
    end

    local targetPlayer = ownerPlayer or player
    local modData = journal:getModData()
    modData.BurdJournals = modData.BurdJournals or {}
    local data = modData.BurdJournals
    local changed = 0
    local normalizeXPMode = args and args.normalizeXPMode == true
    local xpModeBefore = data.recordedWithBaseline
    local xpModeAfter = data.recordedWithBaseline
    local xpModeChanged = false
    local xpModeAutoRepaired = false

    if data.uuid ~= uuid then
        data.uuid = uuid
        changed = changed + 1
    end

    if args and args.markRestored then
        if data.isPlayerCreated ~= true then
            data.isPlayerCreated = true
            changed = changed + 1
        end
        if data.wasRestored ~= true then
            data.wasRestored = true
            changed = changed + 1
        end
        if data.wasFromWorn ~= true and data.wasFromBloody ~= true then
            data.wasFromWorn = true
            changed = changed + 1
        end
        if type(data.restoredBy) ~= "string" or data.restoredBy == "" then
            data.restoredBy = player and player:getUsername() or "Admin"
            changed = changed + 1
        end
        if data.isWorn == true then
            data.isWorn = false
            changed = changed + 1
        end
        if data.isBloody == true then
            data.isBloody = false
            changed = changed + 1
        end
    end

    if normalizeXPMode then
        xpModeBefore, xpModeAfter, xpModeChanged, xpModeAutoRepaired = normalizeDebugJournalXPMode(data, targetPlayer)
        if xpModeChanged then
            changed = changed + 1
        end
    end

    if BurdJournals.migrateJournalIfNeeded then
        BurdJournals.migrateJournalIfNeeded(journal, targetPlayer)
    end
    local sanitizeResult = nil
    if BurdJournals.sanitizeJournalData then
        sanitizeResult = BurdJournals.sanitizeJournalData(journal, targetPlayer)
    end
    if BurdJournals.compactJournalData then
        BurdJournals.compactJournalData(journal)
    end

    if journal.transmitModData and not BurdJournals.Server.shouldDeferBatchTransmit() then
            journal:transmitModData()
    end
    BurdJournals.Server.updateJournalUUIDIndex(journal, ownerPlayer, "repair")

    local cleaned = sanitizeResult and sanitizeResult.cleaned == true
    local message = "UUID repair complete"
        .. " (changed=" .. tostring(changed)
        .. ", sanitized=" .. tostring(cleaned)
    if normalizeXPMode then
        if xpModeChanged then
            message = message .. ", xpMode=" .. getDebugXPModeLabel(xpModeBefore) .. "->" .. getDebugXPModeLabel(xpModeAfter)
        else
            message = message .. ", xpMode=" .. getDebugXPModeLabel(xpModeAfter) .. " (unchanged)"
        end
        if xpModeAutoRepaired then
            message = message .. ", autoModeRepair=true"
        end
    end
    message = message .. ")"

    BurdJournals.Server.sendToClient(player, "debugJournalUUIDRepairResult", {
        uuid = uuid,
        found = true,
        journalId = journal:getID(),
        ownerUsername = data.ownerUsername or (ownerPlayer and ownerPlayer:getUsername()) or "Unknown",
        changed = changed,
        sanitized = cleaned,
        markRestored = args and args.markRestored == true,
        normalizeXPMode = normalizeXPMode,
        xpModeBefore = xpModeBefore,
        xpModeAfter = xpModeAfter,
        xpModeChanged = xpModeChanged == true,
        xpModeAutoRepaired = xpModeAutoRepaired == true,
        message = message,
    })
end

function BurdJournals.Server.handleDebugListJournalUUIDIndex(player, args)
    if not BurdJournals.Server.isDebugAllowed(player) then
        BurdJournals.Server.sendToClient(player, "debugError", {message = "Debug commands not allowed"})
        return
    end

    local isAdminScope = BurdJournals.Server.isServerJournalScopeAdmin(player)
    local requesterUsername, requesterSteamId = nil, nil
    if not isAdminScope then
        requesterUsername, requesterSteamId = BurdJournals.Server.getPlayerJournalIdentity(player)
    end

    -- Read the event-maintained cache; never inventory-scan synchronously from a debug list refresh.
    local scanStats = { cached = true }

    local indexCache = BurdJournals.Server.getJournalUUIDIndex()
    local indexTable = indexCache and indexCache.journals or {}

    local maxEntries = tonumber(args and args.maxEntries) or 400
    if maxEntries < 25 then maxEntries = 25 end
    if maxEntries > 1000 then maxEntries = 1000 end

    local query = trimUUID(args and args.query)
    if query then
        query = string.lower(query)
    end

    local entries = {}
    for keyUuid, entry in pairs(indexTable) do
        if type(entry) == "table" then
            local uuid = trimUUID(entry.uuid or keyUuid)
            if uuid then
                local itemName = tostring(entry.itemName or "")
                local ownerCharacterName = tostring(entry.ownerCharacterName or "")
                local ownerUsername = tostring(entry.ownerUsername or "")
                local itemType = tostring(entry.itemType or "")

                local include = true
                if query then
                    local haystack = string.lower(itemName .. " " .. ownerCharacterName .. " " .. ownerUsername .. " " .. itemType .. " " .. uuid)
                    include = string.find(haystack, query, 1, true) ~= nil
                end
                if include and not isAdminScope then
                    include = BurdJournals.Server.canPlayerAccessJournalSnapshot(
                        player,
                        entry.ownerUsername,
                        entry.ownerSteamId,
                        requesterUsername,
                        requesterSteamId
                    )
                end

                if include then
                    table.insert(entries, {
                        uuid = uuid,
                        itemName = entry.itemName,
                        itemType = entry.itemType,
                        itemId = entry.itemId,
                        ownerUsername = entry.ownerUsername,
                        ownerCharacterName = entry.ownerCharacterName,
                        ownerSteamId = entry.ownerSteamId,
                        isDebugSpawned = entry.isDebugSpawned == true,
                        isPlayerCreated = entry.isPlayerCreated == true,
                        wasRestored = entry.wasRestored == true,
                        wasFromWorn = entry.wasFromWorn == true,
                        wasFromBloody = entry.wasFromBloody == true,
                        skillCount = tonumber(entry.skillCount) or 0,
                        traitCount = tonumber(entry.traitCount) or 0,
                        recipeCount = tonumber(entry.recipeCount) or 0,
                        statCount = tonumber(entry.statCount) or 0,
                        sourceTag = entry.sourceTag,
                        lastSeenTs = tonumber(entry.lastSeenTs) or 0,
                    })
                end
            end
        end
    end

    table.sort(entries, function(a, b)
        local ats = tonumber(a.lastSeenTs) or 0
        local bts = tonumber(b.lastSeenTs) or 0
        if ats ~= bts then
            return ats > bts
        end
        return tostring(a.uuid or "") < tostring(b.uuid or "")
    end)

    local total = #entries
    local truncated = false
    if total > maxEntries then
        for i = total, maxEntries + 1, -1 do
            entries[i] = nil
        end
        truncated = true
    end

    BurdJournals.Server.sendToClient(player, "debugJournalUUIDIndexList", {
        entries = entries,
        total = total,
        count = #entries,
        truncated = truncated,
        maxEntries = maxEntries,
        query = query,
        scope = isAdminScope and "admin" or "self",
        scanStats = scanStats,
    })
end

function BurdJournals.Server.handleDebugDeleteJournalByUUID(player, args)
    if not BurdJournals.Server.isDebugAllowed(player) then
        BurdJournals.Server.sendToClient(player, "debugError", {message = "Debug commands not allowed"})
        return
    end
    if not BurdJournals.Server.isServerJournalScopeAdmin(player) then
        BurdJournals.Server.sendJournalScopeDenied(player, "Journal delete")
        return
    end

    local uuid = trimUUID(args and args.uuid)
    if not uuid then
        BurdJournals.Server.sendToClient(player, "debugError", {message = "UUID is required"})
        return
    end

    local deletedLive = false
    local deletedLiveOwner = nil
    local liveJournal, ownerPlayer = BurdJournals.Server.findLiveJournalByUUID(uuid)
    if liveJournal then
        deletedLive = removeJournalCompletely(ownerPlayer or player, liveJournal)
        if deletedLive then
            deletedLiveOwner = ownerPlayer and ownerPlayer:getUsername() or nil
        end
    end

    local removedIndexEntries, removedBackupEntries = 0, 0
    if BurdJournals.Server.purgeJournalUUIDTracking then
        removedIndexEntries, removedBackupEntries = BurdJournals.Server.purgeJournalUUIDTracking(uuid, {
            removeBackup = true,
        })
    end

    local foundAny = deletedLive or removedIndexEntries > 0 or removedBackupEntries > 0
    local message = nil
    if foundAny then
        message = "UUID delete complete (live=" .. tostring(deletedLive)
            .. ", index=" .. tostring(removedIndexEntries)
            .. ", backup=" .. tostring(removedBackupEntries) .. ")"
    else
        message = "UUID not found in live items or cache."
    end

    BurdJournals.Server.sendToClient(player, "debugJournalUUIDDeleteResult", {
        uuid = uuid,
        found = foundAny,
        deletedLive = deletedLive,
        deletedLiveOwner = deletedLiveOwner,
        removedIndexEntries = removedIndexEntries,
        removedBackupEntries = removedBackupEntries,
        message = message,
    })
end

function BurdJournals.Server.resolveDebugApplyJournalTarget(requestingPlayer, args)
    if not requestingPlayer or not args then
        return nil, nil, nil, nil, "invalidPayload", nil, nil
    end

    local requestedId = tonumber(args.journalId) or args.journalId
    local requestedUUID = trimUUID(args.journalUUID)
    if not requestedUUID and type(args.journalData) == "table" then
        requestedUUID = trimUUID(args.journalData.uuid)
    end
    if not requestedUUID then
        requestedUUID = trimUUID(args.journalKey)
    end

    local function resolveOwnerFromJournal(journalRef, fallbackOwnerPlayer)
        if not journalRef then
            return nil, nil
        end
        local modData = journalRef.getModData and journalRef:getModData() or nil
        local data = modData and modData.BurdJournals or nil
        local ownerUsername = type(data) == "table" and data.ownerUsername or nil
        local ownerSteamId = type(data) == "table" and data.ownerSteamId or nil
        if not ownerUsername and fallbackOwnerPlayer and fallbackOwnerPlayer.getUsername then
            ownerUsername = fallbackOwnerPlayer:getUsername()
        end
        if not ownerSteamId and fallbackOwnerPlayer and BurdJournals.getPlayerSteamId then
            ownerSteamId = BurdJournals.getPlayerSteamId(fallbackOwnerPlayer)
        end
        return ownerUsername, ownerSteamId
    end

    local ownerPlayer = requestingPlayer
    local journal = nil
    local resolvePath = "unresolved"
    local ownerUsername = nil
    local ownerSteamId = nil

    if requestedId then
        journal = BurdJournals.findItemByIdInPlayerInventory(requestingPlayer, requestedId)
        if journal then
            resolvePath = "requesterById"
            ownerUsername, ownerSteamId = resolveOwnerFromJournal(journal, ownerPlayer)
            return journal, ownerPlayer, requestedUUID, requestedId, resolvePath, ownerUsername, ownerSteamId
        end
    end

    if requestedUUID and BurdJournals.findJournalByUUIDInPlayerInventory then
        journal = BurdJournals.findJournalByUUIDInPlayerInventory(requestingPlayer, requestedUUID)
        if journal then
            resolvePath = "requesterByUUID"
            ownerUsername, ownerSteamId = resolveOwnerFromJournal(journal, ownerPlayer)
            return journal, ownerPlayer, requestedUUID, requestedId, resolvePath, ownerUsername, ownerSteamId
        end
    end

    if requestedUUID and BurdJournals.Server.findLiveJournalByUUID then
        journal, ownerPlayer = BurdJournals.Server.findLiveJournalByUUID(requestedUUID)
        if journal then
            resolvePath = "liveByUUID"
            ownerPlayer = ownerPlayer or requestingPlayer
            ownerUsername, ownerSteamId = resolveOwnerFromJournal(journal, ownerPlayer)
            return journal, ownerPlayer, requestedUUID, requestedId, resolvePath, ownerUsername, ownerSteamId
        end
    end

    if requestedUUID and BurdJournals.Server.getJournalUUIDIndex then
        local indexCache = BurdJournals.Server.getJournalUUIDIndex()
        local entry = indexCache and indexCache.journals and indexCache.journals[requestedUUID] or nil
        if type(entry) == "table" then
            ownerUsername = entry.ownerUsername or ownerUsername
            ownerSteamId = entry.ownerSteamId or ownerSteamId
            local indexedOwner = nil
            if entry.ownerUsername and BurdJournals.Server.findPlayerByUsername then
                indexedOwner = BurdJournals.Server.findPlayerByUsername(entry.ownerUsername)
            end
            local indexedId = tonumber(entry.itemId) or entry.itemId
            if indexedOwner and indexedId then
                journal = BurdJournals.findItemByIdInPlayerInventory(indexedOwner, indexedId)
                if journal then
                    resolvePath = "indexOwnerById"
                    ownerPlayer = indexedOwner
                    local liveOwnerUsername, liveOwnerSteamId = resolveOwnerFromJournal(journal, ownerPlayer)
                    return journal, ownerPlayer, requestedUUID, requestedId, resolvePath, liveOwnerUsername, liveOwnerSteamId
                end
            end
        end
    end

    if requestedUUID and BurdJournals.Server.findDebugBackupByUUID then
        local _, backup = BurdJournals.Server.findDebugBackupByUUID(requestedUUID)
        if type(backup) == "table" then
            ownerUsername = ownerUsername or backup.ownerUsername
            ownerSteamId = ownerSteamId or backup.ownerSteamId
        end
    end

    return nil, nil, requestedUUID, requestedId, resolvePath, ownerUsername, ownerSteamId
end

function BurdJournals.Server.normalizeDebugEditorJournalType(typeValue)
    local value = tostring(typeValue or "")
    if value == "blank" or value == "filled" or value == "worn" or value == "bloody" or value == "cursed" or value == "yuletide" then
        return value
    end
    return nil
end

function BurdJournals.Server.getDebugEditorItemTypeForType(typeValue)
    local journalType = BurdJournals.Server.normalizeDebugEditorJournalType(typeValue)
    if not journalType then
        return nil
    end
    if journalType == "blank" then
        return "BurdJournals.BlankSurvivalJournal"
    elseif journalType == "worn" then
        return "BurdJournals.FilledSurvivalJournal_Worn"
    elseif journalType == "bloody" then
        return "BurdJournals.FilledSurvivalJournal_Bloody"
    elseif journalType == "cursed" then
        return BurdJournals.CURSED_ITEM_TYPE or "BurdJournals.CursedJournal"
    elseif journalType == "yuletide" then
        return BurdJournals.YULETIDE_ITEM_TYPE or "BurdJournals.YuletideJournal"
    end
    return "BurdJournals.FilledSurvivalJournal"
end

function BurdJournals.Server.inferDebugEditorJournalTypeFromItem(journal)
    local fullType = journal and journal.getFullType and journal:getFullType() or ""
    local cursedType = BurdJournals.CURSED_ITEM_TYPE or "BurdJournals.CursedJournal"
    local yuletideType = BurdJournals.YULETIDE_ITEM_TYPE or "BurdJournals.YuletideJournal"
    if type(fullType) == "string" and string.find(fullType, "BlankSurvivalJournal", 1, true) then
        return "blank"
    end
    if fullType == yuletideType then
        return "yuletide"
    end
    if fullType == cursedType then
        return "cursed"
    end
    if type(fullType) == "string" and string.find(fullType, "_Worn", 1, true) then
        return "worn"
    end
    if type(fullType) == "string" and string.find(fullType, "_Bloody", 1, true) then
        return "bloody"
    end
    return "filled"
end

function BurdJournals.Server.convertDebugEditorJournalTypeIfNeeded(player, journal, desiredJournalType)
    local normalizedType = BurdJournals.Server.normalizeDebugEditorJournalType(desiredJournalType)
    if not normalizedType then
        return journal, nil
    end

    local desiredItemType = BurdJournals.Server.getDebugEditorItemTypeForType(normalizedType)
    if not desiredItemType then
        return journal, nil
    end

    local currentItemType = journal and journal.getFullType and journal:getFullType() or nil
    if currentItemType == desiredItemType then
        return journal, nil
    end

    if player then
        BurdJournals.safePcall(function()
            if player:getPrimaryHandItem() == journal then
                player:setPrimaryHandItem(nil)
            end
            if player:getSecondaryHandItem() == journal then
                player:setSecondaryHandItem(nil)
            end
        end)
    end

    local sourceContainer = journal and journal.getContainer and journal:getContainer() or nil
    local targetContainer = sourceContainer or (player and player.getInventory and player:getInventory()) or nil

    if sourceContainer and sourceContainer.Remove then
        sourceContainer:Remove(journal)
        if sourceContainer.setDrawDirty then
            sourceContainer:setDrawDirty(true)
        end
        if sendRemoveItemFromContainer then
            sendRemoveItemFromContainer(sourceContainer, journal)
        end
    end

    if player and player.getInventory then
        local inventory = player:getInventory()
        if inventory and inventory:contains(journal) then
            inventory:Remove(journal)
            if inventory.setDrawDirty then
                inventory:setDrawDirty(true)
            end
            if sendRemoveItemFromContainer then
                sendRemoveItemFromContainer(inventory, journal)
            end
        end
    end

    local replacement = nil
    if targetContainer and targetContainer.AddItem then
        replacement = addJournalItemToContainerCompat(targetContainer, desiredItemType)
    end

    if not replacement and player and player.getInventory then
        local inventory = player:getInventory()
        if inventory and inventory ~= targetContainer then
            replacement = addJournalItemToContainerCompat(inventory, desiredItemType)
            if replacement then
                targetContainer = inventory
            end
        end
    end

    if not replacement then
        return nil, "Failed to create converted journal item"
    end

    if targetContainer and targetContainer.setDrawDirty then
        targetContainer:setDrawDirty(true)
    end
    if sendAddItemToContainer and targetContainer then
        sendAddItemToContainer(targetContainer, replacement)
    end

    return replacement, nil
end

function BurdJournals.Server.applyNormalizedTypeHintsFromEditorSelection(normalized, desiredJournalType)
    if type(normalized) ~= "table" then
        return
    end
    local journalType = BurdJournals.Server.normalizeDebugEditorJournalType(desiredJournalType)
    if not journalType then
        return
    end

    local requestedYuletideState = normalized.yuletideState
    local requestedYuletideWrappedVariant = normalized.yuletideWrappedVariant

    normalized.isWorn = nil
    normalized.isBloody = nil
    normalized.wasFromWorn = nil
    normalized.wasFromBloody = nil
    normalized.isCursedJournal = nil
    normalized.isCursedReward = nil
    normalized.cursedState = nil
    normalized.isYuletideJournal = nil
    normalized.yuletideState = nil
    normalized.yuletideImmediateGifts = nil
    normalized.yuletideGiftGranted = nil
    normalized.yuletideGiftTier = nil
    normalized.yuletideGiftRoll = nil
    normalized.yuletideManualRewards = nil
    normalized.yuletideWrappedVariant = nil
    normalized.yuletideDeliveryToken = nil
    normalized.yuletideDeliveredBy = nil
    normalized.yuletideDeliveryLabel = nil
    normalized.yuletidePendingDelivery = nil
    normalized.yuletideBeacon = nil

    if journalType == "worn" then
        normalized.isWorn = true
        normalized.wasFromWorn = true
        normalized.isWritten = true
    elseif journalType == "bloody" then
        normalized.isBloody = true
        normalized.wasFromBloody = true
        normalized.isWritten = true
    elseif journalType == "cursed" then
        normalized.isCursedJournal = true
        normalized.isCursedReward = false
        normalized.cursedState = "dormant"
        -- Expired curses: record spawn time so old curses can go dormant.
        normalized.cursedSpawnedAtHours = normalized.cursedSpawnedAtHours
            or (BurdJournals.Server.getWorldAgeHoursSafe and BurdJournals.Server.getWorldAgeHoursSafe())
            or nil
        normalized.isWritten = true
    elseif journalType == "yuletide" then
        local resolvedYuletideWrappedVariant = BurdJournals.normalizeYuletideWrappedVariant
            and BurdJournals.normalizeYuletideWrappedVariant(requestedYuletideWrappedVariant)
            or (requestedYuletideWrappedVariant ~= nil and tostring(requestedYuletideWrappedVariant) or nil)
        if requestedYuletideState ~= BurdJournals.YULETIDE_STATE_UNWRAPPED and not resolvedYuletideWrappedVariant then
            resolvedYuletideWrappedVariant = BurdJournals.chooseRandomYuletideWrappedVariant
                and BurdJournals.chooseRandomYuletideWrappedVariant()
                or "1"
        end
        normalized.isYuletideJournal = true
        normalized.yuletideState = requestedYuletideState == BurdJournals.YULETIDE_STATE_UNWRAPPED
            and BurdJournals.YULETIDE_STATE_UNWRAPPED
            or BurdJournals.YULETIDE_STATE_WRAPPED
        normalized.yuletideWrappedVariant = resolvedYuletideWrappedVariant
        normalized.isWritten = true
    elseif journalType == "blank" then
        normalized.isWritten = false
    else
        normalized.isWritten = true
    end
end

function BurdJournals.Server.applyNormalizedDebugJournalDataToItem(journal, normalized, requestedUUID)
    if not journal or type(normalized) ~= "table" then
        return nil
    end

    local worldAgeNow = (getGameTime and getGameTime() and getGameTime():getWorldAgeHours()) or nil
    local modData = journal:getModData()
    modData.BurdJournals = modData.BurdJournals or {}
    local bj = modData.BurdJournals

    if not bj.uuid then
        bj.uuid = normalized.uuid
            or requestedUUID
            or ((BurdJournals.generateUUID and BurdJournals.generateUUID()) or ("debug-" .. tostring(journal:getID())))
    end

    bj.skills = {}
    for skillName, skillData in pairs(normalized.skills or {}) do
        if skillName and type(skillData) == "table" then
            bj.skills[skillName] = {
                xp = tonumber(skillData.xp) or 0,
                level = tonumber(skillData.level) or 0
            }
        end
    end

    bj.traits = {}
    for traitId, value in pairs(normalized.traits or {}) do
        if traitId then
            bj.traits[traitId] = value
        end
    end

    bj.recipes = {}
    for recipeName, value in pairs(normalized.recipes or {}) do
        if recipeName then
            bj.recipes[recipeName] = value
        end
    end

    bj.stats = {}
    for statId, statData in pairs(normalized.stats or {}) do
        if statId then
            bj.stats[statId] = statData
        end
    end

    bj.claims = {}
    for characterId, claimData in pairs(normalized.claims or {}) do
        if characterId then
            bj.claims[characterId] = claimData
        end
    end

    bj.claimedSkills = {}
    for skillName, value in pairs(normalized.claimedSkills or {}) do
        if skillName then
            bj.claimedSkills[skillName] = value
        end
    end

    bj.claimedTraits = {}
    for traitId, value in pairs(normalized.claimedTraits or {}) do
        if traitId then
            bj.claimedTraits[traitId] = value
        end
    end

    bj.claimedRecipes = {}
    for recipeName, value in pairs(normalized.claimedRecipes or {}) do
        if recipeName then
            bj.claimedRecipes[recipeName] = value
        end
    end

    bj.claimedStats = {}
    for statId, value in pairs(normalized.claimedStats or {}) do
        if statId then
            bj.claimedStats[statId] = value
        end
    end

    bj.claimedForgetSlot = {}
    for characterId, value in pairs(normalized.claimedForgetSlot or {}) do
        if characterId then
            bj.claimedForgetSlot[characterId] = value
        end
    end

    bj.forgetSlot = (normalized.forgetSlot == true) and true or nil
    bj.forgetSlotCount = bj.forgetSlot and tonumber(normalized.forgetSlotCount) or nil

    bj.readCount = tonumber(normalized.readCount) or 0
    bj.readSessionCount = tonumber(normalized.readSessionCount) or 0
    bj.currentSessionId = normalized.currentSessionId
    bj.currentSessionReadCount = tonumber(normalized.currentSessionReadCount) or 0
    bj.skillReadCounts = {}
    bj.drLegacyMode3Migrated = normalized.drLegacyMode3Migrated == true
    bj.migrationSchemaVersion = tonumber(normalized.migrationSchemaVersion) or (tonumber(BurdJournals.MIGRATION_SCHEMA_VERSION) or 0)
    local normalizedSkillReadCounts = normalized.skillReadCounts
    if type(normalizedSkillReadCounts) ~= "table" and BurdJournals.normalizeTable then
        normalizedSkillReadCounts = BurdJournals.normalizeTable(normalizedSkillReadCounts)
    end
    if type(normalizedSkillReadCounts) == "table" then
        for skillName, count in pairs(normalizedSkillReadCounts) do
            if skillName then
                bj.skillReadCounts[skillName] = tonumber(count) or 0
            end
        end
    end

    local fullType = journal.getFullType and journal:getFullType() or nil
    local isWornType = type(fullType) == "string" and string.find(fullType, "_Worn", 1, true) ~= nil
    local isBloodyType = type(fullType) == "string" and string.find(fullType, "_Bloody", 1, true) ~= nil
    local isCursedType = fullType == (BurdJournals.CURSED_ITEM_TYPE or "BurdJournals.CursedJournal")
    local isYuletideType = fullType == (BurdJournals.YULETIDE_ITEM_TYPE or "BurdJournals.YuletideJournal")
    local explicitPersonalOrigin = tostring(normalized.originMode or normalized.sourceType or "") == "personal"
    local forceFoundClaimMode = (not explicitPersonalOrigin) and (
        isWornType
        or isBloodyType
        or isCursedType
        or isYuletideType
        or normalized.isWorn == true
        or normalized.isBloody == true
        or normalized.isCursedJournal == true
        or normalized.isCursedReward == true
        or normalized.isYuletideJournal == true
    )
    local useDebugProfile = normalized.isDebugSpawned == true

    bj.isDebugSpawned = useDebugProfile
    bj.isDebugEdited = useDebugProfile and (normalized.isDebugEdited ~= false) or nil
    if forceFoundClaimMode then
        bj.isPlayerCreated = false
    elseif normalized.isPlayerCreated ~= nil then
        bj.isPlayerCreated = normalized.isPlayerCreated == true
    end
    if normalized.sourceType ~= nil then
        bj.sourceType = tostring(normalized.sourceType)
    end
    if normalized.originMode ~= nil then
        bj.originMode = tostring(normalized.originMode)
    end
    bj.ownerMode = normalized.ownerMode and tostring(normalized.ownerMode) or bj.ownerMode
    bj.ownerUsername = normalized.ownerUsername
    bj.ownerSteamId = normalized.ownerSteamId
    bj.ownerCharacterName = normalized.ownerCharacterName
    bj.author = normalized.author
    bj.profession = normalized.profession
    bj.professionName = normalized.professionName
    bj.flavorKey = normalized.flavorKey
    bj.flavorText = normalized.flavorText
    bj.loreNoteText = normalized.loreNoteText
    if normalized.timestamp ~= nil then
        bj.timestamp = tonumber(normalized.timestamp) or bj.timestamp
    elseif bj.timestamp == nil and worldAgeNow ~= nil then
        bj.timestamp = tonumber(worldAgeNow) or 0
    end
    if worldAgeNow ~= nil then
        bj.lastModified = tonumber(worldAgeNow) or bj.lastModified
    end
    if normalized.isWorn ~= nil then
        bj.isWorn = normalized.isWorn == true
    elseif isWornType and bj.isWorn ~= true then
        bj.isWorn = true
    end
    if normalized.isBloody ~= nil then
        bj.isBloody = normalized.isBloody == true
    elseif isBloodyType and bj.isBloody ~= true then
        bj.isBloody = true
    end
    if normalized.wasFromWorn ~= nil then
        bj.wasFromWorn = normalized.wasFromWorn == true
    elseif isWornType and bj.wasFromWorn ~= true then
        bj.wasFromWorn = true
    end
    if normalized.wasFromBloody ~= nil then
        bj.wasFromBloody = normalized.wasFromBloody == true
    elseif isBloodyType and bj.wasFromBloody ~= true then
        bj.wasFromBloody = true
    end
    if normalized.wasRestored ~= nil then
        bj.wasRestored = normalized.wasRestored == true
    end

    bj.isCursedReward = normalized.isCursedReward == true
    bj.isCursedJournal = normalized.isCursedJournal == true
    if isCursedType and bj.isCursedReward ~= true then
        bj.isCursedJournal = true
    end
    if bj.isCursedJournal then
        bj.cursedState = (normalized.cursedState == "unleashed") and "unleashed" or "dormant"
    elseif bj.isCursedReward then
        bj.cursedState = "unleashed"
    else
        bj.cursedState = nil
    end
    -- Expired curses: preserve/record spawn time for dormant/hidden cursed journals.
    if bj.isCursedJournal == true or normalized.isHiddenCursedJournal == true then
        bj.cursedSpawnedAtHours = tonumber(normalized.cursedSpawnedAtHours)
            or (BurdJournals.Server.getWorldAgeHoursSafe and BurdJournals.Server.getWorldAgeHoursSafe())
            or nil
    else
        bj.cursedSpawnedAtHours = tonumber(normalized.cursedSpawnedAtHours) or nil
    end
    bj.cursedEffectType = normalized.cursedEffectType
    bj.cursedUnleashedByCharacterId = normalized.cursedUnleashedByCharacterId
    bj.cursedUnleashedByUsername = normalized.cursedUnleashedByUsername
    bj.cursedUnleashedByName = normalized.cursedUnleashedByName
    bj.cursedUnleashedAtHours = tonumber(normalized.cursedUnleashedAtHours) or nil
    bj.cursedSealSoundEvent = normalized.cursedSealSoundEvent
    bj.cursedForcedEffectType = normalizeCurseEffectType(normalized.cursedForcedEffectType)
    bj.cursedForcedTraitId = normalizeForcedTraitId(normalized.cursedForcedTraitId)
    bj.cursedForcedSkillName = normalizeForcedSkillName(normalized.cursedForcedSkillName)
    if type(normalized.cursedPendingRewards) == "table" then
        bj.cursedPendingRewards = BurdJournals.normalizeJournalData
            and BurdJournals.normalizeJournalData(normalized.cursedPendingRewards)
            or normalized.cursedPendingRewards
    else
        bj.cursedPendingRewards = nil
    end

    bj.isYuletideJournal = normalized.isYuletideJournal == true
    if isYuletideType then
        bj.isYuletideJournal = true
    end
    if bj.isYuletideJournal then
        bj.yuletideState = normalized.yuletideState == BurdJournals.YULETIDE_STATE_UNWRAPPED
            and BurdJournals.YULETIDE_STATE_UNWRAPPED
            or BurdJournals.YULETIDE_STATE_WRAPPED
        bj.yuletideImmediateGifts = type(normalized.yuletideImmediateGifts) == "table"
            and normalized.yuletideImmediateGifts
            or {}
        bj.yuletideGiftGranted = normalized.yuletideGiftGranted == true
        bj.yuletideGiftTier = normalized.yuletideGiftTier or bj.yuletideGiftTier or "practical"
        bj.yuletideGiftRoll = tonumber(normalized.yuletideGiftRoll) or nil
        bj.yuletideManualRewards = normalized.yuletideManualRewards == true and true or nil
        bj.yuletideWrappedVariant = BurdJournals.normalizeYuletideWrappedVariant
            and BurdJournals.normalizeYuletideWrappedVariant(normalized.yuletideWrappedVariant)
            or tostring(normalized.yuletideWrappedVariant or "1")
        bj.yuletideOpenedByName = normalized.yuletideOpenedByName
        bj.yuletideDeliveryToken = normalized.yuletideDeliveryToken
        bj.yuletideDeliveredBy = normalized.yuletideDeliveredBy
        bj.yuletideDeliveryLabel = normalized.yuletideDeliveryLabel
        bj.yuletidePendingDelivery = normalized.yuletidePendingDelivery == true
        bj.yuletideBeacon = type(normalized.yuletideBeacon) == "table" and normalized.yuletideBeacon or nil
    else
        bj.yuletideState = nil
        bj.yuletideImmediateGifts = nil
        bj.yuletideGiftGranted = nil
        bj.yuletideGiftTier = nil
        bj.yuletideGiftRoll = nil
        bj.yuletideManualRewards = nil
        bj.yuletideWrappedVariant = nil
        bj.yuletideOpenedByName = nil
        bj.yuletideDeliveryToken = nil
        bj.yuletideDeliveredBy = nil
        bj.yuletideDeliveryLabel = nil
        bj.yuletidePendingDelivery = nil
        bj.yuletideBeacon = nil
    end

    local inferredType = BurdJournals.Server.inferDebugEditorJournalTypeFromItem(journal)
    if normalized.isWritten ~= nil then
        bj.isWritten = normalized.isWritten == true
    elseif inferredType == "blank" then
        bj.isWritten = nil
    else
        bj.isWritten = true
    end
    bj.sanitizedVersion = BurdJournals.SANITIZE_VERSION or 1
    bj.debugRevision = tonumber(normalized.debugRevision) or tonumber(normalized.revision) or tonumber(bj.debugRevision) or 0
    if normalized.uuid then
        bj.uuid = normalized.uuid
    elseif requestedUUID and not bj.uuid then
        bj.uuid = requestedUUID
    end

    return bj
end

-- Apply debug-edited journal payload to server-side item ModData (authoritative MP persistence)
function BurdJournals.Server.handleDebugApplyJournalEdits(player, args)
    if not BurdJournals.Server.isDebugAllowed(player) then
        BurdJournals.Server.sendToClient(player, "debugError", {message = "Debug commands not allowed"})
        return
    end
    if not args or not args.journalData then
        BurdJournals.Server.sendToClient(player, "debugError", {message = "Invalid debug journal edit payload"})
        return
    end
    if not args.journalId and not args.journalUUID and not args.journalKey and not (type(args.journalData) == "table" and args.journalData.uuid) then
        BurdJournals.Server.sendToClient(player, "debugError", {message = "Debug journal edit payload missing journal identity"})
        return
    end

    local normalized = BurdJournals.normalizeJournalData(args.journalData) or args.journalData
    if type(normalized) ~= "table" then
        BurdJournals.Server.sendToClient(player, "debugError", {message = "Invalid normalized debug journal data"})
        return
    end
    local desiredJournalType = BurdJournals.Server.normalizeDebugEditorJournalType(args and args.desiredJournalType)
    local requestedId = tonumber(args.journalId) or args.journalId
    local requestedUUID = trimUUID(args.journalUUID)
        or (type(args.journalData) == "table" and trimUUID(args.journalData.uuid))
        or trimUUID(args.journalKey)

    local journal, ownerPlayer, resolvedRequestedUUID, resolvedRequestedId, resolvePath, targetOwnerUsername, targetOwnerSteamId =
        BurdJournals.Server.resolveDebugApplyJournalTarget(player, args)
    requestedUUID = resolvedRequestedUUID or requestedUUID
    requestedId = resolvedRequestedId or requestedId
    if journal then
        local modData = journal.getModData and journal:getModData() or nil
        local data = modData and modData.BurdJournals or nil
        local ownerUsername = (type(data) == "table" and data.ownerUsername) or targetOwnerUsername or (ownerPlayer and ownerPlayer.getUsername and ownerPlayer:getUsername()) or nil
        local ownerSteamId = (type(data) == "table" and data.ownerSteamId) or targetOwnerSteamId or (ownerPlayer and BurdJournals.getPlayerSteamId and BurdJournals.getPlayerSteamId(ownerPlayer)) or nil
        if not BurdJournals.Server.canPlayerAccessJournalSnapshot(player, ownerUsername, ownerSteamId) then
            BurdJournals.Server.sendJournalScopeDenied(player, "Journal edit")
            return
        end
    end

    if not journal then
        if desiredJournalType then
            BurdJournals.Server.sendToClient(player, "debugError", {
                message = "Type conversion requires a live journal item. Use a local inventory/world journal first."
            })
            return
        end

        if not BurdJournals.Server.isServerJournalScopeAdmin(player) then
            if not requestedUUID
                or not BurdJournals.Server.canPlayerAccessJournalSnapshot(player, targetOwnerUsername, targetOwnerSteamId)
            then
                BurdJournals.Server.sendJournalScopeDenied(player, "Deferred journal edit")
                return
            end
        end

        local fallbackUUID = trimUUID(requestedUUID or args.journalKey or (type(normalized) == "table" and normalized.uuid))
        local fallbackKey = fallbackUUID or (requestedId and tostring(requestedId))
        if not fallbackKey then
            BurdJournals.Server.sendToClient(player, "debugError", {message = "Journal not found for debug apply (missing UUID/key)"})
            return
        end

        if targetOwnerUsername and not normalized.ownerUsername then
            normalized.ownerUsername = targetOwnerUsername
        end
        if targetOwnerSteamId and not normalized.ownerSteamId then
            normalized.ownerSteamId = targetOwnerSteamId
        end

        local deferredUUID = BurdJournals.Server.persistDebugJournalSnapshot(player, fallbackKey, normalized, nil, {
            savedBy = player:getUsername(),
            pendingApply = true,
            pendingReason = "missingLiveItem:" .. tostring(resolvePath or "unknown"),
            pendingRequestedBy = player:getUsername(),
            sourceTag = "debugApplyDeferred",
        })
        if not deferredUUID then
            BurdJournals.Server.sendToClient(player, "debugError", {message = "Failed to queue deferred debug journal edits"})
            return
        end

        BurdJournals.Server.persistJournalUUIDIndex(BurdJournals.Server.getJournalUUIDIndex())

        BurdJournals.Server.sendToClient(player, "debugSuccess", {
            message = "Deferred debug edits saved for UUID " .. tostring(deferredUUID) .. ". Changes will apply when the journal loads."
        })
        return
    end

    if desiredJournalType then
        local convertedJournal, convertErr =
            BurdJournals.Server.convertDebugEditorJournalTypeIfNeeded(ownerPlayer or player, journal, desiredJournalType)
        if not convertedJournal then
            BurdJournals.Server.sendToClient(player, "debugError", {
                message = "Journal type conversion failed: " .. tostring(convertErr or "unknown")
            })
            return
        end
        journal = convertedJournal
        BurdJournals.Server.applyNormalizedTypeHintsFromEditorSelection(normalized, desiredJournalType)
    end

    local bj = BurdJournals.Server.applyNormalizedDebugJournalDataToItem(journal, normalized, requestedUUID)
    if not bj then
        BurdJournals.Server.sendToClient(player, "debugError", {message = "Failed applying normalized debug journal data"})
        return
    end

    if BurdJournals.updateJournalName then
        BurdJournals.updateJournalName(journal, true)
    end
    if BurdJournals.updateJournalIcon then
        BurdJournals.updateJournalIcon(journal)
    end

    -- Also persist an authoritative backup entry so debug edits survive future patch/update edge cases.
    local journalKey = args.journalKey or bj.uuid or tostring(args.journalId)
    local liveUUID = BurdJournals.Server.persistDebugJournalSnapshot(player, journalKey, bj, journal, {
        savedBy = player:getUsername(),
        pendingApply = false,
        sourceTag = "debugApplyJournalEdits:" .. tostring(resolvePath),
    })
    if liveUUID then
        bj.debugRevision = tonumber(bj.debugRevision) or 0
    end

    if journal.transmitModData and not BurdJournals.Server.shouldDeferBatchTransmit() then
            journal:transmitModData()
    end
    BurdJournals.Server.persistJournalUUIDIndex(BurdJournals.Server.getJournalUUIDIndex())
    BurdJournals.Server.updateJournalUUIDIndex(journal, ownerPlayer or player, "debugApplyJournalEdits:" .. tostring(resolvePath))
    if BurdJournals.captureJournalDRState then
        BurdJournals.captureJournalDRState(journal, "debugApplyJournalEdits", player)
    end

    BurdJournals.Server.sendToClient(player, "journalMaterialized", {
        oldJournalId = requestedId,
        oldJournalUUID = requestedUUID,
        newJournalId = journal.getID and journal:getID() or requestedId,
        journalUUID = bj.uuid or requestedUUID,
        journalFingerprint = BurdJournals.buildJournalLookupFingerprint
            and BurdJournals.buildJournalLookupFingerprint(journal, bj)
            or nil,
        journalData = bj,
        source = "debugApplyJournalEdits",
    })
end

function BurdJournals.Server.normalizeJournalExportClaimMode(value)
    local mode = tostring(value or "preserve")
    if mode == "clear" then
        return "clear"
    end
    return "preserve"
end

function BurdJournals.Server.getDebugJournalExportItemInfo(journal, journalData)
    journalData = type(journalData) == "table" and journalData or {}
    return {
        itemType = journal and journal.getFullType and journal:getFullType() or journalData.fullType or journalData.itemType,
        itemName = journal and journal.getName and journal:getName() or journalData.customName,
    }
end

function BurdJournals.Server.getImportedJournalItemType(importedData, source)
    importedData = type(importedData) == "table" and importedData or {}
    source = type(source) == "table" and source or {}
    local itemType = tostring(source.itemType or importedData.fullType or importedData.itemType or "")
    local allowed = {
        ["BurdJournals.BlankSurvivalJournal"] = true,
        ["BurdJournals.FilledSurvivalJournal"] = true,
        ["BurdJournals.FilledSurvivalJournal_Worn"] = true,
        ["BurdJournals.FilledSurvivalJournal_Bloody"] = true,
        [BurdJournals.CURSED_ITEM_TYPE or "BurdJournals.CursedJournal"] = true,
        [BurdJournals.YULETIDE_ITEM_TYPE or "BurdJournals.YuletideJournal"] = true,
    }
    if allowed[itemType] then
        return itemType
    end
    if importedData.isYuletideJournal == true then
        return BurdJournals.YULETIDE_ITEM_TYPE or "BurdJournals.YuletideJournal"
    end
    if importedData.isCursedJournal == true then
        return BurdJournals.CURSED_ITEM_TYPE or "BurdJournals.CursedJournal"
    end
    if importedData.isBloody == true or importedData.wasFromBloody == true then
        return "BurdJournals.FilledSurvivalJournal_Bloody"
    end
    if importedData.isWorn == true or importedData.wasFromWorn == true then
        return "BurdJournals.FilledSurvivalJournal_Worn"
    end
    return "BurdJournals.FilledSurvivalJournal"
end

function BurdJournals.Server.sendDebugJournalImportResult(player, ok, args)
    args = type(args) == "table" and args or {}
    args.success = ok == true
    BurdJournals.Server.sendToClient(player, ok and "debugJournalImportResult" or "debugError", args)
end

function BurdJournals.Server.handleDebugExportJournalJSON(player, args)
    if not BurdJournals.Server.isDebugAllowed(player) then
        BurdJournals.Server.sendToClient(player, "debugError", {message = "Debug commands not allowed"})
        return
    end
    args = type(args) == "table" and args or {}
    local journal, ownerPlayer, resolvedUUID = BurdJournals.Server.resolveDebugApplyJournalTarget(player, args)
    if not journal then
        BurdJournals.Server.sendToClient(player, "debugJournalExportJSON", {
            success = false,
            message = "No live journal found for export"
        })
        return
    end

    local snapshot = BurdJournals.Server.copyJournalData and BurdJournals.Server.copyJournalData(journal) or nil
    if type(snapshot) ~= "table" then
        BurdJournals.Server.sendToClient(player, "debugJournalExportJSON", {
            success = false,
            message = "Selected item has no journal data"
        })
        return
    end
    if resolvedUUID and not snapshot.uuid then
        snapshot.uuid = resolvedUUID
    end
    local payload, payloadErr = BurdJournals.buildJournalExportPayload(
        snapshot,
        BurdJournals.Server.getDebugJournalExportItemInfo(journal, snapshot),
        {exportedBy = player and player.getUsername and player:getUsername() or nil}
    )
    if not payload then
        BurdJournals.Server.sendToClient(player, "debugJournalExportJSON", {
            success = false,
            message = payloadErr or "Failed to build journal export"
        })
        return
    end
    local jsonText, jsonErr = BurdJournals.encodeJournalExportJSON(payload, {pretty = true})
    if not jsonText then
        BurdJournals.Server.sendToClient(player, "debugJournalExportJSON", {
            success = false,
            message = jsonErr or "Failed to encode journal export"
        })
        return
    end
    BurdJournals.Server.sendToClient(player, "debugJournalExportJSON", {
        success = true,
        jsonText = jsonText,
        payload = payload,
        counts = payload.counts,
        source = payload.source,
        ownerUsername = ownerPlayer and ownerPlayer.getUsername and ownerPlayer:getUsername() or nil,
    })
end

function BurdJournals.Server.applyImportedJournalDataToItem(player, journal, importedData, options)
    if not journal or type(importedData) ~= "table" then
        return nil, "No target journal"
    end
    local bj = BurdJournals.Server.applyNormalizedDebugJournalDataToItem(journal, importedData, importedData.uuid)
    if not bj then
        return nil, "Failed applying imported journal data"
    end
    bj.wasImportedFromJSON = true
    bj.importedBy = player and player.getUsername and player:getUsername() or nil
    bj.importClaimMode = BurdJournals.Server.normalizeJournalExportClaimMode(options and options.claimMode)
    if BurdJournals.updateJournalName then
        BurdJournals.updateJournalName(journal, true)
    end
    if BurdJournals.updateJournalIcon then
        BurdJournals.updateJournalIcon(journal)
    end
    if journal.transmitModData and not BurdJournals.Server.shouldDeferBatchTransmit() then
            journal:transmitModData()
    end
    local journalKey = bj.uuid or importedData.uuid or tostring(journal.getID and journal:getID() or "")
    BurdJournals.Server.persistDebugJournalSnapshot(player, journalKey, bj, journal, {
        savedBy = player and player.getUsername and player:getUsername() or nil,
        pendingApply = false,
        sourceTag = "debugImportJournalJSON",
    })
    if BurdJournals.Server.updateJournalUUIDIndex then
        BurdJournals.Server.updateJournalUUIDIndex(journal, player, "debugImportJournalJSON")
    end
    BurdJournals.Server.persistJournalUUIDIndex(BurdJournals.Server.getJournalUUIDIndex())
    return bj
end

function BurdJournals.Server.handleDebugImportJournalJSON(player, args)
    if not BurdJournals.Server.isDebugAllowed(player) then
        BurdJournals.Server.sendToClient(player, "debugError", {message = "Debug commands not allowed"})
        return
    end
    args = type(args) == "table" and args or {}
    local envelope, parseErr = BurdJournals.parseJournalExportPayload(args.jsonText or "")
    if not envelope then
        BurdJournals.Server.sendDebugJournalImportResult(player, false, {message = parseErr or (BurdJournals.safeGetText and BurdJournals.safeGetText("UI_BurdJournals_DebugJournalImportInvalidJSON", "Invalid journal JSON") or "Invalid journal JSON")})
        return
    end
    local claimMode = BurdJournals.Server.normalizeJournalExportClaimMode(args.claimMode)
    local importedData, importMeta = BurdJournals.normalizeImportedJournalPayload(envelope, {claimMode = claimMode})
    if not importedData then
        BurdJournals.Server.sendDebugJournalImportResult(player, false, {message = importMeta or (BurdJournals.safeGetText and BurdJournals.safeGetText("UI_BurdJournals_DebugJournalImportInvalidData", "Invalid imported journal data") or "Invalid imported journal data")})
        return
    end

    local mode = tostring(args.mode or "auto")
    local journal = nil
    local resolvedUUID = nil
    if mode ~= "spawn" then
        journal, _, resolvedUUID = BurdJournals.Server.resolveDebugApplyJournalTarget(player, args)
    end

    local action = "overwrite"
    if not journal then
        action = "spawn"
        local inventory = player and player.getInventory and player:getInventory() or nil
        if not inventory then
            BurdJournals.Server.sendDebugJournalImportResult(player, false, {message = BurdJournals.safeGetText and BurdJournals.safeGetText("UI_BurdJournals_DebugJournalImportNoInventory", "No inventory available for imported journal") or "No inventory available for imported journal"})
            return
        end
        local itemType = BurdJournals.Server.getImportedJournalItemType(importedData, envelope.source)
        journal = inventory:AddItem(itemType)
        if not journal then
            BurdJournals.Server.sendDebugJournalImportResult(player, false, {message = BurdJournals.safeGetText and BurdJournals.safeGetText("UI_BurdJournals_DebugJournalImportCreateFailed", "Failed to create imported journal") or "Failed to create imported journal"})
            return
        end
    end

    local bj, applyErr = BurdJournals.Server.applyImportedJournalDataToItem(player, journal, importedData, {claimMode = claimMode})
    if not bj then
        BurdJournals.Server.sendDebugJournalImportResult(player, false, {message = applyErr or (BurdJournals.safeGetText and BurdJournals.safeGetText("UI_BurdJournals_DebugJournalImportApplyFailed", "Failed to import journal") or "Failed to import journal")})
        return
    end
    local inventory = player and player.getInventory and player:getInventory() or nil
    if inventory and inventory.sync then
        inventory:sync()
    end
    if inventory and inventory.setDrawDirty then
        inventory:setDrawDirty(true)
    end
    if isServer and isServer() and player and player.syncInventory then
        player:syncInventory()
    end
    if action == "spawn" and isServer and isServer() then
        transmitYuletideGiftItem(player, inventory, journal, "Debug journal import")
    end

    BurdJournals.Server.sendToClient(player, "journalMaterialized", {
        oldJournalId = args.journalId,
        oldJournalUUID = resolvedUUID or args.journalUUID,
        newJournalId = journal.getID and journal:getID() or nil,
        journalUUID = bj.uuid,
        journalData = bj,
        source = "debugImportJournalJSON",
    })
    BurdJournals.Server.sendDebugJournalImportResult(player, true, {
        message = (action == "spawn") and (BurdJournals.safeGetText and BurdJournals.safeGetText("UI_BurdJournals_DebugJournalImportSpawned", "Imported journal created in inventory") or "Imported journal created in inventory") or (BurdJournals.safeGetText and BurdJournals.safeGetText("UI_BurdJournals_DebugJournalImportApplied", "Imported journal applied to selected journal") or "Imported journal applied to selected journal"),
        action = action,
        journalId = journal.getID and journal:getID() or nil,
        journalUUID = bj.uuid,
        itemType = journal.getFullType and journal:getFullType() or nil,
        counts = importMeta and importMeta.counts or BurdJournals.getJournalExportCounts(bj),
        claimMode = claimMode,
        requestMode = mode,
    })
end


-- ============================================================================
-- Debug Command Handlers (Server-side)
-- ============================================================================

-- Check if player is allowed to use debug commands
function BurdJournals.Server.isDebugAllowed(player)
    if not player then 
        BurdJournals.debugPrint("[BurdJournals] DEBUG: isDebugAllowed - no player")
        return false 
    end
    
    local username = player:getUsername() or "unknown"
    local isMultiplayerServer = isServer and isServer()
    local isAdmin = false
    if BurdJournals.Server.isDebugAdmin then
        isAdmin = BurdJournals.Server.isDebugAdmin(player)
    else
        isAdmin = player.isAccessLevel and player:isAccessLevel("admin") == true
    end
    
    -- Check sandbox option directly first; B41 DS can be inconsistent about the shared accessor.
    local sandboxEnabled = false
    if SandboxVars and SandboxVars.BurdJournals and SandboxVars.BurdJournals.AllowDebugCommands ~= nil then
        sandboxEnabled = SandboxVars.BurdJournals.AllowDebugCommands == true
    elseif BurdJournals.getSandboxOption then
        sandboxEnabled = BurdJournals.getSandboxOption("AllowDebugCommands") == true
    end
    BurdJournals.debugPrint("[BurdJournals] DEBUG: isDebugAllowed for " .. username
        .. " - sandboxEnabled=" .. tostring(sandboxEnabled)
        .. ", isAdmin=" .. tostring(isAdmin)
        .. ", isMultiplayerServer=" .. tostring(isMultiplayerServer))

    -- Multiplayer safety: explicit sandbox opt-in or elevated access is required.
    if isMultiplayerServer then
        if sandboxEnabled then
            return true
        end
        if isAdmin then
            return true
        end
        BurdJournals.debugPrint("[BurdJournals] DEBUG: isDebugAllowed - DENIED (sandbox/admin required in MP) for " .. username)
        return false
    end

    -- Single-player/local fallback behavior.
    if sandboxEnabled then return true end
    if isAdmin then return true end
    
    -- Check global debug mode
    local debugMode = getDebug and getDebug()
    BurdJournals.debugPrint("[BurdJournals] DEBUG: isDebugAllowed - debugMode=" .. tostring(debugMode))
    if debugMode then return true end
    
    BurdJournals.debugPrint("[BurdJournals] DEBUG: isDebugAllowed - DENIED for " .. username)
    return false
end

-- Helper: Find player by username
function BurdJournals.Server.findPlayerByUsername(username)
    if not username then return nil end
    local onlinePlayers = getOnlinePlayers()
    if onlinePlayers then
        for i = 0, onlinePlayers:size() - 1 do
            local p = onlinePlayers:get(i)
            if p and p:getUsername() == username then
                return p
            end
        end
    end
    return nil
end

function BurdJournals.Server.isDebugAdmin(player)
    if not player then return false end
    if player.isAccessLevel and player:isAccessLevel("admin") then
        return true
    end
    local accessLevel = player.getAccessLevel and player:getAccessLevel() or nil
    local normalized = tostring(accessLevel or ""):lower()
    if normalized == "admin" or normalized == "moderator" or normalized == "gm" then
        return true
    end
    return accessLevel ~= nil and normalized ~= "" and normalized ~= "none"
end

-- Resolve target player for debug character/baseline edits.
-- Non-admin callers may only target themselves.
function BurdJournals.Server.resolveDebugTargetPlayer(requestingPlayer, targetUsername)
    if not requestingPlayer then
        return nil, "Invalid requesting player"
    end

    local requesterUsername = requestingPlayer.getUsername and requestingPlayer:getUsername() or nil
    if not targetUsername or targetUsername == "" or targetUsername == requesterUsername then
        return requestingPlayer, nil
    end

    if not BurdJournals.Server.isDebugAdmin(requestingPlayer) then
        return nil, "Admin access required to modify another player's data."
    end

    local targetPlayer = BurdJournals.Server.findPlayerByUsername(targetUsername)
    if not targetPlayer then
        return nil, "Player not found: " .. tostring(targetUsername)
    end

    return targetPlayer, nil
end

function BurdJournals.Server.syncDebugTraitTargetPlayer(targetPlayer)
    if not targetPlayer then
        return
    end
    if SyncXp then
        pcall(function()
            SyncXp(targetPlayer)
        end)
    elseif targetPlayer.syncXp then
        pcall(function()
            targetPlayer:syncXp()
        end)
    end
    if targetPlayer.transmitModData then
        pcall(function()
            targetPlayer:transmitModData()
        end)
    end
end

function BurdJournals.Server.buildDebugCharacterDataPayload(targetPlayer)
    local payload = {
        targetUsername = targetPlayer and targetPlayer.getUsername and targetPlayer:getUsername() or nil,
        skills = {},
        traits = {},
        recipes = {},
    }
    if not targetPlayer then return payload end

    local xpObj = targetPlayer.getXp and targetPlayer:getXp() or nil
    local skillMetadata = BurdJournals.discoverSkillMetadata and BurdJournals.discoverSkillMetadata(true) or {}
    for skillName, _ in pairs(skillMetadata or {}) do
        local perk = BurdJournals.getPerkByName and BurdJournals.getPerkByName(skillName) or nil
        if perk then
            local level = targetPlayer.getPerkLevel and (tonumber(targetPlayer:getPerkLevel(perk)) or 0) or 0
            local xp = 0
            if BurdJournals.getPlayerSkillTotalXP then
                xp = tonumber(BurdJournals.getPlayerSkillTotalXP(targetPlayer, perk, skillName)) or 0
            elseif xpObj and xpObj.getXP then
                xp = tonumber(xpObj:getXP(perk)) or 0
            end
            payload.skills[skillName] = { level = level, xp = xp }
        end
    end

    if targetPlayer.getTraits then
        local traits = targetPlayer:getTraits()
        if traits and traits.size then
            for i = 0, traits:size() - 1 do
                local traitId = traits:get(i)
                if traitId then
                    local key = tostring(traitId)
                    payload.traits[key] = (tonumber(payload.traits[key]) or 0) + 1
                end
            end
        end
    end

    local knownRecipes = BurdJournals.getAuthoritativeKnownRecipeSet and BurdJournals.getAuthoritativeKnownRecipeSet(targetPlayer) or {}
    for recipeName, known in pairs(knownRecipes or {}) do
        if known == true then
            payload.recipes[tostring(recipeName)] = true
        end
    end
    return payload
end

function BurdJournals.Server.handleDebugRequestCharacterData(player, args)
    if not BurdJournals.Server.isDebugAllowed(player) then
        BurdJournals.Server.sendToClient(player, "debugError", {message = "Debug commands not allowed"})
        return
    end
    local targetPlayer, targetErr = BurdJournals.Server.resolveDebugTargetPlayer(player, args and args.targetUsername)
    if not targetPlayer then
        BurdJournals.Server.sendToClient(player, "debugError", {message = targetErr or "Target player not found"})
        return
    end
    local payload = BurdJournals.Server.buildDebugCharacterDataPayload(targetPlayer)
    payload.reason = args and args.reason or nil
    BurdJournals.Server.sendToClient(player, "debugCharacterData", payload)
end

function BurdJournals.Server.copyDebugTraitResponsePayload(args)
    local out = {}
    if type(args) == "table" then
        for key, value in pairs(args) do
            out[key] = value
        end
    end
    return out
end

function BurdJournals.Server.sendDebugTraitResponseToInterestedPlayers(requestingPlayer, targetPlayer, command, args)
    BurdJournals.Server.sendToClient(requestingPlayer, command, args)

    local requesterUsername = requestingPlayer and requestingPlayer.getUsername and requestingPlayer:getUsername() or nil
    local targetUsername = targetPlayer and targetPlayer.getUsername and targetPlayer:getUsername() or nil
    local samePlayer = targetPlayer == requestingPlayer
        or (requesterUsername ~= nil and targetUsername ~= nil and tostring(requesterUsername) == tostring(targetUsername))
    if targetPlayer and not samePlayer then
        local targetPayload = BurdJournals.Server.copyDebugTraitResponsePayload(args)
        targetPayload.requesterUsername = requesterUsername
        targetPayload.isTargetMirror = true
        BurdJournals.Server.sendToClient(targetPlayer, command, targetPayload)
    end
end

-- Passive skill traits that need to be removed before setting skill level
-- These traits are auto-granted by PZ based on skill level, but having them
-- while trying to set a different level can cause conflicts
BurdJournals.Server.PASSIVE_SKILL_TRAITS = {
    Strength = {"puny", "weak", "feeble", "stout", "strong"},
    Fitness = {"unfit", "outofshape", "fit", "athletic"}
}

-- Remove all passive skill traits for a specific skill before setting its level
-- This prevents the trait system from bouncing the skill back
function BurdJournals.Server.removePassiveSkillTraits(targetPlayer, skillName)
    local traits = BurdJournals.Server.PASSIVE_SKILL_TRAITS[skillName]
    if not traits then return end
    
    BurdJournals.debugPrint("[BurdJournals] DEBUG: Removing passive skill traits for " .. skillName)
    
    for _, traitId in ipairs(traits) do
        local removed = false
        
        -- Try multiple methods to remove the trait
        -- Method 1: Use safeRemoveTrait if available
        if BurdJournals.safeRemoveTrait then
            removed = BurdJournals.safeRemoveTrait(targetPlayer, traitId) == true
            if removed then
                BurdJournals.debugPrint("[BurdJournals] DEBUG: Removed trait '" .. traitId .. "' via safeRemoveTrait")
            end
        end
        
        -- Method 2: Direct trait removal if safeRemoveTrait didn't work
        if not removed and targetPlayer and targetPlayer.getCharacterTraits then
            local charTraits = targetPlayer:getCharacterTraits()
            if charTraits and charTraits.size and charTraits.get then
                for i = charTraits:size() - 1, 0, -1 do
                    local traitObj = charTraits:get(i)
                    if traitObj then
                        local traitName = traitObj.getName and traitObj:getName() or tostring(traitObj)
                        if string.lower(traitName) == string.lower(traitId) then
                            if charTraits.remove then
                                charTraits:remove(traitObj)
                            end
                            if charTraits.set then
                                charTraits:set(traitObj, false)
                            end
                            local stillHas = targetPlayer.hasTrait and targetPlayer:hasTrait(traitObj) or false
                            if not stillHas then
                                removed = true
                                if BurdJournals.applyTraitLifecycleSideEffects then
                                    pcall(function()
                                        BurdJournals.applyTraitLifecycleSideEffects(targetPlayer, traitId, "trait_removed", {
                                            traitObj = traitObj,
                                            source = "removePassiveSkillTraits_direct_fallback",
                                        })
                                    end)
                                end
                                BurdJournals.debugPrint("[BurdJournals] DEBUG: Removed trait '" .. traitId .. "' via direct removal")
                                break
                            end
                        end
                    end
                end
            end
        end
    end
end

function BurdJournals.Server.buildTraitIdsToTry(traitId)
    local traitIdsToTry = {}
    local seen = {}

    local function addTraitId(id)
        if not id then return end
        id = tostring(id)
        if id == "" then return end
        local key = string.lower(id)
        if seen[key] then return end
        seen[key] = true
        table.insert(traitIdsToTry, id)
    end

    addTraitId(traitId)
    addTraitId(string.lower(traitId))

    if BurdJournals and BurdJournals.TRAIT_ALIASES then
        local aliases = BurdJournals.TRAIT_ALIASES[string.lower(traitId)]
        if aliases then
            for _, alias in ipairs(aliases) do
                addTraitId(alias)
                addTraitId(string.lower(alias))
            end
        end
    end

    return traitIdsToTry
end

-- Resolve trait ID/alias into a Build 42 CharacterTrait object.
-- Returns: traitObj, sourceLabel, traitIdsToTry, foundTraits
function BurdJournals.Server.resolveCharacterTrait(traitId, targetPlayer)
    if not traitId then
        return nil, nil, {}, {}
    end

    local traitIdsToTry = BurdJournals.Server.buildTraitIdsToTry(traitId)
    local foundTraits = {}
    local seenTraits = {}

    local function addFoundTrait(traitObj, sourceLabel)
        if not traitObj then return end
        if seenTraits[traitObj] then return end
        seenTraits[traitObj] = true
        table.insert(foundTraits, { trait = traitObj, source = sourceLabel or "unknown" })
    end

    local function tryResourceLookup(resourceLoc)
        if not (CharacterTrait and CharacterTrait.get and ResourceLocation and ResourceLocation.of) then
            return
        end
        local ok, result = BurdJournals.safePcall(function()
            return CharacterTrait.get(ResourceLocation.of(resourceLoc))
        end)
        if ok and result then
            addFoundTrait(result, "ResourceLocation:" .. tostring(resourceLoc))
        end
    end

    -- 1) ResourceLocation lookups
    if CharacterTrait and CharacterTrait.get and ResourceLocation and ResourceLocation.of then
        for _, tryId in ipairs(traitIdsToTry) do
            local original = "base:" .. tostring(tryId)
            local lower = "base:" .. string.lower(tostring(tryId))
            local spaced = "base:" .. string.lower(tostring(tryId):gsub("(%u)", " %1"):sub(2))

            tryResourceLookup(original)
            if lower ~= original then
                tryResourceLookup(lower)
            end
            if spaced ~= original and spaced ~= lower then
                tryResourceLookup(spaced)
            end
        end
    end

    -- 2) CharacterTrait enum style lookups
    if CharacterTrait then
        for _, tryId in ipairs(traitIdsToTry) do
            local underscored = tostring(tryId):gsub("(%u)", "_%1"):sub(2):upper()
            local ct = CharacterTrait[underscored]
            if ct then
                if type(ct) == "string" then
                    tryResourceLookup(ct)
                else
                    addFoundTrait(ct, "Enum:" .. underscored)
                end
            end
        end
    end

    -- 3) CharacterTraitDefinition scan
    if CharacterTraitDefinition and CharacterTraitDefinition.getTraits then
        local allTraits = CharacterTraitDefinition.getTraits()
        if allTraits then
            local wanted = {}
            for _, tryId in ipairs(traitIdsToTry) do
                wanted[string.lower(tostring(tryId))] = true
            end

            for i = 0, allTraits:size() - 1 do
                local def = allTraits:get(i)
                if def then
                    local defType = def.getType and def:getType() or nil
                    local defLabel = def.getLabel and def:getLabel() or nil
                    local defName = nil

                    if defType and defType.getName then
                        local okName, nameResult = BurdJournals.safePcall(function()
                            return defType:getName()
                        end)
                        if okName and nameResult then
                            defName = tostring(nameResult)
                        end
                    end
                    if not defName and defType then
                        defName = tostring(defType)
                    end

                    local nameMatches = defName and wanted[string.lower(defName)] == true
                    local labelMatches = defLabel and wanted[string.lower(tostring(defLabel))] == true
                    if defType and (nameMatches or labelMatches) then
                        addFoundTrait(defType, "Definition:" .. tostring(defName or defLabel or "?"))
                    end
                end
            end
        end
    end

    -- Prefer trait object the target currently has (important for removal)
    local resolvedTrait = nil
    local resolvedSource = nil
    if targetPlayer and targetPlayer.hasTrait then
        for _, entry in ipairs(foundTraits) do
            if targetPlayer:hasTrait(entry.trait) == true then
                resolvedTrait = entry.trait
                resolvedSource = entry.source
                break
            end
        end
    end

    if not resolvedTrait and #foundTraits > 0 then
        resolvedTrait = foundTraits[1].trait
        resolvedSource = foundTraits[1].source
    end

    return resolvedTrait, resolvedSource, traitIdsToTry, foundTraits
end


-- Handle debug: Set skill level
function BurdJournals.Server.handleDebugSetSkill(player, args)
    if not BurdJournals.Server.isDebugAllowed(player) then
        BurdJournals.Server.sendToClient(player, "debugError", {message = "Debug commands not allowed"})
        return
    end
    
    local skillName = args.skillName
    local level = args.level or 10
    
    -- Support targeting other players (admin-only for cross-player edits)
    local targetPlayer, targetErr = BurdJournals.Server.resolveDebugTargetPlayer(player, args and args.targetUsername)
    if not targetPlayer then
        BurdJournals.Server.sendToClient(player, "debugError", {message = targetErr or "Target player not found"})
        return
    end
    
    local perk = BurdJournals.getPerkByName(skillName)
    if not perk then
        BurdJournals.Server.sendToClient(player, "debugError", {message = "Invalid skill: " .. tostring(skillName)})
        return
    end
    
    local xpObj = targetPlayer:getXp()
    local isPassive = (skillName == "Fitness" or skillName == "Strength")
    
    -- For passive skills, remove existing passive traits BEFORE setting level
    -- This prevents the trait system from bouncing the skill back
    if isPassive then
        BurdJournals.Server.removePassiveSkillTraits(targetPlayer, skillName)
    end
    
    BurdJournals.debugPrint("[BurdJournals] DEBUG: Setting " .. skillName .. " (passive=" .. tostring(isPassive) .. ") to level " .. level .. " for " .. targetPlayer:getUsername())
    
    if isPassive then
        -- For passive skills, use setPerkLevelDebug which directly sets level
        -- This bypasses XP scaling issues that affect Strength specifically
        targetPlayer:setPerkLevelDebug(perk, level)
        BurdJournals.debugPrint("[BurdJournals] DEBUG: Set " .. skillName .. " to level " .. level .. " via setPerkLevelDebug")
    else
        -- For non-passive skills, use XP-based approach
        local currentXP = math.max(0, tonumber(BurdJournals.getPlayerSkillTotalXP and BurdJournals.getPlayerSkillTotalXP(targetPlayer, perk, skillName) or xpObj:getXP(perk)) or 0)
        local targetXP = 0
        
        if level > 0 then
            targetXP = (BurdJournals.getXPThresholdForLevel and BurdJournals.getXPThresholdForLevel(skillName, level))
                or (perk.getTotalXpForLevel and perk:getTotalXpForLevel(level))
                or 0
        end
        
        BurdJournals.debugPrint("[BurdJournals] DEBUG: " .. skillName .. " currentXP=" .. currentXP .. " targetXP=" .. targetXP)

        if BurdJournals.setSkillTotalXPCompat then
            BurdJournals.setSkillTotalXPCompat(targetPlayer, perk, targetXP, skillName)
        end
    end
    
    BurdJournals.Server.syncDebugTraitTargetPlayer(targetPlayer)
    if BurdJournals.clearPlayerJournalSkillClaimLocksForSkill
        and BurdJournals.clearPlayerJournalSkillClaimLocksForSkill(targetPlayer, skillName)
        and targetPlayer.transmitModData then
        targetPlayer:transmitModData()
    end
    
    local finalLevel = level
    BurdJournals.debugPrint("[BurdJournals] DEBUG: " .. skillName .. " final level = " .. finalLevel .. " for " .. targetPlayer:getUsername())
    
    -- Send specific response so client can refresh the appropriate tab
    local payload = {
        skillName = skillName,
        level = finalLevel,
        targetUsername = targetPlayer:getUsername()
    }
    BurdJournals.Server.sendToClient(player, "debugSkillSet", payload)
    if targetPlayer ~= player then
        BurdJournals.Server.sendToClient(targetPlayer, "debugSkillSet", payload)
    end
end

-- Handle debug: Set all skills
function BurdJournals.Server.handleDebugSetAllSkills(player, args)
    BurdJournals.debugPrint("[BurdJournals] SERVER: handleDebugSetAllSkills called for " .. (player and player:getUsername() or "nil"))
    BurdJournals.debugPrint("[BurdJournals] SERVER: args.level = " .. tostring(args and args.level))
    
    if not BurdJournals.Server.isDebugAllowed(player) then
        bsjWriteLogLine("[BurdJournals] SERVER: Debug not allowed, sending error")
        BurdJournals.Server.sendToClient(player, "debugError", {message = "Debug commands not allowed"})
        return
    end
    
    -- Support targeting other players (admin-only for cross-player edits)
    local targetPlayer, targetErr = BurdJournals.Server.resolveDebugTargetPlayer(player, args and args.targetUsername)
    if not targetPlayer then
        BurdJournals.Server.sendToClient(player, "debugError", {message = targetErr or "Target player not found"})
        return
    end
    
    local level = args.level or 10
    local count = 0
    local xpObj = targetPlayer:getXp()
    
    BurdJournals.debugPrint("[BurdJournals] SERVER: Setting all skills to level " .. level .. " for " .. targetPlayer:getUsername())
    
    -- Remove ALL passive skill traits FIRST before setting any levels
    -- This prevents the trait system from bouncing Fitness/Strength back
    BurdJournals.Server.removePassiveSkillTraits(targetPlayer, "Strength")
    BurdJournals.Server.removePassiveSkillTraits(targetPlayer, "Fitness")
    
    -- For passive skills, use setPerkLevelDebug which directly sets level
    -- This bypasses XP scaling issues that affect Strength specifically
    local strengthPerk = Perks.Strength
    local fitnessPerk = Perks.Fitness
    
    if strengthPerk then
        targetPlayer:setPerkLevelDebug(strengthPerk, level)
        -- For level 0, also reset XP directly and remove traits again
        -- PZ auto-applies "Weak" trait which bounces Strength back up
        if level == 0 then
            BurdJournals.Server.applyXPWithFallback(targetPlayer, strengthPerk, -xpObj:getXP(strengthPerk), {
                skillName = "Strength",
                useMultipliers = false,
                isPassive = true,
            })
            BurdJournals.Server.removePassiveSkillTraits(targetPlayer, "Strength")
            targetPlayer:setPerkLevelDebug(strengthPerk, 0)
        end
        BurdJournals.debugPrint("[BurdJournals] DEBUG: Set Strength to level " .. level .. " via setPerkLevelDebug")
        count = count + 1
    end
    
    if fitnessPerk then
        targetPlayer:setPerkLevelDebug(fitnessPerk, level)
        -- Same treatment for Fitness just in case
        if level == 0 then
            BurdJournals.Server.applyXPWithFallback(targetPlayer, fitnessPerk, -xpObj:getXP(fitnessPerk), {
                skillName = "Fitness",
                useMultipliers = false,
                isPassive = true,
            })
            BurdJournals.Server.removePassiveSkillTraits(targetPlayer, "Fitness")
            targetPlayer:setPerkLevelDebug(fitnessPerk, 0)
        end
        BurdJournals.debugPrint("[BurdJournals] DEBUG: Set Fitness to level " .. level .. " via setPerkLevelDebug")
        count = count + 1
    end
    
    -- For all other skills, use XP-based approach
    for i = 0, Perks.getMaxIndex() - 1 do
        local perk = Perks.fromIndex(i)
        if perk and perk:getParent() ~= Perks.None then
            local perkName = tostring(perk)
            -- Skip passive skills - already handled above
            if perkName ~= "Fitness" and perkName ~= "Strength" then
                local targetXP = 0
                
                if level > 0 then
                    targetXP = (BurdJournals.getXPThresholdForLevel and BurdJournals.getXPThresholdForLevel(perkName, level))
                        or (perk.getTotalXpForLevel and perk:getTotalXpForLevel(level))
                        or 0
                end

                if BurdJournals.setSkillTotalXPCompat then
                    BurdJournals.setSkillTotalXPCompat(targetPlayer, perk, targetXP, perkName)
                end
                
                count = count + 1
            end
        end
    end
    
    BurdJournals.Server.syncDebugTraitTargetPlayer(targetPlayer)
    if BurdJournals.clearPlayerJournalSkillClaimLocks
        and BurdJournals.clearPlayerJournalSkillClaimLocks(targetPlayer)
        and targetPlayer.transmitModData then
        targetPlayer:transmitModData()
    end
    
    BurdJournals.debugPrint("[BurdJournals] DEBUG: Set all " .. count .. " skills to level " .. level .. " for " .. targetPlayer:getUsername())
    
    -- Send specific response so client can refresh UI after changes are applied
    local payload = {
        level = level,
        count = count,
        targetUsername = targetPlayer:getUsername()
    }
    BurdJournals.Server.sendToClient(player, "debugAllSkillsSet", payload)
    if targetPlayer ~= player then
        BurdJournals.Server.sendToClient(targetPlayer, "debugAllSkillsSet", payload)
    end
end

-- Handle debug: Add XP to skill
function BurdJournals.Server.handleDebugAddXP(player, args)
    if not BurdJournals.Server.isDebugAllowed(player) then
        BurdJournals.Server.sendToClient(player, "debugError", {message = "Debug commands not allowed"})
        return
    end
    
    local skillName = args.skillName
    local xp = args.xp or 1000
    
    local perk = BurdJournals.getPerkByName(skillName)
    if not perk then
        BurdJournals.Server.sendToClient(player, "debugError", {message = "Invalid skill: " .. tostring(skillName)})
        return
    end
    
    -- AddXP adds the specified amount directly for all skills
    local xpToAdd = xp
    
    -- Use sendAddXp for proper MP sync, fall back to direct AddXP
    -- NOTE: Don't call syncXp here - it disrupts batch command processing
    -- Client will request sync at end of batch via requestXpSync command
    BurdJournals.Server.applyXPWithFallback(player, perk, xpToAdd, {
        skillName = skillName,
        useMultipliers = false,
        isPassive = false,
    })
    BurdJournals.debugPrint("[BurdJournals] DEBUG: Added " .. xp .. " XP to " .. skillName .. " for " .. player:getUsername())
    
    BurdJournals.Server.sendToClient(player, "debugSuccess", {message = "Added " .. xp .. " XP to " .. skillName})
end

-- Handle debug: Add XP to skill (supports targeting other players)
function BurdJournals.Server.handleDebugAddSkillXP(player, args)
    if not BurdJournals.Server.isDebugAllowed(player) then
        BurdJournals.Server.sendToClient(player, "debugError", {message = "Debug commands not allowed"})
        return
    end
    
    local skillName = args.skillName
    local xpToAdd = args.xpToAdd or 100
    local targetUsername = args and args.targetUsername
    local targetPlayer, targetErr = BurdJournals.Server.resolveDebugTargetPlayer(player, targetUsername)
    if not targetPlayer then
        BurdJournals.Server.sendToClient(player, "debugError", {message = targetErr or "Target player not found"})
        return
    end
    
    local perk = BurdJournals.getPerkByName(skillName)
    if not perk then
        BurdJournals.Server.sendToClient(player, "debugError", {message = "Invalid skill: " .. tostring(skillName)})
        return
    end
    
    BurdJournals.Server.applyXPWithFallback(targetPlayer, perk, xpToAdd, {
        skillName = skillName,
        useMultipliers = false,
        isPassive = false,
    })
    
    BurdJournals.debugPrint("[BurdJournals] DEBUG: Added " .. xpToAdd .. " XP to " .. skillName .. " for " .. targetPlayer:getUsername())
    
    BurdJournals.Server.sendToClient(player, "debugSuccess", {message = "Added " .. xpToAdd .. " XP to " .. skillName})
end

-- Handle debug: Set skill to specific level (for player journal claims from debug-spawned journals)
-- Uses BSJ's verified XP thresholds plus the common exact-set helper.
function BurdJournals.Server.handleDebugSetSkillToLevel(player, args)
    if not BurdJournals.Server.isDebugAllowed(player) then
        BurdJournals.Server.sendToClient(player, "debugError", {message = "Debug commands not allowed"})
        return
    end
    
    local skillName = args.skillName
    local targetLevel = args.level or 0
    
    local perk = BurdJournals.getPerkByName(skillName)
    if not perk then
        BurdJournals.Server.sendToClient(player, "debugError", {message = "Invalid skill: " .. tostring(skillName)})
        return
    end
    
    local xpObj = player:getXp()
    local levelBefore = player:getPerkLevel(perk)
    local xpBefore = math.max(0, tonumber(BurdJournals.getPlayerSkillTotalXP and BurdJournals.getPlayerSkillTotalXP(player, perk, skillName) or xpObj:getXP(perk)) or 0)
    
    -- Calculate target XP needed for the target level
    -- getTotalXpForLevel(N) = XP threshold to BE AT level N
    -- This matches how the game engine and getSkillLevelFromXP determine level
    local targetXP = (BurdJournals.getXPThresholdForLevel and BurdJournals.getXPThresholdForLevel(skillName, targetLevel))
        or (perk.getTotalXpForLevel and perk:getTotalXpForLevel(targetLevel))
        or 0
    local debugLoggingEnabled = BurdJournals.shouldDebugLog and BurdJournals.shouldDebugLog() or false
    if debugLoggingEnabled then
        BurdJournals.debugPrint("[BurdJournals DEBUG CLAIM]   Target XP threshold: " .. tostring(targetXP))
        BurdJournals.debugPrint("================================================================================")
        BurdJournals.debugPrint("[BurdJournals DEBUG CLAIM] Skill: " .. tostring(skillName))
        BurdJournals.debugPrint("[BurdJournals DEBUG CLAIM]   TARGET LEVEL: " .. tostring(targetLevel))
        BurdJournals.debugPrint("[BurdJournals DEBUG CLAIM]   TARGET XP (for level " .. targetLevel .. "): " .. tostring(targetXP))
        BurdJournals.debugPrint("[BurdJournals DEBUG CLAIM]   PLAYER BEFORE: Level " .. tostring(levelBefore) .. ", XP " .. tostring(xpBefore))
        BurdJournals.debugPrint("================================================================================")
    end
    
    -- Only set if target level is higher than current
    if targetLevel > levelBefore and targetXP > xpBefore then
        local xpToAdd = targetXP - xpBefore
        if debugLoggingEnabled then
            BurdJournals.debugPrint("[BurdJournals DEBUG CLAIM]   XP to add: " .. tostring(xpToAdd))
        end

        local success, setVia = false, "none"
        if BurdJournals.setSkillTotalXPCompat then
            success, setVia = BurdJournals.setSkillTotalXPCompat(player, perk, targetXP, skillName)
        end

        local levelAfter = player:getPerkLevel(perk)
        local xpAfter = math.max(0, tonumber(BurdJournals.getPlayerSkillTotalXP and BurdJournals.getPlayerSkillTotalXP(player, perk, skillName) or xpObj:getXP(perk)) or 0)
        
        if debugLoggingEnabled then
            BurdJournals.debugPrint("================================================================================")
            BurdJournals.debugPrint("[BurdJournals DEBUG CLAIM RESULT] Skill: " .. tostring(skillName))
            BurdJournals.debugPrint("[BurdJournals DEBUG CLAIM RESULT]   TARGET: Level " .. tostring(targetLevel) .. ", XP " .. tostring(targetXP))
            BurdJournals.debugPrint("[BurdJournals DEBUG CLAIM RESULT]   XP ADDED: " .. tostring(xpToAdd))
            BurdJournals.debugPrint("[BurdJournals DEBUG CLAIM RESULT]   Set path: " .. tostring(setVia))
            BurdJournals.debugPrint("[BurdJournals DEBUG CLAIM RESULT]   PLAYER AFTER: Level " .. tostring(levelAfter) .. ", XP " .. tostring(xpAfter))
            if levelAfter < targetLevel then
                bsjWriteLogLine("[BurdJournals DEBUG CLAIM RESULT]   WARNING: Player level (" .. levelAfter .. ") is LESS than target (" .. targetLevel .. ")!")
                BurdJournals.debugPrint("[BurdJournals DEBUG CLAIM RESULT]   This may indicate a PZ XP scaling issue or passive skill behavior")
            elseif levelAfter == targetLevel then
                BurdJournals.debugPrint("[BurdJournals DEBUG CLAIM RESULT]   SUCCESS: Player reached target level " .. targetLevel)
            else
                BurdJournals.debugPrint("[BurdJournals DEBUG CLAIM RESULT]   NOTE: Player exceeded target level (" .. levelAfter .. " > " .. targetLevel .. ")")
            end
            BurdJournals.debugPrint("================================================================================")
        elseif levelAfter < targetLevel then
            bsjWriteLogLine("[BurdJournals DEBUG CLAIM RESULT]   WARNING: Player level (" .. levelAfter .. ") is LESS than target (" .. targetLevel .. ")!")
        end
        
        if success then
            BurdJournals.Server.sendToClient(player, "claimSuccess", {
                skillName = skillName,
                xpAdded = math.max(0, xpAfter - xpBefore),
                message = "Set " .. skillName .. " to level " .. targetLevel,
                debug_targetLevel = targetLevel,
                debug_targetXP = targetXP,
                debug_xpAdded = math.max(0, xpAfter - xpBefore),
                debug_levelAfter = levelAfter,
                debug_xpAfter = xpAfter,
            })
        else
            BurdJournals.Server.sendToClient(player, "debugError", {
                message = "Failed to set " .. skillName .. " to level " .. targetLevel .. " (path: " .. tostring(setVia) .. ")",
            })
        end
    else
        if debugLoggingEnabled then
            BurdJournals.debugPrint("[BurdJournals DEBUG CLAIM] Player already at or above target - levelBefore=" .. levelBefore .. ", xpBefore=" .. xpBefore .. ", targetLevel=" .. targetLevel .. ", targetXP=" .. targetXP)
        end
        BurdJournals.Server.sendToClient(player, "skillMaxed", {
            skillName = skillName,
            alreadyAtLevel = true,
            message = "Already at level " .. levelBefore .. " for " .. skillName
        })
    end
end

-- Handle debug: Set skill to specific XP value (for player journal claims from debug-spawned journals)
-- This uses the actual recorded XP from the journal, not calculated from level
-- This is the correct way to restore Player Journal skills - SET to exact XP, not ADD
-- IMPORTANT: Sets exact total XP through the common compatibility helper.
function BurdJournals.Server.handleDebugSetSkillXP(player, args)
    if not BurdJournals.Server.isDebugAllowed(player) then
        BurdJournals.Server.sendToClient(player, "debugError", {message = "Debug commands not allowed"})
        return
    end

    if not args or not args.skillName then
        BurdJournals.Server.sendToClient(player, "debugError", {message = "Invalid debugSetSkillXP payload"})
        return
    end

    local skillName = args.skillName
    local targetXP = args.targetXP or 0
    local targetLevel = args.targetLevel or 0  -- Target level from journal (for logging only)

    local perk = BurdJournals.getPerkByName(skillName)
    if not perk then
        BurdJournals.Server.sendToClient(player, "debugError", {message = "Invalid skill: " .. tostring(skillName)})
        return
    end

    local function normalizeToTable(value)
        if type(value) == "table" then
            return value
        end
        if BurdJournals.normalizeTable then
            local normalized = BurdJournals.normalizeTable(value)
            if type(normalized) == "table" then
                return normalized
            end
        end
        return nil
    end

    local function mergeMaxNumberMap(targetMap, sourceMap)
        local source = normalizeToTable(sourceMap)
        local target = normalizeToTable(targetMap)
        if type(target) ~= "table" then
            target = {}
        end
        if type(source) ~= "table" then
            return target, false
        end
        local changed = false
        for key, value in pairs(source) do
            if key ~= nil then
                local mapKey = tostring(key)
                local incoming = math.max(0, tonumber(value) or 0)
                local existing = math.max(0, tonumber(target[mapKey]) or 0)
                if incoming > existing then
                    target[mapKey] = incoming
                    changed = true
                end
            end
        end
        return target, changed
    end

    local resolvedJournal = nil
    local requestedJournalId = tonumber(args.journalId) or args.journalId
    if requestedJournalId then
        resolvedJournal = BurdJournals.findItemByIdInPlayerInventory(player, requestedJournalId)
    end
    if (not resolvedJournal) and args.journalUUID and BurdJournals.findJournalByUUIDInPlayerInventory then
        resolvedJournal = BurdJournals.findJournalByUUIDInPlayerInventory(player, args.journalUUID)
    end

    local responseJournalId = nil
    local responseJournalData = nil

    if resolvedJournal then
        local modData = resolvedJournal:getModData()
        modData.BurdJournals = modData.BurdJournals or {}
        local journalData = modData.BurdJournals

        if BurdJournals.restoreJournalDRStateIfMissing then
            BurdJournals.restoreJournalDRStateIfMissing(resolvedJournal, "debugSetSkillXP", player)
            journalData = modData.BurdJournals or journalData
        end

        local didMergeDR = false
        local sourceData = args.journalData
        if type(sourceData) == "table" and BurdJournals.normalizeJournalData then
            sourceData = BurdJournals.normalizeJournalData(sourceData) or sourceData
        end

        if type(sourceData) == "table" then
            local sourceReadCount = math.max(0, tonumber(sourceData.readCount) or 0)
            if sourceReadCount > math.max(0, tonumber(journalData.readCount) or 0) then
                journalData.readCount = sourceReadCount
                didMergeDR = true
            end

            local sourceSessionCount = math.max(0, tonumber(sourceData.readSessionCount) or 0)
            if sourceSessionCount > math.max(0, tonumber(journalData.readSessionCount) or 0) then
                journalData.readSessionCount = sourceSessionCount
                didMergeDR = true
            end

            local sourceSessionReads = math.max(0, tonumber(sourceData.currentSessionReadCount) or 0)
            if sourceSessionReads > math.max(0, tonumber(journalData.currentSessionReadCount) or 0) then
                journalData.currentSessionReadCount = sourceSessionReads
                didMergeDR = true
            end

            if sourceData.currentSessionId and sourceData.currentSessionId ~= journalData.currentSessionId then
                journalData.currentSessionId = sourceData.currentSessionId
                didMergeDR = true
            end

            local mergedSkillReadCounts, skillCountsChanged = mergeMaxNumberMap(journalData.skillReadCounts, sourceData.skillReadCounts)
            journalData.skillReadCounts = mergedSkillReadCounts
            if skillCountsChanged then
                didMergeDR = true
            end
            if sourceData.drLegacyMode3Migrated == true and journalData.drLegacyMode3Migrated ~= true then
                journalData.drLegacyMode3Migrated = true
                didMergeDR = true
            end
            local sourceMigrationSchemaVersion = tonumber(sourceData.migrationSchemaVersion) or 0
            local targetMigrationSchemaVersion = tonumber(journalData.migrationSchemaVersion) or 0
            if sourceMigrationSchemaVersion > targetMigrationSchemaVersion then
                journalData.migrationSchemaVersion = sourceMigrationSchemaVersion
                didMergeDR = true
            end

            local sourceClaims = normalizeToTable(sourceData.claims)
            if type(sourceClaims) == "table" then
                local targetClaims = normalizeToTable(journalData.claims)
                if type(targetClaims) ~= "table" then
                    targetClaims = {}
                end
                for characterId, sourceClaimData in pairs(sourceClaims) do
                    if characterId ~= nil then
                        local sourceClaimTable = normalizeToTable(sourceClaimData)
                        if type(sourceClaimTable) == "table" then
                            local targetClaimTable = normalizeToTable(targetClaims[characterId])
                            if type(targetClaimTable) ~= "table" then
                                targetClaimTable = {}
                            end
                            local mergedDrClaims, drClaimsChanged = mergeMaxNumberMap(targetClaimTable.drSkillReadCounts, sourceClaimTable.drSkillReadCounts)
                            targetClaimTable.drSkillReadCounts = mergedDrClaims
                            targetClaims[characterId] = targetClaimTable
                            if drClaimsChanged then
                                didMergeDR = true
                            end
                        end
                    end
                end
                journalData.claims = targetClaims
            end
        end

        -- Fallback for legacy clients that didn't send DR fields.
        if (not didMergeDR) and BurdJournals.consumeJournalClaimRead then
            BurdJournals.consumeJournalClaimRead(journalData, skillName, args.claimSessionId, player)
            didMergeDR = true
        end

        if didMergeDR and resolvedJournal.transmitModData then
            resolvedJournal:transmitModData()
        end
        if didMergeDR and BurdJournals.captureJournalDRState then
            BurdJournals.captureJournalDRState(resolvedJournal, "debugSetSkillXP", player)
        end

        responseJournalId = resolvedJournal:getID()
        responseJournalData = journalData
    end

    if type(responseJournalData) == "table" and BurdJournals.Server.deepCopy then
        responseJournalData = BurdJournals.Server.deepCopy(responseJournalData)
        if BurdJournals.applyRuntimeProjectionToJournalData then
            BurdJournals.applyRuntimeProjectionToJournalData(responseJournalData, player)
        end
    end

    local xpObj = player:getXp()
    local levelBefore = player:getPerkLevel(perk)
    local xpBefore = math.max(0, tonumber(BurdJournals.getPlayerSkillTotalXP and BurdJournals.getPlayerSkillTotalXP(player, perk, skillName) or xpObj:getXP(perk)) or 0)
    local debugLoggingEnabled = BurdJournals.shouldDebugLog and BurdJournals.shouldDebugLog() or false
    if debugLoggingEnabled then
        BurdJournals.debugPrint("================================================================================")
        BurdJournals.debugPrint("[BurdJournals DEBUG SET XP] Skill: " .. tostring(skillName))
        BurdJournals.debugPrint("[BurdJournals DEBUG SET XP]   TARGET LEVEL: " .. tostring(targetLevel) .. ", TARGET XP: " .. tostring(targetXP))
        BurdJournals.debugPrint("[BurdJournals DEBUG SET XP]   PLAYER BEFORE: Level " .. tostring(levelBefore) .. ", XP " .. tostring(xpBefore))
        BurdJournals.debugPrint("================================================================================")
    end

    -- Use XP-based comparison (more accurate than level comparison)
    -- Only apply XP if target XP is higher than current XP
    if targetXP > xpBefore then
        local xpToAdd = targetXP - xpBefore
        if debugLoggingEnabled then
            BurdJournals.debugPrint("[BurdJournals DEBUG SET XP]   XP to add: " .. tostring(xpToAdd))
        end

        local success, applyVia = false, "none"
        if BurdJournals.setSkillTotalXPCompat then
            success, applyVia = BurdJournals.setSkillTotalXPCompat(player, perk, targetXP, skillName)
        end
        if debugLoggingEnabled then
            BurdJournals.debugPrint("[BurdJournals DEBUG SET XP]   XP apply path: " .. tostring(applyVia))
        end
        
        local levelAfter = player:getPerkLevel(perk)
        local xpAfter = math.max(0, tonumber(BurdJournals.getPlayerSkillTotalXP and BurdJournals.getPlayerSkillTotalXP(player, perk, skillName) or xpObj:getXP(perk)) or 0)
        
        if debugLoggingEnabled then
            BurdJournals.debugPrint("================================================================================")
            BurdJournals.debugPrint("[BurdJournals DEBUG SET XP RESULT] Skill: " .. tostring(skillName))
            BurdJournals.debugPrint("[BurdJournals DEBUG SET XP RESULT]   TARGET: Level " .. tostring(targetLevel) .. ", XP " .. tostring(targetXP))
            BurdJournals.debugPrint("[BurdJournals DEBUG SET XP RESULT]   XP ADDED: " .. tostring(xpToAdd))
            BurdJournals.debugPrint("[BurdJournals DEBUG SET XP RESULT]   PLAYER AFTER: Level " .. tostring(levelAfter) .. ", XP " .. tostring(xpAfter))
            if levelAfter < targetLevel then
                bsjWriteLogLine("[BurdJournals DEBUG SET XP RESULT]   WARNING: Player level (" .. levelAfter .. ") is LESS than target (" .. targetLevel .. ")!")
                BurdJournals.debugPrint("[BurdJournals DEBUG SET XP RESULT]   This may indicate a PZ XP scaling issue or passive skill behavior")
            elseif levelAfter == targetLevel then
                BurdJournals.debugPrint("[BurdJournals DEBUG SET XP RESULT]   SUCCESS: Player reached target level " .. targetLevel)
            else
                BurdJournals.debugPrint("[BurdJournals DEBUG SET XP RESULT]   NOTE: Player exceeded target level (" .. levelAfter .. " > " .. targetLevel .. ")")
            end
            BurdJournals.debugPrint("================================================================================")
        elseif levelAfter < targetLevel then
            bsjWriteLogLine("[BurdJournals DEBUG SET XP RESULT]   WARNING: Player level (" .. levelAfter .. ") is LESS than target (" .. targetLevel .. ")!")
        end
        
        if success then
            BurdJournals.Server.sendToClient(player, "claimSuccess", {
                skillName = skillName,
                xpAdded = math.max(0, xpAfter - xpBefore),
                message = "Set " .. skillName .. " to level " .. levelAfter,
                journalId = responseJournalId,
                journalData = responseJournalData,
                debug_targetLevel = targetLevel,
                debug_targetXP = targetXP,
                debug_xpAdded = math.max(0, xpAfter - xpBefore),
                debug_levelAfter = levelAfter,
                debug_xpAfter = xpAfter,
            })
        else
            BurdJournals.Server.sendToClient(player, "debugError", {
                message = "Failed to set " .. skillName .. " to target XP " .. tostring(targetXP) .. " (path: " .. tostring(applyVia) .. ")",
            })
        end
    else
        if debugLoggingEnabled then
            BurdJournals.debugPrint("[BurdJournals DEBUG SET XP] Player already at or above target XP - xpBefore=" .. xpBefore .. ", targetXP=" .. targetXP)
        end
        BurdJournals.Server.sendToClient(player, "skillMaxed", {
            skillName = skillName,
            journalId = responseJournalId,
            journalData = responseJournalData,
            alreadyAtLevel = true,
            message = "Already at level " .. levelBefore .. " for " .. skillName .. " (target was level " .. targetLevel .. ")"
        })
    end
end

-- Handle debug: Add trait
function BurdJournals.Server.handleDebugAddTrait(player, args)
    if not BurdJournals.Server.isDebugAllowed(player) then
        BurdJournals.Server.sendToClient(player, "debugError", {message = "Debug commands not allowed"})
        return
    end

    local traitId = args.traitId
    if not traitId then
        BurdJournals.Server.sendToClient(player, "debugError", {message = "No trait specified"})
        return
    end

    local targetPlayer, targetErr = BurdJournals.Server.resolveDebugTargetPlayer(player, args and args.targetUsername)
    if not targetPlayer then
        BurdJournals.Server.sendToClient(player, "debugError", {message = targetErr or "Target player not found"})
        return
    end

    BurdJournals.debugPrint("[BurdJournals] DEBUG handleDebugAddTrait: traitId=" .. tostring(traitId) .. " for " .. targetPlayer:getUsername())

    local hadBefore = BurdJournals.playerHasTrait and BurdJournals.playerHasTrait(targetPlayer, traitId) == true
    local addOpts = {}
    if not (args and args.allowTraitReconciliation == true) then
        addOpts.skipTraitReconciliation = true
    end
    local traitWasAdded = BurdJournals.safeAddTrait and BurdJournals.safeAddTrait(targetPlayer, traitId, addOpts) or false
    local hasAfter = BurdJournals.playerHasTrait and BurdJournals.playerHasTrait(targetPlayer, traitId) == true
    if hasAfter then
        local cancelledTraits = {}
        if args and args.allowTraitReconciliation == true and BurdJournals.Server.resolveAndRemoveTraitConflicts then
            cancelledTraits = BurdJournals.Server.resolveAndRemoveTraitConflicts(targetPlayer, traitId) or {}
        end
        BurdJournals.Server.syncDebugTraitTargetPlayer(targetPlayer)
        BurdJournals.debugPrint("[BurdJournals] DEBUG: Trait add success=" .. tostring(not hadBefore) .. " (hadBefore=" .. tostring(hadBefore) .. ")")
        BurdJournals.Server.sendDebugTraitResponseToInterestedPlayers(player, targetPlayer, "debugTraitAdded", {
            traitId = traitId,
            targetUsername = targetPlayer:getUsername(),
            alreadyHad = hadBefore,
            cancelledTraits = cancelledTraits,
            addedTraits = { traitId },
            allowTraitReconciliation = args and args.allowTraitReconciliation == true,
        })
    else
        if not traitWasAdded then
            BurdJournals.Server.sendToClient(player, "debugError", {message = "Invalid or unsupported trait: " .. tostring(traitId)})
        else
            bsjWriteLogLine("[BurdJournals] DEBUG ERROR: Failed to add trait " .. traitId .. " - verification failed")
            BurdJournals.Server.sendToClient(player, "debugError", {message = "Failed to add trait: " .. traitId})
        end
    end
end

-- Handle debug: Remove trait (supports removeAll flag for duplicate traits)
function BurdJournals.Server.handleDebugRemoveTrait(player, args)
    if not BurdJournals.Server.isDebugAllowed(player) then
        BurdJournals.Server.sendToClient(player, "debugError", {message = "Debug commands not allowed"})
        return
    end

    local traitId = args.traitId
    local removeAll = args.removeAll or false
    if not traitId then
        BurdJournals.Server.sendToClient(player, "debugError", {message = "No trait specified"})
        return
    end

    local targetPlayer, targetErr = BurdJournals.Server.resolveDebugTargetPlayer(player, args and args.targetUsername)
    if not targetPlayer then
        BurdJournals.Server.sendToClient(player, "debugError", {message = targetErr or "Target player not found"})
        return
    end

    BurdJournals.debugPrint("[BurdJournals] DEBUG handleDebugRemoveTrait: traitId=" .. tostring(traitId) .. " removeAll=" .. tostring(removeAll) .. " for " .. targetPlayer:getUsername())

    local removeCount = 0
    local removedTraits = {}
    local hadTraitBefore = BurdJournals.playerHasTrait and BurdJournals.playerHasTrait(targetPlayer, traitId) == true
    if not hadTraitBefore then
        BurdJournals.Server.sendDebugTraitResponseToInterestedPlayers(player, targetPlayer, "debugTraitRemoved", {
            traitId = traitId,
            removeCount = 0,
            stillHasTrait = false,
            success = true,
            targetUsername = targetPlayer:getUsername(),
            message = "Player doesn't have trait: " .. traitId,
            removedTraits = { traitId },
        })
        return
    end

    local maxAttempts = removeAll and 64 or 1
    for attempt = 1, maxAttempts do
        local removed = removeTraitAuthoritatively(targetPlayer, traitId)
        if removed then
            removeCount = removeCount + 1
        else
            BurdJournals.debugPrint("[BurdJournals] DEBUG: No removal method worked on attempt #" .. tostring(attempt))
            break
        end

        if removeAll then
            local stillHas = BurdJournals.playerHasTrait and BurdJournals.playerHasTrait(targetPlayer, traitId) == true
            if not stillHas then
                break
            end
        else
            break
        end
    end

    local stillHasTrait = BurdJournals.playerHasTrait and BurdJournals.playerHasTrait(targetPlayer, traitId) == true
    local finalSuccess = not stillHasTrait
    BurdJournals.Server.syncDebugTraitTargetPlayer(targetPlayer)

    BurdJournals.Server.sendDebugTraitResponseToInterestedPlayers(player, targetPlayer, "debugTraitRemoved", {
        traitId = traitId,
        removeCount = removeCount,
        stillHasTrait = stillHasTrait,
        success = finalSuccess,
        targetUsername = targetPlayer:getUsername(),
        removedTraits = { traitId },
    })
end

function BurdJournals.Server.handleDebugAddRecipe(player, args)
    if not BurdJournals.Server.isDebugAllowed(player) then
        BurdJournals.Server.sendToClient(player, "debugError", {message = "Debug commands not allowed"})
        return
    end

    local recipeName = args and args.recipeName or nil
    if not recipeName then
        BurdJournals.Server.sendToClient(player, "debugError", {message = "No recipe specified"})
        return
    end

    local targetPlayer, targetErr = BurdJournals.Server.resolveDebugTargetPlayer(player, args and args.targetUsername)
    if not targetPlayer then
        BurdJournals.Server.sendToClient(player, "debugError", {message = targetErr or "Target player not found"})
        return
    end

    local validatedName = BurdJournals.validateRecipeName and BurdJournals.validateRecipeName(recipeName) or recipeName
    if not validatedName then
        BurdJournals.Server.sendToClient(player, "debugError", {message = "Unknown recipe: " .. tostring(recipeName)})
        return
    end

    local hadBefore = BurdJournals.playerKnowsRecipe and BurdJournals.playerKnowsRecipe(targetPlayer, validatedName) == true
    local learned = hadBefore or (BurdJournals.learnRecipeWithVerification and BurdJournals.learnRecipeWithVerification(targetPlayer, validatedName, "[BurdJournals DEBUG]")) or false
    BurdJournals.Server.syncRecipeKnowledgeForPlayer(targetPlayer)
    local hasAfter = BurdJournals.playerKnowsRecipe and BurdJournals.playerKnowsRecipe(targetPlayer, validatedName) == true

    if learned and hasAfter then
        local payload = {
            recipeName = validatedName,
            displayName = BurdJournals.getRecipeDisplayName and BurdJournals.getRecipeDisplayName(validatedName) or tostring(validatedName),
            alreadyHad = hadBefore,
            targetUsername = targetPlayer:getUsername(),
        }
        BurdJournals.Server.sendToClient(player, "debugRecipeAdded", payload)
        if targetPlayer ~= player then
            BurdJournals.Server.sendToClient(targetPlayer, "debugRecipeAdded", payload)
        end
        return
    end

    BurdJournals.Server.sendToClient(player, "debugError", {
        message = "Failed to learn recipe: " .. tostring(validatedName)
    })
end

function BurdJournals.Server.handleDebugRemoveRecipe(player, args)
    if not BurdJournals.Server.isDebugAllowed(player) then
        BurdJournals.Server.sendToClient(player, "debugError", {message = "Debug commands not allowed"})
        return
    end

    local recipeName = args and args.recipeName or nil
    if not recipeName then
        BurdJournals.Server.sendToClient(player, "debugError", {message = "No recipe specified"})
        return
    end

    local targetPlayer, targetErr = BurdJournals.Server.resolveDebugTargetPlayer(player, args and args.targetUsername)
    if not targetPlayer then
        BurdJournals.Server.sendToClient(player, "debugError", {message = targetErr or "Target player not found"})
        return
    end

    local validatedName = BurdJournals.validateRecipeName and BurdJournals.validateRecipeName(recipeName) or recipeName
    local removeTarget = validatedName or recipeName
    local hadBefore = BurdJournals.playerKnowsRecipe and BurdJournals.playerKnowsRecipe(targetPlayer, removeTarget) == true

    if not hadBefore then
        BurdJournals.Server.syncRecipeKnowledgeForPlayer(targetPlayer)
        local payload = {
            recipeName = removeTarget,
            displayName = BurdJournals.getRecipeDisplayName and BurdJournals.getRecipeDisplayName(removeTarget) or tostring(removeTarget),
            removed = false,
            stillKnown = false,
            targetUsername = targetPlayer:getUsername(),
            message = "Player doesn't know recipe: " .. tostring(removeTarget),
        }
        BurdJournals.Server.sendToClient(player, "debugRecipeRemoved", payload)
        if targetPlayer ~= player then
            BurdJournals.Server.sendToClient(targetPlayer, "debugRecipeRemoved", payload)
        end
        return
    end

    local removed = BurdJournals.forgetRecipeWithVerification and BurdJournals.forgetRecipeWithVerification(targetPlayer, removeTarget, "[BurdJournals DEBUG]") or false
    BurdJournals.Server.syncRecipeKnowledgeForPlayer(targetPlayer)
    local stillKnown = BurdJournals.playerKnowsRecipe and BurdJournals.playerKnowsRecipe(targetPlayer, removeTarget) == true

    if removed and not stillKnown then
        local payload = {
            recipeName = removeTarget,
            displayName = BurdJournals.getRecipeDisplayName and BurdJournals.getRecipeDisplayName(removeTarget) or tostring(removeTarget),
            removed = true,
            stillKnown = false,
            targetUsername = targetPlayer:getUsername(),
        }
        BurdJournals.Server.sendToClient(player, "debugRecipeRemoved", payload)
        if targetPlayer ~= player then
            BurdJournals.Server.sendToClient(targetPlayer, "debugRecipeRemoved", payload)
        end
        return
    end

    BurdJournals.Server.sendToClient(player, "debugError", {
        message = "Failed to remove recipe: " .. tostring(removeTarget)
    })
end

function BurdJournals.Server.handleDebugBulkRecipes(player, args)
    if not BurdJournals.Server.isDebugAllowed(player) then
        BurdJournals.Server.sendToClient(player, "debugError", {message = "Debug commands not allowed"})
        return
    end

    local action = args and tostring(args.action or "") or ""
    local isAdd = action == "add"
    local isRemove = action == "remove"
    if not isAdd and not isRemove then
        BurdJournals.Server.sendToClient(player, "debugError", {message = "Unknown bulk recipe action"})
        return
    end

    local targetPlayer, targetErr = BurdJournals.Server.resolveDebugTargetPlayer(player, args and args.targetUsername)
    if not targetPlayer then
        BurdJournals.Server.sendToClient(player, "debugError", {message = targetErr or "Target player not found"})
        return
    end

    local recipeNames = args and args.recipeNames or nil
    if type(recipeNames) ~= "table" or #recipeNames == 0 then
        BurdJournals.Server.sendToClient(player, "debugError", {message = "No recipes specified"})
        return
    end

    local appliedCount = 0
    local skippedCount = 0
    local failedCount = 0
    local appliedRecipes = {}
    local failedRecipes = {}
    local seen = {}
    local knownRecipes = BurdJournals.getAuthoritativeKnownRecipeSet and BurdJournals.getAuthoritativeKnownRecipeSet(targetPlayer) or {}

    for _, rawName in ipairs(recipeNames) do
        local validatedName = BurdJournals.validateRecipeName and BurdJournals.validateRecipeName(rawName) or rawName
        local recipeName = validatedName and tostring(validatedName) or nil
        local key = recipeName and string.lower(recipeName) or nil
        if not recipeName or recipeName == "" or seen[key] then
            skippedCount = skippedCount + 1
        else
            seen[key] = true
            local knownRecipeKey = BurdJournals.resolveRecipeKey and BurdJournals.resolveRecipeKey(knownRecipes, recipeName) or nil
            local knowsBefore = (knownRecipeKey ~= nil and knownRecipes[knownRecipeKey] == true)
                or (knownRecipes[recipeName] == true)
                or (BurdJournals.playerKnowsRecipe and BurdJournals.playerKnowsRecipe(targetPlayer, recipeName) == true)
            if isAdd then
                if knowsBefore then
                    skippedCount = skippedCount + 1
                else
                    local learned = BurdJournals.Server.learnRecipeInBatch(targetPlayer, recipeName, knownRecipes)
                    if learned then
                        appliedCount = appliedCount + 1
                        appliedRecipes[#appliedRecipes + 1] = recipeName
                    else
                        failedCount = failedCount + 1
                        failedRecipes[#failedRecipes + 1] = recipeName
                    end
                end
            else
                local removed = BurdJournals.forgetRecipeWithVerification and BurdJournals.forgetRecipeWithVerification(targetPlayer, recipeName, "[BurdJournals DEBUG Bulk]") or false
                if removed then
                    knownRecipes[recipeName] = nil
                    if knownRecipeKey ~= nil then
                        knownRecipes[knownRecipeKey] = nil
                    end
                    if knowsBefore then
                        appliedCount = appliedCount + 1
                        appliedRecipes[#appliedRecipes + 1] = recipeName
                    else
                        skippedCount = skippedCount + 1
                    end
                else
                    failedCount = failedCount + 1
                    failedRecipes[#failedRecipes + 1] = recipeName
                end
            end
        end
    end

    BurdJournals.Server.syncRecipeKnowledgeForPlayer(targetPlayer)

    local message = (isAdd and "Learned " or "Removed ")
        .. tostring(appliedCount)
        .. " recipe(s)"
    if skippedCount > 0 then
        message = message .. ", skipped " .. tostring(skippedCount)
    end
    if failedCount > 0 then
        message = message .. ", failed " .. tostring(failedCount)
    end

    local payload = {
        action = action,
        count = appliedCount,
        skippedCount = skippedCount,
        failedCount = failedCount,
        appliedRecipes = appliedRecipes,
        failedRecipes = failedRecipes,
        targetUsername = targetPlayer:getUsername(),
        message = message,
    }
    BurdJournals.Server.sendToClient(player, "debugBulkRecipesApplied", payload)
    if targetPlayer ~= player then
        BurdJournals.Server.sendToClient(targetPlayer, "debugBulkRecipesApplied", payload)
    end
end


-- Handle debug: Remove ALL traits from player
function BurdJournals.Server.handleDebugRemoveAllTraits(player, args)
    if not BurdJournals.Server.isDebugAllowed(player) then
        BurdJournals.Server.sendToClient(player, "debugError", {message = "Debug commands not allowed"})
        return
    end
    
    -- Support targeting other players (admin-only for cross-player edits)
    local targetPlayer, targetErr = BurdJournals.Server.resolveDebugTargetPlayer(player, args and args.targetUsername)
    if not targetPlayer then
        BurdJournals.Server.sendToClient(player, "debugError", {message = targetErr or "Target player not found"})
        return
    end
    
    BurdJournals.debugPrint("[BurdJournals] DEBUG handleDebugRemoveAllTraits for " .. targetPlayer:getUsername())
    
    local removeCount = 0
    local removedTraits = {}

    local traitsToRemove = {}

    for _, traitId in ipairs(collectCurrentTraitIds(targetPlayer, false)) do
        table.insert(traitsToRemove, { id = traitId })
        BurdJournals.debugPrint("[BurdJournals] DEBUG: Player has trait: " .. tostring(traitId))
    end

    BurdJournals.debugPrint("[BurdJournals] DEBUG: Found " .. #traitsToRemove .. " traits to remove")
    
    -- Remove each trait individually using centralized removal path.
    for _, traitData in ipairs(traitsToRemove) do
        local traitId = traitData.id
        local removed = removeTraitAuthoritatively(targetPlayer, traitId)
        if removed then
            removeCount = removeCount + 1
            removedTraits[#removedTraits + 1] = traitId
            BurdJournals.debugPrint("[BurdJournals] DEBUG: Successfully removed " .. traitId)
        else
            BurdJournals.debugPrint("[BurdJournals] DEBUG: Failed to remove " .. traitId)
        end
    end
    
    -- Fallback: try old API if Build 42 approach found no traits
    if #traitsToRemove == 0 then
        local oldTraits = nil
        if targetPlayer.getTraits then
            oldTraits = targetPlayer:getTraits()
        end
        if oldTraits and oldTraits.size then
            local size = oldTraits:size()
            if size > 0 then
                local removedTraitIds = {}
                for i = 0, size - 1 do
                    local traitObj = oldTraits.get and oldTraits:get(i) or nil
                    local traitId = nil
                    if traitObj and traitObj.getName then
                        traitId = traitObj:getName()
                    elseif traitObj ~= nil then
                        traitId = tostring(traitObj)
                    end
                    if traitId and traitId ~= "" then
                        removedTraitIds[#removedTraitIds + 1] = traitId
                    end
                end
                BurdJournals.debugPrint("[BurdJournals] DEBUG: Falling back to old API, clearing " .. size .. " traits")
                if oldTraits.clear then
                    oldTraits:clear()
                    removeCount = size
                    for _, removedTraitId in ipairs(removedTraitIds) do
                        removedTraits[#removedTraits + 1] = removedTraitId
                    end
                    if BurdJournals.applyTraitLifecycleSideEffects then
                        for _, removedTraitId in ipairs(removedTraitIds) do
                            pcall(function()
                                BurdJournals.applyTraitLifecycleSideEffects(targetPlayer, removedTraitId, "trait_removed", {
                                    source = "handleDebugRemoveAllTraits_oldApiClear",
                                    usedOldTraitsClear = true,
                                })
                            end)
                        end
                    end
                end
            end
        end
    end
    
    BurdJournals.Server.syncDebugTraitTargetPlayer(targetPlayer)
    
    BurdJournals.debugPrint("[BurdJournals] DEBUG: Removed " .. removeCount .. " traits from " .. targetPlayer:getUsername())
    
    -- Send specific response so client can refresh UI after changes are applied
    BurdJournals.Server.sendDebugTraitResponseToInterestedPlayers(player, targetPlayer, "debugAllTraitsRemoved", {
        count = removeCount,
        targetUsername = targetPlayer:getUsername(),
        removedTraits = removedTraits,
    })
end

function BurdJournals.Server.handleDebugBulkTraits(player, args)
    if not BurdJournals.Server.isDebugAllowed(player) then
        BurdJournals.Server.sendToClient(player, "debugError", {message = "Debug commands not allowed"})
        return
    end

    local action = args and args.action or nil
    local actionSpec = BurdJournals.Server.getDebugBulkTraitActionSpec(action)
    if not actionSpec then
        BurdJournals.Server.sendToClient(player, "debugError", {message = "Unknown bulk trait action"})
        return
    end

    local targetPlayer, targetErr = BurdJournals.Server.resolveDebugTargetPlayer(player, args and args.targetUsername)
    if not targetPlayer then
        BurdJournals.Server.sendToClient(player, "debugError", {message = targetErr or "Target player not found"})
        return
    end

    local appliedCount = 0
    local skippedCount = 0
    local failedCount = 0
    local addedTraits = {}
    local removedTraits = {}
    local cancelledTraits = {}
    local addOpts = { skipSyncXp = true }
    if not (args and args.allowTraitReconciliation == true) then
        addOpts.skipTraitReconciliation = true
    end
    local traitIds = actionSpec.isAdd
        and BurdJournals.Server.collectAvailableTraitIdsForDebugBulkAction(action)
        or BurdJournals.Server.collectOwnedTraitIdsForDebugBulkAction(targetPlayer, action)

    BurdJournals.debugPrint("[BurdJournals] DEBUG handleDebugBulkTraits: action=" .. tostring(action) .. " target=" .. targetPlayer:getUsername() .. " traits=" .. tostring(#traitIds))

    for _, traitId in ipairs(traitIds) do
        if actionSpec.isAdd then
            local hadBefore = BurdJournals.playerHasTrait and BurdJournals.playerHasTrait(targetPlayer, traitId) == true
            if hadBefore then
                skippedCount = skippedCount + 1
                addedTraits[#addedTraits + 1] = traitId
            else
                local okAdd, added = pcall(function()
                    return BurdJournals.safeAddTrait and BurdJournals.safeAddTrait(targetPlayer, traitId, addOpts) or false
                end)
                if okAdd then
                    local hasAfter = BurdJournals.playerHasTrait and BurdJournals.playerHasTrait(targetPlayer, traitId) == true
                    if added or hasAfter then
                        if hasAfter
                            and args
                            and args.allowTraitReconciliation == true
                            and BurdJournals.Server.resolveAndRemoveTraitConflicts then
                            local removedConflicts = BurdJournals.Server.resolveAndRemoveTraitConflicts(targetPlayer, traitId, { skipSyncXp = true }) or {}
                            for _, cancelledId in ipairs(removedConflicts) do
                                cancelledTraits[#cancelledTraits + 1] = cancelledId
                            end
                        end
                        addedTraits[#addedTraits + 1] = traitId
                        appliedCount = appliedCount + 1
                    else
                        failedCount = failedCount + 1
                    end
                else
                    failedCount = failedCount + 1
                    BurdJournals.writeLogLine("[BurdJournals] handleDebugBulkTraits: add failed for '" .. tostring(traitId) .. "' during '" .. tostring(action) .. "': " .. tostring(added))
                end
            end
        else
            local failedSeen = {}
            for _, pendingTraitId in ipairs(traitIds) do
                local removedPasses, cleared, removeErr = removeDebugTraitCompletelyAuthoritatively(targetPlayer, pendingTraitId)
                if removedPasses > 0 and cleared then
                    appliedCount = appliedCount + removedPasses
                    for _ = 1, removedPasses do
                        removedTraits[#removedTraits + 1] = pendingTraitId
                    end
                else
                    local failKey = string.lower(tostring(pendingTraitId or ""))
                    if failKey ~= "" and not failedSeen[failKey] then
                        failedSeen[failKey] = true
                        failedCount = failedCount + 1
                        BurdJournals.writeLogLine("[BurdJournals] handleDebugBulkTraits: remove failed for '" .. tostring(pendingTraitId) .. "' during '" .. tostring(action) .. "': " .. tostring(removeErr))
                    end
                end
            end
            break
        end
    end

    BurdJournals.Server.syncDebugTraitTargetPlayer(targetPlayer)

    BurdJournals.Server.sendDebugTraitResponseToInterestedPlayers(player, targetPlayer, "debugBulkTraitsApplied", {
        action = tostring(action),
        count = appliedCount,
        skippedCount = skippedCount,
        failedCount = failedCount,
        targetUsername = targetPlayer:getUsername(),
        message = BurdJournals.Server.formatDebugBulkTraitActionMessage(action, appliedCount, skippedCount, failedCount),
        addedTraits = addedTraits,
        removedTraits = removedTraits,
        cancelledTraits = cancelledTraits,
        allowTraitReconciliation = args and args.allowTraitReconciliation == true,
    })
end

-- Handle debug: Clear baseline
function BurdJournals.Server.handleDebugClearBaseline(player, args)
    if not BurdJournals.Server.isDebugAllowed(player) then
        BurdJournals.Server.sendToClient(player, "debugError", {message = "Debug commands not allowed"})
        return
    end
    
    -- Support targeting other players (admin-only for cross-player edits)
    local targetPlayer, targetErr = BurdJournals.Server.resolveDebugTargetPlayer(player, args and args.targetUsername)
    if not targetPlayer then
        BurdJournals.Server.sendToClient(player, "debugError", {message = targetErr or "Target player not found"})
        return
    end
    
    local category = args.category or "all"  -- all, skills, traits, recipes
    local targetCharacterId = BurdJournals.getPlayerCharacterId(targetPlayer)
    local targetSteamId = BurdJournals.getPlayerSteamId and BurdJournals.getPlayerSteamId(targetPlayer) or nil
    local shouldSnapshotMutation = category ~= "all"
    if shouldSnapshotMutation and BurdJournals.Server.captureBaselineSnapshotForPlayer and targetCharacterId then
        local beforeClear = BurdJournals.Server.getCachedBaseline(targetCharacterId, targetPlayer)
        if beforeClear then
            BurdJournals.Server.captureBaselineSnapshotForPlayer(
                targetPlayer,
                targetCharacterId,
                beforeClear,
                (category == "all") and "clear" or "debug_edit",
                "pre_clear_" .. tostring(category),
                { skipTransmit = true }
            )
        end
    end

    local modData = targetPlayer:getModData()
    modData.BurdJournals = modData.BurdJournals or {}

    local purgedSnapshots = 0
    local purgedHistory = 0

    if category == "all" then
        -- Full clear removes baseline payload + fallback recovery layers.
        modData.BurdJournals.skillBaseline = nil
        modData.BurdJournals.mediaSkillBaseline = nil
        modData.BurdJournals.traitBaseline = nil
        modData.BurdJournals.recipeBaseline = nil
        modData.BurdJournals.baselineCaptured = false
        modData.BurdJournals.debugModified = false

        if targetCharacterId then
            local cache = BurdJournals.Server.getBaselineCache()
            cache.players = cache.players or {}
            cache.players[targetCharacterId] = nil
            BurdJournals.Server.removeArchivedBaseline(targetCharacterId, true)
            BurdJournals.Server.transmitBaselineStores(true)
        end

        if BurdJournals.Server.writePlayerBaselineBackup and targetCharacterId then
            BurdJournals.Server.clearPlayerBaselineBackup(targetPlayer, true)
        end
        if BurdJournals.Server.clearPlayerBaselineSnapshotHistory then
            purgedHistory = BurdJournals.Server.clearPlayerBaselineSnapshotHistory(targetPlayer, true)
        end
        if BurdJournals.Server.purgeBaselineSnapshotsForIdentity then
            purgedSnapshots = BurdJournals.Server.purgeBaselineSnapshotsForIdentity(
                targetSteamId,
                targetCharacterId,
                true
            )
        end
        if BurdJournals.Server.transmitBaselineSnapshotStore then
            BurdJournals.Server.transmitBaselineSnapshotStore()
        end
    elseif category == "skills" then
        modData.BurdJournals.skillBaseline = {}
        modData.BurdJournals.mediaSkillBaseline = {}
        modData.BurdJournals.debugModified = true
    elseif category == "traits" then
        modData.BurdJournals.traitBaseline = {}
        modData.BurdJournals.debugModified = true
    elseif category == "recipes" then
        modData.BurdJournals.recipeBaseline = {}
        modData.BurdJournals.debugModified = true
    end

    if targetCharacterId and category ~= "all" then
        local cache = BurdJournals.Server.getBaselineCache()
        cache.players = cache.players or {}
        cache.players[targetCharacterId] = cache.players[targetCharacterId] or {
            skillBaseline = {},
            mediaSkillBaseline = {},
            traitBaseline = {},
            recipeBaseline = {},
            capturedAt = getGameTime and getGameTime():getWorldAgeHours() or 0,
            steamId = BurdJournals.getPlayerSteamId(targetPlayer),
            characterName = BurdJournals.Server.getPlayerCharacterDisplayName(targetPlayer),
            debugModified = true,
        }
        local cached = cache.players[targetCharacterId]
        cached.skillBaseline = BurdJournals.Server.copyBaselineTableEntries(modData.BurdJournals.skillBaseline)
        cached.skillExportBaseline = BurdJournals.Server.copyBaselineTableEntries(modData.BurdJournals.skillExportBaseline)
        cached.mediaSkillBaseline = BurdJournals.Server.copyBaselineTableEntries(modData.BurdJournals.mediaSkillBaseline)
        cached.mediaSkillExportBaseline = BurdJournals.Server.copyBaselineTableEntries(modData.BurdJournals.mediaSkillExportBaseline)
        cached.traitBaseline = BurdJournals.Server.copyBaselineTableEntries(modData.BurdJournals.traitBaseline)
        cached.traitExportBaseline = BurdJournals.Server.copyBaselineTableEntries(modData.BurdJournals.traitExportBaseline)
        cached.recipeBaseline = BurdJournals.Server.copyBaselineTableEntries(modData.BurdJournals.recipeBaseline)
        cached.recipeExportBaseline = BurdJournals.Server.copyBaselineTableEntries(modData.BurdJournals.recipeExportBaseline)
        cached.debugModified = true
        cached.steamId = cached.steamId or BurdJournals.getPlayerSteamId(targetPlayer)
        cached.characterName = cached.characterName or BurdJournals.Server.getPlayerCharacterDisplayName(targetPlayer)
        BurdJournals.Server.storeBaselineArchiveRecord(targetCharacterId, cached, true)
        BurdJournals.Server.transmitBaselineStores(true)
    end

    if BurdJournals.Server.writePlayerBaselineBackup and targetCharacterId and category ~= "all" then
        BurdJournals.Server.writePlayerBaselineBackup(targetPlayer, targetCharacterId, {
            skillBaseline = modData.BurdJournals.skillBaseline or {},
            mediaSkillBaseline = modData.BurdJournals.mediaSkillBaseline or {},
            traitBaseline = modData.BurdJournals.traitBaseline or {},
            recipeBaseline = modData.BurdJournals.recipeBaseline or {},
            debugModified = true,
            steamId = BurdJournals.getPlayerSteamId(targetPlayer),
            characterName = BurdJournals.Server.getPlayerCharacterDisplayName(targetPlayer),
            capturedAt = getGameTime and getGameTime():getWorldAgeHours() or 0
        }, true)
    end

    if shouldSnapshotMutation and BurdJournals.Server.captureBaselineSnapshotForPlayer and targetCharacterId then
        local afterClear = BurdJournals.Server.getCachedBaseline(targetCharacterId, targetPlayer)
        BurdJournals.Server.captureBaselineSnapshotForPlayer(
            targetPlayer,
            targetCharacterId,
            afterClear,
            (category == "all") and "clear" or "debug_edit",
            "post_clear_" .. tostring(category)
        )
    end
    
    -- Transmit changes
    if targetPlayer.transmitModData then
        targetPlayer:transmitModData()
    end

    local message = "Cleared " .. category .. " baseline for " .. targetPlayer:getUsername()
    if category == "all" then
        message = message
            .. " (purged " .. tostring(purgedSnapshots) .. " snapshot(s), "
            .. tostring(purgedHistory) .. " player-history entr" .. ((purgedHistory == 1) and "y" or "ies") .. ")"
    end
    BurdJournals.debugPrint("[BurdJournals] DEBUG: " .. message)
    BurdJournals.Server.sendToClient(player, "debugSuccess", {message = message})
end

-- Handle debug: Recalculate baseline
function BurdJournals.Server.handleDebugRecalcBaseline(player, args)
    if not BurdJournals.Server.isDebugAllowed(player) then
        BurdJournals.Server.sendToClient(player, "debugError", {message = "Debug commands not allowed"})
        return
    end

    local targetPlayer, targetErr = BurdJournals.Server.resolveDebugTargetPlayer(player, args and args.targetUsername)
    if not targetPlayer then
        BurdJournals.Server.sendToClient(player, "debugError", {message = targetErr or "Target player not found"})
        return
    end

    local characterId = BurdJournals.getPlayerCharacterId(targetPlayer)
    if not characterId then
        BurdJournals.Server.sendToClient(player, "debugError", {message = "Could not get character ID"})
        return
    end

    -- Rebuild baseline from authoritative server state
    local baselineData = BurdJournals.Server.buildBaselineForPlayer(targetPlayer)
    baselineData.steamId = BurdJournals.getPlayerSteamId(targetPlayer)
    local descriptor = targetPlayer.getDescriptor and targetPlayer:getDescriptor() or nil
    baselineData.characterName = BurdJournals.getPlayerCharacterName and BurdJournals.getPlayerCharacterName(targetPlayer)
        or (descriptor and (descriptor:getForename() .. " " .. descriptor:getSurname()) or nil)
    baselineData.debugModified = false

    BurdJournals.Server.storeCachedBaseline(characterId, baselineData, true)
    if BurdJournals.Server.captureBaselineSnapshotForPlayer then
        BurdJournals.Server.captureBaselineSnapshotForPlayer(
            targetPlayer,
            characterId,
            BurdJournals.Server.getCachedBaseline(characterId, targetPlayer) or baselineData,
            "recalc"
        )
    end

    -- Update player's modData baseline tables
    local modData = targetPlayer:getModData()
    modData.BurdJournals = modData.BurdJournals or {}
    modData.BurdJournals.skillBaseline = baselineData.skillBaseline or {}
    modData.BurdJournals.mediaSkillBaseline = baselineData.mediaSkillBaseline or {}
    modData.BurdJournals.traitBaseline = baselineData.traitBaseline or {}
    modData.BurdJournals.recipeBaseline = baselineData.recipeBaseline or {}
    modData.BurdJournals.debugModified = false
    modData.BurdJournals.baselineCaptured = true
    modData.BurdJournals_Baseline = nil
    if BurdJournals.Server.writePlayerBaselineBackup then
        BurdJournals.Server.writePlayerBaselineBackup(targetPlayer, characterId, baselineData, true)
    end

    if targetPlayer.transmitModData then
        targetPlayer:transmitModData()
    end

    local msg = "Baseline recalculated for " .. targetPlayer:getUsername()
    BurdJournals.Server.sendToClient(player, "recalculateBaseline", {
        message = msg,
        targetUsername = targetPlayer:getUsername(),
        characterId = characterId,
        skillBaseline = baselineData.skillBaseline or {},
        mediaSkillBaseline = baselineData.mediaSkillBaseline or {},
        traitBaseline = baselineData.traitBaseline or {},
        recipeBaseline = baselineData.recipeBaseline or {},
        debugModified = false,
        baselineCaptured = true,
    })
end

-- Handle debug: Update skill baseline (syncs to server cache for MP persistence)
function BurdJournals.Server.handleDebugUpdateSkillBaseline(player, args)
    if not BurdJournals.Server.isDebugAllowed(player) then
        BurdJournals.Server.sendToClient(player, "debugError", {message = "Debug commands not allowed"})
        return
    end
    
    local skillName = args.skillName
    local baselineXP = args.baselineXP
    local targetUsername = args.targetUsername
    
    if not skillName then
        BurdJournals.Server.sendToClient(player, "debugError", {message = "Missing skill name"})
        return
    end
    
    local targetPlayer, targetErr = BurdJournals.Server.resolveDebugTargetPlayer(player, targetUsername)
    if not targetPlayer then
        BurdJournals.Server.sendToClient(player, "debugError", {message = targetErr or "Target player not found"})
        return
    end
    
    -- Update player's modData
    local modData = targetPlayer:getModData()
    modData.BurdJournals = modData.BurdJournals or {}
    modData.BurdJournals.skillBaseline = modData.BurdJournals.skillBaseline or {}
    modData.BurdJournals.skillBaseline[skillName] = baselineXP
    
    -- Update server cache for persistence across logout/rejoin
    local characterId = BurdJournals.getPlayerCharacterId(targetPlayer)
    if characterId then
        local cache = BurdJournals.Server.getBaselineCache()
        cache.players = cache.players or {}
        if not cache.players[characterId] then
            cache.players[characterId] = {
                skillBaseline = {},
                mediaSkillBaseline = {},
                traitBaseline = {},
                recipeBaseline = {},
                capturedAt = getGameTime():getWorldAgeHours(),
                steamId = BurdJournals.getPlayerSteamId(targetPlayer),
                characterName = targetPlayer:getDescriptor():getForename() .. " " .. targetPlayer:getDescriptor():getSurname()
            }
        end
        cache.players[characterId].skillBaseline[skillName] = baselineXP
        cache.players[characterId].debugModified = true  -- Mark as debug-modified
        BurdJournals.Server.storeBaselineArchiveRecord(characterId, cache.players[characterId], true)
        
        -- Also update the player's local modData (this is the key backup that survives mod updates!)
        local playerModData = targetPlayer:getModData()
        if not playerModData.BurdJournals then
            playerModData.BurdJournals = {}
        end
        playerModData.BurdJournals.debugModified = true
        playerModData.BurdJournals.baselineCaptured = true  -- Ensure this flag is set
        playerModData.BurdJournals.skillBaseline = playerModData.BurdJournals.skillBaseline or {}
        playerModData.BurdJournals.skillBaseline[skillName] = baselineXP

        if BurdJournals.Server.writePlayerBaselineBackup then
            BurdJournals.Server.writePlayerBaselineBackup(targetPlayer, characterId, cache.players[characterId], true)
        end
        
        -- Persist server cache to global ModData
        BurdJournals.Server.transmitBaselineStores(true)
        if BurdJournals.Server.captureBaselineSnapshotForPlayer then
            BurdJournals.Server.captureBaselineSnapshotForPlayer(
                targetPlayer,
                characterId,
                cache.players[characterId],
                "debug_edit",
                "skill:" .. tostring(skillName)
            )
        end
        
        -- CRITICAL: Also transmit player's own ModData to ensure it's saved with their character
        -- This is the fallback that allows recovery after mod updates!
        if targetPlayer.transmitModData then
            targetPlayer:transmitModData()
        end
        
        BurdJournals.debugPrint("[BurdJournals] DEBUG: Updated skill baseline for " .. targetPlayer:getUsername() .. ": " .. skillName .. " = " .. tostring(baselineXP))
        
        -- Send specific response so client can refresh the Baseline tab
        BurdJournals.Server.sendToClient(player, "debugBaselineSkillSet", {
            skillName = skillName,
            baselineXP = baselineXP,
            targetUsername = targetPlayer:getUsername()
        })
    else
        BurdJournals.Server.sendToClient(player, "debugError", {message = "Could not get character ID"})
    end
end

-- Handle debug: Update trait baseline (syncs to server cache for MP persistence)
function BurdJournals.Server.handleDebugUpdateTraitBaseline(player, args)
    if not BurdJournals.Server.isDebugAllowed(player) then
        BurdJournals.Server.sendToClient(player, "debugError", {message = "Debug commands not allowed"})
        return
    end
    
    local traitId = args.traitId
    local isBaseline = args.isBaseline
    local targetUsername = args.targetUsername
    
    if not traitId then
        BurdJournals.Server.sendToClient(player, "debugError", {message = "Missing trait ID"})
        return
    end
    
    local targetPlayer, targetErr = BurdJournals.Server.resolveDebugTargetPlayer(player, targetUsername)
    if not targetPlayer then
        BurdJournals.Server.sendToClient(player, "debugError", {message = targetErr or "Target player not found"})
        return
    end
    
    -- Update player's modData using the shared function (handles aliases)
    if BurdJournals.setTraitBaseline then
        BurdJournals.setTraitBaseline(targetPlayer, traitId, isBaseline)
    end
    
    -- Update server cache for persistence across logout/rejoin
    local characterId = BurdJournals.getPlayerCharacterId(targetPlayer)
    if characterId then
        local cache = BurdJournals.Server.getBaselineCache()
        cache.players = cache.players or {}
        if not cache.players[characterId] then
            cache.players[characterId] = {
                skillBaseline = {},
                mediaSkillBaseline = {},
                traitBaseline = {},
                recipeBaseline = {},
                capturedAt = getGameTime():getWorldAgeHours(),
                steamId = BurdJournals.getPlayerSteamId(targetPlayer),
                characterName = targetPlayer:getDescriptor():getForename() .. " " .. targetPlayer:getDescriptor():getSurname()
            }
        end
        
        -- Get all aliases and store them
        local aliases = BurdJournals.getTraitAliases and BurdJournals.getTraitAliases(traitId) or {traitId, string.lower(traitId)}
        for _, alias in ipairs(aliases) do
            if isBaseline then
                cache.players[characterId].traitBaseline[alias] = true
            else
                cache.players[characterId].traitBaseline[alias] = nil
            end
        end
        cache.players[characterId].debugModified = true  -- Mark as debug-modified
        BurdJournals.Server.storeBaselineArchiveRecord(characterId, cache.players[characterId], true)
        
        -- Also update the player's local modData (this is the key backup that survives mod updates!)
        local playerModData = targetPlayer:getModData()
        if not playerModData.BurdJournals then
            playerModData.BurdJournals = {}
        end
        playerModData.BurdJournals.debugModified = true
        playerModData.BurdJournals.baselineCaptured = true  -- Ensure this flag is set
        -- Also sync trait baseline to player ModData
        playerModData.BurdJournals.traitBaseline = playerModData.BurdJournals.traitBaseline or {}
        for _, alias in ipairs(aliases) do
            if isBaseline then
                playerModData.BurdJournals.traitBaseline[alias] = true
            else
                playerModData.BurdJournals.traitBaseline[alias] = nil
            end
        end

        if BurdJournals.Server.writePlayerBaselineBackup then
            BurdJournals.Server.writePlayerBaselineBackup(targetPlayer, characterId, cache.players[characterId], true)
        end
        
        -- Persist server cache to global ModData
        BurdJournals.Server.transmitBaselineStores(true)
        if BurdJournals.Server.captureBaselineSnapshotForPlayer then
            BurdJournals.Server.captureBaselineSnapshotForPlayer(
                targetPlayer,
                characterId,
                cache.players[characterId],
                "debug_edit",
                "trait:" .. tostring(traitId)
            )
        end
        
        -- CRITICAL: Also transmit player's own ModData to ensure it's saved with their character
        -- This is the fallback that allows recovery after mod updates!
        if targetPlayer.transmitModData then
            targetPlayer:transmitModData()
        end
        
        local status = isBaseline and "added to" or "removed from"
        BurdJournals.debugPrint("[BurdJournals] DEBUG: Trait " .. traitId .. " " .. status .. " baseline for " .. targetPlayer:getUsername())
        
        -- Send specific response so client can refresh the Baseline tab
        BurdJournals.Server.sendToClient(player, "debugBaselineTraitSet", {
            traitId = traitId,
            isBaseline = isBaseline,
            targetUsername = targetPlayer:getUsername()
        })
    else
        BurdJournals.Server.sendToClient(player, "debugError", {message = "Could not get character ID"})
    end
end

-- Forward declaration used by debug baseline draft/save handlers.
-- Handle debug: Save staged baseline draft in one server-authoritative transaction.
-- This intentionally snapshots once per explicit save (not once per click).
function BurdJournals.Server.handleDebugSaveBaselineDraft(player, args)
    if not BurdJournals.Server.isDebugAllowed(player) then
        BurdJournals.Server.sendToClient(player, "debugError", {message = "Debug commands not allowed"})
        return
    end

    args = type(args) == "table" and args or {}
    local targetPlayer, targetErr = BurdJournals.Server.resolveDebugTargetPlayer(player, args.targetUsername)
    if not targetPlayer then
        BurdJournals.Server.sendToClient(player, "debugError", {message = targetErr or "Target player not found"})
        return
    end

    local characterId = BurdJournals.getPlayerCharacterId and BurdJournals.getPlayerCharacterId(targetPlayer) or nil
    if not characterId then
        BurdJournals.Server.sendToClient(player, "debugError", {message = "Could not get character ID"})
        return
    end

    local incomingSkillBaseline = type(args.skillBaseline) == "table" and args.skillBaseline or {}
    local incomingTraitBaseline = type(args.traitBaseline) == "table" and args.traitBaseline or {}
    local incomingRecipeBaseline = type(args.recipeBaseline) == "table" and args.recipeBaseline or {}

    local payload = BurdJournals.Server.buildBaselinePayloadFromTarget(targetPlayer, characterId)
    if type(payload) ~= "table" then
        payload = {
            skillBaseline = {},
            mediaSkillBaseline = {},
            traitBaseline = {},
            recipeBaseline = {},
        }
    else
        payload = BurdJournals.sanitizeBaselinePayloadForSnapshot
            and BurdJournals.sanitizeBaselinePayloadForSnapshot(payload)
            or {
                skillBaseline = BurdJournals.Server.copyBaselineTableEntries(payload.skillBaseline),
                mediaSkillBaseline = BurdJournals.Server.copyBaselineTableEntries(payload.mediaSkillBaseline),
                traitBaseline = BurdJournals.Server.copyBaselineTableEntries(payload.traitBaseline),
                recipeBaseline = BurdJournals.Server.copyBaselineTableEntries(payload.recipeBaseline),
            }
    end

    payload.skillBaseline = {}
    for skillName, rawXP in pairs(incomingSkillBaseline) do
        local key = tostring(skillName or "")
        if key ~= "" then
            local xp = math.floor(math.max(0, tonumber(rawXP) or 0))
            payload.skillBaseline[key] = xp
        end
    end

    payload.traitBaseline = {}
    for traitId, isBaseline in pairs(incomingTraitBaseline) do
        if isBaseline == true then
            local key = tostring(traitId or "")
            if key ~= "" then
                payload.traitBaseline[key] = true
            end
        end
    end

    payload.recipeBaseline = {}
    for recipeName, isBaseline in pairs(incomingRecipeBaseline) do
        if isBaseline == true then
            local key = tostring(recipeName or "")
            if key ~= "" then
                payload.recipeBaseline[key] = true
            end
        end
    end

    -- Baseline draft saves are treated as canonical active baseline edits.
    payload.debugModified = false
    payload.steamId = BurdJournals.getPlayerSteamId and BurdJournals.getPlayerSteamId(targetPlayer) or payload.steamId
    payload.characterName = BurdJournals.Server.getPlayerCharacterDisplayName(targetPlayer) or payload.characterName
    payload.capturedAt = getGameTime() and getGameTime():getWorldAgeHours() or BurdJournals.Server.baselineSnapshotNowHours()

    local stored = BurdJournals.Server.storeCachedBaseline(characterId, payload, true)
    if not stored then
        BurdJournals.Server.sendToClient(player, "debugError", {message = "Failed to save baseline draft"})
        return
    end

    local cached = BurdJournals.Server.getCachedBaseline(characterId, targetPlayer) or payload
    cached.debugModified = false
    BurdJournals.Server.storeBaselineArchiveRecord(characterId, cached, true)

    if BurdJournals.Server.writePlayerBaselineBackup then
        BurdJournals.Server.writePlayerBaselineBackup(targetPlayer, characterId, cached, true)
    end

    BurdJournals.Server.applyBaselinePayloadToPlayerModData(targetPlayer, cached, false)
    BurdJournals.Server.transmitBaselineStores(true)
    if targetPlayer.transmitModData then
        targetPlayer:transmitModData()
    end

    if BurdJournals.Server.captureBaselineSnapshotForPlayer then
        BurdJournals.Server.captureBaselineSnapshotForPlayer(
            targetPlayer,
            characterId,
            cached,
            "debug_edit",
            "baseline_draft_save"
        )
    end

    BurdJournals.Server.sendToClient(player, "debugBaselineDraftSaved", {
        characterId = characterId,
        targetUsername = targetPlayer:getUsername(),
        skillBaseline = BurdJournals.Server.copyBaselineTableEntries(cached.skillBaseline),
        skillExportBaseline = BurdJournals.Server.copyBaselineTableEntries(cached.skillExportBaseline),
        mediaSkillBaseline = BurdJournals.Server.copyBaselineTableEntries(cached.mediaSkillBaseline),
        mediaSkillExportBaseline = BurdJournals.Server.copyBaselineTableEntries(cached.mediaSkillExportBaseline),
        traitBaseline = BurdJournals.Server.copyBaselineTableEntries(cached.traitBaseline),
        traitExportBaseline = BurdJournals.Server.copyBaselineTableEntries(cached.traitExportBaseline),
        recipeBaseline = BurdJournals.Server.copyBaselineTableEntries(cached.recipeBaseline),
        recipeExportBaseline = BurdJournals.Server.copyBaselineTableEntries(cached.recipeExportBaseline),
        baselineVersion = tonumber(cached.baselineVersion) or tonumber(BurdJournals.BASELINE_VERSION) or 5,
        debugModified = cached.debugModified == true,
        counts = BurdJournals.getBaselineSnapshotCounts and BurdJournals.getBaselineSnapshotCounts(cached) or {
            skills = BurdJournals.countTable(cached.skillBaseline),
            mediaSkills = BurdJournals.countTable(cached.mediaSkillBaseline),
            traits = BurdJournals.countTable(cached.traitBaseline),
            recipes = BurdJournals.countTable(cached.recipeBaseline),
        },
    })
end

function BurdJournals.Server.buildBaselinePayloadFromTarget(targetPlayer, characterId)
    if not targetPlayer then
        return nil
    end
    local function hasUsableBaselinePayload(payload)
        if type(payload) ~= "table" then
            return false
        end
        if payload.baselineCaptured == true or payload.baselineBypassed == true or payload.baselineVersion ~= nil then
            return true
        end
        return BurdJournals.Server.snapshotHasEntries(payload) == true
    end
    local resolvedCharacterId = characterId
        or (BurdJournals.getPlayerCharacterId and BurdJournals.getPlayerCharacterId(targetPlayer))
    local baseline = resolvedCharacterId and BurdJournals.Server.getCachedBaseline(resolvedCharacterId, targetPlayer) or nil
    if hasUsableBaselinePayload(baseline) then
        return baseline
    end

    local backup = BurdJournals.Server.readPlayerBaselineBackup
        and BurdJournals.Server.readPlayerBaselineBackup(targetPlayer, resolvedCharacterId)
        or nil
    if hasUsableBaselinePayload(backup) then
        return backup
    end

    local playerPayload = BurdJournals.Server.extractBaselinePayloadFromPlayerModData(targetPlayer)
    if hasUsableBaselinePayload(playerPayload) then
        playerPayload.steamId = BurdJournals.getPlayerSteamId and BurdJournals.getPlayerSteamId(targetPlayer) or nil
        playerPayload.characterName = BurdJournals.Server.getPlayerCharacterDisplayName(targetPlayer)
        playerPayload.capturedAt = BurdJournals.Server.baselineSnapshotNowHours()
        return playerPayload
    end
    return nil
end

function BurdJournals.Server.handleDebugListBaselineCache(player, _args)
    if not BurdJournals.Server.isDebugAllowed(player) then
        BurdJournals.Server.sendToClient(player, "debugError", { message = "Debug commands not allowed" })
        return
    end

    local cache = BurdJournals.Server.getBaselineCache()
    local archive = BurdJournals.Server.getBaselineArchive()
    local snapshots = BurdJournals.Server.getBaselineSnapshotStore()
    local cacheCount = 0
    local archiveCount = 0
    local snapshotCount = 0
    local activeCount = 0

    for _ in pairs(cache.players or {}) do cacheCount = cacheCount + 1 end
    for _ in pairs(archive.byCharacterId or {}) do archiveCount = archiveCount + 1 end
    for _ in pairs(snapshots.bySnapshotId or {}) do snapshotCount = snapshotCount + 1 end
    for _ in pairs(snapshots.activeByCharacterId or {}) do activeCount = activeCount + 1 end

    bsjWriteLogLine("[BurdJournals][ADMIN] Baseline Cache Stats: cache=" .. tostring(cacheCount)
        .. ", archive=" .. tostring(archiveCount)
        .. ", snapshots=" .. tostring(snapshotCount)
        .. ", activeSnapshots=" .. tostring(activeCount))

    BurdJournals.Server.sendToClient(player, "debugSuccess", {
        message = "Baseline cache=" .. tostring(cacheCount)
            .. ", archive=" .. tostring(archiveCount)
            .. ", snapshots=" .. tostring(snapshotCount)
            .. " (active " .. tostring(activeCount) .. ")"
    })
end

function BurdJournals.Server.handleDebugListBaselineSnapshots(player, args)
    if not BurdJournals.Server.isDebugAllowed(player) then
        BurdJournals.Server.sendToClient(player, "debugError", {message = "Debug commands not allowed"})
        return
    end

    args = type(args) == "table" and args or {}
    local targetUsername = BurdJournals.Server.normalizeSnapshotString(args.targetUsername, 96)
    local targetPlayer = nil
    if targetUsername and targetUsername ~= "" then
        targetPlayer = BurdJournals.Server.findPlayerByUsername(targetUsername)
    end
    if targetPlayer == nil and targetUsername and targetUsername ~= "" then
        if not BurdJournals.Server.isDebugAdmin(player) then
            BurdJournals.Server.sendToClient(player, "debugError", {message = "Admin access required to inspect another player's snapshots."})
        else
            BurdJournals.Server.sendToClient(player, "debugError", {message = "Player not found: " .. tostring(targetUsername)})
        end
        return
    end

    local steamId = BurdJournals.Server.normalizeSnapshotString(args.steamId, 96)
    local characterId = BurdJournals.Server.normalizeSnapshotString(args.characterId, 160)
    local useTargetCharacterId = args.useTargetCharacterId == true
    if targetPlayer then
        if BurdJournals.Server.mergeBaselineSnapshotsFromPlayerHistory then
            BurdJournals.Server.mergeBaselineSnapshotsFromPlayerHistory(targetPlayer, false)
        end
        local targetSteamId = BurdJournals.getPlayerSteamId and BurdJournals.getPlayerSteamId(targetPlayer) or nil
        local targetCharacterId = BurdJournals.getPlayerCharacterId and BurdJournals.getPlayerCharacterId(targetPlayer) or nil
        steamId = steamId or targetSteamId
        if characterId then
            -- Explicit character filter (character-mode request)
        elseif useTargetCharacterId then
            characterId = targetCharacterId
        else
            -- Default target flow is Steam-scoped so historical life snapshots remain visible.
            characterId = nil
        end
        targetUsername = targetPlayer.getUsername and targetPlayer:getUsername() or targetUsername
    end

    local listed = BurdJournals.Server.listBaselineSnapshots({
        steamId = steamId,
        characterId = characterId,
        query = args.query,
        includeDead = args.includeDead == true,
        page = tonumber(args.page) or 1,
        pageSize = tonumber(args.pageSize) or 20,
    })

    BurdJournals.Server.sendToClient(player, "debugBaselineSnapshotList", {
        items = listed.items or {},
        total = tonumber(listed.total) or 0,
        page = tonumber(listed.page) or 1,
        pageSize = tonumber(listed.pageSize) or 20,
        targetUsername = targetUsername,
        targetSteamId = steamId,
        targetCharacterId = characterId,
    })
end

function BurdJournals.Server.handleDebugGetBaselineSnapshot(player, args)
    if not BurdJournals.Server.isDebugAllowed(player) then
        BurdJournals.Server.sendToClient(player, "debugError", {message = "Debug commands not allowed"})
        return
    end

    local snapshotId = BurdJournals.Server.normalizeSnapshotString(args and args.snapshotId, 196)
    if not snapshotId then
        BurdJournals.Server.sendToClient(player, "debugError", {message = "Missing snapshot ID"})
        return
    end

    local snapshot = BurdJournals.Server.getBaselineSnapshot(snapshotId)
    if not snapshot then
        BurdJournals.Server.sendToClient(player, "debugError", {message = "Snapshot not found"})
        return
    end

    BurdJournals.Server.sendToClient(player, "debugBaselineSnapshotDetail", {
        snapshot = snapshot
    })
end

function BurdJournals.Server.handleDebugGetTargetBaselinePayload(player, args)
    if not BurdJournals.Server.isDebugAllowed(player) then
        BurdJournals.Server.sendToClient(player, "debugError", {message = "Debug commands not allowed"})
        return
    end

    args = type(args) == "table" and args or {}
    local targetPlayer, targetErr = BurdJournals.Server.resolveDebugTargetPlayer(player, args.targetUsername)
    if not targetPlayer then
        BurdJournals.Server.sendToClient(player, "debugError", {message = targetErr or "Target player not found"})
        return
    end

    local characterId = BurdJournals.getPlayerCharacterId and BurdJournals.getPlayerCharacterId(targetPlayer) or nil
    local steamId = BurdJournals.getPlayerSteamId and BurdJournals.getPlayerSteamId(targetPlayer) or nil
    local payload = BurdJournals.Server.buildBaselinePayloadFromTarget(targetPlayer, characterId)
    local baselinePayload = nil
    local counts = {
        skills = 0,
        mediaSkills = 0,
        traits = 0,
        recipes = 0,
    }

    if type(payload) == "table" then
        baselinePayload = BurdJournals.sanitizeBaselinePayloadForSnapshot
            and BurdJournals.sanitizeBaselinePayloadForSnapshot(payload)
            or payload
        if BurdJournals.getBaselineSnapshotCounts then
            counts = BurdJournals.getBaselineSnapshotCounts(baselinePayload) or counts
        end
    end

    BurdJournals.Server.sendToClient(player, "debugTargetBaselinePayload", {
        targetUsername = targetPlayer.getUsername and targetPlayer:getUsername() or nil,
        steamId = steamId,
        characterId = characterId,
        baselinePayload = baselinePayload,
        counts = counts,
    })
end

function BurdJournals.Server.handleDebugSaveBaselineSnapshot(player, args)
    if not BurdJournals.Server.isDebugAllowed(player) then
        BurdJournals.Server.sendToClient(player, "debugError", {message = "Debug commands not allowed"})
        return
    end

    args = type(args) == "table" and args or {}
    local targetPlayer, targetErr = BurdJournals.Server.resolveDebugTargetPlayer(player, args.targetUsername)
    if not targetPlayer then
        BurdJournals.Server.sendToClient(player, "debugError", {message = targetErr or "Target player not found"})
        return
    end

    local targetCharacterId = BurdJournals.getPlayerCharacterId and BurdJournals.getPlayerCharacterId(targetPlayer) or nil
    if not targetCharacterId then
        BurdJournals.Server.sendToClient(player, "debugError", {message = "Could not resolve target character ID"})
        return
    end

    local baselinePayload = BurdJournals.Server.buildBaselinePayloadFromTarget(targetPlayer, targetCharacterId)
    if not baselinePayload then
        BurdJournals.Server.sendToClient(player, "debugError", {message = "No baseline payload available to snapshot"})
        return
    end

    local source = BurdJournals.Server.normalizeSnapshotString(args.source, 64) or "manual_debug"
    local note = BurdJournals.Server.normalizeSnapshotString(args.note, 256)
    local ok, snapshotId, err = BurdJournals.Server.captureBaselineSnapshotForPlayer(
        targetPlayer,
        targetCharacterId,
        baselinePayload,
        source,
        note,
        { force = true }
    )
    if not ok then
        BurdJournals.Server.sendToClient(player, "debugError", {
            message = "Failed to save baseline snapshot: " .. tostring(err or "unknown")
        })
        return
    end

    BurdJournals.Server.sendToClient(player, "debugBaselineSnapshotSaved", {
        snapshotId = snapshotId,
        targetUsername = targetPlayer:getUsername(),
        source = source
    })
end

function BurdJournals.Server.handleDebugApplyBaselineSnapshot(player, args)
    if not BurdJournals.Server.isDebugAllowed(player) then
        BurdJournals.Server.sendToClient(player, "debugError", {message = "Debug commands not allowed"})
        return
    end

    args = type(args) == "table" and args or {}
    local snapshotId = BurdJournals.Server.normalizeSnapshotString(args.snapshotId, 196)
    if not snapshotId then
        BurdJournals.Server.sendToClient(player, "debugError", {message = "Missing snapshot ID"})
        return
    end

    local targetPlayer, targetErr = BurdJournals.Server.resolveDebugTargetPlayer(player, args.targetUsername)
    if not targetPlayer then
        BurdJournals.Server.sendToClient(player, "debugError", {message = targetErr or "Target player not found"})
        return
    end

    local snapshot = BurdJournals.Server.getBaselineSnapshot(snapshotId)
    if not snapshot then
        BurdJournals.Server.sendToClient(player, "debugError", {message = "Snapshot not found"})
        return
    end

    local requestedMode = args.restoreMode
    if requestedMode == nil or tostring(requestedMode) == "" then
        requestedMode = BurdJournals.getDefaultBaselineRestoreMode
            and BurdJournals.getDefaultBaselineRestoreMode()
            or BurdJournals.BASELINE_SNAPSHOT_RESTORE_UNLOCKED
    end
    local restoreMode = BurdJournals.normalizeBaselineRestoreMode and BurdJournals.normalizeBaselineRestoreMode(requestedMode)
        or BurdJournals.BASELINE_SNAPSHOT_RESTORE_UNLOCKED
    local applied, applyErr, result = BurdJournals.Server.applyBaselineSnapshotToPlayer(targetPlayer, snapshot, restoreMode)
    if not applied then
        BurdJournals.Server.sendToClient(player, "debugError", {
            message = "Failed to apply snapshot: " .. tostring(applyErr or "unknown")
        })
        return
    end

    local targetCharacterId = result and result.characterId
        or (BurdJournals.getPlayerCharacterId and BurdJournals.getPlayerCharacterId(targetPlayer))
        or nil
    local postApply = nil
    if targetCharacterId then
        postApply = BurdJournals.Server.getCachedBaseline(targetCharacterId, targetPlayer)
    end
    if BurdJournals.Server.captureBaselineSnapshotForPlayer and targetCharacterId then
        BurdJournals.Server.captureBaselineSnapshotForPlayer(
            targetPlayer,
            targetCharacterId,
            postApply,
            "debug_edit",
            "apply:" .. tostring(snapshotId)
        )
    end
    if type(postApply) ~= "table" then
        postApply = BurdJournals.Server.buildBaselinePayloadFromTarget(targetPlayer, targetCharacterId)
    end

    BurdJournals.Server.sendToClient(player, "debugBaselineSnapshotApplied", {
        snapshotId = snapshotId,
        characterId = targetCharacterId,
        targetUsername = targetPlayer:getUsername(),
        restoreMode = restoreMode,
        skillBaseline = BurdJournals.Server.copyBaselineTableEntries(postApply and postApply.skillBaseline),
        skillExportBaseline = BurdJournals.Server.copyBaselineTableEntries(postApply and postApply.skillExportBaseline),
        mediaSkillBaseline = BurdJournals.Server.copyBaselineTableEntries(postApply and postApply.mediaSkillBaseline),
        mediaSkillExportBaseline = BurdJournals.Server.copyBaselineTableEntries(postApply and postApply.mediaSkillExportBaseline),
        traitBaseline = BurdJournals.Server.copyBaselineTableEntries(postApply and postApply.traitBaseline),
        traitExportBaseline = BurdJournals.Server.copyBaselineTableEntries(postApply and postApply.traitExportBaseline),
        recipeBaseline = BurdJournals.Server.copyBaselineTableEntries(postApply and postApply.recipeBaseline),
        recipeExportBaseline = BurdJournals.Server.copyBaselineTableEntries(postApply and postApply.recipeExportBaseline),
        baselineVersion = tonumber(postApply and postApply.baselineVersion) or tonumber(BurdJournals.BASELINE_VERSION) or 5,
        debugModified = result and result.debugModified == true,
        counts = result and result.counts or (snapshot.counts or {}),
    })
end

function BurdJournals.Server.handleDebugDeleteBaselineSnapshot(player, args)
    if not BurdJournals.Server.isDebugAllowed(player) then
        BurdJournals.Server.sendToClient(player, "debugError", {message = "Debug commands not allowed"})
        return
    end
    if not BurdJournals.Server.isDebugAdmin(player) then
        BurdJournals.Server.sendToClient(player, "debugError", {message = "Admin access required to delete snapshots."})
        return
    end

    local snapshotId = BurdJournals.Server.normalizeSnapshotString(args and args.snapshotId, 196)
    if not snapshotId then
        BurdJournals.Server.sendToClient(player, "debugError", {message = "Missing snapshot ID"})
        return
    end

    local removed = BurdJournals.Server.deleteBaselineSnapshot(snapshotId, false)
    if not removed then
        BurdJournals.Server.sendToClient(player, "debugError", {message = "Snapshot not found"})
        return
    end

    BurdJournals.Server.sendToClient(player, "debugBaselineSnapshotDeleted", {
        snapshotId = snapshotId
    })
end

function normalizeDebugOriginMode(value)
    local mode = tostring(value or "auto")
    if mode == "personal" or mode == "found" or mode == "world" or mode == "zombie" then
        return mode
    end
    return "auto"
end

function getDefaultDebugOriginModeForType(journalType)
    local t = tostring(journalType or "filled")
    local debugType = BurdJournals.getDebugJournalType and BurdJournals.getDebugJournalType(t) or nil
    if type(debugType) == "table" and debugType.defaultOriginMode then
        return normalizeDebugOriginMode(debugType.defaultOriginMode) ~= "auto"
            and normalizeDebugOriginMode(debugType.defaultOriginMode)
            or "found"
    end
    if t == "worn" then
        return "found"
    end
    if t == "yuletide" then
        return "world"
    end
    if t == "bloody" or t == "cursed" then
        return "zombie"
    end
    return "personal"
end

function resolveDebugOriginModeForType(journalType, requestedMode)
    local mode = normalizeDebugOriginMode(requestedMode)
    if mode == "auto" then
        return getDefaultDebugOriginModeForType(journalType)
    end
    return mode
end

function applyDebugOriginModeToJournalData(data, originMode)
    if type(data) ~= "table" then
        return
    end
    local mode = resolveDebugOriginModeForType(nil, originMode)
    data.originMode = mode
    if mode == "personal" then
        data.isPlayerCreated = true
        data.sourceType = "personal"
    elseif mode == "zombie" then
        data.isPlayerCreated = false
        data.sourceType = "zombie"
    elseif mode == "world" then
        data.isPlayerCreated = false
        data.sourceType = "world"
    else
        data.isPlayerCreated = false
        data.sourceType = "found"
    end
end

function BurdJournals.Server.normalizeDebugCursedSpawnState(requestedState, cursedUnleashed)
    local state = tostring(requestedState or "")
    if state == "dormant" or state == "hidden" or state == "unleashed" then
        return state
    end
    if cursedUnleashed == true then
        return "unleashed"
    end
    return "dormant"
end

function BurdJournals.Server.getDebugSpawnItemType(journalType, cursedSpawnState, cursedUnleashed)
    local debugType = BurdJournals.getDebugJournalType and BurdJournals.getDebugJournalType(tostring(journalType or "")) or nil
    if type(debugType) == "table" and debugType.itemType then
        return tostring(debugType.itemType)
    end
    if journalType == "blank" then
        return "BurdJournals.BlankSurvivalJournal"
    end
    if journalType == "worn" then
        return "BurdJournals.FilledSurvivalJournal_Worn"
    end
    if journalType == "bloody" then
        return "BurdJournals.FilledSurvivalJournal_Bloody"
    end
    if journalType == "yuletide" then
        return BurdJournals.YULETIDE_ITEM_TYPE or "BurdJournals.YuletideJournal"
    end
    if journalType == "cursed" then
        local normalizedCursedState = BurdJournals.Server.normalizeDebugCursedSpawnState(cursedSpawnState, cursedUnleashed)
        if normalizedCursedState == "hidden" or normalizedCursedState == "unleashed" then
            return "BurdJournals.FilledSurvivalJournal_Bloody"
        end
        return BurdJournals.CURSED_ITEM_TYPE or "BurdJournals.CursedJournal"
    end
    return "BurdJournals.FilledSurvivalJournal"
end

function BurdJournals.Server.resolveDebugSpawnOwnerMode(args, journalType, requestedOwnerMode)
    local ownerMode = tostring(requestedOwnerMode or "")
    if ownerMode ~= "" then
        return ownerMode
    end
    if journalType == "filled" and args.ownerSteamId and args.ownerUsername then
        return "player_assignment"
    end
    if args.owner and tostring(args.owner) ~= "" then
        return "custom"
    end
    return "none"
end

function BurdJournals.Server.initializeDebugSpawnJournalItem(player, item, args, context)
    local journalType = context.journalType
    local cursedSpawnState = BurdJournals.Server.normalizeDebugCursedSpawnState(
        context.cursedSpawnState,
        context.cursedUnleashed == true
    )
    local cursedUnleashed = cursedSpawnState == "unleashed"
    local cursedHidden = cursedSpawnState == "hidden"
    local modData = item:getModData()
    modData.BurdJournals = modData.BurdJournals or {}
    local data = modData.BurdJournals

    data.uuid = (BurdJournals.generateUUID and BurdJournals.generateUUID()) or tostring((getTimestampMs and getTimestampMs()) or os.time())
    local worldAge = getGameTime() and getGameTime():getWorldAgeHours() or 0
    data.timestamp = worldAge
    local requestedAge = tonumber(args.ageHours or 0) or 0
    if requestedAge > 0 then
        data.timestamp = math.max(0, worldAge - requestedAge)
    end
    data.lastModified = worldAge

    data.isDebugSpawned = context.isDebugProfile == true
    data.isDebugEdited = context.isDebugProfile == true and true or nil
    data.debugBackupEnabled = args.debugBackupEnabled == true and true or nil
    data.isWritten = true
    data.journalVersion = BurdJournals.VERSION or "dev"
    data.sanitizedVersion = BurdJournals.SANITIZE_VERSION or 1

    data.ownerMode = context.ownerMode
    data.ownerCharacterName = nil
    data.author = nil
    data.ownerSteamId = nil
    data.ownerUsername = nil

    if context.ownerMode == "player_assignment" and journalType == "filled" and args.ownerSteamId and args.ownerUsername then
        data.ownerSteamId = args.ownerSteamId
        data.ownerUsername = args.ownerUsername
        data.ownerCharacterName = args.ownerCharacterName or args.owner or nil
        data.author = data.ownerCharacterName
    elseif context.ownerMode == "player_author" or context.ownerMode == "custom" then
        local authorName = tostring(args.ownerCharacterName or args.owner or "")
        if authorName ~= "" then
            data.author = authorName
        end
    end

    data.isPlayerCreated = args.isPlayerJournal or (context.ownerMode == "player_assignment" and journalType == "filled") or false
    if journalType == "filled" and tostring(args.filledJournalState or "") == "restored" then
        data.isPlayerCreated = true
        data.wasRestored = true
        data.wasFromWorn = true
        data.wasFromBloody = false
        data.restoredBy = player and player.getUsername and player:getUsername() or "Admin"
    end

    data.skills = {}
    data.traits = {}
    data.recipes = {}
    data.stats = {}
    data.claims = {}
    data.claimedSkills = {}
    data.claimedTraits = {}
    data.claimedRecipes = {}
    data.claimedStats = {}
    data.claimedForgetSlot = {}
    data.forgetSlot = args.forgetSlot == true and true or nil
    data.forgetSlotCount = data.forgetSlot and tonumber(args.forgetSlotCount) or nil
    data.isHiddenCursedJournal = false
    data.isCursedJournal = false
    data.cursedState = nil
    data.isCursedReward = false
    data.cursedEffectType = nil
    data.cursedUnleashedByCharacterId = nil
    data.cursedUnleashedByUsername = nil
    data.cursedUnleashedAtHours = nil
    data.cursedSealSoundEvent = BurdJournals.Server.normalizeCursedSealSoundEvent(args.cursedSealSoundEvent)
    data.cursedPendingRewards = nil
    data.cursedForcedEffectType = normalizeCurseEffectType(args.forceCurseType)
    data.cursedForcedTraitId = normalizeForcedTraitId(args.forceCurseTraitId)
    data.cursedForcedSkillName = normalizeForcedSkillName(args.forceCurseSkillName)
    data.loreNoteText = nil
    data.loreNoteGeneratedByName = nil
    data.loreNoteGeneratedAtHours = nil
    data.loreNoteTemplateVersion = nil
    data.loreNoteTemplateFamily = nil
    data.loreNoteTemplateText = nil
    data.cursedUnleashedByName = nil
    data.isYuletideJournal = false
    data.yuletideState = nil
    data.yuletideImmediateGifts = nil
    data.yuletideGiftGranted = nil
    data.yuletideGiftTier = nil
    data.yuletideGiftRoll = nil
    data.yuletideManualRewards = nil
    data.yuletideWrappedVariant = nil
    data.yuletideDeliveryToken = nil
    data.yuletideDeliveredBy = nil
    data.yuletideDeliveryLabel = nil
    data.yuletidePendingDelivery = nil
    data.yuletideBeacon = nil
    data.yuletideOpenedByName = nil

    if journalType == "worn" then
        data.isWorn = true
        data.wasFromWorn = true
        data.loreNoteTemplateVersion = LORE_DYNAMIC_VERSION
        data.loreNoteTemplateFamily = "worn"
    elseif journalType == "bloody" then
        data.isBloody = true
        data.wasFromBloody = true
        data.hasBloodyOrigin = true
        data.loreNoteTemplateVersion = LORE_DYNAMIC_VERSION
        data.loreNoteTemplateFamily = "bloody"
    elseif journalType == "cursed" then
        if cursedUnleashed then
            data.isBloody = true
            data.wasFromBloody = true
            data.hasBloodyOrigin = true
            data.isCursedJournal = false
            data.cursedState = "unleashed"
            data.isCursedReward = true
            data.cursedEffectType = normalizeCurseEffectType(args.forceCurseType)
                or normalizeCurseEffectType(args.cursedEffectType)
                or "panic"
            data.cursedUnleashedByCharacterId = BurdJournals.getPlayerCharacterId and BurdJournals.getPlayerCharacterId(player) or nil
            data.cursedUnleashedByUsername = player:getUsername()
            data.cursedUnleashedByName = BurdJournals.Server.getJournalPlayerDisplayName(player)
            data.cursedUnleashedAtHours = worldAge
            data.loreNoteTemplateVersion = LORE_DYNAMIC_VERSION
            data.loreNoteTemplateFamily = "cursed"
        elseif cursedHidden then
            data.isBloody = true
            data.isWorn = false
            data.wasFromBloody = true
            data.wasFromWorn = false
            data.hasBloodyOrigin = true
            data.isPlayerCreated = false
            data.isZombieJournal = true
            data.isHiddenCursedJournal = true
            data.isCursedJournal = false
            data.cursedState = "hidden"
            data.cursedSealSoundEvent = nil
            -- Expired curses: record spawn time so old curses can go dormant.
            data.cursedSpawnedAtHours = data.cursedSpawnedAtHours
                or (BurdJournals.Server.getWorldAgeHoursSafe and BurdJournals.Server.getWorldAgeHoursSafe())
                or nil
            local cursedIdentity = BurdJournals.Server.resolveCursedJournalIdentity and BurdJournals.Server.resolveCursedJournalIdentity(data) or nil
            if cursedIdentity then
                data.author = data.author or cursedIdentity.author
                data.profession = data.profession or cursedIdentity.profession
                data.professionName = data.professionName or cursedIdentity.professionName
                data.flavorKey = data.flavorKey or cursedIdentity.flavorKey
            end
            data.loreNoteTemplateVersion = LORE_DYNAMIC_VERSION
            data.loreNoteTemplateFamily = "bloody"
        else
            data.isBloody = false
            data.isWorn = false
            data.wasFromBloody = false
            data.wasFromWorn = false
            data.hasBloodyOrigin = false
            data.isPlayerCreated = false
            data.isZombieJournal = true
            data.isCursedJournal = true
            data.cursedState = "dormant"
            -- Expired curses: record spawn time so old curses can go dormant.
            data.cursedSpawnedAtHours = data.cursedSpawnedAtHours
                or (BurdJournals.Server.getWorldAgeHoursSafe and BurdJournals.Server.getWorldAgeHoursSafe())
                or nil
            local cursedIdentity = BurdJournals.Server.resolveCursedJournalIdentity and BurdJournals.Server.resolveCursedJournalIdentity(data) or nil
            if cursedIdentity then
                data.author = data.author or cursedIdentity.author
                data.profession = data.profession or cursedIdentity.profession
                data.professionName = data.professionName or cursedIdentity.professionName
                data.flavorKey = data.flavorKey or cursedIdentity.flavorKey
            end
            data.loreNoteTemplateVersion = LORE_DYNAMIC_VERSION
            data.loreNoteTemplateFamily = "cursed"
        end
    elseif journalType == "yuletide" then
        local manualYuletideRewards = args.manualRewards == true
        local manualSkills = BurdJournals.normalizeTable and BurdJournals.normalizeTable(args.skills) or (type(args.skills) == "table" and args.skills or {})
        local manualTraits = BurdJournals.normalizeTable and BurdJournals.normalizeTable(args.traits) or (type(args.traits) == "table" and args.traits or {})
        local manualRecipes = BurdJournals.normalizeTable and BurdJournals.normalizeTable(args.recipes) or (type(args.recipes) == "table" and args.recipes or {})
        local manualStats = BurdJournals.normalizeTable and BurdJournals.normalizeTable(args.stats) or (type(args.stats) == "table" and args.stats or {})
        local yuletideProfile = BurdJournals.Server.generateYuletideJournalProfile({
            uuid = data.uuid,
            timestamp = data.timestamp,
            author = data.author,
            profession = data.profession,
            professionName = data.professionName,
            flavorKey = data.flavorKey,
            condition = data.condition,
            manualRewards = manualYuletideRewards,
            skills = manualYuletideRewards and manualSkills or nil,
            traits = manualYuletideRewards and manualTraits or nil,
            recipes = manualYuletideRewards and manualRecipes or nil,
            stats = manualYuletideRewards and manualStats or nil,
            forgetSlot = manualYuletideRewards and (args.forgetSlot == true and true or nil) or args.forgetSlot,
            yuletideState = args.yuletideState,
            yuletideWrappedVariant = args.yuletideWrappedVariant,
            yuletideGiftTier = args.yuletideGiftTier,
            loreNoteTemplateVersion = LORE_DYNAMIC_VERSION,
            loreNoteTemplateFamily = "yuletide",
            lootNotesEligible = true,
        })
        for key, value in pairs(yuletideProfile) do
            data[key] = value
        end
        data.isPlayerCreated = false
        data.isZombieJournal = (context.resolvedOriginMode == "zombie")
    end
    if journalType == "cursed" then
        data.isPlayerCreated = false
    end

    applyDebugOriginModeToJournalData(data, context.resolvedOriginMode)

    if (journalType == "worn" or journalType == "bloody") and not args.noProfession then
        if args.profession and args.professionName then
            data.profession = args.profession
            data.professionName = args.professionName
            if args.professionFlavorKey then
                data.flavorKey = args.professionFlavorKey
            end
            local profType = args.isCustomProfession and "Custom" or "Set"
            BurdJournals.debugPrint("[BurdJournals] DEBUG Server spawn: " .. profType .. " profession: " .. args.professionName)
        elseif args.randomProfession ~= false then
            local profId, profName, flavorKey = BurdJournals.getRandomProfession()
            if profId then
                data.profession = profId
                data.professionName = profName
                data.flavorKey = flavorKey
                BurdJournals.debugPrint("[BurdJournals] DEBUG Server spawn: Random profession: " .. profName)
            end
        end
    end

    if args.flavorText and args.flavorText ~= "" then
        data.flavorText = args.flavorText
        data.flavorKey = nil
        BurdJournals.debugPrint("[BurdJournals] DEBUG Server spawn: Custom flavor text: " .. args.flavorText)
    end

    local supportsGeneratedLore = (journalType == "worn" or journalType == "bloody" or journalType == "cursed" or journalType == "yuletide")
    local loreMode = tostring(args.loreMode or "dynamic")
    if supportsGeneratedLore then
        data.loreNoteTemplateVersion = LORE_DYNAMIC_VERSION
        data.loreNoteTemplateText = nil
        data.loreNoteTemplateKey = nil
        if loreMode == "manual" and args.loreTemplateKey then
            data.loreNoteTemplateKey = BurdJournals.Server.normalizeJournalServerText(args.loreTemplateKey)
            BurdJournals.debugPrint("[BurdJournals] DEBUG Server spawn: Forced lore template key: " .. tostring(data.loreNoteTemplateKey))
        end
    end

    local skillJournalContext = data
    if journalType == "filled" then
        skillJournalContext = {isPlayerCreated = true}
    end

    if args.skills then
        BurdJournals.debugPrint("[BurdJournals] DEBUG Server spawn: Processing " .. tostring(BurdJournals.tableCount and BurdJournals.tableCount(args.skills) or "?") .. " skills")
        for skillName, skillData in pairs(args.skills) do
            local enabledForJournal = not BurdJournals.isSkillEnabledForJournal or BurdJournals.isSkillEnabledForJournal(skillJournalContext, skillName)
            if enabledForJournal then
                local xp = skillData.xp or 0
                local level = skillData.level or 0
                BurdJournals.debugPrint("[BurdJournals] DEBUG Server spawn: Skill " .. tostring(skillName) .. " received xp=" .. tostring(xp) .. ", level=" .. tostring(level))
                data.skills[skillName] = {
                    xp = xp,
                    level = level
                }
            else
                BurdJournals.debugPrint("[BurdJournals] DEBUG Server spawn: Skipping disabled passive skill for journal type: " .. tostring(skillName))
            end
        end
    else
        BurdJournals.debugPrint("[BurdJournals] DEBUG Server spawn: No skills in args")
    end

    if args.traits then
        for traitName, _ in pairs(args.traits) do
            data.traits[traitName] = true
        end
    end

    if args.recipes then
        for recipeName, _ in pairs(args.recipes) do
            data.recipes[recipeName] = true
        end
    end

    if args.stats then
        for statName, value in pairs(args.stats) do
            local numValue = tonumber(value)
            if numValue then
                data.stats[statName] = { value = numValue }
            end
        end
    end

    if supportsGeneratedLore
        and data.isPlayerCreated ~= true
        and BurdJournals.Server.tryAttachGeneratedLootNotes
    then
        data.lootNotesEligible = true
        BurdJournals.Server.tryAttachGeneratedLootNotes(player, item, data, data.loreNoteTemplateFamily or journalType)
    end

    if journalType == "cursed" and not cursedUnleashed then
        data.cursedPendingRewards = {
            uuid = data.uuid,
            author = data.author,
            profession = data.profession,
            professionName = data.professionName,
            flavorKey = data.flavorKey,
            flavorText = data.flavorText,
            loreNoteText = data.loreNoteText,
            timestamp = data.timestamp,
            skills = data.skills,
            traits = data.traits,
            recipes = data.recipes,
            stats = data.stats,
            claims = data.claims,
            claimedSkills = data.claimedSkills,
            claimedTraits = data.claimedTraits,
            claimedRecipes = data.claimedRecipes,
            claimedStats = data.claimedStats,
            forgetSlot = data.forgetSlot == true and true or nil,
            forgetSlotCount = data.forgetSlot == true and tonumber(data.forgetSlotCount) or nil,
            claimedForgetSlot = data.claimedForgetSlot,
            condition = data.condition,
            cursedSealSoundEvent = data.cursedSealSoundEvent,
            cursedForcedEffectType = data.cursedForcedEffectType,
            cursedForcedTraitId = data.cursedForcedTraitId,
            cursedForcedSkillName = data.cursedForcedSkillName,
            sourceType = data.sourceType,
            originMode = data.originMode,
            cursedManualRewards = args.manualRewards == true and true or nil,
        }
        data.skills = {}
        data.traits = {}
        data.recipes = {}
        data.stats = {}
        data.forgetSlot = nil
        data.forgetSlotCount = nil
        data.claimedForgetSlot = {}
    end

    local requestedCondition = tonumber(args.conditionOverride or 0) or 0
    if requestedCondition > 0 then
        local cond = math.max(1, math.min(10, math.floor(requestedCondition)))
        if item.setCondition then
            item:setCondition(cond)
        end
        data.condition = cond
    elseif item.getCondition then
        data.condition = item:getCondition()
    end
    if journalType == "cursed" and not cursedUnleashed and type(data.cursedPendingRewards) == "table" then
        data.cursedPendingRewards.condition = data.condition
    end

    local debugType = BurdJournals.getDebugJournalType and BurdJournals.getDebugJournalType(journalType) or nil
    if type(debugType) == "table" and type(debugType.serverInitialize) == "function" then
        debugType.serverInitialize(player, item, args, context, data)
    end

    BurdJournals.debugPrint("[BurdJournals] DEBUG Server spawn: Final journal data initialized with persistence fields")
    BurdJournals.debugPrint("[BurdJournals] DEBUG Server spawn:   spawnProfile=" .. tostring(context.spawnProfile))
    BurdJournals.debugPrint("[BurdJournals] DEBUG Server spawn:   isWritten=" .. tostring(data.isWritten))
    BurdJournals.debugPrint("[BurdJournals] DEBUG Server spawn:   isDebugSpawned=" .. tostring(data.isDebugSpawned))
    BurdJournals.debugPrint("[BurdJournals] DEBUG Server spawn:   sanitizedVersion=" .. tostring(data.sanitizedVersion))
    BurdJournals.debugPrint("[BurdJournals] DEBUG Server spawn:   isPlayerCreated=" .. tostring(data.isPlayerCreated))
    BurdJournals.debugPrint("[BurdJournals] DEBUG Server spawn:   ownerSteamId=" .. tostring(data.ownerSteamId))

    if BurdJournals.updateJournalName then
        BurdJournals.updateJournalName(item)
    end
    if BurdJournals.updateJournalIcon then
        BurdJournals.updateJournalIcon(item)
    end
    if item.transmitModData then
        safeTransmitServerJournalModData(item, "initializeDebugSpawnJournalItem")
    end
end

-- Handle debug: Spawn journal (server-side for MP persistence)
function BurdJournals.Server.handleDebugSpawnJournal(player, args)
    if not BurdJournals.Server.isDebugAllowed(player) then
        BurdJournals.Server.sendToClient(player, "debugError", {message = "Debug commands not allowed"})
        return
    end

    args = type(args) == "table" and args or {}
    local inventory = player:getInventory()
    if not inventory then
        BurdJournals.Server.sendToClient(player, "debugError", {message = "No inventory"})
        return
    end

    local spawnProfile = tostring(args.spawnProfile or "normal")
    if spawnProfile ~= "debug" then
        spawnProfile = "normal"
    end
    local isDebugProfile = spawnProfile == "debug"
    local journalType = args.journalType or "filled"
    local cursedSpawnState = BurdJournals.Server.normalizeDebugCursedSpawnState(args.cursedSpawnState, args.cursedUnleashed == true)
    local cursedUnleashed = cursedSpawnState == "unleashed"
    local resolvedOriginMode = resolveDebugOriginModeForType(journalType, args.originMode)
    local ownerMode = BurdJournals.Server.resolveDebugSpawnOwnerMode(args, journalType, args.ownerMode)
    local itemType = BurdJournals.Server.getDebugSpawnItemType(journalType, cursedSpawnState, cursedUnleashed)
    local context = {
        spawnProfile = spawnProfile,
        isDebugProfile = isDebugProfile,
        resolvedOriginMode = resolvedOriginMode,
        ownerMode = ownerMode,
        journalType = journalType,
        cursedSpawnState = cursedSpawnState,
        cursedUnleashed = cursedUnleashed,
    }

    BurdJournals.debugPrint("[BurdJournals] DEBUG: Server spawning journal type=" .. itemType
        .. " profile=" .. tostring(spawnProfile))

    local item = inventory:AddItem(itemType)
    if not item then
        BurdJournals.Server.sendToClient(player, "debugError", {message = "Failed to create journal"})
        return
    end

    if journalType ~= "blank" then
        BurdJournals.Server.initializeDebugSpawnJournalItem(player, item, args, context)
    end

    if item.transmitModData then
        safeTransmitServerJournalModData(item, "handleDebugSpawnJournal")
    end
    
    -- Sync inventory to client (required for MP)
    if inventory.sync then
        inventory:sync()
    end
    
    -- Also try setDrawDirty to force UI update
    if inventory.setDrawDirty then
        inventory:setDrawDirty(true)
    end
    
    -- Send inventory packet to client (various methods for compatibility)
    if isServer() and player then
        -- Try various inventory sync methods - not all exist in all PZ versions
        if player.syncInventory then
            player:syncInventory()
        end
        transmitYuletideGiftItem(player, inventory, item, "Debug spawn")
    end
    
    local itemId = item.getID and item:getID() or nil
    local spawnedModData = item.getModData and item:getModData() or nil
    local spawnedJournalData = type(spawnedModData) == "table" and spawnedModData.BurdJournals or nil
    if type(spawnedJournalData) == "table"
        and (spawnedJournalData.isDebugSpawned == true or spawnedJournalData.debugBackupEnabled == true)
        and BurdJournals.Server.persistDebugJournalSnapshot
    then
        BurdJournals.Server.persistDebugJournalSnapshot(player, spawnedJournalData.uuid, spawnedJournalData, item, {
            savedBy = player and player.getUsername and player:getUsername() or "Admin",
            pendingApply = false,
            sourceTag = "debugSpawnJournal",
        })
    end
    BurdJournals.debugPrint("[BurdJournals] DEBUG: Server spawned journal ID=" .. tostring(itemId)
        .. " type=" .. journalType .. " profile=" .. tostring(spawnProfile) .. " origin=" .. tostring(resolvedOriginMode))
    local profileLabel = isDebugProfile and "Debug" or "Normal"
    BurdJournals.Server.sendToClient(player, "debugSuccess", {
        message = "Spawned " .. journalType .. " journal [" .. profileLabel .. ", origin: " .. tostring(resolvedOriginMode) .. "] (check inventory)",
        refreshInventory = true,
        journalId = itemId,
        journalUUID = spawnedJournalData and spawnedJournalData.uuid or nil,
        itemType = itemType,
        journalType = journalType
    })
end

-- Handle debug: Force dissolve any journal
function BurdJournals.Server.handleDebugDissolveJournal(player, args)
    if not BurdJournals.Server.isDebugAllowed(player) then
        BurdJournals.Server.sendToClient(player, "debugError", {message = "Debug commands not allowed"})
        return
    end
    
    local journalId = args.journalId
    if not journalId then
        BurdJournals.Server.sendToClient(player, "debugError", {message = "No journal ID specified"})
        return
    end
    
    BurdJournals.debugPrint("[BurdJournals] DEBUG: Force dissolve requested for ID=" .. tostring(journalId))
    
    local journal = BurdJournals.findItemByIdInPlayerInventory
        and BurdJournals.findItemByIdInPlayerInventory(player, journalId) or nil
    
    if not journal then
        BurdJournals.Server.sendToClient(player, "debugError", {message = "Journal not found"})
        return
    end
    
    -- Force remove the journal (no restrictions)
    BurdJournals.debugPrint("[BurdJournals] DEBUG: Force dissolving journal " .. tostring(journalId))
    if not BurdJournals.Server.dissolveJournal(player, journal) then
        BurdJournals.Server.sendToClient(player, "debugError", {message = "Journal removal failed"})
        return
    end
    
    BurdJournals.Server.sendToClient(player, "debugSuccess", {message = "Journal dissolved"})
    BurdJournals.Server.sendToClient(player, "journalDissolved", {
        message = "Debug dissolved",
        journalId = journalId
    })
end

BurdJournals.debugPrint("[BurdJournals] Registering OnClientCommand handler...")
BurdJournals.debugPrint("[BurdJournals] Events table exists: " .. tostring(Events ~= nil))
BurdJournals.debugPrint("[BurdJournals] Events.OnClientCommand exists: " .. tostring(Events and Events.OnClientCommand ~= nil))
BurdJournals.debugPrint("[BurdJournals] BurdJournals.Server.onClientCommand exists: " .. tostring(BurdJournals.Server.onClientCommand ~= nil))

if Events and Events.OnClientCommand and Events.OnClientCommand.Add then
    Events.OnClientCommand.Add(BurdJournals.Server.onClientCommand)
    BurdJournals.debugPrint("[BurdJournals] OnClientCommand handler registered SUCCESSFULLY")
else
    bsjWriteLogLine("[BurdJournals] ERROR registering OnClientCommand: Events.OnClientCommand.Add not available")
end

Events.OnServerStarted.Add(BurdJournals.Server.init)
Events.EveryHours.Add(BurdJournals.Server.checkBaselineCleanup)
if Events.OnPlayerDeath then
    Events.OnPlayerDeath.Add(function(player)
        BurdJournals.Server.cancelBatchRewardJobsForPlayer(player, "playerDied")
    end)
end

local playerJournalRegistryContainerScanLastAt = {}
local PLAYER_JOURNAL_REGISTRY_CONTAINER_SCAN_DEBOUNCE_MS = 1000
local PLAYER_JOURNAL_REGISTRY_CONTAINER_SCAN_CLEANUP_MS = 300000
local playerJournalRegistryContainerScanCleanupLastAt = 0
BurdJournals.Server._playerJournalRegistryContainerScanQueue = BurdJournals.Server._playerJournalRegistryContainerScanQueue or {}
BurdJournals.Server._playerJournalRegistryContainerScanOrder = BurdJournals.Server._playerJournalRegistryContainerScanOrder or {}
BurdJournals.Server.PLAYER_JOURNAL_REGISTRY_CONTAINER_SCAN_ITEMS_PER_TICK = 24
BurdJournals.Server.PLAYER_JOURNAL_REGISTRY_CONTAINER_SCAN_MAX_QUEUED = 64
BurdJournals.Server.PLAYER_JOURNAL_REGISTRY_CONTAINER_SCAN_TTL_MS = 30000
BurdJournals.Server.PLAYER_JOURNAL_REGISTRY_CONTAINER_SCAN_MAX_RESTARTS = 2

local function cleanupPlayerJournalRegistryContainerScanCache(nowMs)
    nowMs = tonumber(nowMs) or 0
    if nowMs <= 0 then
        return
    end
    if (nowMs - playerJournalRegistryContainerScanCleanupLastAt) < PLAYER_JOURNAL_REGISTRY_CONTAINER_SCAN_CLEANUP_MS then
        return
    end
    playerJournalRegistryContainerScanCleanupLastAt = nowMs
    for key, lastScanAt in pairs(playerJournalRegistryContainerScanLastAt) do
        if (nowMs - (tonumber(lastScanAt) or 0)) >= PLAYER_JOURNAL_REGISTRY_CONTAINER_SCAN_CLEANUP_MS then
            playerJournalRegistryContainerScanLastAt[key] = nil
        end
    end
end

local function getPlayerJournalRegistryContainerScanKey(container)
    if not container then
        return nil
    end
    if container.getID then
        local id = container:getID()
        if id ~= nil then
            return "id:" .. tostring(id)
        end
    end
    return tostring(container)
end

function BurdJournals.Server.touchPlayerJournalRegistryContainerItem(item, scanState)
    if not item or not item.getModData then return end
    local fullType = item.getFullType and item:getFullType() or nil
    if not fullType or not fullType:find("^BurdJournals%.") then return end
    local modData = item:getModData()
    local data = modData and modData.BurdJournals or nil
    if type(data) == "table" and data.isPlayerCreated == true and data.entryStoreEnabled ~= true
        and data.deferredBlankMaterialization ~= true and data.replacedByJournalId == nil and data.replacedByJournalUUID == nil
    then
        local uuid = BurdJournals.Server.touchPlayerJournalRegistry(item, nil, "containerUpdate", { skipTransmit = true })
        if uuid then scanState.touched = scanState.touched + 1 end
    end
end

function BurdJournals.Server.getPlayerJournalRegistryScanItems(container)
    if not (container and container.getItems) then return nil end
    -- OnContainerUpdate can leave a queued Java container reference stale after
    -- transfer/removal. This narrow probe guards only that documented lifetime
    -- boundary; scan mutations themselves remain unprotected and visible.
    local ok, items = pcall(container.getItems, container)
    return ok and items or nil
end

-- Keep this event-driven scan local to the updated container, but spread deep
-- inventories across ticks so one inventory change cannot hitch a server chunk.
function BurdJournals.Server.drainPlayerJournalRegistryContainerScanQueue(itemBudget)
    local remaining = math.max(1, tonumber(itemBudget) or BurdJournals.Server.PLAYER_JOURNAL_REGISTRY_CONTAINER_SCAN_ITEMS_PER_TICK)
    local scanQueue = BurdJournals.Server._playerJournalRegistryContainerScanQueue
    local scanOrder = BurdJournals.Server._playerJournalRegistryContainerScanOrder
    local nowMs = getTimestampMs and getTimestampMs() or 0
    while remaining > 0 and #scanOrder > 0 do
        local key = table.remove(scanOrder, 1)
        local scanState = scanQueue[key]
        local rootItems = scanState and BurdJournals.Server.getPlayerJournalRegistryScanItems(scanState.container) or nil
        local expired = not scanState or rootItems == nil
            or (nowMs > 0 and (nowMs - (tonumber(scanState.queuedAt) or nowMs)) > BurdJournals.Server.PLAYER_JOURNAL_REGISTRY_CONTAINER_SCAN_TTL_MS)
        if expired then
            scanQueue[key] = nil
        else
            -- Process one item, then rotate this container behind every other
            -- queued container. A single deep inventory cannot monopolize a tick.
            local consumed = false
            while not consumed and type(scanState.stack) == "table" and #scanState.stack > 0 do
            local frame = scanState.stack[#scanState.stack]
            local items = BurdJournals.Server.getPlayerJournalRegistryScanItems(frame.container)
            if items == nil then
                scanQueue[key] = nil
                scanState.stack = {}
                break
            end
            local size = items and items:size() or 0
            if frame.index >= size then
                table.remove(scanState.stack)
            else
                local item = items:get(frame.index)
                frame.index = frame.index + 1
                remaining = remaining - 1
                consumed = true
                BurdJournals.Server.touchPlayerJournalRegistryContainerItem(item, scanState)
                local subInventory = item and item.getInventory and item:getInventory() or nil
                if subInventory and subInventory.getItems then
                    scanState.stack[#scanState.stack + 1] = { container = subInventory, index = 0 }
                end
            end
            end
            if type(scanState.stack) ~= "table" or #scanState.stack == 0 then
            if scanState.dirty == true then
                scanState.dirty = false
                scanState.restarts = (tonumber(scanState.restarts) or 0) + 1
                if scanState.restarts <= BurdJournals.Server.PLAYER_JOURNAL_REGISTRY_CONTAINER_SCAN_MAX_RESTARTS then
                    scanState.stack = { { container = scanState.container, index = 0 } }
                else
                    scanQueue[key] = nil
                end
            else
                scanQueue[key] = nil
                local nowMs = getTimestampMs and getTimestampMs() or 0
                if nowMs > 0 then playerJournalRegistryContainerScanLastAt[key] = nowMs end
                if scanState.touched > 0 then
                    BurdJournals.debugPrint("[BurdJournals] Updated player journal registry for " .. tostring(scanState.touched) .. " container item(s)")
                end
            end
            end
            if scanQueue[key] then
                scanOrder[#scanOrder + 1] = key
            end
        end
    end
end

function BurdJournals.Server.onPlayerJournalRegistryContainerUpdate(container)
    if isClient and isClient() and not (isServer and isServer()) then return end
    if not container or not container.getItems then return end
    if BurdJournals.isEnabled and not BurdJournals.isEnabled() then return end
    local nowMs = getTimestampMs and getTimestampMs() or 0
    cleanupPlayerJournalRegistryContainerScanCache(nowMs)
    local containerKey = getPlayerJournalRegistryContainerScanKey(container)
    if not containerKey then return end
    local scanQueue = BurdJournals.Server._playerJournalRegistryContainerScanQueue
    local scanOrder = BurdJournals.Server._playerJournalRegistryContainerScanOrder
    local queued = scanQueue[containerKey]
    if queued then queued.dirty = true return end
    local lastScanAt = playerJournalRegistryContainerScanLastAt[containerKey]
    if lastScanAt and nowMs > 0 and (nowMs - lastScanAt) < PLAYER_JOURNAL_REGISTRY_CONTAINER_SCAN_DEBOUNCE_MS then return end
    if #scanOrder >= BurdJournals.Server.PLAYER_JOURNAL_REGISTRY_CONTAINER_SCAN_MAX_QUEUED then
        local evictedKey = table.remove(scanOrder, 1)
        scanQueue[evictedKey] = nil
    end
    scanQueue[containerKey] = {
        container = container, stack = { { container = container, index = 0 } }, touched = 0, dirty = false,
        queuedAt = nowMs, restarts = 0,
    }
    scanOrder[#scanOrder + 1] = containerKey
end

if Events.OnContainerUpdate then
    Events.OnContainerUpdate.Add(BurdJournals.Server.onPlayerJournalRegistryContainerUpdate)
end

function BurdJournals.Server.onEntryStoreTransmitTick()
    if BurdJournals.Server.drainPlayerJournalRegistryContainerScanQueue then
        BurdJournals.Server.drainPlayerJournalRegistryContainerScanQueue()
    end
    if BurdJournals.flushPendingEntryStoreShardTransmits then
        BurdJournals.flushPendingEntryStoreShardTransmits(false)
    end
    if BurdJournals.flushPendingRuntimeShardTransmits then
        BurdJournals.flushPendingRuntimeShardTransmits(false)
    end
    if BurdJournals.flushPendingDRCacheTransmit then
        BurdJournals.flushPendingDRCacheTransmit(false)
    end
    if BurdJournals.Server.flushPendingBaselineTransmit then
        BurdJournals.Server.flushPendingBaselineTransmit(false)
    end
    if BurdJournals.Server.drainPendingEntryFileBackups then
        BurdJournals.Server.drainPendingEntryFileBackups(1)
    end
end

if Events.OnTick then
    Events.OnTick.Add(BurdJournals.Server.onEntryStoreTransmitTick)
end

-- Register for ModData initialization to ensure baseline cache is properly loaded
if Events.OnInitGlobalModData then
    Events.OnInitGlobalModData.Add(BurdJournals.Server.onInitGlobalModData)
    BurdJournals.debugPrint("[BurdJournals] OnInitGlobalModData handler registered")
else
    bsjWriteLogLine("[BurdJournals] WARNING: OnInitGlobalModData event not available")
end

BurdJournals.debugPrint("[BurdJournals] Server module fully loaded!")
